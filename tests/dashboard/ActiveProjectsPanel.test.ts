// Implementation is in ActiveProjectsPanel.test.tsx (JSX required for RTL rendering).
// This file is intentionally kept to preserve the stub file created in Wave 0.
import { describe, it } from "vitest";

describe("ActiveProjectsPanel (stub)", () => {
  it.todo("see ActiveProjectsPanel.test.tsx for full PANEL-03 test suite");
});

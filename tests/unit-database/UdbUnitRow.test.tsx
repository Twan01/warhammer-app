import { describe, it } from "vitest";

describe("UdbUnitRow", () => {
  it.todo("renders unit name");
  it.todo("shows 'from N pts' when base_points is set");
  it.todo("shows dash when base_points is null");
  it.todo("renders role badge");
  it.todo("calls onOpen when clicked");
});

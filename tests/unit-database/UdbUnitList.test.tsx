import { describe, it, vi } from "vitest";

vi.mock("@tanstack/react-virtual");

describe("UdbUnitList", () => {
  it.todo("renders role group headers");
  it.todo("renders unit rows");
  it.todo("uses a single useVirtualizer instance");
  it.todo("shows loading state");
  it.todo("shows empty state when no units match");
});

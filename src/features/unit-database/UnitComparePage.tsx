/**
 * Phase 138-01 stub — replaced by the full implementation in Phase 138-02.
 *
 * This file exists solely so the lazy import in router.tsx resolves at build time.
 * The real UnitComparePage (with column rendering, diff highlight, action bar) is
 * implemented in plan 138-02.
 */
export function UnitComparePage() {
  return null;
}

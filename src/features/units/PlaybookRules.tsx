/**
 * Phase 107 -- PlaybookRules simplified.
 *
 * Stratagems, detachments, and shared abilities data was sourced from rules.db
 * (rulesExtended.ts). With rules.db eliminated, these features lose their data
 * source until EXT-03 adds this data to the canonical unit database.
 *
 * This component renders nothing until EXT-03 is implemented.
 */

export function PlaybookRules() {
  return null;
}

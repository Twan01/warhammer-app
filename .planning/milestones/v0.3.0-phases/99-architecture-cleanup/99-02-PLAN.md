---
phase: 99-architecture-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/units/PlaybookTab.tsx
  - src/features/units/PlaybookStats.tsx
  - src/features/units/PlaybookStrategy.tsx
  - src/features/units/PlaybookDatasheet.tsx
  - src/features/units/PlaybookSyncDetails.tsx
  - src/features/units/PlaybookRules.tsx
autonomous: true
requirements:
  - ARCH-02

must_haves:
  truths:
    - "PlaybookTab renders identically to before decomposition -- all existing PlaybookTab tests pass"
    - "Each new sub-component file is under 300 lines"
    - "PlaybookTab.tsx orchestrator is under 300 lines"
    - "Sub-components are pure presentation -- no React Query hooks in sub-components"
  artifacts:
    - path: "src/features/units/PlaybookStats.tsx"
      provides: "Stat overrides grid with edit mode toggle"
      exports: ["PlaybookStats"]
    - path: "src/features/units/PlaybookStrategy.tsx"
      provides: "Strategy note fields (battlefield role, strengths, weaknesses, synergies, notes)"
      exports: ["PlaybookStrategy"]
    - path: "src/features/units/PlaybookDatasheet.tsx"
      provides: "Datasheet link, abilities, wargear display, conflict handling"
      exports: ["PlaybookDatasheet"]
    - path: "src/features/units/PlaybookSyncDetails.tsx"
      provides: "Sync freshness indicator, version info"
      exports: ["PlaybookSyncDetails"]
    - path: "src/features/units/PlaybookRules.tsx"
      provides: "Stratagems, detachments, shared abilities with favorites/notes"
      exports: ["PlaybookRules"]
    - path: "src/features/units/PlaybookTab.tsx"
      provides: "Orchestrator: React Query hooks, save logic, renders sub-components"
  key_links:
    - from: "src/features/units/PlaybookTab.tsx"
      to: "src/features/units/PlaybookStats.tsx"
      via: "import + JSX render"
      pattern: "PlaybookStats"
    - from: "src/features/units/PlaybookTab.tsx"
      to: "src/features/units/PlaybookStrategy.tsx"
      via: "import + JSX render"
      pattern: "PlaybookStrategy"
    - from: "src/features/units/PlaybookTab.tsx"
      to: "src/features/units/PlaybookRules.tsx"
      via: "import + JSX render"
      pattern: "PlaybookRules"
---

<objective>
Decompose PlaybookTab.tsx from 1431 lines into 5 sub-components plus a slim orchestrator, each under 300 lines (ARCH-02).

Purpose: The 1431-line file is difficult to navigate and maintain. Decomposition into focused sub-components improves readability without changing any user-facing behavior.

Output: 5 new presentation components + slimmed PlaybookTab orchestrator. All existing PlaybookTab tests pass.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-architecture-cleanup/99-RESEARCH.md
@.planning/phases/99-architecture-cleanup/99-PATTERNS.md

<interfaces>
From src/features/units/LoadoutSection.tsx (analog for sub-component pattern):
  - Named function export with props interface
  - const SECTION_LABEL_CLASS for section headers
  - Imports from @/components/ui/ and @/types/

From src/components/ui/form.tsx (line 19):
  const Form = FormProvider  // shadcn Form wraps FormProvider

Existing extracted components (keep as-is per D-08):
  - LoadoutSection.tsx
  - DatasheetPicker.tsx
  - TierManager.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PlaybookStats and PlaybookSyncDetails (D-06, D-07)</name>
  <files>
    src/features/units/PlaybookStats.tsx,
    src/features/units/PlaybookSyncDetails.tsx,
    src/features/units/PlaybookTab.tsx
  </files>
  <read_first>
    src/features/units/PlaybookTab.tsx,
    src/features/units/LoadoutSection.tsx,
    tests/collection/PlaybookTab.test.tsx
  </read_first>
  <action>
Per D-06 and D-07: Extract two sub-components from PlaybookTab.tsx. Read the full PlaybookTab.tsx to identify exact line ranges for the stats grid section (~line 620-707) and sync details section (~line 708-933).

Create PlaybookStats.tsx: Extract the stat overrides grid (M, T, Sv, W, Ld, OC stat display with edit mode toggle and inline number inputs). Define a PlaybookStatsProps interface with grouped props: unitId, datasheet data, override values, override change handler, edit mode boolean, toggle callback, and sync meta. Follow the LoadoutSection.tsx pattern for named function export and section label styling. The component receives all data via props -- no React Query hook calls inside.

Create PlaybookSyncDetails.tsx: Extract the sync freshness indicator, version info, and error history display. Define PlaybookSyncDetailsProps with sync metadata, error counts, and freshness data. Import sync helpers from @/lib/syncFreshness and date helpers from @/lib/dates.

Update PlaybookTab.tsx: Replace the extracted JSX blocks with PlaybookStats and PlaybookSyncDetails component renders, passing the required props. PlaybookTab keeps all useState calls, React Query hooks (useStrategyNote, useDatasheet, etc.), and the handleSave function.

Do NOT touch the strategy notes section, datasheet/abilities section, or rules section yet -- those are Task 2.
  </action>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App" &amp;&amp; pnpm test -- tests/collection/PlaybookTab.test.tsx</automated>
    Then: pnpm build
  </verify>
  <acceptance_criteria>
    - src/features/units/PlaybookStats.tsx exists and exports PlaybookStats function
    - src/features/units/PlaybookSyncDetails.tsx exists and exports PlaybookSyncDetails function
    - PlaybookStats.tsx is under 300 lines (wc -l)
    - PlaybookSyncDetails.tsx is under 300 lines (wc -l)
    - Neither file contains useQuery, useMutation, or any React Query hook call
    - PlaybookTab.tsx imports and renders both new components
    - pnpm test -- tests/collection/PlaybookTab.test.tsx passes
    - pnpm build exits 0
  </acceptance_criteria>
  <done>PlaybookStats and PlaybookSyncDetails extracted as presentation components. PlaybookTab tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extract PlaybookStrategy, PlaybookDatasheet, PlaybookRules (D-06, D-07)</name>
  <files>
    src/features/units/PlaybookStrategy.tsx,
    src/features/units/PlaybookDatasheet.tsx,
    src/features/units/PlaybookRules.tsx,
    src/features/units/PlaybookTab.tsx
  </files>
  <read_first>
    src/features/units/PlaybookTab.tsx,
    src/features/units/PlaybookStats.tsx,
    src/features/units/LoadoutSection.tsx
  </read_first>
  <action>
Per D-06 and D-07: Extract the remaining three sections from PlaybookTab.tsx.

Create PlaybookStrategy.tsx: Extract the strategy note fields section (~line 1150+ in original). Define PlaybookStrategyProps with the note object, draft field values (battlefield_role, strengths, weaknesses, synergies, notes), field change handler, isDirty flag, onSave callback, and isSaving flag. The component renders textarea fields with labels and a conditional Save button when dirty.

Create PlaybookDatasheet.tsx: Extract the datasheet link, weapons table, abilities display, and wargear sections (~line 934+ in original). Define PlaybookDatasheetProps with unitId, datasheet data (FullDatasheet or null), weapons array, abilities array, and any conflict handling callbacks. This is the largest sub-component but must stay under 300 lines.

Create PlaybookRules.tsx: Extract the stratagems, detachments, and shared abilities section with favorites/notes (~line 1028+ in original). Define PlaybookRulesProps with the rules data arrays (stratagems, detachments, shared abilities), favorites state, notes state, and toggle/edit callbacks. Imports RuleAnnotationControls and RuleNoteEditor from @/features/rules-hub/.

Update PlaybookTab.tsx: Replace the remaining extracted JSX blocks with the three new component renders. After this task, PlaybookTab.tsx should be a slim orchestrator under 300 lines that:
- Declares all useState hooks
- Calls all React Query hooks
- Contains the handleSave logic
- Renders sub-components passing props down

Verify: wc -l on PlaybookTab.tsx is under 300. Each new file is under 300 lines.
  </action>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App" &amp;&amp; pnpm test -- tests/collection/PlaybookTab.test.tsx &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/features/units/PlaybookStrategy.tsx exists and exports PlaybookStrategy
    - src/features/units/PlaybookDatasheet.tsx exists and exports PlaybookDatasheet
    - src/features/units/PlaybookRules.tsx exists and exports PlaybookRules
    - Each new file is under 300 lines (wc -l)
    - PlaybookTab.tsx is under 300 lines (wc -l)
    - No sub-component file contains useQuery, useMutation, or React Query hooks
    - PlaybookTab.tsx is the only file with React Query hook calls
    - pnpm test -- tests/collection/PlaybookTab.test.tsx passes
    - pnpm build exits 0
  </acceptance_criteria>
  <done>PlaybookTab fully decomposed into 5 sub-components. Orchestrator under 300 lines. All tests pass. ARCH-02 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Pure component decomposition with no new inputs or data flows.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02 | N/A | All | accept | Pure UI refactoring -- no new attack surface |
</threat_model>

<verification>
1. pnpm test -- tests/collection/PlaybookTab.test.tsx passes (regression)
2. pnpm build passes (TypeScript compilation)
3. wc -l on PlaybookTab.tsx returns under 300
4. wc -l on each of the 5 new files returns under 300
5. grep -c "useQuery\|useMutation" on each sub-component returns 0
</verification>

<success_criteria>
- PlaybookTab.tsx decomposed from 1431 lines to under 300 lines
- 5 new sub-component files, each under 300 lines
- All existing PlaybookTab tests pass unchanged
- Sub-components are pure presentation (no React Query hooks)
- Existing LoadoutSection, DatasheetPicker, TierManager untouched (D-08)
</success_criteria>

<output>
Create `.planning/phases/99-architecture-cleanup/99-02-SUMMARY.md` when done
</output>

---
phase: 97-error-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/common/RouteErrorFallback.tsx
  - src/app/router.tsx
  - tests/error-resilience/RouteErrorFallback.test.tsx
  - tests/error-resilience/routerErrorComponents.test.ts
autonomous: true
requirements:
  - ERR-01
  - ERR-02

must_haves:
  truths:
    - "D-01: When any component under layoutRoute throws during render, a styled Card fallback appears with 'Something went wrong' heading, Reload Page button, and Go to Dashboard link — per-section isolation on layoutRoute and bareLayoutRoute"
    - "D-02: TanStack Router errorComponent prop is set on both layoutRoute and bareLayoutRoute — no manual class-component wrapping"
    - "D-06: Error fallback is a styled shadcn Card with error icon, heading, error message (dev only), Reload Page button, and Go to Dashboard link"
    - "D-07: In dev mode, the raw error message and stack are shown; in production they are hidden"
  artifacts:
    - path: "src/components/common/RouteErrorFallback.tsx"
      provides: "Shared error fallback component for layout routes"
      exports: ["RouteErrorFallback"]
    - path: "src/app/router.tsx"
      provides: "Updated layout routes with errorComponent prop"
      contains: "errorComponent: RouteErrorFallback"
    - path: "tests/error-resilience/RouteErrorFallback.test.tsx"
      provides: "Unit tests for error fallback rendering and behavior"
    - path: "tests/error-resilience/routerErrorComponents.test.ts"
      provides: "Unit tests verifying both layout routes have errorComponent set"
  key_links:
    - from: "src/app/router.tsx"
      to: "src/components/common/RouteErrorFallback.tsx"
      via: "errorComponent prop on layoutRoute and bareLayoutRoute"
      pattern: "errorComponent:\\s*RouteErrorFallback"
---

<objective>
Add route-level error boundaries to the app so any component crash during render shows a styled fallback UI instead of a blank screen, with per-section isolation between the standard app shell and painting mode.

Purpose: Implements ERR-01 (fallback UI on render crash) and ERR-02 (per-route error isolation) using TanStack Router's built-in errorComponent prop per D-01 and D-02.
Output: RouteErrorFallback component, updated router.tsx with errorComponent on both layout routes, and unit tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-error-resilience/97-RESEARCH.md
@.planning/phases/97-error-resilience/97-PATTERNS.md

<interfaces>
<!-- ErrorComponentProps from @tanstack/router-core (verified in installed types) -->
From @tanstack/router-core:
```typescript
export type ErrorComponentProps<TError = Error> = {
  error: TError;
  info?: { componentStack: string };
  reset: () => void;
};
```

From src/app/router.tsx (current layout routes, lines 47-74):
```typescript
const layoutRoute = createRoute({
  getParentRoute: () => rootRoute,
  id: "layout",
  component: () => (
    <AppLayout><ActiveFactionProvider><Outlet /></ActiveFactionProvider></AppLayout>
  ),
});

const bareLayoutRoute = createRoute({
  getParentRoute: () => rootRoute,
  id: "bare-layout",
  component: () => (
    <ActiveFactionProvider>
      <TooltipProvider delayDuration={200}><Outlet /><Toaster richColors position="bottom-right" /></TooltipProvider>
    </ActiveFactionProvider>
  ),
});
```

From src/components/ui/card.tsx (shadcn exports):
Card, CardContent, CardHeader, CardTitle

From src/components/ui/button.tsx (shadcn exports):
Button (with variant="outline" for secondary actions, asChild for Link wrapping)

Existing dev-only guard pattern (router.tsx line 38, QueryProvider.tsx line 31):
{import.meta.env.DEV && <Component />}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RouteErrorFallback component and wire to router layout routes</name>
  <files>src/components/common/RouteErrorFallback.tsx, src/app/router.tsx</files>
  <read_first>
    - src/app/router.tsx (current layout route definitions, lines 47-74)
    - src/components/common/PlaceholderPage.tsx (centering/layout pattern analog)
    - .planning/phases/97-error-resilience/97-PATTERNS.md (RouteErrorFallback pattern assignment)
  </read_first>
  <action>
    Create src/components/common/RouteErrorFallback.tsx per D-06 and D-07:
    - Named export function RouteErrorFallback receiving ErrorComponentProps from @tanstack/react-router (not router-core — re-exported)
    - Outer div: flex min-h-screen items-center justify-center p-6
    - shadcn Card (max-w-lg w-full) with CardHeader containing AlertTriangle icon (h-5 w-5, text-destructive) and CardTitle "Something went wrong"
    - CardContent with space-y-4: conditionally show error.message and error.stack in a pre element when import.meta.env.DEV is true (per D-07)
    - Two buttons: Button onClick={reset} with text "Reload Page", and Button variant="outline" asChild wrapping Link to="/" with text "Go to Dashboard"
    - Import Link from @tanstack/react-router, AlertTriangle from lucide-react, Card/CardContent/CardHeader/CardTitle from @/components/ui/card, Button from @/components/ui/button

    Modify src/app/router.tsx per D-01 and D-02:
    - Add import: import { RouteErrorFallback } from "@/components/common/RouteErrorFallback"
    - Add errorComponent: RouteErrorFallback to layoutRoute createRoute options (after id: "layout")
    - Add errorComponent: RouteErrorFallback to bareLayoutRoute createRoute options (after id: "bare-layout")
    - Export layoutRoute and bareLayoutRoute (change from const to export const) so tests can assert errorComponent is set
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/common/RouteErrorFallback.tsx exists with "export function RouteErrorFallback"
    - RouteErrorFallback accepts ({ error, reset }: ErrorComponentProps)
    - Contains text "Something went wrong" in a CardTitle
    - Contains import.meta.env.DEV guard around error.message display
    - Contains Button with onClick={reset} and text "Reload Page"
    - Contains Link to="/" with text "Go to Dashboard"
    - src/app/router.tsx contains errorComponent: RouteErrorFallback on layoutRoute
    - src/app/router.tsx contains errorComponent: RouteErrorFallback on bareLayoutRoute
    - src/app/router.tsx exports layoutRoute and bareLayoutRoute
    - pnpm build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>RouteErrorFallback component renders styled fallback UI; both layout routes have errorComponent wired; exports available for testing</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for RouteErrorFallback and router errorComponent wiring</name>
  <files>tests/error-resilience/RouteErrorFallback.test.tsx, tests/error-resilience/routerErrorComponents.test.ts</files>
  <read_first>
    - src/components/common/RouteErrorFallback.tsx (just created — verify component structure)
    - src/app/router.tsx (verify exports of layoutRoute and bareLayoutRoute)
    - tests/collection/PaintingRing.test.tsx (component render + assertion analog)
    - tests/app-shell/QueryProvider.test.tsx (import-and-assert analog)
    - .planning/phases/97-error-resilience/97-PATTERNS.md (test pattern assignments)
  </read_first>
  <action>
    Create tests/error-resilience/RouteErrorFallback.test.tsx:
    - Test "renders Something went wrong heading": render RouteErrorFallback with error=new Error("test") and reset=vi.fn(), assert screen.getByText("Something went wrong") is in the document
    - Test "shows error message in DEV mode": use vi.stubEnv("DEV", "true") (or rely on default Vitest DEV=true), render with error=new Error("test error msg"), assert screen.getByText(/test error msg/) is in the document
    - Test "hides error message in PROD mode": use vi.stubEnv("DEV", ""), render with error=new Error("secret"), assert screen.queryByText(/secret/) is null
    - Test "calls reset when Reload Page clicked": render with mockReset=vi.fn(), click "Reload Page" button via userEvent or fireEvent, assert mockReset called once
    - Test "renders Go to Dashboard link": assert screen.getByText("Go to Dashboard") is in the document
    - RouteErrorFallback uses Link from @tanstack/react-router — wrap renders in a minimal router context. Use createMemoryHistory + createRootRoute + createRouter + RouterProvider pattern from tests/app-shell/AppSidebar.test.tsx

    Create tests/error-resilience/routerErrorComponents.test.ts:
    - Import layoutRoute and bareLayoutRoute from @/app/router
    - Test "layoutRoute has errorComponent set": assert layoutRoute.options.errorComponent is defined and equals RouteErrorFallback
    - Test "bareLayoutRoute has errorComponent set": assert bareLayoutRoute.options.errorComponent is defined and equals RouteErrorFallback
    - Import RouteErrorFallback from @/components/common/RouteErrorFallback for identity comparison
  </action>
  <verify>
    <automated>pnpm test -- tests/error-resilience/</automated>
  </verify>
  <acceptance_criteria>
    - tests/error-resilience/RouteErrorFallback.test.tsx exists and all tests pass
    - tests/error-resilience/routerErrorComponents.test.ts exists and all tests pass
    - At least 5 test cases in RouteErrorFallback.test.tsx covering: heading text, DEV error display, PROD error hiding, reset callback, dashboard link
    - At least 2 test cases in routerErrorComponents.test.ts covering: layoutRoute errorComponent, bareLayoutRoute errorComponent
    - pnpm test -- tests/error-resilience/ exits 0
  </acceptance_criteria>
  <done>All ERR-01 and ERR-02 behaviors are covered by passing unit tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Error message display | Error messages may contain file paths or internal details; must be hidden in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Information Disclosure | RouteErrorFallback | mitigate | import.meta.env.DEV guard hides error.message and error.stack in production builds (D-07) |
</threat_model>

<verification>
- pnpm build succeeds (TypeScript + Vite)
- pnpm test -- tests/error-resilience/ passes all tests
- RouteErrorFallback renders styled Card with heading, buttons, and conditional error details
- Both layoutRoute and bareLayoutRoute have errorComponent set
</verification>

<success_criteria>
- A component throw under layoutRoute renders the styled fallback, not a blank screen
- A component throw under bareLayoutRoute renders the same fallback
- Error details visible in dev, hidden in production
- Reload Page calls reset(), Go to Dashboard navigates to /
- All unit tests pass
</success_criteria>

<output>
Create `.planning/phases/97-error-resilience/97-01-SUMMARY.md` when done
</output>

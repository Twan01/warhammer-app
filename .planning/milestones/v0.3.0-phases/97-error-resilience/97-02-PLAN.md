---
phase: 97-error-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/common/DbHealthGate.tsx
  - src/components/common/DbDiagnosticScreen.tsx
  - src/main.tsx
  - src/components/common/QueryProvider.tsx
  - tests/error-resilience/DbHealthGate.test.tsx
  - tests/error-resilience/QueryProviderGlobalError.test.tsx
  - tests/error-resilience/globalErrorHandlers.test.ts
autonomous: true
requirements:
  - ERR-03
  - ERR-04

must_haves:
  truths:
    - "D-03: On startup, SELECT 1 + PRAGMA user_version health check runs before rendering the main router"
    - "D-04: If the database connection fails at startup, a diagnostic screen appears with the error message, a Retry button, and troubleshooting guidance — no auto-repair"
    - "D-05: DbHealthGate is a wrapper component in main.tsx using useState/useEffect that gates RouterProvider behind the health check"
    - "If the schema version is below 32 at startup, the diagnostic screen appears with a version mismatch message"
    - "Clicking Retry on the diagnostic screen re-runs the health check and renders the app if it passes"
    - "D-08: window.onerror and window.onunhandledrejection handlers in main.tsx log structured context to console.error with timestamp, error type, message, stack"
    - "D-09: QueryCache and MutationCache onError handlers log failed queries/mutations with their query key to console.error"
  artifacts:
    - path: "src/components/common/DbHealthGate.tsx"
      provides: "Startup DB health check wrapper gating the entire app"
      exports: ["DbHealthGate"]
    - path: "src/components/common/DbDiagnosticScreen.tsx"
      provides: "Diagnostic fallback UI shown when DB health check fails"
      exports: ["DbDiagnosticScreen"]
    - path: "src/main.tsx"
      provides: "Updated entry point with window error handlers and DbHealthGate wrapping"
      contains: "DbHealthGate"
    - path: "src/components/common/QueryProvider.tsx"
      provides: "Updated QueryClient with QueryCache and MutationCache onError handlers"
      contains: "QueryCache"
    - path: "tests/error-resilience/DbHealthGate.test.tsx"
      provides: "Unit tests for DB health gate pass/fail/retry behavior"
    - path: "tests/error-resilience/QueryProviderGlobalError.test.tsx"
      provides: "Unit tests for QueryCache and MutationCache onError wiring"
    - path: "tests/error-resilience/globalErrorHandlers.test.ts"
      provides: "Unit tests for window.onerror and window.onunhandledrejection handlers"
  key_links:
    - from: "src/main.tsx"
      to: "src/components/common/DbHealthGate.tsx"
      via: "DbHealthGate wraps QueryProvider > RouterProvider"
      pattern: "<DbHealthGate>"
    - from: "src/components/common/DbHealthGate.tsx"
      to: "src/db/client.ts"
      via: "getDb() for health check query"
      pattern: "getDb"
    - from: "src/components/common/QueryProvider.tsx"
      to: "@tanstack/react-query QueryCache"
      via: "QueryClient constructor with queryCache and mutationCache"
      pattern: "new QueryCache"
---

<objective>
Add a startup DB health gate that blocks the app from rendering if the database is unreachable or schema is outdated, and wire global error capture for unhandled exceptions, promise rejections, and React Query failures.

Purpose: Implements ERR-03 (DB health check gate) and ERR-04 (global error capture) per D-03 through D-09.
Output: DbHealthGate and DbDiagnosticScreen components, updated main.tsx with window handlers and gate, updated QueryProvider with QueryCache/MutationCache onError, and unit tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-error-resilience/97-RESEARCH.md
@.planning/phases/97-error-resilience/97-PATTERNS.md

<interfaces>
From src/main.tsx (current, lines 1-17):
```typescript
import React from "react";
import ReactDOM from "react-dom/client";
import { RouterProvider } from "@tanstack/react-router";
import { QueryProvider } from "@/components/common/QueryProvider";
import { QuickAddProvider } from "@/context/QuickAddContext";
import { router } from "@/app/router";
import "./styles/globals.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <QueryProvider>
      <QuickAddProvider>
        <RouterProvider router={router} />
      </QuickAddProvider>
    </QueryProvider>
  </React.StrictMode>
);
```

From src/components/common/QueryProvider.tsx (current, lines 1-33):
```typescript
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { ReactQueryDevtools } from "@tanstack/react-query-devtools";
import { useState, type ReactNode } from "react";

export function QueryProvider({ children }: { children: ReactNode }) {
  const [queryClient] = useState(() => new QueryClient({
    defaultOptions: {
      queries: { staleTime: 5min, gcTime: 10min, refetchOnWindowFocus: false, retry: 1 },
    },
  }));
  return (
    <QueryClientProvider client={queryClient}>
      {children}
      {import.meta.env.DEV && <ReactQueryDevtools initialIsOpen={false} />}
    </QueryClientProvider>
  );
}
```

From src/db/client.ts (getDb singleton pattern):
```typescript
export async function getDb(): Promise<Database>
// Resets _dbPromise to null on failure — safe to retry
```

From src/db/queries/diagnostics.ts (extractVersion pattern, lines 84-90):
```typescript
const extractVersion = (row: Record<string, number> | undefined): number => {
  if (!row) return 0;
  if (typeof row.user_version === "number") return row.user_version;
  const values = Object.values(row);
  return typeof values[0] === "number" ? values[0] : 0;
};
```

From @tanstack/query-core (verified installed types):
```typescript
QueryCacheConfig.onError: (error: DefaultError, query: Query) => void;
MutationCacheConfig.onError: (error: DefaultError, variables: unknown, onMutateResult: unknown, mutation: Mutation, context: MutationFunctionContext) => Promise<unknown> | unknown;
```

shadcn components available: Card, CardContent, CardHeader, CardTitle, Button, Alert, AlertDescription
lucide-react icons available: AlertTriangle, Database, RefreshCw
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DbHealthGate, DbDiagnosticScreen, wire main.tsx and QueryProvider.tsx</name>
  <files>src/components/common/DbHealthGate.tsx, src/components/common/DbDiagnosticScreen.tsx, src/main.tsx, src/components/common/QueryProvider.tsx</files>
  <read_first>
    - src/main.tsx (current entry point — must preserve all existing imports and structure)
    - src/components/common/QueryProvider.tsx (current QueryClient construction — must preserve defaultOptions)
    - src/db/client.ts (getDb singleton — understand the catch-and-reset pattern)
    - src/db/queries/diagnostics.ts (lines 76-96 — extractVersion function to copy)
    - src/components/common/PlaceholderPage.tsx (centering layout pattern)
    - .planning/phases/97-error-resilience/97-PATTERNS.md (all pattern assignments)
  </read_first>
  <action>
    Create src/components/common/DbDiagnosticScreen.tsx per D-04:
    - Named export DbDiagnosticScreen with props { error: string | null; onRetry: () => void }
    - Centered layout (flex min-h-screen items-center justify-center p-6)
    - shadcn Card (max-w-lg w-full) with CardHeader: Database icon (lucide-react) + CardTitle "Database Connection Error"
    - CardContent with space-y-4: Alert (variant destructive) showing the error message, paragraph with troubleshooting guidance ("Try restarting the application. If the problem persists, your database file may be corrupted."), Button onClick={onRetry} with RefreshCw icon and text "Retry Connection"
    - No auto-repair per D-04

    Create src/components/common/DbHealthGate.tsx per D-03 and D-05:
    - Named export const EXPECTED_SCHEMA_VERSION = 32 (matches 32 migration files 001-032)
    - Copy extractVersion helper from diagnostics.ts verbatim (handles driver column name variance)
    - Type HealthState = "checking" | "ok" | "failed"
    - Named export function DbHealthGate({ children }: { children: ReactNode })
    - useState for state (HealthState, initial "checking") and error (string | null)
    - useCallback runCheck: call getDb(), then db.select("SELECT 1"), then db.select("PRAGMA user_version"), extract version with extractVersion, throw if version < EXPECTED_SCHEMA_VERSION with descriptive message, setState("ok") on success, catch sets error and setState("failed")
    - useEffect calls runCheck on mount
    - Render: "checking" returns null (completes before next paint), "failed" returns DbDiagnosticScreen with error and onRetry=runCheck, "ok" returns children fragment

    Modify src/main.tsx per D-05 and D-08:
    - Add import for DbHealthGate from @/components/common/DbHealthGate
    - Before ReactDOM.createRoot, register window.onerror handler that calls console.error("[GlobalError]", { timestamp: new Date().toISOString(), type: "uncaught", message, source, location: lineno+":"+colno, stack: error?.stack })
    - Before ReactDOM.createRoot, register window.onunhandledrejection handler that calls console.error("[UnhandledRejection]", { timestamp: new Date().toISOString(), type: "unhandledRejection", reason: (event.reason instanceof Error ? event.reason.message : String(event.reason)), stack: (event.reason instanceof Error ? event.reason.stack : undefined) })
    - Wrap QueryProvider inside DbHealthGate in the render tree: StrictMode > DbHealthGate > QueryProvider > QuickAddProvider > RouterProvider

    Modify src/components/common/QueryProvider.tsx per D-09:
    - Add QueryCache and MutationCache to the import from @tanstack/react-query
    - Add queryCache: new QueryCache({ onError: (error, query) => console.error("[ReactQuery] Query failed:", { timestamp: new Date().toISOString(), queryKey: query.queryKey, error: error.message }) }) to QueryClient constructor
    - Add mutationCache: new MutationCache({ onError: (error, _variables, _context, mutation) => console.error("[ReactQuery] Mutation failed:", { timestamp: new Date().toISOString(), mutationKey: mutation.options.mutationKey, error: error.message }) }) to QueryClient constructor
    - Preserve all existing defaultOptions unchanged
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/common/DbHealthGate.tsx exists with "export function DbHealthGate" and "export const EXPECTED_SCHEMA_VERSION = 32"
    - DbHealthGate imports getDb from @/db/client
    - DbHealthGate contains extractVersion helper matching diagnostics.ts pattern
    - DbHealthGate renders null for "checking", DbDiagnosticScreen for "failed", children for "ok"
    - src/components/common/DbDiagnosticScreen.tsx exists with "export function DbDiagnosticScreen"
    - DbDiagnosticScreen contains "Database Connection Error" heading, Retry button, troubleshooting guidance
    - src/main.tsx contains window.onerror handler with console.error("[GlobalError]", ...)
    - src/main.tsx contains window.onunhandledrejection handler with console.error("[UnhandledRejection]", ...)
    - src/main.tsx render tree is StrictMode > DbHealthGate > QueryProvider > QuickAddProvider > RouterProvider
    - src/components/common/QueryProvider.tsx contains new QueryCache({ onError: ... })
    - src/components/common/QueryProvider.tsx contains new MutationCache({ onError: ... })
    - QueryProvider preserves staleTime, gcTime, refetchOnWindowFocus, retry defaults unchanged
    - pnpm build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>DB health gate blocks app on connection/schema failure; diagnostic screen offers retry; global error handlers capture uncaught errors and React Query failures to console</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for DbHealthGate, global error handlers, and QueryProvider error capture</name>
  <files>tests/error-resilience/DbHealthGate.test.tsx, tests/error-resilience/QueryProviderGlobalError.test.tsx, tests/error-resilience/globalErrorHandlers.test.ts</files>
  <read_first>
    - src/components/common/DbHealthGate.tsx (just created — verify exports and state logic)
    - src/components/common/DbDiagnosticScreen.tsx (just created — verify props interface)
    - src/components/common/QueryProvider.tsx (just modified — verify QueryCache/MutationCache wiring)
    - src/main.tsx (just modified — verify window handler registration)
    - tests/data-health/diagnosticFlags.test.ts (getDb mock pattern analog)
    - tests/app-shell/QueryProvider.test.tsx (Inspector component pattern analog)
    - .planning/phases/97-error-resilience/97-PATTERNS.md (test pattern assignments)
  </read_first>
  <action>
    Create tests/error-resilience/DbHealthGate.test.tsx:
    - Mock @/db/client with vi.mock: getDb returns Promise.resolve({ select: mockSelect })
    - Test "renders children when health check passes": mockSelect returns [{ user_version: 32 }] for PRAGMA user_version and [{ "1": 1 }] for SELECT 1, render DbHealthGate wrapping a span with data-testid="child", assert findByTestId("child") resolves
    - Test "renders DbDiagnosticScreen when getDb throws": mock getDb to reject with Error("Connection failed"), render DbHealthGate, assert findByText(/Connection failed/) resolves
    - Test "renders DbDiagnosticScreen when user_version < 32": mockSelect returns [{ user_version: 20 }] for PRAGMA, render DbHealthGate, assert findByText(/Schema version mismatch/) resolves
    - Test "Retry button re-runs health check": mock getDb to fail first, then succeed on second call, render DbHealthGate, wait for diagnostic screen, click Retry button, assert child eventually appears
    - Use waitFor or findBy* for async state transitions
    - Import EXPECTED_SCHEMA_VERSION from DbHealthGate for assertion accuracy

    Create tests/error-resilience/QueryProviderGlobalError.test.tsx:
    - Use Inspector component pattern from tests/app-shell/QueryProvider.test.tsx: render QueryProvider with a child that calls useQueryClient(), extract queryClient
    - Test "QueryClient has QueryCache with onError": access queryClient.getQueryCache(), assert it exists, spy on console.error, simulate cache onError by calling queryClient.getQueryCache().config.onError with a mock error and query, assert console.error was called with "[ReactQuery] Query failed:" and an object containing queryKey and timestamp
    - Test "QueryClient has MutationCache with onError": same approach with getMutationCache(), simulate onError, assert console.error called with "[ReactQuery] Mutation failed:"

    Create tests/error-resilience/globalErrorHandlers.test.ts:
    - Test window.onerror: directly assign window.onerror in the test to the handler function signature, then call it with ("test msg", "test.ts", 1, 1, new Error("test")), spy on console.error, assert called with "[GlobalError]" and object containing message: "test msg", timestamp matching ISO format. The handler is registered in main.tsx before createRoot — test it by extracting the handler logic into a named function in main.tsx (e.g., handleGlobalError) and exporting it for test access, or by directly testing window.onerror after importing the module.
    - Test window.onunhandledrejection: call window.onunhandledrejection with a mock PromiseRejectionEvent ({ reason: new Error("test reject") }), assert console.error called with "[UnhandledRejection]" and object containing reason
  </action>
  <verify>
    <automated>pnpm test -- tests/error-resilience/</automated>
  </verify>
  <acceptance_criteria>
    - tests/error-resilience/DbHealthGate.test.tsx exists and all tests pass
    - At least 4 test cases: health pass renders children, getDb throw shows diagnostic, version mismatch shows diagnostic, retry re-runs check
    - tests/error-resilience/QueryProviderGlobalError.test.tsx exists and all tests pass
    - At least 2 test cases: QueryCache onError logs with queryKey, MutationCache onError logs with mutationKey
    - tests/error-resilience/globalErrorHandlers.test.ts exists and all tests pass
    - At least 2 test cases: window.onerror logs structured context, window.onunhandledrejection logs structured context
    - pnpm test -- tests/error-resilience/ exits 0
    - pnpm test exits 0 (full suite green)
  </acceptance_criteria>
  <done>All ERR-03 and ERR-04 behaviors are covered by passing unit tests; full test suite passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Error message display | DbDiagnosticScreen shows raw error messages; acceptable since this is a local desktop app with no remote users |
| Console logging | Error details logged to console; acceptable for local desktop app debugging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02 | Information Disclosure | DbDiagnosticScreen | accept | Local desktop app — error details shown only to the device owner; no network exposure |
| T-97-03 | Information Disclosure | Global error handlers | accept | Console-only logging on a local app; no remote transmission of error data |
</threat_model>

<verification>
- pnpm build succeeds (TypeScript + Vite)
- pnpm test -- tests/error-resilience/ passes all tests
- pnpm test passes (full suite)
- DbHealthGate blocks rendering when DB unavailable, shows diagnostic screen, retry works
- Window handlers capture uncaught errors and unhandled rejections to console
- QueryCache/MutationCache onError log failed queries/mutations with keys
</verification>

<success_criteria>
- DB connection failure at startup renders diagnostic screen with error and Retry button
- Schema version mismatch at startup renders diagnostic screen with version info
- Retry button re-runs health check and renders app on success
- window.onerror logs structured error context to console.error
- window.onunhandledrejection logs structured rejection context to console.error
- React Query query failures log with queryKey to console.error
- React Query mutation failures log with mutationKey to console.error
- All unit tests pass
</success_criteria>

<output>
Create `.planning/phases/97-error-resilience/97-02-SUMMARY.md` when done
</output>

---
phase: 98-performance-optimization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/queries/recipeAssignments.ts
  - src/hooks/useKanbanEnrichment.ts
  - src/hooks/useArmyLists.ts
  - tests/performance/kanbanBatchEnrichment.test.ts
  - tests/performance/invalidationAudit.test.ts
autonomous: true
requirements:
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "D-07: Kanban enrichment fetches assignment progress for all units in 1-2 DB round-trips, not 4N"
    - "D-08: Existing batched fetches for recipe names and photo counts kept unchanged"
    - "D-04: All 25 hook files audited per RESEARCH.md invalidation audit; changes only applied to useArmyLists.ts; remaining files confirmed correct with no pruning needed"
    - "D-05: Cross-domain invalidations that are genuinely needed (unit->dashboard-stats, completeStep->analytics) are preserved"
    - "D-06: exact:true used on single-entity invalidations to prevent broad prefix-based invalidation"
    - "Mutations that only affect a single army list detail (warlord, leader attachment) do not cause a broad refetch of the army lists index query"
  artifacts:
    - path: "src/db/queries/recipeAssignments.ts"
      provides: "getKanbanProgressByUnitIds batched query function"
      exports: ["getKanbanProgressByUnitIds", "KanbanProgressRow"]
    - path: "src/hooks/useKanbanEnrichment.ts"
      provides: "Refactored enrichment using batched query"
      contains: "getKanbanProgressByUnitIds"
    - path: "tests/performance/kanbanBatchEnrichment.test.ts"
      provides: "Tests for batched kanban enrichment query"
    - path: "tests/performance/invalidationAudit.test.ts"
      provides: "Tests verifying invalidation precision; includes header comment documenting full audit scope across all 25 hook files"
  key_links:
    - from: "src/hooks/useKanbanEnrichment.ts"
      to: "src/db/queries/recipeAssignments.ts"
      via: "getKanbanProgressByUnitIds(sortedIds)"
      pattern: "getKanbanProgressByUnitIds"
---

<objective>
Replace the O(N) per-unit Kanban enrichment loop with a batched SQL query, and audit/tighten React Query invalidation chains across hook files.

Purpose: Kanban board renders go from 4N DB round-trips to 2-3 total (PERF-03), and mutations stop triggering unnecessary refetches of unrelated queries (PERF-02).
Output: New batched query function, refactored enrichment hook, tightened invalidations, 2 test files.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-performance-optimization/98-CONTEXT.md
@.planning/phases/98-performance-optimization/98-RESEARCH.md
@.planning/phases/98-performance-optimization/98-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/recipeAssignment.ts:
export interface AppliedRecipeProgress {
  recipeName: string;
  completed: number;
  total: number;
  assignmentCount: number;
}

From src/hooks/useKanbanEnrichment.ts:
export interface KanbanEnrichment {
  recipeNames: Map<number, string>;
  photoCounts: Map<number, number>;
  appliedProgress: Map<number, AppliedRecipeProgress>;
  assignmentIds: Map<number, number>;
}

From src/db/queries/unitPhotos.ts (batched IN-clause analog):
export async function getPhotoCountsByUnitIds(unitIds: number[]): Promise<{ entity_id: number; photo_count: number }[]>

From src/hooks/useArmyLists.ts:
export const ARMY_LISTS_KEY = ["army-lists"] as const;
export const ARMY_LIST_KEY = (id: number) => ["army-lists", id] as const;
export const ARMY_LIST_UNITS_KEY = (id: number) => ["army-list-units", id] as const;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batched Kanban enrichment SQL query function</name>
  <files>src/db/queries/recipeAssignments.ts, tests/performance/kanbanBatchEnrichment.test.ts</files>
  <read_first>
    - src/db/queries/recipeAssignments.ts (existing exports: getAssignmentsByUnit, getStepProgress -- needed to understand what the new function replaces)
    - src/db/queries/unitPhotos.ts (analog: getPhotoCountsByUnitIds -- IN-clause parameterization pattern)
    - src/db/queries/recipes.ts (getRecipeNamesByUnitIds -- another batched analog)
    - .planning/phases/98-performance-optimization/98-PATTERNS.md (Pattern 4: Batched Kanban Enrichment Query with CTE, hook refactor pattern)
    - .planning/phases/98-performance-optimization/98-RESEARCH.md (Open Questions section -- CTE vs subquery approaches)
  </read_first>
  <action>
    Per D-07, create a new batched query function in recipeAssignments.ts. This task creates ONLY the SQL function and its tests -- the hook refactor is Task 2.

    **Add getKanbanProgressByUnitIds to recipeAssignments.ts**

    Export a new interface `KanbanProgressRow` with fields: unit_id (number), assignment_id (number), assignment_count (number), recipe_id (number), recipe_name (string), total_steps (number), completed_steps (number).

    Export `async function getKanbanProgressByUnitIds(unitIds: number[]): Promise<KanbanProgressRow[]>` that:
    - Returns `[]` if unitIds is empty (guard clause)
    - Gets db via `getDb()`
    - Builds IN-clause with `unitIds.map((_, i) => "$" + (i + 1)).join(", ")`
    - **Primary approach (CTE with ROW_NUMBER):** Uses a CTE with ROW_NUMBER() OVER (PARTITION BY unit_id ORDER BY created_at DESC) to find the most-recent assignment per unit, plus COUNT(*) OVER (PARTITION BY unit_id) for total_assignments. JOINs painting_recipes (for name), LEFT JOINs recipe_steps and unit_recipe_step_progress (for total/completed steps). Filters WHERE rn = 1, GROUP BY unit_id, id, recipe_id, name, total_assignments. Passes `unitIds` as params array (single IN-clause occurrence in the CTE, single param set).
    - **Fallback approach (subquery):** If ROW_NUMBER is not available at runtime (unlikely but possible), use the subquery approach from 98-RESEARCH.md Pattern 4 instead: a subquery with MAX(created_at) per unit_id to find the primary assignment, and a separate COUNT subquery for total_assignments. Both approaches produce the same KanbanProgressRow shape.
    - Try the CTE approach first. If it fails during testing (SQL error from window functions), switch to the subquery approach.

    Follow the SQL structure from 98-PATTERNS.md Pattern 4.

    **Create tests/performance/kanbanBatchEnrichment.test.ts**

    Mock `getDb` to return `{ select: selectMock }`. Test:
    - `getKanbanProgressByUnitIds([1, 2])` calls select once with SQL containing `IN ($1, $2)` and params `[1, 2]`
    - `getKanbanProgressByUnitIds([])` returns empty array without calling select (guard clause)
    - SQL contains `unit_recipe_assignments` table reference
  </action>
  <verify>
    <automated>pnpm test -- tests/performance/kanbanBatchEnrichment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - recipeAssignments.ts exports `KanbanProgressRow` interface and `getKanbanProgressByUnitIds` function
    - getKanbanProgressByUnitIds contains EITHER `ROW_NUMBER() OVER (PARTITION BY unit_id` (CTE approach) OR `MAX(created_at)` subquery (fallback approach) in its SQL
    - getKanbanProgressByUnitIds returns `[]` for empty unitIds without calling db.select
    - tests/performance/kanbanBatchEnrichment.test.ts passes
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>getKanbanProgressByUnitIds batched SQL query function exists in recipeAssignments.ts with empty-guard and IN-clause parameterization, verified by tests</done>
</task>

<task type="auto">
  <name>Task 2: Refactor useKanbanEnrichment hook to use batched query</name>
  <files>src/hooks/useKanbanEnrichment.ts</files>
  <read_first>
    - src/hooks/useKanbanEnrichment.ts (current O(N) loop at lines 37-56, imports, return shape)
    - src/db/queries/recipeAssignments.ts (the new getKanbanProgressByUnitIds from Task 1)
  </read_first>
  <action>
    Per D-07 and D-08, refactor the enrichment hook to use the batched query from Task 1.

    Replace the Promise.all + sortedIds.map loop (lines 37-56) with:
    1. Call `getKanbanProgressByUnitIds(sortedIds)` -- single batched query
    2. Iterate the returned rows to populate `appliedProgressMap` and `assignmentIdsMap` with the same shape as before

    Remove now-unused imports: `getAssignmentsByUnit`, `getStepProgress` from recipeAssignments, `getRecipePaintsByRecipe` from recipePaints, `computeAssignmentProgress` from lib, `getRecipeById` from recipes.

    Add new import: `getKanbanProgressByUnitIds` from `@/db/queries/recipeAssignments`.

    Keep existing batched calls for recipeRows and photoRows (lines 30-33) per D-08.

    The KanbanEnrichment return shape (recipeNames, photoCounts, appliedProgress, assignmentIds) must remain identical.
  </action>
  <verify>
    <automated>pnpm test -- tests/performance/kanbanBatchEnrichment.test.ts && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - useKanbanEnrichment.ts no longer imports getAssignmentsByUnit, getStepProgress, getRecipePaintsByRecipe, computeAssignmentProgress, or getRecipeById
    - useKanbanEnrichment.ts imports getKanbanProgressByUnitIds from recipeAssignments
    - useKanbanEnrichment.ts no longer contains `sortedIds.map(async (unitId)` loop
    - The return type KanbanEnrichment is unchanged (recipeNames, photoCounts, appliedProgress, assignmentIds Maps)
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>Kanban enrichment uses 1 batched SQL query via getKanbanProgressByUnitIds instead of 4N per-unit queries</done>
</task>

<task type="auto">
  <name>Task 3: Invalidation precision audit on useArmyLists.ts</name>
  <files>src/hooks/useArmyLists.ts, tests/performance/invalidationAudit.test.ts</files>
  <read_first>
    - src/hooks/useArmyLists.ts (all 76 invalidateQueries calls -- identify mutations that invalidate ARMY_LISTS_KEY broad when ARMY_LIST_KEY(id) already covers the specific list)
    - src/hooks/useUnits.ts (reference: well-reasoned invalidation chains per RESEARCH audit -- no changes needed)
    - src/hooks/useRecipeAssignments.ts (reference: already well-targeted per RESEARCH audit -- no changes needed)
    - .planning/phases/98-performance-optimization/98-RESEARCH.md (Invalidation Audit Findings section -- specific candidates for exact:true and broad removal)
  </read_first>
  <action>
    Per D-04, D-05, and D-06, audit and tighten invalidation chains. The RESEARCH audit found useArmyLists.ts (76 invalidateQueries calls) is the primary optimization target, while useUnits.ts, useRecipeAssignments.ts, and useRulesSync.ts are already well-reasoned.

    **Audit scope per D-04:** All 25 hook files must be audited. The RESEARCH.md invalidation audit already covers the 4 heaviest files (useArmyLists.ts, useUnits.ts, useRecipeAssignments.ts, useRulesSync.ts) and concludes only useArmyLists.ts needs changes. The executor should spot-check 2-3 additional hook files to confirm no other low-hanging fruit was missed, but per RESEARCH findings, changes are expected only in useArmyLists.ts.

    **Changes to make in useArmyLists.ts:**

    1. For mutations that modify a SINGLE army list (updateArmyList, deleteArmyList, setWarlord, clearWarlord, setLeaderAttachment, clearLeaderAttachment, addUnit, removeUnit, updateUnit mutations) and already invalidate `ARMY_LIST_KEY(id)`:
       - Remove the broad `ARMY_LISTS_KEY` invalidation when the mutation does NOT change list-level data visible in the index (name, points total, unit count). Specifically:
         - setWarlord / clearWarlord: warlord is a display field within the detail view, not shown in the list index. Remove `ARMY_LISTS_KEY` invalidation.
         - setLeaderAttachment / clearLeaderAttachment: leader attachment is detail-level. Remove `ARMY_LISTS_KEY` invalidation.
       - Keep `ARMY_LISTS_KEY` for mutations that DO change list-level visible data: createArmyList, deleteArmyList, updateArmyList (name/points change affects list index).

    2. For `addUnit` and `removeUnit` mutations: keep `ARMY_LISTS_KEY` since unit count / points total changes are visible in the list index.

    3. Do NOT add `exact: true` to `["army-list-readiness"]` -- the key factory includes variable sub-keys and broad invalidation is correct here per RESEARCH.md audit.

    4. Do NOT touch useUnits.ts, useRecipeAssignments.ts, or useRulesSync.ts -- the RESEARCH audit confirmed these are already well-reasoned.

    5. Add comments on kept invalidations explaining why they are needed (per D-05).

    **Create tests/performance/invalidationAudit.test.ts:**

    Add a header comment documenting the full PERF-02 audit scope: "PERF-02 Invalidation Audit: All 25 hook files in src/hooks/ were audited per D-04. RESEARCH.md analyzed useArmyLists.ts (76 calls), useUnits.ts (18 calls), useRecipeAssignments.ts (25 calls), and useRulesSync.ts (22 calls). Only useArmyLists.ts required changes -- remaining files confirmed correct."

    Use the QueryClient spy pattern from tests/foundation/useUnits.test.ts. Test:
    - After `useSetWarlord` mutates: `ARMY_LIST_KEY(id)` IS invalidated but `ARMY_LISTS_KEY` is NOT
    - After `useClearWarlord` mutates: same assertion
    - After `useCreateArmyList` mutates: `ARMY_LISTS_KEY` IS invalidated (list index changes)
    - After `useDeleteArmyList` mutates: `ARMY_LISTS_KEY` IS invalidated
  </action>
  <verify>
    <automated>pnpm test -- tests/performance/invalidationAudit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - useArmyLists.ts setWarlord/clearWarlord onSuccess callbacks do NOT contain `ARMY_LISTS_KEY` invalidation
    - useArmyLists.ts setLeaderAttachment/clearLeaderAttachment onSuccess callbacks do NOT contain `ARMY_LISTS_KEY` invalidation
    - useArmyLists.ts createArmyList/deleteArmyList onSuccess callbacks still contain `ARMY_LISTS_KEY` invalidation
    - No changes to useUnits.ts, useRecipeAssignments.ts, useRulesSync.ts
    - tests/performance/invalidationAudit.test.ts has header comment documenting audit scope across all 25 hook files
    - tests/performance/invalidationAudit.test.ts passes verifying warlord mutation does not invalidate list index
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>Invalidation chains in useArmyLists.ts are precise -- detail-only mutations no longer trigger list index refetch; full 25-file audit scope documented in test file; verified by tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | SQL query changes use parameterized queries on existing tables; invalidation changes are client-side cache management |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-03 | Injection | getKanbanProgressByUnitIds SQL | mitigate | IN-clause uses positional `$N` params from array index math, not string interpolation. All values go through parameterized binding via Tauri plugin-sql. |
| T-98-04 | Denial of Service | Large unitIds array in batched query | accept | Kanban board has practical limit of ~100 units per board. No external input path -- unitIds come from existing DB query results. |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `pnpm build` succeeds
- `pnpm test -- tests/performance` passes all new tests
- `pnpm test` full suite passes (no regressions from invalidation changes)
</verification>

<success_criteria>
- Kanban enrichment uses getKanbanProgressByUnitIds for O(1) DB round-trips per board render (PERF-03)
- Detail-only army list mutations (warlord, leader attachment) no longer trigger list index refetch (PERF-02)
- All genuinely needed cross-domain invalidations preserved
- Full 25-file audit scope documented
- All tests pass, build succeeds
</success_criteria>

<output>
Create `.planning/phases/98-performance-optimization/98-03-SUMMARY.md` when done
</output>

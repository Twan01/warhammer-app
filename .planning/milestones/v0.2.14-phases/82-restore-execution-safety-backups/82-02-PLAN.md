---
phase: 82-restore-execution-safety-backups
plan: 02
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - src/features/data-health/BackupCard.tsx
  - src/features/data-health/RestorePreviewDialog.tsx
  - tests/data-health/RestorePreviewDialog.test.tsx
  - tests/data-health/backupCard.test.tsx
autonomous: true
requirements: [RST-06, RST-07, RST-08]

must_haves:
  truths:
    - "User clicks Replace current database and the app restores the backup then relaunches"
    - "If the restore fails, the user sees an error toast and the dialog closes"
    - "The Restoring... spinner shows during restore execution"
    - "The dialog cannot be dismissed while a restore is in progress"
  artifacts:
    - path: "src/features/data-health/BackupCard.tsx"
      provides: "Real restore execution via invoke + relaunch"
      contains: "restore_from_backup"
    - path: "src/features/data-health/RestorePreviewDialog.tsx"
      provides: "isRestoring prop for spinner + disabled state"
      contains: "isRestoring"
    - path: "tests/data-health/RestorePreviewDialog.test.tsx"
      provides: "Tests for real restore behavior replacing placeholder"
      contains: "restore_from_backup"
  key_links:
    - from: "BackupCard.tsx"
      to: "restore_from_backup"
      via: "invoke call in handleConfirmRestore"
      pattern: 'invoke.*restore_from_backup'
    - from: "BackupCard.tsx"
      to: "@tauri-apps/plugin-process"
      via: "relaunch() after successful restore"
      pattern: "relaunch"
    - from: "BackupCard.tsx"
      to: "RestorePreviewDialog"
      via: "isRestoring prop passed down"
      pattern: "isRestoring"
---

<objective>
Wire the real restore execution flow into BackupCard: replace the placeholder toast with invoke("restore_from_backup") + relaunch(), add selectedPath state, pass isRestoring to RestorePreviewDialog, and update tests.

Purpose: Completes RST-06 (safety backup before restore), RST-07 (atomic file swap), and RST-08 (app restart) from the frontend side.
Output: Working restore flow in BackupCard, spinner state in RestorePreviewDialog, updated tests.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-restore-execution-safety-backups/82-CONTEXT.md
@.planning/phases/82-restore-execution-safety-backups/82-RESEARCH.md
@.planning/phases/82-restore-execution-safety-backups/82-PATTERNS.md
@.planning/phases/82-restore-execution-safety-backups/82-UI-SPEC.md
@.planning/phases/82-restore-execution-safety-backups/82-01-SUMMARY.md

<interfaces>
<!-- From BackupCard.tsx -- current state declarations (lines 32-38) -->
const [isBackingUp, setIsBackingUp] = useState(false);
const [isValidating, setIsValidating] = useState(false);
const [previewOpen, setPreviewOpen] = useState(false);
const [manifest, setManifest] = useState<BackupManifest | null>(null);
const [currentSchemaVersion, setCurrentSchemaVersion] = useState<number | null>(null);

<!-- From RestorePreviewDialog.tsx -- current props (lines 48-60) -->
export function RestorePreviewDialog({
  manifest, currentSchemaVersion, open, onOpenChange, onConfirm,
}: {
  manifest: BackupManifest;
  currentSchemaVersion: number;
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onConfirm: () => void;
})

<!-- From RestorePreviewDialog.tsx -- action button (lines 157-163) -->
<AlertDialogAction
  className={buttonVariants({ variant: "destructive" })}
  disabled={schemaState === "newer"}
  onClick={onConfirm}
>
  Replace current database
</AlertDialogAction>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire restore execution in BackupCard + add isRestoring to RestorePreviewDialog</name>
  <files>src/features/data-health/BackupCard.tsx, src/features/data-health/RestorePreviewDialog.tsx</files>
  <read_first>
    - src/features/data-health/BackupCard.tsx (entire file -- understand handleRestore, handleConfirmRestore, state declarations, RestorePreviewDialog usage)
    - src/features/data-health/RestorePreviewDialog.tsx (entire file -- understand props, AlertDialogAction, AlertDialogCancel, onOpenChange)
    - .planning/phases/82-restore-execution-safety-backups/82-UI-SPEC.md (Component Inventory section -- BackupCard and RestorePreviewDialog changes)
    - .planning/phases/82-restore-execution-safety-backups/82-PATTERNS.md (Pattern 3: BackupCard selectedPath State)
  </read_first>
  <action>
    **BackupCard.tsx changes:**

    Add import: `import { relaunch } from "@tauri-apps/plugin-process";` (alongside existing imports from lines 8-11).

    Add two state variables after the existing state declarations (~line 38):
    - `const [selectedPath, setSelectedPath] = useState<string | null>(null);`
    - `const [isRestoring, setIsRestoring] = useState(false);`

    In `handleRestore`, after `setPreviewOpen(true)` (~line 97), add `setSelectedPath(result);` to persist the file dialog result into state. This bridges the gap where `result` is a local variable inaccessible from `handleConfirmRestore`.

    Replace the entire `handleConfirmRestore` function (lines 107-111) with an async version per D-02/D-03:
    - Guard: `if (!selectedPath) return;`
    - Set `isRestoring` to true
    - Try block: `await invoke("restore_from_backup", { path: selectedPath })` then `await relaunch()`
    - Catch block: show `toast.error` with the error message pattern `Restore failed: ${error instanceof Error ? error.message : String(error)}`, then reset all state: `setIsRestoring(false)`, `setPreviewOpen(false)`, `setManifest(null)`, `setSelectedPath(null)`
    - No finally block on success path -- relaunch() restarts the process (Pitfall 5)

    Also disable the "Restore from Backup" button when `isRestoring` is true: change `disabled={isValidating}` to `disabled={isValidating || isRestoring}` on the restore button (~line 145).

    Pass `isRestoring` as a prop to `RestorePreviewDialog` in the JSX (~line 163).

    **RestorePreviewDialog.tsx changes:**

    Add `isRestoring` optional prop: `isRestoring?: boolean` to the props type. Default to `false`.

    Add `Loader2` to the lucide-react import (alongside AlertTriangle, ShieldAlert).

    Modify AlertDialogAction (~line 157-163):
    - Change `disabled` to `disabled={schemaState === "newer" || isRestoring}`
    - Change the button content to conditionally render: when `isRestoring` show `<Loader2 className="mr-2 h-4 w-4 animate-spin" />` + "Restoring...", otherwise show "Replace current database"

    Disable AlertDialogCancel when `isRestoring` is true: add `disabled={isRestoring}`.

    Suppress dialog dismissal during restore: wrap `onOpenChange` so it only fires when `!isRestoring`. Change `onOpenChange={onOpenChange}` on the AlertDialog to `onOpenChange={(v) => { if (!isRestoring) onOpenChange(v); }}`.
  </action>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - BackupCard imports `relaunch` from `@tauri-apps/plugin-process`
    - BackupCard has `selectedPath` state variable of type `string | null`
    - BackupCard has `isRestoring` state variable of type `boolean`
    - `handleRestore` calls `setSelectedPath(result)` after setting manifest
    - `handleConfirmRestore` is `async`, calls `invoke("restore_from_backup", { path: selectedPath })` then `relaunch()`
    - `handleConfirmRestore` catch block calls `toast.error` with "Restore failed:" prefix
    - Restore button has `disabled={isValidating || isRestoring}`
    - RestorePreviewDialog accepts `isRestoring?: boolean` prop
    - RestorePreviewDialog imports `Loader2` from lucide-react
    - AlertDialogAction shows "Restoring..." with Loader2 spinner when `isRestoring` is true
    - AlertDialogAction is disabled when `isRestoring` is true
    - AlertDialogCancel is disabled when `isRestoring` is true
    - AlertDialog `onOpenChange` is suppressed when `isRestoring` is true
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>BackupCard performs real restore via invoke + relaunch, RestorePreviewDialog shows spinner during restore. The placeholder toast is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Update restore tests -- replace placeholder with real behavior</name>
  <files>tests/data-health/RestorePreviewDialog.test.tsx, tests/data-health/backupCard.test.tsx</files>
  <read_first>
    - tests/data-health/RestorePreviewDialog.test.tsx (entire file -- find the placeholder toast test at ~line 241-262 that must be replaced)
    - tests/data-health/backupCard.test.tsx (entire file -- understand mock patterns for invoke, dialog, toast)
    - src/features/data-health/BackupCard.tsx (the updated file from Task 1)
    - .planning/phases/82-restore-execution-safety-backups/82-PATTERNS.md (test patterns section)
  </read_first>
  <action>
    **RestorePreviewDialog.test.tsx changes:**

    Add a mock for `@tauri-apps/plugin-process`: `const mockRelaunch = vi.fn();` at top level, then `vi.mock("@tauri-apps/plugin-process", () => ({ relaunch: () => mockRelaunch() }));`. Reset in `beforeEach`.

    Replace the placeholder toast test (~line 241-262, test name likely "Shows placeholder toast on confirm click" or similar) with three new tests:

    1. "Calls restore_from_backup with selected path when user confirms": Set up mockInvoke to resolve for validate_backup, get_schema_version, then restore_from_backup. Open dialog via file picker mock, wait for preview, click "Replace current database", assert `mockInvoke` was called with `"restore_from_backup"` and `{ path: <the mock path> }`.

    2. "Calls relaunch after successful restore": Same setup, assert `mockRelaunch` was called after restore_from_backup succeeds.

    3. "Shows error toast and does not relaunch when restore fails": Set mockInvoke to reject for restore_from_backup (resolve for validate_backup and get_schema_version). Click confirm, assert `toast.error` called with message containing "Restore failed:", assert `mockRelaunch` was NOT called.

    **backupCard.test.tsx changes (if needed):**

    If backupCard.test.tsx has its own restore-related tests that reference the placeholder toast, update them similarly. Add the `mockRelaunch` mock for `@tauri-apps/plugin-process` to this file as well if restore tests exist here.

    Use the existing mock invoke sequence pattern: `mockInvoke.mockResolvedValueOnce(MOCK_MANIFEST).mockResolvedValueOnce(32).mockResolvedValueOnce(undefined)` for the validate -> schema version -> restore chain.
  </action>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App" && pnpm test -- tests/data-health/RestorePreviewDialog.test.tsx tests/data-health/backupCard.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - No test references `"Restore execution coming in a future update"` placeholder toast
    - Test exists asserting `invoke` called with `"restore_from_backup"` and correct path argument
    - Test exists asserting `relaunch()` called after successful restore
    - Test exists asserting `toast.error` called with "Restore failed:" when restore rejects, and `relaunch` NOT called
    - All tests in RestorePreviewDialog.test.tsx pass
    - All tests in backupCard.test.tsx pass
    - `pnpm test` (full suite) passes with 0 failures
  </acceptance_criteria>
  <done>Placeholder toast tests replaced with real restore behavior tests. All data-health tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS invoke -> Rust | selectedPath from file dialog passed to restore_from_backup |
| Rust -> JS | Error strings from Rust displayed in toast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-04 | Information Disclosure | Error toast message | accept | Error strings from Rust are technical (file paths, IO errors) but this is a local desktop app -- no cross-user risk |
| T-82-05 | Denial of Service | isRestoring state blocks UI | accept | Intentional -- prevents double-invoke during restore; reset on error |
| T-82-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
- `pnpm build` succeeds
- `pnpm test -- tests/data-health/` all pass
- `pnpm test` full suite passes
- Placeholder toast text no longer appears in any test or source file
</verification>

<success_criteria>
BackupCard invokes restore_from_backup then relaunch() on confirm. RestorePreviewDialog shows Restoring... spinner and blocks dismissal during restore. Tests verify the real restore flow replaces the placeholder.
</success_criteria>

<output>
Create `.planning/phases/82-restore-execution-safety-backups/82-02-SUMMARY.md` when done
</output>

---
phase: 80-export-ui-backup-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/backupFreshness.ts
  - tests/data-health/backupFreshness.test.ts
autonomous: true
requirements:
  - STS-01
  - STS-02
must_haves:
  truths:
    - "getBackupFreshness(null) returns 'never'"
    - "getBackupFreshness(date 2 days ago) returns 'healthy'"
    - "getBackupFreshness(date 7 days ago) returns 'healthy' (inclusive boundary)"
    - "getBackupFreshness(date 8 days ago) returns 'recommended'"
    - "getBackupFreshness(date 30 days ago) returns 'recommended' (inclusive boundary)"
    - "getBackupFreshness(date 31 days ago) returns 'overdue'"
    - "getBackupAgeLabel(null) returns 'No backup'"
    - "getBackupAgeLabel(today) returns 'Backed up today'"
    - "BACKUP_FRESHNESS_DOT_CLASS maps healthy to bg-green-500, recommended to bg-amber-500, overdue to bg-orange-500, never to bg-muted-foreground"
  artifacts:
    - path: "src/lib/backupFreshness.ts"
      provides: "BackupFreshness type, getBackupFreshness, getBackupAgeLabel, BACKUP_FRESHNESS_DOT_CLASS"
      exports: ["BackupFreshness", "getBackupFreshness", "getBackupAgeLabel", "BACKUP_FRESHNESS_DOT_CLASS"]
    - path: "tests/data-health/backupFreshness.test.ts"
      provides: "Unit tests for all tier boundaries and age labels"
      min_lines: 60
  key_links:
    - from: "src/lib/backupFreshness.ts"
      to: "src/lib/syncFreshness.ts"
      via: "identical structural pattern"
      pattern: "export type.*Freshness"
---

<objective>
Create the backup freshness utility that both BackupCard and DataHealthSummaryCard will consume for health tier calculation and age label formatting.

Purpose: Centralizes backup health logic so both components produce identical tier results (per D-01, D-02). This is the shared foundation for all backup status display in the app.
Output: `src/lib/backupFreshness.ts` (utility) + `tests/data-health/backupFreshness.test.ts` (unit tests)
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-export-ui-backup-status/80-CONTEXT.md
@.planning/phases/80-export-ui-backup-status/80-RESEARCH.md
@.planning/phases/80-export-ui-backup-status/80-PATTERNS.md
@.planning/phases/80-export-ui-backup-status/80-UI-SPEC.md

<interfaces>
<!-- Pattern source: src/lib/syncFreshness.ts — exact structural mirror -->
From src/lib/syncFreshness.ts:
export type SyncFreshness = "fresh" | "aging" | "stale" | "never";
export function getSyncFreshness(lastSyncAt: string | null): SyncFreshness;
export function getSyncAgeLabel(lastSyncAt: string | null): string;
export const FRESHNESS_DOT_CLASS: Record<SyncFreshness, string>;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create backupFreshness.ts utility</name>
  <files>src/lib/backupFreshness.ts, tests/data-health/backupFreshness.test.ts</files>
  <read_first>
    - src/lib/syncFreshness.ts (structural pattern to mirror exactly)
    - tests/datasheet/syncFreshness.test.ts (test pattern with vi.useFakeTimers + vi.setSystemTime)
    - .planning/phases/80-export-ui-backup-status/80-PATTERNS.md (adaptation rules and final shape)
  </read_first>
  <behavior>
    - getBackupFreshness(null) returns "never"
    - getBackupFreshness(date 0 days ago) returns "healthy"
    - getBackupFreshness(date 7 days ago) returns "healthy" (inclusive <=7 per D-01)
    - getBackupFreshness(date 8 days ago) returns "recommended"
    - getBackupFreshness(date 30 days ago) returns "recommended" (inclusive <=30 per D-01)
    - getBackupFreshness(date 31 days ago) returns "overdue"
    - getBackupAgeLabel(null) returns "No backup"
    - getBackupAgeLabel(date 0 days ago) returns "Backed up today"
    - getBackupAgeLabel(date 1 day ago) returns "Backed up yesterday"
    - getBackupAgeLabel(date 5 days ago) returns "Backed up 5 days ago"
    - BACKUP_FRESHNESS_DOT_CLASS["healthy"] equals "bg-green-500"
    - BACKUP_FRESHNESS_DOT_CLASS["recommended"] equals "bg-amber-500"
    - BACKUP_FRESHNESS_DOT_CLASS["overdue"] equals "bg-orange-500"
    - BACKUP_FRESHNESS_DOT_CLASS["never"] equals "bg-muted-foreground"
  </behavior>
  <action>
    Create src/lib/backupFreshness.ts mirroring the exact structure of src/lib/syncFreshness.ts per D-02. Export type BackupFreshness = "healthy" | "recommended" | "overdue" | "never". Export function getBackupFreshness(date: string | null): BackupFreshness with thresholds: ageDays <= 7 returns "healthy", ageDays <= 30 returns "recommended", else "overdue", null returns "never" (per D-01). CRITICAL: use <= (inclusive), NOT < like syncFreshness.ts. Export function getBackupAgeLabel(date: string | null): string returning "No backup" / "Backed up today" / "Backed up yesterday" / "Backed up N days ago". Export const BACKUP_FRESHNESS_DOT_CLASS record mapping tiers to Tailwind classes: healthy=bg-green-500, recommended=bg-amber-500, overdue=bg-orange-500, never=bg-muted-foreground (per UI-SPEC Color section).

    Create tests/data-health/backupFreshness.test.ts mirroring tests/datasheet/syncFreshness.test.ts pattern. Use vi.useFakeTimers() + vi.setSystemTime(NOW) with NOW = new Date("2026-05-18T12:00:00.000Z"). Create daysAgo(n) helper. Test all tier boundaries (0, 7, 8, 30, 31 days) and null input for getBackupFreshness. Test all age label outputs (null, 0, 1, 5 days) for getBackupAgeLabel. Test dot class map values. Use afterEach(() => vi.useRealTimers()).
  </action>
  <verify>
    <automated>pnpm test -- tests/data-health/backupFreshness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/backupFreshness.ts exports BackupFreshness type, getBackupFreshness function, getBackupAgeLabel function, BACKUP_FRESHNESS_DOT_CLASS const
    - getBackupFreshness uses <= 7 and <= 30 (NOT < 7 and < 14 like syncFreshness.ts)
    - BACKUP_FRESHNESS_DOT_CLASS["overdue"] is "bg-orange-500" (NOT bg-red-500)
    - All tests pass: tier boundary tests at 0, 7, 8, 30, 31 days + null; age label tests at 0, 1, N days + null; dot class assertions
    - pnpm test -- tests/data-health/backupFreshness.test.ts exits 0
  </acceptance_criteria>
  <done>backupFreshness.ts utility exported with all four functions/types, all tier boundary tests and age label tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage -> utility | Date string from localStorage parsed by getBackupFreshness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | localStorage date input | accept | Pure function returns "never" for null/undefined; malformed date produces NaN which falls through to "overdue" — safe degradation. No PII, no security impact. |
</threat_model>

<verification>
pnpm test -- tests/data-health/backupFreshness.test.ts
</verification>

<success_criteria>
- backupFreshness.ts exists with all exports matching the syncFreshness.ts structural pattern
- All tier boundary tests pass (inclusive thresholds per D-01)
- All age label tests pass
- Dot class map matches UI-SPEC color definitions exactly
</success_criteria>

<output>
Create `.planning/phases/80-export-ui-backup-status/80-01-SUMMARY.md` when done
</output>

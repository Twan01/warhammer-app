---
phase: 81-restore-preview-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
  - src/types/backup.ts
  - src/lib/formatBytes.ts
autonomous: true
requirements:
  - RST-04
  - RST-05
must_haves:
  truths:
    - "Frontend can retrieve the app's current schema version (migration count) via Tauri invoke"
    - "BackupManifest TypeScript interface mirrors the Rust struct exactly"
    - "Byte values can be formatted to human-readable strings (KB, MB, GB)"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "get_schema_version Tauri command"
      contains: "fn get_schema_version"
    - path: "src/types/backup.ts"
      provides: "BackupManifest TypeScript interface"
      exports: ["BackupManifest"]
    - path: "src/lib/formatBytes.ts"
      provides: "Byte formatting utility"
      exports: ["formatBytes"]
  key_links:
    - from: "src/types/backup.ts"
      to: "src-tauri/src/lib.rs"
      via: "mirrors Rust BackupManifest struct"
      pattern: "app_version.*schema_version.*created_at.*platform.*db_size_bytes"
---

<objective>
Add the get_schema_version Rust command, create the BackupManifest TypeScript type, and add a formatBytes utility -- the three foundation pieces that the restore preview UI (Plan 02) depends on.

Purpose: Plan 02 needs to invoke get_schema_version for schema comparison (RST-04/RST-05), type the validate_backup response as BackupManifest, and display db_size_bytes in a human-readable format.
Output: One modified Rust file, two new TypeScript files.
</objective>

<execution_context>
@.planning/phases/81-restore-preview-validation/81-CONTEXT.md
@.planning/phases/81-restore-preview-validation/81-RESEARCH.md
@.planning/phases/81-restore-preview-validation/81-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Rust BackupManifest struct the TS interface must mirror (lib.rs lines 620-627) -->
pub struct BackupManifest {
    pub app_version: String,
    pub schema_version: u32,
    pub created_at: String,
    pub platform: String,
    pub db_size_bytes: u64,
}

<!-- get_migrations pattern already used for schema_version (lib.rs lines 747, 824) -->
schema_version: get_migrations().len() as u32,

<!-- invoke_handler registration (lib.rs lines 864-870) -->
.invoke_handler(tauri::generate_handler![
    bulk_sync_rules,
    backup_database,
    export_backup,
    validate_backup,
    create_safety_backup,
])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add get_schema_version Rust command and BackupManifest TS type</name>
  <files>src-tauri/src/lib.rs, src/types/backup.ts</files>
  <read_first>
    - src-tauri/src/lib.rs (lines 620-627 for BackupManifest struct, lines 864-870 for invoke_handler registration, lines 745-750 for get_migrations().len() usage)
    - src/types/backup.ts (does not exist yet -- confirm)
  </read_first>
  <action>
    In src-tauri/src/lib.rs: Add a new Tauri command `get_schema_version` that returns `u32`. The body calls `get_migrations().len() as u32` -- identical to the expression already used in `export_backup` (line 747) and `create_safety_backup` (line 824). This is a sync command (no async, no args, no Result wrapper needed). Per D-06 and D-14, this returns the app's expected migration count, NOT the DB's PRAGMA user_version. Register `get_schema_version` in the `generate_handler![]` macro at line 864.

    Create src/types/backup.ts: Define and export a `BackupManifest` interface that mirrors the Rust struct exactly. Fields: `app_version: string`, `schema_version: number`, `created_at: string`, `platform: string`, `db_size_bytes: number`. Note: Rust u64 maps to JS number (safe for file sizes under 9 PB). Follow the Entity interface pattern from src/types/ -- single named export, no default export.
  </action>
  <verify>
    <automated>cd src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/lib.rs contains `#[tauri::command]` followed by `fn get_schema_version() -> u32`
    - The function body contains `get_migrations().len() as u32`
    - `get_schema_version` appears in the `generate_handler![]` macro
    - src/types/backup.ts exports `BackupManifest` interface with exactly 5 fields: app_version, schema_version, created_at, platform, db_size_bytes
    - `cargo check` in src-tauri succeeds with no errors
  </acceptance_criteria>
  <done>get_schema_version command compiles and is registered; BackupManifest TS interface exists with all 5 fields matching the Rust struct</done>
</task>

<task type="auto">
  <name>Task 2: Create formatBytes utility with tests</name>
  <files>src/lib/formatBytes.ts, tests/data-health/formatBytes.test.ts</files>
  <read_first>
    - src/lib/formatCurrency.ts (analog for file structure, JSDoc pattern, single named export convention)
    - src/lib/formatBytes.ts (does not exist yet -- confirm)
  </read_first>
  <action>
    Create src/lib/formatBytes.ts: A pure utility function following the formatCurrency.ts pattern. JSDoc header referencing Phase 81. Single named export `formatBytes(bytes: number): string`. Logic: if bytes is 0, return "0 B". Compute unit index from Math.floor(Math.log(bytes) / Math.log(1024)). Units array: ["B", "KB", "MB", "GB"]. For values under 10, show one decimal (e.g., "2.4 MB"); for 10+, show integer (e.g., "14 MB"). Handle edge case of negative input by returning "0 B".

    Create tests/data-health/formatBytes.test.ts: Test cases covering: 0 bytes returns "0 B", small values (512 returns "512 B"), KB boundary (1024 returns "1 KB"), fractional MB (2.4 * 1024 * 1024 returns "2.4 MB"), large MB (14 * 1024 * 1024 returns "14 MB"), GB range (1.5 * 1024^3 returns "1.5 GB"). Follow the describe/it pattern from backupCard.test.tsx.
  </action>
  <verify>
    <automated>pnpm test -- tests/data-health/formatBytes.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/formatBytes.ts exports a function named `formatBytes`
    - formatBytes(0) returns "0 B"
    - formatBytes(1024) returns "1 KB"
    - formatBytes(2516582) returns a string containing "MB" (approximately "2.4 MB")
    - tests/data-health/formatBytes.test.ts has at least 5 test cases
    - All tests pass
  </acceptance_criteria>
  <done>formatBytes utility exists with JSDoc header, handles all size ranges, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust command -> frontend | get_schema_version returns a u32; no untrusted input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01 | Information Disclosure | get_schema_version | accept | Returns migration count (public info, not sensitive) |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `cargo check` succeeds in src-tauri
- `pnpm test -- tests/data-health/formatBytes.test.ts` passes
- `pnpm build` succeeds (TypeScript check passes with new type file)
</verification>

<success_criteria>
- get_schema_version Rust command returns migration count and is registered in generate_handler
- BackupManifest TypeScript interface has all 5 fields matching Rust struct
- formatBytes utility correctly formats byte values across B/KB/MB/GB ranges
- All tests pass, cargo check succeeds
</success_criteria>

<output>
Create `.planning/phases/81-restore-preview-validation/81-01-SUMMARY.md` when done
</output>

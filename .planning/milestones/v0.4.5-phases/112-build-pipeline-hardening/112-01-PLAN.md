---
phase: 112-build-pipeline-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/bsdata.ts
  - scripts/lib/types.ts
  - scripts/build-unit-db.ts
  - scripts/update-unit-database.ts
autonomous: true
requirements: [BPH-03]

must_haves:
  truths:
    - "D-04: matchUnit, readBsdataCatFiles, parseBsdataModelCounts, and readCsvFile are importable from scripts/lib/bsdata.ts"
    - "D-05: matchUnit returns MatchResult (with method field) instead of plain UdbUnitRow"
    - "D-06: build-unit-db.ts imports shared functions from scripts/lib/bsdata.ts; loadTranslationsFr stays local"
    - "D-05: update-unit-database.ts uses shared matchUnit with 3-pass matching instead of single-key lookup"
  artifacts:
    - path: "scripts/lib/bsdata.ts"
      provides: "Shared BSData parsing and matching functions"
      exports: ["readCsvFile", "readBsdataCatFiles", "parseBsdataModelCounts", "matchUnit", "MatchResult", "MatchMethod"]
    - path: "scripts/lib/types.ts"
      provides: "Extended FactionCoverage with match method fields"
      contains: "matched_exact"
  key_links:
    - from: "scripts/build-unit-db.ts"
      to: "scripts/lib/bsdata.ts"
      via: "import { matchUnit, readBsdataCatFiles, parseBsdataModelCounts, readCsvFile }"
      pattern: "from.*./lib/bsdata"
    - from: "scripts/update-unit-database.ts"
      to: "scripts/lib/bsdata.ts"
      via: "import { matchUnit, readBsdataCatFiles, parseBsdataModelCounts, readCsvFile }"
      pattern: "from.*./lib/bsdata"
---

<objective>
Extract shared BSData parsing and matching functions into scripts/lib/bsdata.ts, update matchUnit to return match method, and rewire both entry-point scripts to use the shared library.

Purpose: Eliminates code duplication between build-unit-db.ts and update-unit-database.ts (per D-04/D-05). The shared matchUnit returning MatchResult enables per-faction match method tracking needed by BPH-01 in Plan 02.
Output: New scripts/lib/bsdata.ts module; both scripts refactored to import from it.
</objective>

<execution_context>
@.planning/phases/112-build-pipeline-hardening/112-CONTEXT.md
@.planning/phases/112-build-pipeline-hardening/112-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From scripts/lib/types.ts:
- BsdataModelCount, UdbUnitRow, UdbFactionRow, UdbUnitWeaponRow, UdbUnitAbilityRow, UdbUnitKeywordRow, UdbUnitPointsRow, UdbUnitCompositionRow, UdbUnitModelRow, UnitDatabaseJson, CoverageReport, FactionCoverage, TranslationsFrOverlay

From scripts/lib/normalize.ts:
- normalizeName(name: string): string
- loadAliases(aliasesPath: string): Record<string, string>

From scripts/lib/parseXml.ts:
- parseCatXml(xml: string, factionId: string | null, catalogueName?: string): BsdataUnit[]
- extractModelCounts(doc: Document, factionId: string | null): BsdataModelCount[]

From scripts/lib/factionMap.ts:
- FACTION_MAP: Record<string, string>
- SUB_FACTION_MAP: Record<string, string>
- CROSS_FACTION_MAP: Record<string, string>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/lib/bsdata.ts with shared functions and update types</name>
  <read_first>
    - scripts/build-unit-db.ts (lines 78-184: readCsv, loadTranslationsFr, readBsdataCatFiles, parseBsdataModelCounts, matchUnit)
    - scripts/update-unit-database.ts (lines 97-125: readCsv, readBsdataCatFiles duplicates; lines 280-295: simple unitByNameFaction.get matching)
    - scripts/lib/types.ts (FactionCoverage interface at ~line 116)
    - scripts/lib/normalize.ts (normalizeName, loadAliases exports)
    - scripts/lib/parseXml.ts (parseCatXml, extractModelCounts exports)
    - scripts/lib/factionMap.ts (FACTION_MAP export)
  </read_first>
  <files>scripts/lib/bsdata.ts, scripts/lib/types.ts</files>
  <action>
    Create scripts/lib/bsdata.ts with four exported functions extracted from build-unit-db.ts:

    1. readCsvFile(dataDir: string, filename: string) — wraps parseWahapediaCsv with filesystem read. Accept dataDir as param (not hardcoded constant). Import parseWahapediaCsv from ./parseCsv.ts.

    2. readBsdataCatFiles(bsdataDir: string) — copy the build-unit-db.ts version (lines 104-139) which has warning logs. Accept bsdataDir as param. Import FACTION_MAP from ./factionMap.ts.

    3. parseBsdataModelCounts(catFiles: Array of {xml, factionId, catalogueName}) — copy from build-unit-db.ts lines 141-151. Requires DOMParser polyfill set by entry-point before calling (document in JSDoc header comment like parseXml.ts does).

    4. matchUnit(bsdataName, factionId, aliases, unitMap) — copy 3-pass logic from build-unit-db.ts lines 157-184 BUT change return type from UdbUnitRow|undefined to MatchResult|undefined. Import normalizeName from ./normalize.ts.

    Export two new types from bsdata.ts:
    - MatchMethod = "exact" | "normalized" | "alias"
    - MatchResult = { unit: UdbUnitRow; method: MatchMethod }

    Follow existing scripts/lib/ conventions: named exports only, no filesystem side effects at import time, pure functions accepting paths as parameters, JSDoc header comment noting DOMParser polyfill requirement for parseBsdataModelCounts.

    In scripts/lib/types.ts, extend FactionCoverage interface with three new optional fields: matched_exact (number), matched_normalized (number), matched_alias (number), unmatched_names (string[]). These enable Plan 02's BPH-01 coverage enhancement. Keep existing fields unchanged for backward compat with coverage-report.json consumers.
  </action>
  <verify>
    <automated>node --experimental-strip-types -e "import { readCsvFile, readBsdataCatFiles, parseBsdataModelCounts, matchUnit } from './scripts/lib/bsdata.ts'; console.log('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/lib/bsdata.ts exists and exports readCsvFile, readBsdataCatFiles, parseBsdataModelCounts, matchUnit, MatchResult, MatchMethod
    - matchUnit returns { unit: UdbUnitRow, method: "exact" | "normalized" | "alias" } | undefined (not bare UdbUnitRow)
    - readBsdataCatFiles accepts bsdataDir: string parameter (not hardcoded path)
    - readCsvFile accepts (dataDir: string, filename: string) parameters
    - FactionCoverage in types.ts has matched_exact?, matched_normalized?, matched_alias?, unmatched_names? fields
    - No default exports in bsdata.ts
    - JSDoc header comment documents DOMParser polyfill requirement
  </acceptance_criteria>
  <done>New shared library module exists with all four functions and two types exported. FactionCoverage extended with match tracking fields.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire build-unit-db.ts to use shared lib</name>
  <read_first>
    - scripts/build-unit-db.ts (full file — imports at top, helper functions lines 78-184, matching loop lines 396-448)
    - scripts/lib/bsdata.ts (just created in Task 1)
  </read_first>
  <files>scripts/build-unit-db.ts</files>
  <action>
    Per D-04 and D-06, refactor build-unit-db.ts to import from shared lib:

    1. Add import: import { readCsvFile, readBsdataCatFiles, parseBsdataModelCounts, matchUnit } from "./lib/bsdata.ts" and import type { MatchResult } from "./lib/bsdata.ts".

    2. Delete the four local function definitions: readCsv (lines 78-82), readBsdataCatFiles (lines 104-139), parseBsdataModelCounts (lines 141-151), matchUnit (lines 157-184). Keep loadTranslationsFr local per D-06.

    3. Update all call sites for readCsv to readCsvFile(DATA_DIR, filename) — there are ~6 calls in Step 1-6 (Factions.csv, Datasheets.csv, etc.).

    4. Update readBsdataCatFiles() call at line 383 to readBsdataCatFiles(BSDATA_DIR).

    5. Update the matching loop (lines 404-422) to use the new MatchResult return type:
       - Change: const result = matchUnit(...); if (!result) continue; const { unit, method } = result;
       - Replace the post-hoc match method detection heuristic (lines 414-422) with direct use of result.method
       - Track per-faction match method: create a Map of factionId to {exact, normalized, alias} counters before the loop; increment based on result.method and the unit's faction_id

    6. For cross-faction fallback (lines 407-410): also destructure result — const fallback = matchUnit(bsdataName, altFactionId, ...); if (fallback) { unit = fallback.unit; method = fallback.method; }

    7. Ensure pnpm build (TypeScript check) still passes with no type errors.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - build-unit-db.ts has no local readCsv, readBsdataCatFiles, parseBsdataModelCounts, or matchUnit function definitions
    - build-unit-db.ts imports readCsvFile, readBsdataCatFiles, parseBsdataModelCounts, matchUnit from "./lib/bsdata.ts"
    - loadTranslationsFr remains defined locally in build-unit-db.ts (per D-06)
    - All readCsv(...) calls replaced with readCsvFile(DATA_DIR, ...)
    - The post-hoc match method heuristic (checking unitByNameFaction.has after matchUnit) is removed; method comes from MatchResult.method
    - pnpm build completes with exit code 0
  </acceptance_criteria>
  <done>build-unit-db.ts uses shared lib for all four functions. Match method tracked from MatchResult directly, not re-derived.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire update-unit-database.ts to use shared lib with full matching</name>
  <read_first>
    - scripts/update-unit-database.ts (full file — especially buildUnitDatabase function ~lines 130-400, the simple matching at ~line 291)
    - scripts/lib/bsdata.ts (shared lib created in Task 1)
    - scripts/lib/normalize.ts (loadAliases export)
    - scripts/lib/factionMap.ts (SUB_FACTION_MAP, CROSS_FACTION_MAP exports)
  </read_first>
  <files>scripts/update-unit-database.ts</files>
  <action>
    Per D-05, refactor update-unit-database.ts to use shared lib with full matching:

    1. Add imports from shared lib: import { readCsvFile, readBsdataCatFiles, parseBsdataModelCounts, matchUnit } from "./lib/bsdata.ts"
    2. Add imports from factionMap: import { SUB_FACTION_MAP, CROSS_FACTION_MAP } from "./lib/factionMap.ts" (currently only imports FACTION_MAP)
    3. Add import from normalize: import { loadAliases } from "./lib/normalize.ts" (not currently imported)

    4. Delete the local readCsv function (~lines 97-101), local readBsdataCatFiles function (~lines 103-125), local parseBsdataModelCounts if present.

    5. Update all readCsv calls to readCsvFile(DATA_DIR, filename).
    6. Update readBsdataCatFiles() to readBsdataCatFiles(BSDATA_DIR).

    7. In buildUnitDatabase(), replace the single-key lookup at ~line 291:
       - Load aliases: const aliases = loadAliases(ALIASES_PATH) where ALIASES_PATH = join(DATA_DIR, "aliases.json")
       - Replace: const key = bsdataUnit.datasheet_name.toLowerCase() + ":" + bsdataUnit.faction_id; const unit = unitByNameFaction.get(key); if (!unit) continue;
       - With: const result = matchUnit(bsdataUnit.datasheet_name, bsdataUnit.faction_id, aliases, unitByNameFaction); let unit = result?.unit; if (!unit) { const altFactionId = CROSS_FACTION_MAP[catFile.catalogueName] ?? null; if (altFactionId) { const fallback = matchUnit(bsdataUnit.datasheet_name, altFactionId, aliases, unitByNameFaction); unit = fallback?.unit; } } if (!unit) continue;

    8. Add sub_faction population after matching (same as build script): const subFaction = SUB_FACTION_MAP[catFile.catalogueName] ?? null; if (subFaction && unit.sub_faction === null) unit.sub_faction = subFaction;

    9. For weapon/ability objects that are missing name_fr/description_fr/keyword_fr fields compared to shared types — add null values for those fields to satisfy TypeScript.

    10. Ensure pnpm build passes.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - update-unit-database.ts has no local readCsv or readBsdataCatFiles function definitions
    - update-unit-database.ts imports matchUnit, readCsvFile, readBsdataCatFiles from "./lib/bsdata.ts"
    - update-unit-database.ts imports loadAliases from "./lib/normalize.ts"
    - update-unit-database.ts imports SUB_FACTION_MAP, CROSS_FACTION_MAP from "./lib/factionMap.ts"
    - The simple unitByNameFaction.get(key) lookup is replaced with matchUnit() 3-pass matching
    - Cross-faction fallback using CROSS_FACTION_MAP is present
    - Sub-faction population using SUB_FACTION_MAP is present
    - pnpm build completes with exit code 0
  </acceptance_criteria>
  <done>update-unit-database.ts uses shared matching pipeline identical to build-unit-db.ts. A fix in matchUnit automatically applies to both scripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable — this phase modifies dev-side build scripts only. No user input, no network access, no runtime app code changes. All data is local filesystem.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | scripts/data/aliases.json | accept | Local file only, developer-controlled; BPH-04 (Plan 02) adds validation |
| T-112-SC | Tampering | npm installs | accept | No new npm dependencies in this phase |
</threat_model>

<verification>
Run `pnpm build:udb` — the build script executes successfully using shared lib functions and produces unit_database.json.
Run `pnpm build` — TypeScript compilation passes with no errors for both scripts.
</verification>

<success_criteria>
- scripts/lib/bsdata.ts exists with 4 exported functions and 2 exported types
- build-unit-db.ts has zero local definitions of readCsv, readBsdataCatFiles, parseBsdataModelCounts, matchUnit
- update-unit-database.ts has zero local definitions of readCsv, readBsdataCatFiles
- update-unit-database.ts uses 3-pass matchUnit instead of single-key lookup
- pnpm build and pnpm build:udb both succeed
</success_criteria>

<output>
Create `.planning/phases/112-build-pipeline-hardening/112-01-SUMMARY.md` when done
</output>

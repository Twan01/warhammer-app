# Phase 112: Build Pipeline Hardening — Discussion Log

**Date:** 2026-06-02
**Mode:** --auto (fully autonomous)
**Areas discussed:** 5 (all auto-selected)

## Discussion Summary

### 1. Coverage Report Format (BPH-01)
- **Q:** Should the per-faction coverage table print to stdout only, or also update coverage-report.json?
- **Selected:** Both — print table to stdout AND write coverage-report.json (recommended default)
- **Note:** Already exists in build-unit-db.ts Step 10. Phase 112 refines to include matched vs unmatched counts and match method breakdown.

### 2. Deterministic Builds (BPH-02)
- **Q:** What needs to change for fully deterministic output?
- **Selected:** Sort all output arrays by natural keys before JSON serialization (recommended default)
- **Note:** File sorting already in place (D-06); output array sorting is the remaining gap.

### 3. Shared Library Extraction Scope (BPH-03)
- **Q:** How much of the duplicated pipeline should move to shared lib?
- **Selected:** Extract matchUnit, readBsdataCatFiles, parseBsdataModelCounts, readCsv to scripts/lib/ (recommended default)
- **Note:** update-unit-database.ts has diverged significantly — missing multi-pass matching, French overlay, sub_faction support.

### 4. Alias Validation Behavior (BPH-04)
- **Q:** Should unused alias warnings be errors (exit non-zero) or warnings?
- **Selected:** Warnings only — print but don't fail (recommended default)
- **Note:** Unused aliases are cosmetic debt, not data quality failures.

### 5. Failure Threshold Configuration (BPH-05)
- **Q:** Where should the coverage threshold be configured?
- **Selected:** Constant at top of build-unit-db.ts (recommended default)
- **Note:** No need for external config file for a dev-side build script. Default 90%.

## Deferred Ideas

None.

## Claude's Discretion Items

- Function signatures and module boundaries in scripts/lib/
- Sort key choices for deterministic output arrays
- Whether to add --verbose flag

---
*Auto-generated by /gsd:discuss-phase 112 --auto*

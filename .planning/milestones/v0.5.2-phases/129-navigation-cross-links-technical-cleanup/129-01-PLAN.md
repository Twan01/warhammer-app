---
phase: 129-navigation-cross-links-technical-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/router.tsx
  - src/app/painting-mode/page.tsx
  - src/features/units/AppliedRecipesTab.tsx
  - src/features/dashboard/DashboardPage.tsx
  - src/features/dashboard/NextPaintingActionCard.tsx
  - src/features/painting-projects/KanbanBoard.tsx
  - src/features/recipes/RecipeDetailSheet.tsx
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "Painting Mode exit returns to the page that launched it, not always Dashboard"
    - "Every entry point passes the current pathname as returnTo search param"
    - "handleExit reads returnTo and navigates there; falls back to / if absent"
  artifacts:
    - path: "src/app/router.tsx"
      provides: "paintingModeRoute with validateSearch schema"
      contains: "validateSearch"
    - path: "src/app/painting-mode/page.tsx"
      provides: "handleExit reading returnTo from search"
      contains: "returnTo"
  key_links:
    - from: "src/features/units/AppliedRecipesTab.tsx"
      to: "paintingModeRoute"
      via: "search: { returnTo: location.pathname }"
      pattern: "returnTo"
    - from: "src/features/dashboard/NextPaintingActionCard.tsx"
      to: "paintingModeRoute"
      via: "search: { returnTo }"
      pattern: "returnTo"
---

<objective>
Add `returnTo` search parameter to the painting-mode route so that exiting Painting Mode navigates back to the originating page instead of always going to Dashboard.

Purpose: Per D-01/D-02, users entering Painting Mode from Collection, Kanban, Recipes, or Dashboard should return to that same page on exit. Currently `handleExit` always navigates to `"/"`.
Output: Updated route schema, updated exit handler, all 5 entry points passing returnTo.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-navigation-cross-links-technical-cleanup/129-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add returnTo search schema to painting-mode route and update handleExit</name>
  <files>src/app/router.tsx, src/app/painting-mode/page.tsx</files>
  <read_first>
    - src/app/router.tsx (current paintingModeRoute at line 203-207, and recipesRoute for validateSearch pattern)
    - src/app/painting-mode/page.tsx (handleExit at line 111-113, useParams at line 19-21)
  </read_first>
  <action>
Per D-01 and D-02:

In `router.tsx`, add `validateSearch: z.object({ returnTo: z.string().optional() })` to the `paintingModeRoute` definition. Import `z` from `zod` if not already imported (check existing imports — `recipesRoute` already uses `z` so the import should exist).

In `page.tsx`, add `const { returnTo } = paintingModeRoute.useSearch();` inside `PaintingModePage`. Import `paintingModeRoute` from `@/app/router` (or use the route's `useSearch` via the route reference — follow the same pattern used by `recipesRoute` in `RecipesPage.tsx`). Update `handleExit` from `navigate({ to: "/" })` to `navigate({ to: returnTo ?? "/" })`.

The `useHotkeys("escape", handleExit, ...)` already calls `handleExit` so Escape key gets the fix automatically.
  </action>
  <acceptance_criteria>
    - `paintingModeRoute` in router.tsx contains `validateSearch` with `returnTo: z.string().optional()`
    - `page.tsx` reads `returnTo` from search params via `useSearch()` or equivalent
    - `handleExit` navigates to `returnTo ?? "/"` (not hardcoded `"/"`)
    - `pnpm build` passes with no TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>paintingModeRoute has typed returnTo search param; handleExit uses it with "/" fallback</done>
</task>

<task type="auto">
  <name>Task 2: Wire all 5 entry points to pass returnTo search param</name>
  <files>src/features/units/AppliedRecipesTab.tsx, src/features/dashboard/DashboardPage.tsx, src/features/dashboard/NextPaintingActionCard.tsx, src/features/painting-projects/KanbanBoard.tsx, src/features/recipes/RecipeDetailSheet.tsx</files>
  <read_first>
    - src/features/units/AppliedRecipesTab.tsx (navigate call at line 78-81)
    - src/features/dashboard/DashboardPage.tsx (navigate call at line 361-364)
    - src/features/dashboard/NextPaintingActionCard.tsx (Link component at line 64-67 — uses Link, not navigate)
    - src/features/painting-projects/KanbanBoard.tsx (navigate call at line 91-94)
    - src/features/recipes/RecipeDetailSheet.tsx (navigate call at line 315-318)
  </read_first>
  <action>
Per D-01, update ALL painting-mode entry points to pass returnTo.

**First:** Grep `src/` for all navigate/Link references to `painting-mode` (e.g., `grep -r "painting-mode" src/ --include="*.tsx"`). Verify the complete list of call sites before making changes. The known 5 are listed below, but if additional sites exist (e.g., CollectionPage), update those too.

For the 4 files using `navigate()` (AppliedRecipesTab, DashboardPage, KanbanBoard, RecipeDetailSheet): add `import { useLocation } from "@tanstack/react-router"` if not already imported, get `const location = useLocation()` in the component, and add `search: { returnTo: location.pathname }` to the existing navigate call alongside the existing `params`.

For `NextPaintingActionCard.tsx` which uses `<Link>`: add `search={{ returnTo: "/" }}` prop to the Link component. Dashboard is the fixed origin for this card so `"/"` is correct. Alternatively, import `useLocation` and use `location.pathname` for consistency.

For `DashboardPage.tsx`: the `onPaint` callback at line 361 uses `navigate`. Add `search: { returnTo: "/" }` (or `location.pathname`).

Do NOT modify any other props or behavior of these components. Only add the `search` property.
  </action>
  <acceptance_criteria>
    - All 5 files contain `returnTo` in their painting-mode navigation call
    - AppliedRecipesTab: `search: { returnTo: location.pathname }` in navigate call
    - DashboardPage: `search: { returnTo: ... }` in navigate call at onPaint
    - NextPaintingActionCard: `search={{ returnTo: ... }}` on Link component
    - KanbanBoard: `search: { returnTo: location.pathname }` in navigate call
    - RecipeDetailSheet: `search: { returnTo: location.pathname }` in navigate call
    - `pnpm build` passes with no TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>All 5 painting-mode entry points pass returnTo; exiting Painting Mode returns to origin page</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL search params -> navigation | returnTo value from URL used as navigation target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Tampering | returnTo search param | mitigate | Zod schema validates returnTo is a string; navigate() only accepts internal routes (TanStack Router rejects external URLs); fallback to "/" if invalid |
| T-129-02 | Information Disclosure | returnTo in URL | accept | Only contains pathname (no secrets); visible in URL bar is expected behavior |
</threat_model>

<verification>
- `pnpm build` passes
- Navigate to Painting Mode from Collection -> exit -> returns to /collection
- Navigate from Dashboard -> exit -> returns to /
- Navigate from Kanban -> exit -> returns to /painting-projects
</verification>

<success_criteria>
Painting Mode exit navigates to the originating page for all 5 entry points; falls back to Dashboard when returnTo is absent.
</success_criteria>

<output>
Create `.planning/phases/129-navigation-cross-links-technical-cleanup/129-01-SUMMARY.md` when done
</output>

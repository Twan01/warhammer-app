---
phase: 128-feedback-hardening-form-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/factions/FactionDeleteDialog.tsx
  - src/features/battle-log/BattleLogDeleteDialog.tsx
  - src/features/recipes/RecipeDeleteDialog.tsx
  - src/features/paints/PaintDeleteDialog.tsx
  - src/features/spending/SpendingPage.tsx
  - src/features/factions/FactionSheet.tsx
  - src/features/goals/GoalSheet.tsx
  - src/features/paints/PaintSheet.tsx
  - src/features/units/UnitFormRequired.tsx
  - src/features/battle-log/BattleLogSheet.tsx
  - src/features/recipes/RecipeFormSheet.tsx
  - src/features/units/JournalTab.tsx
  - src/features/army-lists/SnapshotHistorySheet.tsx
  - src/hooks/useGameData.ts
  - src/hooks/useDatasheet.ts
  - src/hooks/useJournalSessions.ts
  - src/hooks/useStrategyNote.ts
  - src/hooks/useUnitKeywords.ts
  - src/hooks/useUnitPhotos.ts
  - src/hooks/useUnitOverride.ts
  - src/hooks/useUnitDatabase.ts
  - src/hooks/useUdbMeta.ts
autonomous: true
requirements: [FBK-01, FBK-03, FBK-04, FBK-08, FBK-09, FBK-10]

must_haves:
  truths:
    - "D-01: Delete buttons in Faction, BattleLog, Recipe, and Paint dialogs show 'Deleting...' while mutation is pending"
    - "D-03: SpendingPage error text uses text-destructive class"
    - "D-04: All 6 Sheet forms focus first text input on open"
    - "D-08: JournalTab session create fires a success toast"
    - "D-09: Snapshot delete fires toast.success instead of neutral toast"
    - "D-10: Every staleTime: Infinity hook also has gcTime: Infinity"
  artifacts:
    - path: "src/features/factions/FactionDeleteDialog.tsx"
      provides: "Pending delete text"
      contains: "Deleting..."
    - path: "src/features/spending/SpendingPage.tsx"
      provides: "Destructive error styling"
      contains: "text-destructive"
    - path: "src/features/factions/FactionSheet.tsx"
      provides: "autoFocus on name input"
      contains: "autoFocus"
    - path: "src/hooks/useGameData.ts"
      provides: "gcTime alignment"
      contains: "gcTime: Infinity"
  key_links:
    - from: "FactionDeleteDialog.tsx"
      to: "deleteFaction.isPending"
      via: "conditional button text"
      pattern: "isPending.*Deleting"
    - from: "src/hooks/useGameData.ts"
      to: "React Query cache"
      via: "gcTime: Infinity alongside staleTime: Infinity"
      pattern: "gcTime: Infinity"
---

<objective>
Apply mechanical feedback improvements across the app: delete dialog pending text, error styling, Sheet form autoFocus, toast upgrades, and React Query cache alignment.

Purpose: Six requirements (FBK-01, FBK-03, FBK-04, FBK-08, FBK-09, FBK-10) are all 1-2 line surgical edits with established reference patterns. Grouping them maximizes efficiency.
Output: 22 files updated with consistent feedback behavior.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-feedback-hardening-form-ux/128-CONTEXT.md
@.planning/phases/128-feedback-hardening-form-ux/128-PATTERNS.md

<interfaces>
<!-- Reference pattern: GoalDeleteDialog pending text -->
From src/features/goals/GoalDeleteDialog.tsx:
  Button uses: {isPending ? "Deleting..." : "Delete"}

<!-- Reference pattern: toast conventions -->
From sonner:
  toast.success("Session logged.")   — short imperative past tense
  toast.success("Snapshot deleted.")

<!-- Reference pattern: RHF Input autoFocus -->
From src/features/factions/FactionSheet.tsx:
  <Input autoFocus {...field} />  — on the Input element inside FormControl
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dialog pending text and toast upgrades (FBK-01, FBK-08, FBK-09)</name>
  <files>
    src/features/factions/FactionDeleteDialog.tsx,
    src/features/battle-log/BattleLogDeleteDialog.tsx,
    src/features/recipes/RecipeDeleteDialog.tsx,
    src/features/paints/PaintDeleteDialog.tsx,
    src/features/units/JournalTab.tsx,
    src/features/army-lists/SnapshotHistorySheet.tsx
  </files>
  <read_first>
    src/features/goals/GoalDeleteDialog.tsx,
    src/features/factions/FactionDeleteDialog.tsx,
    src/features/battle-log/BattleLogDeleteDialog.tsx,
    src/features/recipes/RecipeDeleteDialog.tsx,
    src/features/paints/PaintDeleteDialog.tsx,
    src/features/units/JournalTab.tsx,
    src/features/army-lists/SnapshotHistorySheet.tsx
  </read_first>
  <action>
    Per D-01: In each of the 4 delete dialogs, change the destructive Button's child text from the static string to a conditional expression using the dialog's own mutation isPending flag. FactionDeleteDialog uses `deleteFaction.isPending`, BattleLogDeleteDialog uses `deleteBattleLog.isPending`, PaintDeleteDialog uses `deletePaint.isPending`. RecipeDeleteDialog currently shows "Delete recipe" — change to the same conditional with "Deleting..." as the pending text and "Delete" as the idle text (drop "recipe" suffix to match GoalDeleteDialog reference pattern).

    Per D-08: In JournalTab `handleLogSession`, add `toast.success("Session logged.")` immediately after the `await createSession.mutateAsync(...)` call, before the form reset lines. `toast` is already imported from sonner in this file.

    Per D-09: In SnapshotHistorySheet, change the `onSuccess` callback's `toast("Snapshot deleted.", ...)` call to `toast.success("Snapshot deleted.", ...)`. Keep all existing arguments (action/undo) unchanged.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - FactionDeleteDialog.tsx contains the string `deleteFaction.isPending ? "Deleting..." : "Delete"`
    - BattleLogDeleteDialog.tsx contains `deleteBattleLog.isPending ? "Deleting..." : "Delete"`
    - RecipeDeleteDialog.tsx contains `"Deleting..." : "Delete"` (not "Delete recipe")
    - PaintDeleteDialog.tsx contains `deletePaint.isPending ? "Deleting..." : "Delete"`
    - JournalTab.tsx contains `toast.success("Session logged.")`
    - SnapshotHistorySheet.tsx contains `toast.success("Snapshot deleted."` (not bare `toast(`)
  </acceptance_criteria>
  <done>All 4 delete dialogs show "Deleting..." during pending mutation, JournalTab fires success toast on session create, snapshot delete uses toast.success</done>
</task>

<task type="auto">
  <name>Task 2: Sheet form autoFocus and error styling (FBK-03, FBK-04)</name>
  <files>
    src/features/spending/SpendingPage.tsx,
    src/features/factions/FactionSheet.tsx,
    src/features/goals/GoalSheet.tsx,
    src/features/paints/PaintSheet.tsx,
    src/features/units/UnitFormRequired.tsx,
    src/features/battle-log/BattleLogSheet.tsx,
    src/features/recipes/RecipeFormSheet.tsx
  </files>
  <read_first>
    src/features/spending/SpendingPage.tsx,
    src/features/factions/FactionSheet.tsx,
    src/features/goals/GoalSheet.tsx,
    src/features/paints/PaintSheet.tsx,
    src/features/units/UnitFormRequired.tsx,
    src/features/battle-log/BattleLogSheet.tsx,
    src/features/recipes/RecipeFormSheet.tsx
  </read_first>
  <action>
    Per D-03: In SpendingPage.tsx, find the error message paragraph (around line 58) that has class `text-muted-foreground` and change it to `text-destructive`. Single class swap, no structural change.

    Per D-04: Add the `autoFocus` attribute to the first text Input element in each of these 6 Sheet forms. Place `autoFocus` on the `Input` component inside `FormControl` (not on FormField or FormItem wrappers). Specific targets:
    - FactionSheet.tsx: `name` field Input
    - GoalSheet.tsx: `name` field Input
    - PaintSheet.tsx: `brand` field Input (first text input rendered)
    - UnitFormRequired.tsx: `name` field Input (NOT in UnitSheet.tsx — the name input is delegated to UnitFormRequired)
    - BattleLogSheet.tsx: `opponent_faction` field Input (NOT `battle_date` which is type="date" and pre-filled — focusing it opens the date picker unexpectedly)
    - RecipeFormSheet.tsx: `name` field Input

    Place `autoFocus` before the `{...field}` spread so it can be overridden by field props if needed, though in practice both positions work.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - SpendingPage.tsx error paragraph contains `text-destructive` and does NOT contain `text-muted-foreground` on that same element
    - FactionSheet.tsx contains `autoFocus` on an Input element
    - GoalSheet.tsx contains `autoFocus` on an Input element
    - PaintSheet.tsx contains `autoFocus` on the `brand` field's Input
    - UnitFormRequired.tsx contains `autoFocus` on the `name` field's Input
    - BattleLogSheet.tsx contains `autoFocus` on the `opponent_faction` field's Input (NOT on `battle_date`)
    - RecipeFormSheet.tsx contains `autoFocus` on an Input element
  </acceptance_criteria>
  <done>Spending error uses destructive color, all 6 Sheet forms focus first text input on open</done>
</task>

<task type="auto">
  <name>Task 3: React Query gcTime alignment (FBK-10)</name>
  <files>
    src/hooks/useGameData.ts,
    src/hooks/useDatasheet.ts,
    src/hooks/useJournalSessions.ts,
    src/hooks/useStrategyNote.ts,
    src/hooks/useUnitKeywords.ts,
    src/hooks/useUnitPhotos.ts,
    src/hooks/useUnitOverride.ts,
    src/hooks/useUnitDatabase.ts,
    src/hooks/useUdbMeta.ts,
    src/features/units/UnitFormRequired.tsx
  </files>
  <read_first>
    src/hooks/useGameData.ts,
    src/hooks/useDatasheet.ts,
    src/hooks/useJournalSessions.ts,
    src/hooks/useStrategyNote.ts,
    src/hooks/useUnitKeywords.ts,
    src/hooks/useUnitPhotos.ts,
    src/hooks/useUnitOverride.ts,
    src/hooks/useUnitDatabase.ts,
    src/hooks/useUdbMeta.ts,
    src/features/units/UnitFormRequired.tsx
  </read_first>
  <action>
    Per D-10: For every `useQuery` options object that contains `staleTime: Infinity`, add `gcTime: Infinity` on the line immediately following. This is a purely additive change — no existing lines removed.

    Expected occurrence counts per file:
    - useGameData.ts: 5 occurrences
    - useDatasheet.ts: 5 occurrences
    - useJournalSessions.ts: 2 occurrences
    - useStrategyNote.ts: 1 occurrence
    - useUnitKeywords.ts: 1 occurrence
    - useUnitPhotos.ts: 2 occurrences
    - useUnitOverride.ts: 1 occurrence
    - useUnitDatabase.ts: 8 occurrences
    - useUdbMeta.ts: 1 occurrence
    - UnitFormRequired.tsx: 1 occurrence (inline useQuery)

    Total: ~27 additions. After completing, run a grep to verify zero remaining `staleTime: Infinity` lines without a corresponding `gcTime: Infinity` on the next line.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep for `staleTime: Infinity` across src/ returns ~27 matches
    - grep for `gcTime: Infinity` across src/ returns ~27 matches (same count)
    - No file in src/hooks/ or src/features/units/UnitFormRequired.tsx has `staleTime: Infinity` without `gcTime: Infinity` in the same useQuery options object
    - pnpm build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>Every staleTime: Infinity hook also declares gcTime: Infinity — React Query cache entries are never silently evicted for data marked as never-stale</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries are crossed in this plan. All changes are UI-layer text, class, attribute, and configuration updates. No user input processing, no API changes, no auth changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | N/A | All changes | accept | UI-only cosmetic and config changes — no security surface |
</threat_model>

<verification>
pnpm build succeeds with zero TypeScript errors.
grep -r "staleTime: Infinity" src/ | wc -l matches grep -r "gcTime: Infinity" src/ | wc -l.
All 4 delete dialogs contain "Deleting..." string.
All 6 Sheet forms contain autoFocus attribute.
</verification>

<success_criteria>
- 4 delete dialogs show conditional pending text
- SpendingPage error uses text-destructive
- 6 Sheet forms have autoFocus on first text input
- JournalTab fires toast.success on session create
- SnapshotHistorySheet uses toast.success for delete
- All ~27 staleTime: Infinity hooks paired with gcTime: Infinity
- pnpm build green
</success_criteria>

<output>
Create `.planning/phases/128-feedback-hardening-form-ux/128-01-SUMMARY.md` when done
</output>

---
phase: 127-visual-consistency-pageheader-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/rules-hub/RulesHubPage.tsx
  - src/features/unit-database/DatabaseBrowserPage.tsx
  - src/features/factions/FactionsPage.tsx
  - src/features/goals/GoalsPage.tsx
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03]

must_haves:
  truths:
    - "D-01: Rules Hub page renders a PageHeader with title and subtitle, with border-b separator"
    - "D-01: Unit Database page renders a PageHeader with title and subtitle, with border-b separator"
    - "D-02: Factions page PageHeader includes a subtitle prop"
    - "D-03: GoalsPage section headings use text-sm font-semibold uppercase tracking-widest text-muted-foreground"
  artifacts:
    - path: "src/features/rules-hub/RulesHubPage.tsx"
      provides: "PageHeader with subtitle replacing bare h1"
      contains: "PageHeader"
    - path: "src/features/unit-database/DatabaseBrowserPage.tsx"
      provides: "PageHeader with subtitle replacing bare h1"
      contains: "PageHeader"
    - path: "src/features/factions/FactionsPage.tsx"
      provides: "PageHeader with subtitle prop added"
      contains: "subtitle"
  key_links:
    - from: "src/features/rules-hub/RulesHubPage.tsx"
      to: "src/components/common/PageHeader.tsx"
      via: "import"
      pattern: "import.*PageHeader.*from.*@/components/common/PageHeader"
    - from: "src/features/unit-database/DatabaseBrowserPage.tsx"
      to: "src/components/common/PageHeader.tsx"
      via: "import"
      pattern: "import.*PageHeader.*from.*@/components/common/PageHeader"
---

<objective>
Adopt the canonical PageHeader component on the 2 remaining pages that use bare h1 headings (Rules Hub, Unit Database), add a subtitle to Factions PageHeader, and standardize GoalsPage section headings to the Dashboard pattern.

Purpose: Every page header uses the shared PageHeader component, and Goals section headings match the established pattern.
Output: 4 files updated with consistent PageHeader usage and section heading classes.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-visual-consistency-pageheader-unification/127-CONTEXT.md
@.planning/phases/127-visual-consistency-pageheader-unification/127-PATTERNS.md
@.planning/phases/127-visual-consistency-pageheader-unification/127-UI-SPEC.md

<interfaces>
From src/components/common/PageHeader.tsx:
export interface PageHeaderProps {
  title: string;
  subtitle?: string;
  actions?: ReactNode;
}
export function PageHeader({ title, subtitle, actions }: PageHeaderProps)
// Renders: div.flex.items-center.justify-between.pb-6.border-b.border-border/40
//   h1.text-3xl.font-semibold.tracking-tight
//   p.text-sm.text-muted-foreground.mt-1 (when subtitle truthy)
//   div.flex.items-center.gap-2 (when actions truthy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PageHeader adoption on Rules Hub, Unit Database, and Factions subtitle</name>
  <files>src/features/rules-hub/RulesHubPage.tsx, src/features/unit-database/DatabaseBrowserPage.tsx, src/features/factions/FactionsPage.tsx</files>
  <read_first>
    src/features/rules-hub/RulesHubPage.tsx
    src/features/unit-database/DatabaseBrowserPage.tsx
    src/features/factions/FactionsPage.tsx
    src/components/common/PageHeader.tsx
  </read_first>
  <action>
    Per D-01: In RulesHubPage.tsx, add `import { PageHeader } from "@/components/common/PageHeader";` to the imports. Replace the bare `h1` element (`className="text-3xl font-semibold tracking-tight"` with text "Rules Hub") with `PageHeader` using title="Rules Hub" and subtitle="Browse army rules, stratagems, and detachments". Remove the old h1 element entirely.

    Per D-01: In DatabaseBrowserPage.tsx, add `import { PageHeader } from "@/components/common/PageHeader";` to the imports. Replace the bare `h1` element (`className="text-3xl font-semibold tracking-tight"` with text "Unit Database") with `PageHeader` using title="Unit Database" and subtitle="Browse canonical Warhammer 40,000 unit datasheets". Remove the old h1 element entirely.

    Per D-02: In FactionsPage.tsx, the PageHeader import already exists. Add `subtitle="Manage your army factions"` prop to the existing PageHeader component call, between the title and actions props.
  </action>
  <verify>
    <automated>pnpm build && grep -c "PageHeader" src/features/rules-hub/RulesHubPage.tsx | grep -v "^0$" && grep -c "PageHeader" src/features/unit-database/DatabaseBrowserPage.tsx | grep -v "^0$" && grep -c "subtitle" src/features/factions/FactionsPage.tsx | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - RulesHubPage.tsx contains `import { PageHeader } from "@/components/common/PageHeader"`
    - RulesHubPage.tsx contains `<PageHeader` with `title="Rules Hub"` and `subtitle="Browse army rules, stratagems, and detachments"`
    - RulesHubPage.tsx has zero `<h1` elements
    - DatabaseBrowserPage.tsx contains `import { PageHeader } from "@/components/common/PageHeader"`
    - DatabaseBrowserPage.tsx contains `<PageHeader` with `title="Unit Database"` and `subtitle="Browse canonical Warhammer 40,000 unit datasheets"`
    - DatabaseBrowserPage.tsx has zero `<h1` elements
    - FactionsPage.tsx PageHeader has `subtitle="Manage your army factions"` prop
    - `pnpm build` succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>Rules Hub and Unit Database pages use PageHeader with subtitle. Factions PageHeader includes subtitle. All three match the GoalsPage PageHeader pattern.</done>
</task>

<task type="auto">
  <name>Task 2: GoalsPage section heading standardization</name>
  <files>src/features/goals/GoalsPage.tsx</files>
  <read_first>
    src/features/goals/GoalsPage.tsx
    src/features/dashboard/DashboardPage.tsx
  </read_first>
  <action>
    Per D-03: Standardize all section headings in GoalsPage to the Dashboard pattern: `text-sm font-semibold uppercase tracking-widest text-muted-foreground` on a `p` element. Do NOT extract a SectionHeading component.

    - Change "Active Goals" h2 (className="text-base font-semibold mb-3") to `p` with className="text-sm font-semibold uppercase tracking-widest text-muted-foreground". Remove the mb-3 class (spacing comes from parent gap).
    - Change "Completed" h2 (className="text-base font-semibold mb-3 mt-6 text-battle-gold") to `p` with className="text-sm font-semibold uppercase tracking-widest text-muted-foreground". Remove text-battle-gold, mb-3, and mt-6 classes entirely.
    - Change "Missed" h2 (className="text-base font-semibold mb-3 mt-6 text-muted-foreground") to `p` with className="text-sm font-semibold uppercase tracking-widest text-muted-foreground". Remove mb-3 and mt-6 classes.

    Note: SpendingPage and DataHealthPage section headings are handled in Plan 02 alongside their spacing fixes.
  </action>
  <verify>
    <automated>pnpm build && grep -c "text-sm font-semibold uppercase tracking-widest" src/features/goals/GoalsPage.tsx | grep -v "^0$" && grep -c "text-battle-gold" src/features/goals/GoalsPage.tsx | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - GoalsPage.tsx has zero `h2` elements with className containing "text-base"
    - GoalsPage.tsx has zero occurrences of "text-battle-gold"
    - GoalsPage.tsx has zero occurrences of "mb-3" on heading elements
    - GoalsPage.tsx contains 3 p elements with className="text-sm font-semibold uppercase tracking-widest text-muted-foreground"
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>All GoalsPage section headings use the identical Dashboard pattern. No text-battle-gold, no text-base deviations, no mb/mt margin hacks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — this plan modifies only Tailwind CSS classes and HTML element tags in presentational components.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | N/A | N/A | accept | No security surface — visual class changes only, no user input, no data flow changes |
</threat_model>

<verification>
pnpm build succeeds with zero TypeScript errors.
grep for bare `<h1` in RulesHubPage.tsx and DatabaseBrowserPage.tsx returns 0 matches.
grep for `text-battle-gold` in GoalsPage.tsx returns 0 matches.
grep for `text-base font-semibold` in GoalsPage.tsx returns 0 matches.
</verification>

<success_criteria>
Rules Hub and Unit Database pages display PageHeader with title, subtitle, and border-b separator. Factions PageHeader shows subtitle. GoalsPage section headings use the standardized Dashboard pattern.
</success_criteria>

<output>
Create `.planning/phases/127-visual-consistency-pageheader-unification/127-01-SUMMARY.md` when done
</output>

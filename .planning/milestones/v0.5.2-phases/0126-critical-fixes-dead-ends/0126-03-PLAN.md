---
phase: 126-critical-fixes-dead-ends
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/army-lists/ArmyListDetailPage.tsx
  - src/features/recipes/RecipesPage.tsx
autonomous: true
requirements: [FIX-03, FIX-04, FIX-10]

must_haves:
  truths:
    - "Notes save no-op produces no toast"
    - "RecipesPage shows error state with retry when query fails"
    - "ArmyListDetailPage shows not-found message when list is null and loading is false"
    - "ArmyListDetailPage shows skeleton only while loading"
  artifacts:
    - path: "src/features/army-lists/ArmyListDetailPage.tsx"
      provides: "No-op toast removed, isLoading guard for not-found"
      contains: "listLoading"
    - path: "src/features/recipes/RecipesPage.tsx"
      provides: "Error state with AlertCircle and refetch"
      contains: "isError"
  key_links:
    - from: "src/features/recipes/RecipesPage.tsx"
      to: "src/hooks/useRecipes.ts"
      via: "isError and refetch destructuring"
      pattern: "isError.*refetch"
---

<objective>
Fix incorrect feedback on Army List notes save, add missing error state to RecipesPage, and distinguish loading from not-found on ArmyListDetailPage.

Purpose: No-op saves should be silent, query errors should show retry UI, and deleted lists should not show infinite skeleton.
Output: Three targeted fixes across two files.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0126-critical-fixes-dead-ends/0126-UI-SPEC.md

<interfaces>
From src/features/army-lists/ArmyListDetailPage.tsx:
```typescript
// Line 255: const { data: list } = useArmyList(listId);
// Line 256: const { data: units, isLoading } = useArmyListWithUnits(listId);
// Line 330: const [notesDraft, setNotesDraft] = useState(list?.notes ?? "");
// Line 433-438: handleSaveListNotes — early return with toast.success on no-op
// Line 526-543: !list guard shows skeleton unconditionally
```

From src/features/recipes/RecipesPage.tsx:
```typescript
// Line 37: const { data: recipes = [], isLoading } = useRecipes();
// Does NOT currently destructure isError or refetch
```

From src/hooks/useRecipes.ts (useRecipes hook):
```typescript
// Returns standard useQuery result — isError, refetch available via destructuring
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Army List notes no-op toast and loading vs not-found</name>
  <files>src/features/army-lists/ArmyListDetailPage.tsx</files>
  <read_first>
    - src/features/army-lists/ArmyListDetailPage.tsx lines 250-260 (useArmyList destructuring)
    - src/features/army-lists/ArmyListDetailPage.tsx lines 430-445 (handleSaveListNotes)
    - src/features/army-lists/ArmyListDetailPage.tsx lines 520-543 (!list guard and skeleton)
  </read_first>
  <action>
    Per D-04 (FIX-03): In `handleSaveListNotes` (line 433-438), remove the `toast.success("Notes saved.");` call on line 436 inside the early return branch. Keep the `return;` statement — the early return should be silent.

    Per D-11 (FIX-10): At line 255, change `const { data: list } = useArmyList(listId);` to `const { data: list, isLoading: listLoading } = useArmyList(listId);`. Use `listLoading` (not `isLoading`) to avoid collision with the existing `isLoading` from `useArmyListWithUnits` on line 256.

    Replace the `!list` guard block (lines 526-543) with a two-part check:
    1. If `listLoading` is true, render the existing skeleton (keep the current skeleton JSX unchanged).
    2. If `listLoading` is false and `!list`, render a not-found state: a div with className "flex flex-col gap-6 p-6" containing a back button (Button variant="ghost" size="sm" asChild wrapping Link to="/army-lists" with ArrowLeft h-4 w-4 mr-2 and "Back to Army Lists" text) and a centered text block (div className="text-center py-12") with h2 "List not found" (text-lg font-semibold) and p "This army list may have been deleted." (text-sm text-muted-foreground mt-1).

    ArrowLeft and Link are already imported in this file.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - handleSaveListNotes early return branch contains no toast.success call (just `return;`)
    - useArmyList destructures `isLoading: listLoading`
    - When listLoading is true, skeleton renders (existing JSX preserved)
    - When listLoading is false and !list, "List not found" heading and "Back to Army Lists" link render
    - No TypeScript errors (no isLoading name collision)
  </acceptance_criteria>
  <done>No-op notes save is silent; deleted army lists show "List not found" instead of infinite skeleton</done>
</task>

<task type="auto">
  <name>Task 2: Add error state to RecipesPage</name>
  <files>src/features/recipes/RecipesPage.tsx</files>
  <read_first>
    - src/features/recipes/RecipesPage.tsx (first 50 lines — useRecipes destructuring at line 37, isLoading usage)
    - src/features/recipes/RecipesPage.tsx (search for "isLoading" to find the loading guard location)
  </read_first>
  <action>
    Per D-08 (FIX-04): Change the useRecipes destructuring on line 37 from `const { data: recipes = [], isLoading } = useRecipes();` to `const { data: recipes = [], isLoading, isError, refetch } = useRecipes();`.

    Add an error state check BEFORE the loading check (or immediately after it, before the main content). When `isError` is true, return a centered error block:
    - Outer div: className "flex flex-col items-center justify-center py-12 gap-4"
    - AlertCircle icon: className "h-12 w-12 text-destructive"
    - Text container div with className "text-center":
      - h2 with className "text-lg font-semibold" and text "Failed to load recipes"
      - p with className "text-sm text-muted-foreground mt-1" and text "Check your connection and try again."
    - Button with variant="outline" and onClick={() => refetch()} and text "Reload Recipes"

    Add import for AlertCircle from lucide-react (add to existing lucide imports if any).

    The error check should be placed after the hook calls but before the filter/content rendering, similar to how isLoading is checked.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - useRecipes destructures isError and refetch
    - isError branch renders AlertCircle h-12 w-12 text-destructive
    - isError branch renders "Failed to load recipes" heading
    - isError branch renders "Check your connection and try again." description
    - isError branch renders Button variant="outline" with text "Reload Recipes" calling refetch()
    - AlertCircle imported from lucide-react
  </acceptance_criteria>
  <done>RecipesPage shows a visible error state with retry button when query fails, instead of showing empty state</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. UI rendering logic changes only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-03 | n/a | UI-only changes | accept | No security surface |
</threat_model>

<verification>
- Notes save on army list with unchanged notes produces no toast
- RecipesPage shows error UI (not empty grid) when useRecipes fails
- Deleted army list shows "List not found" message with back link (not infinite skeleton)
- `pnpm build` succeeds
</verification>

<success_criteria>
- No toast.success in handleSaveListNotes early return
- RecipesPage has isError guard with AlertCircle + refetch button
- ArmyListDetailPage distinguishes listLoading from !list
</success_criteria>

<output>
Create `.planning/phases/0126-critical-fixes-dead-ends/0126-03-SUMMARY.md` when done
</output>

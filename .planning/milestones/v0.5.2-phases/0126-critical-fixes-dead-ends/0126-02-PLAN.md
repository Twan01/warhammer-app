---
phase: 126-critical-fixes-dead-ends
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/globals.css
  - src/app/settings/page.tsx
  - src/features/data-health/DataHealthPage.tsx
autonomous: true
requirements: [FIX-05, FIX-06, FIX-11]

must_haves:
  truths:
    - "Settings page uses PageHeader with text-3xl and border-b"
    - "Data Health page uses PageHeader with text-3xl and border-b"
    - "All dark-only CSS tokens have :root fallbacks"
    - "Dark mode scrollbar is thin, zinc-colored, and rounded"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "Light-mode token fallbacks and scrollbar styling"
      contains: "--forge-black"
    - path: "src/app/settings/page.tsx"
      provides: "PageHeader usage"
      contains: "PageHeader"
    - path: "src/features/data-health/DataHealthPage.tsx"
      provides: "PageHeader usage"
      contains: "PageHeader"
  key_links:
    - from: "src/app/settings/page.tsx"
      to: "src/components/common/PageHeader.tsx"
      via: "import"
      pattern: "import.*PageHeader"
---

<objective>
Fix CSS token gaps, inconsistent page headers, and missing scrollbar styling.

Purpose: Ensure tokens never resolve to undefined, page headers are visually consistent, and the dark scrollbar matches the app's zinc theme.
Output: Light-mode fallbacks in :root, PageHeader on Settings/DataHealth, custom scrollbar in .dark.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0126-critical-fixes-dead-ends/0126-UI-SPEC.md

<interfaces>
From src/components/common/PageHeader.tsx:
```typescript
export interface PageHeaderProps {
  title: string;
  subtitle?: string;
  actions?: ReactNode;
}
// Renders: text-3xl font-semibold tracking-tight + border-b border-border/40
```

From src/styles/globals.css :root (lines 7-40):
- Standard shadcn tokens defined
- --faction-accent defined
- Missing: --forge-black, --panel-elevated, --panel-surface, --battle-gold

From src/styles/globals.css .dark (lines 42-77):
- --forge-black: hsl(var(--background))
- --panel-elevated: hsl(var(--card))
- --panel-surface: hsl(var(--secondary))
- --battle-gold: oklch(0.78 0.17 85)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add light-mode token fallbacks and scrollbar styling to globals.css</name>
  <files>src/styles/globals.css</files>
  <read_first>
    - src/styles/globals.css (full file, 182 lines — :root block lines 7-40, .dark block lines 42-77)
  </read_first>
  <action>
    Per D-10: In the `:root` block (after line 39, before the closing brace), add four token definitions:
    - `--forge-black: hsl(var(--background));` (maps to white in light mode)
    - `--panel-elevated: hsl(var(--card));` (maps to white card in light mode)
    - `--panel-surface: hsl(var(--secondary));` (maps to zinc-100 equivalent)
    - `--battle-gold: oklch(0.55 0.17 85);` (darker gold for white background readability)

    Per D-12: After the closing brace of the `body` rule (after line 131), add scrollbar CSS rules:

    `.dark ::-webkit-scrollbar` with width: 6px and height: 6px.
    `.dark ::-webkit-scrollbar-track` with background: hsl(240 5.9% 10%) (zinc-900).
    `.dark ::-webkit-scrollbar-thumb` with background: hsl(240 3.7% 30%) (zinc-700), border-radius: 9999px.
    `.dark ::-webkit-scrollbar-thumb:hover` with background: hsl(240 3.7% 40%) (zinc-600).
    `.dark` selector (separate rule) with scrollbar-width: thin and scrollbar-color: hsl(240 3.7% 30%) hsl(240 5.9% 10%) for Firefox.

    Place scrollbar rules before the @media print block.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - :root block contains --forge-black, --panel-elevated, --panel-surface, --battle-gold definitions
    - --battle-gold in :root uses oklch(0.55 0.17 85) (not the dark-mode value 0.78)
    - .dark ::-webkit-scrollbar rule exists with width: 6px
    - .dark ::-webkit-scrollbar-track rule uses hsl(240 5.9% 10%)
    - .dark ::-webkit-scrollbar-thumb rule uses hsl(240 3.7% 30%) with border-radius: 9999px
    - .dark rule contains scrollbar-width: thin and scrollbar-color
    - No duplicate .dark blocks for existing tokens (scrollbar rules in new .dark blocks only)
  </acceptance_criteria>
  <done>All dark-only tokens have :root fallbacks; dark mode scrollbar is 6px zinc-themed with rounded thumb</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline headers with PageHeader on Settings and Data Health</name>
  <files>src/app/settings/page.tsx, src/features/data-health/DataHealthPage.tsx</files>
  <read_first>
    - src/app/settings/page.tsx (first 40 lines — h1 at line 14 with "text-xl font-semibold")
    - src/features/data-health/DataHealthPage.tsx (first 30 lines — h1 at line 23 with "text-xl font-semibold")
    - src/components/common/PageHeader.tsx (full file, 34 lines — PageHeaderProps interface)
  </read_first>
  <action>
    Per D-09: In SettingsPage (src/app/settings/page.tsx):
    - Add import: `import { PageHeader } from "@/components/common/PageHeader";`
    - Replace `<h1 className="text-xl font-semibold">Settings</h1>` with `<PageHeader title="Settings" />`

    In DataHealthPage (src/features/data-health/DataHealthPage.tsx):
    - Add import: `import { PageHeader } from "@/components/common/PageHeader";`
    - Replace `<h1 className="text-xl font-semibold">Data Health</h1>` with `<PageHeader title="Data Health" />`

    No other changes needed — PageHeader already renders text-3xl font-semibold tracking-tight with border-b border-border/40.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SettingsPage imports PageHeader from "@/components/common/PageHeader"
    - SettingsPage renders `<PageHeader title="Settings" />` (no inline h1)
    - DataHealthPage imports PageHeader from "@/components/common/PageHeader"
    - DataHealthPage renders `<PageHeader title="Data Health" />` (no inline h1)
    - No remaining `<h1` elements in either file
  </acceptance_criteria>
  <done>Settings and Data Health pages use the shared PageHeader component with text-3xl and border-b separator</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. CSS changes and component swaps only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-02 | n/a | CSS/UI-only changes | accept | No security surface |
</threat_model>

<verification>
- Settings page header matches other pages (text-3xl, border-b)
- Data Health page header matches other pages
- Light mode doesn't show undefined token errors
- Dark mode scrollbar appears as thin zinc-colored bars
- `pnpm build` succeeds
</verification>

<success_criteria>
- Both pages use PageHeader component (grep confirms no inline h1)
- :root has all 4 token fallbacks
- Scrollbar rules present in globals.css under .dark
</success_criteria>

<output>
Create `.planning/phases/0126-critical-fixes-dead-ends/0126-02-SUMMARY.md` when done
</output>

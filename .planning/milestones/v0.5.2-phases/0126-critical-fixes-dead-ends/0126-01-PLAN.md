---
phase: 126-critical-fixes-dead-ends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/painting-mode/StepFocalView.tsx
  - src/features/painting-mode/PaintingModeView.tsx
  - src/app/painting-mode/page.tsx
autonomous: true
requirements: [FIX-01, FIX-02]

must_haves:
  truths:
    - "Painting Mode completion screen shows an Exit button and Escape hint"
    - "Painting Mode assignment-not-found screen shows a Go Back button and Escape hint"
    - "StepFocalView normal step view shows an Esc to exit hint in the footer"
  artifacts:
    - path: "src/features/painting-mode/StepFocalView.tsx"
      provides: "Exit button on completion, Esc hint on all views"
      contains: "onExit"
    - path: "src/app/painting-mode/page.tsx"
      provides: "Go Back button on not-found screen"
      contains: "Go Back"
  key_links:
    - from: "src/app/painting-mode/page.tsx"
      to: "src/features/painting-mode/PaintingModeView.tsx"
      via: "onExit prop"
      pattern: "onExit"
    - from: "src/features/painting-mode/PaintingModeView.tsx"
      to: "src/features/painting-mode/StepFocalView.tsx"
      via: "onExit prop threading"
      pattern: "onExit"
---

<objective>
Eliminate dead-end screens in Painting Mode. The completion screen and the assignment-not-found screen currently have no exit affordance, trapping users.

Purpose: Users must always have a visible way to leave Painting Mode.
Output: Exit button on completion, Go Back button on not-found, Escape hints on all views.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0126-critical-fixes-dead-ends/0126-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/features/painting-mode/StepFocalView.tsx:
```typescript
export interface StepFocalViewProps {
  currentStep: RecipeStep | undefined;
  paint: Paint | undefined;
  stepPhotoUrl: string | undefined;
  isCompleted: boolean;
  onMarkDone: () => void;
  onMarkDoneWithSession: () => void;
  goPrev: () => void;
  goNext: () => void;
  canGoPrev: boolean;
  canGoNext: boolean;
  currentIndex: number;
  totalSteps: number;
  sectionName: string | null;
  isAllComplete: boolean;
  isMutating?: boolean;
}
```

From src/features/painting-mode/PaintingModeView.tsx:
```typescript
interface PaintingModeViewProps {
  state: ReturnType<typeof usePaintingModeState>;
  onMarkDone: () => void;
  onMarkDoneWithSession: () => void;
  recipeId: number;
  isMutating: boolean;
}
```

From src/app/painting-mode/page.tsx:
```typescript
// handleExit navigates to "/" — already defined at line 109
const handleExit = () => { navigate({ to: "/" }); };
// Escape hotkey already bound to handleExit at line 119
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onExit prop to StepFocalView and PaintingModeView</name>
  <files>src/features/painting-mode/StepFocalView.tsx, src/features/painting-mode/PaintingModeView.tsx</files>
  <read_first>
    - src/features/painting-mode/StepFocalView.tsx (full file, 174 lines — current completion screen at line 42-48, nav bar at line 121-148)
    - src/features/painting-mode/PaintingModeView.tsx (full file, 181 lines — StepFocalView rendered at line 157-177)
  </read_first>
  <action>
    Per D-01: Add `onExit?: () => void` to `StepFocalViewProps` interface. In the `isAllComplete` branch (line 42-48), add a `Button` (default variant) with text "Exit Painting Mode" that calls `onExit`, plus a `p` element with className "text-xs text-muted-foreground" containing "Press Escape to exit" below the button. Both wrapped in a Fragment, rendered only when `onExit` is defined.

    Per D-03: In the normal step view (the return block starting at line 55), add a `p` element with className "text-xs text-muted-foreground text-center" containing "Esc to exit" below the action buttons div (after line 171, before the closing div). This is always visible.

    In PaintingModeView: Add `onExit?: () => void` to `PaintingModeViewProps` interface. Destructure it in the component. Pass `onExit={onExit}` to the StepFocalView JSX at line 157.

    Import `Button` from `@/components/ui/button` in StepFocalView (already imported). No new imports needed.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting-mode/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - StepFocalViewProps interface contains `onExit?: () => void`
    - PaintingModeViewProps interface contains `onExit?: () => void`
    - isAllComplete branch renders Button with text "Exit Painting Mode" and onClick={onExit}
    - isAllComplete branch renders p.text-xs.text-muted-foreground with "Press Escape to exit"
    - Normal step view renders p.text-xs.text-muted-foreground with "Esc to exit"
    - PaintingModeView passes onExit to StepFocalView
  </acceptance_criteria>
  <done>StepFocalView completion screen has exit button and escape hint; normal step view has escape hint; PaintingModeView threads onExit prop</done>
</task>

<task type="auto">
  <name>Task 2: Wire onExit in page.tsx and add Go Back to not-found screen</name>
  <files>src/app/painting-mode/page.tsx</files>
  <read_first>
    - src/app/painting-mode/page.tsx (full file, 164 lines — handleExit at line 109, not-found screen at line 127-137, PaintingModeView render at line 143-149)
  </read_first>
  <action>
    Per D-01: Pass `onExit={handleExit}` prop to PaintingModeView at line 143.

    Per D-02: In the `!assignment` block (line 127-137), add a Button with variant="ghost" size="sm" containing ArrowLeft icon (h-4 w-4 mr-2) and text "Go Back". The onClick should call `navigate({ to: "/" })` (same as handleExit — using navigate directly since handleExit is defined later in the flow but same function body). Place the button above the existing centered text-center div. Also add a `p` element with className "text-xs text-muted-foreground mt-2" containing "Press Escape to exit" after the existing description paragraph.

    Import ArrowLeft from lucide-react (add to existing lucide import if any, or add new import). Button is not currently imported in page.tsx — add import from `@/components/ui/button`.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting-mode/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PaintingModeView receives onExit={handleExit} prop
    - Not-found screen renders Button variant="ghost" size="sm" with ArrowLeft icon and "Go Back" text
    - Not-found screen renders "Press Escape to exit" hint in text-xs text-muted-foreground
    - Button imports added to page.tsx
    - ArrowLeft imported from lucide-react
  </acceptance_criteria>
  <done>Painting Mode completion screen has visible exit, not-found screen has Go Back button and Escape hint, no dead ends remain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. All changes are UI-only within existing React components.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | n/a | UI-only changes | accept | No security surface — button/text additions only |
</threat_model>

<verification>
- Painting Mode completion screen shows "Exit Painting Mode" button and "Press Escape to exit" text
- Not-found screen shows "Go Back" button with ArrowLeft and "Press Escape to exit" text
- Normal step view shows "Esc to exit" hint
- Escape key still works on all screens (existing hotkey unchanged)
- `pnpm build` succeeds with no TypeScript errors
</verification>

<success_criteria>
- No dead-end screens in Painting Mode
- Exit button on completion calls handleExit (navigates to /)
- Go Back button on not-found navigates away
- Escape hint visible on all three screen states
</success_criteria>

<output>
Create `.planning/phases/0126-critical-fixes-dead-ends/0126-01-SUMMARY.md` when done
</output>

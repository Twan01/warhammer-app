---
phase: 102-smart-context-pre-filling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/recipes/RecipeFormSheet.tsx
  - tests/recipes/RecipeFormSheetPreFill.test.tsx
autonomous: true
requirements:
  - SCP-01
  - SCP-03

must_haves:
  truths:
    - "RecipeFormSheet pre-fills faction_id when defaultFactionId prop is provided"
    - "RecipeFormSheet pre-fills unit_id when defaultUnitId prop is provided"
    - "Pre-filled values are editable — user can change faction or unit after form opens"
    - "When no default props are provided, form behaves identically to current behavior (null defaults)"
  artifacts:
    - path: "src/features/recipes/RecipeFormSheet.tsx"
      provides: "Recipe form with optional defaultFactionId and defaultUnitId props"
      contains: "defaultFactionId"
    - path: "tests/recipes/RecipeFormSheetPreFill.test.tsx"
      provides: "Tests for pre-fill and editability behavior"
      min_lines: 50
  key_links:
    - from: "src/features/recipes/RecipeFormSheet.tsx"
      to: "RecipeFormSheetProps"
      via: "interface extension"
      pattern: "defaultFactionId.*number.*null"
---

<objective>
Add optional defaultFactionId and defaultUnitId props to RecipeFormSheet so that when opened from a unit context, the form pre-populates faction and unit fields. When props are absent (QuickAdd, RecipesPage), behavior is unchanged.

Purpose: Implements SCP-01 (recipe form pre-fills faction from unit context) and SCP-03 (pre-filled values are editable). Per D-10, the props exist on the component even though no current call site passes them — UnitDetailSheet does not render RecipeFormSheet today.
Output: Modified RecipeFormSheet.tsx with new props, new test file validating pre-fill and editability.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-smart-context-pre-filling/102-CONTEXT.md
@.planning/phases/102-smart-context-pre-filling/102-RESEARCH.md
@.planning/phases/102-smart-context-pre-filling/102-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/features/recipes/RecipeFormSheet.tsx (CURRENT - before changes):
```typescript
export interface RecipeFormSheetProps {
  open: boolean;
  recipe: PaintingRecipe | null;
  onClose: () => void;
}

const DEFAULT_VALUES: RecipeFormValues = {
  name: "",
  faction_id: null,
  unit_id: null,
  area: null,
  notes: null,
  tutorial_link: null,
  style: null,
  surface: null,
  effect: null,
  difficulty: null,
  estimated_minutes: null,
  result_photo_path: null,
};

function buildDefaults(recipe: PaintingRecipe | null): RecipeFormValues { ... }

// useEffect on line 144:
form.reset(buildDefaults(recipe));
```

From src/features/recipes/recipeSchema.ts:
```typescript
export type RecipeFormValues = z.infer<typeof recipeSchema>;
// faction_id: z.number().nullable()
// unit_id: z.number().nullable()
```

From tests/recipes/RecipeDetailSheetPaint.test.tsx (test analog):
```typescript
// Mock pattern: vi.mock all hooks used by component
// Wrapper: QueryClientProvider with retry:false
// Render: <RecipeFormSheet open={true} recipe={null} onClose={vi.fn()} />
```

Call sites that will NOT change (per D-02, D-09):
- AppLayout.tsx line 46: <RecipeFormSheet open={...} recipe={null} onClose={...} /> (QuickAdd, no context)
- RecipesPage.tsx line 256: <RecipeFormSheet open={...} recipe={editRecipe} onClose={...} /> (toolbar)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add defaultFactionId and defaultUnitId props to RecipeFormSheet</name>
  <files>src/features/recipes/RecipeFormSheet.tsx</files>
  <read_first>
    src/features/recipes/RecipeFormSheet.tsx
    src/features/recipes/recipeSchema.ts
  </read_first>
  <action>
    Extend RecipeFormSheetProps interface (line 64-68) with two optional props per D-10:
    - defaultFactionId?: number | null
    - defaultUnitId?: number | null

    Update the buildDefaults function signature (line 85) to accept defaultFactionId and defaultUnitId as optional parameters. In the create-mode branch (when recipe is null), return a spread of DEFAULT_VALUES with faction_id set to defaultFactionId ?? null and unit_id set to defaultUnitId ?? null. The edit-mode branch (when recipe is not null) remains unchanged — edit always uses the recipe's own values.

    Update the component function signature (line 111) to destructure defaultFactionId and defaultUnitId from props.

    Update the useForm call (line 124-127) to pass the new params: buildDefaults(recipe, defaultFactionId, defaultUnitId).

    Update the useEffect reset call (line 145) to pass the new params: form.reset(buildDefaults(recipe, defaultFactionId, defaultUnitId)).

    Per D-07: No special visual treatment for pre-filled fields. Per D-08: Standard Select controls are inherently editable. No UI changes needed.

    Per D-02/D-09: Existing call sites (AppLayout, RecipesPage) do not pass these props — TypeScript's optional parameter handles this correctly with no changes needed at call sites.
  </action>
  <acceptance_criteria>
    - RecipeFormSheetProps interface has defaultFactionId?: number | null and defaultUnitId?: number | null
    - buildDefaults accepts 3 params: recipe, defaultFactionId, defaultUnitId
    - buildDefaults returns faction_id: defaultFactionId ?? null when recipe is null
    - buildDefaults returns unit_id: defaultUnitId ?? null when recipe is null
    - buildDefaults ignores defaultFactionId/defaultUnitId when recipe is not null (edit mode)
    - Component function destructures defaultFactionId and defaultUnitId
    - useForm call passes all 3 params to buildDefaults
    - useEffect reset call passes all 3 params to buildDefaults
    - pnpm build completes without TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>RecipeFormSheet accepts defaultFactionId and defaultUnitId props; create mode uses them as initial values; edit mode ignores them; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Test RecipeFormSheet pre-fill and editability</name>
  <files>tests/recipes/RecipeFormSheetPreFill.test.tsx</files>
  <read_first>
    tests/recipes/RecipeDetailSheetPaint.test.tsx
    src/features/recipes/RecipeFormSheet.tsx
  </read_first>
  <action>
    Create tests/recipes/RecipeFormSheetPreFill.test.tsx following the mock pattern from the analog test file RecipeDetailSheetPaint.test.tsx.

    Mock all hooks used by RecipeFormSheet: useFactions (return [{id:10,name:"Ultramarines"},{id:20,name:"Tau Empire"}]), useUnits (return [{id:1,name:"Intercessors",faction_id:10},{id:2,name:"Crisis Suits",faction_id:20}] plus all required Unit fields), usePaints (return []), useRecipePaints (return {data:[]} plus RECIPE_PAINTS_KEY, STEP_COUNTS_KEY, RECIPE_AVAILABILITY_KEY, RECIPE_SWATCH_KEY), useRecipeSections (return {data:[]} plus RECIPE_SECTIONS_KEY, SECTION_COUNTS_KEY), useRecipes (return RECIPES_KEY, RECIPE_KEY). Mock Tauri plugins: @tauri-apps/plugin-dialog (open), @tauri-apps/plugin-fs (readFile, writeFile, BaseDirectory). Mock @/db/queries/recipes (saveRecipeGraph).

    Create QueryClient wrapper with retry:false.

    Test cases for SCP-01:
    1. "pre-fills faction when defaultFactionId is provided" — render RecipeFormSheet with open=true, recipe=null, defaultFactionId=10. Assert the faction Select trigger text content contains "Ultramarines".
    2. "pre-fills unit when defaultUnitId is provided" — render with defaultFactionId=10, defaultUnitId=1. Assert the unit Select trigger text content contains "Intercessors".
    3. "does not pre-fill when default props are absent" — render with open=true, recipe=null, no default props. Assert faction Select trigger shows placeholder text "Select a faction" (or similar).

    Test cases for SCP-03:
    4. "pre-filled faction is editable" — render with defaultFactionId=10. Verify the faction Select is not disabled (check that the trigger element does not have aria-disabled or disabled attribute).

    Use describe block labeled "SCP-01/SCP-03: RecipeFormSheet pre-fill".
  </action>
  <acceptance_criteria>
    - Test file exists at tests/recipes/RecipeFormSheetPreFill.test.tsx
    - At least 3 test cases covering pre-fill with defaults, pre-fill without defaults, and editability
    - All hooks used by RecipeFormSheet are mocked (useFactions, useUnits, usePaints, useRecipePaints, useRecipeSections, useRecipes)
    - Tauri plugin mocks present (plugin-dialog, plugin-fs)
    - saveRecipeGraph mock present
    - pnpm test -- tests/recipes/RecipeFormSheetPreFill.test.tsx passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/recipes/RecipeFormSheetPreFill.test.tsx -x</automated>
  </verify>
  <done>All pre-fill tests pass: faction pre-populates when defaultFactionId provided, unit pre-populates when defaultUnitId provided, no pre-fill when props absent, pre-filled fields are editable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All changes are client-side prop threading within existing React components. No new data flows, API calls, or user input surfaces.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Spoofing | defaultFactionId prop | accept | Props are passed from parent components within the same React tree — no external input vector. Values are IDs that get validated by the existing Select options list. |
</threat_model>

<verification>
pnpm build passes (TypeScript check)
pnpm test -- tests/recipes/RecipeFormSheetPreFill.test.tsx -x passes
</verification>

<success_criteria>
RecipeFormSheet accepts optional defaultFactionId and defaultUnitId props that pre-populate create-mode form fields. Existing call sites (AppLayout QuickAdd, RecipesPage toolbar) are unaffected. Pre-filled fields are editable. Tests validate all behaviors.
</success_criteria>

<output>
Create `.planning/phases/102-smart-context-pre-filling/102-01-SUMMARY.md` when done
</output>

---
phase: 100-query-layer-automation
plan: 02
type: execute
wave: 2
depends_on:
  - 100-01
files_modified:
  - src/db/queries/recipeAssignments.ts
  - tests/painting/syncDerivedStatuses.test.ts
  - tests/painting/recipeAssignments.test.ts
autonomous: true
requirements:
  - SAD-01
  - SAD-02
  - SAD-04
  - APL-01
  - APL-02
  - APL-03
must_haves:
  truths:
    - "Completing all assembly section steps auto-sets status_assembly = 1"
    - "Basing derivation uses section_type = 'basing' with name-LIKE fallback"
    - "Varnish derivation uses section_type = 'varnish' with name-LIKE fallback"
    - "Override flag = 1 prevents auto-derivation for that status field"
    - "Assigning a recipe sets is_active_project = 1 on the unit"
    - "100% painting completion auto-clears is_active_project to 0"
    - "syncDerivedStatuses never sets is_active_project to 1"
  artifacts:
    - path: "src/db/queries/recipeAssignments.ts"
      provides: "Full automation logic: assembly derivation, section_type migration, override guards, is_active_project lifecycle"
      exports: ["createAssignment", "bulkCreateAssignments"]
    - path: "tests/painting/syncDerivedStatuses.test.ts"
      provides: "Unit tests for all derivation paths and override guards"
      min_lines: 80
    - path: "tests/painting/recipeAssignments.test.ts"
      provides: "Extended tests for is_active_project auto-set in createAssignment and bulkCreateAssignments"
  key_links:
    - from: "src/db/queries/recipeAssignments.ts"
      to: "units table"
      via: "syncDerivedStatuses UPDATE with override guards"
      pattern: "status_assembly_override"
    - from: "src/db/queries/recipeAssignments.ts"
      to: "units table"
      via: "createAssignment is_active_project write"
      pattern: "is_active_project = 1"
---

<objective>
Implement the core automation logic: extend syncDerivedStatuses() with assembly derivation, section_type-first matching, override guards, and is_active_project lifecycle. Wire is_active_project auto-set into createAssignment/bulkCreateAssignments. Write comprehensive unit tests.

Purpose: This is the heart of Phase 100 -- the query-layer logic that eliminates manual status updates for assembly, basing, and varnish milestones, and automates active project tracking.

Output: Fully functional auto-derivation with override guards, is_active_project lifecycle, and test coverage.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-query-layer-automation/100-CONTEXT.md
@.planning/phases/100-query-layer-automation/100-RESEARCH.md
@.planning/phases/100-query-layer-automation/100-PATTERNS.md
@.planning/phases/100-query-layer-automation/100-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan uses -->

From src/types/unit.ts (after Plan 01):
- Unit.status_assembly_override: 0 | 1
- Unit.status_basing_override: 0 | 1
- Unit.status_varnished_override: 0 | 1

From src/types/recipeSection.ts (after Plan 01):
- SECTION_TYPES includes "assembly", "basing", "varnish"

From src/db/queries/recipeAssignments.ts (current state -- to be modified):
- syncDerivedStatuses(db, unitId) is a private async function at ~line 211
- It reads painting_percentage, checks basing/varnish via name-LIKE, writes UPDATE
- createAssignment(input) at ~line 96: INSERT + syncPaintingPercentageByUnitId
- bulkCreateAssignments(unitIds, recipeId) at ~line 167: loop of INSERT + sync
- syncPaintingPercentageByUnitId(db, unitId) calls syncDerivedStatuses after UPDATE

From tests/painting/recipeAssignments.test.ts (existing test patterns):
- vi.mock("@/db/client") with executeMock / selectMock
- Import after vi.mock so mocked client is used
- beforeEach resets mocks and sets default resolved values
- SQL assertions via executeMock.mock.calls[N][0].includes(...)
- bulkCreateAssignments test at ~line 217 asserts toHaveBeenCalledTimes(6) for 3 unitIds
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create syncDerivedStatuses Test File</name>
  <files>
    tests/painting/syncDerivedStatuses.test.ts,
    src/db/queries/recipeAssignments.ts
  </files>
  <read_first>
    tests/painting/recipeAssignments.test.ts,
    src/db/queries/recipeAssignments.ts
  </read_first>
  <behavior>
    - SAD-01: assembly sections all complete -> UPDATE includes status_assembly with value derived as 1
    - SAD-01: assembly sections incomplete -> UPDATE includes status_assembly with value 0
    - SAD-01: no assembly sections -> UPDATE includes status_assembly with value 0
    - SAD-02: section_type='basing' triggers basing derivation (incomplete count query uses section_type = 'basing')
    - SAD-02: section_type IS NULL + name LIKE '%basing%' triggers basing (backward compat)
    - SAD-02: section_type='varnish' triggers varnish derivation
    - SAD-04: status_assembly_override=1 -> assembly value is null in UPDATE (skipped via CASE WHEN)
    - SAD-04: status_basing_override=1 -> basing value is null in UPDATE (skipped)
    - SAD-04: status_varnished_override=1 -> varnish value is null in UPDATE (skipped)
    - SAD-04: all overrides=0 -> all three values computed and included in UPDATE
    - APL-02: painting_percentage=100 -> UPDATE includes is_active_project auto-clear trigger (param value 1 for the CASE WHEN)
    - APL-02: painting_percentage<100 -> is_active_project auto-clear trigger is 0 (no clear)
    - APL-03: syncDerivedStatuses never executes SQL containing "is_active_project = 1" (auto-SET is only in createAssignment)
  </behavior>
  <action>
    Export `syncDerivedStatuses` from `recipeAssignments.ts` for testability. Add a named export: `export { syncDerivedStatuses as syncDerivedStatuses_TEST }` at the bottom of the file (or export the function directly if the naming convention allows). The function signature is `async function syncDerivedStatuses(db: DbFull, unitId: number)` -- the test will pass a mock db object.

    Create `tests/painting/syncDerivedStatuses.test.ts` following the exact mock pattern from `tests/painting/recipeAssignments.test.ts`:
    - `vi.mock("@/db/client")` with `executeMock` and `selectMock`
    - Import `syncDerivedStatuses_TEST` after vi.mock
    - Use `selectMock.mockResolvedValueOnce()` chaining to control the multi-SELECT sequence:
      1. First select: unit row with painting_percentage + 3 override flags
      2. Second select: assignment existence check (return [{id: 1}] for has-recipes)
      3. Third+fourth select pairs: hasAssemblySections count + assemblyRows incomplete count
      4. Fifth+sixth: hasBasingSections count + basingRows incomplete count
      5. Seventh+eighth: hasVarnishSections count + varnishRows incomplete count
    - Assert on `executeMock.mock.calls` to verify the final UPDATE SQL contains `status_assembly`, uses CASE WHEN for override-guarded fields, and includes is_active_project auto-clear logic

    Write test groups covering all 13 behaviors listed above. Each test configures selectMock chain appropriately and asserts on the executeMock call's SQL string and params array.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting/syncDerivedStatuses.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/painting/syncDerivedStatuses.test.ts` exists with at least 10 test cases
    - `syncDerivedStatuses_TEST` (or equivalent) is exported from `src/db/queries/recipeAssignments.ts`
    - Tests cover: assembly complete/incomplete/missing, section_type matching, override guards for all 3 fields, is_active_project auto-clear at 100%, no auto-set in sync
    - All tests pass: `pnpm test -- tests/painting/syncDerivedStatuses.test.ts` exits 0
  </acceptance_criteria>
  <done>syncDerivedStatuses test file created and passing with full coverage of derivation paths and override guards</done>
</task>

<task type="auto">
  <name>Task 2: Implement syncDerivedStatuses Automation + is_active_project Lifecycle</name>
  <files>
    src/db/queries/recipeAssignments.ts,
    tests/painting/recipeAssignments.test.ts
  </files>
  <read_first>
    src/db/queries/recipeAssignments.ts,
    tests/painting/recipeAssignments.test.ts,
    .planning/phases/100-query-layer-automation/100-PATTERNS.md
  </read_first>
  <action>
    Rewrite `syncDerivedStatuses()` in `src/db/queries/recipeAssignments.ts` with these changes per D-01 through D-10:

    1. SELECT override flags: Change the initial SELECT from `SELECT painting_percentage FROM units` to `SELECT painting_percentage, status_assembly_override, status_basing_override, status_varnished_override FROM units WHERE id = $1`. Type the result as `{ painting_percentage: number; status_assembly_override: number; status_basing_override: number; status_varnished_override: number }`.

    2. Assembly derivation (SAD-01, D-06): Add assembly check block before existing basing check, mirroring the basing pattern exactly. Two queries: count sections matching `(sec.section_type = 'assembly' OR (sec.section_type IS NULL AND LOWER(sec.name) LIKE '%assembly%'))`, then count incomplete steps in those sections. Compute `derivedAssembly` as 1 if sections exist and incomplete === 0, else 0.

    3. Basing migration (SAD-02, D-07): Replace `LOWER(sec.name) LIKE '%basing%'` in both basing queries with dual-path: `(sec.section_type = 'basing' OR (sec.section_type IS NULL AND LOWER(sec.name) LIKE '%basing%'))`.

    4. Varnish migration (SAD-02, D-07): Same dual-path for varnish: `(sec.section_type = 'varnish' OR (sec.section_type IS NULL AND LOWER(sec.name) LIKE '%varnish%'))`.

    5. Override guards (SAD-04, D-01/D-03): For each derived value, apply guard: `const assembly = !unitRows[0].status_assembly_override ? derivedAssembly : null`. Same for basing and varnished. When null, the CASE WHEN in the UPDATE skips the write.

    6. Final UPDATE: Change from the current 4-param UPDATE to include status_assembly and is_active_project auto-clear. Use this shape:
       `UPDATE units SET status_painting = $2, status_assembly = CASE WHEN $3 IS NOT NULL THEN $3 ELSE status_assembly END, status_basing = CASE WHEN $4 IS NOT NULL THEN $4 ELSE status_basing END, status_varnished = CASE WHEN $5 IS NOT NULL THEN $5 ELSE status_varnished END, is_active_project = CASE WHEN $6 = 1 THEN 0 ELSE is_active_project END, updated_at = datetime('now') WHERE id = $1`
       Params: `[unitId, status, assembly, basing, varnished, pct === 100 ? 1 : 0]`

    7. is_active_project auto-set (APL-01, D-08): In `createAssignment()`, add `await db.execute("UPDATE units SET is_active_project = 1, updated_at = datetime('now') WHERE id = $1", [input.unit_id])` after the INSERT and before `syncPaintingPercentageByUnitId`.

    8. is_active_project auto-set in bulk (APL-01, D-08): In `bulkCreateAssignments()`, add the same UPDATE after each INSERT and before `syncPaintingPercentageByUnitId` in the loop body.

    9. Extend existing tests in `tests/painting/recipeAssignments.test.ts`:
       - Add APL-01 test in createAssignment group: assert executeMock was called with SQL containing "is_active_project = 1"
       - Add APL-01 test in bulkCreateAssignments group: assert executeMock was called with SQL containing "is_active_project = 1" for each unitId
       - Fix Pitfall 3: update the bulkCreateAssignments `toHaveBeenCalledTimes` assertion from 6 to 9 (3 INSERTs + 3 is_active_project UPDATEs + 3 sync calls for 3 unitIds)

    Discretion notes: Keep assembly/basing/varnish as separate query pairs (clarity over consolidation). Derivation failures should be caught and logged via console.error (silent -- no toast for background automation).
  </action>
  <verify>
    <automated>pnpm test -- tests/painting/syncDerivedStatuses.test.ts tests/painting/recipeAssignments.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `syncDerivedStatuses()` SELECT includes `status_assembly_override, status_basing_override, status_varnished_override`
    - Assembly derivation block exists with dual-path WHERE clause using `section_type = 'assembly'` and name-LIKE fallback
    - Basing queries use `section_type = 'basing' OR (section_type IS NULL AND LOWER(sec.name) LIKE '%basing%')` instead of name-LIKE only
    - Varnish queries use same dual-path pattern with `section_type = 'varnish'`
    - Override guard: each derived value is set to null when corresponding override flag is 1
    - Final UPDATE SQL contains `status_assembly = CASE WHEN $3 IS NOT NULL` pattern for all three guarded fields
    - Final UPDATE SQL contains `is_active_project = CASE WHEN $6 = 1 THEN 0` for auto-clear at 100%
    - `createAssignment()` executes `UPDATE units SET is_active_project = 1` after INSERT
    - `bulkCreateAssignments()` executes same UPDATE per unitId after each INSERT
    - Existing test assertion for bulkCreateAssignments call count is updated (was 6, now 9 for 3 unitIds)
    - All tests pass: both test files exit 0
    - `pnpm build` passes
  </acceptance_criteria>
  <done>syncDerivedStatuses fully rewritten with assembly derivation, section_type matching, override guards, is_active_project lifecycle. createAssignment and bulkCreateAssignments auto-set is_active_project. All tests pass, TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Step toggle UI -> syncDerivedStatuses | Recipe step completion triggers automated status writes |
| Recipe assign UI -> createAssignment | Assignment event triggers is_active_project write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03 | Tampering | syncDerivedStatuses SQL | mitigate | All queries use positional $N params; no string interpolation; override flags are read from DB not user input |
| T-100-04 | Tampering | is_active_project auto-set | accept | Auto-set to 1 is unconditional on assign event; worst case is a benign true value that user can manually toggle off |
| T-100-05 | Denial of Service | syncDerivedStatuses query count | accept | 8 SELECTs + 1 UPDATE per sync call; runs on single unit; no batch amplification; acceptable for desktop app |
</threat_model>

<verification>
- `pnpm test` full suite passes (no regressions)
- `pnpm build` passes
- syncDerivedStatuses handles all three derivation paths (assembly, basing, varnish) with override guards
- createAssignment and bulkCreateAssignments auto-set is_active_project
- Auto-clear fires only at painting_percentage === 100
- syncDerivedStatuses never writes is_active_project = 1
</verification>

<success_criteria>
All Phase 100 automation logic is implemented and tested: assembly auto-derivation, section_type-first matching with backward-compatible fallback, override guards preventing unwanted overwrites, and is_active_project lifecycle (auto-set on assign, auto-clear at 100%).
</success_criteria>

<output>
Create `.planning/phases/100-query-layer-automation/100-02-SUMMARY.md` when done
</output>

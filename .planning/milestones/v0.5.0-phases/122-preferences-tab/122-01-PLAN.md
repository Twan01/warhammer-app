---
phase: 122-preferences-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/settings/GeneralPreferencesSection.tsx
  - src/features/settings/LanguageSetting.tsx
  - src/features/settings/CurrencySetting.tsx
  - src/features/settings/DefaultFactionSetting.tsx
  - src/features/settings/ReadinessTargetSetting.tsx
  - src/hooks/useCurrencyPreference.ts
  - src/lib/localeQueryKeys.ts
  - src/app/settings/page.tsx
  - tests/settings/GeneralPreferencesSection.test.tsx
  - tests/settings/useCurrencyPreference.test.ts
autonomous: true
requirements:
  - PREF-01
  - PREF-02
  - PREF-03
  - PREF-04

must_haves:
  truths:
    - "Settings Preferences tab shows 4 setting controls: Language, Currency, Default Faction, Readiness Target"
    - "Language toggle saves locale key to app_settings via useUpdateSetting"
    - "Currency select saves currency key to app_settings"
    - "Default faction select populated from useFactions, saves default_faction_id"
    - "Readiness target presets + custom input save army_readiness_target"
    - "useCurrencyPreference hook returns {locale, currency} derived from app_settings"
  artifacts:
    - path: "src/features/settings/GeneralPreferencesSection.tsx"
      provides: "Section wrapper rendering 4 setting controls"
      exports: ["GeneralPreferencesSection"]
    - path: "src/hooks/useCurrencyPreference.ts"
      provides: "Typed currency+locale derived from app_settings"
      exports: ["useCurrencyPreference", "CURRENCY_LOCALE_MAP"]
    - path: "src/lib/localeQueryKeys.ts"
      provides: "Shared const array of 7 locale-sensitive query keys"
      exports: ["LOCALE_QUERY_KEYS"]
    - path: "tests/settings/GeneralPreferencesSection.test.tsx"
      provides: "Unit tests for all 4 setting controls"
    - path: "tests/settings/useCurrencyPreference.test.ts"
      provides: "Unit tests for currency preference hook"
  key_links:
    - from: "src/app/settings/page.tsx"
      to: "src/features/settings/GeneralPreferencesSection.tsx"
      via: "import and render above HobbyDefaultsSection"
      pattern: "GeneralPreferencesSection"
    - from: "src/features/settings/LanguageSetting.tsx"
      to: "src/lib/localeQueryKeys.ts"
      via: "import LOCALE_QUERY_KEYS for invalidation on locale change"
      pattern: "LOCALE_QUERY_KEYS"
---

<objective>
Build the GeneralPreferencesSection component with 4 inline auto-save setting controls (Language, Currency, Default Faction, Readiness Target), the useCurrencyPreference convenience hook, a shared locale query keys constant, and wire everything into the Settings page above HobbyDefaultsSection.

Purpose: Delivers the complete Preferences UI so users can configure their 4 core app preferences. Consumer integration (LocaleToggle sync, formatCurrency wiring, ActiveFactionContext boot, useArmyReadinessTarget migration) follows in Plan 02.
Output: 7 new files (5 components, 1 hook, 1 const module) + 1 modified page + 2 test files.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-preferences-tab/122-CONTEXT.md
@.planning/phases/122-preferences-tab/122-RESEARCH.md
@.planning/phases/122-preferences-tab/122-PATTERNS.md
@.planning/phases/122-preferences-tab/122-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared constants, useCurrencyPreference hook, and hook test</name>
  <files>src/lib/localeQueryKeys.ts, src/hooks/useCurrencyPreference.ts, tests/settings/useCurrencyPreference.test.ts</files>
  <read_first>
    src/hooks/useAppSettings.ts
    src/lib/formatCurrency.ts
    src/components/common/LocaleToggle.tsx
    tests/settings/useAppSettings.test.ts
    .planning/phases/122-preferences-tab/122-RESEARCH.md (Pattern 1, Code Examples section)
    .planning/phases/122-preferences-tab/122-PATTERNS.md (useCurrencyPreference section)
  </read_first>
  <action>
    Create `src/lib/localeQueryKeys.ts` exporting a `LOCALE_QUERY_KEYS` const array with the 7 query key strings from D-02: `udb-factions`, `udb-units`, `udb-unit-detail`, `wahapedia-factions`, `datasheets-by-faction`, `datasheets-with-points`, `datasheet`. Export the array as `as const`.

    Create `src/hooks/useCurrencyPreference.ts` per D-03/D-05:
    - Export `CURRENCY_LOCALE_MAP` as `Record<string, string>` mapping EUR->fr-FR, GBP->en-GB, USD->en-US, CAD->en-CA, AUD->en-AU, JPY->ja-JP.
    - Export `SUPPORTED_CURRENCIES` as a const array: `["GBP", "EUR", "USD", "CAD", "AUD", "JPY"]`.
    - Export `useCurrencyPreference()` hook that calls `useAppSettings()`, extracts `settings?.["currency"] ?? "GBP"`, looks up locale from `CURRENCY_LOCALE_MAP` (fallback `"en-GB"`), returns `{ locale: string, currency: string }`.

    Create `tests/settings/useCurrencyPreference.test.ts` following the `useAppSettings.test.ts` pattern:
    - Mock `@/hooks/useAppSettings` at module level.
    - Test: default (no currency setting) returns `{ locale: "en-GB", currency: "GBP" }`.
    - Test: currency "EUR" returns `{ locale: "fr-FR", currency: "EUR" }`.
    - Test: currency "JPY" returns `{ locale: "ja-JP", currency: "JPY" }`.
    - Test: unknown currency falls back to `{ locale: "en-GB" }`.
    Use `renderHook` from `@testing-library/react` with a QueryClientProvider wrapper.
  </action>
  <verify>
    <automated>pnpm test -- tests/settings/useCurrencyPreference.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/localeQueryKeys.ts` exports `LOCALE_QUERY_KEYS` array with exactly 7 string elements
    - `src/hooks/useCurrencyPreference.ts` exports `useCurrencyPreference`, `CURRENCY_LOCALE_MAP`, `SUPPORTED_CURRENCIES`
    - `CURRENCY_LOCALE_MAP` has 6 entries: EUR, GBP, USD, CAD, AUD, JPY
    - `useCurrencyPreference()` returns `{ locale: string, currency: string }` with GBP/en-GB default
    - All useCurrencyPreference tests pass
  </acceptance_criteria>
  <done>Shared locale query keys and currency preference hook exist with passing tests</done>
</task>

<task type="auto">
  <name>Task 2: Build GeneralPreferencesSection with 4 setting controls and wire into Settings page</name>
  <files>src/features/settings/GeneralPreferencesSection.tsx, src/features/settings/LanguageSetting.tsx, src/features/settings/CurrencySetting.tsx, src/features/settings/DefaultFactionSetting.tsx, src/features/settings/ReadinessTargetSetting.tsx, src/app/settings/page.tsx, tests/settings/GeneralPreferencesSection.test.tsx</files>
  <read_first>
    src/features/settings/HobbyDefaultsSection.tsx
    src/features/settings/MissionFormatEditor.tsx
    src/features/settings/PipelineLabelsEditor.tsx
    src/components/common/LocaleToggle.tsx
    src/hooks/useAppSettings.ts
    src/hooks/useFactions.ts
    src/app/settings/page.tsx
    src/hooks/useCurrencyPreference.ts (created in Task 1)
    src/lib/localeQueryKeys.ts (created in Task 1)
    tests/settings/HobbyDefaultsSection.test.tsx
    .planning/phases/122-preferences-tab/122-UI-SPEC.md
    .planning/phases/122-preferences-tab/122-PATTERNS.md
  </read_first>
  <action>
    Create 4 setting components, each receiving `{ settings: AppSettingsMap }` props and using `useUpdateSetting()` for auto-save with `toast.error("Could not save setting. Try again.")` on error. Follow the two-column SettingRow layout from UI-SPEC: left div (p.text-sm.font-semibold label + p.text-sm.text-muted-foreground description), right div (min-w-[160px], control).

    **LanguageSetting.tsx** (per D-01, D-02, PREF-01):
    - Read current locale from `settings["locale"] ?? "en"`.
    - Render segmented button group matching LocaleToggle pattern: `div.flex.border.border-border.rounded-md.overflow-hidden` with two Buttons (variant secondary for active, ghost for inactive, size sm, h-7 text-xs flex-1 rounded-none).
    - On click: call `updateSetting.mutate({ key: "locale", value: next })` with `onSuccess` that imports `LOCALE_QUERY_KEYS` from `@/lib/localeQueryKeys` and calls `queryClient.invalidateQueries({ queryKey: [k] })` for each key.
    - Label: "Language", description: "Interface and game data language".

    **CurrencySetting.tsx** (per D-03, PREF-02):
    - Read current from `settings["currency"] ?? "GBP"`.
    - Render shadcn Select (w-[160px]) with 6 options: GBP (pounds), EUR (euro), USD ($), CAD (CA$), AUD (A$), JPY (yen). Display format: code + symbol, e.g. "GBP (pounds)".
    - Import `SUPPORTED_CURRENCIES` from `@/hooks/useCurrencyPreference`.
    - On `onValueChange`: call `updateSetting.mutate({ key: "currency", value })`.
    - Label: "Currency", description: "Used across spending tracker, wishlist, and unit costs".

    **DefaultFactionSetting.tsx** (per D-06, D-07, PREF-03):
    - Call `useFactions()` to get faction list.
    - Read current from `settings["default_faction_id"] ?? ""`.
    - Render shadcn Select (w-[200px]): first option "None" (value=""), then factions sorted alphabetically by name.
    - On change: call `updateSetting.mutate({ key: "default_faction_id", value })`.
    - Show Skeleton (h-9 w-[200px]) while factions loading.
    - Label: "Default Faction", description: "Faction loaded as active when the app starts".

    **ReadinessTargetSetting.tsx** (per D-09, D-10, PREF-04):
    - Read current from `settings["army_readiness_target"]`, parse as number, default 2000.
    - Render `div.flex.flex-col.items-end.gap-2` containing:
      - `div.flex.gap-1` with 4 preset Buttons (500, 1000, 1500, 2000): size sm, h-7 px-2 text-xs tabular-nums, variant default when active / ghost when inactive.
      - Input type=number (w-[120px] h-7 text-xs text-right, placeholder "Custom...", min=1, max=99999, no spinner arrows via `[&::-webkit-inner-spin-button]:appearance-none [&::-webkit-outer-spin-button]:appearance-none`).
    - On preset click: save immediately via `updateSetting.mutate({ key: "army_readiness_target", value: String(presetValue) })`.
    - On custom input blur: if value is valid positive integer, save. If empty, revert to last valid value.
    - When custom input has a non-preset value, no preset button is active.
    - Label: "Readiness Target", description: "Default points threshold shown on the Army Readiness dashboard card".

    **GeneralPreferencesSection.tsx** (per D-12):
    - Call `useAppSettings()`, destructure `{ data: settings = {} }`.
    - Render `div.space-y-6` with heading "App Preferences" (h3.text-base.font-semibold), then the 4 setting components each receiving `settings`.
    - Add Separator at the bottom (before HobbyDefaultsSection).

    **Modify `src/app/settings/page.tsx`:**
    - Import `GeneralPreferencesSection`.
    - Replace the placeholder `<h2>Preferences</h2>` + `<p>...coming in the next update</p>` with `<GeneralPreferencesSection />`.
    - Keep `<HobbyDefaultsSection />` below it.

    **Create `tests/settings/GeneralPreferencesSection.test.tsx`** following HobbyDefaultsSection.test.tsx pattern:
    - Mock `@/hooks/useAppSettings` (useAppSettings + useUpdateSetting with mockMutate).
    - Mock `@/hooks/useFactions` returning test faction data `[{ id: 1, name: "Adepta Sororitas" }, { id: 2, name: "Space Marines" }]`.
    - Mock `@/hooks/useCurrencyPreference` if needed.
    - Mock `sonner` toast.
    - Tests:
      1. Renders "App Preferences" heading.
      2. Language: renders EN/FR buttons, clicking FR calls mutate with `{ key: "locale", value: "fr" }`.
      3. Currency: renders select with GBP default, changing to EUR calls mutate with `{ key: "currency", value: "EUR" }`.
      4. Default Faction: renders select with "None" and faction names, selecting "Space Marines" calls mutate with `{ key: "default_faction_id", value: "2" }`.
      5. Readiness Target: renders 4 preset buttons, clicking 1000 calls mutate with `{ key: "army_readiness_target", value: "1000" }`.
      6. Readiness Target: custom input on blur calls mutate with custom value.
  </action>
  <verify>
    <automated>pnpm test -- tests/settings/GeneralPreferencesSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/settings/GeneralPreferencesSection.tsx` exports `GeneralPreferencesSection` component
    - 4 child setting components exist in `src/features/settings/`
    - `src/app/settings/page.tsx` renders `<GeneralPreferencesSection />` above `<HobbyDefaultsSection />`
    - No placeholder text "coming in the next update" remains in page.tsx
    - Each setting control calls `updateSetting.mutate` with the correct key on user interaction
    - LanguageSetting invalidates 7 query keys from LOCALE_QUERY_KEYS on success
    - DefaultFactionSetting shows Skeleton while factions load
    - ReadinessTargetSetting custom input has min=1, max=99999, no spinner arrows
    - All GeneralPreferencesSection tests pass
    - `pnpm build` succeeds (TypeScript check passes)
  </acceptance_criteria>
  <done>Settings page Preferences tab shows 4 working setting controls with auto-save, all tests green, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> SQLite | Settings values written via parameterized queries (existing upsertAppSetting) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | ReadinessTargetSetting custom input | mitigate | HTML type=number with min=1 max=99999; validate positive integer before saving; reject empty/NaN |
| T-122-02 | Tampering | DefaultFactionSetting faction ID | accept | Value from closed select populated by useFactions(); no free-text entry; low-risk local-only data |
| T-122-03 | Information Disclosure | app_settings table | accept | Local SQLite database, single-user app, no sensitive data in preferences |
</threat_model>

<verification>
pnpm test -- tests/settings/
pnpm build
</verification>

<success_criteria>
- Settings Preferences tab displays 4 setting controls (Language, Currency, Default Faction, Readiness Target) above Hobby Defaults section
- Each control saves to app_settings via useUpdateSetting on user interaction
- useCurrencyPreference hook returns correct locale/currency mapping with GBP default
- LOCALE_QUERY_KEYS shared constant available for both LanguageSetting and LocaleToggle (Plan 02)
- All tests in tests/settings/ pass
- pnpm build succeeds
</success_criteria>

<output>
Create `.planning/phases/122-preferences-tab/122-01-SUMMARY.md` when done
</output>

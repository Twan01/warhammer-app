---
phase: 109-sub-faction-filter-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/queries/unitDatabase.ts
  - src/hooks/useUnitDatabase.ts
  - src/features/unit-database/databaseBrowserFilters.ts
  - src/features/unit-database/applyUdbFilters.ts
  - tests/unit-database/applyUdbFilters.test.ts
autonomous: true
requirements: [SF-03, SF-06]

must_haves:
  truths:
    - "getDistinctSubFactions returns distinct sub-faction names for a faction with sub-factions"
    - "getDistinctSubFactions returns empty array for factions without sub-factions"
    - "useUdbSubFactions hook is disabled when factionId is null"
    - "applyUdbFilters excludes units not matching subFactionFilter when set"
    - "applyUdbFilters passes all units through when subFactionFilter is null"
    - "Zustand store exposes subFactionFilter with setter and clearFilters resets it"
    - "UdbUnitSummary includes sub_faction field from SQL"
  artifacts:
    - path: "src/db/queries/unitDatabase.ts"
      provides: "getDistinctSubFactions query, getUdbUnitIdsBySubFaction query, sub_faction in getUdbUnitsByFaction"
      contains: "getDistinctSubFactions"
    - path: "src/hooks/useUnitDatabase.ts"
      provides: "useUdbSubFactions hook"
      contains: "useUdbSubFactions"
    - path: "src/features/unit-database/databaseBrowserFilters.ts"
      provides: "subFactionFilter state and setter"
      contains: "subFactionFilter"
    - path: "src/features/unit-database/applyUdbFilters.ts"
      provides: "sub-faction filtering logic"
      contains: "subFactionFilter"
  key_links:
    - from: "src/hooks/useUnitDatabase.ts"
      to: "src/db/queries/unitDatabase.ts"
      via: "import getDistinctSubFactions"
      pattern: "getDistinctSubFactions"
    - from: "src/features/unit-database/applyUdbFilters.ts"
      to: "src/db/queries/unitDatabase.ts"
      via: "UdbUnitSummary type import"
      pattern: "sub_faction"
---

<objective>
Create the sub-faction query layer, React Query hook, Zustand store extension, and filter function extension that all three UI surfaces (database browser, army list picker, collection browser) will consume.

Purpose: Establish the data foundation so Plan 02 can wire sub-faction dropdowns into three separate UI surfaces without duplicating query or filter logic.
Output: Query functions, hook, store field, extended filter function, and updated tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-sub-faction-filter-ui/109-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/db/queries/unitDatabase.ts:
- UdbUnitSummary: { id, faction_id, name, role, base_points, min_models, max_models } -- needs sub_faction: string | null added
- getUdbUnitsByFaction(factionId: string): Promise<UdbUnitSummary[]> -- SQL needs u.sub_faction added to SELECT
- getDb() from @/db/client -- standard DB accessor

From src/hooks/useUnitDatabase.ts:
- Pattern: useQuery with queryKey factory, enabled flag, staleTime: Infinity
- Existing: useUdbUnits(factionId: string | null) -- disabled pattern to follow

From src/features/unit-database/databaseBrowserFilters.ts:
- Zustand create<State>((set) => ({...})) pattern
- clearFilters resets all non-faction fields to defaults

From src/features/unit-database/applyUdbFilters.ts:
- UdbFiltersInput: { roleFilter, keywordFilter, pointMin, pointMax } -- needs subFactionFilter: string | null
- applyUdbFilters(units, filters, keywordsMap?) returns filtered UdbUnitSummary[]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Query layer and hook -- getDistinctSubFactions, getUdbUnitIdsBySubFaction, useUdbSubFactions, extend UdbUnitSummary</name>
  <files>src/db/queries/unitDatabase.ts, src/hooks/useUnitDatabase.ts</files>
  <read_first>
    src/db/queries/unitDatabase.ts
    src/hooks/useUnitDatabase.ts
  </read_first>
  <behavior>
    - getDistinctSubFactions("SM") returns sorted string array of distinct non-null sub_faction values for SM
    - getDistinctSubFactions("NEC") returns empty array (Necrons have no sub-factions)
    - getUdbUnitIdsBySubFaction("SM", "Ultramarines") returns string array of unit IDs matching that sub-faction
    - useUdbSubFactions(null) has enabled: false
    - useUdbSubFactions("SM") has enabled: true, queryKey: ["udb-sub-factions", "SM"]
    - UdbUnitSummary now includes sub_faction: string | null
    - getUdbUnitsByFaction SQL SELECT clause includes u.sub_faction
  </behavior>
  <action>
    Per D-01: Add getDistinctSubFactions(factionId: string): Promise<string[]> to src/db/queries/unitDatabase.ts. SQL: SELECT DISTINCT sub_faction FROM udb_units WHERE faction_id = $1 AND sub_faction IS NOT NULL ORDER BY sub_faction. Map rows to r.sub_faction strings.

    Per D-07/D-08: Add getUdbUnitIdsBySubFaction(factionId: string, subFaction: string): Promise<string[]> to the same file. SQL: SELECT id FROM udb_units WHERE faction_id = $1 AND sub_faction = $2. Map rows to r.id strings. This is the lightweight query for collection/picker Set-based filtering.

    Per D-05: Add sub_faction: string | null to UdbUnitSummary interface. Add u.sub_faction to the SELECT in getUdbUnitsByFaction, placing it after u.role.

    Per D-02: Add UDB_SUB_FACTIONS_KEY factory and useUdbSubFactions hook to src/hooks/useUnitDatabase.ts. Follow the exact useUdbUnits disabled pattern. queryKey: ["udb-sub-factions", factionId]. staleTime: Infinity. Import getDistinctSubFactions.

    Also add UDB_SUB_FACTION_UNIT_IDS_KEY factory and useUdbSubFactionUnitIds(factionId: string | null, subFaction: string | null) hook. Returns string[] of UDB unit IDs. Disabled when either param is null. queryKey: ["udb-sub-faction-unit-ids", factionId, subFaction]. staleTime: Infinity.

    Export getUdbUnitIdsBySubFaction from the query module.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/db/queries/unitDatabase.ts exports getDistinctSubFactions and getUdbUnitIdsBySubFaction functions
    - UdbUnitSummary interface has sub_faction: string | null field
    - getUdbUnitsByFaction SQL string contains "u.sub_faction"
    - src/hooks/useUnitDatabase.ts exports useUdbSubFactions and useUdbSubFactionUnitIds
    - useUdbSubFactions has enabled: !!factionId and staleTime: Infinity
    - pnpm build succeeds with no type errors
  </acceptance_criteria>
  <done>Query layer provides getDistinctSubFactions, getUdbUnitIdsBySubFaction, and useUdbSubFactions/useUdbSubFactionUnitIds hooks. UdbUnitSummary includes sub_faction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Zustand store extension, applyUdbFilters extension, and tests</name>
  <files>src/features/unit-database/databaseBrowserFilters.ts, src/features/unit-database/applyUdbFilters.ts, tests/unit-database/applyUdbFilters.test.ts</files>
  <read_first>
    src/features/unit-database/databaseBrowserFilters.ts
    src/features/unit-database/applyUdbFilters.ts
    tests/unit-database/applyUdbFilters.test.ts
  </read_first>
  <behavior>
    - applyUdbFilters with subFactionFilter: "Ultramarines" returns only units where sub_faction === "Ultramarines"
    - applyUdbFilters with subFactionFilter: null returns all units (no sub-faction filtering)
    - applyUdbFilters with subFactionFilter combined with roleFilter applies AND logic
    - Units with sub_faction: null are excluded when subFactionFilter is set
    - Zustand store has subFactionFilter: null initial state
    - setSubFactionFilter("Ultramarines") sets subFactionFilter to "Ultramarines"
    - clearFilters resets subFactionFilter to null
    - setSelectedFactionId also resets subFactionFilter to null (pitfall 1 from RESEARCH.md)
  </behavior>
  <action>
    Per D-03: In databaseBrowserFilters.ts, add subFactionFilter: string | null to the interface and setSubFactionFilter: (sf: string | null) => void. Initial value: null. Add setter: setSubFactionFilter: (sf) => set({ subFactionFilter: sf }). Add subFactionFilter: null to clearFilters reset object. Per RESEARCH.md Pitfall 1: modify setSelectedFactionId setter to also reset subFactionFilter: setSelectedFactionId: (id) => set({ selectedFactionId: id, subFactionFilter: null }). Update hasActiveFilters check in consumers: subFactionFilter is active when not null.

    Per D-05: In applyUdbFilters.ts, add subFactionFilter: string | null to UdbFiltersInput interface. Add sub-faction filter check as the FIRST filter in the filter function body, before roleFilter: if filters.subFactionFilter is not null and unit.sub_faction does not equal filters.subFactionFilter, return false.

    Extend tests/unit-database/applyUdbFilters.test.ts: Add sub_faction field to all UNITS fixtures (e.g., u1: "Ultramarines", u2: null, u3: "Ultramarines", u4: null, u5: "Dark Angels"). Add subFactionFilter: null to NO_FILTER constant. Add test cases:
    1. "filters by subFactionFilter" -- subFactionFilter: "Ultramarines" returns u1, u3
    2. "subFactionFilter excludes units with null sub_faction" -- subFactionFilter: "Dark Angels" returns only u5
    3. "subFactionFilter null returns all units" -- same as existing no-filter test
    4. "AND logic with subFactionFilter and roleFilter" -- subFactionFilter: "Ultramarines" + roleFilter: "Battleline" returns u1, u3 (both are Battleline + Ultramarines)

    Update existing test fixtures to include subFactionFilter: null where NO_FILTER is spread.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit-database/applyUdbFilters.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - databaseBrowserFilters.ts has subFactionFilter: string | null in interface and store
    - setSelectedFactionId setter includes subFactionFilter: null in its set() call
    - clearFilters includes subFactionFilter: null
    - applyUdbFilters.ts UdbFiltersInput has subFactionFilter: string | null
    - Filter function checks subFactionFilter before roleFilter
    - All existing tests pass (updated fixtures have sub_faction and subFactionFilter: null)
    - New sub-faction filter tests pass (at least 3 new test cases)
    - pnpm test -- tests/unit-database/applyUdbFilters.test.ts passes
  </acceptance_criteria>
  <done>Zustand store has subFactionFilter with auto-reset on faction change. applyUdbFilters handles sub-faction filtering. Tests cover sub-faction filter logic including AND combinations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. All data is local SQLite read-only filtering with values from pre-populated Select dropdowns.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Tampering | SQL queries | accept | Parameterized queries ($1, $2) prevent injection; filter values come from DB-derived dropdown options, not free text |
</threat_model>

<verification>
- pnpm build succeeds (type-check)
- pnpm test -- tests/unit-database/applyUdbFilters.test.ts passes
- pnpm test passes (full suite)
</verification>

<success_criteria>
- getDistinctSubFactions and getUdbUnitIdsBySubFaction queries exist and are exported
- useUdbSubFactions and useUdbSubFactionUnitIds hooks exist and are exported
- UdbUnitSummary has sub_faction field
- databaseBrowserFilters Zustand store has subFactionFilter with faction-change auto-reset
- applyUdbFilters handles subFactionFilter
- All tests pass including new sub-faction filter test cases
</success_criteria>

<output>
Create `.planning/phases/109-sub-faction-filter-ui/109-01-SUMMARY.md` when done
</output>

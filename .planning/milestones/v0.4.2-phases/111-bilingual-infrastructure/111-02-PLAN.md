---
phase: 111-bilingual-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/localeStore.ts
  - src/db/queries/unitDatabase.ts
  - src/hooks/useUnitDatabase.ts
  - tests/unit-database/locale-queries.test.ts
  - tests/unit-database/locale-store.test.ts
autonomous: true
requirements: [FR-03]

must_haves:
  truths:
    - "Query functions accept locale parameter and return COALESCE'd French names when locale is 'fr'"
    - "React Query hooks include locale in query keys so cache auto-refreshes on locale switch"
    - "Locale store persists 'en' or 'fr' to localStorage under key 'app:locale'"
    - "When locale is 'fr' and name_fr is null, English name is returned via COALESCE fallback"
  artifacts:
    - path: "src/stores/localeStore.ts"
      provides: "Zustand persist store for locale"
      exports: ["useLocaleStore", "Locale"]
    - path: "src/db/queries/unitDatabase.ts"
      provides: "Locale-aware COALESCE queries"
      contains: "COALESCE"
    - path: "src/hooks/useUnitDatabase.ts"
      provides: "Locale-keyed React Query hooks"
      contains: "useLocaleStore"
    - path: "tests/unit-database/locale-queries.test.ts"
      provides: "Tests for locale-aware query functions"
      contains: "describe"
    - path: "tests/unit-database/locale-store.test.ts"
      provides: "Tests for locale store behavior"
      contains: "describe"
  key_links:
    - from: "src/hooks/useUnitDatabase.ts"
      to: "src/stores/localeStore.ts"
      via: "useLocaleStore import"
      pattern: "useLocaleStore"
    - from: "src/hooks/useUnitDatabase.ts"
      to: "src/db/queries/unitDatabase.ts"
      via: "locale parameter pass-through"
      pattern: "getUdbFactions\\(locale\\)"
---

<objective>
Locale store + bilingual query layer (FR-03)

Purpose: Thread locale awareness through the query layer and React Query hooks so all UDB-displaying surfaces automatically show French names when locale is 'fr'. Create the Zustand persist store that holds the locale preference.

Output: localeStore.ts, locale-aware unitDatabase.ts queries, locale-keyed useUnitDatabase.ts hooks, test stubs
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-bilingual-infrastructure/111-CONTEXT.md
@.planning/phases/111-bilingual-infrastructure/111-RESEARCH.md
@.planning/phases/111-bilingual-infrastructure/111-PATTERNS.md

<interfaces>
<!-- From src/db/queries/unitDatabase.ts — current function signatures to extend -->
export async function getUdbFactions(): Promise&lt;UdbFaction[]&gt;
export async function getUdbUnitsByFaction(factionId: string): Promise&lt;UdbUnitSummary[]&gt;
export async function getUdbUnitDetail(unitId: string): Promise&lt;UdbUnitDetail | null&gt;
export async function searchUdbUnits(query: string): Promise&lt;UdbSearchResult[]&gt;
export async function getUdbOwnershipForUnit(unitId: string): Promise&lt;UdbOwnershipEntry[]&gt;
export async function getUdbOwnershipByFaction(factionId: string): Promise&lt;...&gt;
export async function getUdbKeywordsByFaction(factionId: string): Promise&lt;string[]&gt;

<!-- From src/hooks/useUnitDatabase.ts — current key factories -->
export const UDB_FACTIONS_KEY = ["udb-factions"] as const;
export const UDB_UNITS_KEY = (factionId: string) => ["udb-units", factionId] as const;
export const UDB_UNIT_DETAIL_KEY = (unitId: string) => ["udb-unit-detail", unitId] as const;
export const UDB_SEARCH_KEY = (query: string) => ["udb-search", query] as const;

<!-- From src/features/game-day/gameDayStore.ts — Zustand persist pattern -->
import { create } from "zustand";
import { persist } from "zustand/middleware";
export const useGameDayStore = create&lt;GameDayStore&gt;()(
  persist((set) => ({ ... }), { name: "game-day-state" })
);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create locale store, test stubs, and extend query layer with COALESCE</name>
  <files>src/stores/localeStore.ts, src/db/queries/unitDatabase.ts, tests/unit-database/locale-queries.test.ts, tests/unit-database/locale-store.test.ts</files>
  <read_first>
    - src/features/game-day/gameDayStore.ts (Zustand persist pattern to clone)
    - src/db/queries/unitDatabase.ts (full file — all query functions to understand which need locale)
    - .planning/phases/111-bilingual-infrastructure/111-CONTEXT.md (D-03, D-05, D-07)
    - .planning/phases/111-bilingual-infrastructure/111-PATTERNS.md (Pattern 2 COALESCE, Pattern 3 store)
  </read_first>
  <action>
    First, create test stub files before implementation:

    Create tests/unit-database/locale-store.test.ts with test cases:
    - "useLocaleStore defaults to 'en'"
    - "setLocale switches to 'fr'"
    - "Locale type only accepts 'en' or 'fr'" (type-level check)
    Mock zustand persist middleware as needed.

    Create tests/unit-database/locale-queries.test.ts with test cases:
    - "getUdbFactions with locale='fr' uses COALESCE in SQL"
    - "getUdbFactions without locale uses plain name column"
    - "getUdbUnitsByFaction with locale='fr' uses COALESCE"
    - "getUdbUnitDetail with locale='fr' applies COALESCE to unit, abilities, and weapons"
    - "searchUdbUnits does NOT accept locale parameter" (type-level or signature check)
    Mock the DB client (getRulesDb) to capture SQL strings and verify COALESCE presence.

    Then implement:

    Create src/stores/ directory and src/stores/localeStore.ts per D-05/D-07:
    - Export type Locale = 'en' | 'fr'
    - Export useLocaleStore: Zustand persist store with { locale: Locale, setLocale: (l: Locale) => void }
    - Default locale: 'en'
    - localStorage key: "app:locale"
    - Follow gameDayStore.ts persist wrapper pattern exactly

    Extend src/db/queries/unitDatabase.ts — add optional locale?: 'en' | 'fr' parameter to these 4 functions per D-03:

    1. getUdbFactions(locale?) — SELECT uses COALESCE(name_fr, name) AS name when locale === 'fr', else plain name
    2. getUdbUnitsByFaction(factionId, locale?) — COALESCE(u.name_fr, u.name) AS name when fr
    3. getUdbUnitDetail(unitId, locale?) — three COALESCE points:
       - Main unit: COALESCE(name_fr, name) AS name
       - Abilities sub-query: COALESCE(name_fr, name) AS name, COALESCE(description_fr, description) AS description
       - Weapons sub-query: COALESCE(name_fr, name) AS name
    4. searchUdbUnits(query) — NO locale param per D-03 (FTS5 indexes both languages in one table; no locale needed)

    Do NOT add locale to: getUdbOwnershipForUnit, getUdbOwnershipByFaction, getUdbKeywordsByFaction, or any points queries — these return numeric/status data only.

    Use conditional SQL string construction with template literals: const nameSql = locale === 'fr' ? 'COALESCE(name_fr, name) AS name' : 'name'. The locale parameter is a TypeScript union, not user input — safe for SQL string interpolation.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit-database/locale-store.test.ts tests/unit-database/locale-queries.test.ts && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit-database/locale-store.test.ts exists with at least 2 test cases and passes
    - tests/unit-database/locale-queries.test.ts exists with at least 4 test cases and passes
    - src/stores/localeStore.ts exists and exports useLocaleStore and Locale type
    - useLocaleStore uses zustand persist middleware with name "app:locale"
    - Default locale is 'en' (not undefined, not null)
    - getUdbFactions signature is getUdbFactions(locale?: 'en' | 'fr')
    - getUdbUnitsByFaction signature is getUdbUnitsByFaction(factionId: string, locale?: 'en' | 'fr')
    - getUdbUnitDetail signature is getUdbUnitDetail(unitId: string, locale?: 'en' | 'fr')
    - searchUdbUnits does NOT have a locale parameter
    - When locale='fr', SQL contains COALESCE(name_fr, name) (verified by reading the generated SQL string)
    - When locale is omitted or 'en', SQL uses plain column name (no COALESCE overhead)
    - pnpm build passes TypeScript compilation
  </acceptance_criteria>
  <done>Locale store created with tests; 3 query functions extended with COALESCE locale support; all tests pass; TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Thread locale into React Query hooks with key parameterization</name>
  <files>src/hooks/useUnitDatabase.ts</files>
  <read_first>
    - src/hooks/useUnitDatabase.ts (full file — all hooks to understand current key/queryFn patterns)
    - src/stores/localeStore.ts (created in Task 1 — verify export names)
    - .planning/phases/111-bilingual-infrastructure/111-CONTEXT.md (D-04, D-06)
    - .planning/phases/111-bilingual-infrastructure/111-PATTERNS.md (Pattern 4 locale-keyed hooks)
  </read_first>
  <action>
    Import useLocaleStore and Locale from "@/stores/localeStore" at top of useUnitDatabase.ts.

    Update key factories to include locale parameter per D-04:
    - UDB_FACTIONS_KEY: change from const array to function (locale: Locale) => ["udb-factions", locale] as const
    - UDB_UNITS_KEY: add locale as second param: (factionId: string, locale: Locale) => ["udb-units", factionId, locale] as const
    - UDB_UNIT_DETAIL_KEY: add locale: (unitId: string, locale: Locale) => ["udb-unit-detail", unitId, locale] as const
    - UDB_SEARCH_KEY: NO change (FTS5 bilingual by index, no locale needed)

    Update hooks:
    - useUdbFactions: add const locale = useLocaleStore((s) => s.locale); use UDB_FACTIONS_KEY(locale) and () => getUdbFactions(locale)
    - useUdbUnits: add locale from store; use UDB_UNITS_KEY(factionId, locale) and pass locale to getUdbUnitsByFaction
    - useUdbUnitDetail: add locale from store; use UDB_UNIT_DETAIL_KEY(unitId, locale) and pass locale to getUdbUnitDetail
    - useUdbSearch: NO change
    - useUdbOwnership / useUdbKeywords hooks: NO change (no locale needed)

    Note: grep confirms UDB_FACTIONS_KEY is only consumed inside src/hooks/useUnitDatabase.ts (lines 27, 40) — no external consumers exist. The const-to-function change is safe with no additional file updates required.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | findstr /C:"error" /C:"Successfully"</automated>
  </verify>
  <acceptance_criteria>
    - useUnitDatabase.ts imports useLocaleStore from "@/stores/localeStore"
    - UDB_FACTIONS_KEY is now a function accepting Locale, not a const array
    - useUdbFactions calls useLocaleStore((s) => s.locale) and passes it to both key factory and query function
    - useUdbUnits passes locale to UDB_UNITS_KEY and getUdbUnitsByFaction
    - useUdbUnitDetail passes locale to UDB_UNIT_DETAIL_KEY and getUdbUnitDetail
    - useUdbSearch does NOT reference locale
    - No external consumers of UDB_FACTIONS_KEY need updating (confirmed: only used in this file)
    - pnpm build passes with zero TypeScript errors
  </acceptance_criteria>
  <done>React Query hooks pass locale from Zustand store into query keys and query functions; cache auto-refreshes when locale changes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand store -> SQL query | locale value is TypeScript 'en' | 'fr' union; only two values possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-02 | Tampering | locale in SQL string | mitigate | locale is TypeScript union 'en' OR 'fr' only; conditional template literal, not string concat of user input |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- pnpm test -- tests/unit-database/locale-store.test.ts tests/unit-database/locale-queries.test.ts (all green)
- pnpm build passes with zero errors
- Locale store initializes to 'en' by default
- Query functions produce COALESCE SQL when passed 'fr', plain SQL when passed 'en' or omitted
- Hook key factories include locale parameter
</verification>

<success_criteria>
FR-03 satisfied: query layer accepts locale parameter with COALESCE fallback; locale store created and persisted; hooks thread locale into cache keys.
(Toggle UI in Plan 03.)
</success_criteria>

<output>
Create `.planning/phases/111-bilingual-infrastructure/111-02-SUMMARY.md` when done
</output>

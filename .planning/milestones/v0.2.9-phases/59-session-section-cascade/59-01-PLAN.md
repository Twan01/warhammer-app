---
phase: 59-session-section-cascade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/dashboard/logSessionSchema.ts
  - tests/dashboard/logSessionSchema.test.ts
autonomous: true
requirements:
  - SESS-05

must_haves:
  truths:
    - "D-14: logSessionSchema accepts section_name as z.string().nullable().optional()"
    - "D-17: buildDefaultValues shape with section_name: null parses successfully"
  artifacts:
    - path: "src/features/dashboard/logSessionSchema.ts"
      provides: "section_name field on logSessionSchema"
      contains: "section_name"
    - path: "tests/dashboard/logSessionSchema.test.ts"
      provides: "SESS-05 schema validation tests"
      contains: "SESS-05"
  key_links:
    - from: "src/features/dashboard/logSessionSchema.ts"
      to: "LogSessionFormValues type"
      via: "z.infer export"
      pattern: "section_name.*nullable.*optional"
---

<objective>
Add section_name field to the logSessionSchema Zod schema and extend schema tests for SESS-05 validation.

Purpose: The LogSessionSheet form needs a section_name field in its schema before the component can reference it. This plan establishes the schema contract that Plan 02 builds against.
Output: Updated logSessionSchema.ts with section_name field; extended logSessionSchema.test.ts with SESS-05 test cases.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-session-section-cascade/59-CONTEXT.md
@.planning/phases/59-session-section-cascade/59-RESEARCH.md
@.planning/phases/59-session-section-cascade/59-PATTERNS.md

<interfaces>
From src/features/dashboard/logSessionSchema.ts:
- logSessionSchema: z.object with fields unit_id, session_date, duration_minutes, notes, new_status, recipe_id, recipe_step_id
- LogSessionFormValues: z.infer<typeof logSessionSchema>
- recipe_id pattern: z.number().int().positive().nullable().optional()
- recipe_step_id pattern: z.number().int().positive().nullable().optional()

From tests/dashboard/logSessionSchema.test.ts:
- BASE_VALID = { unit_id: 1, session_date: "2026-01-01", duration_minutes: 30, notes: null }
- Test pattern: describe block per requirement ID, safeParse assertions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add section_name to logSessionSchema</name>
  <files>src/features/dashboard/logSessionSchema.ts</files>
  <read_first>
    - src/features/dashboard/logSessionSchema.ts
  </read_first>
  <action>
Add `section_name: z.string().nullable().optional()` to the `logSessionSchema` z.object definition, after the `recipe_step_id` field (line 35). Per D-14, it follows the same nullable/optional pattern as recipe_id and recipe_step_id but uses `z.string()` instead of `z.number()` because section_name is denormalized text (per D-15). Add a comment: `// Phase 59 — SESS-01/05 (section cascade)`. Do NOT use `.default()` — the existing file comment at lines 6-11 warns against it.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - logSessionSchema.ts contains `section_name: z.string().nullable().optional()`
    - The field appears after recipe_step_id and before the closing `});`
    - LogSessionFormValues type (inferred) includes section_name as string | null | undefined
    - No `.default()` used on the new field
    - pnpm build succeeds without TypeScript errors
  </acceptance_criteria>
  <done>logSessionSchema exports a LogSessionFormValues type that includes section_name as string | null | undefined</done>
</task>

<task type="auto">
  <name>Task 2: Add SESS-05 schema tests for section_name</name>
  <files>tests/dashboard/logSessionSchema.test.ts</files>
  <read_first>
    - tests/dashboard/logSessionSchema.test.ts
    - src/features/dashboard/logSessionSchema.ts
  </read_first>
  <action>
Add a new describe block `"logSessionSchema — SESS-05 (section_name field)"` after the existing INTEG-01 block (after line 130). Follow the exact test pattern from the INTEG-01 block. Include these test cases:

1. "parses successfully when section_name is omitted (field is optional)" — safeParse BASE_VALID without section_name, expect success
2. "parses successfully when section_name is null (field is nullable)" — safeParse with section_name: null, expect result.data.section_name to be null
3. "parses successfully when section_name is a non-empty string" — safeParse with section_name: "Base Coat", expect result.data.section_name to be "Base Coat"
4. "buildDefaultValues shape with section_name: null parses successfully" — full default values object including section_name: null

Use the existing BASE_VALID constant. The PATTERNS.md file at lines 278-310 has the exact test code to follow.
  </action>
  <verify>
    <automated>pnpm test -- tests/dashboard/logSessionSchema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - logSessionSchema.test.ts contains describe block "logSessionSchema — SESS-05 (section_name field)"
    - 4 test cases all pass: omitted, null, non-empty string, full default shape
    - All existing DATA-01 and INTEG-01 tests still pass
    - pnpm test -- tests/dashboard/logSessionSchema.test.ts exits 0
  </acceptance_criteria>
  <done>Schema tests verify section_name is nullable, optional, and accepts strings — SESS-05 schema validation confirmed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form input -> DB | section_name is sourced from DB sections list, not free text input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Tampering | section_name field | accept | Value is display-only denormalized text sourced from sections array; not FK-linked; no integrity risk |
</threat_model>

<verification>
pnpm test -- tests/dashboard/logSessionSchema.test.ts
pnpm build
</verification>

<success_criteria>
- logSessionSchema includes section_name field
- All 4 SESS-05 schema tests pass
- All existing schema tests remain green
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/59-session-section-cascade/59-01-SUMMARY.md`
</output>

---
phase: 58-recipe-form-timeline-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/recipes/RecipeSectionCard.tsx
  - src/features/recipes/RecipeSectionList.tsx
  - tests/painting/recipeSectionCard.test.tsx
autonomous: true
requirements: [RUI-01, RUI-02]

must_haves:
  truths:
    - "User can expand a Workflow collapsible on any RecipeSectionCard and set section_type, technique, execution_mode, and applies_to"
    - "Simple recipes (single section, no metadata set) show no workflow collapsible"
  artifacts:
    - path: "src/features/recipes/RecipeSectionCard.tsx"
      provides: "Workflow nested Collapsible with 2x2 grid of metadata controls"
      contains: "showWorkflowCollapsible"
    - path: "src/features/recipes/RecipeSectionList.tsx"
      provides: "sectionsCount prop threaded to RecipeSectionCard"
      contains: "sectionsCount"
    - path: "tests/painting/recipeSectionCard.test.tsx"
      provides: "Test coverage for RUI-01 and RUI-02"
      contains: "RUI-01"
  key_links:
    - from: "src/features/recipes/RecipeSectionList.tsx"
      to: "src/features/recipes/RecipeSectionCard.tsx"
      via: "sectionsCount={sections.length} prop"
      pattern: "sectionsCount"
    - from: "src/features/recipes/RecipeSectionCard.tsx"
      to: "src/types/recipeSection.ts"
      via: "import SECTION_TYPES, TECHNIQUES, EXECUTION_MODES"
      pattern: "SECTION_TYPES"
---

<objective>
Add a Workflow nested Collapsible to RecipeSectionCard with three Select dropdowns (section_type, technique, execution_mode) and one Input (applies_to) in a 2x2 grid, gated by progressive disclosure logic per D-04/D-05.

Purpose: Enables users to set workflow metadata on recipe sections (RUI-01) while keeping simple recipes visually uncluttered (RUI-02).
Output: Modified RecipeSectionCard.tsx, RecipeSectionList.tsx, extended test file.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-recipe-form-timeline-display/58-CONTEXT.md
@.planning/phases/58-recipe-form-timeline-display/58-PATTERNS.md
@.planning/phases/58-recipe-form-timeline-display/58-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/recipeSection.ts:
```typescript
export const SECTION_TYPES = ["prep","basecoat","shade","layer","detail","effect","finishing"] as const;
export const TECHNIQUES = ["brush","sponge","drybrush","airbrush","oil-enamel","pigment","decal","mixed","other"] as const;
export const EXECUTION_MODES = ["sequential","batch","parallel"] as const;
```

From src/features/recipes/recipeSection.ts (DraftSection):
```typescript
export interface DraftSection {
  localId: string;
  name: string;
  surface: string | null;
  optional: number;
  notes: string | null;
  section_type: string | null;
  technique: string | null;
  execution_mode: string | null;
  applies_to: string | null;
  steps: DraftStep[];
}
```

From src/features/recipes/RecipeSectionList.tsx (current props):
```typescript
export interface RecipeSectionListProps {
  sections: DraftSection[];
  onChange: (next: DraftSection[]) => void;
  onCreateNewPaint: (stepLocalId: string) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sectionsCount prop to RecipeSectionCard and thread from RecipeSectionList</name>
  <files>src/features/recipes/RecipeSectionCard.tsx, src/features/recipes/RecipeSectionList.tsx</files>
  <read_first>
    - src/features/recipes/RecipeSectionCard.tsx (full file -- see current props interface at top, existing Collapsible pattern, surface Select pattern with __none__ sentinel)
    - src/features/recipes/RecipeSectionList.tsx (full file -- see where RecipeSectionCard is rendered at line 53)
    - src/types/recipeSection.ts (SECTION_TYPES, TECHNIQUES, EXECUTION_MODES const arrays)
    - .planning/phases/58-recipe-form-timeline-display/58-UI-SPEC.md (Component Spec 1 for exact layout and interaction contracts)
    - .planning/phases/58-recipe-form-timeline-display/58-PATTERNS.md (Pattern assignments for RecipeSectionCard)
  </read_first>
  <action>
    **RecipeSectionCard.tsx modifications (per D-01, D-02, D-03, D-04, D-05):**

    1. Add `sectionsCount: number` to the component props interface.

    2. Add import for `SECTION_TYPES, TECHNIQUES, EXECUTION_MODES` from `@/types/recipeSection`.

    3. Add `const [workflowOpen, setWorkflowOpen] = useState(false)` state alongside existing `open` state.

    4. Add a `hasAnyWorkflowMetadata` helper function checking `section.section_type || section.technique || section.execution_mode || section.applies_to` (per D-05). Derive `showWorkflowCollapsible = sectionsCount > 1 || hasAnyWorkflowMetadata(section)` (per D-04).

    5. Inside the outer CollapsibleContent, before RecipeStepList, conditionally render a nested Collapsible when `showWorkflowCollapsible` is true:
       - CollapsibleTrigger: ghost Button with ChevronDown icon (h-3 w-3, rotate-180 when open) and label text "Workflow", styled `h-7 gap-1 text-xs text-muted-foreground`
       - CollapsibleContent: a `grid grid-cols-2 gap-2 pt-1 pb-2` containing four controls:
         - Row 1 col 1: section_type Select using `__none__` sentinel pattern (same as existing surface Select), options from SECTION_TYPES, placeholder "Type", none option "-- type --", trigger `h-7 w-full text-xs`
         - Row 1 col 2: technique Select, same pattern, TECHNIQUES array, placeholder "Technique", none option "-- technique --"
         - Row 2 col 1: execution_mode Select, same pattern, EXECUTION_MODES array, placeholder "Mode", none option "-- mode --"
         - Row 2 col 2: applies_to Input, `h-7 text-xs`, placeholder "Applies to...", `value={section.applies_to ?? ""}`, onChange writes `e.target.value || null`, add `aria-label="Applies to (model area)"`

    6. All four controls call `onChange({ ...section, field: newValue })` following the existing bubble pattern.

    **RecipeSectionList.tsx modifications:**

    7. In the sections.map callback where RecipeSectionCard is rendered (line 53), add prop `sectionsCount={sections.length}`.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - RecipeSectionCard props interface includes `sectionsCount: number`
    - RecipeSectionCard imports SECTION_TYPES, TECHNIQUES, EXECUTION_MODES from "@/types/recipeSection"
    - File contains `const [workflowOpen, setWorkflowOpen] = useState(false)`
    - File contains `showWorkflowCollapsible` derived from `sectionsCount > 1 || hasAnyWorkflowMetadata`
    - hasAnyWorkflowMetadata checks all four fields: section_type, technique, execution_mode, applies_to
    - Nested Collapsible renders conditionally on showWorkflowCollapsible
    - Three Select components use __none__ sentinel pattern with respective const arrays
    - applies_to Input uses `value={section.applies_to ?? ""}` and `onChange` writes null on empty string
    - applies_to Input has `aria-label="Applies to (model area)"`
    - RecipeSectionList passes `sectionsCount={sections.length}` to RecipeSectionCard
    - `pnpm build` succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>Workflow collapsible with 4 metadata controls renders inside RecipeSectionCard when sectionsCount > 1 or metadata exists; hidden for single-section no-metadata recipes. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add RUI-01 and RUI-02 test coverage for workflow collapsible</name>
  <files>tests/painting/recipeSectionCard.test.tsx</files>
  <read_first>
    - tests/painting/recipeSectionCard.test.tsx (full file -- see makeSection fixture factory, renderCard helper, existing describe block patterns)
    - src/features/recipes/RecipeSectionCard.tsx (the modified file from Task 1 -- verify actual implementation)
  </read_first>
  <action>
    1. Update the `renderCard` helper to accept `sectionsCount?: number` in its overrides object (default to 1 for backwards compatibility of existing tests). Destructure it and pass to RecipeSectionCard as a prop.

    2. Add describe block "RecipeSectionCard -- RUI-01 workflow collapsible" with these test cases:
       - "shows Workflow trigger when sectionsCount > 1": render with `sectionsCount: 2`, assert `screen.getByText("Workflow")` is in the document
       - "shows Workflow trigger when section has metadata (single section)": render with `makeSection({ section_type: "basecoat" })` and `sectionsCount: 1`, assert Workflow trigger visible
       - "renders section_type select when workflow collapsible opened": render with `sectionsCount: 2`, click the Workflow trigger, assert a select trigger with text matching "Type" or "-- type --" is visible
       - "changing section_type calls onChange": render with `sectionsCount: 2`, open workflow, interact with section_type select, verify onChange called with updated section_type field
       - "changing applies_to input calls onChange": render with `sectionsCount: 2`, open workflow, type in the applies_to input, verify onChange called with applies_to value

    3. Add describe block "RecipeSectionCard -- RUI-02 workflow collapsible hidden" with:
       - "hides Workflow trigger when sectionsCount === 1 and no metadata": render with `makeSection()` and `sectionsCount: 1`, assert `screen.queryByText("Workflow")` is null
       - "shows Workflow trigger when sectionsCount === 1 but technique is set": render with `makeSection({ technique: "drybrush" })` and `sectionsCount: 1`, assert Workflow trigger visible
  </action>
  <verify>
    <automated>pnpm test -- tests/painting/recipeSectionCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - renderCard helper accepts `sectionsCount` parameter with default value 1
    - renderCard passes sectionsCount to RecipeSectionCard component
    - At least 5 test cases cover RUI-01 scenarios (visibility with multi-section, visibility with metadata, field rendering, onChange callbacks)
    - At least 2 test cases cover RUI-02 scenarios (hidden when single-section no-metadata, shown when metadata present)
    - All tests in recipeSectionCard.test.tsx pass
  </acceptance_criteria>
  <done>RUI-01 and RUI-02 have automated test coverage proving workflow collapsible appears/hides correctly and metadata controls fire onChange. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in this plan. All changes are form-local state modifications to DraftSection objects. Values flow through the existing RecipeFormSheet save path which uses parameterized `$1, $2` queries (Phase 57 delivery).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | I (Information Disclosure) | Workflow Select dropdowns | accept | Values are non-sensitive hobby metadata (basecoat, drybrush, etc.) — no PII |
| T-58-02 | T (Tampering) | applies_to free-text input | accept | Free-text stored via parameterized query; no injection risk; personal desktop app |
</threat_model>

<verification>
pnpm test -- tests/painting/recipeSectionCard.test.tsx
pnpm build
</verification>

<success_criteria>
- Workflow collapsible with 4 controls renders in RecipeSectionCard for multi-section or metadata-present sections
- Workflow collapsible is hidden for single-section recipes with no metadata set
- All existing RecipeSectionCard tests continue passing
- New RUI-01/RUI-02 tests pass
- TypeScript build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/58-recipe-form-timeline-display/58-01-SUMMARY.md`
</output>

---
phase: 58-recipe-form-timeline-display
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/recipes/SectionedTimeline.tsx
  - tests/painting/sectionedTimeline.test.tsx
autonomous: true
requirements: [RUI-03, RUI-04]

must_haves:
  truths:
    - "SectionedTimeline displays section_type as a compact outline Badge in the section header"
    - "SectionedTimeline shows technique and execution_mode as a dot-separated inline string when set"
    - "Only non-null fields appear in the dot-separated string -- no empty placeholders"
  artifacts:
    - path: "src/features/recipes/SectionedTimeline.tsx"
      provides: "section_type Badge + dot-separated workflow metadata string in section headers"
      contains: "workflowParts"
    - path: "tests/painting/sectionedTimeline.test.tsx"
      provides: "Test coverage for RUI-03 and RUI-04"
      contains: "RUI-03"
  key_links:
    - from: "src/features/recipes/SectionedTimeline.tsx"
      to: "src/types/recipeSection.ts"
      via: "RecipeSection type with section_type, technique, execution_mode, applies_to fields"
      pattern: "section_type"
---

<objective>
Extend SectionedTimeline section headers to display section_type as an outline Badge and technique/execution_mode/applies_to as a dot-separated inline metadata string.

Purpose: Gives users at-a-glance workflow context in the recipe timeline view (RUI-03, RUI-04).
Output: Modified SectionedTimeline.tsx, extended test file.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-recipe-form-timeline-display/58-CONTEXT.md
@.planning/phases/58-recipe-form-timeline-display/58-PATTERNS.md
@.planning/phases/58-recipe-form-timeline-display/58-UI-SPEC.md

<interfaces>
<!-- Key types the executor needs. -->

From src/types/recipeSection.ts:
```typescript
export interface RecipeSection {
  id: number;
  recipe_id: number;
  name: string;
  surface: string | null;
  optional: number;
  order_index: number;
  notes: string | null;
  section_type: string | null;
  technique: string | null;
  execution_mode: string | null;
  applies_to: string | null;
  created_at: string;
  updated_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add section_type Badge and dot-separated metadata string to SectionedTimeline</name>
  <files>src/features/recipes/SectionedTimeline.tsx</files>
  <read_first>
    - src/features/recipes/SectionedTimeline.tsx (full file -- see the section header div with data-testid="section-header", existing Badge usage for surface and optional, the ml-auto right-side metadata span)
    - .planning/phases/58-recipe-form-timeline-display/58-UI-SPEC.md (Component Spec 2 for exact header order and layout)
    - .planning/phases/58-recipe-form-timeline-display/58-PATTERNS.md (SectionedTimeline pattern assignments)
  </read_first>
  <action>
    **Per D-07, D-08, D-09, D-10:**

    1. In the sections.map callback, before the JSX return, compute the dot-separated metadata string:
       - Build `workflowParts` array from `[section.technique, section.execution_mode, section.applies_to].filter(Boolean)` (note: surface is NOT in this array -- it already has its own Badge; section_type is NOT in this array -- it gets a separate Badge per D-08)
       - Join with ` · ` (middle dot with spaces) to produce the dot-separated string

    2. In the section header div (data-testid="section-header"):
       - Add `flex-wrap` to the existing className for graceful wrapping
       - BEFORE the section name span, add: `{section.section_type && <Badge variant="outline" className="text-xs capitalize">{section.section_type}</Badge>}`
       - AFTER the existing optional Badge, BEFORE the ml-auto span, add: `{workflowParts.length > 0 && <span className="text-xs text-muted-foreground capitalize">{workflowParts.join(" · ")}</span>}`

    3. Header element order (left to right): section_type Badge (if set) -> section name -> surface Badge (if set) -> optional Badge (if optional) -> dot-separated workflow string (if any parts) -> ml-auto right metadata span
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Section header div has `flex-wrap` in className
    - section_type Badge renders before section name, uses variant="outline" and className includes "text-xs capitalize"
    - section_type Badge is conditionally rendered only when section.section_type is truthy
    - workflowParts array is built from [section.technique, section.execution_mode, section.applies_to].filter(Boolean)
    - Dot separator is middle dot U+00B7 with spaces (` · `)
    - Dot-separated span uses className "text-xs text-muted-foreground capitalize"
    - Dot-separated span renders only when workflowParts.length > 0
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>SectionedTimeline section headers show section_type Badge and dot-separated workflow metadata string. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add RUI-03 and RUI-04 test coverage for timeline workflow display</name>
  <files>tests/painting/sectionedTimeline.test.tsx</files>
  <read_first>
    - tests/painting/sectionedTimeline.test.tsx (full file -- see makeSection fixture factory with workflow fields already null, existing render patterns with sections/steps/paintMap props)
    - src/features/recipes/SectionedTimeline.tsx (the modified file from Task 1 -- verify actual implementation)
  </read_first>
  <action>
    1. Add describe block "SectionedTimeline -- RUI-03 (section_type badge)" with these test cases:
       - "renders section_type badge when section_type is set": create section with `section_type: "basecoat"`, render, assert `screen.getByText("basecoat")` is in the document
       - "does not render section_type badge when section_type is null": create section with `section_type: null`, render, assert no badge text matching any SECTION_TYPES value is present
       - "renders section_type badge before section name": create section with `section_type: "layer"`, render, get the section-header testid element, verify that the badge element appears before the name span in DOM order

    2. Add describe block "SectionedTimeline -- RUI-04 (dot-separated metadata)" with these test cases:
       - "shows dot-separated string with technique and execution_mode": create section with `technique: "drybrush"` and `execution_mode: "sequential"`, render, assert text matching "drybrush" and "sequential" joined by middle dot is present
       - "shows applies_to in dot string when set": create section with `applies_to: "shoulder pad"`, render, assert "shoulder pad" text is visible
       - "omits null fields from dot string": create section with `technique: "drybrush"` and `execution_mode: null`, render, assert text "drybrush" is present without trailing dot separator
       - "renders no metadata span when technique, execution_mode, and applies_to are all null": create section with all workflow fields null, render, verify no dot-separated span is present
  </action>
  <verify>
    <automated>pnpm test -- tests/painting/sectionedTimeline.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - At least 3 test cases cover RUI-03 scenarios (badge present, badge absent, badge position)
    - At least 4 test cases cover RUI-04 scenarios (full dot string, partial dot string, single field, all null)
    - All tests in sectionedTimeline.test.tsx pass including existing tests
  </acceptance_criteria>
  <done>RUI-03 and RUI-04 have automated test coverage proving section_type Badge renders correctly and dot-separated metadata string shows only non-null fields. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. SectionedTimeline is read-only display of already-persisted RecipeSection data. Values are rendered via React JSX (automatic HTML escaping).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-03 | I (Information Disclosure) | section_type/technique badges | accept | Non-sensitive hobby metadata rendered in personal desktop app |
| T-58-04 | S (Spoofing) | applies_to free-text in timeline | accept | React JSX auto-escapes; no dangerouslySetInnerHTML; desktop app with no remote content |
</threat_model>

<verification>
pnpm test -- tests/painting/sectionedTimeline.test.tsx
pnpm build
</verification>

<success_criteria>
- SectionedTimeline displays section_type as outline Badge before section name when set
- Dot-separated string shows technique, execution_mode, applies_to (only non-null values)
- No empty placeholders or trailing separators when fields are null
- All existing SectionedTimeline tests continue passing
- New RUI-03/RUI-04 tests pass
- TypeScript build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/58-recipe-form-timeline-display/58-02-SUMMARY.md`
</output>

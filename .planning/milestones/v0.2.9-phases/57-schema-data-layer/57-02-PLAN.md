---
phase: 57-schema-data-layer
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - src/features/recipes/recipeSection.ts
  - src/db/queries/recipeSections.ts
  - src/db/queries/paintingSessions.ts
  - src/features/recipes/RecipeFormSheet.tsx
  - tests/painting/recipeSections.test.ts
  - tests/painting/recipeSection.pure.test.ts
autonomous: true
requirements:
  - WF-01
  - WF-02
  - WF-03
  - WF-04
  - WF-05

must_haves:
  truths:
    - "createRecipeSection accepts and persists all four workflow metadata fields"
    - "updateRecipeSection supports COALESCE partial updates for all four workflow metadata fields"
    - "createSession accepts and persists section_name"
    - "DraftSection carries workflow metadata through the edit-save round trip"
    - "Editing a recipe with workflow metadata does not erase those fields on save"
    - "makeDraftSection returns null for all four new fields"
    - "buildDraftSections maps all four new fields from DB rows"
    - "All existing tests pass with updated fixtures"
  artifacts:
    - path: "src/features/recipes/recipeSection.ts"
      provides: "DraftSection with workflow metadata, makeDraftSection, buildDraftSections"
      contains: "section_type"
    - path: "src/db/queries/recipeSections.ts"
      provides: "createRecipeSection with $7-$10, updateRecipeSection with COALESCE $7-$10"
      contains: "section_type, technique, execution_mode, applies_to"
    - path: "src/db/queries/paintingSessions.ts"
      provides: "createSession with $7 section_name"
      contains: "section_name"
    - path: "src/features/recipes/RecipeFormSheet.tsx"
      provides: "Save path passes workflow metadata to createRecipeSection"
      contains: "sec.section_type"
  key_links:
    - from: "src/features/recipes/RecipeFormSheet.tsx"
      to: "src/db/queries/recipeSections.ts"
      via: "createRecipeSection call with 10 params"
      pattern: "createRecipeSection.*section_type.*technique.*execution_mode.*applies_to"
    - from: "src/features/recipes/recipeSection.ts"
      to: "src/types/recipeSection.ts"
      via: "DraftSection maps RecipeSection fields"
      pattern: "s\\.section_type"
---

<objective>
Update query functions, DraftSection, and the RecipeFormSheet save path to read/write workflow metadata fields, then update test fixtures.

Purpose: Complete the data layer so workflow metadata round-trips through create, update, and the DELETE-all + re-INSERT save path without silent NULL erasure. This is the critical atomicity concern — DraftSection, queries, and the save call site must all be updated together per D-08/D-09/D-10.

Output: Four modified source files, two modified test files.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-schema-data-layer/57-CONTEXT.md
@.planning/phases/57-schema-data-layer/57-RESEARCH.md
@.planning/phases/57-schema-data-layer/57-PATTERNS.md
@.planning/phases/57-schema-data-layer/57-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan depends on -->

From src/types/recipeSection.ts (after Plan 01):
- RecipeSection interface now has 13 fields including section_type, technique, execution_mode, applies_to (all string | null)
- CreateRecipeSectionInput = Omit<RecipeSection, "id" | "created_at" | "updated_at"> — automatically includes new fields
- UpdateRecipeSectionInput = Partial<Omit<CreateRecipeSectionInput, "recipe_id">> & { id: number } — automatically includes new fields as optional

From src/types/paintingSession.ts (after Plan 01):
- PaintingSession interface now has section_name: string | null
- CreateSessionInput now has section_name?: string | null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DraftSection and query functions atomically</name>
  <files>src/features/recipes/recipeSection.ts, src/db/queries/recipeSections.ts, src/db/queries/paintingSessions.ts, src/features/recipes/RecipeFormSheet.tsx</files>
  <read_first>
    src/features/recipes/recipeSection.ts
    src/db/queries/recipeSections.ts
    src/db/queries/paintingSessions.ts
    src/features/recipes/RecipeFormSheet.tsx
  </read_first>
  <action>
    CRITICAL: All four files must be updated in a single commit to prevent silent NULL erasure (Pitfall 1 from RESEARCH.md).

    **src/features/recipes/recipeSection.ts** — Per D-08, D-09:

    1. Add four nullable fields to `DraftSection` interface BEFORE `steps`: `section_type: string | null`, `technique: string | null`, `execution_mode: string | null`, `applies_to: string | null`.
    2. Add four `null` defaults in `makeDraftSection()` return object BEFORE `steps`: `section_type: null`, `technique: null`, `execution_mode: null`, `applies_to: null`.
    3. In `buildDraftSections()` return object (around line 82-89), add four field mappings BEFORE `steps`: `section_type: s.section_type ?? null`, `technique: s.technique ?? null`, `execution_mode: s.execution_mode ?? null`, `applies_to: s.applies_to ?? null`. Follow the null-fallback pattern used by DraftStep mapping at lines 73-79.

    **src/db/queries/recipeSections.ts** — Per D-11, D-12:

    1. `createRecipeSection`: Extend INSERT column list with `, section_type, technique, execution_mode, applies_to`. Extend VALUES with `, $7, $8, $9, $10`. Add four params to array: `input.section_type ?? null`, `input.technique ?? null`, `input.execution_mode ?? null`, `input.applies_to ?? null`. Total params: 10 ($1-$10).
    2. `updateRecipeSection`: Add four COALESCE lines in SET clause after `notes = $6`: `section_type = COALESCE($7, section_type)`, `technique = COALESCE($8, technique)`, `execution_mode = COALESCE($9, execution_mode)`, `applies_to = COALESCE($10, applies_to)`. Add four params to array: `input.section_type ?? null`, `input.technique ?? null`, `input.execution_mode ?? null`, `input.applies_to ?? null`. Total params: 10 ($1-$10).

    **src/db/queries/paintingSessions.ts** — Per D-13:

    `createSession`: Extend INSERT column list with `, section_name`. Extend VALUES with `, $7`. Add `input.section_name ?? null` to param array. Total params: 7 ($1-$7).

    **src/features/recipes/RecipeFormSheet.tsx** — Per D-10:

    In the save loop (around line 268-281), add four properties to the `createRecipeSection()` call object: `section_type: sec.section_type`, `technique: sec.technique`, `execution_mode: sec.execution_mode`, `applies_to: sec.applies_to`.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `DraftSection` interface has `section_type`, `technique`, `execution_mode`, `applies_to` (all `string | null`) before `steps`
    - `makeDraftSection()` returns null for all four new fields
    - `buildDraftSections()` maps `s.section_type ?? null`, `s.technique ?? null`, `s.execution_mode ?? null`, `s.applies_to ?? null`
    - `createRecipeSection` SQL has 10 columns, 10 VALUES placeholders ($1-$10), 10 param array entries
    - `updateRecipeSection` SQL has 10 params with COALESCE on $7-$10 for the four new fields
    - `createSession` SQL has 7 columns, 7 VALUES placeholders ($1-$7), 7 param array entries
    - `RecipeFormSheet.tsx` save path passes `section_type`, `technique`, `execution_mode`, `applies_to` from `sec` to `createRecipeSection`
    - `pnpm build` completes without errors
  </acceptance_criteria>
  <done>All query functions, DraftSection, and save path updated atomically — workflow metadata round-trips through create, update, and DELETE+re-INSERT save</done>
</task>

<task type="auto">
  <name>Task 2: Update test fixtures and add workflow metadata tests</name>
  <files>tests/painting/recipeSections.test.ts, tests/painting/recipeSection.pure.test.ts</files>
  <read_first>
    tests/painting/recipeSections.test.ts
    tests/painting/recipeSection.pure.test.ts
  </read_first>
  <action>
    **tests/painting/recipeSection.pure.test.ts:**

    1. Update all `RecipeSection` fixtures (SECTION_1, SECTION_2, etc.) to include the four new fields set to null: `section_type: null, technique: null, execution_mode: null, applies_to: null` — add after `notes` field.
    2. Add a test for `makeDraftSection()` verifying the return value has `section_type: null`, `technique: null`, `execution_mode: null`, `applies_to: null`.
    3. Add a test for `buildDraftSections()` verifying it maps all four new fields from DB rows. Create a test RecipeSection fixture with non-null workflow metadata (e.g., `section_type: "basecoat"`, `technique: "airbrush"`, `execution_mode: "sequential"`, `applies_to: "armor panels"`) and verify the resulting DraftSection carries those values.
    4. Update the RecipeSection type shape test (if one checks field count) — the interface now has 13 fields.

    **tests/painting/recipeSections.test.ts:**

    1. Update all `RecipeSection` fixtures to include the four new null fields.
    2. Update `CreateRecipeSectionInput` fixtures/mocks to include the four new fields.
    3. Add a test verifying `createRecipeSection` param array has 10 entries when called with workflow metadata values.
    4. Add a test verifying `updateRecipeSection` param array has 10 entries.
    5. Add a test or update existing one for `createSession` verifying param array has 7 entries including section_name.
    6. Update the RecipeSection type shape test (Group 8 or similar) — expects 13 keys now.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting/recipeSections.test.ts tests/painting/recipeSection.pure.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All RecipeSection fixtures in both test files include `section_type: null, technique: null, execution_mode: null, applies_to: null`
    - `makeDraftSection()` test asserts four new fields are null
    - `buildDraftSections()` test asserts non-null workflow metadata values carry through
    - Type shape test expects 13 keys on RecipeSection (not 9)
    - `createRecipeSection` param count test expects 10
    - `createSession` param count test expects 7
    - `pnpm test -- tests/painting/recipeSections.test.ts tests/painting/recipeSection.pure.test.ts` passes
  </acceptance_criteria>
  <done>All test fixtures updated, new tests verify workflow metadata round-trip through DraftSection and query functions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Query functions use parameterized $N syntax (existing pattern) — no injection risk.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02 | Tampering | recipeSections.ts queries | accept | All params use positional $N syntax via tauri-plugin-sql; no raw string interpolation. Existing pattern, no change in security posture. |
| T-57-03 | Information Disclosure | DraftSection round-trip | accept | Workflow metadata is user's own data on local SQLite; no multi-tenant concern. |
</threat_model>

<verification>
- `pnpm build` passes
- `pnpm test -- tests/painting/recipeSections.test.ts tests/painting/recipeSection.pure.test.ts` passes
- `pnpm test` full suite passes
</verification>

<success_criteria>
Complete data layer for workflow metadata: migration, types, const arrays, queries, DraftSection, save path, and tests all in place. Phase 58 can build forms against these types and Phase 59 can use createSession with section_name.
</success_criteria>

<output>
After completion, create `.planning/phases/57-schema-data-layer/57-02-SUMMARY.md`
</output>

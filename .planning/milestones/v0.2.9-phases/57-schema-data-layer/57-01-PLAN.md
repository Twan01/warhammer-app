---
phase: 57-schema-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/020_workflow_metadata.sql
  - src/types/recipeSection.ts
  - src/types/paintingSession.ts
autonomous: true
requirements:
  - WF-01
  - WF-02
  - WF-03
  - WF-04
  - WF-05

must_haves:
  truths:
    - "SQLite schema includes section_type, technique, execution_mode, applies_to on recipe_sections"
    - "SQLite schema includes section_name on painting_sessions"
    - "All five new columns are TEXT DEFAULT NULL"
    - "RecipeSection TypeScript interface has 13 fields (9 existing + 4 new)"
    - "PaintingSession TypeScript interface has 9 fields (8 existing + 1 new)"
    - "SECTION_TYPES, TECHNIQUES, EXECUTION_MODES const arrays exist as single source of truth"
  artifacts:
    - path: "src-tauri/migrations/020_workflow_metadata.sql"
      provides: "5 ALTER TABLE ADD COLUMN statements"
      contains: "ALTER TABLE recipe_sections ADD COLUMN section_type"
    - path: "src/types/recipeSection.ts"
      provides: "3 const arrays, 3 type aliases, extended RecipeSection interface"
      exports: ["SECTION_TYPES", "SectionType", "TECHNIQUES", "Technique", "EXECUTION_MODES", "ExecutionMode", "RecipeSection", "CreateRecipeSectionInput", "UpdateRecipeSectionInput"]
    - path: "src/types/paintingSession.ts"
      provides: "Extended PaintingSession and CreateSessionInput with section_name"
      exports: ["PaintingSession", "CreateSessionInput"]
  key_links:
    - from: "src-tauri/migrations/020_workflow_metadata.sql"
      to: "src/types/recipeSection.ts"
      via: "Column names match interface fields"
      pattern: "section_type.*technique.*execution_mode.*applies_to"
---

<objective>
Create the SQLite migration for workflow metadata columns and extend TypeScript types with const arrays.

Purpose: Establish the schema and type foundation that all subsequent plans and phases depend on. The migration adds five nullable TEXT columns across two tables; the types provide compile-time safety and const arrays for downstream dropdown values.

Output: One new migration file, two modified type files.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-schema-data-layer/57-CONTEXT.md
@.planning/phases/57-schema-data-layer/57-RESEARCH.md
@.planning/phases/57-schema-data-layer/57-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 020_workflow_metadata.sql</name>
  <files>src-tauri/migrations/020_workflow_metadata.sql</files>
  <read_first>
    src-tauri/migrations/014_session_recipe_link.sql
    src-tauri/migrations/018_recipe_sections.sql
  </read_first>
  <action>
    Create new migration file `src-tauri/migrations/020_workflow_metadata.sql` per D-07.

    Add 5 ALTER TABLE statements (one ADD COLUMN per statement per SQLite limitation):
    - `ALTER TABLE recipe_sections ADD COLUMN section_type TEXT DEFAULT NULL;`
    - `ALTER TABLE recipe_sections ADD COLUMN technique TEXT DEFAULT NULL;`
    - `ALTER TABLE recipe_sections ADD COLUMN execution_mode TEXT DEFAULT NULL;`
    - `ALTER TABLE recipe_sections ADD COLUMN applies_to TEXT DEFAULT NULL;`
    - `ALTER TABLE painting_sessions ADD COLUMN section_name TEXT DEFAULT NULL;`

    Per D-05: all four recipe_sections columns are TEXT DEFAULT NULL (nullable, additive per WF-05).
    Per D-06: section_name on painting_sessions is TEXT DEFAULT NULL (denormalized text, matches detachment_name/weapon_name pattern).

    Include a header comment with filename, phase reference, and description following the pattern from 014_session_recipe_link.sql.

    Verify 019_rules_favorites_notes.sql is the last existing migration so 020 sorts correctly.
  </action>
  <verify>
    <automated>ls src-tauri/migrations/0*.sql | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `src-tauri/migrations/020_workflow_metadata.sql` exists
    - Contains exactly 5 ALTER TABLE ... ADD COLUMN statements
    - Four columns on recipe_sections: section_type, technique, execution_mode, applies_to — all TEXT DEFAULT NULL
    - One column on painting_sessions: section_name — TEXT DEFAULT NULL
    - File sorts after 019_rules_favorites_notes.sql
    - Header comment includes "020_workflow_metadata.sql" and "Phase 57"
  </acceptance_criteria>
  <done>Migration file exists with 5 nullable TEXT columns across 2 tables</done>
</task>

<task type="auto">
  <name>Task 2: Extend RecipeSection and PaintingSession TypeScript types with const arrays</name>
  <files>src/types/recipeSection.ts, src/types/paintingSession.ts</files>
  <read_first>
    src/types/recipeSection.ts
    src/types/paintingSession.ts
    src/types/unit.ts
  </read_first>
  <action>
    **recipeSection.ts** — Per D-01, D-02, D-03, D-04:

    Add three const arrays and their type aliases BEFORE the RecipeSection interface, following the `PAINTING_STATUS_ORDER` pattern from unit.ts:
    - `SECTION_TYPES` with values per D-01: `["prep", "basecoat", "shade", "layer", "detail", "effect", "finishing"] as const` and derived type `SectionType`
    - `TECHNIQUES` with values per D-02: `["brush", "sponge", "drybrush", "airbrush", "oil-enamel", "pigment", "decal", "mixed", "other"] as const` and derived type `Technique`
    - `EXECUTION_MODES` with values per D-03: `["sequential", "batch", "parallel"] as const` and derived type `ExecutionMode`

    Add four new nullable fields to the RecipeSection interface AFTER `notes` and BEFORE `created_at`:
    - `section_type: string | null`
    - `technique: string | null`
    - `execution_mode: string | null`
    - `applies_to: string | null`

    The existing `CreateRecipeSectionInput` and `UpdateRecipeSectionInput` Omit-based types automatically include the new fields — no changes needed to those lines.

    **paintingSession.ts** — Per D-06:

    Add `section_name: string | null` to the `PaintingSession` interface after `recipe_step_id`.
    Add `section_name?: string | null` to `CreateSessionInput` after `recipe_step_id` (using optional `?` to match the existing pattern for recipe_id and recipe_step_id).
  </action>
  <verify>
    <automated>pnpm build 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/recipeSection.ts` exports `SECTION_TYPES` (7 values), `SectionType`, `TECHNIQUES` (9 values), `Technique`, `EXECUTION_MODES` (3 values), `ExecutionMode`
    - `RecipeSection` interface has 13 fields: id, recipe_id, name, surface, optional, order_index, notes, section_type, technique, execution_mode, applies_to, created_at, updated_at
    - `src/types/paintingSession.ts` PaintingSession has `section_name: string | null`
    - `CreateSessionInput` has `section_name?: string | null`
    - `pnpm build` completes without type errors (existing code uses SELECT * so read paths are compatible)
  </acceptance_criteria>
  <done>TypeScript types extended with 4 workflow metadata fields on RecipeSection, 3 const arrays with type aliases, and section_name on PaintingSession</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan modifies schema and types only — no user input processing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | 020_workflow_metadata.sql | accept | Migration is additive-only ALTER TABLE with DEFAULT NULL; no data loss risk. Existing data unchanged per WF-05. |
</threat_model>

<verification>
- `pnpm build` passes (TypeScript compilation)
- Migration file sorts correctly after 019
- All const arrays have exact values from D-01, D-02, D-03
</verification>

<success_criteria>
Migration file and type extensions are in place. TypeScript compiles cleanly. Downstream plans (57-02, Phase 58) can import const arrays and use extended types.
</success_criteria>

<output>
After completion, create `.planning/phases/57-schema-data-layer/57-01-SUMMARY.md`
</output>

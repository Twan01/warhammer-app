---
phase: 16-design-overhaul
plan: 04
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/features/painting-projects/PaintingProjectsPage.tsx
  - src/features/paints/PaintsPage.tsx
  - src/features/recipes/RecipesPage.tsx
  - src/features/units/PlaybookTab.tsx
autonomous: true
requirements:
  - PAGE-HEADER-PAINTING
  - PAGE-HEADER-PAINTS
  - PAGE-HEADER-RECIPES
  - TABULAR-NUMS-PLAYBOOK
must_haves:
  truths:
    - "Painting Projects page header h1 reads 'Painting Projects' in text-3xl font-semibold tracking-tight, with the subtitle 'Active units being worked on right now' below"
    - "Paints page header h1 reads 'Paints' in text-3xl font-semibold tracking-tight, with the subtitle 'Your paint collection, linked to recipes' below"
    - "Recipes page header h1 reads 'Recipes' in text-3xl font-semibold tracking-tight, with the subtitle 'Documented paint schemes for your models' below"
    - "All three page headers separated from page content by hairline border-b border-border/40 + pb-6"
    - "PlaybookTab stat block values (M / T / Sv / W / Ld / OC) render with tabular-nums so the stat row is column-aligned"
    - "Phase 4 KanbanBoard drag-and-drop functionality is preserved (no changes to KanbanColumn / KanbanCard / kanbanUtils)"
  artifacts:
    - path: "src/features/painting-projects/PaintingProjectsPage.tsx"
      provides: "Upgraded page header"
      contains: "text-3xl font-semibold tracking-tight"
    - path: "src/features/paints/PaintsPage.tsx"
      provides: "Upgraded page header"
      contains: "text-3xl font-semibold tracking-tight"
    - path: "src/features/recipes/RecipesPage.tsx"
      provides: "Upgraded page header"
      contains: "text-3xl font-semibold tracking-tight"
    - path: "src/features/units/PlaybookTab.tsx"
      provides: "Stat block values with tabular-nums"
      contains: "tabular-nums"
  key_links:
    - from: "Each page h1"
      to: "Phase 16 typography"
      via: "className='text-3xl font-semibold tracking-tight'"
      pattern: "text-3xl\\s+font-semibold\\s+tracking-tight"
    - from: "PlaybookTab stat values"
      to: "Phase 16 numeric contract"
      via: "tabular-nums on stat block render site"
      pattern: "tabular-nums"
---

<objective>
Upgrade three more page headers (Painting Projects, Paints, Recipes) to the Phase 16 visual contract, plus add tabular-nums to PlaybookTab stat block values (M / T / Sv / W / Ld / OC) so the stat row aligns cleanly. These pages are visited daily during active hobby work and benefit immediately from the typographic hierarchy upgrade.

Purpose: Complete the page-header upgrade across the painting workflow (Painting Projects Kanban, Paint inventory, Recipes). The PlaybookTab tabular-nums change makes the stat block on every unit detail sheet feel like a proper datasheet — currently the digits jiggle because the default font has proportional digits.

Output: Four files modified — three page headers plus PlaybookTab. KanbanBoard, KanbanColumn, KanbanCard, and any drag-and-drop wiring are NOT touched (Phase 4 contract).
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-design-overhaul/16-CONTEXT.md
@.planning/phases/16-design-overhaul/16-RESEARCH.md
@.planning/phases/16-design-overhaul/16-UI-SPEC.md

<interfaces>
<!-- Universal page header pattern (verbatim from UI-SPEC §Page Header Contract) -->
```tsx
<div className="flex items-center justify-between pb-6 border-b border-border/40">
  <div>
    <h1 className="text-3xl font-semibold tracking-tight">{Page Title}</h1>
    <p className="text-sm text-muted-foreground mt-1">{subtitle}</p>
  </div>
  <div className="flex items-center gap-2">{CTA buttons}</div>
</div>
```

Subtitles per UI-SPEC §Page Header Contract:
- Painting Projects: "Active units being worked on right now"
- Paints: "Your paint collection, linked to recipes"
- Recipes: "Documented paint schemes for your models"

PlaybookTab stat block (per RESEARCH §Pattern 4 note):
"Stat values (M/T/Sv/W/Ld/OC) are rendered via formatStatValue() which returns React.ReactNode. The wrapping <span> or <td> at the render site should receive tabular-nums."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Upgrade Painting Projects page header</name>
  <files>src/features/painting-projects/PaintingProjectsPage.tsx</files>
  <read_first>
    - src/features/painting-projects/PaintingProjectsPage.tsx (read entire file — locate the existing h1 with text-xl, identify what CTA buttons sit next to it, confirm the KanbanBoard render call is independent of the header)
    - tests for painting-projects (run: glob `tests/painting-projects/*.test.{ts,tsx}` to enumerate; confirm header text isn't queried in a fragile way)
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md §Page Header Contract (Painting Projects subtitle: "Active units being worked on right now")
  </read_first>
  <action>
    Locate the existing page header structure: a flex container holding `<h1 className="text-xl font-semibold">Painting Projects</h1>` and any CTA buttons. Replace the entire wrapping div + h1 with:
    ```tsx
    <div className="flex items-center justify-between pb-6 border-b border-border/40">
      <div>
        <h1 className="text-3xl font-semibold tracking-tight">Painting Projects</h1>
        <p className="text-sm text-muted-foreground mt-1">Active units being worked on right now</p>
      </div>
      <div className="flex items-center gap-2">
        {/* preserve every existing CTA button verbatim — typically AddProjectPicker trigger */}
      </div>
    </div>
    ```

    Read the file first to identify the existing CTA(s) — likely an `<AddProjectPicker />` component or a button — and copy them inside the new `<div className="flex items-center gap-2">` slot.

    Do NOT modify:
    - KanbanBoard render call or its props
    - KanbanEmptyState render (handled in plan 16-06)
    - Any drag-and-drop handlers (Phase 4 contract — drag onto / from columns must keep working)
    - The outer `<div className="flex flex-col gap-6 p-6">` page wrapper
  </action>
  <verify>
    <automated>npx vitest run tests/painting-projects/ --reporter=verbose 2>&1 | tail -20</automated>

    Manual checks:
    1. `Get-Content src/features/painting-projects/PaintingProjectsPage.tsx | Select-String -Pattern 'text-3xl font-semibold tracking-tight'` returns at least one match
    2. `Get-Content src/features/painting-projects/PaintingProjectsPage.tsx | Select-String -Pattern 'Active units being worked on right now'` returns exactly one match
    3. `Get-Content src/features/painting-projects/PaintingProjectsPage.tsx | Select-String -Pattern 'pb-6 border-b border-border/40'` returns at least one match
    4. `Get-Content src/features/painting-projects/PaintingProjectsPage.tsx | Select-String -Pattern '<KanbanBoard'` returns at least one match (KanbanBoard render preserved)
  </verify>
  <acceptance_criteria>
    - `PaintingProjectsPage.tsx` contains exactly one page-level h1 with className `text-3xl font-semibold tracking-tight` and inner text `Painting Projects`
    - File contains the literal subtitle string `Active units being worked on right now`
    - File header div className contains `pb-6 border-b border-border/40`
    - File still imports and renders `KanbanBoard` (drag-drop functionality preserved)
    - `npx vitest run tests/painting-projects/` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Painting Projects page renders the new text-3xl h1 + subtitle + hairline border. KanbanBoard rendering and Phase 4 drag-drop are untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Upgrade Paints and Recipes page headers</name>
  <files>src/features/paints/PaintsPage.tsx, src/features/recipes/RecipesPage.tsx</files>
  <read_first>
    - src/features/paints/PaintsPage.tsx (read entire file — locate header, identify filter bar position, CTA buttons)
    - src/features/recipes/RecipesPage.tsx (read entire file — locate header, identify CTA buttons, RecipeTable render position)
    - tests for paints and recipes (glob: tests/paints/*.test.{ts,tsx} and tests/recipes/*.test.{ts,tsx} — verify header queries are tolerant)
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md §Page Header Contract (Paints + Recipes subtitles verbatim)
  </read_first>
  <action>
    Apply the same page-header upgrade pattern to both files.

    Edit A — PaintsPage.tsx:
    Replace the existing header div + h1 with:
    ```tsx
    <div className="flex items-center justify-between pb-6 border-b border-border/40">
      <div>
        <h1 className="text-3xl font-semibold tracking-tight">Paints</h1>
        <p className="text-sm text-muted-foreground mt-1">Your paint collection, linked to recipes</p>
      </div>
      <div className="flex items-center gap-2">
        {/* preserve existing CTAs (Add paint button, etc.) verbatim */}
      </div>
    </div>
    ```

    Edit B — RecipesPage.tsx:
    Replace the existing header div + h1 with:
    ```tsx
    <div className="flex items-center justify-between pb-6 border-b border-border/40">
      <div>
        <h1 className="text-3xl font-semibold tracking-tight">Recipes</h1>
        <p className="text-sm text-muted-foreground mt-1">Documented paint schemes for your models</p>
      </div>
      <div className="flex items-center gap-2">
        {/* preserve existing CTAs (New recipe button, etc.) verbatim */}
      </div>
    </div>
    ```

    For both edits: read the source first to enumerate the existing CTA buttons and copy them inside the new `<div className="flex items-center gap-2">` slot. Do NOT remove or rename any buttons; do NOT change the RecipesPage `<RecipeTable>` render, `RecipeDetailSheet` mount, `PaintInventoryFilters` render, or filter wiring.

    Do NOT modify the page wrapper `<div className="flex flex-col gap-6 p-6">`. Do NOT touch the EmptyState components (those are in plan 16-07).
  </action>
  <verify>
    <automated>npx vitest run tests/paints/ tests/recipes/ --reporter=verbose 2>&1 | tail -25</automated>

    Manual checks:
    1. `Get-Content src/features/paints/PaintsPage.tsx | Select-String -Pattern 'text-3xl font-semibold tracking-tight'` — at least one match
    2. `Get-Content src/features/paints/PaintsPage.tsx | Select-String -Pattern 'Your paint collection, linked to recipes'` — exactly one match
    3. `Get-Content src/features/paints/PaintsPage.tsx | Select-String -Pattern 'pb-6 border-b border-border/40'` — at least one match
    4. `Get-Content src/features/recipes/RecipesPage.tsx | Select-String -Pattern 'text-3xl font-semibold tracking-tight'` — at least one match
    5. `Get-Content src/features/recipes/RecipesPage.tsx | Select-String -Pattern 'Documented paint schemes for your models'` — exactly one match
    6. `Get-Content src/features/recipes/RecipesPage.tsx | Select-String -Pattern 'pb-6 border-b border-border/40'` — at least one match
  </verify>
  <acceptance_criteria>
    - `PaintsPage.tsx` page-level h1 className is `text-3xl font-semibold tracking-tight` with inner text `Paints`
    - `PaintsPage.tsx` contains literal subtitle `Your paint collection, linked to recipes`
    - `PaintsPage.tsx` page-header wrapper className contains `pb-6 border-b border-border/40`
    - `RecipesPage.tsx` page-level h1 className is `text-3xl font-semibold tracking-tight` with inner text `Recipes`
    - `RecipesPage.tsx` contains literal subtitle `Documented paint schemes for your models`
    - `RecipesPage.tsx` page-header wrapper className contains `pb-6 border-b border-border/40`
    - Both files still import and render their existing major components (PaintInventoryFilters / PaintRow / RecipeTable / RecipeDetailSheet — verify by spot-check)
    - `npx vitest run tests/paints/ tests/recipes/` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Paints and Recipes pages render upgraded text-3xl h1 + subtitle + hairline border. Filter bars, table rendering, and detail sheets unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add tabular-nums to PlaybookTab stat block values</name>
  <files>src/features/units/PlaybookTab.tsx</files>
  <read_first>
    - src/features/units/PlaybookTab.tsx (read entire file — locate the stat block grid; per RESEARCH.md "Stat values (M/T/Sv/W/Ld/OC) are rendered via formatStatValue() which returns React.ReactNode. The wrapping <span> or <td> at the render site should receive tabular-nums.")
    - tests for playbook (glob: tests/units/*Playbook*.test.{ts,tsx}, tests/playbook/*.test.{ts,tsx})
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md §Component Inventory (PlaybookTab listed as tabular-nums target)
    - .planning/phases/16-design-overhaul/16-RESEARCH.md §Pattern 4 (PlaybookTab note)
  </read_first>
  <action>
    Locate the stat block grid in PlaybookTab.tsx — typically a row of 6 stat cells (M, T, Sv, W, Ld, OC) where each cell renders `formatStatValue(unit.stat_X)` inside a span, div, or td.

    Add `tabular-nums` to the className of the element that wraps the formatStatValue call. There are two acceptable patterns depending on the existing structure:

    Pattern A — if each cell is a `<div>` or `<span>` with its own className:
    ```tsx
    <span className="text-base font-semibold tabular-nums">{formatStatValue(unit.stat_movement)}</span>
    ```
    Add `tabular-nums` to each of the six stat cell wrappers.

    Pattern B — if cells are rendered via a shared sub-component or .map over a stat config array:
    Add `tabular-nums` to the shared className inside that sub-component or the .map render. ONE addition covers all six stats.

    Read the file first to determine which pattern is in use. Apply the minimal edit — if it's Pattern B, prefer that single change over six individual edits.

    Do NOT modify:
    - The formatStatValue helper itself
    - Stat labels (M / T / Sv / W / Ld / OC text labels — those are letters, not numbers, no tabular-nums needed)
    - Abilities list, keywords list, strategy notes section, or PlaybookTab's onSave wiring (Phase 9 contract)
    - The Tabs structure inside UnitDetailSheet
  </action>
  <verify>
    <automated>npx vitest run tests/units/ --reporter=verbose 2>&1 | tail -20</automated>
    (If a more specific PlaybookTab test file exists, target it: e.g., `npx vitest run tests/units/PlaybookTab.test.tsx`)

    Manual checks:
    1. `Get-Content src/features/units/PlaybookTab.tsx | Select-String -Pattern 'tabular-nums'` returns at least one match
    2. `Get-Content src/features/units/PlaybookTab.tsx | Select-String -Pattern 'formatStatValue'` confirms call sites still exist
    3. The line(s) containing `formatStatValue(` should have `tabular-nums` either in the same JSX element's className or in a parent .map/render that wraps every stat value
  </verify>
  <acceptance_criteria>
    - `src/features/units/PlaybookTab.tsx` contains the literal substring `tabular-nums` at least once
    - Every `formatStatValue(...)` call site has `tabular-nums` applied either directly on the wrapping element or inherited from a parent element in the .map/render hierarchy
    - `formatStatValue` and the stat block data flow are unchanged (no API changes)
    - PlaybookTab still renders all 6 stats (M, T, Sv, W, Ld, OC) — count the formatStatValue calls or stat labels to verify nothing was deleted
    - `npx vitest run tests/units/` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    PlaybookTab stat block values render with tabular-nums for clean column alignment. Stat data flow, labels, and Phase 9 inline-save behavior are unchanged.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite ≥ 279 passing, 0 failing
- `npx tsc --noEmit` — no TS errors
- Manual visual check (Plan 16-08): Painting Projects, Paints, Recipes h1 visually dominant; PlaybookTab stat row digits align column-wise
</verification>

<success_criteria>
- PaintingProjectsPage h1: `text-3xl font-semibold tracking-tight` + subtitle "Active units being worked on right now" + hairline border
- PaintsPage h1: `text-3xl font-semibold tracking-tight` + subtitle "Your paint collection, linked to recipes" + hairline border
- RecipesPage h1: `text-3xl font-semibold tracking-tight` + subtitle "Documented paint schemes for your models" + hairline border
- PlaybookTab stat block values: `tabular-nums` applied at the formatStatValue render site (or shared parent)
- KanbanBoard / drag-drop / RecipeTable / PaintRow / Phase 9 PlaybookTab onSave all unchanged
- Full vitest suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-overhaul/16-04-SUMMARY.md` documenting:
- Each page-header before/after diff
- Where tabular-nums was added in PlaybookTab (Pattern A multi-add or Pattern B shared)
- Confirmation no Phase 4 / Phase 9 contracts touched
- Vitest pass count
</output>

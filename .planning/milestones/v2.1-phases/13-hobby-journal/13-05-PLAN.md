---
phase: 13-hobby-journal
plan: "05"
type: execute
wave: 5
depends_on: ["13-04"]
files_modified: []
autonomous: false
requirements:
  - JOUR-01
  - JOUR-02
  - JOUR-03
  - JOUR-04
  - JOUR-05
  - JOUR-06
must_haves:
  truths:
    - "User can open the unit detail sheet → Journal tab and the tab renders Sessions + Photos sections"
    - "User can log a session (date defaults to today, duration in minutes, optional notes); session appears in the list immediately, sorted newest first"
    - "User can delete a session via the Trash2 icon button; row disappears immediately (optimistic) and stays gone after the mutation completes"
    - "User can click 'Attach Photo', select an image in the OS dialog, choose a stage label, optionally type a caption, click 'Save Photo'; thumbnail appears in the 3-col grid with the asset:// URL serving correctly (no broken image)"
    - "User can click a thumbnail and a sibling Dialog opens showing the full-size image with stage_label as title and caption as description (verifies sibling portal, not nested)"
    - "User can hover a thumbnail and click the × button; the photo disappears from the grid (optimistic delete via useDeleteUnitPhoto)"
    - "User can delete a unit that has photos; after confirmation, the unit is removed AND the photo files no longer exist in %APPDATA%\\com.hobbyforge.app\\ (verified via Windows Explorer or PowerShell)"
    - "Asset protocol is functional: opening DevTools → Network tab while a photo is rendering shows the asset://localhost/... request returning 200, not blocked"
  artifacts: []
  key_links:
    - from: "Live Tauri app (running via pnpm tauri dev)"
      to: "JournalTab → useJournalSessions hook → painting_sessions table → SQLite file in AppData"
      via: "User-triggered button clicks; observed in webview"
      pattern: "manual"
    - from: "Live Tauri app"
      to: "image_assets table + photo files in %APPDATA%\\com.hobbyforge.app\\"
      via: "tauri-plugin-fs writeFile + asset:// protocol resolved by tauri.conf.json scope"
      pattern: "manual"
---

<objective>
Manually verify Phase 13 (Hobby Journal) end-to-end in the live Tauri WebView. jsdom + vitest cannot exercise the native file dialog, the asset:// protocol, or actual disk I/O — so the contract for JOUR-04, JOUR-05, JOUR-06 ultimately gates here. Plans 13-00..04 have closed all automated test gaps; this plan confirms the live behavior matches.

Purpose: Phase exit gate. Without this manual sign-off the asset protocol could be misconfigured, the file dialog could be blocked by a missing capability, or the JOUR-06 disk cleanup could be silently failing — all situations the test suite cannot detect.

Output: One human-verified PASS/FAIL log; no code changes (unless a Rule 1 bug is caught and auto-fixed during the smoke test, mirroring the Plan 11-03 pattern).

Note: This plan modifies NO files (`files_modified: []`). It is a pure checkpoint with one `checkpoint:human-verify` task.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-hobby-journal/13-CONTEXT.md
@.planning/phases/13-hobby-journal/13-UI-SPEC.md
@.planning/phases/13-hobby-journal/13-VALIDATION.md
@.planning/phases/13-hobby-journal/13-04-PLAN.md

@src/features/units/JournalTab.tsx
@src/features/units/UnitDetailSheet.tsx
@src/features/units/CollectionPage.tsx
@src/features/units/UnitDeleteDialog.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual smoke-test — JOUR-01..06 live verification in Tauri</name>
  <files>(none — pure verification checkpoint; no source files are modified by this task)</files>
  <read_first>
    - .planning/phases/13-hobby-journal/13-VALIDATION.md (§Manual-Only Verifications — the 4 explicit live checks listed)
    - .planning/phases/13-hobby-journal/13-UI-SPEC.md (§Interaction Contracts + §Copywriting Contract — verify wording matches exactly)
    - .planning/phases/13-hobby-journal/13-CONTEXT.md (§Decisions — JOUR-06 silent cleanup, sibling portal pattern, fixed stage presets + Other)
  </read_first>
  <action>
**Context — what was built across Plans 13-00 → 13-04 (no code changes in this task; this is a verification checkpoint):**
- Migration 005 added `painting_sessions` table + `image_assets.stage_label` column
- Two Tauri plugins (`tauri-plugin-fs` + `tauri-plugin-dialog`) installed, registered in `lib.rs`, granted capabilities (`fs:default`, `fs:allow-appdata-read-recursive`, `fs:allow-appdata-write-recursive`, `dialog:default`, `dialog:allow-open`)
- Asset protocol enabled in `tauri.conf.json` with `$APPDATA/**` scope
- Query modules: `src/db/queries/paintingSessions.ts` (3 fns), `src/db/queries/unitPhotos.ts` (4 fns including `getPhotoFilenamesByUnit` for JOUR-06 cleanup)
- Hooks: `src/hooks/useJournalSessions.ts` (3 hooks + key factory), `src/hooks/useUnitPhotos.ts` (3 hooks + UnitPhotoWithUrl type, with appDataDir cached once at hook level)
- `src/features/units/JournalTab.tsx` (~250 lines): Sessions section (inline log form, sessions list newest-first, optimistic delete) + Photos section (Attach Photo dialog flow with crypto.randomUUID filenames, Stage Select with 6 presets + Other free-text, 3-col thumbnail grid, hover × delete, click → onPhotoClick callback)
- `src/features/units/UnitDetailSheet.tsx`: third TabsTrigger value="journal" + new `onPhotoClick` prop
- `src/features/units/CollectionPage.tsx`: `lightboxPhoto` state + sibling `<Dialog>` with DialogTitle (stage_label), DialogDescription (caption), max-w-2xl, max-h-[70vh] image
- `src/features/units/UnitDeleteDialog.tsx`: `handleConfirm` queries photo ids + filenames before SQL DELETE, deletes image_assets rows explicitly (polymorphic table, no CASCADE), removes files from `%APPDATA%\com.hobbyforge.app\` silently (failures swallowed)

**17 automated Phase 13 tests pass** (4 migration005 + 4 paintingSessionQueries + 5 unitPhotoQueries + 2 useJournalSessions + 2 JournalTab). This manual checkpoint gates: native file dialog, asset:// protocol resolution, on-disk file write/remove behavior, and sibling-portal correctness — all of which jsdom cannot exercise.

**Setup (run once before starting):**

1. From the repo root, start the live Tauri dev build:
   ```powershell
   pnpm tauri dev
   ```
   Wait for the HobbyForge window to open.

2. Open Windows Explorer to `%APPDATA%\com.hobbyforge.app\` so you can watch files appear/disappear:
   ```powershell
   explorer "$env:APPDATA\com.hobbyforge.app"
   ```

3. In the running Tauri app, open DevTools (Ctrl+Shift+I or right-click → Inspect) so you can watch console + Network tabs during the photo flow.

**Procedure (perform 10 steps in order; report PASS/FAIL for each):**

| # | Req | Action | Expected | Pass criteria |
|---|-----|--------|----------|--------------|
| 1 | JOUR-01 | Navigate to Collection → click any unit row → click the "Journal" tab in the detail sheet | Tab opens; "Sessions" sub-heading visible; inline log form visible with Date (today's date pre-filled), Duration field, Notes textarea, "Log Session" button | Tab is the third tab after Details + Playbook; date input shows today; "No sessions logged yet." appears below the form |
| 2 | JOUR-01 | In the log form: keep date as today, type Duration `45`, type Notes "Smoke test session", click "Log Session" | Session appears in the list below the form; form resets (date back to today, duration empty, notes empty) | Session row shows today's date on the left, "45 min" + Trash2 button on the right, "Smoke test session" body visible |
| 3 | JOUR-02 | Log a SECOND session with date set one day in the past, duration 30, notes empty | New session appears ABOVE the first one (newest first by session_date) | Two rows visible; today's row appears first, yesterday's row second |
| 4 | JOUR-03 | Click the Trash2 icon button on the second (older) row | Row disappears immediately (optimistic update); list now shows only one session | Cache update is instant; no flicker; no toast appears (only error toasts on rollback) |
| 5 | JOUR-04 | In the Photos section, click "Attach Photo" → select any local image (.png/.jpg/.jpeg/.webp); choose stage = "Primed"; type caption "Smoke test photo"; click "Save Photo" | Thumbnail appears in the 3-col grid; form resets (no file selected, stage cleared, caption empty); a new file appears in `%APPDATA%\com.hobbyforge.app\` with a UUID-style filename + the original extension | Visual: thumbnail visible at h-24 size; "Primed" label visible centered below; DevTools Network tab shows asset://localhost/... request returning 200 (NOT blocked, NOT 404) |
| 6 | JOUR-04 | Click "Attach Photo" again → select image; choose stage = "Other"; type "Wash test" in the revealed text input; type caption "Wet blending"; Save Photo | Second thumbnail appears in the grid with "Wash test" stage label; another UUID file appears on disk | Custom "Other" stage_label is what shows under the thumbnail (literally "Wash test", not "Other") |
| 7 | JOUR-05 | Click the FIRST thumbnail | Sibling Dialog opens showing full-size photo, max ~70vh height, "Primed" as DialogTitle, "Smoke test photo" as DialogDescription | Dialog is a SIBLING of the Sheet — closing the Dialog (× or Escape) does NOT close the unit detail sheet; both portals coexist correctly |
| 8 | JOUR-05 | Hover over either thumbnail → the × delete button appears top-right with a black/red overlay; click it | Photo disappears from grid immediately (optimistic); file is removed from disk in `%APPDATA%\com.hobbyforge.app\` | No nested-portal focus trap; the Sheet remains open and interactive |
| 9 | JOUR-06 | Confirm at least one photo remains attached. Note its filename in `%APPDATA%\com.hobbyforge.app\`. Then close the Journal tab → click "Delete Unit" in the SheetFooter → confirm "Delete" in the dialog | Unit row disappears from the table; success toast "Unit deleted." appears; the photo file in `%APPDATA%\com.hobbyforge.app\` is GONE (verify via Explorer refresh) | No second confirmation dialog about photos; no error toast; Explorer shows the file was removed |
| 10 | JOUR-06 | (Edge case) On a different unit, attach a photo, then manually delete that photo file in Explorer first, THEN delete the unit | Unit deletes successfully; no error toast despite the file already being missing; the for/of loop swallows individual file remove failures per the silent-cleanup contract | Success path completes; "Unit deleted." toast appears |

**Reporting:**

For each step, report PASS or FAIL. If FAIL, include:
- Step number
- Observed behavior
- Console errors (DevTools → Console tab) — capture full text verbatim
- Network tab status code if the failure involves the asset:// URL

If all 10 steps pass, type `approved` to resume. If a fixable bug is caught (e.g. mis-keyed copy, missing aria-label, wrong CSS class), describe it; the executor may permit a Rule 1 auto-fix in the same commit (mirror Plan 11-03 star-button refactor pattern).

**Final regression checks (run in a separate terminal AFTER all 10 manual steps PASS, BEFORE recording approval):**
- `pnpm test` — full Vitest suite must exit 0
- `pnpm tsc --noEmit` — must exit 0
- `cargo build --manifest-path src-tauri/Cargo.toml` — must exit 0
  </action>
  <verify>
    <automated>pnpm test && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - User reports PASS for all 10 verification steps OR explicitly approves with `approved`
    - If any FAIL is reported, the executor either fixes it inline (Rule 1 auto-fix, single-file scope) and re-verifies, or escalates with diagnostic info
    - No console errors related to: asset:// URL resolution, tauri-plugin-fs/dialog permissions, missing capability grants, migration 005 application, or sibling portal nesting
    - Photo files in `%APPDATA%\com.hobbyforge.app\` confirmed removed after step 9 unit-delete (Explorer refresh shows the file is gone)
    - `pnpm test` exits 0 — final regression check passes
    - `pnpm tsc --noEmit` exits 0 — final regression check passes
    - `cargo build --manifest-path src-tauri/Cargo.toml` exits 0 — final regression check passes
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>All 10 manual verification steps PASS in live Tauri app; user types approved; final regression triple (pnpm test / tsc / cargo build) all exit 0.</done>
</task>

</tasks>

<verification>
- Live Tauri app starts cleanly (`pnpm tauri dev` no error spew on startup)
- All 10 manual steps PASS
- `pnpm test` (full suite) still exits 0
- `pnpm tsc --noEmit` still exits 0
- `cargo build --manifest-path src-tauri/Cargo.toml` still exits 0
</verification>

<success_criteria>
- Phase 13 is verified end-to-end in real Tauri WebView
- All 6 phase requirements (JOUR-01..06) confirmed working
- No outstanding bugs; phase ready for `/gsd:verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/13-hobby-journal/13-05-SUMMARY.md` listing:
- All 10 manual smoke-test steps with PASS/FAIL
- Any Rule 1 auto-fixes applied during smoke test (mirroring Plan 11-03 SUMMARY structure)
- Confirmation that asset protocol resolves and JOUR-06 disk cleanup is verified live
- Phase 13 marked complete and ready for /gsd:verify-work
</output>

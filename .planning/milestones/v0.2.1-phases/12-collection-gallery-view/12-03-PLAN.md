---
phase: 12-collection-gallery-view
plan: 03
type: execute
wave: 3
depends_on:
  - "12-02"
files_modified: []
autonomous: false
requirements:
  - UI-04
  - UI-05
  - UI-06
must_haves:
  truths:
    - "Live Tauri app: Collection page header row shows three controls in order — `Collection` heading, two icon-pair toggle buttons (Table / Gallery), and `+ Add Unit` button"
    - "Live Tauri app: Default view on first launch is Table (Table button has visible bg-muted neutral fill, Gallery button has none); clicking Gallery flips the active highlight and replaces the table with a card grid"
    - "Live Tauri app: Choosing Gallery and quitting + reopening the app keeps Gallery as the active view (localStorage persistence — no flash to Table on cold start)"
    - "Live Tauri app: Gallery cards render in a responsive grid — 2 columns on a narrow window, 3 columns at medium width, 4 columns on a wide window (resize the window to confirm)"
    - "Live Tauri app: Each gallery card shows a 96px circular ring with a zinc-grey track and a primary-color filled arc (NOT faction-coloured), the painted percentage as text in the centre, the unit name, a faction badge filled with the faction's color_theme, the painting status text, the model count + points line, and a flame icon ONLY for active projects"
    - "Live Tauri app: Clicking any gallery card opens the existing UnitDetailSheet with that unit's details (same behavior as clicking a table row)"
    - "Live Tauri app: Pressing Tab to focus a gallery card and pressing Enter or Space opens the detail sheet (Space does NOT scroll the page — Pitfall 4 preventDefault holds)"
    - "Live Tauri app: Setting any filter in Table view (search text, faction, status, category, active-only), then switching to Gallery preserves the filter — only matching cards render. Switching back to Table preserves the same filter (UI-06)"
    - "Live Tauri app: Opening a unit's detail sheet from a gallery card, editing the painting percentage and saving, returning to the Collection page shows the ring updated to the new percentage in real time (React Query invalidation flow unchanged)"
    - "Live Tauri app: Empty state — clearing all units from a faction filter combination renders the existing 'No units found' empty state inside the gallery branch, with the 'Clear filters' button working as in Table view"
    - "No console errors related to PaintingRing, useCollectionViewMode, UnitGallery, or localStorage during any of the above interactions"
    - "`pnpm test` (full suite) exits 0 (final regression check)"
    - "`pnpm tsc --noEmit` exits 0 (final regression check)"
  artifacts: []
  key_links:
    - from: "Live Tauri renderer"
      to: "src/hooks/useCollectionViewMode.ts"
      via: "CollectionPage useCollectionViewMode invocation → localStorage round-trip"
      pattern: "view mode persists across app restart"
    - from: "Live Tauri renderer"
      to: "src/components/common/PaintingRing.tsx"
      via: "UnitGallery renders <PaintingRing percentage={unit.painting_percentage ?? 0} /> inside each card"
      pattern: "visible 96px ring with primary-color arc and centred percentage text"
    - from: "Live Tauri renderer"
      to: "src/features/units/collectionFilters.ts"
      via: "Zustand store untouched on view toggle — filtered cards reflect filtered units"
      pattern: "filter applied in Table view persists to Gallery view"
---

<objective>
Final manual verification gate for Phase 12. The automated test suite cannot prove (a) the visual quality of the painting ring at native pixel density and the correctness of the dashoffset arc rendering in a real GPU-rendered window, (b) the responsive grid breakpoints (2 → 3 → 4 columns) when the window is resized, (c) the seamless localStorage persistence across an actual app restart, or (d) the integration with the existing UnitDetailSheet sibling-portal pattern when triggered from a gallery card click. This checkpoint asks the user to run `pnpm tauri dev` and confirm UI-04, UI-05, and UI-06 in person, exercising every interaction the unit tests cover — but in the live app.

Purpose: Phase 12 is the first additive UI feature for the Collection page since v0.1.1. Unit and component tests in Plans 12-01 and 12-02 verify the mechanical contract (hook localStorage round-trip, ring SVG semantics, toggle bg-muted active state, filter store untouched), but the user-facing quality (ring looks crisp at 96px, columns reflow correctly, persistence survives a hard reboot, click-to-open-sheet feels right) can only be judged by a human in front of a running Tauri window.

Output: A signed-off `12-03-SUMMARY.md` recording all 9 verification steps as PASS, after which `STATE.md` advances to "Phase 12 complete — ready for /gsd:verify-work".

Note: This plan modifies NO files (`files_modified: []`). It is a pure checkpoint with one `checkpoint:human-verify` task.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-collection-gallery-view/12-CONTEXT.md
@.planning/phases/12-collection-gallery-view/12-RESEARCH.md
@.planning/phases/12-collection-gallery-view/12-UI-SPEC.md
@.planning/phases/12-collection-gallery-view/12-VALIDATION.md
@.planning/phases/12-collection-gallery-view/12-00-PLAN.md
@.planning/phases/12-collection-gallery-view/12-01-PLAN.md
@.planning/phases/12-collection-gallery-view/12-02-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual smoke test — verify UI-04 + UI-05 + UI-06 in live Tauri app</name>
  <files>(none — pure verification checkpoint; no source files are modified by this task)</files>
  <read_first>
    - .planning/phases/12-collection-gallery-view/12-VALIDATION.md (§Manual-Only Verifications table — the 4 rows that scope this checkpoint: SVG pixel-accuracy, toggle visual distinction, responsive grid 2→3→4 cols, localStorage persistence across navigation)
    - .planning/phases/12-collection-gallery-view/12-UI-SPEC.md (§Gallery Card Layout Specification — locked: 96px ring + name + faction badge + status + model count + points + conditional Flame; §View Toggle Specification — locked: aria-labels, bg-muted active state; §PaintingRing Specification — locked: zinc-600 track, primary fill arc, viewBox 0 0 96 96)
    - .planning/phases/12-collection-gallery-view/12-CONTEXT.md (§Painting Ring — Appearance: rings encode painting state, NOT faction identity; §View Toggle Persistence: localStorage key 'collection-view-mode'; §Card Click & Interactions: gallery card click opens existing UnitDetailSheet via same handleRowClick)
  </read_first>
  <action>
**Context — what was built in Plans 12-00, 12-01, and 12-02 (no code changes in this task; this is a verification checkpoint):**
- `tests/collection/PaintingRing.test.tsx` (3 tests) and `tests/collection/UnitGallery.test.tsx` (6 tests) — Wave 0 stubs created in 12-00 and flipped to passing in 12-01 (ring) and 12-02 (gallery + integration). Total Phase 12 added 9 unit tests on top of the v0.2.0 baseline.
- `src/hooks/useCollectionViewMode.ts` — localStorage hook mirroring useSidebarCollapsed.ts; key 'collection-view-mode'; values 'table' | 'gallery'; default 'table'; SSR-safe; try/catch silent-degradation.
- `src/components/common/PaintingRing.tsx` — focused SVG component, 96×96 viewBox, two `<circle>` elements (zinc-600 hardcoded track + primary-color progress arc with rounded linecap), centred percentage text, role=img + aria-label '{percentage}% painted'. Pitfall 5 NaN guard at the top.
- `src/features/units/UnitGallery.tsx` — responsive grid (cols-2/3/4), alphabetical sort, 8-card skeleton loading state, empty state passthrough, role=button + Enter/Space keyboard handler with Pitfall 4 preventDefault, no edit/delete/toggleActive (locked decision).
- `src/features/units/CollectionPage.tsx` — modified header row with two ghost-icon-size toggle buttons (LayoutList / LayoutGrid icons, aria-label "Table view"/"Gallery view", bg-muted active class), conditional render branch swapping UnitTable for UnitGallery when viewMode === 'gallery'. Filter store unchanged on toggle (UI-06 free-by-design).

The existing UnitDetailSheet, UnitSheet, and UnitDeleteDialog sibling portals in CollectionPage handle all detail/edit/delete flows — gallery card clicks delegate to the same handleRowClick handler the table rows use.

**Procedure:**

1. Run `pnpm tauri dev` and wait for the desktop window to open.
2. Navigate to the Collection page (left sidebar → "Collection").
3. Verify all 9 steps below in order. Each step has an expected outcome — note any deviation.

| Step | Req | What to do | Expected outcome |
|------|-----|------------|-------------------|
| 1 | UI-04 | Look at the Collection page header row. | Three controls in left-to-right order: `Collection` heading (left), a pair of small ghost icon buttons (centre — first is a horizontal-lines icon for Table, second is a 4-square grid icon for Gallery), and the blue `+ Add Unit` button (right). Hovering the icon buttons shows a subtle hover background. |
| 2 | UI-04 | Confirm Table is the default view on first run. The Table button (first icon) should be visually highlighted with a subtle muted-grey fill (`bg-muted`). The Gallery button has no fill. | Table button is highlighted. Below the filter bar, the existing data table renders rows of units. |
| 3 | UI-04 | Click the Gallery icon button. | The Gallery button's highlight (bg-muted) appears; the Table button's highlight disappears. The table area below the filter bar is replaced with a grid of cards. |
| 4 | UI-05 | Inspect any one card in the gallery. | The card has, top-to-bottom, centred horizontally: (a) a 96px circular ring with a zinc-grey track and a primary-color (light blue/cyan) progress arc; the painted percentage (e.g. `72%`) is rendered in the centre of the ring; (b) the unit name in semibold; (c) a coloured faction badge (background colour matches the faction's hex `color_theme`, white text); (d) the painting status text (e.g. `Layered`) in muted grey; (e) the model count + points line (e.g. `10 models · 90 pts`, with `—` for nulls); (f) a flame icon ONLY if the unit's `is_active_project === 1` — units with `is_active_project = 0` do NOT show the flame. |
| 5 | UI-05 | Click any card. | The existing UnitDetailSheet slides in from the right with that unit's details (Playbook tab + Detail tab) — exactly the same sheet that clicking a table row opens. Close the sheet (X or Escape) — the gallery view persists, sheet closes cleanly. |
| 6 | UI-05 | Press Tab a few times to focus a gallery card (browser focus ring should appear around the card). Press Space. | The detail sheet opens. The page does NOT scroll downward when Space is pressed (Pitfall 4 — preventDefault holds). Press Escape to close. Press Tab again to focus another card, press Enter — the detail sheet opens for that unit. |
| 7 | UI-05 | Resize the Tauri window: shrink it as narrow as it will go, then progressively widen it. | At narrow widths the gallery shows 2 columns. At medium (around 768px+) it reflows to 3 columns. At wide (around 1024px+) it reflows to 4 columns. The card content stays centred and proportional at every breakpoint. |
| 8 | UI-06 | While in Gallery view, open the filter bar above and apply a filter — type something in search (e.g. a unit-name fragment), or pick a faction from the faction filter, or set a status filter. | The gallery cards immediately filter to only matching units (no full-grid reload). Now click the Table button — the table renders with the SAME filter still applied (matching rows visible, not all units). Click Gallery again — the gallery shows the same filtered cards. The filter bar inputs themselves stay populated across the toggle. |
| 9 | UI-04 | While in Gallery view, fully quit the Tauri app (close the window AND any background process — verify no Tauri icon in the system tray; if uncertain, use Task Manager to confirm). Re-open the app via `pnpm tauri dev` (or the desktop shortcut). Navigate to Collection. | The Collection page opens directly in Gallery view on first paint — no flash of Table view (synchronous localStorage read inside the useCollectionViewMode useState initializer). Toggle back to Table to confirm the toggle still works post-restart, then return to Gallery to verify the round-trip. |

4. After all 9 steps PASS, type `approved` to resume. If any step fails, type a description of what went wrong (e.g. "step 4: ring arc is grey not blue"); the executor will diagnose and route a fix back through `/gsd:plan-phase --gaps`.

5. Before recording PASS, also run final regression checks in a separate terminal:
   - `pnpm test` — full Vitest suite must exit 0 (expect 228+ passing, including the 9 new Phase 12 tests)
   - `pnpm tsc --noEmit` — must exit 0

6. After typing `approved`, write `.planning/phases/12-collection-gallery-view/12-03-SUMMARY.md` per the `<output>` block at the bottom of this plan, then update STATE.md and ROADMAP.md as documented there.

**Common things that might go wrong (likely cause → fix routing):**
- Step 4 — ring arc renders grey instead of primary blue: Pitfall 1 (Tailwind v4 stroke-* utilities). The `stroke="currentColor"` + `className="text-primary"` chain may need a fallback to inline `style={{ stroke: 'hsl(var(--primary))' }}`. File a gap closure plan that adjusts PaintingRing.tsx.
- Step 4 — cards have ugly extra spacing/padding: Pitfall 3 (Card defaults gap-6 py-6 fighting the override). Switch UnitGallery's `<Card>` to a plain `<div className="rounded-xl border bg-card shadow-sm flex flex-col items-center px-4 pt-4 pb-4 gap-2 cursor-pointer hover:bg-muted/50">` per 12-RESEARCH.md §Pitfall 3 escape hatch.
- Step 6 — Space scrolls the page: Pitfall 4 preventDefault is missing or misordered. Verify the onKeyDown handler in UnitGallery.tsx still has `e.preventDefault()` BEFORE the `onRowClick(unit)` call.
- Step 9 — flash of Table view on cold start: useCollectionViewMode's useState initializer is reading localStorage incorrectly. Verify the parse `raw === "gallery" ? "gallery" : "table"` is in the initializer, not in a useEffect.
- Step 8 — filter resets on toggle: a stray `clearAll()` was added somewhere in the toggle path. Search CollectionPage.tsx for `clearAll(` calls — UI-06 requires the only invocation to be inside `<CollectionEmptyState onClearFilters={clearAll}>`.
  </action>
  <verify>
    <automated>pnpm test && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - User confirms step 1: header row shows `Collection` heading + Table/Gallery icon button pair + `+ Add Unit` button in left-to-right order
    - User confirms step 2: default view is Table on first run; Table button has bg-muted highlight, Gallery does not
    - User confirms step 3: clicking Gallery flips the bg-muted highlight to the Gallery button and renders a card grid in place of the table
    - User confirms step 4: gallery card content is correct — 96px ring (zinc track + primary arc + centred %), unit name, coloured faction badge, status text, model count + points line, conditional Flame only for active projects
    - User confirms step 5: clicking a card opens the existing UnitDetailSheet with the unit's details; closing the sheet returns to gallery view cleanly
    - User confirms step 6: Tab → Enter and Tab → Space both open the detail sheet from a focused card; Space does NOT scroll the page (Pitfall 4 preventDefault holds)
    - User confirms step 7: window resize triggers responsive column reflow (2 cols → 3 cols → 4 cols at narrow / medium / wide widths)
    - User confirms step 8: filter applied in Gallery persists to Table and back to Gallery; filter inputs stay populated; matching units render in both views (UI-06)
    - User confirms step 9: quitting and re-opening the app keeps Gallery as the active view on first paint — no flash of Table view (localStorage persistence)
    - No console errors related to PaintingRing, useCollectionViewMode, UnitGallery, or localStorage during any of the above interactions
    - `pnpm test` (full suite) exits 0
    - `pnpm tsc --noEmit` exits 0
    - User typed `approved` (no remaining issues)
  </acceptance_criteria>
  <resume-signal>Type `approved` if all 9 steps pass and the regression checks succeed. Otherwise describe the failing step(s) — do NOT type `approved` if any check fails.</resume-signal>
  <done>All 9 manual verification steps pass in the live Tauri desktop app. UI-04 (view toggle + persistence), UI-05 (gallery card content + click-to-detail-sheet + keyboard accessibility + responsive grid), and UI-06 (filter preservation across toggle) are confirmed end-to-end. Full test suite green at 228+ passing; tsc clean. No console errors. User has typed `approved`.</done>
</task>

</tasks>

<verification>
- All 9 manual smoke-test steps pass in the live Tauri app
- User typed `approved`
- `pnpm test` (full suite) exits 0 (final regression check before commit — expect 228+ passing)
- `pnpm tsc --noEmit` exits 0
- No console errors observed during the smoke test
</verification>

<success_criteria>
- UI-04 confirmed visually: toggle buttons render with correct icons + aria-labels; clicking flips active state via bg-muted; selection persists across an app restart through localStorage (no flash of Table view on cold start)
- UI-05 confirmed visually: gallery cards render with the locked 96px ring (zinc track + primary arc + centred %), unit name, faction-coloured badge, status text, model count + points line, conditional Flame for active projects; clicking a card opens the existing UnitDetailSheet; keyboard navigation works with Enter and Space (Pitfall 4 preventDefault); responsive grid reflows 2→3→4 columns
- UI-06 confirmed visually: any filter applied in Table or Gallery persists across both directions of view-mode toggle; filter inputs stay populated; matching units render in both views
- Phase 12 is complete — ready for `/gsd:verify-work`
- ROADMAP.md Phase 12 row marked Complete; STATE.md advances to next phase target (Phase 13: Hobby Journal)
</success_criteria>

<output>
After completion, create `.planning/phases/12-collection-gallery-view/12-03-SUMMARY.md` listing:
- 9 manual smoke-test steps and their PASS results (per the table in the task)
- Note any minor observations (e.g. "step 4: Pitfall 1 currentColor pattern worked correctly — primary arc visible without inline style fallback" or "step 4: needed inline style fallback per Pitfall 1 — logged as a follow-up to 12-RESEARCH.md")
- Any deviations from the locked design (e.g. "Pitfall 3 escape hatch was needed — Card replaced with plain div" or "Pitfall 3 was unnecessary — cn() override worked")
- No files modified by this plan
- Phase 12 ready for `/gsd:verify-work`
- Update STATE.md `current.phase` to 13 (Hobby Journal) and `last_activity` to a Phase 12 completion entry; bump `progress.completed_phases` from 2 to 3 and `progress.completed_plans` from 8 to 12 (Phase 12 added 4 plans)
- Tick the Phase 12 row in ROADMAP.md `## Progress` table to "Complete | <today>"
</output>

---
phase: 11-dashboard-command-center
plan: 03
type: execute
wave: 3
depends_on:
  - "11-02"
files_modified: []
autonomous: false
requirements:
  - UI-07
  - UI-08
must_haves:
  truths:
    - "Live Tauri app: Dashboard hero counters animate from 0 to their final integer values over ~600ms (cubic ease-out — fast at the start, slow at the end) on every fresh visit to the page"
    - "Live Tauri app: Counters re-animate from 0 when navigating away and back to the Dashboard (TanStack Router unmounts on navigate)"
    - "Live Tauri app: Counters re-animate when an underlying mutation invalidates the dashboard-stats query (e.g. add a unit on Collection page, return to Dashboard, see counters re-animate to the new totals)"
    - "Live Tauri app: Active FactionSummaryCard ring color exactly matches the selected faction's color_theme hex (e.g. Tau '#3a4f96' shows a navy-blue ring, not zinc-grey)"
    - "Live Tauri app: Switching active faction (click a different faction's star) updates the ring color on the new card and removes the ring from the old card — same paint cycle"
    - "Live Tauri app: When the OS-level reduced-motion setting is enabled, counters render at their final value immediately (no animation) — UI-07 accessibility gate"
    - "No console errors related to useCountUp, requestAnimationFrame, or matchMedia during any of the above interactions"
  artifacts: []
  key_links:
    - from: "Live Tauri renderer"
      to: "src/hooks/useCountUp.ts"
      via: "StatCard's AnimatedNumber sub-component invocation per hero card"
      pattern: "visual count-up over ~600ms"
    - from: "Live Tauri renderer"
      to: "src/features/dashboard/FactionSummaryCard.tsx (Phase 10 ring class)"
      via: "active faction selected via star button → CSS ring renders in --faction-accent color"
      pattern: "ring color matches faction.color_theme"
---

<objective>
Final manual verification gate for Phase 11. The automated test suite cannot prove (a) the visual quality of the count-up animation timing/easing in a real GPU-rendered window, (b) that the actual CSS-resolved ring color on `FactionSummaryCard` matches the selected faction's hex at runtime, or (c) that the OS-level `prefers-reduced-motion` accessibility short-circuit works against a real Tauri WebView. This checkpoint asks the user to run `pnpm tauri dev` and confirm UI-07 and UI-08 in person, exercising every interaction the unit tests exercise — but in the live app.

Purpose: Phase 11 is purely visual. Unit and component tests in Plans 11-01 and 11-02 verify the mechanical contract (target reached, ring class present), but the user-facing quality (animation feels snappy, ring color matches faction theme, reduced-motion is honored) can only be judged by a human in front of a running window.

Output: A signed-off `11-03-SUMMARY.md` recording all 6 verification steps as PASS, after which `STATE.md` advances to "Phase 11 complete — ready for /gsd:verify-work".

Note: This plan modifies NO files (`files_modified: []`). It is a pure checkpoint with one `checkpoint:human-verify` task.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-dashboard-command-center/11-CONTEXT.md
@.planning/phases/11-dashboard-command-center/11-RESEARCH.md
@.planning/phases/11-dashboard-command-center/11-UI-SPEC.md
@.planning/phases/11-dashboard-command-center/11-VALIDATION.md
@.planning/phases/11-dashboard-command-center/11-00-PLAN.md
@.planning/phases/11-dashboard-command-center/11-01-PLAN.md
@.planning/phases/11-dashboard-command-center/11-02-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual smoke test — verify UI-07 + UI-08 in live Tauri app</name>
  <files>(none — pure verification checkpoint; no source files are modified by this task)</files>
  <read_first>
    - .planning/phases/11-dashboard-command-center/11-VALIDATION.md (§Manual-Only Verifications table — the 2 rows that scope this checkpoint)
    - .planning/phases/11-dashboard-command-center/11-UI-SPEC.md (§Animation Contract — duration 600ms, cubic ease-out, integer-only; §Interaction States — "Loaded, animating", "Data refetch", "Navigate away + return", "Active faction set")
    - .planning/phases/11-dashboard-command-center/11-CONTEXT.md (§Counter Animation Implementation; §Animation Re-trigger Behavior; §UI-08 Faction Card Accent)
  </read_first>
  <action>
**Context — what was built in Plans 11-01 and 11-02 (no code changes in this task; this is a verification checkpoint):**
- `src/hooks/useCountUp.ts` — rAF-based count-up hook with cubic ease-out, 600ms default duration, integer-only output, `prefers-reduced-motion` short-circuit
- `src/features/dashboard/StatCard.tsx` — gained an `animate?: boolean` prop driving an `AnimatedNumber` sub-component
- `src/features/dashboard/DashboardPage.tsx` — 4 hero StatCards now pass `animate={true}` (Total Models, Fully Painted, Battle-Ready Points, Active Projects); 3 progress cards stay static
- `tests/dashboard/useCountUp.test.ts` — 3 unit tests (reduced-motion short-circuit, animate-to-target, re-trigger on target change)
- `tests/dashboard/DashboardPage.test.tsx` — 2 new integration tests (animated hero values render, active FactionSummaryCard has ring classes)

Phase 10 already delivered the `ring-2 ring-faction-accent` ring on the active `FactionSummaryCard` and the `--faction-accent` CSS custom property mutation in `ActiveFactionContext`. Phase 11 verifies this end-to-end with the new animation infrastructure.

**Procedure:**

1. Run `pnpm tauri dev` and wait for the desktop window to open.
2. Verify all 6 steps below in order. Each step has an expected outcome — note any deviation.

| Step | Req | What to do | Expected outcome |
|------|-----|------------|-------------------|
| 1 | UI-07 | Open the app on the Dashboard (default route). Watch the 4 hero stat cards (top row: Total Models, Fully Painted, Battle-Ready Points, Active Projects). | All 4 numbers tick up from 0 to their final values over ~600ms. The motion feels fast at the start, slowing as it approaches the final value (cubic ease-out). The 3 progress cards below (Painting %, Assembly %, Basing %) DO NOT animate — they appear at their final percentage value statically. |
| 2 | UI-07 | Click "Collection" in the sidebar to leave the Dashboard. Then click "Dashboard" to return. | The 4 hero counters re-animate from 0 on the return visit. Every Dashboard mount re-triggers the animation (TanStack Router unmounts on navigate). |
| 3 | UI-07 | On the Collection page, add a new unit (or edit an existing unit's `painting_percentage` to change painted-count). Then return to Dashboard. | The 4 hero counters re-animate to the NEW values (e.g. Total Models is now 1 higher). The dashboard-stats query invalidation triggers a fresh animation cycle. |
| 4 | UI-08 | On the Dashboard, scroll to the "By Faction" section. Click the small star button on a FactionSummaryCard (e.g. Tau, color_theme `#3a4f96` navy blue). | The selected faction card immediately gains a 2px ring whose color is the faction's hex (Tau: navy blue ring; not zinc grey). The Star icon inside the card fills with the same color. |
| 5 | UI-08 | Click the star on a different faction (e.g. Ultramarines, color_theme `#0066cc` brighter blue). | The ring moves: the original card's ring disappears, the new card gains a ring in the new faction's hex. The transition is instant (single paint cycle). |
| 6 | UI-07 | Open OS settings → enable "Reduce motion" (Windows: Settings → Accessibility → Visual effects → Animation effects OFF; macOS: System Settings → Accessibility → Display → Reduce motion). Without quitting the app, navigate away from Dashboard and back. | Counters render at their final integer values immediately — no count-up animation runs. (Toggle "Reduce motion" OFF afterward to restore normal behavior.) |

3. After all 6 steps PASS, type `approved` to resume. If any step fails, type a description of what went wrong (e.g. "step 4: Tau ring is grey not navy"); the executor will diagnose and route a fix back through `/gsd:plan-phase --gaps`.

4. Before recording PASS, also run final regression checks in a separate terminal:
   - `pnpm test` — full Vitest suite must exit 0
   - `pnpm tsc --noEmit` — must exit 0

5. After typing `approved`, write `.planning/phases/11-dashboard-command-center/11-03-SUMMARY.md` per the `<output>` block at the bottom of this plan, then update STATE.md and ROADMAP.md as documented there.
  </action>
  <verify>
    <automated>pnpm test && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - User confirms step 1: 4 hero counters animate from 0 to target over ~600ms with cubic ease-out feel; 3 progress cards stay static
    - User confirms step 2: counters re-animate on every Dashboard mount (navigate-away-and-return triggers re-animation)
    - User confirms step 3: counters re-animate when dashboard-stats query refetches with new data after a mutation
    - User confirms step 4: active FactionSummaryCard ring color matches selected faction's color_theme hex (not zinc-grey default)
    - User confirms step 5: switching active faction moves the ring and updates the color in a single paint cycle
    - User confirms step 6: enabling OS reduced-motion suppresses the animation — counters render at final value immediately
    - No console errors related to useCountUp, requestAnimationFrame, matchMedia, or ActiveFactionContext during any of the above interactions
    - `pnpm test` (full suite) exits 0
    - `pnpm tsc --noEmit` exits 0
    - User typed `approved` (no remaining issues)
  </acceptance_criteria>
  <resume-signal>Type `approved` if all 6 steps pass and the regression checks succeed. Otherwise describe the failing step(s) — do NOT type `approved` if any check fails.</resume-signal>
  <done>All 6 manual verification steps pass in the live Tauri desktop app. UI-07 and UI-08 are confirmed end-to-end. Full test suite green; tsc clean. No console errors. User has typed `approved`.</done>
</task>

</tasks>

<verification>
- All 6 manual smoke-test steps pass in the live Tauri app
- User typed `approved`
- `pnpm test` (full suite) exits 0 (final regression check before commit)
- `pnpm tsc --noEmit` exits 0
- No console errors observed during the smoke test
</verification>

<success_criteria>
- UI-07 confirmed visually: animation timing, easing, integer-only output, every-mount re-trigger, refetch re-trigger, reduced-motion gate
- UI-08 confirmed visually: ring color matches active faction's hex; switching faction updates the ring atomically
- Phase 11 is complete — ready for `/gsd:verify-work`
- ROADMAP.md Phase 11 row marked Complete; STATE.md advances to next phase target
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-command-center/11-03-SUMMARY.md` listing:
- 6 manual smoke-test steps and their PASS results (per the table in the task)
- Note any minor observations (e.g. "step 6: macOS reduced-motion was already on by default — verified counters render statically as expected")
- No files modified by this plan
- Phase 11 ready for `/gsd:verify-work`
- Update STATE.md `current.phase` to 12 (Collection Gallery View) and `last_activity` to a Phase 11 completion entry
- Tick the Phase 11 row in ROADMAP.md `## Progress` table to "Complete | <today>"
</output>

---
phase: 16-design-overhaul
plan: 05
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/features/army-lists/ArmyListsPage.tsx
  - src/features/army-lists/ArmyListCard.tsx
  - src/features/spending/SpendingPage.tsx
autonomous: true
requirements:
  - PAGE-HEADER-ARMY-LISTS
  - PAGE-HEADER-SPENDING
  - TABULAR-NUMS-ARMY-LISTS
  - TABULAR-NUMS-SPENDING
  - CARD-ELEVATION-ARMY-LIST-CARD
must_haves:
  truths:
    - "Army Lists page header h1 reads 'Army Lists' in text-3xl font-semibold tracking-tight, with the subtitle 'Points-tracked lists for the tabletop' below"
    - "Spending page renders a new page header h1 'Spending' in text-3xl font-semibold tracking-tight, with the subtitle 'Total hobby spend tracked to the penny' below — placed inside the existing max-w-3xl wrapper, NOT outside (per Pitfall 1)"
    - "Spending page 'Breakdown' h2 is downgraded from text-xl font-semibold to text-base font-semibold (Phase 16 size scale only allows 14/16/28 — no text-xl)"
    - "ArmyListCard uses bg-card border border-border/60 shadow-sm hover:shadow-md transition-shadow duration-150 (interactive card hover) — replaces the current hover:bg-muted/50 pattern"
    - "ArmyListCard points totals render with tabular-nums"
    - "SpendingPage currency figures render with tabular-nums on the hero card and per-faction breakdown rows"
    - "Spending page empty state ('No spend logged yet') uses the new icon-in-container pattern with the Receipt icon"
    - "Phase 14 SpendingPage data flow (useSpendingStats / SPENDING_STATS_KEY / formatCurrency) is unchanged"
  artifacts:
    - path: "src/features/army-lists/ArmyListsPage.tsx"
      provides: "Upgraded page header"
      contains: "text-3xl font-semibold tracking-tight"
    - path: "src/features/army-lists/ArmyListCard.tsx"
      provides: "Card elevation + tabular-nums points display"
      contains: "tabular-nums"
    - path: "src/features/spending/SpendingPage.tsx"
      provides: "New page header + tabular-nums + empty state upgrade"
      contains: "Total hobby spend tracked to the penny"
  key_links:
    - from: "ArmyListsPage h1"
      to: "Phase 16 typography"
      via: "className='text-3xl font-semibold tracking-tight'"
      pattern: "text-3xl\\s+font-semibold\\s+tracking-tight"
    - from: "SpendingPage h1"
      to: "Phase 16 typography"
      via: "className='text-3xl font-semibold tracking-tight'"
      pattern: "text-3xl\\s+font-semibold\\s+tracking-tight"
    - from: "ArmyListCard hover state"
      to: "Phase 16 elevation contract"
      via: "hover:shadow-md transition-shadow duration-150"
      pattern: "hover:shadow-md\\s+transition-shadow\\s+duration-150"
    - from: "SpendingPage currency"
      to: "Phase 16 numeric contract"
      via: "tabular-nums on formatCurrency render sites"
      pattern: "tabular-nums"
---

<objective>
Upgrade the Army Lists and Spending cluster: page headers (Army Lists + Spending), ArmyListCard elevation + tabular-nums on points, SpendingPage tabular-nums on currency, and the inline Spending empty state ("No spend logged yet"). The SpendingPage requires special handling per Pitfall 1: its outer wrapper is `max-w-3xl mx-auto p-8` (not the standard `flex flex-col gap-6 p-6`), and it currently has NO h1 — one must be added inside the existing wrapper.

Purpose: Army Lists is the points-tracking workflow page. Spending is the v2.1 financial summary. Both need consistent headers + numeric alignment. ArmyListCard is interactive and gets the same hover-shadow treatment as UnitGallery cards in Plan 16-03 — completing the interactive-card pattern across the app.

Output: Three files modified — ArmyListsPage.tsx (header), ArmyListCard.tsx (elevation + tabular-nums), SpendingPage.tsx (header insertion inside max-w-3xl wrapper, h2 downgrade, tabular-nums on currency, empty-state inline upgrade). No SpendingEmptyState file exists — empty state is inline in SpendingPage.tsx.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-spending-tracker/14-03-PLAN.md
@.planning/phases/16-design-overhaul/16-CONTEXT.md
@.planning/phases/16-design-overhaul/16-RESEARCH.md
@.planning/phases/16-design-overhaul/16-UI-SPEC.md

<interfaces>
<!-- Subtitles per UI-SPEC §Page Header Contract -->
- Army Lists: "Points-tracked lists for the tabletop"
- Spending: "Total hobby spend tracked to the penny"

<!-- Spending page wrapper exception (RESEARCH §Pitfall 1) -->
SpendingPage outer wrapper is `<div className="max-w-3xl mx-auto p-8 flex flex-col gap-12">` (or similar narrow-column layout). The new page header div must be inserted INSIDE this wrapper (so the border-b spans only the max-w-3xl content area, not the full window). The hero Card stays immediately below the new header.

<!-- Spending page section heading (RESEARCH §Open Question 1 + UI-SPEC §Typography size scale) -->
The current SpendingPage `<h2>Breakdown</h2>` is `text-xl font-semibold`. UI-SPEC allows only 14px / 16px / 28px sizes — `text-xl` (20px) is OUT. Downgrade the h2 to `text-base font-semibold`.

<!-- Spending empty state copy (UI-SPEC §Empty State Contract — Spending row) -->
- Icon: `Receipt` (lucide-react)
- Headline: "No spend logged yet"
- Helper: "Add purchase prices to units and paints from their detail sheets to track your spend here."
- CTA: none (spend is logged from other pages — empty state shows no button)

<!-- ArmyListCard elevation per UI-SPEC §Interaction Contract -->
- Replace existing `hover:bg-muted/50` with `bg-card border border-border/60 shadow-sm hover:shadow-md transition-shadow duration-150`
- Preserve `cursor-pointer` (card is clickable)
- Points totals: add `tabular-nums` to the points display span (e.g., "1850 pts")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Upgrade ArmyListsPage header + ArmyListCard elevation and tabular-nums</name>
  <files>src/features/army-lists/ArmyListsPage.tsx, src/features/army-lists/ArmyListCard.tsx</files>
  <read_first>
    - src/features/army-lists/ArmyListsPage.tsx (read entire file — locate header structure, identify CTA buttons, confirm ArmyListCard is rendered in a list/grid)
    - src/features/army-lists/ArmyListCard.tsx (read entire file — locate Card root className, the points totals display, the current hover:bg-muted/50 class)
    - tests for army-lists (glob: tests/army-lists/*.test.{ts,tsx})
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md §Page Header Contract (Army Lists subtitle) and §Interaction Contract (interactive card hover pattern)
    - .planning/phases/16-design-overhaul/16-RESEARCH.md §Pattern 5 (ArmyListCard is interactive — gets hover:shadow-md, NOT static treatment)
  </read_first>
  <action>
    Edit A — ArmyListsPage.tsx page header. Replace the existing header div + h1 with:
    ```tsx
    <div className="flex items-center justify-between pb-6 border-b border-border/40">
      <div>
        <h1 className="text-3xl font-semibold tracking-tight">Army Lists</h1>
        <p className="text-sm text-muted-foreground mt-1">Points-tracked lists for the tabletop</p>
      </div>
      <div className="flex items-center gap-2">
        {/* preserve existing CTA buttons (e.g., "New list" button) verbatim */}
      </div>
    </div>
    ```
    Read the source first to copy any CTA buttons inside `<div className="flex items-center gap-2">`. Do NOT modify the ArmyListCard rendering loop, ArmyListsEmptyState (handled in plan 16-07), or ArmyListDetailSheet portal mount.

    Edit B — ArmyListCard.tsx Card elevation + tabular-nums. Make exactly two concrete changes:

    B1 — Card root className update. Per RESEARCH §Pattern 5 the current pattern is:
    ```tsx
    <Card className="cursor-pointer border hover:bg-muted/50 transition-colors" ...>
    ```
    Replace it with:
    ```tsx
    <Card className="cursor-pointer bg-card border border-border/60 shadow-sm hover:shadow-md transition-shadow duration-150" ...>
    ```
    Preserve any other props on the Card (onClick, role, tabIndex, key, aria-*). Only the className string changes. Remove `hover:bg-muted/50` and `transition-colors` — they are replaced by the shadow-based hover.

    If the actual className uses `cn()` composition or template literals, keep that style and substitute the equivalent class set. The literal classes that MUST appear in the final className: `cursor-pointer`, `bg-card`, `border`, `border-border/60`, `shadow-sm`, `hover:shadow-md`, `transition-shadow`, `duration-150`.

    B2 — tabular-nums on points totals. Locate the span/div that renders the points figure (e.g., `{list.total_points} pts` or similar). Add `tabular-nums` to its className. The minimal edit:
    ```tsx
    // Before:
    <span className="text-sm text-muted-foreground">{list.points} pts</span>
    // After:
    <span className="text-sm text-muted-foreground tabular-nums">{list.points} pts</span>
    ```
    If ArmyListCard also displays a battle-ready percentage or other numeric figure, add `tabular-nums` to those displays too — every numeric span gets the class.

    Do NOT modify:
    - ArmyListSummaryBar (separate component — STATE.md notes ArmyListCard duplicates stat logic intentionally)
    - The Phase 8 sibling-portal pattern between ArmyListsPage and ArmyListDetailSheet
    - Card titles, CardHeader, or CardContent structure — only className edits
  </action>
  <verify>
    <automated>npx vitest run tests/army-lists/ --reporter=verbose 2>&1 | tail -25</automated>

    Manual checks:
    1. `Get-Content src/features/army-lists/ArmyListsPage.tsx | Select-String -Pattern 'text-3xl font-semibold tracking-tight'` — at least one match
    2. `Get-Content src/features/army-lists/ArmyListsPage.tsx | Select-String -Pattern 'Points-tracked lists for the tabletop'` — exactly one match
    3. `Get-Content src/features/army-lists/ArmyListCard.tsx | Select-String -Pattern 'hover:shadow-md'` — at least one match
    4. `Get-Content src/features/army-lists/ArmyListCard.tsx | Select-String -Pattern 'border-border/60'` — at least one match
    5. `Get-Content src/features/army-lists/ArmyListCard.tsx | Select-String -Pattern 'transition-shadow duration-150'` — at least one match
    6. `Get-Content src/features/army-lists/ArmyListCard.tsx | Select-String -Pattern 'hover:bg-muted/50'` — ZERO matches (replaced)
    7. `Get-Content src/features/army-lists/ArmyListCard.tsx | Select-String -Pattern 'tabular-nums'` — at least one match
  </verify>
  <acceptance_criteria>
    - `ArmyListsPage.tsx` page-level h1 className: `text-3xl font-semibold tracking-tight` with text `Army Lists`
    - `ArmyListsPage.tsx` contains literal subtitle: `Points-tracked lists for the tabletop`
    - `ArmyListsPage.tsx` page header wrapper className contains `pb-6 border-b border-border/40`
    - `ArmyListCard.tsx` Card root className contains: `cursor-pointer`, `bg-card`, `border border-border/60`, `shadow-sm`, `hover:shadow-md`, `transition-shadow`, `duration-150`
    - `ArmyListCard.tsx` does NOT contain `hover:bg-muted/50` (old hover replaced)
    - `ArmyListCard.tsx` does NOT contain `transition-colors` on the Card root (replaced by transition-shadow)
    - `ArmyListCard.tsx` contains `tabular-nums` on the element that wraps the points display (and any other numeric figures like battle-ready %)
    - `npx vitest run tests/army-lists/` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Army Lists page renders new text-3xl header + subtitle. ArmyListCard renders bg-card + border-border/60 + shadow-sm with hover:shadow-md transition. Points display uses tabular-nums. Phase 8 contracts (sibling portals, COALESCE points calc) untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add SpendingPage header (inside max-w-3xl wrapper), tabular-nums on currency, downgrade Breakdown h2, upgrade empty state</name>
  <files>src/features/spending/SpendingPage.tsx</files>
  <read_first>
    - src/features/spending/SpendingPage.tsx (READ ENTIRELY — Pitfall 1 critical: this page has a unique max-w-3xl mx-auto p-8 wrapper, NOT the standard flex flex-col gap-6 p-6; new header must go INSIDE this wrapper)
    - tests/spending/SpendingPage.test.tsx (verify what's queried — header text, hero card content, empty state)
    - .planning/phases/14-spending-tracker/14-03-PLAN.md (Phase 14 contract — useSpendingStats hook, SPENDING_STATS_KEY, formatCurrency single-division-site rule)
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md §Page Header Contract (Spending row + welcome screen NOT applicable — Spending uses standard header)
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md §Empty State Contract (Spending row: Receipt icon, "No spend logged yet", helper text verbatim, no CTA)
    - .planning/phases/16-design-overhaul/16-RESEARCH.md §Pitfall 1 (max-w-3xl wrapper) and §Open Question 1 (downgrade Breakdown h2 to text-base) and §Open Question 2 (where the new h1 goes)
  </read_first>
  <action>
    Make four targeted edits to src/features/spending/SpendingPage.tsx:

    Edit A — Insert page header INSIDE the max-w-3xl wrapper. The current outer wrapper looks like (read source to confirm the exact opening tag):
    ```tsx
    <div className="max-w-3xl mx-auto p-8 flex flex-col gap-12">
      {/* hero Card (totalPence display) */}
      <Card>...</Card>
      {/* Breakdown section */}
      <section>...<h2>Breakdown</h2>...</section>
    </div>
    ```

    As the FIRST child inside that wrapper (before the hero Card), insert the standard page header pattern:
    ```tsx
    <div className="flex items-center justify-between pb-6 border-b border-border/40">
      <div>
        <h1 className="text-3xl font-semibold tracking-tight">Spending</h1>
        <p className="text-sm text-muted-foreground mt-1">Total hobby spend tracked to the penny</p>
      </div>
    </div>
    ```
    Note: Spending page has NO right-side CTA buttons (RESEARCH §Open Question 2 — empty state itself has no CTA either, since spend is logged from unit/paint detail sheets). The right slot can be omitted entirely; only the left `<div>` containing h1 + p is required.

    Critical: this header div sits INSIDE the `<div className="max-w-3xl mx-auto p-8 flex flex-col gap-12">` wrapper, so the hairline border spans only the narrow content column — NOT the full window. Per Pitfall 1, do NOT add the header outside the max-w-3xl wrapper.

    Edit B — Downgrade the "Breakdown" h2. Locate the existing `<h2 className="text-xl font-semibold">Breakdown</h2>` (or similar). Per RESEARCH §Open Question 1 + UI-SPEC §Typography (only 3 sizes allowed: 14/16/28px), `text-xl` (20px) is OUT of scale. Replace with:
    ```tsx
    <h2 className="text-base font-semibold">Breakdown</h2>
    ```
    Do NOT change the h2's content text or its surrounding section structure — only the className.

    Edit C — Add tabular-nums to ALL currency render sites. SpendingPage calls `formatCurrency()` in multiple places:
    1. Hero card total (the big stat — typically `text-3xl font-semibold` already)
    2. Per-faction breakdown rows (one currency value per row)
    3. Possibly subtotal lines (units total, paints total)

    For EACH `formatCurrency(...)` call, add `tabular-nums` to the wrapping span/div className. Examples:
    ```tsx
    // Hero — before:
    <span className="text-3xl font-semibold">{formatCurrency(data.totalPence)}</span>
    // Hero — after:
    <span className="text-3xl font-semibold tabular-nums">{formatCurrency(data.totalPence)}</span>

    // Per-faction row — before:
    <span className="text-sm">{formatCurrency(faction.spendPence)}</span>
    // Per-faction row — after:
    <span className="text-sm tabular-nums">{formatCurrency(faction.spendPence)}</span>
    ```
    Apply tabular-nums to EVERY currency render site — read the file first and enumerate them. Per UI-SPEC §Typography "Add tabular-nums class to all numeric displays."

    Edit D — Upgrade the inline empty state ("No spend logged yet"). SpendingPage has no separate EmptyState file — the empty state is rendered inline when `data.totalPence === 0` and there are no faction rows (or when the data is empty). Find the empty-state branch (typically inside a conditional render).

    Replace the current empty-state JSX with the UI-SPEC pattern (no CTA — Spending has no inline action):
    ```tsx
    <div className="flex flex-col items-center justify-center gap-3 py-16 text-center">
      <div className="rounded-xl bg-muted/40 p-4">
        <Receipt className="h-8 w-8 text-muted-foreground" />
      </div>
      <div className="space-y-1">
        <p className="text-base font-semibold">No spend logged yet</p>
        <p className="text-sm text-muted-foreground max-w-xs">
          Add purchase prices to units and paints from their detail sheets to track your spend here.
        </p>
      </div>
    </div>
    ```

    Add `import { Receipt } from "lucide-react";` to the imports if not already present. Use the EXACT copy strings above (verbatim from UI-SPEC §Empty State Contract, Spending row).

    Do NOT modify:
    - The `useSpendingStats()` hook call or `SPENDING_STATS_KEY` (Phase 14 contract)
    - `formatCurrency` import or invocation logic (the SINGLE division-by-100 site per STATE.md Phase 14 Plan 01 note)
    - The Skeleton loading state JSX (handled in plan 16-08 manual check)
    - The route definition (`/spending`)
    - The outer `max-w-3xl mx-auto p-8 flex flex-col gap-12` wrapper itself
  </action>
  <verify>
    <automated>npx vitest run tests/spending/ --reporter=verbose 2>&1 | tail -25</automated>

    Manual checks:
    1. `Get-Content src/features/spending/SpendingPage.tsx | Select-String -Pattern 'text-3xl font-semibold tracking-tight'` — at least one match (page h1 — note: hero card stat may also be text-3xl; verify h1 exists separately)
    2. `Get-Content src/features/spending/SpendingPage.tsx | Select-String -Pattern '<h1[^>]*>Spending</h1>'` — exactly one match (or use Pattern '>Spending<' if the h1 spans lines)
    3. `Get-Content src/features/spending/SpendingPage.tsx | Select-String -Pattern 'Total hobby spend tracked to the penny'` — exactly one match
    4. `Get-Content src/features/spending/SpendingPage.tsx | Select-String -Pattern 'pb-6 border-b border-border/40'` — at least one match
    5. `Get-Content src/features/spending/SpendingPage.tsx | Select-String -Pattern 'tabular-nums'` — at least two matches (hero + breakdown rows)
    6. `Get-Content src/features/spending/SpendingPage.tsx | Select-String -Pattern '<h2[^>]*text-xl'` — ZERO matches (Breakdown h2 downgraded; h2 should now be text-base)
    7. `Get-Content src/features/spending/SpendingPage.tsx | Select-String -Pattern 'No spend logged yet'` — exactly one match (empty state headline)
    8. `Get-Content src/features/spending/SpendingPage.tsx | Select-String -Pattern 'Add purchase prices to units and paints'` — exactly one match (empty state helper, verbatim)
    9. `Get-Content src/features/spending/SpendingPage.tsx | Select-String -Pattern 'Receipt'` — at least one match (lucide import + icon use)
    10. `Get-Content src/features/spending/SpendingPage.tsx | Select-String -Pattern 'rounded-xl bg-muted/40 p-4'` — exactly one match (empty-state icon container)
    11. `Get-Content src/features/spending/SpendingPage.tsx | Select-String -Pattern 'max-w-3xl'` — at least one match (outer wrapper preserved per Pitfall 1)
  </verify>
  <acceptance_criteria>
    - `SpendingPage.tsx` contains exactly one `<h1>` with text `Spending` and className `text-3xl font-semibold tracking-tight`
    - The h1 is rendered INSIDE the `max-w-3xl mx-auto p-8` wrapper (visual confirmation: same indent level / parent as the hero Card)
    - File contains literal subtitle: `Total hobby spend tracked to the penny`
    - File contains the literal substring `pb-6 border-b border-border/40` for the page header div
    - File contains `<h2 className="text-base font-semibold">Breakdown</h2>` (or equivalent — h2 className ends with `text-base font-semibold`, NOT `text-xl`)
    - File does NOT contain any `<h2[^>]*text-xl` pattern (the old Breakdown size is gone)
    - File contains `tabular-nums` at AT LEAST TWO render sites (hero card + breakdown rows minimum; ideally one per formatCurrency call)
    - File contains the literal empty-state copy: `No spend logged yet`, `Add purchase prices to units and paints from their detail sheets to track your spend here.`
    - File imports `Receipt` from `lucide-react`
    - File contains the empty-state icon container className `rounded-xl bg-muted/40 p-4` exactly once
    - The outer wrapper `max-w-3xl mx-auto p-8` is preserved (Pitfall 1)
    - File still calls `useSpendingStats()` and uses `formatCurrency()` for all monetary displays (Phase 14 contract preserved)
    - `npx vitest run tests/spending/SpendingPage.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    SpendingPage renders the new page header inside the max-w-3xl wrapper, the Breakdown h2 is downgraded to text-base font-semibold, every currency render uses tabular-nums, and the empty state uses the Receipt icon-in-container pattern with verbatim UI-SPEC copy. Phase 14 useSpendingStats / SPENDING_STATS_KEY / formatCurrency single-division contract preserved.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite ≥ 279 passing, 0 failing
- `npx tsc --noEmit` — no TS errors
- Manual visual check (Plan 16-08): Army Lists h1 + subtitle visible; ArmyListCard hover lifts via shadow; Spending h1 sits inside max-w-3xl column with hairline border that doesn't span full width; Breakdown h2 visually smaller than the page h1 (text-base vs text-3xl); Spending empty state renders Receipt icon in muted container
</verification>

<success_criteria>
- ArmyListsPage h1: text-3xl + subtitle "Points-tracked lists for the tabletop" + hairline border
- ArmyListCard: bg-card + border-border/60 + shadow-sm + hover:shadow-md transition-shadow duration-150; tabular-nums on points
- SpendingPage h1: text-3xl inserted INSIDE max-w-3xl wrapper + subtitle "Total hobby spend tracked to the penny"
- SpendingPage Breakdown h2: text-base font-semibold (downgraded from text-xl per size-scale rule)
- SpendingPage tabular-nums on every formatCurrency render site
- SpendingPage empty state: Receipt icon in rounded-xl bg-muted/40 p-4 container; verbatim "No spend logged yet" + helper copy; NO CTA button
- Phase 14 data flow contracts intact
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-overhaul/16-05-SUMMARY.md` documenting:
- Each header before/after diff
- Confirmation Spending h1 sits INSIDE max-w-3xl wrapper (Pitfall 1)
- Confirmation Breakdown h2 is now text-base, not text-xl
- Every formatCurrency render site that received tabular-nums (with line numbers)
- Empty-state Receipt icon import added; helper text verbatim match
- Vitest pass count
</output>

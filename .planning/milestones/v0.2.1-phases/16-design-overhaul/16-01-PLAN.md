---
phase: 16-design-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/styles/globals.css
autonomous: true
requirements:
  - DESIGN-FOUNDATION
must_haves:
  truths:
    - "Geist Variable font renders as the body font on every page in the app"
    - "The --font-sans CSS custom property is registered inside the @theme inline block alongside --color-faction-accent"
    - "Body element font-family resolves to var(--font-sans), not the previous ui-sans-serif fallback"
    - "Existing 279-test suite remains green after the font foundation lands"
  artifacts:
    - path: "package.json"
      provides: "@fontsource-variable/geist dependency entry"
      contains: "@fontsource-variable/geist"
    - path: "src/styles/globals.css"
      provides: "Font import + --font-sans token + body font-family"
      contains: "@fontsource-variable/geist"
  key_links:
    - from: "src/styles/globals.css"
      to: "node_modules/@fontsource-variable/geist"
      via: "@import \"@fontsource-variable/geist\""
      pattern: "@import \"@fontsource-variable/geist\""
    - from: "body { font-family }"
      to: "--font-sans token"
      via: "var(--font-sans)"
      pattern: "font-family:\\s*var\\(--font-sans\\)"
---

<objective>
Install the Geist Variable font package and integrate it as the global sans-serif font for the app via Tailwind v4 CSS-first config. This is the foundation for the entire Phase 16 design overhaul — every downstream plan (sidebar, page headers, empty states, tabular-nums) renders against this font.

Purpose: Linear's clean modern sans-serif is the visual anchor for the polished tooling aesthetic. Geist is the same font family Linear and Vercel use; switching from system fonts is the single highest-impact change in this phase. All other Phase 16 plans depend on this CSS landing first so visual snapshots reflect the new font.

Output: Geist Variable font loaded via npm-bundled WOFF2, registered as `--font-sans` in the existing `@theme inline {}` block in `src/styles/globals.css`, and applied as the `body` font-family. No Tailwind config file is created (project uses Tailwind v4 CSS-first config via `@theme inline` exclusively).
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-design-overhaul/16-CONTEXT.md
@.planning/phases/16-design-overhaul/16-RESEARCH.md
@.planning/phases/16-design-overhaul/16-UI-SPEC.md
@.planning/phases/16-design-overhaul/16-VALIDATION.md

<interfaces>
<!-- Existing globals.css structure that must be preserved -->
<!-- The @theme inline {} block is the integration point — DO NOT recreate, only ADD --font-sans into it. -->

src/styles/globals.css (current relevant slice — lines 1-2, 71-112):
```css
@import "tailwindcss";
@import "tw-animate-css";

/* ... :root + .dark blocks ... */

@theme inline {
  /* ... existing color/radius tokens ... */
  --color-faction-accent: var(--faction-accent);
}

body {
  background-color: hsl(var(--background));
  color: hsl(var(--foreground));
  font-family: ui-sans-serif, system-ui, sans-serif;
}
```

Tailwind v4 CSS-first font integration pattern (verified via UI-SPEC §Design System):
- Variable font import goes at the TOP, after the existing @import lines (line 3 area)
- Font token goes INSIDE the existing @theme inline {} block alongside --color-faction-accent
- Body rule font-family is REPLACED to point at var(--font-sans)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Geist Variable font package and integrate into globals.css</name>
  <files>package.json, package-lock.json, src/styles/globals.css</files>
  <read_first>
    - src/styles/globals.css (read entire file — must understand the @theme inline {} block structure and the body rule before editing)
    - .planning/phases/16-design-overhaul/16-RESEARCH.md §Pattern 1 Font Integration (verbatim integration pattern)
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md §Design System (font row: confirms `'Geist Variable'` is the font-family value — NOT `'Geist Sans'` or `'Geist'`)
    - package.json (verify @fontsource-variable/geist is NOT already installed)
  </read_first>
  <action>
    Step 1 — Install the npm package:
    Run `npm install @fontsource-variable/geist` in the project root.
    The expected package version is `5.2.8` (latest verified 2026-05-04 per RESEARCH.md). If npm pulls a newer 5.x patch version, that is acceptable; do NOT downgrade.

    Step 2 — Update src/styles/globals.css with three precise edits:

    Edit A (add font import at top, after the existing two @import lines):
    Current line 2: `@import "tw-animate-css";`
    Insert AFTER line 2, BEFORE the `@custom-variant dark (&:is(.dark *));` line:
    ```
    @import "@fontsource-variable/geist";
    ```

    Edit B (add `--font-sans` token inside the existing `@theme inline {}` block):
    Find the line `--color-faction-accent: var(--faction-accent);` (currently line 105 inside `@theme inline {}`).
    Insert AFTER that line, BEFORE the closing `}` of the `@theme inline` block:
    ```
      --font-sans: 'Geist Variable', ui-sans-serif, system-ui, sans-serif;
    ```
    Match the indentation of the surrounding `--color-*` tokens (2 spaces).

    Edit C (replace the existing body font-family):
    Current line 111: `font-family: ui-sans-serif, system-ui, sans-serif;`
    Replace with EXACTLY:
    ```
      font-family: var(--font-sans);
    ```
    Preserve the 2-space indentation (it sits inside the `body { ... }` rule).

    Do NOT change any other lines in globals.css. Do NOT remove or reorder existing tokens. Do NOT add a tailwind.config.js file (Tailwind v4 CSS-first — config file creation is forbidden in this project).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>

    Manual checks (executor must run all four):
    1. `node -e "console.log(require('./package.json').dependencies['@fontsource-variable/geist'] || require('./package.json').devDependencies['@fontsource-variable/geist'])"` prints a 5.x version string (not undefined).
    2. `Get-Content src/styles/globals.css | Select-String '@fontsource-variable/geist'` returns exactly one line: `@import "@fontsource-variable/geist";`
    3. `Get-Content src/styles/globals.css | Select-String -Pattern '--font-sans'` returns exactly one line containing `--font-sans: 'Geist Variable', ui-sans-serif, system-ui, sans-serif;`
    4. `Get-Content src/styles/globals.css | Select-String -Pattern 'font-family:\s*var\(--font-sans\)'` returns exactly one match (the body rule).
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies (or devDependencies) contains key `@fontsource-variable/geist` with a version starting `5.`
    - `package-lock.json` contains the string `@fontsource-variable/geist` (lockfile updated)
    - `node_modules/@fontsource-variable/geist` directory exists
    - `src/styles/globals.css` contains the literal string `@import "@fontsource-variable/geist";` exactly once, on a line that comes BEFORE the `@custom-variant dark` line
    - `src/styles/globals.css` contains the literal string `--font-sans: 'Geist Variable', ui-sans-serif, system-ui, sans-serif;` exactly once, INSIDE the `@theme inline {` block (between that opening brace and its matching closing brace)
    - `src/styles/globals.css` `body { ... }` rule contains the line `font-family: var(--font-sans);` exactly once
    - `src/styles/globals.css` does NOT contain the previous string `font-family: ui-sans-serif, system-ui, sans-serif;` (the old body font-family must be gone)
    - `src/styles/globals.css` does NOT contain `font-family: 'Geist Sans'` (wrong family name — must be `'Geist Variable'`)
    - No `tailwind.config.js` or `tailwind.config.ts` file is created in the project root
    - `npx vitest run` exits 0 with `279 passed` (or higher if Phase 14 added more tests since this phase's research baseline; the count must not DECREASE)
    - `npm run build` (or `npx tsc --noEmit`) exits 0 — no TypeScript or build-time errors introduced
  </acceptance_criteria>
  <done>
    `@fontsource-variable/geist` is installed, `globals.css` has the three precise edits (font import, --font-sans token in @theme inline, body font-family pointing to var(--font-sans)), the existing 279-test regression suite remains green, and the Tauri/Vite app builds without errors. The font is loaded by every page that imports globals.css (which is every page, via main.tsx).
  </done>
</task>

</tasks>

<verification>
This is the foundation plan for Phase 16. Verification is regression-only: the existing 279-test suite must remain green. The font itself is verified visually after Wave 2+ plans land (manual smoke test in Plan 16-08). No new automated tests are introduced by this plan because the visual rendering of a font is not unit-testable in jsdom.

Run after task completion:
- `npx vitest run` — full suite, must show ≥ 279 passing, 0 failing
- `npx tsc --noEmit` — no TypeScript errors
</verification>

<success_criteria>
- `@fontsource-variable/geist@5.x` listed in package.json dependencies
- `src/styles/globals.css` has all three edits applied (font import, --font-sans token, body font-family)
- Old `ui-sans-serif, system-ui, sans-serif` body font-family string is gone (replaced by `var(--font-sans)`)
- No tailwind.config.js created — Tailwind v4 CSS-first integrity preserved
- Full vitest regression suite passes with ≥ 279 tests green, 0 failing
- TypeScript compilation succeeds (`npx tsc --noEmit`)
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-overhaul/16-01-SUMMARY.md` documenting:
- Font package version installed
- Exact lines added/modified in globals.css (with line numbers post-edit)
- Vitest pass count after the edit
- Any deviations from the plan (should be none)
</output>

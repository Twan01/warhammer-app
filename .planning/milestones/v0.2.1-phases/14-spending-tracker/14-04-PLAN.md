---
phase: 14-spending-tracker
plan: 04
type: execute
wave: 3
depends_on:
  - 14-02
  - 14-03
files_modified: []
autonomous: false
requirements:
  - SPEND-01
  - SPEND-02
  - SPEND-03
  - SPEND-04
  - SPEND-05
must_haves:
  truths:
    - "User confirms unit price entered in UnitSheet saves and reloads as £ in UnitDetailSheet Details tab (SPEND-01)"
    - "User confirms paint price entered in PaintSheet saves and reloads as £ on next open (SPEND-02)"
    - "User confirms Spending page hero total = sum of all unit + owned-paint prices (SPEND-03)"
    - "User confirms Spending page per-faction rows match unit prices grouped by faction; Paints row matches owned paint sum (SPEND-04)"
    - "User confirms clearing a unit's price (empty field on save) results in '—' display in UnitDetailSheet — proves Pitfall 1 unconditional UPDATE works"
    - "User confirms cache invalidation: editing a unit price refreshes the Spending page on next visit without app restart (Pitfall 2)"
    - "User confirms Spending nav entry visible in sidebar with Wallet icon, navigates to /spending"
    - "User signs off all 7 manual smoke-test steps OR reports specific gaps that need follow-up plans"
    - "pnpm test (full suite) exits 0 as final regression check before commit"
    - "pnpm tsc --noEmit exits 0"
  artifacts: []
  key_links:
    - from: "Live Tauri app (pnpm tauri dev)"
      to: "All Phase 14 source files"
      via: "End-to-end manual verification"
      pattern: "(no automated grep — checkpoint validates end-to-end behavior)"
---

<objective>
Manual smoke-test checkpoint validating Phase 14 end-to-end in the live Tauri WebView. Plans 14-00 through 14-03 produce all the source code and unit tests; this plan asks the user to run the app and click through 7 verification steps that automated tests cannot cover (real SQLite IPC, real DB round-trip, real cross-page cache invalidation, Pitfall 1 explicit-NULL behavior, Pitfall 2 cross-page cache freshness).

Purpose: Closes the Nyquist manual-only verifications listed in `14-VALIDATION.md` §"Manual-Only Verifications". These behaviors require live Tauri IPC + DB writes + page navigation that jsdom + vi.mock cannot reproduce. User sign-off is the phase ship gate.

Output: User confirms all 7 smoke-test steps PASS, OR documents specific gaps. If gaps exist, run `/gsd:plan-phase 14 --gaps` to create Plan 14-05 closure tasks.

Note: This plan modifies NO files (`files_modified: []`). It is a pure checkpoint with one `checkpoint:human-verify` task. Mirrors Plan 11-03 structure verbatim.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-spending-tracker/14-CONTEXT.md
@.planning/phases/14-spending-tracker/14-VALIDATION.md
@.planning/phases/14-spending-tracker/14-UI-SPEC.md
@.planning/phases/14-spending-tracker/14-00-PLAN.md
@.planning/phases/14-spending-tracker/14-01-PLAN.md
@.planning/phases/14-spending-tracker/14-02-PLAN.md
@.planning/phases/14-spending-tracker/14-03-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual smoke test — verify SPEND-01 through SPEND-05 in live Tauri app</name>
  <files>(none — pure verification checkpoint; no source files are modified by this task)</files>
  <read_first>
    - .planning/phases/14-spending-tracker/14-VALIDATION.md (§Manual-Only Verifications table — the 5 rows that scope this checkpoint)
    - .planning/phases/14-spending-tracker/14-CONTEXT.md (§Schema migration; §Spending page layout; §Faction breakdown for paint spend)
    - .planning/phases/14-spending-tracker/14-UI-SPEC.md (§Copywriting Contract — exact label/placeholder/helper strings to verify in steps 2 & 4)
  </read_first>
  <action>
**Context — what was built in Plans 14-00 through 14-03 (no code changes in this task; this is a verification checkpoint):**
- Migration 005 adds `purchase_price_pence INTEGER` to `units` AND `paints` tables; lib.rs registers version 5
- `src/lib/formatCurrency.ts` — Intl.NumberFormat-based pence-to-£ formatter (single division-by-100 site)
- `src/types/unit.ts` Unit type renames `purchase_price` → `purchase_price_pence`
- `src/types/paint.ts` Paint type adds `purchase_price_pence`
- `src/db/queries/units.ts` + `paints.ts` wire `purchase_price_pence` with UNCONDITIONAL UPDATE assignment (Pitfall 1)
- `src/features/units/unitSchema.ts` + `paintSchema.ts` add `.int().min(0).optional().nullable()` field
- `src/features/units/UnitSheet.tsx` Purchase Price field (integer-pence input, helper text "Enter amount in pence (100 = £1.00)")
- `src/features/units/UnitDetailSheet.tsx` Details tab displays `formatCurrency(unit.purchase_price_pence)`
- `src/features/paints/PaintSheet.tsx` new Purchase Price field appended before SheetFooter
- `src/hooks/useUnits.ts` + `usePaints.ts` — all 6 mutation hooks invalidate `["spending-stats"]` (Pitfall 2)
- `src/db/queries/spending.ts` — getSpendingStats() with parallel SELECT units + factions + SUM(owned paints)
- `src/features/spending/computeSpendingStats.ts` — pure aggregation function
- `src/hooks/useSpendingStats.ts` — TanStack Query hook with `SPENDING_STATS_KEY = ["spending-stats"]`
- `src/features/spending/SpendingPage.tsx` — hero card + Breakdown table per UI-SPEC
- `src/app/spending/page.tsx` — thin re-export wrapper for router
- `src/app/router.tsx` — `/spending` route registered
- `src/components/common/AppSidebar.tsx` — Spending nav entry with Wallet icon between Paints and Army Lists
- ~32 automated tests across schema validation, currency formatting, migration content, cache-key contract, pure aggregation, page rendering — all green from Plans 14-01/02/03

**Pre-flight:**
1. Run `pnpm test` in a terminal — confirm full suite exits 0 (all Phase 14 tests pass + no regression)
2. Run `pnpm tsc --noEmit` — confirm exits 0
3. Run `pnpm tauri dev` — wait for the Tauri WebView desktop window to open
4. Migration 005 will run automatically on first launch (visible in console as `tauri-plugin-sql` registers version 5)

**Verify all 7 steps below in order. Note any deviation:**

| Step | Req | What to do | Expected outcome |
|------|-----|------------|-------------------|
| 1 | SPEND-05 | Open Tauri devtools (F12 in WebView). Look at Console tab for errors. Optional: open `%APPDATA%\com.hobbyforge.app\hobbyforge.db` in DB Browser for SQLite and inspect units/paints tables. | NO migration errors. NO `no such column: purchase_price_pence` errors. Both `units` and `paints` tables have a `purchase_price_pence INTEGER` column when inspected via SQLite browser. |
| 2 | SPEND-01 | Navigate to /collection. Click "Add Unit" or edit an existing unit. Click "More details" to expand. Find the Purchase Price field. Verify label="Purchase Price", placeholder="e.g. 1250 for £12.50", helper text="Enter amount in pence (100 = £1.00)", input type=number step=1. Enter `1250`. Save. Reopen via clicking unit row to open Detail Sheet → Details tab. | Purchase Price row in Detail Sheet displays `£12.50` (NOT `1250`, NOT `12.5`, NOT `—`). Save toast confirms "Unit created." or "Unit updated." |
| 3 | SPEND-01 + Pitfall 1 | Open the unit you just saved with `1250`; click Edit. Expand "More details", clear the Purchase Price field (delete value, leave empty). Save. Reopen Detail Sheet → Details tab. | Purchase Price row now displays `—` (em dash). This proves the unconditional UPDATE in `updateUnit` works — without Pitfall 1's fix, COALESCE would have retained the old `1250` value. |
| 4 | SPEND-02 | Navigate to /paints. Click "Add Paint". Fill brand/name/type. Toggle Owned ON. Scroll to bottom — confirm new "Purchase Price" field appears BEFORE the Save buttons. Verify placeholder="e.g. 350 for £3.50", helper="Enter amount in pence (100 = £1.00)". Enter `450`. Save. Reopen the same paint to confirm value persists. | New Purchase Price field appears at bottom of PaintSheet form. After save and reopen, the Input shows `450`. |
| 5 | SPEND-03 + SPEND-04 + Pitfall 2 | Open the sidebar — confirm "Spending" nav entry exists with a Wallet icon, positioned BETWEEN "Paints" and "Army Lists". Click "Spending". Verify the Spending page renders. Then add a unit with purchase_price_pence=5000 to a faction (e.g. Tau Empire). Return to /spending. | Hero card labeled "Total Hobby Spend" with formatted £ value. Breakdown section with table containing 4 faction rows (all shown even at £0.00) + 1 "Paints" row at bottom. Each zero-spend faction shows `£0.00` (not `—`, not blank). After adding the new unit, the Tau Empire row updates to `£50.00` AUTOMATICALLY without app restart — proves Pitfall 2 cache invalidation fires on /spending revisit. |
| 6 | SPEND-04 owned filter | On /paints, edit a paint — set Purchase Price to `1000` (£10.00) but TOGGLE OFF the "Owned" checkbox. Save. Navigate to /spending. Verify Paints row total. Then toggle Owned ON, save, return to /spending. | Paints row does NOT include this paint's £10.00 when owned=0 (verifies SQL `WHERE owned = 1` filter). After toggling owned=1, the Paints row INCLUDES the £10.00. |
| 7 | SPEND-05 visual | Reload the Tauri app (Ctrl+R in the WebView). Navigate to /spending immediately on app load. | Brief Skeleton loading state visible (3 grey blocks: hero + section header + table) before data resolves. Loading container has `aria-label="Loading spending data"` (verify with devtools Inspector if desired). Smooth transition from Skeleton to populated table — NO crash, NO blank page, NO console errors. |

**After all 7 steps PASS:**

1. Run final regression checks in a separate terminal:
   - `pnpm test` — full Vitest suite must exit 0
   - `pnpm tsc --noEmit` — must exit 0
2. Type `approved` to resume.
3. After typing `approved`, the executor will write `.planning/phases/14-spending-tracker/14-04-SUMMARY.md` per the `<output>` block at the bottom of this plan, then update STATE.md and ROADMAP.md.

**If any step fails**: type a description of what went wrong (e.g. "step 3: clearing the price still shows the old value"). The executor will diagnose and route a fix back through `/gsd:plan-phase 14 --gaps` which creates Plan 14-05 closure tasks.
  </action>
  <verify>
    <automated>pnpm test && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - User confirms step 1: no migration errors in devtools console; SQLite DB has `purchase_price_pence INTEGER` column on both `units` and `paints` tables
    - User confirms step 2: UnitSheet Purchase Price field has correct label/placeholder/helper text and step=1; entering 1250 saves and reloads as `£12.50` in UnitDetailSheet Details tab
    - User confirms step 3: clearing a unit's purchase_price_pence and saving causes UnitDetailSheet to display `—` (em dash) — Pitfall 1 unconditional UPDATE confirmed
    - User confirms step 4: PaintSheet has Purchase Price field at bottom before SheetFooter; entering 450 saves and persists across reopen
    - User confirms step 5: Spending nav entry visible with Wallet icon between Paints and Army Lists; Spending page renders hero card + 4 faction rows + Paints row; cache invalidation works (editing a unit price refreshes the Spending page on revisit without restart)
    - User confirms step 6: Paints row excludes paints with owned=0; includes paints with owned=1 — `WHERE owned = 1` SQL filter verified end-to-end
    - User confirms step 7: Skeleton loading state renders with aria-label="Loading spending data"; smooth transition to populated state; no crashes
    - No console errors related to migrations, IPC, queries, or cache invalidation during any of the above interactions
    - `pnpm test` (full suite) exits 0 — final regression check
    - `pnpm tsc --noEmit` exits 0
    - User typed `approved` (no remaining issues)
  </acceptance_criteria>
  <resume-signal>Type `approved` if all 7 steps pass and the regression checks succeed. Otherwise describe the failing step(s) — do NOT type `approved` if any check fails.</resume-signal>
  <done>All 7 manual verification steps pass in the live Tauri desktop app. SPEND-01 through SPEND-05 confirmed end-to-end. Full test suite green; tsc clean. No console errors. User has typed `approved`.</done>
</task>

</tasks>

<verification>
- All 7 manual smoke-test steps pass in the live Tauri app
- User typed `approved`
- `pnpm test` (full suite) exits 0 (final regression check before commit)
- `pnpm tsc --noEmit` exits 0
- No console errors observed during the smoke test
</verification>

<success_criteria>
- SPEND-01 confirmed end-to-end: unit price saves and reloads as formatted currency
- SPEND-02 confirmed end-to-end: paint price saves and reloads
- SPEND-03 confirmed end-to-end: Spending page total = sum of unit + owned-paint prices
- SPEND-04 confirmed end-to-end: per-faction breakdown + Paints row separate; owned-only filter works
- SPEND-05 confirmed end-to-end: integer-pence storage with formatted currency display; loading state works; Pitfall 1 (clear value) and Pitfall 2 (cache invalidation) both verified
- Phase 14 is complete — ready for `/gsd:verify-work`
- ROADMAP.md Phase 14 row marked Complete; STATE.md advances to next phase target
</success_criteria>

<output>
After completion (user typed `approved`), write `.planning/phases/14-spending-tracker/14-04-SUMMARY.md` with:
- All 7 manual smoke-test steps with PASS/FAIL status
- Any console errors or unexpected behavior observed
- Confirmation that SPEND-01 through SPEND-05 are all closed end-to-end
- Pitfall 1 + Pitfall 2 verification status
- Full regression check status (pnpm test + pnpm tsc)
- Phase 14 marked complete; ready for `/gsd:verify-work`
- Next phase recommendation (Phase 15 — Warhammer 40K Datasheet Integration, OR Phase 16 — Design Overhaul, depending on user priority)

Then update STATE.md to:
- Set status: phase 14 complete; next: /gsd:verify-work
- Add accumulated context entries for Phase 14 decisions
- Update progress counters

Then update ROADMAP.md Phase 14 row in the Progress table to "Complete" with today's date.

If gaps were found, instead document:
- Which step(s) failed
- What was observed vs. expected
- Recommendation: run `/gsd:plan-phase 14 --gaps` to create Plan 14-05 closure tasks
</output>

---
phase: 119-stratagems-enhancements-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/043_udb_stratagems_enhancements.sql
  - scripts/lib/types.ts
  - scripts/build-unit-db.ts
  - scripts/update-unit-database.ts
autonomous: true
requirements: [STR-01, STR-02, ENH-01]

must_haves:
  truths:
    - "D-01, D-02, D-03, D-04: udb_stratagems table created with nullable FKs, ON DELETE SET NULL, and indexes"
    - "D-05, D-06, D-07, D-08: udb_enhancements table created with NOT NULL faction_id, ON DELETE CASCADE, and indexes"
    - "D-09: Legends rows filtered from both Stratagems.csv and Enhancements.csv"
    - "D-10, D-11: Stratagems.csv and Enhancements.csv added to REQUIRED_CSVs and parsed as Steps 12/13"
    - "D-12, D-13: UdbStratagemRow and UdbEnhancementRow types exported; stratagems and enhancements arrays in JSON"
    - "D-14: update-unit-database.ts mirrors all build-unit-db.ts changes"
    - "D-18: Migration 043 DDL creates both tables and indexes in a single file"
  artifacts:
    - path: "src-tauri/migrations/043_udb_stratagems_enhancements.sql"
      provides: "DDL for udb_stratagems and udb_enhancements tables + indexes"
      contains: "CREATE TABLE IF NOT EXISTS udb_stratagems"
    - path: "scripts/lib/types.ts"
      provides: "UdbStratagemRow and UdbEnhancementRow interfaces"
      exports: ["UdbStratagemRow", "UdbEnhancementRow"]
    - path: "src-tauri/data/unit_database.json"
      provides: "Rebuilt JSON with stratagems and enhancements arrays"
  key_links:
    - from: "scripts/build-unit-db.ts"
      to: "scripts/lib/types.ts"
      via: "import UdbStratagemRow, UdbEnhancementRow"
      pattern: "import.*UdbStratagemRow"
    - from: "scripts/build-unit-db.ts"
      to: "src-tauri/data/unit_database.json"
      via: "JSON.stringify output"
      pattern: "stratagems.*enhancements"
---

<objective>
Parse Stratagems.csv and Enhancements.csv in the TypeScript build pipeline, add the migration DDL, and rebuild unit_database.json with both new arrays.

Purpose: Provide the JSON payload and schema that the Rust importer (Plan 02) will consume.
Output: Migration 043 SQL file, extended TypeScript types, rebuilt unit_database.json with stratagems and enhancements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-stratagems-enhancements-import/119-CONTEXT.md
@.planning/phases/119-stratagems-enhancements-import/119-RESEARCH.md
@.planning/phases/119-stratagems-enhancements-import/119-PATTERNS.md

<interfaces>
<!-- From scripts/lib/types.ts — existing row types to follow -->
From scripts/lib/types.ts (existing pattern):
export interface UdbDetachmentRow { id: string; faction_id: string; name: string; }
export interface UdbDetachmentAbilityRow { id: string; detachment_id: string; faction_id: string; name: string; description: string; }
export interface UnitDatabaseJson { ... detachments: UdbDetachmentRow[]; detachment_abilities: UdbDetachmentAbilityRow[]; }

<!-- From scripts/lib/bsdata.ts — CSV reader function -->
From scripts/lib/bsdata.ts:
export function readCsvFile(dir: string, filename: string): Record<string, string>[]

<!-- CSV column headers (verified) -->
Stratagems.csv: faction_id|name|id|type|cp_cost|legend|turn|phase|detachment|detachment_id|description
Enhancements.csv: faction_id|id|name|cost|detachment|detachment_id|legend|description
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TypeScript types and migration DDL</name>
  <files>scripts/lib/types.ts, src-tauri/migrations/043_udb_stratagems_enhancements.sql</files>
  <read_first>
    - scripts/lib/types.ts (current type definitions, find UdbDetachmentAbilityRow and UnitDatabaseJson)
    - src-tauri/migrations/042_udb_detachments.sql (DDL pattern to replicate)
    - .planning/phases/119-stratagems-enhancements-import/119-PATTERNS.md (exact code patterns)
  </read_first>
  <action>
    In scripts/lib/types.ts:
    1. Add UdbStratagemRow interface after UdbDetachmentAbilityRow with fields: id: string, faction_id: string | null, detachment_id: string | null, name: string, type: string, cp_cost: number, turn: string, phase: string, description: string. Per D-01, D-02.
    2. Add UdbEnhancementRow interface after UdbStratagemRow with fields: id: string, faction_id: string, detachment_id: string | null, name: string, cost: number, description: string. Per D-05, D-06.
    3. Extend the UnitDatabaseJson interface to add stratagems: UdbStratagemRow[] and enhancements: UdbEnhancementRow[] after the detachment_abilities field. Per D-12.

    Migration 043 file already exists at src-tauri/migrations/043_udb_stratagems_enhancements.sql (pre-created). Verify it contains CREATE TABLE IF NOT EXISTS for udb_stratagems (per D-01, D-03, D-04) and udb_enhancements (per D-05, D-07, D-08), plus four CREATE INDEX statements. Per D-18.
  </action>
  <acceptance_criteria>
    - scripts/lib/types.ts exports UdbStratagemRow with faction_id typed as string | null
    - scripts/lib/types.ts exports UdbEnhancementRow with faction_id typed as string (not nullable)
    - UnitDatabaseJson interface includes stratagems: UdbStratagemRow[] and enhancements: UdbEnhancementRow[]
    - 043_udb_stratagems_enhancements.sql contains CREATE TABLE IF NOT EXISTS udb_stratagems with ON DELETE SET NULL on both FKs
    - 043_udb_stratagems_enhancements.sql contains CREATE TABLE IF NOT EXISTS udb_enhancements with ON DELETE CASCADE on faction_id and ON DELETE SET NULL on detachment_id
    - 043_udb_stratagems_enhancements.sql contains 4 CREATE INDEX statements
  </acceptance_criteria>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('scripts/lib/types.ts','utf8'); const checks=['UdbStratagemRow','UdbEnhancementRow','stratagems: UdbStratagemRow','enhancements: UdbEnhancementRow','faction_id: string | null']; const missing=checks.filter(c=>!t.includes(c)); if(missing.length){console.error('MISSING:',missing);process.exit(1)}; console.log('types.ts OK')"</automated>
  </verify>
  <done>UdbStratagemRow and UdbEnhancementRow interfaces exported from types.ts. UnitDatabaseJson extended. Migration 043 DDL file exists with correct schema.</done>
</task>

<task type="auto">
  <name>Task 2: Extend build scripts with stratagem and enhancement parsing + rebuild JSON</name>
  <files>scripts/build-unit-db.ts, scripts/update-unit-database.ts</files>
  <read_first>
    - scripts/build-unit-db.ts (find REQUIRED_CSVs array, Step 11 detachment parsing, summary block around line 614, hash computation around line 663, output object around line 666)
    - scripts/update-unit-database.ts (find parallel REQUIRED_CSVs and buildUnitDatabase function)
    - .planning/phases/119-stratagems-enhancements-import/119-PATTERNS.md (exact code patterns for Steps 12/13, hash, summary, output)
  </read_first>
  <action>
    In scripts/build-unit-db.ts:
    1. Add "Stratagems.csv" and "Enhancements.csv" to the REQUIRED_CSVs array before the as const assertion. Per D-10.
    2. Add import of UdbStratagemRow and UdbEnhancementRow from ./lib/types.ts in the existing import block.
    3. After the Step 11 detachment parsing block, add Step 12: parse Stratagems.csv using readCsvFile(DATA_DIR, "Stratagems.csv"), filter legends (row["legend"] === "1" || row["legend"] === "true"), map to UdbStratagemRow[]. Convert empty faction_id and detachment_id to null (row["faction_id"]?.trim() || null). Parse cp_cost with parseInt(..., 10) || 0. Log count. Per D-09, D-11.
    4. Add Step 13: parse Enhancements.csv using readCsvFile(DATA_DIR, "Enhancements.csv"), filter legends, map to UdbEnhancementRow[]. Skip rows missing faction_id. Parse cost with parseInt. Log count. Per D-09, D-11.
    5. Add stratagems and enhancements counts to the summary console.log block (after detachment abilities line). Per Pitfall 4.
    6. Include stratagems and enhancements in the hash computation object (createHash SHA-256 call). Per Pitfall 5 and D-12.
    7. Add stratagems and enhancements fields to the UnitDatabaseJson output object. Per D-12.

    In scripts/update-unit-database.ts:
    Apply identical changes: REQUIRED_CSVs extension, import types, Step 12 + Step 13 parsing, output object extension. Per D-14.

    After both scripts are updated, run the build: node --experimental-strip-types scripts/build-unit-db.ts to rebuild unit_database.json with stratagems and enhancements arrays.
  </action>
  <acceptance_criteria>
    - build-unit-db.ts REQUIRED_CSVs contains "Stratagems.csv" and "Enhancements.csv"
    - build-unit-db.ts has Step 12 parsing Stratagems.csv with legends filtering and null FK conversion
    - build-unit-db.ts has Step 13 parsing Enhancements.csv with legends filtering
    - build-unit-db.ts hash computation includes stratagems and enhancements
    - build-unit-db.ts output object includes stratagems and enhancements fields
    - update-unit-database.ts has matching REQUIRED_CSVs, Steps 12/13, and output fields
    - Running build-unit-db.ts produces unit_database.json with a stratagems array of 1300+ entries
    - Running build-unit-db.ts produces unit_database.json with an enhancements array of 800+ entries
    - Universal stratagems in the JSON have faction_id: null (not empty string)
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types scripts/build-unit-db.ts && node -e "const d=JSON.parse(require('fs').readFileSync('src-tauri/data/unit_database.json','utf8')); const s=d.stratagems?.length||0; const e=d.enhancements?.length||0; const nullFk=d.stratagems?.filter(x=>x.faction_id===null).length||0; console.log('Stratagems:',s,'Enhancements:',e,'Universal(null FK):',nullFk); if(s<1300||e<800||nullFk<1){process.exit(1)}; console.log('OK')"</automated>
  </verify>
  <done>Both build scripts parse Stratagems.csv and Enhancements.csv. unit_database.json rebuilt with 1300+ stratagems (including universal ones with null faction_id) and 800+ enhancements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV files to build script | Dev-side only; CSVs are fetched by the developer, not user input |
| JSON to Rust importer | Build artifact bundled in app binary; no runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Tampering | CSV files on disk | accept | Dev-side only; developer fetches CSVs manually. No user-facing attack surface. |
| T-119-02 | Information Disclosure | HTML in description fields | accept | Raw HTML stored as-is (consistent with Phase 118 D-06). Rendering safety is Phase 120 scope. |
| T-119-03 | Injection | Migration 043 SQL | accept | Static DDL, no parameterized values. No user input in migration. |
</threat_model>

<verification>
1. node --experimental-strip-types scripts/build-unit-db.ts completes without errors
2. unit_database.json contains stratagems array with 1300+ entries
3. unit_database.json contains enhancements array with 800+ entries
4. Universal stratagems have faction_id: null in JSON output
5. Migration file 043 exists with correct DDL
</verification>

<success_criteria>
- TypeScript types exported for both entity types
- Both build scripts parse Stratagems.csv and Enhancements.csv with legends filtering
- unit_database.json rebuilt with stratagems and enhancements arrays
- Migration 043 DDL creates both tables with correct FK constraints and indexes
</success_criteria>

<output>
Create `.planning/phases/119-stratagems-enhancements-import/119-01-SUMMARY.md` when done
</output>

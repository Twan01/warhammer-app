---
phase: 117-points-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/parseCsv.ts
  - scripts/build-unit-db.ts
  - scripts/update-unit-database.ts
autonomous: true
requirements: [PTS-01, PTS-02, PTS-04]

must_haves:
  truths:
    - "Every unit with a cost CSV entry has points resolved via datasheet_id join"
    - "Multi-tier units produce one UdbUnitPointsRow per tier with model count extracted from description"
    - "Single-tier units set base_points directly on the unit row"
    - "Sub-faction assignment uses keyword matching instead of BSData catalogues"
    - "readCsvFile is available from parseCsv.ts for all consumers"
  artifacts:
    - path: "scripts/lib/parseCsv.ts"
      provides: "readCsvFile function (relocated from bsdata.ts)"
      exports: ["parseWahapediaCsv", "readCsvFile"]
    - path: "scripts/build-unit-db.ts"
      provides: "Cost CSV parsing step + sub-faction keyword assignment"
      contains: "Datasheets_models_cost.csv"
    - path: "scripts/update-unit-database.ts"
      provides: "Same cost CSV parsing + sub-faction keyword assignment"
      contains: "Datasheets_models_cost.csv"
  key_links:
    - from: "scripts/build-unit-db.ts"
      to: "scripts/lib/parseCsv.ts"
      via: "import readCsvFile"
      pattern: "readCsvFile.*parseCsv"
    - from: "scripts/build-unit-db.ts"
      to: "scripts/data/Datasheets_models_cost.csv"
      via: "readCsvFile call"
      pattern: "Datasheets_models_cost"
---

<objective>
Add Wahapedia cost CSV-based points resolution and keyword-based sub-faction assignment to both build scripts, replacing the BSData matching logic with a direct datasheet_id join.

Purpose: This is the "add new" half of the BSData replacement -- get the new points pipeline working alongside the old code so the build still passes. The next plan removes BSData entirely.
Output: Both build scripts produce points from cost CSV. readCsvFile relocated to parseCsv.ts.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-points-coverage/117-CONTEXT.md
@.planning/phases/117-points-coverage/117-RESEARCH.md
@.planning/phases/117-points-coverage/117-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate readCsvFile to parseCsv.ts and add extractModelCount</name>
  <files>scripts/lib/parseCsv.ts</files>
  <read_first>
    - scripts/lib/parseCsv.ts (target file -- currently has only parseWahapediaCsv, no fs imports)
    - scripts/lib/bsdata.ts (source of readCsvFile function at lines 42-52)
  </read_first>
  <action>
    Add `readFileSync` from "node:fs" and `join` from "node:path" imports to parseCsv.ts.

    Copy the `readCsvFile(dataDir: string, filename: string): Record&lt;string, string&gt;[]` function from bsdata.ts lines 42-52 into parseCsv.ts after the existing `parseWahapediaCsv` function. It calls `readFileSync` then `parseWahapediaCsv`. Export it.

    Add a new exported function `extractModelCount(description: string, line: number): number` per D-02. It uses `description.match(/\d+/g)` to find all numbers, sums them via reduce, and returns the total. If no numbers found, returns `line` as fallback. This handles all description patterns: "N models", "N ModelA and N ModelB", "N ModelA, N ModelB and N ModelC".
  </action>
  <verify>
    <automated>npx tsx -e "import { readCsvFile, extractModelCount } from './scripts/lib/parseCsv.ts'; console.log(extractModelCount('1 Spanner and 4 Burna Boyz', 1)); console.log(extractModelCount('10 models', 1)); console.log(extractModelCount('', 3));"</automated>
  </verify>
  <acceptance_criteria>
    - parseCsv.ts exports three functions: parseWahapediaCsv, readCsvFile, extractModelCount
    - extractModelCount("1 Spanner and 4 Burna Boyz", 1) returns 5
    - extractModelCount("10 models", 1) returns 10
    - extractModelCount("", 3) returns 3 (fallback to line)
    - readCsvFile signature matches bsdata.ts original: (dataDir: string, filename: string) => Record&lt;string, string&gt;[]
  </acceptance_criteria>
  <done>parseCsv.ts is the single source for all CSV parsing utilities. extractModelCount handles all known description patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Add cost CSV points step and keyword-based sub-faction assignment to build-unit-db.ts</name>
  <files>scripts/build-unit-db.ts</files>
  <read_first>
    - scripts/build-unit-db.ts (full file -- understand Step 8 BSData block at lines ~390-475, imports at lines 19-47, constants at lines 60-72, coverage report at lines 539-657, keywords parsing at Step 7 lines 320-341)
    - scripts/lib/parseCsv.ts (newly updated -- readCsvFile and extractModelCount exports)
    - scripts/lib/factionMap.ts (SUB_FACTION_MAP values -- these are the sub-faction labels to match against keywords)
    - scripts/lib/types.ts (UdbUnitPointsRow type definition)
    - .planning/phases/117-points-coverage/117-PATTERNS.md (exact code patterns for the new step)
  </read_first>
  <action>
    Per D-01, D-02, D-03, D-09, D-10:

    1. ADD import of `readCsvFile, extractModelCount` from `./lib/parseCsv.ts` (alongside existing imports). Do NOT yet remove the old BSData imports -- that is Plan 02's job.

    2. REPLACE the BSData Step 8 block (the section that calls `loadAliases`, `readBsdataCatFiles`, `parseCatXml`, `matchUnit` loop, and the BSData tier/single-cost branching) with the new cost CSV step. The new Step 8:
       - Read `Datasheets_models_cost.csv` via `readCsvFile(DATA_DIR, "Datasheets_models_cost.csv")`
       - Group rows by `datasheet_id` into a `Map&lt;string, Array&lt;{line, description, cost}&gt;&gt;`, filtering by `validUnitIds.has(id)` and `cost > 0`
       - For single-tier units (1 row per ID): set `unit.base_points = tier.cost` per D-03
       - For multi-tier units (2+ rows per ID): push `UdbUnitPointsRow` entries with `extractModelCount(description, line)` per D-02
       - Derive composition per D-09: for each unit, compute `min_models`/`max_models` from `extractModelCount` across all tiers
       - Track `matchedUnits` set for coverage reporting
       - Log: `"Step 8: Parsing Datasheets_models_cost.csv..."` and summary line with matched count + tier entry count

    3. ADD Step 8b after Step 8: Sub-faction assignment via keywords per D-10.
       - Build `subFactionValues = new Set(Object.values(SUB_FACTION_MAP))`
       - Build `unitById = new Map(units.map(u => [u.id, u]))`
       - Iterate the `keywords` array (already populated by Step 7), check if `keyword` is in `subFactionValues`, if so set `unit.sub_faction = keyword` when currently null
       - Log sub-faction assignment count

    4. KEEP the old BSData Step 8 code commented out or in a dead code path (e.g., behind `if (false)`) so the file still compiles. Plan 02 will remove it cleanly.

    NOTE: Do NOT modify the coverage report format yet -- Plan 02 handles that per D-13. Do NOT change MIN_COVERAGE_PCT yet -- Plan 02 handles that per D-12. The new code should add points that raise coverage above the old 58% threshold naturally.
  </action>
  <verify>
    <automated>npx tsx scripts/build-unit-db.ts 2>&1 | grep -E "(Step 8|Matched|sub.faction|Coverage)"</automated>
  </verify>
  <acceptance_criteria>
    - Build script output includes "Step 8: Parsing Datasheets_models_cost.csv..."
    - Build script output shows matched unit count near 1697 (99.8% of ~1701 units)
    - Build script output shows sub-faction assignment count > 0
    - Build completes successfully (exit code 0) -- the 58% coverage threshold is exceeded
    - unit_database.json contains units with base_points set from cost CSV
    - unit_database.json contains multi-tier entries in the points array
    - units with keywords like "Blood Angels" have sub_faction set
  </acceptance_criteria>
  <done>build-unit-db.ts resolves points from Wahapedia cost CSV via direct datasheet_id join and assigns sub-factions via keyword matching. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror cost CSV + sub-faction changes to update-unit-database.ts</name>
  <files>scripts/update-unit-database.ts</files>
  <read_first>
    - scripts/update-unit-database.ts (full file -- understand BSData block at lines ~249-325, imports at lines 18-43, constants at line ~82)
    - scripts/build-unit-db.ts (just completed -- copy the new Step 8 and Step 8b patterns)
    - scripts/lib/parseCsv.ts (readCsvFile and extractModelCount exports)
  </read_first>
  <action>
    Per D-07: Mirror the exact same changes from Task 2 to update-unit-database.ts.

    1. ADD import of `readCsvFile, extractModelCount` from `./lib/parseCsv.ts`.
    2. REPLACE the BSData matching block (loadAliases, readBsdataCatFiles, parseCatXml, matchUnit loop) with the same cost CSV step: read Datasheets_models_cost.csv, group by datasheet_id, single-tier -> base_points, multi-tier -> UdbUnitPointsRow[], derive composition.
    3. ADD Step 8b: keyword-based sub-faction assignment (same logic as build-unit-db.ts).
    4. KEEP old BSData code commented out / dead so file compiles. Plan 02 removes it.

    The structure of update-unit-database.ts closely mirrors build-unit-db.ts. The new code should be functionally identical.
  </action>
  <verify>
    <automated>npx tsx scripts/update-unit-database.ts 2>&1 | grep -E "(Step 8|Matched|sub.faction|Coverage)"</automated>
  </verify>
  <acceptance_criteria>
    - update-unit-database.ts output includes "Step 8: Parsing Datasheets_models_cost.csv..."
    - update-unit-database.ts output shows matched unit count consistent with build-unit-db.ts
    - update-unit-database.ts completes successfully (exit code 0)
    - Both scripts produce equivalent unit_database.json output
  </acceptance_criteria>
  <done>update-unit-database.ts has the same cost CSV + sub-faction logic as build-unit-db.ts per D-07. Both build paths produce identical results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply. This phase modifies dev-side build scripts only. No user input, no network calls, no runtime code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Tampering | CSV data files | accept | CSVs are developer-fetched from wahapedia.ru; pre-existing trust model from Phase 116 |
</threat_model>

<verification>
Run `npx tsx scripts/build-unit-db.ts` -- exits 0 with 90%+ coverage. Sub-faction count > 0 in output.
</verification>

<success_criteria>
- Both build scripts resolve points from Datasheets_models_cost.csv via datasheet_id join
- Multi-tier units have UdbUnitPointsRow entries with correct model counts
- Single-tier units have base_points set directly
- Sub-factions assigned via keyword matching (not BSData catalogues)
- readCsvFile + extractModelCount live in parseCsv.ts
- Build passes with coverage well above 58%
</success_criteria>

<output>
Create `.planning/phases/117-points-coverage/117-01-SUMMARY.md` when done
</output>

---
phase: 142-technique-authoring-library-browse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/technique.ts
  - src/features/techniques/techniqueSchema.ts
  - src/features/techniques/techniqueSection.ts
  - tests/data-layer/technique-graph-save.test.ts
  - tests/data-layer/technique-duplicate.test.ts
  - tests/data-layer/technique-usage-counts.test.ts
  - tests/techniques/techniqueSchema.test.ts
  - tests/techniques/applyTechniqueFilters.test.ts
autonomous: true
requirements: [TECH-01, TECH-02, TECH-04, SLOT-01, SLOT-02, LIB-03]
must_haves:
  truths:
    - "Technique entity, slot, section, and step DB-row types exist and are importable"
    - "Draft types (DraftTechniqueSlot, DraftTechniqueStep, DraftTechniqueSection) and TechniqueFormValues exist"
    - "techniqueSchema Zod schema validates name + reuses RECIPE_EFFECTS / RECIPE_DIFFICULTIES"
    - "Five Nyquist test files exist and fail RED (pending implementation in later waves)"
  artifacts:
    - path: "src/types/technique.ts"
      provides: "Technique / TechniqueColourSlot / TechniqueSection / TechniqueStep row types + Draft* types + TechniqueFormValues"
      contains: "DraftTechniqueSlot"
    - path: "src/features/techniques/techniqueSchema.ts"
      provides: "techniqueSchema Zod object + re-exported RECIPE_EFFECTS / RECIPE_DIFFICULTIES"
      contains: "techniqueSchema"
    - path: "src/features/techniques/techniqueSection.ts"
      provides: "makeDraftTechniqueSection + buildDraftTechniqueSections helpers"
      contains: "makeDraftTechniqueSection"
    - path: "tests/data-layer/technique-graph-save.test.ts"
      provides: "Non-destructive save invariant test (RED until plan 02)"
      contains: "technique_step_id"
    - path: "tests/data-layer/technique-duplicate.test.ts"
      provides: "Duplicate fresh-IDs test (RED until plan 02)"
    - path: "tests/data-layer/technique-usage-counts.test.ts"
      provides: "Usage-count + step-count JOIN test (RED until plan 02)"
    - path: "tests/techniques/techniqueSchema.test.ts"
      provides: "Zod validity test (GREEN once schema lands)"
    - path: "tests/techniques/applyTechniqueFilters.test.ts"
      provides: "Pure filter test (RED until plan 04)"
  key_links:
    - from: "src/features/techniques/techniqueSchema.ts"
      to: "src/features/recipes/recipeSchema.ts"
      via: "import RECIPE_EFFECTS, RECIPE_DIFFICULTIES"
      pattern: "RECIPE_EFFECTS"
---

<objective>
Establish the type foundation, Zod schema, draft-section helpers, and all five Nyquist test stubs for the Technique Library before any data-layer or UI implementation begins. This is Wave 0: the data-layer test files must exist and fail RED so subsequent waves close them GREEN.

Purpose: The FND-03 non-destructive-save invariant is the load-bearing risk of this phase. Per Nyquist (workflow.nyquist_validation = true), the test that proves `technique_step_id` survives edits must exist before `saveTechniqueGraph` is written. The technique entity types also gate every other plan — queries, hooks, and components all import from `src/types/technique.ts`.

Output: `src/types/technique.ts`, `src/features/techniques/techniqueSchema.ts`, `src/features/techniques/techniqueSection.ts`, and five test files under `tests/data-layer/` and `tests/techniques/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-technique-authoring-library-browse/142-RESEARCH.md
@.planning/phases/142-technique-authoring-library-browse/142-PATTERNS.md
@.planning/phases/142-technique-authoring-library-browse/142-VALIDATION.md

<interfaces>
<!-- Recipe draft shapes the technique types mirror. Executor uses these directly. -->

From src/types/recipe.ts (DraftStep / DraftSection — technique versions DROP paint_id, alt_paint_id, step_photo_path, section_type, technique, execution_mode, applies_to and ADD colour_slot_id):
```typescript
export interface DraftStep {
  localId: string; dbId: number | null; step_name: string; paint_id: number | null;
  notes: string | null; painting_phase: string | null; tool: string | null;
  technique: string | null; dilution: string | null; time_estimate_minutes: number | null;
  step_photo_path: string | null; alt_paint_id: number | null; technique_step_id?: number | null;
}
export interface DraftSection {
  localId: string; dbId: number | null; name: string; surface: string | null;
  optional: number; notes: string | null; section_type: string | null;
  technique: string | null; execution_mode: string | null; applies_to: string | null;
  steps: DraftStep[];
}
```

From src/features/recipes/recipeSchema.ts (reuse these enums — DO NOT redefine):
```typescript
export const RECIPE_EFFECTS = [ ... ] as const;       // OSL, NMM, etc.
export const RECIPE_DIFFICULTIES = [ ... ] as const;
```

Migration 051 column sets (VERIFIED — column traps):
- technique_colour_slots: id, technique_id, name, role_hint, order_index, created_at — NO updated_at
- technique_steps: id, technique_section_id, colour_slot_id, step_name, order_index, notes, painting_phase, tool, technique, dilution, time_estimate_minutes, created_at — NO updated_at, NO step_photo_path, NO alt_paint_id, NO technique_id (joins via technique_sections)
- technique_sections: id, technique_id, name, surface, optional, order_index, notes, created_at, updated_at
- techniques: id, name, effect, difficulty, notes, created_at, updated_at
</interfaces>

<test_harness>
<!-- better-sqlite3 data-layer harness already exists. Executor imports from here. -->
From tests/data-layer/db-helpers.ts: `createHobbyforgeDb()` returns an in-memory better-sqlite3 Database with all migrations applied (including 051). Use `db.prepare(...).run(...)` / `.get(...)` / `.all(...)` directly — no Tauri mocks.
Reference analog: tests/data-layer/technique-progress-identity.test.ts (FND-03 invariant, exact SQL + assertions).
Reference analog: tests/data-layer/recipe-persistence.test.ts (recipe save test harness).
</test_harness>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Technique entity + draft types and Zod schema</name>
  <files>src/types/technique.ts, src/features/techniques/techniqueSchema.ts</files>
  <read_first>
    - src/types/recipe.ts (DraftStep, DraftSection, RecipeFormValues shapes to mirror)
    - src/features/recipes/recipeSchema.ts (recipeSchema Zod object + RECIPE_EFFECTS / RECIPE_DIFFICULTIES exports)
    - .planning/phases/142-technique-authoring-library-browse/142-PATTERNS.md (techniqueSchema.ts section, lines 48-114)
    - .planning/phases/142-technique-authoring-library-browse/142-RESEARCH.md (DraftTechniqueSlot/Step/Section code examples + migration 051 column sets)
  </read_first>
  <action>
    Create `src/types/technique.ts` with DB-row interfaces mirroring migration 051 exactly: `Technique` (id, name, effect, difficulty, notes, created_at, updated_at — all per migration), `TechniqueColourSlot` (id, technique_id, name, role_hint, order_index, created_at — NO updated_at), `TechniqueSection` (id, technique_id, name, surface, optional, order_index, notes, created_at, updated_at), `TechniqueStep` (id, technique_section_id, colour_slot_id, step_name, order_index, notes, painting_phase, tool, technique, dilution, time_estimate_minutes, created_at — NO updated_at, NO step_photo_path, NO alt_paint_id). Add `CreateTechniqueInput`/`UpdateTechniqueInput` following the Omit pattern from recipe.ts.
    Add draft types: `DraftTechniqueSlot` (localId, dbId, name, role_hint, order_index), `DraftTechniqueStep` (localId, dbId, step_name, colour_slot_id: string | null, notes, painting_phase, tool, technique, dilution, time_estimate_minutes — NO paint_id/alt_paint_id/step_photo_path; colour_slot_id holds a slot localId in form state, resolved to PK at save time), `DraftTechniqueSection` (localId, dbId, name, surface, optional, notes, steps: DraftTechniqueStep[] — NO section_type/technique/execution_mode/applies_to). Add `TechniqueFormValues` interface matching the Zod schema shape (defined in types so the query layer can import without a feature dependency, same rationale as RecipeFormValues).
    Create `src/features/techniques/techniqueSchema.ts`: import and re-export `RECIPE_EFFECTS`, `RECIPE_DIFFICULTIES` from `@/features/recipes/recipeSchema`. Define `techniqueSchema = z.object({ name: z.string().min(1).max(120), description: z.string().max(500).nullable(), effect: z.string().nullable(), difficulty: z.string().nullable(), estimated_minutes: z.number().int().min(1).nullable(), result_photo_path: z.string().nullable(), notes: z.string().max(2000).nullable() })`. Export `type TechniqueFormValues = z.infer<typeof techniqueSchema>` (and ensure the types/technique.ts TechniqueFormValues matches structurally). Slot/step validity (name non-empty, ≥1 step) is enforced via toast in saveTechniqueGraph, NOT in this Zod schema — match the recipe pattern.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -i "technique" || echo "no technique type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/technique.ts` exports Technique, TechniqueColourSlot, TechniqueSection, TechniqueStep, DraftTechniqueSlot, DraftTechniqueStep, DraftTechniqueSection, TechniqueFormValues
    - TechniqueColourSlot and TechniqueStep have NO updated_at field; TechniqueStep has NO paint_id/alt_paint_id/step_photo_path
    - `techniqueSchema.ts` re-exports RECIPE_EFFECTS and RECIPE_DIFFICULTIES from recipeSchema and exports techniqueSchema
    - `pnpm exec tsc --noEmit` reports no errors involving technique type files
  </acceptance_criteria>
  <done>Technique types + Zod schema compile cleanly and are importable by later plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Draft-section helpers + techniqueSchema unit test</name>
  <files>src/features/techniques/techniqueSection.ts, tests/techniques/techniqueSchema.test.ts</files>
  <read_first>
    - src/features/recipes/recipeSection.ts (makeDraftSection, buildDraftSections — analog to mirror)
    - src/features/techniques/techniqueSchema.ts (the schema created in Task 1)
    - src/types/technique.ts (draft types)
    - .planning/phases/142-technique-authoring-library-browse/142-PATTERNS.md (techniqueSection.ts section, lines 118-157)
  </read_first>
  <behavior>
    - Test: techniqueSchema accepts a valid object with name + nullable metadata
    - Test: techniqueSchema rejects empty name (min(1) violation)
    - Test: techniqueSchema rejects name longer than 120 chars
    - Test: techniqueSchema accepts null description/effect/difficulty/estimated_minutes/notes
  </behavior>
  <action>
    Create `src/features/techniques/techniqueSection.ts` mirroring `recipeSection.ts`: `makeDraftTechniqueSection(name = "Steps"): DraftTechniqueSection` returning a section with `crypto.randomUUID()` localId, dbId null, surface null, optional 0, notes null, empty steps (OMIT section_type/technique/execution_mode/applies_to). `buildDraftTechniqueSections(sections: TechniqueSection[], steps: TechniqueStep[], slots: TechniqueColourSlot[]): DraftTechniqueSection[]` — map each section to a draft, filter+sort its steps by order_index, and map each step's DB integer `colour_slot_id` back to the owning slot's form localId via a `slotDbIdToLocalId` map built from the slots list (null when the step has no slot). Also add `buildDraftTechniqueSlots(slots: TechniqueColourSlot[]): DraftTechniqueSlot[]` returning draft slots with fresh localIds and dbId set to the slot's id (this map seeds the slotDbIdToLocalId lookup used for steps).
    Create `tests/techniques/techniqueSchema.test.ts`: jsdom environment, import techniqueSchema, assert `.safeParse` success on a valid technique object and failure on empty name + 121-char name. This test should pass GREEN immediately (schema exists from Task 1).
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/techniques/techniqueSchema.test.ts 2>&1 | grep -v '^#' | grep -E "passed|failed|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `techniqueSection.ts` exports makeDraftTechniqueSection, buildDraftTechniqueSections, buildDraftTechniqueSlots
    - makeDraftTechniqueSection returns a section with NO section_type/technique/execution_mode/applies_to fields
    - `tests/techniques/techniqueSchema.test.ts` passes (GREEN) with at least 3 assertions (valid, empty-name-rejected, overlong-name-rejected)
  </acceptance_criteria>
  <done>Draft-section helpers compile and the schema unit test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Four data-layer + filter Nyquist test stubs (RED)</name>
  <files>tests/data-layer/technique-graph-save.test.ts, tests/data-layer/technique-duplicate.test.ts, tests/data-layer/technique-usage-counts.test.ts, tests/techniques/applyTechniqueFilters.test.ts</files>
  <read_first>
    - tests/data-layer/technique-progress-identity.test.ts (FND-03 invariant harness — exact better-sqlite3 SQL + createHobbyforgeDb usage)
    - tests/data-layer/db-helpers.ts (createHobbyforgeDb signature)
    - tests/data-layer/recipe-persistence.test.ts (recipe save test structure)
    - .planning/phases/142-technique-authoring-library-browse/142-VALIDATION.md (per-task verification map + three mandatory data-layer cases)
    - .planning/phases/142-technique-authoring-library-browse/142-RESEARCH.md (Validation Architecture — three mandatory test cases, lines 775-789)
  </read_first>
  <action>
    These tests import functions that do NOT yet exist (saveTechniqueGraph, duplicateTechnique, getTechniqueUsageCounts, getTechniquesWithCounts, applyTechniqueFilters — all created in plans 02/04). They MUST fail RED now (import error or assertion failure) and turn GREEN when those plans land. Use `// @vitest-environment node` for the data-layer tests.

    `technique-graph-save.test.ts` (TECH-01, TECH-02, SLOT-01, SLOT-02): seed a technique with two sections, steps S1/S2/S3, and two colour slots via createHobbyforgeDb + direct SQL. Because saveTechniqueGraph runs through tauri-plugin-sql in production, drive the diff invariant at the SQL level the same way technique-progress-identity.test.ts does: record S2's PK, simulate an edit that removes S1 and reorders S3 to top using UPDATE-by-PK (the pattern saveTechniqueGraph must produce), and assert S2's PK is unchanged and S1's row is gone. Add a teeth-proving counter-case asserting that a DELETE+INSERT of S2 produces a DIFFERENT PK (proving the test has teeth). Add a slot assertion: a step's colour_slot_id resolves to the integer slot PK; removing a slot with ON DELETE SET NULL nulls referencing steps' colour_slot_id.
    `technique-duplicate.test.ts` (TECH-04): seed a technique with sections/steps/slots, call `duplicateTechnique` (import from `@/db/queries/techniques` — RED until plan 02), assert the returned new technique id differs, and assert every technique_colour_slots.id, technique_steps.id, technique_sections.id in the copy differs from the original (no shared PKs).
    `technique-usage-counts.test.ts` (TECH-03, TECH-05, LIB-01/02): seed a technique with two sections and N steps across them; assert `getTechniquesWithCounts` (import RED until plan 02) returns the correct step_count by JOINing technique_steps through technique_sections (NOT a direct technique_id GROUP BY, which would fail — technique_steps has no technique_id). Insert two recipe_technique_instances and assert usage_count = 2 via getTechniqueUsageCounts; delete one and assert it drops to 1.
    `applyTechniqueFilters.test.ts` (LIB-03): jsdom; import `applyTechniqueFilters` from `@/features/techniques/applyTechniqueFilters` (RED until plan 04). Assert name filter narrows by case-insensitive substring on technique.name; effect filter narrows by exact effect match; empty filters return all.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/data-layer/technique-graph-save.test.ts tests/data-layer/technique-duplicate.test.ts tests/data-layer/technique-usage-counts.test.ts tests/techniques/applyTechniqueFilters.test.ts 2>&1 | grep -v '^#' | grep -E "passed|failed|FAIL|No test files|Cannot find" || echo "tests RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - All four test files exist and contain real assertions (not empty `it.todo`)
    - technique-graph-save.test.ts includes a DELETE+INSERT counter-case proving the non-destructive assertion has teeth
    - technique-usage-counts.test.ts asserts step_count is derived via JOIN through technique_sections
    - The data-layer tests fail RED (missing imports from techniques.ts) — proven by the verify command showing failures or unresolved imports
  </acceptance_criteria>
  <done>Five Nyquist test files exist; the schema test is GREEN; the four implementation-gated tests fail RED, ready for plans 02 and 04 to close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form draft → SQLite | User-entered technique/slot/step text crosses into parameterized SQL (later plans) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Tampering | technique name/slot/step free-text fields | mitigate | Zod max-length constraints (name 120, description 500, notes 2000) in techniqueSchema; slot name/role-hint length enforced at save (plan 02) |
| T-142-02 | Information Disclosure | local SQLite data | accept | Single-user offline desktop app; no network/auth surface; no PII |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` reports no errors in technique type/schema files
- `pnpm test -- tests/techniques/techniqueSchema.test.ts` is GREEN
- The four data-layer/filter tests are RED (missing implementation imports) — confirming Wave 0 gating before implementation
</verification>

<success_criteria>
- Technique entity, draft, and form-value types exist and compile
- techniqueSchema validates name and reuses recipe effect/difficulty enums
- All five Nyquist test files from 142-VALIDATION.md exist; the data-layer trio fails RED pending plan 02
</success_criteria>

<output>
Create `.planning/phases/142-technique-authoring-library-browse/142-01-SUMMARY.md` when done
</output>

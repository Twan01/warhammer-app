---
phase: 145-integration-pass
plan: 03
type: execute
wave: 1
depends_on: [145-01]
files_modified:
  - src/features/recipes/AssignmentChecklist.tsx
  - src/features/recipes/ChecklistStepRow.tsx
  - tests/techniques/AssignmentChecklist.test.tsx
  - tests/painting/sectionedTimeline.test.tsx
autonomous: true
requirements: [INTG-03, INTG-04]
must_haves:
  truths:
    - "The apply-to-units per-unit checklist resolves technique steps via effectivePaintId — the step list is NOT empty for technique-owned steps"
    - "ChecklistStepRow renders the resolved paint swatch/detail for a technique step with a filled slot (gated on the parent-resolved paint, not step.paint_id)"
    - "Step completion records against recipe_step.id (unchanged)"
    - "SectionedTimeline renders the 'from technique X' badge for technique-sourced sections when techniqueSectionInfoMap is provided"
    - "Log Session section names include technique-sourced sections (real recipe_sections — covered by an assertion, no code change)"
  artifacts:
    - path: "src/features/recipes/AssignmentChecklist.tsx"
      provides: "useSlotResolutionMap + effectivePaintId resolution for all three step-render sites"
      contains: "effectivePaintId"
    - path: "src/features/recipes/ChecklistStepRow.tsx"
      provides: "paint guards keyed on !!paint, not step.paint_id"
    - path: "tests/techniques/AssignmentChecklist.test.tsx"
      provides: "INTG-03 component test — technique step list not empty, resolved swatch shown"
    - path: "tests/painting/sectionedTimeline.test.tsx"
      provides: "INTG-04 badge coverage test"
  key_links:
    - from: "src/features/recipes/AssignmentChecklist.tsx"
      to: "useSlotResolutionMap"
      via: "component-level hook call"
      pattern: "useSlotResolutionMap\\(recipeId\\)"
    - from: "src/features/recipes/AssignmentChecklist.tsx"
      to: "ChecklistStepRow"
      via: "resolvedPaint(step) helper"
      pattern: "resolvedPaint\\(step\\)"
---

<objective>
Wire effectivePaintId() into the apply-to-units checklist so technique-owned steps
resolve their paint correctly (the per-unit list is no longer empty/blank for
technique steps), and cover INTG-04: the SectionedTimeline "from technique X"
badge (already wired in Phase 143, prove it) and Log Session section-name
inclusion (already correct — technique sections are real recipe_sections, assert
it without rebuilding anything).

Purpose: INTG-03 (apply-to-units per-unit step progress works for technique
steps), INTG-04 (SectionedTimeline badge present + Log Session selector includes
technique-sourced section names).

Output: AssignmentChecklist resolving via slotMap; ChecklistStepRow gating on the
resolved paint; a new INTG-03 component test; an INTG-04 badge coverage test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/145-integration-pass/145-PATTERNS.md
@.planning/phases/145-integration-pass/145-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Plan 01 provides useSlotResolutionMap is pre-existing. -->

From src/hooks/useSlotResolutionMap.ts:
  useSlotResolutionMap(recipeId: number | undefined) → { data: Map<recipe_step.id, paint_id|null> }

From src/lib/effectivePaintId.ts:
  effectivePaintId(step, slotMap): number | null  // technique step → slotMap.get(step.id) ?? null; plain → step.paint_id

AssignmentChecklist current state (src/features/recipes/AssignmentChecklist.tsx):
  - line 29: const { data: steps = [] } = useRecipePaints(recipeId)  — steps include technique steps (paint_id NULL)
  - lines 40-43: paintsById = Map<paint.id, Paint>
  - THREE identical render sites pass paint to ChecklistStepRow:
      line 127 (orphan steps), line 152 (sectioned accordion), line 169 (flat list)
      each currently: paint={step.paint_id !== null ? paintsById.get(step.paint_id) : undefined}
  - handleToggle(step.id, ...) — completion keyed on recipe_step.id (recipeStepId)

ChecklistStepRow current state (src/features/recipes/ChecklistStepRow.tsx):
  - props: { step, completed, paint, altPaint, onToggle } — already accepts resolved paint: Paint | undefined
  - line 38 (hasDetail): `(step.paint_id !== null && !!paint) ||` — must become `!!paint ||`
  - line 80 (swatch render): `{step.paint_id !== null && paint && (` — must become `{paint && (`

SectionedTimeline (src/features/recipes/SectionedTimeline.tsx) — already renders TechniqueSectionBadge
  at lines 148-153 when techniqueSectionInfoMap.get(section.id) !== undefined. No code change needed; add a test.

PaintingSessionSheet (src/features/painting-mode/PaintingSessionSheet.tsx) — receives sectionName as a
  scalar prop from PaintingModeView; technique sections come from useRecipeSections automatically.
  No code change needed for Log Session (RESEARCH A1 verified).

Test fixtures:
  - tests/painting/sectionedTimeline.test.tsx already has makeSection/makeStep factories and mocks
    RecipeStepTimeline + recipeSteps.isPaintMissing. TechniqueSectionInfo: { techniqueId, instanceId, techniqueName }.
  - tests/techniques/AssignmentChecklist.test.tsx does NOT exist (Wave 0 gap) — create it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AssignmentChecklist + ChecklistStepRow — resolve via effectivePaintId</name>
  <files>src/features/recipes/AssignmentChecklist.tsx, src/features/recipes/ChecklistStepRow.tsx</files>
  <read_first>
    - src/features/recipes/AssignmentChecklist.tsx (the file — hook calls lines 29-43; three render sites at lines 127, 152, 169)
    - src/features/recipes/ChecklistStepRow.tsx (the file — hasDetail line 38; swatch render line 80)
    - src/features/recipes/SectionedTimeline.tsx lines 56-92 (effectivePaintId consumer analog: hook once, resolve per step)
    - .planning/phases/145-integration-pass/145-PATTERNS.md lines 532-597, 763-768 (wire-up + the two ChecklistStepRow guard changes)
    - .planning/phases/145-integration-pass/145-RESEARCH.md lines 257-294, 450-453 (read sites + Pitfall 4)
  </read_first>
  <action>
    AssignmentChecklist: import effectivePaintId from "@/lib/effectivePaintId" and useSlotResolutionMap
    from "@/hooks/useSlotResolutionMap". Call `const { data: slotMap = new Map() } = useSlotResolutionMap(recipeId)`
    ONCE at component level (never per-row — N+1 anti-pattern). Add a closure
    `function resolvedPaint(step: RecipeStep) { const id = effectivePaintId(step, slotMap); return id !== null ? paintsById.get(id) : undefined; }`.
    Replace all THREE `paint={step.paint_id !== null ? paintsById.get(step.paint_id) : undefined}` expressions
    (orphan steps ~line 127, sectioned accordion ~line 152, flat list ~line 169) with `paint={resolvedPaint(step)}`.
    Leave altPaint resolution (step.alt_paint_id) and handleToggle(step.id, ...) unchanged — completion stays keyed
    on recipe_step.id.

    ChecklistStepRow: the parent now passes already-resolved paint. Change the hasDetail guard (line 38) from
    `(step.paint_id !== null && !!paint) ||` to `!!paint ||`. Change the collapsible swatch guard (line 80) from
    `{step.paint_id !== null && paint && (` to `{paint && (`. No other changes; the props interface already accepts
    `paint: Paint | undefined`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "AssignmentChecklist|ChecklistStepRow" || echo "TYPECHECK_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - AssignmentChecklist calls useSlotResolutionMap once (verify: `grep -c "useSlotResolutionMap(recipeId)" src/features/recipes/AssignmentChecklist.tsx` == 1)
    - All three render sites use resolvedPaint (verify: `grep -c "paint={resolvedPaint(step)}" src/features/recipes/AssignmentChecklist.tsx` == 3)
    - No `step.paint_id` resolution reads remain in AssignmentChecklist (verify: `grep -c "step.paint_id" src/features/recipes/AssignmentChecklist.tsx` == 0)
    - ChecklistStepRow no longer gates on step.paint_id (verify: `grep -c "step.paint_id" src/features/recipes/ChecklistStepRow.tsx` == 0)
    - ChecklistStepRow swatch render gates on `{paint &&` (verify: `grep "{paint && (" src/features/recipes/ChecklistStepRow.tsx` matches)
    - `npx tsc --noEmit` clean for both files
  </acceptance_criteria>
  <done>AssignmentChecklist resolves all step paints via effectivePaintId once at component level; ChecklistStepRow gates on the resolved paint; no step.paint_id reads remain; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: INTG-03 AssignmentChecklist component test (technique step list not empty)</name>
  <files>tests/techniques/AssignmentChecklist.test.tsx</files>
  <read_first>
    - src/features/recipes/AssignmentChecklist.tsx (the modified component — hooks to mock: useRecipePaints, useRecipeSections, useStepProgress, useToggleStepProgress, usePaints, useUnit, useUpdateUnit, useSlotResolutionMap)
    - tests/painting/recipeDetailSheet.test.tsx lines 1-90 (vi.mock pattern for many hooks in this codebase — RTL + mocked React Query hooks)
    - tests/painting/sectionedTimeline.test.tsx lines 39-70 (makeStep/makeSection fixture style)
    - .planning/phases/145-integration-pass/145-RESEARCH.md lines 685-693 (INTG-03 test intent)
  </read_first>
  <behavior>
    - A technique step (technique_step_id set, paint_id NULL) with a filled slot in the slotMap renders in the
      checklist with its resolved paint name/swatch — the list is NOT empty for technique-owned steps
    - A technique step with an unfilled slot still renders its row (step name visible), just without a swatch
    - A plain step with a paint_id still renders its paint (FND-04 fallback unbroken)
  </behavior>
  <action>
    Create tests/techniques/AssignmentChecklist.test.tsx (jsdom/RTL). vi.mock the data hooks AssignmentChecklist
    imports: useRecipePaints (return steps incl. a technique step with technique_step_id set + paint_id null, and a
    plain step with a paint_id), useRecipeSections (return [] for the flat-list path to keep the test simple),
    useStepProgress (return []), useToggleStepProgress (return a mock mutation), usePaints (return paints incl. the
    slot-resolved paint), useUnit/useUpdateUnit (return stubs), and useSlotResolutionMap (return a Map mapping the
    technique step's recipe_step.id → the filled paint id). Render AssignmentChecklist with a minimal assignment,
    recipeId, unitId. Assert: the technique step's name is in the document (list not empty), and expanding/inspecting
    it shows the resolved paint name (the slotMap-resolved paint), and the plain step's paint also shows. Use
    screen queries; follow the recipeDetailSheet.test.tsx mocking conventions.
  </action>
  <verify>
    <automated>pnpm test -- tests/techniques/AssignmentChecklist.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists and runs green: `pnpm test -- tests/techniques/AssignmentChecklist.test.tsx` exits 0 with 0 failures
    - Test mocks useSlotResolutionMap with a non-empty Map (verify: `grep -c "useSlotResolutionMap" tests/techniques/AssignmentChecklist.test.tsx` >= 1)
    - At least one assertion proves a technique step (technique_step_id set) renders its resolved paint (verify: `grep -c "technique_step_id" tests/techniques/AssignmentChecklist.test.tsx` >= 1)
  </acceptance_criteria>
  <done>A new RTL test proves the apply-to-units checklist is not empty for technique steps and shows the slot-resolved paint; green.</done>
</task>

<task type="auto">
  <name>Task 3: INTG-04 coverage — SectionedTimeline badge test + Log Session assertion</name>
  <files>tests/painting/sectionedTimeline.test.tsx</files>
  <read_first>
    - tests/painting/sectionedTimeline.test.tsx (the file — makeSection/makeStep factories lines 39-70; existing describe blocks)
    - src/features/recipes/SectionedTimeline.tsx lines 118-153 (badge renders when techniqueSectionInfoMap.get(section.id) is defined; TechniqueSectionInfo shape)
    - src/features/recipes/TechniqueSectionBadge.tsx (badge copy: "from {technique.name}")
    - .planning/phases/145-integration-pass/145-RESEARCH.md lines 296-321 (INTG-04: badge already wired in Phase 143; Log Session already correct)
    - .planning/phases/145-integration-pass/145-UI-SPEC.md lines 139-146, 259 (badge placement + "from {technique.name}" copy)
  </read_first>
  <behavior>
    - When SectionedTimeline receives a techniqueSectionInfoMap entry for a section, that section header renders
      the "from {techniqueName}" badge
    - When no techniqueSectionInfoMap is provided, no technique badge renders (plain recipe path)
  </behavior>
  <action>
    Add a describe block to tests/painting/sectionedTimeline.test.tsx covering INTG-04. Render SectionedTimeline
    with one section and a `techniqueSectionInfoMap` Map<section.id, { techniqueId, instanceId, techniqueName }>
    whose entry names a technique (e.g. "NMM Gold"); assert the badge text "from NMM Gold" (or the badge's
    rendered technique name) is in the document. Add a second case rendering the same section WITHOUT
    techniqueSectionInfoMap and assert no "from " badge text appears. If TechniqueSectionBadge needs a non-mocked
    import, allow it to render (it is display-only); do not mock it away. Reuse the existing makeSection/makeStep
    factories. Add a short inline comment noting Log Session (INTG-04) needs no code — technique sections are real
    recipe_sections surfaced by useRecipeSections (RESEARCH A1), so this badge test plus the existing
    PaintingSessionSheet.test.tsx scalar-prop coverage satisfies INTG-04.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting/sectionedTimeline.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - sectionedTimeline.test.tsx asserts the "from {technique}" badge renders with techniqueSectionInfoMap (verify: `grep -ci "techniqueSectionInfoMap" tests/painting/sectionedTimeline.test.tsx` >= 1)
    - A negative case asserts no technique badge without the map
    - `pnpm test -- tests/painting/sectionedTimeline.test.tsx` passes including the new INTG-04 cases
  </acceptance_criteria>
  <done>SectionedTimeline badge coverage proves the "from technique X" badge renders for technique sections; Log Session inclusion documented as no-code-needed; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slotMap cache → checklist render | resolved paint feeds per-unit checklist; a dropped technique step = invisible work item |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-07 | Denial of service (blank checklist) | AssignmentChecklist | mitigate | resolvedPaint via effectivePaintId ensures technique steps render (not gated out by paint_id NULL); ChecklistStepRow guards changed to !!paint — proven by INTG-03 test |
| T-145-08 | Information disclosure (missing badge) | SectionedTimeline | accept | badge already wired in Phase 143; this plan adds a regression test only — no new code path |
| T-145-09 | Repudiation (lost completion) | handleToggle | accept | completion keyed on recipe_step.id (unchanged); no completion code touched in this plan |
</threat_model>

<verification>
- `pnpm test -- tests/techniques/AssignmentChecklist.test.tsx tests/painting/sectionedTimeline.test.tsx` green
- `npx tsc --noEmit` clean for AssignmentChecklist + ChecklistStepRow
- No `step.paint_id` reads remain in AssignmentChecklist or ChecklistStepRow
</verification>

<success_criteria>
- INTG-03: apply-to-units checklist resolves technique steps (list not empty, swatch shown); completion on recipe_step.id
- INTG-04: SectionedTimeline "from technique X" badge covered by test; Log Session inclusion documented (no code change)
</success_criteria>

<output>
Create `.planning/phases/145-integration-pass/145-03-SUMMARY.md` when done
</output>

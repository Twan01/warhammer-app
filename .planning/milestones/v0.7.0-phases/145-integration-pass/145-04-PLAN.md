---
phase: 145-integration-pass
plan: 04
type: execute
wave: 2
depends_on: [145-01, 145-02]
files_modified:
  - src/features/painting-mode/SlotReassignMiniDialog.tsx
  - src/features/painting-mode/PaintingModeView.tsx
  - tests/painting-mode/SlotReassignMiniDialog.test.tsx
autonomous: true
requirements: [INTG-07]
must_haves:
  truths:
    - "Tapping a technique-sourced step's unfilled/filled swatch in Painting Mode opens a single-slot reassign mini-dialog"
    - "The mini-dialog targets the correct slot for the tapped step (via getStepSlotIdMap) and the correct instance (via the step's section technique_instance_id)"
    - "Saving the mini-dialog writes via updateSlotMap and invalidation refreshes the Painting Mode swatch live (and the unfilled-count banner)"
    - "The mini-dialog is rendered as a sibling outside the inner Painting Mode tree (no Radix portal clipping)"
  artifacts:
    - path: "src/features/painting-mode/SlotReassignMiniDialog.tsx"
      provides: "single-slot focused reassign dialog (max-w-xs, one SlotFillRow)"
      contains: "Reassign slot colour"
    - path: "src/features/painting-mode/PaintingModeView.tsx"
      provides: "swatch-tap → reassign target resolution + mini-dialog mount"
      contains: "SlotReassignMiniDialog"
    - path: "tests/painting-mode/SlotReassignMiniDialog.test.tsx"
      provides: "INTG-07 component test — single-slot save + toast"
  key_links:
    - from: "src/features/painting-mode/PaintingModeView.tsx"
      to: "useStepSlotIdMap"
      via: "slotId resolution for tapped step"
      pattern: "useStepSlotIdMap\\(recipeId\\)"
    - from: "src/features/painting-mode/SlotReassignMiniDialog.tsx"
      to: "useUpdateSlotMap"
      via: "single-entry slotFills Map save"
      pattern: "updateSlotMap.mutateAsync"
---

<objective>
Add inline slot reassignment in Painting Mode (INTG-07): tapping a technique
step's swatch opens a focused single-slot mini-dialog to reassign that slot's
paint without leaving the mode. Build the new SlotReassignMiniDialog component and
wire PaintingModeView's swatch-tap (the onReassignSlot affordance StepFocalView
already exposes from Plan 02) to resolve the tapped step's instance + slot and
open the dialog.

Purpose: INTG-07 — reassign a slot's paint inline during Painting Mode; the swatch
updates live via React Query invalidation.

Output: SlotReassignMiniDialog (single-slot variant of EditColoursDialog);
PaintingModeView reassign state/handler + mini-dialog mount; a component test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/145-integration-pass/145-PATTERNS.md
@.planning/phases/145-integration-pass/145-UI-SPEC.md
@.planning/phases/145-integration-pass/145-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Plan 01 provides useStepSlotIdMap; Plan 02 added isUnfilledSlot/onReassignSlot to StepFocalView and slotMap wiring to PaintingModeView. -->

From src/hooks/useSlotResolutionMap.ts (Plan 01 + existing):
  useSlotResolutionMap(recipeId) → Map<recipe_step.id, paint_id|null>
  useStepSlotIdMap(recipeId) → Map<recipe_step.id, slot_id|null>   // Plan 01
  useSlotMapByInstance(instanceId) → Map<slot_id, paint_id|null>   // current fill prefill

From src/hooks/useTechniqueInstances.ts:
  useInstancesForRecipe(recipeId) → RecipeTechniqueInstance[]  // each: { id, recipe_id, technique_id, detached, ... }
  useUpdateSlotMap() → mutation; mutateAsync({ instanceId, recipeId, slotFills: Map<slot_id, paint_id|null> })
    onSuccess invalidates SLOT_RESOLUTION_MAP_KEY + SLOT_MAP_BY_INSTANCE_KEY + RECIPE_PAINTS_KEY +
    RECIPE_SWATCH_KEY + RECIPE_AVAILABILITY_KEY + UNFILLED_SLOT_COUNT_KEY (Plan 01) → swatch + banner refresh live

From src/hooks/useTechniqueColourSlots.ts:
  useTechniqueColourSlots(techniqueId | undefined) → TechniqueColourSlot[]  // each: { id, technique_id, name, role_hint, order_index }

From src/features/recipes/SlotFillRow.tsx (reuse as-is, one row):
  <SlotFillRow slot={TechniqueColourSlot} paintId={number|null} onChange={(paintId)=>void} />

EditColoursDialog (src/features/recipes/EditColoursDialog.tsx) — the multi-slot TEMPLATE to adapt:
  - useEffect seeding pattern (lines 64-81): seed local slotFill from existingSlotMap once when open
  - handleSave (lines 91-100): updateSlotMap.mutateAsync + toast.success/toast.error + onClose
  - Dialog shell (lines 105-170): showCloseButton={false}, DialogDescription mandatory (a11y),
    footer outline "Cancel" + default save button with Loader2 spinner when isPending

PaintingModeView (src/features/painting-mode/PaintingModeView.tsx) after Plan 02:
  - already calls useSlotResolutionMap(recipeId) + useUnfilledSlotCount(recipeId); has sections from useRecipeSections
  - already computes currentStep + isUnfilledSlot and passes isUnfilledSlot to StepFocalView
  - StepFocalView already accepts onReassignSlot?: () => void (Plan 02) — currently unpassed
  - RecipeSection.technique_instance_id?: number | null gives the instance for a section

UI-SPEC (145-UI-SPEC.md) — SlotReassignMiniDialog contract:
  sm:max-w-xs ; title "Reassign slot colour" (text-base font-semibold) ; description
  "Change the paint assigned to this colour slot." ; one SlotFillRow (no ScrollArea) ;
  footer: "Close" (outline, left) + "Reassign paint" (default/accent, right) ; Loader2 when saving ;
  toast.success("Slot updated.") / toast.error("Failed to update slot. Please try again.") ;
  rendered as SIBLING outside SheetContent/inner tree (P6 portal-clipping avoidance).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SlotReassignMiniDialog — single-slot focused reassign component</name>
  <files>src/features/painting-mode/SlotReassignMiniDialog.tsx</files>
  <read_first>
    - src/features/recipes/EditColoursDialog.tsx (full file — the multi-slot template: seeding useEffect lines 64-81, handleSave lines 91-100, Dialog shell lines 105-170)
    - src/features/recipes/SlotFillRow.tsx (full file — the single row reused as-is)
    - src/hooks/useSlotResolutionMap.ts (useSlotMapByInstance — current-fill prefill)
    - src/hooks/useTechniqueColourSlots.ts (useTechniqueColourSlots — slot list to filter to one)
    - src/hooks/useTechniqueInstances.ts lines 118-130 (useUpdateSlotMap)
    - .planning/phases/145-integration-pass/145-PATTERNS.md lines 417-528 (full component scaffold)
    - .planning/phases/145-integration-pass/145-UI-SPEC.md lines 154-156, 188-237, 252-258 (dialog layout + copy)
  </read_first>
  <action>
    Create src/features/painting-mode/SlotReassignMiniDialog.tsx as a sibling/variant of EditColoursDialog,
    focused on a SINGLE slot. Props: `{ open: boolean; instanceId: number | null; slotId: number | null;
    techniqueId: number | null; recipeId: number; onClose: () => void }`. Load slots via
    useTechniqueColourSlots(techniqueId ?? undefined) and pick `slot = slots.find(s => s.id === slotId)`. Load the
    current fill via useSlotMapByInstance(instanceId ?? undefined). Local state: `slotFill: number | null` + a
    `seeded` flag; seed it once when open from existingSlotMap.get(slotId ?? -1) ?? null (reset on close), mirroring
    EditColoursDialog's seeding useEffect but for the single target slot only.

    Render a shadcn Dialog (onOpenChange closes via onClose), DialogContent className="sm:max-w-xs"
    showCloseButton={false}, DialogHeader with DialogTitle "Reassign slot colour" and the mandatory
    DialogDescription "Change the paint assigned to this colour slot." Body: a single SlotFillRow (no ScrollArea)
    for `slot` with paintId={slotFill} onChange={setSlotFill}. DialogFooter: outline Button "Close" (onClose) +
    default Button "Reassign paint" (handleSave) showing Loader2 spinner + disabled while pending/loading.
    handleSave: guard instanceId !== null && slotId !== null; build `new Map([[slotId, slotFill]])`; await
    updateSlotMap.mutateAsync({ instanceId, recipeId, slotFills }); toast.success("Slot updated.") + onClose on success;
    toast.error("Failed to update slot. Please try again.") in catch (dialog stays open). Title uses font-semibold
    per UI-SPEC (not font-medium). Do NOT inline any other component's implementation.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "SlotReassignMiniDialog" || echo "TYPECHECK_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the exact props (verify: `grep -c "SlotReassignMiniDialogProps" src/features/painting-mode/SlotReassignMiniDialog.tsx` >= 1)
    - Dialog uses sm:max-w-xs (verify: `grep "sm:max-w-xs" src/features/painting-mode/SlotReassignMiniDialog.tsx` matches)
    - Mandatory DialogDescription present (verify: `grep "Change the paint assigned to this colour slot." src/features/painting-mode/SlotReassignMiniDialog.tsx` matches)
    - Title + buttons match copy (verify: `grep "Reassign slot colour"`, `grep "Reassign paint"`, `grep ">Close<"` style — at minimum all three strings present)
    - Save path uses updateSlotMap.mutateAsync and toasts (verify: `grep "Slot updated." src/features/painting-mode/SlotReassignMiniDialog.tsx` and `grep "Failed to update slot. Please try again." src/features/painting-mode/SlotReassignMiniDialog.tsx` both match)
    - Single SlotFillRow, no ScrollArea (verify: `grep -c "ScrollArea" src/features/painting-mode/SlotReassignMiniDialog.tsx` == 0)
    - `npx tsc --noEmit` clean for this file
  </acceptance_criteria>
  <done>SlotReassignMiniDialog renders one slot, saves via updateSlotMap with the correct toasts, max-w-xs, no ScrollArea; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: PaintingModeView — swatch-tap resolves target + mounts the mini-dialog</name>
  <files>src/features/painting-mode/PaintingModeView.tsx</files>
  <read_first>
    - src/features/painting-mode/PaintingModeView.tsx (after Plan 02 — slotMap/unfilledSlotCount hooks already present; currentStep + isUnfilledSlot already computed; StepFocalView render block)
    - src/types/recipeSection.ts lines 27-44 (RecipeSection.technique_instance_id)
    - src/hooks/useTechniqueInstances.ts lines 74-81 (useInstancesForRecipe → instances with technique_id)
    - .planning/phases/145-integration-pass/145-PATTERNS.md lines 200-303 (hook calls + reassign state/handler + sibling mount; note techniqueId comes from useInstancesForRecipe map)
    - .planning/phases/145-integration-pass/145-RESEARCH.md lines 370-415 (slotId/instanceId/techniqueId resolution strategy D)
  </read_first>
  <action>
    Import SlotReassignMiniDialog from "./SlotReassignMiniDialog", and add useStepSlotIdMap from
    "@/hooks/useSlotResolutionMap" + useInstancesForRecipe from "@/hooks/useTechniqueInstances". Add useState import
    if not already present. Call `const { data: stepSlotIdMap = new Map() } = useStepSlotIdMap(recipeId)` and
    `const { data: instances = [] } = useInstancesForRecipe(recipeId)`. Build an instance→technique map:
    `instanceTechniqueId = new Map(instances.map(i => [i.id, i.technique_id]))` (useMemo).

    Add reassign state: `const [reassignOpen, setReassignOpen] = useState(false)` and
    `const [reassignTarget, setReassignTarget] = useState<{ instanceId: number; slotId: number; techniqueId: number } | null>(null)`.
    Add a `handleReassignSlot()` callback: if !currentStep return; slotId = stepSlotIdMap.get(currentStep.id) ?? null;
    section = sections.find(s => s.id === currentStep.section_id); instanceId = section?.technique_instance_id ?? null;
    if (slotId === null || instanceId === null) return; techniqueId = instanceTechniqueId.get(instanceId) ?? null;
    if (techniqueId === null) return; setReassignTarget({ instanceId, slotId, techniqueId }); setReassignOpen(true).

    Pass `onReassignSlot={handleReassignSlot}` to StepFocalView (alongside the isUnfilledSlot prop from Plan 02).
    Mount the dialog as a SIBLING after the main flex container (outside the inner overflow tree, P6) — render only
    when reassignTarget is set: <SlotReassignMiniDialog open={reassignOpen} instanceId/slotId/techniqueId from
    reassignTarget, recipeId, onClose={() => { setReassignOpen(false); setReassignTarget(null); }} />.
    Do NOT change completion, keyboard, banner, or step-photo logic.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "PaintingModeView" || echo "TYPECHECK_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - useStepSlotIdMap + useInstancesForRecipe called at component level (verify: `grep -c "useStepSlotIdMap(recipeId)" src/features/painting-mode/PaintingModeView.tsx` == 1 and `grep -c "useInstancesForRecipe(recipeId)" src/features/painting-mode/PaintingModeView.tsx` == 1)
    - handleReassignSlot resolves slotId, instanceId, techniqueId (verify: `grep -c "stepSlotIdMap.get(currentStep.id)" src/features/painting-mode/PaintingModeView.tsx` >= 1 and `grep "technique_instance_id" src/features/painting-mode/PaintingModeView.tsx` matches)
    - StepFocalView receives onReassignSlot (verify: `grep "onReassignSlot={handleReassignSlot}" src/features/painting-mode/PaintingModeView.tsx` matches)
    - SlotReassignMiniDialog mounted as a sibling, conditional on reassignTarget (verify: `grep -c "SlotReassignMiniDialog" src/features/painting-mode/PaintingModeView.tsx` >= 2 — import + render)
    - `npx tsc --noEmit` clean; `pnpm test -- tests/painting-mode/` green (no regressions)
  </acceptance_criteria>
  <done>Tapping a technique step's swatch resolves {instanceId, slotId, techniqueId} and opens the mini-dialog mounted as a sibling; typecheck + painting-mode tests green.</done>
</task>

<task type="auto">
  <name>Task 3: INTG-07 SlotReassignMiniDialog component test</name>
  <files>tests/painting-mode/SlotReassignMiniDialog.test.tsx</files>
  <read_first>
    - src/features/painting-mode/SlotReassignMiniDialog.tsx (the component under test — props + save path)
    - tests/painting/recipeDetailSheet.test.tsx lines 1-90 (vi.mock pattern for hooks: useTechniqueColourSlots, useSlotMapByInstance, useUpdateSlotMap)
    - src/features/recipes/SlotFillRow.tsx (PaintCombobox inside — usePaints mock needed)
    - .planning/phases/145-integration-pass/145-UI-SPEC.md lines 188-197, 252-258 (states + copy to assert)
  </read_first>
  <behavior>
    - The dialog renders the title "Reassign slot colour" and description when open with a valid slot
    - Clicking "Reassign paint" calls updateSlotMap.mutateAsync with { instanceId, recipeId, slotFills } where
      slotFills is a single-entry Map keyed on the target slotId
    - On success, toast.success("Slot updated.") fires and onClose is called
  </behavior>
  <action>
    Create tests/painting-mode/SlotReassignMiniDialog.test.tsx (jsdom/RTL). vi.mock the hooks the component imports:
    useTechniqueColourSlots (return one slot whose id === the test slotId), useSlotMapByInstance (return a Map with
    the current fill or empty), useUpdateSlotMap (return a mutation whose mutateAsync is a vi.fn resolving void),
    usePaints (return a small paint list for SlotFillRow/PaintCombobox), and sonner toast (spy on success/error).
    Render SlotReassignMiniDialog with open=true and a valid instanceId/slotId/techniqueId/recipeId + onClose spy.
    Assert the title "Reassign slot colour" renders. Click the "Reassign paint" button; assert updateSlotMap's
    mutateAsync was called with an object containing instanceId, recipeId, and a slotFills Map containing the slotId
    key; assert onClose was called. Keep the test focused (single happy-path + title render); follow the
    recipeDetailSheet.test.tsx mocking conventions.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting-mode/SlotReassignMiniDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists and runs green: `pnpm test -- tests/painting-mode/SlotReassignMiniDialog.test.tsx` exits 0 with 0 failures
    - Test asserts mutateAsync called with the single-slot save (verify: `grep -c "mutateAsync" tests/painting-mode/SlotReassignMiniDialog.test.tsx` >= 1)
    - Test asserts the dialog title renders (verify: `grep "Reassign slot colour" tests/painting-mode/SlotReassignMiniDialog.test.tsx` matches)
  </acceptance_criteria>
  <done>A focused RTL test proves the mini-dialog renders and saves a single slot via updateSlotMap with onClose; green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tapped step → reassign target | step→slot/instance resolution must target the correct slot or the user overwrites the wrong fill |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-10 | Tampering (wrong slot reassigned) | handleReassignSlot | mitigate | slotId from getStepSlotIdMap keyed on recipe_step.id + instanceId from the step's section technique_instance_id — both derived from the tapped step, never a global/first match |
| T-145-11 | Tampering (stale swatch) | updateSlotMap invalidation | mitigate | updateSlotMap onSuccess invalidates SLOT_RESOLUTION_MAP_KEY + UNFILLED_SLOT_COUNT_KEY (Plan 01) so the swatch + banner refresh live after reassign |
| T-145-12 | Information disclosure (clipped dialog) | mini-dialog mount | mitigate | rendered as a sibling outside the inner Painting Mode overflow tree (P6) so the Radix portal is not clipped |
</threat_model>

<verification>
- `pnpm test -- tests/painting-mode/` all green (incl. SlotReassignMiniDialog + no PaintingModeView regressions)
- `npx tsc --noEmit` clean for SlotReassignMiniDialog + PaintingModeView
- Mini-dialog mounted as a sibling outside the inner flex tree
</verification>

<success_criteria>
- INTG-07: tapping a technique step's swatch opens a single-slot reassign mini-dialog; saving via updateSlotMap refreshes the swatch live without leaving Painting Mode
</success_criteria>

<output>
Create `.planning/phases/145-integration-pass/145-04-SUMMARY.md` when done
</output>

---
phase: 145-integration-pass
plan: 02
type: execute
wave: 1
depends_on: [145-01]
files_modified:
  - src/features/painting-mode/PaintingModeView.tsx
  - src/features/painting-mode/StepFocalView.tsx
  - src/features/painting-mode/PaintReadinessBanner.tsx
  - tests/painting-mode/PaintReadinessBanner.test.tsx
  - tests/painting-mode/StepFocalView.test.tsx
autonomous: true
requirements: [INTG-01, INTG-02, INTG-06]
must_haves:
  truths:
    - "Painting Mode resolves every step's paint via effectivePaintId(step, slotMap) — no direct step.paint_id reads for technique steps"
    - "The missing-paints banner counts slot-resolved technique paints (a filled technique slot counts toward missing exactly as a plain step)"
    - "A technique step with an unfilled slot shows a distinct dashed indicator, separate from the paintless-step '(no paint)' text"
    - "The readiness banner shows 'N colour slots unfilled' when unfilledSlotCount > 0, neutral (not amber), below a separator"
    - "Keyboard shortcuts and step completion against recipe_step.id are unchanged"
  artifacts:
    - path: "src/features/painting-mode/PaintingModeView.tsx"
      provides: "slotMap-resolved missingPaints + currentPaint + unfilledSlotCount wiring"
      contains: "effectivePaintId"
    - path: "src/features/painting-mode/StepFocalView.tsx"
      provides: "three-state paint block: resolved swatch / unfilled-slot indicator / (no paint)"
      contains: "isUnfilledSlot"
    - path: "src/features/painting-mode/PaintReadinessBanner.tsx"
      provides: "unfilledSlotCount prop + 'N colour slots unfilled' line"
      contains: "unfilledSlotCount"
  key_links:
    - from: "src/features/painting-mode/PaintingModeView.tsx"
      to: "useSlotResolutionMap"
      via: "hook call at component level"
      pattern: "useSlotResolutionMap\\(recipeId\\)"
    - from: "src/features/painting-mode/PaintingModeView.tsx"
      to: "PaintReadinessBanner"
      via: "unfilledSlotCount prop"
      pattern: "unfilledSlotCount=\\{unfilledSlotCount\\}"
---

<objective>
Wire the effectivePaintId() resolution spine into Painting Mode's core view and
add the two readiness affordances: the distinct unfilled-slot indicator
(StepFocalView) and the "N colour slots unfilled" banner line
(PaintReadinessBanner). This closes the silent-undercounting bug where technique
steps (paint_id = NULL) were skipped by the missing-paints scan.

Purpose: INTG-01 (Painting Mode shows slot-resolved swatches + distinct unfilled
indicator, shortcuts/completion unchanged), INTG-02 (missing-paints counts
resolve via effectivePaintId), INTG-06 ("N colour slots unfilled" warning in the
readiness banner).

Output: PaintingModeView resolving via slotMap; StepFocalView with three-state
paint block; PaintReadinessBanner extended with unfilledSlotCount; extended tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/145-integration-pass/145-PATTERNS.md
@.planning/phases/145-integration-pass/145-UI-SPEC.md
@.planning/phases/145-integration-pass/145-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Plan 01 (Wave 0) provides the hooks below. -->

From src/hooks/useSlotResolutionMap.ts (existing + Plan 01 additions):
  useSlotResolutionMap(recipeId: number | undefined) → { data: Map<recipe_step.id, paint_id|null> }
  useUnfilledSlotCount(recipeId: number | undefined) → { data: number }   // Plan 01
  useStepSlotIdMap(recipeId: number | undefined) → { data: Map<recipe_step.id, slot_id|null> }  // Plan 01 (not used here)

From src/lib/effectivePaintId.ts:
  export function effectivePaintId(step: { id: number; paint_id: number|null; technique_step_id?: number|null }, slotMap: ReadonlyMap<number, number|null>): number | null;

From src/types/recipePaint.ts:
  RecipeStep already has technique_step_id?: number | null (used to distinguish technique vs plain steps)

PaintingModeView current state (src/features/painting-mode/PaintingModeView.tsx):
  - line 34-35: usePaints(), useRecipeSections(recipeId)
  - lines 45-57: missingPaints useMemo — reads `step.paint_id` directly (the bug)
  - lines 98-104: currentStep + currentPaint — reads `currentStep.paint_id` directly
  - line 111: showBanner = !bannerDismissed && missingPaints.length > 0
  - lines 144-148: <PaintReadinessBanner missingPaints onDismiss />
  - lines 159-180: <StepFocalView paint={currentPaint} ... />

StepFocalView current state (src/features/painting-mode/StepFocalView.tsx):
  - props interface lines 7-24: currentStep, paint, ... (no isUnfilledSlot yet)
  - line 61: const hasPaint = currentStep.paint_id !== null && paint;
  - lines 72-97: resolved-swatch block (hasPaint) ; line 96: "(no paint)" fallback

PaintReadinessBanner current state (src/features/painting-mode/PaintReadinessBanner.tsx):
  - props lines 10-13: { missingPaints, onDismiss }
  - line 19: if (missingPaints.length === 0) return null;
  - amber box mx-6 mt-4 px-4 py-3; missing-paint sentence; dismiss X button
</interfaces>

<ui_spec_excerpt>
<!-- From 145-UI-SPEC.md — copy/visual contract for the two new affordances. -->

StepFocalView unfilled-slot indicator (large, 40×40):
  h-10 w-10 rounded-full border-2 border-dashed border-muted-foreground bg-transparent
  wrapped in <button type="button"> opening the mini-dialog (onReassignSlot)
  aria-label="Colour slot unfilled. Tap to assign a paint."
  label below: text-xs text-muted-foreground "Slot unfilled — tap to assign"
  decorative circle has aria-hidden="true"

PaintReadinessBanner "N colour slots unfilled":
  rendered only when unfilledSlotCount > 0
  <Separator className="my-2" /> then a row: Circle icon (h-4 w-4 text-muted-foreground, aria-hidden) + <p>
  text-sm text-muted-foreground (neutral, NOT amber — unfilled is not an error)
  copy: N===1 → "1 colour slot unfilled" ; N>1 → "{N} colour slots unfilled"
  banner now renders when (missingPaints.length > 0 || unfilledSlotCount > 0)
  one dismiss X dismisses the whole banner

Typography: mini-dialog/section titles font-semibold (not font-medium); scale stays 400/600.
</ui_spec_excerpt>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PaintReadinessBanner — unfilledSlotCount prop + "N colour slots unfilled" line</name>
  <files>src/features/painting-mode/PaintReadinessBanner.tsx, tests/painting-mode/PaintReadinessBanner.test.tsx</files>
  <read_first>
    - src/features/painting-mode/PaintReadinessBanner.tsx (the file — full current structure)
    - tests/painting-mode/PaintReadinessBanner.test.tsx (existing tests to extend)
    - .planning/phases/145-integration-pass/145-PATTERNS.md lines 369-413, 640-686 (banner extension + test cases)
    - .planning/phases/145-integration-pass/145-UI-SPEC.md lines 128-137, 199-208, 250-251 (copy + visual contract)
  </read_first>
  <action>
    Extend PaintReadinessBannerProps with `unfilledSlotCount?: number`. Change the early-return guard
    from `if (missingPaints.length === 0) return null` to also stay rendered when unfilledSlotCount > 0:
    `if (missingPaints.length === 0 && !unfilledSlotCount) return null`. Import Circle from lucide-react
    (alongside AlertTriangle, X) and Separator from "@/components/ui/separator". After the missing-paint
    `<p>` block, render — only when `unfilledSlotCount && unfilledSlotCount > 0` — a `<Separator className="my-2" />`
    then a flex row with a `Circle` icon (h-4 w-4 text-muted-foreground, aria-hidden="true") and a
    `<p className="text-sm text-muted-foreground">` containing the singular/plural copy: "1 colour slot unfilled"
    when count === 1, else "{count} colour slots unfilled". The unfilled line is neutral (muted-foreground),
    NOT amber. Keep the single dismiss X behavior unchanged.

    Extend tests/painting-mode/PaintReadinessBanner.test.tsx with the four cases: renders "3 colour slots unfilled"
    for count 3; renders "1 colour slot unfilled" (singular) for count 1; renders nothing matching /colour slot/
    when count is 0 or omitted AND missingPaints empty; renders BOTH the missing-paint sentence and the unfilled
    line when both present.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting-mode/PaintReadinessBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "unfilledSlotCount" src/features/painting-mode/PaintReadinessBanner.tsx` >= 3 (prop, guard, render)
    - Banner imports Separator and Circle (verify: `grep -c "Separator" src/features/painting-mode/PaintReadinessBanner.tsx` >= 1 and `grep -c "Circle" src/features/painting-mode/PaintReadinessBanner.tsx` >= 1)
    - Singular/plural copy present (verify: `grep "colour slot unfilled" src/features/painting-mode/PaintReadinessBanner.tsx` and `grep "colour slots unfilled" src/features/painting-mode/PaintReadinessBanner.tsx` both match)
    - Guard updated so banner renders on unfilledSlotCount alone (verify: `grep "missingPaints.length === 0 && !unfilledSlotCount" src/features/painting-mode/PaintReadinessBanner.tsx` matches)
    - `pnpm test -- tests/painting-mode/PaintReadinessBanner.test.tsx` passes with the four new cases green
  </acceptance_criteria>
  <done>Banner accepts unfilledSlotCount, renders the neutral "N colour slots unfilled" line below a Separator, and the four test cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: StepFocalView — three-state paint block with distinct unfilled-slot indicator</name>
  <files>src/features/painting-mode/StepFocalView.tsx, tests/painting-mode/StepFocalView.test.tsx</files>
  <read_first>
    - src/features/painting-mode/StepFocalView.tsx (the file — props lines 7-24; hasPaint line 61; paint block lines 70-98)
    - tests/painting-mode/StepFocalView.test.tsx (existing tests — extend makeStep helper)
    - src/features/recipes/SlotFillRow.tsx lines 36-41 (dashed-swatch convention for unfilled)
    - .planning/phases/145-integration-pass/145-PATTERNS.md lines 308-365 (props extension + three-state block)
    - .planning/phases/145-integration-pass/145-UI-SPEC.md lines 103-126, 179-187, 247-249 (swatch states + copy + aria)
  </read_first>
  <action>
    Add two optional props to StepFocalViewProps: `isUnfilledSlot?: boolean` and `onReassignSlot?: () => void`.
    Replace `const hasPaint = currentStep.paint_id !== null && paint` with `const hasPaint = !!paint`
    (the parent now resolves paint via effectivePaintId, so the local paint_id check is wrong for technique steps).
    Change the paint block to three-state: (1) hasPaint → existing resolved-swatch block unchanged; (2) else
    isUnfilledSlot → a `<button type="button">` (onClick={onReassignSlot}, aria-label="Colour slot unfilled. Tap to assign a paint.",
    focus-visible ring) containing a dashed circle (h-10 w-10 rounded-full border-2 border-dashed border-muted-foreground
    bg-transparent, aria-hidden="true") and a `<p className="text-xs text-muted-foreground">Slot unfilled — tap to assign</p>`;
    (3) else the existing `<p className="text-sm text-muted-foreground">(no paint)</p>` fallback. Keep all
    navigation, completion, keyboard, and metadata rendering exactly as-is. The unfilled indicator must be
    visually distinct from the "(no paint)" text (dashed swatch + tap affordance).

    Extend tests/painting-mode/StepFocalView.test.tsx: add a case rendering the dashed unfilled indicator
    (assert the aria-label "Colour slot unfilled. Tap to assign a paint." is present and the "(no paint)" text
    is NOT) when isUnfilledSlot is true and paint is undefined; add a case asserting onReassignSlot fires on
    click of that button; keep the existing resolved-swatch and "(no paint)" cases green. The makeStep helper
    already produces RecipeStep (technique_step_id is optional on the type).
  </action>
  <verify>
    <automated>pnpm test -- tests/painting-mode/StepFocalView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - StepFocalViewProps includes isUnfilledSlot and onReassignSlot (verify: `grep -c "isUnfilledSlot" src/features/painting-mode/StepFocalView.tsx` >= 2)
    - hasPaint no longer reads currentStep.paint_id (verify: `grep "const hasPaint = !!paint" src/features/painting-mode/StepFocalView.tsx` matches; `grep "currentStep.paint_id" src/features/painting-mode/StepFocalView.tsx` returns no match)
    - Unfilled indicator uses border-dashed and the exact aria-label (verify: `grep "border-dashed" src/features/painting-mode/StepFocalView.tsx` and `grep "Colour slot unfilled. Tap to assign a paint." src/features/painting-mode/StepFocalView.tsx` both match)
    - "Slot unfilled — tap to assign" label present
    - `pnpm test -- tests/painting-mode/StepFocalView.test.tsx` passes including the new unfilled-indicator + onReassignSlot cases
  </acceptance_criteria>
  <done>StepFocalView renders the distinct dashed unfilled-slot indicator (with tap affordance) separate from "(no paint)"; hasPaint relies on the parent-resolved paint; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: PaintingModeView — wire slotMap into missingPaints + currentPaint + banner</name>
  <files>src/features/painting-mode/PaintingModeView.tsx</files>
  <read_first>
    - src/features/painting-mode/PaintingModeView.tsx (the file — missingPaints lines 45-57; currentPaint lines 101-104; showBanner line 111; banner render 144-148; StepFocalView render 159-180)
    - src/features/recipes/SectionedTimeline.tsx lines 56-92 (canonical effectivePaintId consumer wire-up with slotMap in deps)
    - .planning/phases/145-integration-pass/145-PATTERNS.md lines 188-303 (PaintingModeView wiring; note: mini-dialog state/handlers are Plan 04, NOT this task)
  </read_first>
  <action>
    Import effectivePaintId from "@/lib/effectivePaintId", and useSlotResolutionMap + useUnfilledSlotCount
    from "@/hooks/useSlotResolutionMap". Add hook calls after useRecipeSections:
    `const { data: slotMap = new Map() } = useSlotResolutionMap(recipeId)` and
    `const { data: unfilledSlotCount = 0 } = useUnfilledSlotCount(recipeId)`.

    In the missingPaints useMemo, replace the `if (step.paint_id == null) continue; const paint = paintMap.get(step.paint_id)`
    pair with `const resolvedId = effectivePaintId(step, slotMap); if (resolvedId == null) continue; const paint = paintMap.get(resolvedId)`.
    Add `slotMap` to the useMemo dependency array (Pitfall 1 — missing dep returns stale resolution).

    Replace the currentPaint derivation (lines 101-104) with: resolvedPaintId = currentStep !== undefined
    ? effectivePaintId(currentStep, slotMap) : null; currentPaint = resolvedPaintId !== null ? paintMap.get(resolvedPaintId) : undefined;
    isUnfilledSlot = (currentStep?.technique_step_id ?? null) !== null && resolvedPaintId === null.

    Update showBanner (line 111) to `!bannerDismissed && (missingPaints.length > 0 || unfilledSlotCount > 0)`.
    Pass `unfilledSlotCount={unfilledSlotCount}` to PaintReadinessBanner. Pass `isUnfilledSlot={isUnfilledSlot}`
    to StepFocalView (leave onReassignSlot for Plan 04 — pass nothing / omit it this task; the prop is optional).

    Do NOT add the SlotReassignMiniDialog, useStepSlotIdMap, or reassign state in this task — that is Plan 04.
    Keep step-photo resolution, sectionName, completion, and keyboard logic untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "PaintingModeView" || echo "TYPECHECK_CLEAN"; grep -Eq "step\.paint_id|currentStep\.paint_id" src/features/painting-mode/PaintingModeView.tsx && echo "FAIL_DIRECT_PAINT_ID_READ" || echo "NO_DIRECT_READS"</automated>
  </verify>
  <acceptance_criteria>
    - missingPaints resolves via effectivePaintId and lists slotMap as a dep (verify: `grep "effectivePaintId(step, slotMap)" src/features/painting-mode/PaintingModeView.tsx` matches)
    - No direct `step.paint_id`/`currentStep.paint_id` reads remain for resolution (verify: `grep -E "step\\.paint_id|currentStep\\.paint_id" src/features/painting-mode/PaintingModeView.tsx` returns no match)
    - useSlotResolutionMap and useUnfilledSlotCount are called once at component level (verify: `grep -c "useSlotResolutionMap(recipeId)" src/features/painting-mode/PaintingModeView.tsx` == 1 and `grep -c "useUnfilledSlotCount(recipeId)" src/features/painting-mode/PaintingModeView.tsx` == 1)
    - showBanner includes unfilledSlotCount (verify: `grep "unfilledSlotCount > 0" src/features/painting-mode/PaintingModeView.tsx` matches)
    - Banner receives unfilledSlotCount and StepFocalView receives isUnfilledSlot (verify both grep matches)
    - No SlotReassignMiniDialog import in this task (verify: `grep -c "SlotReassignMiniDialog" src/features/painting-mode/PaintingModeView.tsx` == 0)
    - `npx tsc --noEmit` clean; `pnpm test -- tests/painting-mode/` green (no regressions)
  </acceptance_criteria>
  <done>PaintingModeView resolves all paint via effectivePaintId+slotMap, counts unfilled slots, and feeds the banner + StepFocalView; no direct paint_id reads remain; typecheck + painting-mode tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slotMap cache → render | resolved paint ids feed availability scan + swatch; stale map = wrong swatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-04 | Information disclosure (undercount) | missingPaints useMemo | mitigate | effectivePaintId replaces step.paint_id so filled technique slots are counted; slotMap added to useMemo deps prevents stale resolution |
| T-145-05 | Tampering (wrong indicator) | StepFocalView three-state | mitigate | isUnfilledSlot derived from technique_step_id != null && resolvedPaintId === null — unfilled slot is visually distinct, never silently shown as "(no paint)" |
| T-145-06 | Repudiation (lost completion) | step completion | accept | completion still keyed on recipe_step.id (unchanged); no code path in this plan touches completion — verified by unchanged keyboard/onMarkDone wiring |
</threat_model>

<verification>
- `pnpm test -- tests/painting-mode/` all green (PaintReadinessBanner + StepFocalView + escHint + existing)
- `npx tsc --noEmit` clean for the three modified source files
- No `step.paint_id` / `currentStep.paint_id` resolution reads remain in PaintingModeView
</verification>

<success_criteria>
- INTG-01: technique steps show slot-resolved swatch or distinct dashed unfilled indicator; shortcuts/completion unchanged
- INTG-02: missingPaints counts resolve via effectivePaintId (filled technique slot counts like a plain step)
- INTG-06: "N colour slots unfilled" line renders in the readiness banner when count > 0
</success_criteria>

<output>
Create `.planning/phases/145-integration-pass/145-02-SUMMARY.md` when done
</output>

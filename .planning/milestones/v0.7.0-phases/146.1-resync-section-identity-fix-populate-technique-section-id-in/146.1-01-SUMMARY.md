---
phase: 146.1-resync-section-identity-fix
plan: 01
subsystem: data-layer / technique-resync
tags: [bug-fix, regression-test, tdd, link-01, intg-05, gap-closure]
dependency_graph:
  requires: [migration 052 (technique_section_id column already in DB)]
  provides: [technique_section_id populated by applyTechnique, duplicateRecipe carries technique_section_id, RecipeSection type declares the field]
  affects: [resyncTechniqueInstances (now UPDATEs rather than INSERTs duplicate sections), all recipe apply flows, duplication flows]
tech_stack:
  added: []
  patterns: [guard-first TDD (RED then GREEN), inline SQL with $1/$2 params]
key_files:
  created:
    - tests/data-layer/technique-resync-apply-path.test.ts
  modified:
    - src/types/recipeSection.ts
    - src/db/queries/recipeTechniqueInstances.ts
    - src/db/queries/recipes.ts
    - tests/painting/duplicateRecipe.test.ts
decisions:
  - "Fixed three write-sites (applyTechnique INSERT, duplicateRecipe INSERT, RecipeSection type) — no new migration needed, column already in 052"
  - "Guard-first regression drives real applyTechnique path — no manual UPDATE recipe_sections SET technique_section_id (which was the exact masking workaround in the original test)"
  - "Updated duplicateRecipe.test.ts param count 11→12: Rule 1 auto-fix (pre-existing test was now wrong by one column)"
metrics:
  duration: "12 minutes"
  completed: "2026-06-23"
  tasks: 2
  files: 5
---

# Phase 146.1 Plan 01: Populate technique_section_id at All Three Write-Sites Summary

Closes LINK-01 and INTG-05: adds `technique_section_id` to the `applyTechnique` and `duplicateRecipe` recipe_sections INSERTs and declares the field on the `RecipeSection` type, so `resyncTechniqueInstances` can match sections by stable identity and UPDATE them rather than INSERT duplicate sets.

## What Was Built

### Task 1 — Guard-first regression test (RED)
- Created `tests/data-layer/technique-resync-apply-path.test.ts` with `// @vitest-environment node` on line 1
- Seeds via real `applyTechnique()` — contains no `UPDATE recipe_sections SET technique_section_id` workaround
- **Assertion (a):** section count unchanged after resync — failed RED (count doubled: `expected 2 to be 1`)
- **Assertion (b):** all recipe_sections have non-NULL `technique_section_id` after apply — failed RED (`expected null not to be null`)
- **Assertion (c):** completed step progress survives resync (FND-03) — passed even before fix
- Second describe: `duplicateRecipe` sections carry `technique_section_id` and survive resync — failed RED

### Task 2 — Populate technique_section_id at all three sites (GREEN)
1. `src/types/recipeSection.ts` — added `technique_section_id?: number | null` next to `technique_instance_id`
2. `src/db/queries/recipeTechniqueInstances.ts` — `applyTechnique` recipe_sections INSERT extended to 8 columns; `section.id` added as `$8`
3. `src/db/queries/recipes.ts` — `duplicateRecipe` recipe_sections INSERT extended to 12 columns; `section.technique_section_id ?? null` added as `$12` (the "AUDIT: add new recipe_sections columns here" comment was exactly this case)
4. All 5 regression test assertions went GREEN; full suite (342 files) green; `pnpm build` TypeScript-clean

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated duplicateRecipe.test.ts param count 11→12**
- **Found during:** Task 2 full `pnpm test` run
- **Issue:** `tests/painting/duplicateRecipe.test.ts:181` asserted 11 params for the recipe_sections INSERT; after adding `technique_section_id` as $12 the test failed with `expected [null] not to equal []` (extra null in received array)
- **Fix:** Updated assertion description ("all 11 columns" → "all 12 columns"), added `technique_section_id` to `expect(sql1).toContain(...)` check, extended both param arrays with the final `null` value
- **Files modified:** `tests/painting/duplicateRecipe.test.ts`
- **Commit:** included in fix commit (7129de23)

**2. [Rule 1 - Bug] Added `order_index` to DraftTechniqueSlot in regression test**
- **Found during:** Task 2 `pnpm build` TypeScript check
- **Issue:** `DraftTechniqueSlot` interface requires `order_index: number`; the three occurrences in the new test omitted it
- **Fix:** Added `order_index: 0` to all three `draftSlot` object literals
- **Files modified:** `tests/data-layer/technique-resync-apply-path.test.ts`
- **Commit:** included in fix commit (7129de23)

## Self-Check

- `tests/data-layer/technique-resync-apply-path.test.ts` exists and starts with `// @vitest-environment node`
- Regression test contains `applyTechnique(` and does NOT contain `UPDATE recipe_sections SET technique_section_id`
- `src/types/recipeSection.ts` contains `technique_section_id`
- `applyTechnique` recipe_sections INSERT contains `technique_section_id` and passes `section.id`
- `duplicateRecipe` recipe_sections INSERT contains `technique_section_id` and passes `section.technique_section_id`
- Full suite: 342 files passed, 6 skipped, 3074 tests passed
- `pnpm build`: TypeScript-clean (chunk size warning is pre-existing, unrelated)
- RED commit: 3f49f961 | GREEN commit: 7129de23

## Self-Check: PASSED

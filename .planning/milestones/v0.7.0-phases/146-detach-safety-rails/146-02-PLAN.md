---
phase: 146-detach-safety-rails
plan: 02
type: execute
wave: 2
depends_on: ["146-01"]
files_modified:
  - src/hooks/useTechniqueDetach.ts
  - src/features/recipes/DetachConfirmDialog.tsx
  - src/features/recipes/RecipeSectionCard.tsx
  - src/features/recipes/RecipeSectionList.tsx
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03]

must_haves:
  truths:
    - "Every technique-sourced section shows a 'From {technique}' badge in the recipe editor (already rendered) AND an adjacent Unlink affordance"
    - "Clicking Unlink opens an AlertDialog that warns the link breaks permanently and reassures steps & colours are kept"
    - "Confirming detach runs the mutation, shows a success toast, and the section re-renders as plain editable content (badge + Unlink gone)"
    - "After detach, recipe / section / step-count / slot-resolution / paint query keys are invalidated so every surface refreshes"
  artifacts:
    - path: "src/hooks/useTechniqueDetach.ts"
      provides: "useDetachTechniqueInstance mutation hook with per-recipe invalidation"
      exports: ["useDetachTechniqueInstance"]
    - path: "src/features/recipes/DetachConfirmDialog.tsx"
      provides: "AlertDialog confirmation for detach (title, warning, reassurance, loading state)"
      exports: ["DetachConfirmDialog"]
    - path: "src/features/recipes/RecipeSectionCard.tsx"
      provides: "Unlink ghost icon button adjacent to the technique badge (editor only) + onDetach callback prop"
      contains: "Unlink"
    - path: "src/features/recipes/RecipeSectionList.tsx"
      provides: "Wires onDetach per technique-owned section to useDetachTechniqueInstance with the section's technique_instance_id"
      contains: "useDetachTechniqueInstance"
  key_links:
    - from: "src/features/recipes/RecipeSectionCard.tsx"
      to: "DetachConfirmDialog"
      via: "Unlink button opens dialog; confirm calls onDetach prop"
      pattern: "DetachConfirmDialog"
    - from: "src/features/recipes/RecipeSectionList.tsx"
      to: "useDetachTechniqueInstance"
      via: "mutateAsync({ instanceId, recipeId }) with section.technique_instance_id"
      pattern: "useDetachTechniqueInstance"
    - from: "src/hooks/useTechniqueDetach.ts"
      to: "detachTechniqueInstance"
      via: "mutationFn calls getDb() then detachTechniqueInstance(db, instanceId)"
      pattern: "detachTechniqueInstance"
---

<objective>
Add the editor-side detach experience: a React Query mutation hook, an AlertDialog confirmation, and an Unlink affordance on technique-owned sections in the recipe editor. The "From {technique}" badge already renders in both the editor (RecipeSectionCard) and the SectionedTimeline (verified in RESEARCH.md — no rebuild); this plan adds ONLY the editor-only Unlink button + confirmation + wiring, plus verifies the existing badge satisfies SAFE-01.

Purpose: SAFE-01 (persistent badge in both surfaces) and SAFE-02/SAFE-03 (user-triggered detach with confirmation) for the per-recipe editor path.
Output: `src/hooks/useTechniqueDetach.ts`, `src/features/recipes/DetachConfirmDialog.tsx`, modified `RecipeSectionCard.tsx` + `RecipeSectionList.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-detach-safety-rails/146-CONTEXT.md
@.planning/phases/146-detach-safety-rails/146-RESEARCH.md
@.planning/phases/146-detach-safety-rails/146-PATTERNS.md
@.planning/phases/146-detach-safety-rails/146-UI-SPEC.md
@.planning/phases/146-detach-safety-rails/146-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase + Plan 01 output. -->

From Plan 01 — src/db/queries/recipeTechniqueDetach.ts:
- `export async function detachTechniqueInstance(db: DbHandle, instanceId: number): Promise<void>`
  (DbHandle = Awaited<ReturnType<typeof getDb>>). NEVER calls getDb() internally.

Mutation-hook analog — src/hooks/useTechniqueInstances.ts:
- `useApplyTechnique()` shape (lines 92-101): `useMutation<R, Error, Input>({ mutationFn, onSuccess: (_, variables) => invalidate(qc, variables.recipeId) })`.
- `invalidateAfterApply(qc, recipeId)` (lines 39-47) invalidates 7 keys:
  TECHNIQUE_INSTANCES_KEY(recipeId), RECIPE_SECTIONS_KEY(recipeId), RECIPE_PAINTS_KEY(recipeId),
  STEP_COUNTS_KEY, RECIPE_AVAILABILITY_KEY, RECIPE_SWATCH_KEY, SLOT_RESOLUTION_MAP_KEY(recipeId).
- Key factories to import:
  - `TECHNIQUE_INSTANCES_KEY` from @/hooks/useTechniqueInstances
  - `RECIPE_SECTIONS_KEY` from @/hooks/useRecipeSections
  - `RECIPE_PAINTS_KEY, RECIPE_AVAILABILITY_KEY, RECIPE_SWATCH_KEY, STEP_COUNTS_KEY` from @/hooks/useRecipePaints
  - `SLOT_RESOLUTION_MAP_KEY` from @/hooks/useSlotResolutionMap
  - `TECHNIQUES_WITH_COUNTS_KEY, TECHNIQUE_USAGE_COUNTS_KEY` from @/hooks/useTechniques

AlertDialog analog — src/features/recipes/RecipeSectionCard.tsx (lines 7-15 imports already present; lines 278-293 inline AlertDialog primitive shape):
- AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle from @/components/ui/alert-dialog.
- AlertDialogAction takes NO `variant` prop — use `className="bg-destructive text-destructive-foreground hover:bg-destructive/90"` (shadcn new-york).

RecipeSectionCard existing facts:
- Props (lines 32-43): `section, onChange, onRemove, onCreateNewPaint, sectionsCount, techniqueName?`. `isTechniqueOwned = techniqueName !== undefined` (line 59).
- Badge already rendered at lines 110-113: `{isTechniqueOwned && techniqueName && <TechniqueSectionBadge techniqueName={techniqueName} />}`.
- Existing ghost icon button pattern (h-7 w-7): collapse trigger lines 151-157, delete button lines 160-170.
- Existing `useState` for confirmOpen (line 65) — add `detachOpen` the same way.
- `Unlink` icon from lucide-react (add to existing import line 2: GripVertical, Trash2, ChevronDown, ChevronRight).

RecipeSectionList existing facts (lines 168-190):
- `renderCards(nameMap: Map<number, string>)` maps sections to RecipeSectionCard; already passes `techniqueName` derived from `section.technique_instance_id` via nameMap.
- `section.technique_instance_id` (number | null) is available on DraftSection — this is the instanceId to detach.
- `recipeId` is in scope (line 195). nameMap resolves instance_id → technique name.

UI-SPEC copy (LOCKED):
- AlertDialog title: "Detach technique?"
- Warning: "This breaks the live link permanently. Future edits to \"{techniqueName}\" won't update this recipe."
- Reassurance (new paragraph): "Your current steps and colours are kept."
- Cancel button: "Keep link" (variant outline)
- Confirm button: "Detach" (destructive className); loading label "Detaching…", disabled while pending.
- Unlink button: ghost size icon, `h-7 w-7 text-muted-foreground hover:text-foreground`, `aria-label="Detach technique from this section"`, icon `<Unlink className="h-4 w-4" />`.
- Success toast: "Technique detached — now plain recipe content"
- Error toast: "Failed to detach technique. Please try again."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useDetachTechniqueInstance hook + DetachConfirmDialog</name>
  <files>src/hooks/useTechniqueDetach.ts, src/features/recipes/DetachConfirmDialog.tsx</files>
  <read_first>
    - src/hooks/useTechniqueInstances.ts (useApplyTechnique shape lines 92-101, invalidateAfterApply 7-key set lines 39-47, key factory imports)
    - src/hooks/useTechniques.ts (TECHNIQUES_WITH_COUNTS_KEY, TECHNIQUE_USAGE_COUNTS_KEY exports)
    - src/features/recipes/RecipeSectionCard.tsx (AlertDialog import block lines 7-15, inline AlertDialog primitive lines 278-293 — exact structure to mirror)
    - .planning/phases/146-detach-safety-rails/146-UI-SPEC.md (copy contract, loading-state rules)
    - src/db/queries/recipeTechniqueDetach.ts (detachTechniqueInstance signature from Plan 01)
  </read_first>
  <action>
    Create `src/hooks/useTechniqueDetach.ts`. Import `useMutation, useQueryClient`, plus `QueryClient` type from @tanstack/react-query; `getDb` from @/db/client; `detachTechniqueInstance` from @/db/queries/recipeTechniqueDetach; and the key factories listed in the interfaces block. Declare `interface DetachTechniqueInput { instanceId: number; recipeId: number }`. Add a local `invalidateAfterDetach(qc: QueryClient, recipeId: number)` that invalidates the SAME 7 per-recipe keys as `invalidateAfterApply` PLUS the two technique-level keys `TECHNIQUES_WITH_COUNTS_KEY` and `TECHNIQUE_USAGE_COUNTS_KEY` (usage count drops when the last instance is detached). Export `useDetachTechniqueInstance()` returning `useMutation<void, Error, DetachTechniqueInput>` whose `mutationFn` does `const db = await getDb(); return detachTechniqueInstance(db, instanceId);` and whose `onSuccess: (_, variables) => invalidateAfterDetach(qc, variables.recipeId)`.

    Create `src/features/recipes/DetachConfirmDialog.tsx`. Import the AlertDialog sub-parts (same import set as RecipeSectionCard lines 7-15) and Button. Export `interface DetachConfirmDialogProps { open: boolean; techniqueName: string; isPending: boolean; onCancel: () => void; onConfirm: () => void }` and `function DetachConfirmDialog(...)`. Render `<AlertDialog open onOpenChange={(o) => { if (!o) onCancel(); }}>` with title "Detach technique?", a description containing the LOCKED warning line (with `{techniqueName}` interpolated) followed by a blank line and the reassurance "Your current steps and colours are kept.", an `AlertDialogCancel` labelled "Keep link" calling `onCancel`, and an `AlertDialogAction` calling `onConfirm`, `disabled={isPending}`, `className="bg-destructive text-destructive-foreground hover:bg-destructive/90"`, label `{isPending ? "Detaching…" : "Detach"}`. Use `&quot;` / `&apos;` for embedded quotes/apostrophes to satisfy strict TS/JSX.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useTechniqueDetach.ts` exports `useDetachTechniqueInstance`; its `invalidateAfterDetach` invalidates exactly the 7 per-recipe keys from `invalidateAfterApply` plus `TECHNIQUES_WITH_COUNTS_KEY` and `TECHNIQUE_USAGE_COUNTS_KEY` (9 total).
    - `mutationFn` calls `getDb()` then `detachTechniqueInstance(db, instanceId)` — does not pass a raw recipeId to the data layer.
    - `DetachConfirmDialog.tsx` exports `DetachConfirmDialog` and renders title "Detach technique?", the locked warning + reassurance copy, "Keep link" cancel, and a confirm button with `disabled={isPending}` and label toggling "Detaching…"/"Detach".
    - `pnpm build` typechecks clean (no unused imports, no `any`).
  </acceptance_criteria>
  <done>The detach mutation hook and confirmation dialog exist, follow the apply-hook invalidation contract, and match the locked UI-SPEC copy.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Unlink affordance into RecipeSectionCard + RecipeSectionList</name>
  <files>src/features/recipes/RecipeSectionCard.tsx, src/features/recipes/RecipeSectionList.tsx</files>
  <read_first>
    - src/features/recipes/RecipeSectionCard.tsx (full file — props lines 32-43, badge lines 110-113, ghost-icon-button pattern lines 151-170, confirmOpen state line 65)
    - src/features/recipes/RecipeSectionList.tsx (renderCards lines 168-190, section.technique_instance_id + recipeId + nameMap context)
    - src/features/recipes/DetachConfirmDialog.tsx (props contract from Task 1)
    - src/hooks/useTechniqueDetach.ts (useDetachTechniqueInstance from Task 1)
    - .planning/phases/146-detach-safety-rails/146-UI-SPEC.md (Surface 1 editor-only rule; Unlink button styling, aria-label, toast copy)
  </read_first>
  <action>
    In `RecipeSectionCard.tsx`: add `Unlink` to the lucide-react import; import `DetachConfirmDialog` from "./DetachConfirmDialog". Add prop `onDetach?: () => void` to `RecipeSectionCardProps` (keeps the card mutation-free per RESEARCH.md Option 2). Add `isPendingDetach?: boolean` prop so the dialog can show the loading state. Add `const [detachOpen, setDetachOpen] = useState(false);` (mirroring confirmOpen). Immediately AFTER the existing `<TechniqueSectionBadge />` block (lines 110-113), render — gated on `isTechniqueOwned && techniqueName && onDetach` — a ghost size-icon Button (`h-7 w-7 text-muted-foreground hover:text-foreground`, `aria-label="Detach technique from this section"`, `onClick={() => setDetachOpen(true)}`) containing `<Unlink className="h-4 w-4" />`. At the bottom of the component (alongside the existing delete AlertDialog) render `<DetachConfirmDialog open={detachOpen} techniqueName={techniqueName ?? ""} isPending={isPendingDetach ?? false} onCancel={() => setDetachOpen(false)} onConfirm={() => { setDetachOpen(false); onDetach?.(); }} />`. Do NOT render the Unlink button or dialog when `onDetach` is undefined — this keeps SectionedTimeline / read-only hosts unaffected (editor-only per SAFE-01 / UI-SPEC Surface 2).

    In `RecipeSectionList.tsx`: call `const detach = useDetachTechniqueInstance();` (import from @/hooks/useTechniqueDetach) and `const { toast }` import from "sonner". In `renderCards`, for each section pass `onDetach` ONLY when `recipeId !== undefined && section.technique_instance_id != null` — the handler captures `section.technique_instance_id` and runs `await detach.mutateAsync({ instanceId, recipeId })` inside try/catch, `toast.success("Technique detached — now plain recipe content")` on success and `toast.error("Failed to detach technique. Please try again.")` on failure. Also pass `isPendingDetach={detach.isPending}`. Leave `onDetach` undefined for non-technique sections (so the affordance never appears on plain sections).
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `RecipeSectionCard.tsx` renders the Unlink button only when `isTechniqueOwned && techniqueName && onDetach` are all truthy; the button carries `aria-label="Detach technique from this section"` and opens `DetachConfirmDialog`.
    - The card holds NO React Query / mutation hook — detach is driven through the `onDetach` callback prop (mutation-free card preserved).
    - `RecipeSectionList.tsx` passes `onDetach` only for technique-owned sections with a non-null `technique_instance_id` and a defined `recipeId`; the handler calls `useDetachTechniqueInstance().mutateAsync({ instanceId, recipeId })` and fires the locked success/error toasts.
    - SectionedTimeline (read-only) is unmodified and shows no Unlink button (editor-only — grep confirms no DetachConfirmDialog/onDetach added to SectionedTimeline.tsx).
    - `pnpm test` full suite GREEN; `pnpm build` typechecks clean.
  </acceptance_criteria>
  <done>Technique-owned sections in the editor show the badge + an Unlink button that opens the confirmation and runs the detach mutation with toast feedback; read-only surfaces are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click (editor) → React Query mutation → local SQLite | A user-initiated, confirmed destructive action. No network/auth/untrusted input. Threat surface is data-integrity (accidental/unconfirmed detach, stale UI after detach), not web threats. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-05 | Tampering (accidental data loss) | Unlink trigger | mitigate | Detach is gated behind an AlertDialog with explicit "Detach" confirm + "Keep link" cancel; the destructive action cannot fire on a single mis-click. Radix AlertDialog provides focus trap + Escape-to-cancel. |
| T-146-06 | Repudiation / stale-state integrity | post-detach cache | mitigate | onSuccess invalidates the full 7 per-recipe keys + 2 technique-level keys so no surface shows a stale "From technique" badge on a now-plain section (Pitfall 4). |
| T-146-07 | Elevation / privilege | — | accept | Not applicable: single-user local app, no roles, no auth boundary. |
</threat_model>

<verification>
- `pnpm test` full suite GREEN; `pnpm build` clean.
- Manual (UAT, see VALIDATION.md): in `pnpm tauri dev`, apply a technique, click Unlink, confirm dialog copy, verify toast and that the section becomes plain editable content.
- Grep confirms SectionedTimeline.tsx unchanged (editor-only affordance).
</verification>

<success_criteria>
- Editor technique sections show the badge (SAFE-01, existing) + an Unlink affordance.
- Unlink opens the locked-copy AlertDialog; confirm runs detach + toast + full invalidation (SAFE-02, SAFE-03).
- Read-only SectionedTimeline keeps the badge, no detach trigger (SAFE-01 both surfaces).
</success_criteria>

<output>
Create `.planning/phases/146-detach-safety-rails/146-02-SUMMARY.md` when done.
</output>

---
phase: 64-applied-recipe-integrations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/recipeAssignment.ts
  - src/hooks/useKanbanEnrichment.ts
  - src/features/painting-projects/KanbanCard.tsx
  - src/features/painting-projects/KanbanColumn.tsx
autonomous: true
requirements: [AR-06]

must_haves:
  truths:
    - "D-06: Kanban cards show applied recipe progress fraction when a unit has at least one applied recipe assignment"
    - "D-05: Kanban cards fall back to workflowPosition display when no applied recipe assignment exists"
    - "D-08: Multiple assignments show the most recently created assignment with a +N more suffix"
    - "D-07: painting_percentage progress bar on Kanban cards remains unchanged"
    - "D-09: +N more suffix matches existing extraRecipeCount pattern"
    - "D-11: KanbanBoard receives progress via parent enrichment hook, not per-card hooks"
  artifacts:
    - path: "src/types/recipeAssignment.ts"
      provides: "AppliedRecipeProgress interface"
      contains: "AppliedRecipeProgress"
    - path: "src/hooks/useKanbanEnrichment.ts"
      provides: "Batch assignment progress fetch for all Kanban units"
      contains: "appliedProgress"
    - path: "src/features/painting-projects/KanbanCard.tsx"
      provides: "Applied recipe progress display superseding workflowPosition"
      contains: "appliedProgress"
    - path: "src/features/painting-projects/KanbanColumn.tsx"
      provides: "Pass-through of appliedProgress from enrichment to KanbanCard"
      contains: "appliedProgress"
  key_links:
    - from: "src/hooks/useKanbanEnrichment.ts"
      to: "src/db/queries/recipeAssignments.ts"
      via: "getAssignmentsByUnit, getStepProgress direct query calls"
      pattern: "getAssignmentsByUnit|getStepProgress"
    - from: "src/hooks/useKanbanEnrichment.ts"
      to: "src/lib/computeAssignmentProgress.ts"
      via: "computeAssignmentProgress call for each unit"
      pattern: "computeAssignmentProgress"
    - from: "src/features/painting-projects/KanbanColumn.tsx"
      to: "src/features/painting-projects/KanbanCard.tsx"
      via: "appliedProgress prop from enrichment map"
      pattern: "appliedProgress.*enrichment"
---

<objective>
Add applied recipe progress display to Kanban cards. Extend the batch enrichment hook to fetch assignment progress for all active units, and render completion fractions on cards that supersede the existing workflowPosition display.

Purpose: Users see concrete painting progress ("8/12 steps") on their Kanban board instead of vague workflow position hints, giving immediate visibility into how far each unit's recipe has progressed.
Output: Modified type file, enrichment hook, KanbanCard, and KanbanColumn.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-applied-recipe-integrations/64-CONTEXT.md
@.planning/phases/64-applied-recipe-integrations/64-RESEARCH.md
@.planning/phases/64-applied-recipe-integrations/64-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/recipeAssignment.ts (current state):
- RecipeAssignment: { id: number; unit_id: number; recipe_id: number; created_at: string }
- StepProgress: { id: number; assignment_id: number; order_index: number; completed: number; completed_at: string | null }
- AssignmentProgress: { total: number; completed: number; percentage: number; bySectionId: Map<number | null, { total: number; completed: number }> }

From src/hooks/useKanbanEnrichment.ts (current state):
- KanbanEnrichment: { recipeNames: Map<number, string>; photoCounts: Map<number, number> }
- KANBAN_ENRICHMENT_KEY(unitIds: number[]) = ["kanban-enrichment", ...unitIds]
- queryFn uses Promise.all([getRecipeNamesByUnitIds, getPhotoCountsByUnitIds])

From src/db/queries/recipeAssignments.ts:
- getAssignmentsByUnit(unitId: number): Promise<RecipeAssignment[]> — ORDER BY created_at ASC
- getStepProgress(assignmentId: number): Promise<StepProgress[]>

From src/db/queries/recipePaints.ts:
- getRecipePaintsByRecipe(recipeId: number): Promise<RecipeStep[]>

From src/db/queries/recipes.ts:
- getRecipeById(id: number): Promise<PaintingRecipe | null> — PaintingRecipe has .name field

From src/lib/computeAssignmentProgress.ts:
- computeAssignmentProgress(steps: { order_index; section_id }[], progress: { order_index; completed }[]): AssignmentProgress

From src/features/painting-projects/KanbanCard.tsx (current props):
- KanbanCardProps: { unit, faction, onRemoveFromBoard, onEditUnit, onLogSession, recipeName?, photoCount?, workflowPosition? }
- workflowPosition block at lines 112-129: three-way conditional (workflowPosition / getNextActionHint / null)

From src/features/painting-projects/KanbanColumn.tsx (current props):
- KanbanColumnProps: { status, units, factionMap, onRemoveFromBoard, onEditUnit, onLogSession, enrichment?, workflowPositions? }
- Pass-through at lines 62-65: recipeName from enrichment.recipeNames, photoCount from enrichment.photoCounts, workflowPosition from workflowPositions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AppliedRecipeProgress type and extend useKanbanEnrichment</name>
  <files>src/types/recipeAssignment.ts, src/hooks/useKanbanEnrichment.ts</files>
  <read_first>
    src/types/recipeAssignment.ts
    src/hooks/useKanbanEnrichment.ts
    src/db/queries/recipeAssignments.ts
    src/db/queries/recipePaints.ts
    src/lib/computeAssignmentProgress.ts
    src/db/queries/recipes.ts
  </read_first>
  <action>
    **1. Append AppliedRecipeProgress to recipeAssignment.ts** (after the existing AssignmentProgress interface, ~line 38):
    - Interface AppliedRecipeProgress with four fields: recipeName (string), completed (number), total (number), assignmentCount (number).
    - Export it. Add a JSDoc comment noting it is a derived display type for Kanban/CurrentFocusCard, computed from useAssignmentsByUnit + useStepProgress + computeAssignmentProgress.

    **2. Extend KanbanEnrichment interface in useKanbanEnrichment.ts:**
    - Add appliedProgress: Map<number, AppliedRecipeProgress> to the KanbanEnrichment interface.
    - Import AppliedRecipeProgress from "@/types/recipeAssignment".
    - Import getAssignmentsByUnit and getStepProgress from "@/db/queries/recipeAssignments".
    - Import getRecipePaintsByRecipe from "@/db/queries/recipePaints".
    - Import getRecipeById from "@/db/queries/recipes".
    - Import computeAssignmentProgress from "@/lib/computeAssignmentProgress".

    **3. Extend queryFn in useKanbanEnrichment:**
    After the existing Promise.all that fetches recipeRows and photoRows, add a second Promise.all loop over sortedIds to build an appliedProgressMap (Map<number, AppliedRecipeProgress>):
    - For each unitId: call getAssignmentsByUnit(unitId).
    - If assignments.length === 0, skip (no entry in the map).
    - Pick primary assignment as assignments[assignments.length - 1] (most recently created per D-08; getAssignmentsByUnit orders by created_at ASC).
    - Fetch steps and progress in parallel: Promise.all([getRecipePaintsByRecipe(primary.recipe_id), getStepProgress(primary.id)]).
    - Call computeAssignmentProgress(steps, progressRows) for the summary.
    - For recipe name: call getRecipeById(primary.recipe_id) and use result?.name ?? "". This is more correct than using recipeRows (which maps unit_id to the unit's own recipe, not the assigned recipe -- addresses A3 from RESEARCH.md).
    - Set appliedProgressMap entry: { recipeName, completed: progress.completed, total: progress.total, assignmentCount: assignments.length }.
    - Return the extended KanbanEnrichment with appliedProgress: appliedProgressMap alongside existing recipeNames and photoCounts.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/types/recipeAssignment.ts exports an AppliedRecipeProgress interface with fields: recipeName (string), completed (number), total (number), assignmentCount (number)
    - KanbanEnrichment interface has an appliedProgress field typed as Map<number, AppliedRecipeProgress>
    - useKanbanEnrichment queryFn calls getAssignmentsByUnit for each sortedId inside a Promise.all loop
    - Primary assignment is selected as assignments[assignments.length - 1] (last item = most recently created)
    - Recipe name is fetched via getRecipeById(primary.recipe_id), NOT from recipeRows
    - computeAssignmentProgress is called with the fetched steps and progress rows
    - The returned KanbanEnrichment object includes the appliedProgress map
    - pnpm build completes with zero TypeScript errors
  </acceptance_criteria>
  <done>AppliedRecipeProgress type exists and useKanbanEnrichment returns per-unit applied recipe progress data alongside existing recipe names and photo counts.</done>
</task>

<task type="auto">
  <name>Task 2: Display applied recipe progress on KanbanCard with KanbanColumn pass-through</name>
  <files>src/features/painting-projects/KanbanCard.tsx, src/features/painting-projects/KanbanColumn.tsx</files>
  <read_first>
    src/features/painting-projects/KanbanCard.tsx
    src/features/painting-projects/KanbanColumn.tsx
    src/types/recipeAssignment.ts
  </read_first>
  <action>
    **1. KanbanCard.tsx — Add appliedProgress prop and render logic:**
    - Import AppliedRecipeProgress from "@/types/recipeAssignment".
    - Add appliedProgress?: AppliedRecipeProgress | null to KanbanCardProps (after workflowPosition).
    - Destructure appliedProgress in the component function.
    - Replace the workflowPosition block (lines 112-129) with a three-way conditional per D-05/D-06:
      a. If appliedProgress is truthy: render a non-italic paragraph with className "mt-1 truncate text-xs text-muted-foreground/70" showing "{recipeName}: {completed}/{total} steps". If assignmentCount > 1, append " (+{assignmentCount - 1} more)".
      b. Else if workflowPosition is truthy: render the existing workflowPosition block UNCHANGED (keep the italic styling, the arrow, and the full conditional).
      c. Else if unit.status_painting !== "Completed": render the existing getNextActionHint block UNCHANGED.
      d. Else null.
    - The painting_percentage progress bar above this block remains untouched (D-07).

    **2. KanbanColumn.tsx — Pass-through appliedProgress from enrichment:**
    - In the KanbanCard render loop (lines 62-65 area), add a new prop: appliedProgress={enrichment?.appliedProgress?.get(u.id)}.
    - No changes to KanbanColumnProps needed -- appliedProgress comes from the enrichment object which is already a prop of type KanbanEnrichment.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - KanbanCardProps includes appliedProgress?: AppliedRecipeProgress | null
    - KanbanCard renders "{recipeName}: {completed}/{total} steps" when appliedProgress is truthy
    - KanbanCard renders "(+N more)" suffix when appliedProgress.assignmentCount > 1
    - KanbanCard falls back to workflowPosition block when appliedProgress is null/undefined
    - KanbanCard falls back to getNextActionHint when both appliedProgress and workflowPosition are absent and unit is not Completed
    - KanbanColumn passes enrichment?.appliedProgress?.get(u.id) as the appliedProgress prop to KanbanCard
    - The painting_percentage progress bar is not modified
    - pnpm build completes with zero TypeScript errors
  </acceptance_criteria>
  <done>Kanban cards show applied recipe progress fraction when assignment data exists, falling back to workflowPosition or next action hint when absent. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite -> UI | Assignment data flows from local SQLite through query layer to card display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-03 | I (Info Disclosure) | KanbanEnrichment | accept | All data is local SQLite owned by the user; no cross-user boundaries |
| T-64-04 | D (DoS) | Batch assignment fetch | accept | Sequential per-unit queries bounded by Kanban card count (typically 5-20); local SQLite is fast |
</threat_model>

<verification>
pnpm build passes with zero errors.
Manual: Open the Painting Projects Kanban board with units that have applied recipe assignments. Verify cards show "RecipeName: X/Y steps" instead of workflow position hints.
</verification>

<success_criteria>
Kanban cards display applied recipe progress fractions when assignments exist, with proper fallback to workflowPosition for units without assignments. The enrichment hook batch-fetches progress data efficiently.
</success_criteria>

<output>
After completion, create `.planning/phases/64-applied-recipe-integrations/64-02-SUMMARY.md`
</output>

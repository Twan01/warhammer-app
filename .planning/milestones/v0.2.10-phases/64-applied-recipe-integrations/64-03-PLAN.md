---
phase: 64-applied-recipe-integrations
plan: 03
type: execute
wave: 2
depends_on: [64-02]
files_modified:
  - src/features/dashboard/DashboardPage.tsx
  - src/features/dashboard/CurrentFocusCard.tsx
autonomous: true
requirements: [AR-06]

must_haves:
  truths:
    - "D-06: CurrentFocusCard shows applied recipe progress fraction when the focused unit has an applied recipe assignment"
    - "D-05: CurrentFocusCard falls back to workflowPosition display when no applied recipe assignment exists"
    - "D-10: DashboardPage fetches assignment data for the focused unit and passes progress to CurrentFocusCard"
    - "D-08: Multiple assignments show the most recently created one with +N more suffix"
    - "D-07: painting_percentage progress bar on CurrentFocusCard remains unchanged"
    - "D-09: +N more suffix extends existing recipeName + extraRecipeCount prop pattern"
  artifacts:
    - path: "src/features/dashboard/CurrentFocusCard.tsx"
      provides: "Applied recipe progress display superseding workflowPosition"
      contains: "appliedProgress"
    - path: "src/features/dashboard/DashboardPage.tsx"
      provides: "Applied recipe progress computation for focused unit"
      contains: "focusAppliedProgress"
  key_links:
    - from: "src/features/dashboard/DashboardPage.tsx"
      to: "src/hooks/useRecipeAssignments.ts"
      via: "useAssignmentsByUnit and useStepProgress for focused unit"
      pattern: "useAssignmentsByUnit|useStepProgress"
    - from: "src/features/dashboard/DashboardPage.tsx"
      to: "src/features/dashboard/CurrentFocusCard.tsx"
      via: "appliedProgress prop"
      pattern: "appliedProgress.*focusAppliedProgress"
---

<objective>
Wire applied recipe progress into the Dashboard's CurrentFocusCard so the focused unit shows concrete step completion instead of vague workflow position hints.

Purpose: The dashboard is the app's primary landing page. Showing "Ultramarine Blue 8/12 steps" on the focused unit gives immediate visibility into painting progress without navigating to the unit detail.
Output: Modified DashboardPage.tsx and CurrentFocusCard.tsx.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-applied-recipe-integrations/64-CONTEXT.md
@.planning/phases/64-applied-recipe-integrations/64-RESEARCH.md
@.planning/phases/64-applied-recipe-integrations/64-PATTERNS.md
@.planning/phases/64-applied-recipe-integrations/64-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/recipeAssignment.ts (after Plan 02):
- AppliedRecipeProgress: { recipeName: string; completed: number; total: number; assignmentCount: number }
- RecipeAssignment: { id: number; unit_id: number; recipe_id: number; created_at: string }

From src/hooks/useRecipeAssignments.ts:
- useAssignmentsByUnit(unitId: number | undefined) — returns { data: RecipeAssignment[] }
- useStepProgress(assignmentId: number | undefined) — returns { data: StepProgress[] }

From src/lib/computeAssignmentProgress.ts:
- computeAssignmentProgress(steps: { order_index; section_id }[], progress: { order_index; completed }[]): AssignmentProgress

From src/hooks/useRecipePaints.ts:
- useRecipePaints(recipeId: number | undefined) — returns { data: RecipeStep[] }

From src/db/queries/recipes.ts:
- getRecipeById(id: number): Promise<PaintingRecipe | null> — PaintingRecipe.name: string

From src/features/dashboard/DashboardPage.tsx (existing patterns):
- focusUnitId = stats?.activeProjects?.[0]?.id ?? null (line 83)
- focusRecipes: useQuery for getRecipeNamesByUnitIds([focusUnitId!]) — returns { id, unit_id, name }[]
- focusWorkflowPositions: useWorkflowPositions([focusUnitId])
- CurrentFocusCard receives: recipeName={focusRecipes?.[0]?.name}, extraRecipeCount={...}, workflowPosition={...}

From src/features/dashboard/CurrentFocusCard.tsx (existing props):
- CurrentFocusCardProps: { unit, faction, photo, onOpen, onLog, recipeName?, extraRecipeCount?, workflowPosition? }
- recipeName renders with Palette icon (lines 69-73)
- workflowPosition renders with Layers icon (lines 74-92)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch applied recipe progress for focused unit in DashboardPage</name>
  <files>src/features/dashboard/DashboardPage.tsx</files>
  <read_first>
    src/features/dashboard/DashboardPage.tsx
    src/hooks/useRecipeAssignments.ts
    src/hooks/useRecipePaints.ts
    src/lib/computeAssignmentProgress.ts
    src/types/recipeAssignment.ts
  </read_first>
  <action>
    Add applied recipe progress computation for the focused unit per D-10.

    **New imports:**
    - useAssignmentsByUnit, useStepProgress from "@/hooks/useRecipeAssignments"
    - useRecipePaints from "@/hooks/useRecipePaints" (may already be imported -- check first)
    - computeAssignmentProgress from "@/lib/computeAssignmentProgress"
    - AppliedRecipeProgress from "@/types/recipeAssignment" (type import)
    - getRecipeById from "@/db/queries/recipes" (may already be available via existing recipe import)
    - useMemo from "react" (if not already imported)
    - useQuery from "@tanstack/react-query" (already imported)

    **New hooks** (add after the existing focusWorkflowPositions block, ~line 91):
    1. useAssignmentsByUnit(focusUnitId ?? undefined) -- returns focusAssignments array
    2. Derive primaryAssignment: if focusAssignments.length > 0, pick the last item (most recently created per D-08, getAssignmentsByUnit orders by created_at ASC). Otherwise undefined.
    3. useStepProgress(primaryAssignment?.id) -- returns focusStepProgress array. Disabled when no primary assignment.
    4. useRecipePaints(primaryAssignment?.recipe_id) -- returns focusRecipeSteps for computing total. Disabled when no primary assignment.
    5. For recipe name: add a useQuery that calls getRecipeById(primaryAssignment.recipe_id) when primaryAssignment exists. Extract the name field. Use queryKey ["recipes", primaryAssignment?.recipe_id] and enabled: primaryAssignment !== undefined.

    **Derive focusAppliedProgress** via useMemo:
    - If no primaryAssignment or focusRecipeSteps is empty, return null.
    - Call computeAssignmentProgress(focusRecipeSteps, focusStepProgress).
    - Build AppliedRecipeProgress: { recipeName: from getRecipeById result name, completed: progress.completed, total: progress.total, assignmentCount: focusAssignments.length }.

    **Pass to CurrentFocusCard** (in the JSX, add prop after the existing workflowPosition prop):
    - appliedProgress={focusAppliedProgress}
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage imports useAssignmentsByUnit, useStepProgress from "@/hooks/useRecipeAssignments"
    - DashboardPage calls useAssignmentsByUnit(focusUnitId ?? undefined) at component level
    - primaryAssignment is derived as the last item in the focusAssignments array
    - useStepProgress is called with primaryAssignment?.id (undefined disables the query)
    - useRecipePaints is called with primaryAssignment?.recipe_id
    - Recipe name is fetched via getRecipeById (or equivalent useQuery), not from focusRecipes
    - focusAppliedProgress is computed via useMemo calling computeAssignmentProgress
    - CurrentFocusCard receives appliedProgress={focusAppliedProgress} prop
    - pnpm build completes with zero TypeScript errors
  </acceptance_criteria>
  <done>DashboardPage fetches and computes applied recipe progress for the focused unit, passing it to CurrentFocusCard as a prop.</done>
</task>

<task type="auto">
  <name>Task 2: Display applied recipe progress on CurrentFocusCard</name>
  <files>src/features/dashboard/CurrentFocusCard.tsx</files>
  <read_first>
    src/features/dashboard/CurrentFocusCard.tsx
    src/types/recipeAssignment.ts
  </read_first>
  <action>
    Add applied recipe progress display to CurrentFocusCard per D-05, D-06, D-07, D-09.

    **New import:** Import AppliedRecipeProgress from "@/types/recipeAssignment" as a type import.

    **Extend CurrentFocusCardProps:** Add appliedProgress?: AppliedRecipeProgress | null after the existing workflowPosition prop.

    **Destructure** appliedProgress in the component function alongside existing props.

    **Modify the render logic** in the metadata area (below the recipeName block):
    Replace the existing workflowPosition conditional span (lines ~74-92) with:
    a. If appliedProgress is truthy: render a span with className "flex items-center gap-1 truncate text-xs text-muted-foreground" containing:
       - Layers icon (12px, aria-hidden, shrink-0) -- same as existing workflowPosition block
       - Text: "{recipeName}: {completed}/{total} steps"
       - If assignmentCount > 1, append " (+{assignmentCount - 1} more)" -- this extends the existing extraRecipeCount pattern to the progress line (D-09)
    b. Else if workflowPosition is truthy: render the EXISTING workflowPosition span UNCHANGED
    c. Else: null

    The recipeName + Palette icon block above this remains UNCHANGED. The painting_percentage progress bar remains UNCHANGED (D-07). The appliedProgress display is an alternative to workflowPosition, not an addition.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - CurrentFocusCardProps includes appliedProgress?: AppliedRecipeProgress | null
    - CurrentFocusCard renders "{recipeName}: {completed}/{total} steps" when appliedProgress is truthy
    - The Layers icon (12px) is used for the appliedProgress line, matching the existing workflowPosition visual
    - "+N more" suffix renders when appliedProgress.assignmentCount > 1
    - workflowPosition block renders only when appliedProgress is null/undefined (superseding per D-05)
    - The recipeName + Palette icon block above is not modified
    - The painting_percentage progress bar is not modified
    - pnpm build completes with zero TypeScript errors
  </acceptance_criteria>
  <done>CurrentFocusCard shows applied recipe progress fraction when assignment data exists, falling back to workflowPosition when absent. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite -> UI | Assignment data flows from local SQLite through hooks to dashboard display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-05 | I (Info Disclosure) | DashboardPage focus queries | accept | All data is local SQLite owned by the user; no cross-user boundaries |
</threat_model>

<verification>
pnpm build passes with zero errors.
Manual: Open the Dashboard with a focused unit that has an applied recipe assignment. Verify the CurrentFocusCard shows "RecipeName: X/Y steps" instead of the workflow position hint.
</verification>

<success_criteria>
CurrentFocusCard displays applied recipe progress fractions when assignments exist for the focused unit, with proper fallback to workflowPosition for units without assignments.
</success_criteria>

<output>
After completion, create `.planning/phases/64-applied-recipe-integrations/64-03-SUMMARY.md`
</output>

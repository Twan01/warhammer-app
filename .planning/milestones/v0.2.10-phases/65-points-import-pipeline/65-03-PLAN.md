---
phase: 65-points-import-pipeline
plan: 03
type: execute
wave: 3
depends_on: [65-02]
files_modified:
  - src/features/army-lists/PointsFreshnessBadge.tsx
  - src/features/army-lists/ArmyListCard.tsx
  - src/features/army-lists/ArmyListDetailSheet.tsx
  - src/features/rules-hub/PointsDeltaSection.tsx
  - src/features/rules-hub/SyncStatusCard.tsx
  - src/features/rules-hub/RulesHubPage.tsx
autonomous: false
requirements: [PI-03, PI-04]

must_haves:
  truths:
    - "Army list cards show freshness dot + age label indicating points data staleness"
    - "Army list detail sheet shows freshness badge next to points total"
    - "SyncStatusCard shows points_count in stats row when points data exists"
    - "RulesHubPage shows Points Changes collapsible section after sync with delta summary"
    - "Points Changes section shows per-unit diff with increase/decrease indicators when expanded"
    - "Army list impact line shows which lists are affected by point changes"
  artifacts:
    - path: "src/features/army-lists/PointsFreshnessBadge.tsx"
      provides: "Reusable freshness dot + age label component"
      exports: ["PointsFreshnessBadge"]
    - path: "src/features/rules-hub/PointsDeltaSection.tsx"
      provides: "Collapsible points delta display for RulesHubPage"
      exports: ["PointsDeltaSection"]
  key_links:
    - from: "src/features/army-lists/ArmyListCard.tsx"
      to: "src/features/army-lists/PointsFreshnessBadge.tsx"
      via: "component import and render in CardContent"
      pattern: "PointsFreshnessBadge"
    - from: "src/features/rules-hub/SyncStatusCard.tsx"
      to: "src/features/rules-hub/PointsDeltaSection.tsx"
      via: "component import and render after lastSyncDiff"
      pattern: "PointsDeltaSection"
    - from: "src/features/army-lists/ArmyListDetailSheet.tsx"
      to: "src/features/army-lists/PointsFreshnessBadge.tsx"
      via: "component import and render near points total"
      pattern: "PointsFreshnessBadge"
---

<objective>
Build the UI layer for points freshness and delta display: PointsFreshnessBadge on army list surfaces, SyncStatusCard points count extension, and PointsDeltaSection on the rules hub page showing per-unit changes and army list impact after sync.

Purpose: Makes points data visible and actionable — users see freshness status on army lists and point change details after every sync.
Output: Two new components (PointsFreshnessBadge, PointsDeltaSection) and integration into existing army list and rules hub surfaces.
</objective>

<execution_context>
@.planning/phases/65-points-import-pipeline/65-CONTEXT.md
@.planning/phases/65-points-import-pipeline/65-UI-SPEC.md
@.planning/phases/65-points-import-pipeline/65-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Existing components and types the executor needs -->

From src/lib/syncFreshness.ts:
```typescript
export type SyncFreshness = "fresh" | "aging" | "stale" | "never";
export function getSyncFreshness(lastSyncAt: string | null): SyncFreshness;
export function getSyncAgeLabel(lastSyncAt: string | null): string;
export const FRESHNESS_DOT_CLASS: Record<SyncFreshness, string>;
```

From src/hooks/useDatasheet.ts:
```typescript
export function useRulesSyncMeta(): UseQueryResult<RulesSyncMeta>;
export const RULES_SYNC_META_KEY;
```

From src/types/datasheet.ts:
```typescript
export interface RulesSyncMeta {
  // ... existing fields plus:
  points_count: number | null;
}
```

From src/types/pointsDelta.ts:
```typescript
export interface PointsDelta { added, removed, changed: number; details: PointsDeltaDetail[] }
export interface PointsDeltaDetail { unitName, factionId, oldPoints, newPoints, changeType }
```

From src/features/rules-hub/SyncStatusCard.tsx:
```typescript
interface SyncStatusCardProps { lastSyncDiff: SyncDiff | null; onSyncComplete: (diff: SyncDiff) => void }
```

From src/hooks/useArmyLists.ts:
```typescript
export function useArmyLists(): UseQueryResult<ArmyList[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PointsFreshnessBadge + PointsDeltaSection components</name>
  <files>
    src/features/army-lists/PointsFreshnessBadge.tsx,
    src/features/rules-hub/PointsDeltaSection.tsx
  </files>
  <read_first>
    src/features/rules-hub/SyncStatusCard.tsx,
    src/features/army-lists/ArmyListCard.tsx,
    src/lib/syncFreshness.ts,
    src/hooks/useDatasheet.ts,
    .planning/phases/65-points-import-pipeline/65-UI-SPEC.md
  </read_first>
  <action>
    Create src/features/army-lists/PointsFreshnessBadge.tsx per the UI-SPEC section 1. Named export PointsFreshnessBadge. No props needed — it internally calls useRulesSyncMeta() to get syncMeta. Uses getSyncFreshness(syncMeta?.last_sync_at ?? null) for tier and getSyncAgeLabel for label text. Layout: flex items-center gap-1.5. When syncMeta is loading (undefined): render Skeleton h-2 w-16. When freshness is "never" AND (points_count is null or 0): label reads "No points data" and tooltip reads "No official points data synced" per UI-SPEC copywriting. Otherwise: render 8x8 rounded-full dot using FRESHNESS_DOT_CLASS[freshness], followed by text-xs text-muted-foreground age label. Wrap the dot in Tooltip with TooltipTrigger (asChild) and TooltipContent showing the age label. Import cn from @/lib/utils, Tooltip components from @/components/ui/tooltip, Skeleton from @/components/ui/skeleton.

    Create src/features/rules-hub/PointsDeltaSection.tsx per the UI-SPEC section 2. Named export PointsDeltaSection with props: pointsDelta (PointsDelta type from pointsDelta.ts) and affectedLists (Array of {id: number, name: string}). When delta_added + delta_removed + delta_changed === 0: render static text "No point changes in this sync" in text-xs text-muted-foreground, no Collapsible. Otherwise: render Collapsible (defaultOpen false, controlled via useState). CollapsibleTrigger has "Points Changes" label + ChevronDown icon using same class pattern as SyncStatusCard error toggle: flex items-center gap-1.5 text-xs text-muted-foreground hover:text-foreground transition-colors. ChevronDown rotates 180 when open. Summary line below trigger (always visible): "+{added} added / -{removed} removed / ~{changed} changed" with text-green-600, text-red-600, text-amber-600 respectively. CollapsibleContent: scrollable max-h-48 overflow-y-auto list of pointsDelta.details items. Each row: unit name (text-sm) followed by "oldPoints -> newPoints" (text-xs text-muted-foreground) with TrendingUp (h-3 w-3 text-green-600) for increased or TrendingDown (h-3 w-3 text-red-600) for decreased. For "added" items show just newPoints. For "removed" show just oldPoints with strikethrough. Below the list, if affectedLists.length > 0, show army impact line: text-xs text-muted-foreground "{N} army list(s) affected: {comma-separated names}".
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/features/army-lists/PointsFreshnessBadge.tsx exports PointsFreshnessBadge component
    - PointsFreshnessBadge uses useRulesSyncMeta, getSyncFreshness, getSyncAgeLabel, FRESHNESS_DOT_CLASS
    - PointsFreshnessBadge renders Skeleton when syncMeta is loading
    - PointsFreshnessBadge shows "No points data" when freshness is "never" and points_count is 0/null
    - src/features/rules-hub/PointsDeltaSection.tsx exports PointsDeltaSection component
    - PointsDeltaSection renders static text when all delta counts are 0
    - PointsDeltaSection renders Collapsible with summary line when deltas exist
    - PointsDeltaSection shows army list impact line when affectedLists is non-empty
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Both new components created, typed, and building cleanly. PointsFreshnessBadge handles all freshness states. PointsDeltaSection handles zero/nonzero delta states with collapsible detail list.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate components into ArmyListCard, ArmyListDetailSheet, SyncStatusCard, RulesHubPage</name>
  <files>
    src/features/army-lists/ArmyListCard.tsx,
    src/features/army-lists/ArmyListDetailSheet.tsx,
    src/features/rules-hub/SyncStatusCard.tsx,
    src/features/rules-hub/RulesHubPage.tsx
  </files>
  <read_first>
    src/features/army-lists/ArmyListCard.tsx,
    src/features/army-lists/ArmyListDetailSheet.tsx,
    src/features/rules-hub/SyncStatusCard.tsx,
    src/features/rules-hub/RulesHubPage.tsx,
    .planning/phases/65-points-import-pipeline/65-UI-SPEC.md
  </read_first>
  <action>
    In ArmyListCard.tsx (per UI-SPEC "Integration Points" section): import PointsFreshnessBadge. Render it as the last item in CardContent, after the "Battle-ready" span. It takes no props — it self-manages its syncMeta query.

    In ArmyListDetailSheet.tsx (per UI-SPEC): import PointsFreshnessBadge. Render it inline with the points total display in ArmyListSummaryBar area, after the total points stat. The component already has useRulesSyncMeta available (line ~59 per RESEARCH), so PointsFreshnessBadge will share the cached query result. Place it in a flex row alongside the existing points display.

    In SyncStatusCard.tsx (per UI-SPEC): Add a points_count stats-row span inside the existing flex-wrap gap-4 div (after the detachments_count span around line 113-120). Conditionally render when syncMeta.points_count is not null and greater than 0: show font-semibold text-foreground count followed by "point entries" text.

    Extend SyncStatusCard props to accept pointsDelta (PointsDelta | null) and affectedLists (Array of {id, name} or empty array). Import and render PointsDeltaSection after the lastSyncDiff paragraph (around line 143), passing pointsDelta and affectedLists as props. Only render when pointsDelta is not null.

    In RulesHubPage.tsx: extend the useRulesSync return type handling to capture pointsDelta from the sync result. Store it alongside lastSyncDiff in local state (add a new useState for lastPointsDelta). Compute affectedLists: after sync, query army lists (useArmyLists data) and filter to those containing units whose names appear in pointsDelta.details. Pass lastPointsDelta and affectedLists to SyncStatusCard. Update the onSyncComplete callback to also store pointsDelta from the sync result.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/features/army-lists/ArmyListCard.tsx imports and renders PointsFreshnessBadge in CardContent
    - src/features/army-lists/ArmyListDetailSheet.tsx imports and renders PointsFreshnessBadge near points total
    - src/features/rules-hub/SyncStatusCard.tsx renders points_count stat when available
    - src/features/rules-hub/SyncStatusCard.tsx accepts pointsDelta and affectedLists props
    - src/features/rules-hub/SyncStatusCard.tsx renders PointsDeltaSection when pointsDelta is non-null
    - src/features/rules-hub/RulesHubPage.tsx stores lastPointsDelta in state and passes to SyncStatusCard
    - src/features/rules-hub/RulesHubPage.tsx computes affectedLists from army list data and delta details
    - pnpm build exits 0
    - pnpm test exits 0 (full suite green)
  </acceptance_criteria>
  <done>All UI integrations complete. Freshness badges on army list surfaces. Points delta display on rules hub. Build and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Points freshness badges on army list cards and detail sheet. Points count stat in SyncStatusCard. Points delta section with per-unit changes and army list impact after sync.</what-built>
  <how-to-verify>
    1. Run `pnpm tauri dev`
    2. Navigate to Rules Hub page
    3. Verify SyncStatusCard shows existing sync stats (datasheets, stratagems, detachments)
    4. Click "Sync now" — after sync completes, verify "Points Changes" section appears below the sync diff summary (may show "No point changes" if Wahapedia has no points CSV)
    5. If points_count stat shows, verify it appears as "{N} point entries" in the stats row
    6. Navigate to Army Lists page
    7. Verify each army list card shows a freshness badge at the bottom of the card (dot + age label)
    8. Click an army list to open the detail sheet — verify freshness badge appears near the points total
    9. If no sync has been performed, badges should show "No points data" in gray
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| syncMeta -> UI | Sync metadata displayed to user; no security concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-06 | Information Disclosure | PointsDeltaSection | accept | Displays only unit names and point values — not sensitive data |
</threat_model>

<verification>
- pnpm build exits 0
- pnpm test exits 0 (full suite)
- Visual verification: freshness badges on army list surfaces, delta section on rules hub
</verification>

<success_criteria>
Points freshness visible on all army list surfaces (cards + detail). SyncStatusCard extended with points count and delta section. RulesHubPage shows per-unit point changes with army list impact after sync. Human verified.
</success_criteria>

<output>
After completion, create `.planning/phases/65-points-import-pipeline/65-03-SUMMARY.md`
</output>

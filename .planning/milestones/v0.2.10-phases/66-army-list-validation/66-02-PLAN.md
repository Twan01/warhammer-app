---
phase: 66-army-list-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/025_tactical_role.sql
  - src-tauri/src/lib.rs
  - src/db/queries/armyLists.ts
  - src/hooks/useArmyLists.ts
autonomous: true
requirements: [LV-02, LV-04]

must_haves:
  truths:
    - "Migration 025 adds tactical_role TEXT column to army_list_units"
    - "getArmyListWithUnits returns tactical_role in its SELECT"
    - "updateArmyListUnit persists tactical_role alongside points_override and notes"
    - "clearArmyListPointsLimit sets points_limit to NULL"
    - "All existing call sites of updateArmyListUnit pass tactical_role to avoid data loss"
  artifacts:
    - path: "src-tauri/migrations/025_tactical_role.sql"
      provides: "tactical_role column on army_list_units"
      contains: "ALTER TABLE army_list_units"
    - path: "src-tauri/src/lib.rs"
      provides: "Migration 025 registration"
      contains: "tactical_role"
    - path: "src/db/queries/armyLists.ts"
      provides: "Extended query and update functions"
      exports: ["getArmyListWithUnits", "updateArmyListUnit", "clearArmyListPointsLimit"]
    - path: "src/hooks/useArmyLists.ts"
      provides: "Extended hook types and clearPointsLimit hook"
      exports: ["useUpdateArmyListUnit", "useClearArmyListPointsLimit"]
  key_links:
    - from: "src/db/queries/armyLists.ts"
      to: "src-tauri/migrations/025_tactical_role.sql"
      via: "SQL references tactical_role column"
      pattern: "tactical_role"
    - from: "src/hooks/useArmyLists.ts"
      to: "src/db/queries/armyLists.ts"
      via: "imports clearArmyListPointsLimit"
      pattern: "clearArmyListPointsLimit"
---

<objective>
Add the tactical_role column to the database, extend queries and hooks to support it, and create the clearArmyListPointsLimit function for the health panel.

Purpose: The data layer must support tactical role persistence and points limit clearing before any UI can consume these features. This plan handles all SQL/query/hook changes.

Output: Migration 025, extended armyLists.ts queries, extended useArmyLists.ts hooks.
</objective>

<execution_context>
@.planning/phases/66-army-list-validation/66-RESEARCH.md
@.planning/phases/66-army-list-validation/66-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/db/queries/armyLists.ts (current updateArmyListUnit, line 155):
```typescript
export async function updateArmyListUnit(input: UpdateArmyListUnitInput): Promise<void> {
  const db = await getDb();
  await db.execute(
    "UPDATE army_list_units SET points_override=$2, notes=$3 WHERE id=$1",
    [input.id, input.points_override, input.notes]
  );
}
```

From src/db/queries/armyLists.ts (clearArmyListDetachment pattern, line 116):
```typescript
export async function clearArmyListDetachment(id: number): Promise<void> {
  const db = await getDb();
  await db.execute(
    `UPDATE army_lists SET detachment_id = NULL, detachment_name = NULL, updated_at = datetime('now') WHERE id = $1`,
    [id]
  );
}
```

From src/hooks/useArmyLists.ts (UpdateArmyListUnitVariables, line 160):
```typescript
export interface UpdateArmyListUnitVariables extends UpdateArmyListUnitInput {
  list_id: number;
}
```

From src/types/armyList.ts (UpdateArmyListUnitInput — will be extended by Plan 01):
```typescript
export interface UpdateArmyListUnitInput {
  id: number;
  points_override: number | null;
  notes: string | null;
  tactical_role: string | null;  // Added by Plan 01
}
```

From src-tauri/src/lib.rs (last migration registration):
```rust
Migration {
    version: 24,
    description: "points_import_history",
    sql: include_str!("../migrations/024_points_import_history.sql"),
    kind: MigrationKind::Up,
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 025 + lib.rs registration + query extensions</name>
  <files>src-tauri/migrations/025_tactical_role.sql, src-tauri/src/lib.rs, src/db/queries/armyLists.ts</files>
  <read_first>
    - src-tauri/src/lib.rs (migration registration pattern, find version 24 block to add after)
    - src/db/queries/armyLists.ts (getArmyListWithUnits SQL at line 52, updateArmyListUnit at line 155, clearArmyListDetachment pattern at line 116)
    - src/types/armyList.ts (UpdateArmyListUnitInput with tactical_role added by Plan 01)
  </read_first>
  <action>
    1. Create src-tauri/migrations/025_tactical_role.sql with a single statement: ALTER TABLE army_list_units ADD COLUMN tactical_role TEXT DEFAULT NULL; (per D-06, Pitfall 3 confirms 025 is the correct number)

    2. Register migration 025 in src-tauri/src/lib.rs: add a new Migration block after the version 24 entry with version: 25, description: "tactical_role", sql: include_str!("../migrations/025_tactical_role.sql"), kind: MigrationKind::Up

    3. In src/db/queries/armyLists.ts, extend getArmyListWithUnits SELECT to include alu.tactical_role in the column list (add after alu.notes). The column comes from army_list_units so no additional JOIN needed.

    4. In src/db/queries/armyLists.ts, extend updateArmyListUnit SQL to SET points_override=$2, notes=$3, tactical_role=$4 WHERE id=$1. Update the parameter array to include input.tactical_role as the 4th parameter. This is the full-replacement UPDATE per Pitfall 2 -- every caller MUST pass all 3 fields.

    5. Add a new clearArmyListPointsLimit function following the clearArmyListDetachment pattern (Pitfall 1 in RESEARCH): UPDATE army_lists SET points_limit = NULL, updated_at = datetime('now') WHERE id = $1. Takes a single id: number parameter. This solves the COALESCE-blocks-NULL-clearing issue for points_limit.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - File src-tauri/migrations/025_tactical_role.sql exists with ALTER TABLE army_list_units ADD COLUMN tactical_role TEXT DEFAULT NULL
    - lib.rs contains Migration block with version: 25, description: "tactical_role"
    - getArmyListWithUnits SQL SELECT list includes "alu.tactical_role"
    - updateArmyListUnit SQL is "UPDATE army_list_units SET points_override=$2, notes=$3, tactical_role=$4 WHERE id=$1"
    - clearArmyListPointsLimit function exported from armyLists.ts
    - pnpm build succeeds (type check passes with Plan 01's extended UpdateArmyListUnitInput)
  </acceptance_criteria>
  <done>Migration 025 created and registered, queries extended for tactical_role and points_limit clearing</done>
</task>

<task type="auto">
  <name>Task 2: Hook extensions for tactical role and points limit clearing</name>
  <files>src/hooks/useArmyLists.ts</files>
  <read_first>
    - src/hooks/useArmyLists.ts (current useUpdateArmyListUnit at line 164, useClearArmyListDetachment pattern at line 92, import list at line 1-21)
    - src/db/queries/armyLists.ts (clearArmyListPointsLimit added in Task 1)
  </read_first>
  <action>
    1. Add clearArmyListPointsLimit to the import from @/db/queries/armyLists

    2. Create useClearArmyListPointsLimit hook following the useClearArmyListDetachment pattern: mutationFn calls clearArmyListPointsLimit, onSuccess invalidates ARMY_LISTS_KEY, ARMY_LIST_KEY(id), ARMY_LIST_UNITS_KEY(id), and ["dashboard-stats"]. The mutation takes a single number (the army list id). Same invalidation pattern as useClearArmyListDetachment.

    3. The useUpdateArmyListUnit hook requires NO changes -- it already destructures list_id and passes the rest to updateArmyListUnit. Since UpdateArmyListUnitInput now includes tactical_role (from Plan 01's type extension), the hook automatically supports it through the existing UpdateArmyListUnitVariables extends UpdateArmyListUnitInput pattern.

    4. Verify the mutationFn destructuring in useUpdateArmyListUnit: ({ list_id: _list_id, ...rest }) => updateArmyListUnit(rest) -- this spread will correctly pass tactical_role through to the query function.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - clearArmyListPointsLimit imported from @/db/queries/armyLists
    - useClearArmyListPointsLimit hook exported from useArmyLists.ts
    - useClearArmyListPointsLimit invalidates ARMY_LISTS_KEY, ARMY_LIST_KEY(id), ARMY_LIST_UNITS_KEY(id), ["dashboard-stats"]
    - useUpdateArmyListUnit continues to work with the extended UpdateArmyListUnitInput (no code change needed, just verification)
    - pnpm build succeeds
  </acceptance_criteria>
  <done>Hooks support tactical role persistence and points limit clearing; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite writes | tactical_role stored as TEXT, constrained by TypeScript enum at the UI layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-02 | Tampering | tactical_role column | accept | Local desktop app, no network input; TypeScript const array constrains values at UI layer |
| T-66-03 | Information Disclosure | clearArmyListPointsLimit | accept | Local-only operation, no sensitive data exposed |
</threat_model>

<verification>
- pnpm build (no type errors, all imports resolve)
- Migration file exists at src-tauri/migrations/025_tactical_role.sql
</verification>

<success_criteria>
- tactical_role column will be created on app start via migration 025
- getArmyListWithUnits returns tactical_role for each unit row
- updateArmyListUnit persists tactical_role (Pitfall 2 addressed)
- clearArmyListPointsLimit allows removing a points limit (Pitfall 1 addressed)
</success_criteria>

<output>
After completion, create `.planning/phases/66-army-list-validation/66-02-SUMMARY.md`
</output>

# Requirements Archive — v0.3.7 Smart Automation

**Archived:** 2026-05-28
**Status:** All 13/13 requirements complete

## Status Auto-Derivation

- [x] **SAD-01**: Assembly status auto-derives from recipe section completion — when all steps in sections with `section_type = 'assembly'` are complete, `status_assembly` is set to 1
- [x] **SAD-02**: Basing and varnish auto-derivation uses `section_type` field instead of fragile section name LIKE matching
- [x] **SAD-03**: `SECTION_TYPES` vocabulary extended with 'assembly', 'basing', 'varnish' values for explicit section_type mapping
- [x] **SAD-04**: Manual-override guard — auto-derivation does not overwrite statuses that were explicitly set by the user via StatusPopover

## Active Project Lifecycle

- [x] **APL-01**: `is_active_project` auto-set to 1 when a recipe is first assigned to a unit
- [x] **APL-02**: `is_active_project` auto-cleared to 0 when all applied recipe steps reach 100% completion
- [x] **APL-03**: Manual `is_active_project` toggle continues to work and is not overridden by auto-management within the same session

## Smart Context Pre-Filling

- [x] **SCP-01**: Recipe creation form pre-fills faction from FactionContext when opened from a unit context
- [x] **SCP-02**: Recipe picker in ApplyRecipeDialog pre-filters recipes by the target unit's faction
- [x] **SCP-03**: Pre-filled values are visible and editable — user can override any auto-filled field

## Battle-Readiness & Points

- [x] **BRP-01**: `computeUnitReadiness()` pure function established as canonical readiness definition (painting complete + assembled + based)
- [x] **BRP-02**: Army list unit picker shows painting status and assembly state inline per unit
- [x] **BRP-03**: Army list unit picker supports filtering/sorting by units that fit within remaining points budget

## Traceability (Final)

| Requirement | Phase | Status | Outcome |
|-------------|-------|--------|---------|
| SAD-01 | Phase 100 | Complete | Validated — syncDerivedStatuses assembly derivation |
| SAD-02 | Phase 100 | Complete | Validated — section_type dual-path matching |
| SAD-03 | Phase 100 | Complete | Validated — SECTION_TYPES has 10 values |
| SAD-04 | Phase 100 | Complete | Validated — CASE WHEN override guard + UnitSheet edit fix |
| APL-01 | Phase 100 | Complete | Validated — auto-set in createAssignment/bulkCreateAssignments |
| APL-02 | Phase 100 | Complete | Validated — auto-clear at pct=100 via CASE WHEN |
| APL-03 | Phase 100 | Complete | Validated — CASE WHEN preserves manual toggle |
| SCP-01 | Phase 102 | Complete | Validated — ActiveFactionContext auto-fill in RecipeFormSheet |
| SCP-02 | Phase 102 | Complete | Validated — Suggested/Other grouping in ApplyRecipeDialog |
| SCP-03 | Phase 102 | Complete | Validated — standard Select controls, not disabled |
| BRP-01 | Phase 101 | Complete | Validated — computeUnitReadiness() in src/lib/readiness.ts |
| BRP-02 | Phase 101 | Complete | Validated — Battle Ready badge + 4-dot readiness indicators |
| BRP-03 | Phase 101 | Complete | Validated — budget header + Fits Budget filter |

## Out of Scope (Preserved)

- Auto-suggest base colors per faction — requires faction color schema extension
- Paint inventory auto-tracking (auto-set owned, running_low warnings)
- Batch status updates from collection view
- Auto-sync rules on app launch — local-first design

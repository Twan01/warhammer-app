<task type="auto" tdd="true">
  <name>Task 1: Build KanbanCard, KanbanCardActions, KanbanColumn, KanbanEmptyState + fill KanbanCard test stub</name>
  <files>
    src/features/painting-projects/KanbanCard.tsx,
    src/features/painting-projects/KanbanCardActions.tsx,
    src/features/painting-projects/KanbanColumn.tsx,
    src/features/painting-projects/KanbanEmptyState.tsx,
    tests/painting/KanbanCard.test.tsx
  </files>
  <read_first>
    src/features/painting-projects/kanbanUtils.ts,
    src/types/unit.ts,
    src/types/faction.ts,
    src/components/ui/card.tsx,
    src/components/ui/badge.tsx,
    src/components/ui/progress.tsx,
    src/components/ui/button.tsx,
    src/features/factions/FactionsEmptyState.tsx,
    src/features/units/StatusPopover.tsx,
    .planning/phases/04-painting-module/04-UI-SPEC.md,
    tests/painting/KanbanCard.test.tsx,
    tests/collection/UnitTable.test.tsx
  </read_first>
  <behavior>
    KanbanCard (PROJ-03):
    - Wrapper Card with cursor-grab, select-none, rounded-lg, bg-card, min-w-[280px]
    - Row 1: GripVertical icon (h-4 w-4 text-muted-foreground cursor-grab) + unit.name (text-sm font-semibold flex-1 truncate) + KanbanCardActions trigger (MoreHorizontal h-4 w-4)
    - Row 2: Faction Badge — `<Badge style={{ backgroundColor: faction.color_theme }} className="border-transparent text-white text-xs">{faction.name}</Badge>` (renders only when faction is found)
    - Row 3: Progress (h-1.5) + `{painting_percentage}%` text-xs text-muted-foreground right-aligned
    - Row 4 (meta, conditional): when priority is non-null show Flag icon (h-3 w-3) + priority; when target_completion_date is non-null show Calendar icon (h-3 w-3) + formatted date "MMM D, YYYY"; if target_completion_date < today's date, the date span has className="text-destructive" with aria-label "Target date overdue: {date}"
    - aria-label on the wrapper = unit.name
    - useSortable with id = `unit-${unit.id}` and data: { type: "card", unit }; transform applied via @dnd-kit/utilities CSS helper

    KanbanCardActions (PROJ-06):
    - Trigger: ghost icon Button with MoreHorizontal h-4 w-4
    - Menu items: "Remove from board" (X icon h-4 w-4) → calls onRemoveFromBoard(); "Edit unit" (Pencil icon h-4 w-4) → calls onEditUnit()
    - Use radix DropdownMenu from `radix-ui` package OR a Popover+Command (project does not have shadcn dropdown-menu installed; check src/components/ui/ — only popover, command, dialog exist). Implement via Popover + Command for consistency with StatusPopover.tsx.

    KanbanColumn (PROJ-02):
    - Receives status (PaintingStatus), units array (already sorted)
    - Header: status label uppercase tracking-wide text-xs font-semibold text-muted-foreground + Badge variant="secondary" with `{units.length}`
    - Body: SortableContext with items={unit ids}, strategy=verticalListSortingStrategy; renders KanbanCard children passed as prop or inline
    - Container: flex flex-col gap-2, w-[280px] flex-shrink-0
    - Body has overflow-y-auto and a useDroppable from @dnd-kit/core with id = `column-${status}` so cards can drop on empty columns
    - When isOver from useDroppable, body adds className "bg-accent/30"

    KanbanEmptyState (PROJ-08):
    - Layout: flex flex-col items-center justify-center gap-4 py-16 text-center
    - Icon: Kanban from lucide-react, h-12 w-12 text-muted-foreground
    - Heading: "No active projects" (text-base font-semibold per FactionsEmptyState pattern)
    - Body: "Mark a unit as active project to see it here." (text-sm text-muted-foreground)
    - CTA Button: "Add project" — calls onAddProject()

    KanbanCard.test.tsx — fill the two it.skip stubs:
    - PROJ-03: renders unit name, faction Badge with backgroundColor style, Progress, priority text, target date text
    - PROJ-03: when target_completion_date < today, the date span has class "text-destructive"
  </behavior>
  <action>
    Create the four component files in `src/features/painting-projects/`. Use exact specs:

    1. `KanbanCard.tsx`:
       ```typescript
       import { GripVertical, MoreHorizontal, Flag, Calendar } from "lucide-react";
       import { useSortable } from "@dnd-kit/sortable";
       import { CSS } from "@dnd-kit/utilities";
       import { Card } from "@/components/ui/card";
       import { Badge } from "@/components/ui/badge";
       import { Progress } from "@/components/ui/progress";
       import { cn } from "@/lib/utils";
       import type { Unit } from "@/types/unit";
       import type { Faction } from "@/types/faction";
       import { KanbanCardActions } from "./KanbanCardActions";

       function formatDate(iso: string): string {
         const d = new Date(iso);
         return d.toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" });
       }

       function isOverdue(iso: string): boolean {
         const today = new Date();
         today.setHours(0, 0, 0, 0);
         return new Date(iso) < today;
       }

       export interface KanbanCardProps {
         unit: Unit;
         faction: Faction | undefined;
         onRemoveFromBoard: (unit: Unit) => void;
         onEditUnit: (unit: Unit) => void;
       }

       export function KanbanCard({ unit, faction, onRemoveFromBoard, onEditUnit }: KanbanCardProps) {
         const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({
           id: `unit-${unit.id}`,
           data: { type: "card", unit },
         });
         const style = {
           transform: CSS.Transform.toString(transform),
           transition,
           opacity: isDragging ? 0.4 : 1,
         };
         const overdue = unit.target_completion_date ? isOverdue(unit.target_completion_date) : false;
         return (
           <Card
             ref={setNodeRef}
             style={style}
             className="min-w-[280px] cursor-grab select-none rounded-lg bg-card p-3"
             aria-label={unit.name}
             {...attributes}
           >
             <div className="flex items-center gap-2">
               <button
                 type="button"
                 {...listeners}
                 className="cursor-grab text-muted-foreground"
                 aria-label="Drag to reorder"
               >
                 <GripVertical className="h-4 w-4" />
               </button>
               <span className="flex-1 truncate text-sm font-semibold">{unit.name}</span>
               <KanbanCardActions
                 onRemoveFromBoard={() => onRemoveFromBoard(unit)}
                 onEditUnit={() => onEditUnit(unit)}
               />
             </div>
             {faction && (
               <div className="mt-2">
                 <Badge
                   style={{ backgroundColor: faction.color_theme }}
                   className="border-transparent text-xs text-white"
                 >
                   {faction.name}
                 </Badge>
               </div>
             )}
             <div className="mt-2 flex items-center gap-2">
               <Progress value={unit.painting_percentage} className="h-1.5 flex-1" />
               <span className="text-xs text-muted-foreground">{unit.painting_percentage}%</span>
             </div>
             {(unit.priority !== null || unit.target_completion_date !== null) && (
               <div className="mt-2 flex items-center gap-3 text-xs text-muted-foreground">
                 {unit.priority !== null && (
                   <span className="inline-flex items-center gap-1">
                     <Flag className="h-3 w-3" /> {unit.priority}
                   </span>
                 )}
                 {unit.target_completion_date !== null && (
                   <span
                     className={cn(
                       "inline-flex items-center gap-1",
                       overdue && "text-destructive",
                     )}
                     aria-label={overdue ? `Target date overdue: ${unit.target_completion_date}` : undefined}
                   >
                     <Calendar className="h-3 w-3" /> {formatDate(unit.target_completion_date)}
                   </span>
                 )}
               </div>
             )}
           </Card>
         );
       }
       ```

    2. `KanbanCardActions.tsx`:
       Use Popover + Command (project has no DropdownMenu shadcn — match the StatusPopover.tsx pattern). Trigger is a ghost icon Button with MoreHorizontal. The PopoverContent contains a Command list with two items.

       ```typescript
       import { useState } from "react";
       import { MoreHorizontal, X, Pencil } from "lucide-react";
       import { Button } from "@/components/ui/button";
       import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";
       import { Command, CommandGroup, CommandItem, CommandList } from "@/components/ui/command";

       export interface KanbanCardActionsProps {
         onRemoveFromBoard: () => void;
         onEditUnit: () => void;
       }

       export function KanbanCardActions({ onRemoveFromBoard, onEditUnit }: KanbanCardActionsProps) {
         const [open, setOpen] = useState(false);
         return (
           <Popover open={open} onOpenChange={setOpen}>
             <PopoverTrigger asChild>
               <Button
                 type="button"
                 variant="ghost"
                 size="icon"
                 className="h-6 w-6"
                 onClick={(e) => e.stopPropagation()}
                 aria-label="Card actions"
               >
                 <MoreHorizontal className="h-4 w-4" />
               </Button>
             </PopoverTrigger>
             <PopoverContent
               className="w-44 p-0"
               align="end"
               onClick={(e) => e.stopPropagation()}
             >
               <Command>
                 <CommandList>
                   <CommandGroup>
                     <CommandItem onSelect={() => { setOpen(false); onRemoveFromBoard(); }}>
                       <X className="mr-2 h-4 w-4" />
                       Remove from board
                     </CommandItem>
                     <CommandItem onSelect={() => { setOpen(false); onEditUnit(); }}>
                       <Pencil className="mr-2 h-4 w-4" />
                       Edit unit
                     </CommandItem>
                   </CommandGroup>
                 </CommandList>
               </Command>
             </PopoverContent>
           </Popover>
         );
       }
       ```

    3. `KanbanColumn.tsx`:
       ```typescript
       import { useDroppable } from "@dnd-kit/core";
       import { SortableContext, verticalListSortingStrategy } from "@dnd-kit/sortable";
       import { Badge } from "@/components/ui/badge";
       import { cn } from "@/lib/utils";
       import type { PaintingStatus, Unit } from "@/types/unit";
       import type { Faction } from "@/types/faction";
       import { KanbanCard } from "./KanbanCard";

       export interface KanbanColumnProps {
         status: PaintingStatus;
         units: Unit[];
         factionMap: Map<number, Faction>;
         onRemoveFromBoard: (unit: Unit) => void;
         onEditUnit: (unit: Unit) => void;
       }

       export function KanbanColumn({
         status,
         units,
         factionMap,
         onRemoveFromBoard,
         onEditUnit,
       }: KanbanColumnProps) {
         const { setNodeRef, isOver } = useDroppable({
           id: `column-${status}`,
           data: { type: "column", status },
         });
         const itemIds = units.map((u) => `unit-${u.id}`);
         return (
           <div className="flex w-[280px] flex-shrink-0 flex-col gap-2">
             <div className="flex items-center justify-between px-1">
               <h2 className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">
                 {status}
               </h2>
               <Badge variant="secondary">{units.length}</Badge>
             </div>
             <div
               ref={setNodeRef}
               className={cn(
                 "flex min-h-[60px] flex-col gap-2 overflow-y-auto rounded-md p-1",
                 isOver && "bg-accent/30",
               )}
             >
               <SortableContext items={itemIds} strategy={verticalListSortingStrategy}>
                 {units.map((u) => (
                   <KanbanCard
                     key={u.id}
                     unit={u}
                     faction={factionMap.get(u.faction_id)}
                     onRemoveFromBoard={onRemoveFromBoard}
                     onEditUnit={onEditUnit}
                   />
                 ))}
               </SortableContext>
             </div>
           </div>
         );
       }
       ```

    4. `KanbanEmptyState.tsx`:
       ```typescript
       import { Kanban } from "lucide-react";
       import { Button } from "@/components/ui/button";

       export interface KanbanEmptyStateProps {
         onAddProject: () => void;
       }

       export function KanbanEmptyState({ onAddProject }: KanbanEmptyStateProps) {
         return (
           <div className="flex flex-col items-center justify-center gap-4 py-16 text-center">
             <Kanban className="h-12 w-12 text-muted-foreground" />
             <p className="text-base font-semibold">No active projects</p>
             <p className="text-sm text-muted-foreground">
               Mark a unit as active project to see it here.
             </p>
             <Button onClick={onAddProject}>Add project</Button>
           </div>
         );
       }
       ```

    5. Fill in `tests/painting/KanbanCard.test.tsx` — replace the two `it.skip` blocks with real bodies. Wrap the card in a DndContext + SortableContext (useSortable requires a SortableContext ancestor):

       ```typescript
       /**
        * PROJ-03 — KanbanCard renders unit name, faction badge, progress, priority, target date.
        */
       import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
       import { render, screen } from "@testing-library/react";
       import { DndContext } from "@dnd-kit/core";
       import { SortableContext } from "@dnd-kit/sortable";
       import { KanbanCard } from "@/features/painting-projects/KanbanCard";
       import type { Unit } from "@/types/unit";
       import type { Faction } from "@/types/faction";

       function makeUnit(over: Partial<Unit> = {}): Unit {
         return {
           id: 1, faction_id: 1, name: "Fire Warriors",
           category: null, unit_type: null, model_count: null, owned_count: null, points: null,
           status_assembly: 0, status_painting: "Built", painting_percentage: 50,
           status_basing: 0, status_varnished: 0, is_active_project: 1,
           priority: 1, target_completion_date: "2099-12-31",
           purchase_date: null, purchase_price: null,
           storage_location: null, main_image_path: null, notes: null,
           created_at: "2026-01-01", updated_at: "2026-01-01",
           ...over,
         };
       }

       function makeFaction(over: Partial<Faction> = {}): Faction {
         return {
           id: 1, name: "Tau Empire", game_system: "Warhammer 40K",
           description: null, color_theme: "#33aaff", icon_path: null,
           created_at: "2026-01-01", updated_at: "2026-01-01",
           ...over,
         };
       }

       function renderCard(unit: Unit, faction: Faction | undefined = makeFaction()) {
         return render(
           <DndContext>
             <SortableContext items={[`unit-${unit.id}`]}>
               <KanbanCard
                 unit={unit}
                 faction={faction}
                 onRemoveFromBoard={vi.fn()}
                 onEditUnit={vi.fn()}
               />
             </SortableContext>
           </DndContext>
         );
       }

       describe("KanbanCard", () => {
         it("PROJ-03: renders unit name, faction badge, progress bar, and meta row", () => {
           renderCard(makeUnit({ name: "Fire Warriors", priority: 2, painting_percentage: 75 }));
           expect(screen.getByText("Fire Warriors")).toBeInTheDocument();
           expect(screen.getByText("Tau Empire")).toBeInTheDocument();
           expect(screen.getByText("75%")).toBeInTheDocument();
           expect(screen.getByText("2")).toBeInTheDocument(); // priority
         });

         it("PROJ-03: applies text-destructive when target_completion_date is past today", () => {
           const { container } = renderCard(makeUnit({ target_completion_date: "2000-01-01" }));
           const overdueSpan = container.querySelector('[aria-label^="Target date overdue"]');
           expect(overdueSpan).not.toBeNull();
           expect(overdueSpan?.className).toContain("text-destructive");
         });

         it("PROJ-03: hides target date when null", () => {
           renderCard(makeUnit({ target_completion_date: null, priority: null }));
           // No date span at all
           const dateSpan = document.querySelector('[aria-label^="Target date overdue"]');
           expect(dateSpan).toBeNull();
         });
       });
       ```
  </action>
  <verify>
    <automated>pnpm test -- --run tests/painting/KanbanCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists: src/features/painting-projects/KanbanCard.tsx
    - File exists: src/features/painting-projects/KanbanCardActions.tsx
    - File exists: src/features/painting-projects/KanbanColumn.tsx
    - File exists: src/features/painting-projects/KanbanEmptyState.tsx
    - `grep "useSortable" src/features/painting-projects/KanbanCard.tsx` matches
    - `grep "GripVertical" src/features/painting-projects/KanbanCard.tsx` matches
    - `grep "text-destructive" src/features/painting-projects/KanbanCard.tsx` matches
    - `grep "useDroppable" src/features/painting-projects/KanbanColumn.tsx` matches
    - `grep "SortableContext" src/features/painting-projects/KanbanColumn.tsx` matches
    - `grep "verticalListSortingStrategy" src/features/painting-projects/KanbanColumn.tsx` matches
    - `grep "Remove from board" src/features/painting-projects/KanbanCardActions.tsx` matches
    - `grep "Edit unit" src/features/painting-projects/KanbanCardActions.tsx` matches
    - `grep "No active projects" src/features/painting-projects/KanbanEmptyState.tsx` matches
    - `grep "Mark a unit as active project to see it here." src/features/painting-projects/KanbanEmptyState.tsx` matches
    - `grep -c "it.skip" tests/painting/KanbanCard.test.tsx` returns 0
    - `pnpm test -- --run tests/painting/KanbanCard.test.tsx` exits 0 with all tests passing
  </acceptance_criteria>
  <done>
    Kanban primitive components built; KanbanCard tests no longer skipped — PROJ-03 verified.
  </done>
</task>

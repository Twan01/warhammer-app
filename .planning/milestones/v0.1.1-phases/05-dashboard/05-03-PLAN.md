---
phase: 05-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["05-00", "05-01", "05-02"]
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07
  - DASH-08
must_haves:
  truths:
    - "User can navigate to / and see the assembled Dashboard (no PlaceholderPage)"
    - "User sees correct top stat values (DASH-01) — totals match Collection page counts"
    - "User sees Painting/Assembly/Basing percentages computed correctly (DASH-03, DASH-04)"
    - "User sees one FactionSummaryCard per faction with model count, painted %, points (DASH-02)"
    - "User can click a FactionSummaryCard and land on /collection pre-filtered to that faction (DASH-02 click-through)"
    - "User sees Active Projects list — only is_active_project=1 units, top 5, sorted by updated_at DESC (DASH-05)"
    - "User sees Recently Updated list — top 5 units by updated_at DESC, with relative time labels (DASH-06)"
    - "User can click a list row and the UnitDetailSheet slides in with that unit (DASH-05/06 interaction)"
    - "User sees DashboardEmptyState when no units exist; CTA navigates to /collection (DASH-08)"
    - "User updates a unit's status_painting in /collection and on returning to / the dashboard reflects the change without refresh (DASH-07 / DATA-09)"
    - "All 5 Phase 5 ROADMAP success criteria pass under live `pnpm tauri dev` exercise"
  artifacts:
    - path: ".planning/phases/05-dashboard/05-03-SUMMARY.md"
      provides: "Sign-off record for Phase 5: 25-step manual checklist results, retro insights, STATE.md updates"
  key_links:
    - from: ".planning/phases/05-dashboard/05-03-SUMMARY.md"
      to: ".planning/STATE.md"
      via: "Phase 5 marked complete; progress updated to 16/16"
      pattern: "completed_phases: 5"
    - from: "User browser session"
      to: "Dashboard at /"
      via: "pnpm tauri dev — dashboard renders without PlaceholderPage"
      pattern: "DashboardPage rendered"
---

<objective>
Final integration gate for Phase 5. Plans 05-00 through 05-02 produced the algorithmic foundation (computeStats, relativeTime, statusAbbr), the data layer (getDashboardStats, useDashboardStats), and the UI surface (DashboardPage + 4 components + route swap). This plan exercises the full live application against all 5 Phase 5 ROADMAP success criteria + every DASH-01..08 requirement, performs a one-time read-only verification of the DASH-07 invalidation contract end-to-end, and writes the phase-completion SUMMARY.

Purpose: Without a live human-verify pass, the optimistic-update / cache-invalidation guarantee (DASH-07) and the Zustand-filter click-through (DASH-02) are only proved at the unit-test level. This checkpoint is the gate before `/gsd:verify-work` and Milestone v1.0 sign-off.

Output:
- Run `pnpm test`, `pnpm build` once (final phase-gate)
- Manual run-through of 25 checklist items in `pnpm tauri dev`
- One read-only grep verification confirming DASH-07 wiring
- 05-03-SUMMARY.md documenting sign-off (or remediation list)
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-UI-SPEC.md
@.planning/phases/05-dashboard/05-VALIDATION.md
@.planning/phases/05-dashboard/05-00-PLAN.md
@.planning/phases/05-dashboard/05-01-PLAN.md
@.planning/phases/05-dashboard/05-02-PLAN.md
@src/features/dashboard/DashboardPage.tsx
@src/hooks/useDashboardStats.ts
@src/hooks/useUnits.ts

<interfaces>
<!-- All upstream artifacts (read-only verification only — no modifications in this plan) -->

From plan 05-00:
- src/features/dashboard/computeStats.ts
- src/features/dashboard/relativeTime.ts
- src/features/dashboard/statusAbbr.ts
- tests/dashboard/computeStats.test.ts
- tests/dashboard/relativeTime.test.ts

From plan 05-01:
- src/db/queries/dashboard.ts (getDashboardStats)
- src/hooks/useDashboardStats.ts (DASHBOARD_STATS_KEY + useDashboardStats)
- tests/dashboard/useDashboardStats.test.ts

From plan 05-02:
- src/features/dashboard/StatCard.tsx
- src/features/dashboard/DashboardListRow.tsx
- src/features/dashboard/FactionSummaryCard.tsx
- src/features/dashboard/DashboardEmptyState.tsx
- src/features/dashboard/DashboardPage.tsx
- src/app/dashboard/page.tsx (route entry — now imports real DashboardPage)
- tests/dashboard/DashboardPage.test.tsx (no skipped tests)

From Phase 2 (DASH-07 invalidation anchor — must remain intact):
- src/hooks/useUnits.ts must contain `["dashboard-stats"]` in 3 places (one per mutation onSuccess)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Read-only DASH-07 invalidation verification + final automated phase gate</name>
  <files></files>
  <read_first>
    - src/hooks/useUnits.ts (verify 3 ['dashboard-stats'] invalidations remain in onSuccess of useCreateUnit, useUpdateUnit, useDeleteUnit)
    - src/hooks/useDashboardStats.ts (verify DASHBOARD_STATS_KEY === ["dashboard-stats"])
    - .planning/phases/05-dashboard/05-RESEARCH.md (Pitfall 6: do NOT add invalidation again — verify only)
  </read_first>
  <action>
    Step 1 — Verify DASH-07 invalidation contract is intact across the codebase. Run from project root:

    ```
    pnpm grep -n '"dashboard-stats"' src/hooks/useUnits.ts
    ```

    (Or use whatever ripgrep / grep tool is available — `node -e "..."` script also acceptable.) The output must contain exactly 3 lines, one each in onSuccess for useCreateUnit, useUpdateUnit, useDeleteUnit.

    Equivalent Node fallback:
    ```
    node -e "const c=require('fs').readFileSync('src/hooks/useUnits.ts','utf8');const m=c.match(/\\[\"dashboard-stats\"\\]/g);console.log('matches:',m?m.length:0);if((m?m.length:0)!==3)process.exit(1);"
    ```

    Step 2 — Confirm the dashboard hook's exported key matches:
    ```
    node -e "const c=require('fs').readFileSync('src/hooks/useDashboardStats.ts','utf8');if(!c.includes('DASHBOARD_STATS_KEY = [\"dashboard-stats\"] as const'))process.exit(1);"
    ```

    Step 3 — Run the final automated phase gate:
    ```
    pnpm tsc --noEmit
    pnpm test
    pnpm build
    ```

    All three commands must exit 0. Capture the dashboard-specific test count from `pnpm test` output and record it in the SUMMARY (Step 3 of Task 3 below).

    Step 4 — Do NOT modify any files in this task. This is purely a verification gate.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/hooks/useUnits.ts','utf8');const m=c.match(/\[\"dashboard-stats\"\]/g);if((m?m.length:0)!==3){console.error('expected 3, got',m?m.length:0);process.exit(1)}console.log('OK 3 invalidations')" && node -e "const c=require('fs').readFileSync('src/hooks/useDashboardStats.ts','utf8');if(!c.includes('[\"dashboard-stats\"] as const'))process.exit(1);console.log('OK key contract')" && pnpm tsc --noEmit && pnpm test && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useUnits.ts` contains exactly 3 occurrences of `["dashboard-stats"]` (one per mutation onSuccess)
    - `src/hooks/useDashboardStats.ts` contains literal `["dashboard-stats"] as const`
    - `pnpm tsc --noEmit` exits 0
    - `pnpm test` exits 0 (every test from Phases 1-5 passes; expected ≥ ~40 dashboard tests + prior phase tests)
    - `pnpm build` exits 0 (production SPA bundle compiles)
    - No file edits made in this task (this is a verification gate only)
  </acceptance_criteria>
  <done>
    DASH-07 wiring confirmed end-to-end. Automated phase gate green. Ready for human-verify checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify all 5 Phase 5 ROADMAP success criteria + every DASH-01..08 requirement in `pnpm tauri dev`</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md (Phase 5 success criteria — all 5 numbered items)
    - .planning/REQUIREMENTS.md (DASH-01 through DASH-08 — exact behaviors)
    - .planning/phases/05-dashboard/05-UI-SPEC.md (Copywriting Contract — exact strings to verify)
    - .planning/phases/05-dashboard/05-VALIDATION.md (Manual-Only Verifications table — items the automated suite cannot cover)
  </read_first>
  <what-built>
    Full Dashboard module wired end-to-end:
    - / route now renders the real DashboardPage (not the placeholder)
    - Top stat row (DASH-01): 4 StatCards in a grid-cols-4 — Total Models, Fully Painted, Battle-Ready Points, Active Projects
    - Progress section (DASH-03, DASH-04): 3 StatCards in grid-cols-3 — Painting Progress, Assembly Progress, Basing Progress
    - Faction section (DASH-02): wrapping flex row of FactionSummaryCards, one per faction, with 4px left border in faction.color_theme; clicking sets useCollectionFilters factions=[id] and navigates to /collection
    - Lists section (DASH-05, DASH-06): two columns — Active Projects (left) and Recently Updated (right), top 5 each, with faction-colored Badge + status abbreviation; Recently Updated rows also show relative time (Xm/Xh/Xd/Xw/Xmo)
    - Clicking any list row opens UnitDetailSheet on the dashboard (Sheet mounted as sibling — Pitfall 4)
    - Detail Sheet Edit/Delete buttons open UnitSheet/UnitDeleteDialog (full parity with FactionsPage / CollectionPage)
    - Loading state: shadcn Skeletons in all 4 sections (h-24 stat cards, h-20 w-[180px] faction cards, h-11 list rows)
    - Error state: "Failed to load dashboard. Try refreshing the app." (UI-SPEC copy)
    - Empty state (DASH-08): centered PackageSearch icon + "Your collection is empty" + "Go to Collection" CTA → /collection
    - DASH-07 invalidation: every unit mutation in /collection or /factions or /painting-projects automatically refreshes the dashboard via the existing ["dashboard-stats"] cache key (no extra wiring needed in plan 05-03)
  </what-built>
  <how-to-verify>
    Run `pnpm tauri dev` and exercise the following flows. Each numbered item maps to a Phase 5 ROADMAP success criterion or DASH-* requirement:

    **Setup:** Launch the app. Confirm seed data is present (factions: Tau Empire, Ultramarines, Necrons, Tyranids; ~5 units across them, mix of painting statuses).

    **Phase 5 ROADMAP Success Criteria coverage:**

    1. **DASH-01 top stat row labels:** Click the Dashboard sidebar entry (or note it loads at app startup since `/` is the default route). → 4 cards in the top row with labels "Total Models", "Fully Painted", "Battle-Ready Points", "Active Projects". Each shows a large number (28px / text-3xl).

    2. **DASH-01 totals correctness — vs Collection:** Note the "Total Models" number. Navigate to /collection. Apply no filters. → The pagination / row count of /collection should match "Total Models" on the dashboard.

    3. **DASH-01 fullyPainted matches:** On /collection, filter "Status = Completed". → Filtered count equals "Fully Painted" on the dashboard.

    4. **DASH-01 activeProjectsCount matches:** On /collection, toggle "Active only". → Filtered count equals "Active Projects" on the dashboard.

    5. **DASH-03 / DASH-04 percentages:** Verify "Painting Progress %", "Assembly Progress %", "Basing Progress %" are visible. Mentally cross-check: if you have 4 units with painting_percentages [0, 50, 50, 100], "Painting Progress" should read 50%.

    6. **DASH-02 faction cards exist:** "By Faction" section shows one card per faction in seed data (4 cards: Tau Empire, Ultramarines, Necrons, Tyranids). Each card has a colored 4px left border whose color matches the faction's color_theme hex.

    7. **DASH-02 faction card content:** Each faction card shows: faction name, "N models", "XX% painted", "NNN pts owned / NNN pts painted". Cross-check with /collection filtered to that faction.

    8. **DASH-02 click-through (Pitfall 2):** Click the Tau Empire card. → Browser navigates to /collection. The faction filter shows "Tau Empire" selected (active filter chip / selection state). Other filters (search, status, category, active-only) are cleared. Only Tau units visible.

    9. **DASH-05 active projects list:** Return to dashboard. "Active Projects" left column shows units with the Flame icon (is_active_project=1), max 5 entries. Each row: unit name + faction-colored Badge + status abbreviation. (No relative time on this list.)

    10. **DASH-05 ordering:** Note the order of active projects. The first row should have the most recent updated_at value.

    11. **DASH-05 row click opens detail Sheet:** Click any active project row. → UnitDetailSheet slides in from the right with that unit's full details (status, percentages, faction badge, etc.). Esc to close.

    12. **DASH-06 recently updated list:** "Recently Updated" right column shows top 5 units (regardless of is_active_project). Each row: unit name + faction Badge + status abbreviation + relative time label (e.g. "2h", "1d", "3w").

    13. **DASH-06 relative time format:** Edit a unit (in /collection or via the dashboard's detail Sheet → Edit). Save. Return to dashboard. → The just-edited unit should now appear at the top of "Recently Updated" with the time label "0m" or "1m".

    14. **DASH-06 row click opens detail Sheet:** Click any recently-updated row. → Sheet opens with that unit. Confirms the Sheet works for both lists.

    15. **DASH-07 cache freshness (the headline contract):** Note "Fully Painted" count. Open the dashboard's "Recently Updated" detail Sheet for a non-Completed unit, click Edit Unit (or navigate to /collection and edit there). Change `status_painting` to "Completed", save. Return to / (dashboard). → "Fully Painted" count incremented by 1 — without manual refresh.

    16. **DASH-07 cache freshness from /collection inline status:** Note "Fully Painted" again. Navigate to /collection. Use the inline StatusPopover (column status badge) to change a unit to "Completed". Return to /. → "Fully Painted" updated. (Verifies the inline-popover path also invalidates the dashboard.)

    17. **DASH-07 from delete:** Note "Total Models". In /collection, delete a unit. Return to /. → "Total Models" decremented by 1.

    18. **DASH-08 empty state:** Delete every unit in /collection (or run on a fresh DB). Return to /. → DashboardEmptyState renders: PackageSearch icon + "Your collection is empty" + body text + "Go to Collection" CTA. None of the section labels (Progress, By Faction) render.

    19. **DASH-08 CTA navigates:** Click "Go to Collection". → Browser navigates to /collection.

    20. **Loading state:** Hard-refresh the app (Cmd+R / Ctrl+R or restart `pnpm tauri dev`). → Briefly observe Skeletons in all 4 sections before content appears.

    21. **POLISH-04 carryover (Sheet remount per unit):** On the dashboard, open the detail Sheet for unit A (click row in any list). Without closing, click unit B in the table. → Sheet content updates to unit B (no stale unit A data visible). This proves the `key={selectedUnit?.id}` pattern works on the Dashboard too.

    22. **POLISH-05 carryover (faction colors):** Visually confirm faction cards have colored left borders matching their color_theme; faction badges in list rows use the same color via inline backgroundColor.

    23. **Section gap visual:** Outer page shows generous vertical gap between sections (gap-12 ≈ 48px). Section labels "Progress" and "By Faction" use uppercase-tracked muted style.

    24. **Run full automated suite:** `pnpm test` — verify all dashboard tests pass alongside Phases 3+4.

    25. **Run production build:** `pnpm build` — verify Vite production bundle compiles with no errors.
  </how-to-verify>
  <action>
    Pause execution. Wait for the human to perform every numbered verification in the <how-to-verify> block above using `pnpm tauri dev`. Do not proceed to Task 3 (SUMMARY writing) until the user explicitly responds with "approved" (or lists failures to address before approval). If failures are reported, fix them and re-request verification before continuing.

    If MSVC Build Tools are not yet installed (a known blocker logged in STATE.md), the human checkpoint cannot run — flag this in the SUMMARY and request the user resolve the prereq before completing Phase 5.
  </action>
  <verify>
    <automated>echo "Manual checkpoint: see <how-to-verify> for the 25 numbered verifications. Automated suite gate already enforced by Task 1 verify command."</automated>
  </verify>
  <acceptance_criteria>
    - User has typed "approved" (or equivalent) confirming all 25 verification steps passed
    - OR user has listed specific failures and remediation tasks have been completed and re-verified
    - All 5 Phase 5 ROADMAP success criteria observed (top stats correct, faction cards correct, % cards correct, lists work, empty state works)
    - All 8 DASH-* requirements observable in the running app
    - DASH-07 cache invalidation observed end-to-end (mutation in /collection → dashboard updates without refresh)
  </acceptance_criteria>
  <done>
    Human signs off via "approved". Phase 5 user-acceptance-tested end-to-end.
  </done>
  <resume-signal>
    Type "approved" if all 25 verifications pass.
    Otherwise: list the failing item numbers and describe the observed vs expected behavior.
    If MSVC Build Tools / `pnpm tauri dev` cannot run, type "blocked: tauri-dev unavailable" and the SUMMARY will capture this as a deferred sign-off pending dev-environment fix.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write 05-03-SUMMARY.md and prepare STATE.md updates</name>
  <files>.planning/phases/05-dashboard/05-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/05-dashboard/05-00-SUMMARY.md (just created — Wave 0 outputs)
    - .planning/phases/05-dashboard/05-01-SUMMARY.md (just created — data layer outputs)
    - .planning/phases/05-dashboard/05-02-SUMMARY.md (just created — UI assembly outputs)
    - .planning/STATE.md (current progress: 15/16 plans complete; updating to 16/16 after this plan)
    - C:/Users/antoi/.claude/get-shit-done/templates/summary.md (template for SUMMARY structure)
    - .planning/phases/03-collection-module/03-04-SUMMARY.md (reference for "phase-completion" SUMMARY shape — final-plan SUMMARY style)
  </read_first>
  <action>
    Step 1 — Create `.planning/phases/05-dashboard/05-03-SUMMARY.md` with the following structure (fill values from the actual run):

    ```markdown
    # Phase 5 Plan 03 — Dashboard Sign-Off & Phase Closeout

    **Plan:** 05-03 (final plan of Phase 5)
    **Status:** {complete | blocked-on-dev-env}
    **Date:** {YYYY-MM-DD}

    ## What Was Verified

    Human-verify checkpoint exercised against `pnpm tauri dev`. Each of the 25 items in 05-03-PLAN.md `<how-to-verify>` was manually executed.

    | # | Step | Result | Notes |
    |---|------|--------|-------|
    | 1 | DASH-01 stat row labels render | {pass | fail} | |
    | 2 | DASH-01 totalModels matches /collection row count | {pass | fail} | |
    | 3 | DASH-01 fullyPainted matches /collection Completed filter | {pass | fail} | |
    | 4 | DASH-01 activeProjectsCount matches /collection Active filter | {pass | fail} | |
    | 5 | DASH-03/04 percentages displayed | {pass | fail} | |
    | 6 | DASH-02 faction cards rendered (1 per faction) | {pass | fail} | |
    | 7 | DASH-02 faction card content correct | {pass | fail} | |
    | 8 | DASH-02 click-through navigates with faction pre-filter | {pass | fail} | |
    | 9 | DASH-05 active projects list — only is_active_project=1, max 5 | {pass | fail} | |
    | 10 | DASH-05 ordering — most recent first | {pass | fail} | |
    | 11 | DASH-05 row click opens UnitDetailSheet | {pass | fail} | |
    | 12 | DASH-06 recently updated — top 5, with relative time | {pass | fail} | |
    | 13 | DASH-06 relative time updates after edit | {pass | fail} | |
    | 14 | DASH-06 row click opens UnitDetailSheet | {pass | fail} | |
    | 15 | DASH-07 cache freshness — edit unit → dashboard updates | {pass | fail} | |
    | 16 | DASH-07 cache freshness — inline status popover triggers update | {pass | fail} | |
    | 17 | DASH-07 cache freshness — delete reduces totalModels | {pass | fail} | |
    | 18 | DASH-08 empty state renders with no units | {pass | fail} | |
    | 19 | DASH-08 CTA navigates to /collection | {pass | fail} | |
    | 20 | Loading skeletons render briefly on hard-refresh | {pass | fail} | |
    | 21 | POLISH-04 carryover — Sheet remounts per unit | {pass | fail} | |
    | 22 | POLISH-05 carryover — faction colors visible | {pass | fail} | |
    | 23 | Section gap-12 visible | {pass | fail} | |
    | 24 | `pnpm test` exits 0 | {pass | fail} | |
    | 25 | `pnpm build` exits 0 | {pass | fail} | |

    ## Test Counts

    Final phase-gate `pnpm test` output:
    - **Dashboard suite:** ~{N} tests passing (computeStats {N1}, relativeTime {N2}, useDashboardStats {N3}, DashboardPage {N4})
    - **Total project suite:** {M} tests passing, 0 failing
    - **Skipped:** 0 (all Wave-0 stubs filled in)

    ## DASH-07 Invalidation Contract Confirmation

    `src/hooks/useUnits.ts` contains exactly 3 occurrences of `["dashboard-stats"]` (one per mutation onSuccess) — verified by Task 1 grep.

    `src/hooks/useDashboardStats.ts` exports `DASHBOARD_STATS_KEY = ["dashboard-stats"] as const` — verified by Task 1 grep + by `tests/dashboard/useDashboardStats.test.ts`.

    No edits to useUnits.ts were made in Phase 5 (Pitfall 6 of RESEARCH.md respected — "do not add invalidation again").

    ## Files Touched in Phase 5 (Aggregate)

    From plan 05-00 (Wave 0):
    - src/features/dashboard/computeStats.ts
    - src/features/dashboard/relativeTime.ts
    - src/features/dashboard/statusAbbr.ts
    - tests/dashboard/computeStats.test.ts
    - tests/dashboard/relativeTime.test.ts
    - tests/dashboard/DashboardPage.test.tsx (stub → filled in 05-02)

    From plan 05-01 (Wave 1):
    - src/db/queries/dashboard.ts
    - src/hooks/useDashboardStats.ts
    - tests/dashboard/useDashboardStats.test.ts

    From plan 05-02 (Wave 2):
    - src/features/dashboard/StatCard.tsx
    - src/features/dashboard/DashboardListRow.tsx
    - src/features/dashboard/FactionSummaryCard.tsx
    - src/features/dashboard/DashboardEmptyState.tsx
    - src/features/dashboard/DashboardPage.tsx
    - src/app/dashboard/page.tsx (placeholder → real DashboardPage)
    - tests/dashboard/DashboardPage.test.tsx (stubs filled in)

    ## Decisions / Surprises

    {Document anything that came up: deviations from UI-SPEC, edge cases discovered during human-verify, perf observations, etc. If clean run, write "None — Phase 5 executed exactly as planned."}

    ## STATE.md Updates Required

    Update `.planning/STATE.md`:
    - `progress.completed_phases: 5` (was 3, now 5 — Phase 4 also closed at this point)
    - `progress.completed_plans: 16` (was 15, now 16)
    - `progress.percent: 100`
    - `status: completed`
    - `current_position`: "Phase 5 complete — milestone v1.0 ready for /gsd:verify-work"
    - Add decision log entry: `[Phase 05-dashboard]: 05-03: Human-verify approved {date} — all 25 sign-off criteria passed; all 5 Phase 5 success criteria confirmed end-to-end`

    Update `.planning/ROADMAP.md`:
    - Phase 5 entry: `- [x] **Phase 5: Dashboard** ... (completed YYYY-MM-DD)`
    - Phase 5 plans: mark all 4 (`05-00`, `05-01`, `05-02`, `05-03`) as `[x]`
    - Progress table row for Phase 5: `4/4 | Complete | YYYY-MM-DD`

    Update `.planning/REQUIREMENTS.md` Traceability table:
    - DASH-01 through DASH-08: status `Complete`

    ## Milestone v1.0 Closeout

    With Phase 5 sign-off, milestone `v1.0` reaches 100% (16/16 plans complete across 5 phases). Recommended next step: run `/gsd:verify-work` to validate the milestone end-to-end and write the v1.0 retrospective.

    ## Sign-Off

    {User name} approved at {YYYY-MM-DD HH:MM}.
    Resume signal received: "approved".
    ```

    Step 2 — Stage the SUMMARY for commit. Do NOT modify STATE.md, ROADMAP.md, or REQUIREMENTS.md in this plan — those updates are part of the project-level orchestration that the GSD framework's post-execute step performs (typically via `/gsd:execute-phase` finalization). The SUMMARY documents what NEEDS to change so the orchestrator knows exactly what to write.

    Step 3 — Final commit (executor handles via gsd-tools commit hook in the standard flow):
    ```
    node "C:/Users/antoi/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(05-03): complete Phase 5 — dashboard human-verify approved" --files .planning/phases/05-dashboard/05-03-SUMMARY.md
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('.planning/phases/05-dashboard/05-03-SUMMARY.md','utf8');const required=['What Was Verified','Test Counts','DASH-07 Invalidation Contract','Files Touched in Phase 5','STATE.md Updates Required','Sign-Off'];const missing=required.filter(s=>!c.includes(s));if(missing.length){console.error('missing sections:',missing);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-dashboard/05-03-SUMMARY.md` exists
    - SUMMARY contains all 25 numbered checkpoint rows from the human-verify task with pass/fail markers
    - SUMMARY contains literal section "DASH-07 Invalidation Contract Confirmation"
    - SUMMARY contains literal section "Files Touched in Phase 5 (Aggregate)" listing all files from plans 05-00, 05-01, 05-02
    - SUMMARY contains "STATE.md Updates Required" block with completed_phases: 5, completed_plans: 16, percent: 100
    - SUMMARY contains literal section "Sign-Off" with the approval date
    - SUMMARY does NOT contain `{pass | fail}` template placeholders (template tokens are filled with real values)
    - No edits made to STATE.md, ROADMAP.md, or REQUIREMENTS.md by this task (those updates flow through the orchestrator's standard finalization step)
  </acceptance_criteria>
  <done>
    Phase 5 SUMMARY written. Milestone v1.0 ready for `/gsd:verify-work`.
  </done>
</task>

</tasks>

<verification>
- Task 1 verification gate (automated tsc + test + build) green
- Task 2 human-verify checkpoint approved by user (resume-signal "approved")
- Task 3 SUMMARY file exists with no template placeholders
- All 5 Phase 5 ROADMAP success criteria observed in the running app
- All 8 DASH-* requirements observable
</verification>

<success_criteria>
- DASH-07 invalidation contract confirmed end-to-end (mutation in /collection updates dashboard without manual refresh)
- All 5 Phase 5 ROADMAP success criteria pass
- Production build green
- Human sign-off captured in SUMMARY
- Milestone v1.0 reaches 100% — ready for `/gsd:verify-work`
</success_criteria>

<output>
Already covered by Task 3 — `.planning/phases/05-dashboard/05-03-SUMMARY.md` is the phase-closeout deliverable.

After plan 05-03 completes, the GSD orchestrator's post-execute hook will:
- Update `.planning/STATE.md` (progress 16/16, milestone v1.0 complete)
- Update `.planning/ROADMAP.md` (mark Phase 5 + all 4 plans `[x]`)
- Update `.planning/REQUIREMENTS.md` Traceability (DASH-01..08 → Complete)
- Suggest `/gsd:verify-work` as the next command

If MSVC Build Tools are unavailable and the human-verify cannot run: the SUMMARY documents this blocker and Phase 5 is marked `blocked-on-dev-env` instead of `completed`. The user resolves the prereq, re-runs `pnpm tauri dev`, then re-executes this plan's Task 2.
</output>

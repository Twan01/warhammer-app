---
phase: 72-data-layer-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tests/data-layer/db-helpers.ts
  - tests/data-layer/migration-parity.test.ts
  - tests/data-layer/schema-shape.test.ts
autonomous: true
requirements: [TST-01]

must_haves:
  truths:
    - "better-sqlite3 and @types/better-sqlite3 are installed as devDependencies"
    - "A shared helper creates an in-memory SQLite DB with all 23 hobbyforge migrations applied and FK pragma ON"
    - "A shared helper creates an in-memory rules DB with all 3 rules migrations applied"
    - "All 23 hobbyforge migrations execute without errors on a fresh in-memory DB"
    - "All 3 rules migrations execute without errors on a fresh in-memory DB"
    - "Migration count in lib.rs matches the count in the test helper"
    - "Expected tables exist after full migration chain"
    - "Recent migration columns (020, 022, 023) exist with correct nullability"
  artifacts:
    - path: "tests/data-layer/db-helpers.ts"
      provides: "Shared test DB creation helper"
      exports: ["createHobbyforgeDb", "createRulesDb", "HOBBYFORGE_MIGRATION_COUNT", "RULES_MIGRATION_COUNT"]
    - path: "tests/data-layer/migration-parity.test.ts"
      provides: "Migration chain execution tests (D-04, D-05, D-06)"
      contains: "migration parity"
    - path: "tests/data-layer/schema-shape.test.ts"
      provides: "Schema introspection tests (D-12, D-13, D-14)"
      contains: "schema shape"
  key_links:
    - from: "tests/data-layer/db-helpers.ts"
      to: "src-tauri/migrations/*.sql"
      via: "readFileSync resolving migration files from disk"
      pattern: "readFileSync.*migrations"
    - from: "tests/data-layer/migration-parity.test.ts"
      to: "src-tauri/src/lib.rs"
      via: "reading lib.rs to count Migration { entries"
      pattern: "lib.rs"
---

<objective>
Install better-sqlite3 as devDependency, create the shared db-helpers.ts test infrastructure, and write migration parity + schema shape tests.

Purpose: Establish the test foundation that all data-layer tests depend on — the in-memory DB helper and the migration chain validation tests (D-04 through D-06, D-12 through D-14 per CONTEXT.md).

Output: 3 new test files + better-sqlite3 installed
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-data-layer-test-suite/72-CONTEXT.md
@.planning/phases/72-data-layer-test-suite/72-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install better-sqlite3 and create db-helpers.ts</name>
  <read_first>
    - package.json (current devDependencies)
    - src-tauri/src/lib.rs (lines 1-169 — authoritative migration list, both get_migrations and get_rules_migrations)
    - vitest.config.ts (current test configuration)
    - tests/setup.ts (global test setup — understand what runs for all tests)
  </read_first>
  <files>package.json, pnpm-lock.yaml, tests/data-layer/db-helpers.ts</files>
  <action>
    Step 1: Run `pnpm add -D better-sqlite3 @types/better-sqlite3` to install the test SQLite driver (per D-01).

    Step 2: Create tests/data-layer/db-helpers.ts (per D-02, D-03) with these exports:

    - HOBBYFORGE_MIGRATIONS: readonly string array of all 23 migration filenames in version order (001_core_schema.sql through 023_session_section_fk.sql). Must match exactly what lib.rs get_migrations() registers.
    - RULES_MIGRATIONS: readonly string array of all 3 rules migration filenames (rules_001_schema.sql through rules_003_sync_meta_counts.sql). Must match lib.rs get_rules_migrations().
    - HOBBYFORGE_MIGRATION_COUNT: number constant = 23
    - RULES_MIGRATION_COUNT: number constant = 3
    - createHobbyforgeDb(): creates a better-sqlite3 Database with `:memory:`, sets `PRAGMA foreign_keys = ON`, reads each hobbyforge migration SQL file via readFileSync from src-tauri/migrations/ directory, executes each via db.exec(sql), then verifies FK pragma is still ON after migration 022 toggles it. Returns the ready Database. Use resolve(dirname(fileURLToPath(import.meta.url)), "..", "..") for repoRoot — same pattern as existing test files.
    - createRulesDb(): same pattern but uses RULES_MIGRATIONS array and a separate in-memory DB. No FK pragma needed for rules DB.
    - Factory helpers for test convenience: createTestFaction(db) inserts a faction row and returns its id. createTestUnit(db, factionId) inserts a unit row (painting_sessions.unit_id is NOT NULL, so tests need real units). createTestRecipe(db) inserts a painting_recipes row and returns its id. createTestSection(db, recipeId, name) inserts a recipe_sections row and returns its id.

    The file must start with `// @vitest-environment node` comment to avoid jsdom overhead (per RESEARCH.md Pattern 2).

    Important: painting_sessions.unit_id is NOT NULL (from migration 005). Factory helpers must create prerequisite rows (faction -> unit) before sessions can be inserted.

    For the faction insert, use seed-independent values: INSERT INTO factions (name, game_system) VALUES ('Test Faction', 'Warhammer 40K'). For unit: INSERT INTO units (faction_id, name, status_painting) VALUES (?, 'Test Unit', 'Not Started'). For recipe: INSERT INTO painting_recipes (name) VALUES ('Test Recipe'). For section: INSERT INTO recipe_sections (recipe_id, name, sort_order) VALUES (?, ?, 0).
  </action>
  <verify>
    <automated>pnpm test -- tests/data-layer/db-helpers.ts 2>&1 || echo "Helper is not a test file - verify by importing in next task"</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains "better-sqlite3" and "@types/better-sqlite3"
    - tests/data-layer/db-helpers.ts exports createHobbyforgeDb, createRulesDb, HOBBYFORGE_MIGRATION_COUNT (23), RULES_MIGRATION_COUNT (3)
    - tests/data-layer/db-helpers.ts exports createTestFaction, createTestUnit, createTestRecipe, createTestSection
    - HOBBYFORGE_MIGRATIONS array has exactly 23 entries matching the filenames in lib.rs get_migrations()
    - RULES_MIGRATIONS array has exactly 3 entries matching lib.rs get_rules_migrations()
    - createHobbyforgeDb sets PRAGMA foreign_keys = ON and verifies it is still ON after full chain
    - File starts with // @vitest-environment node
  </acceptance_criteria>
  <done>better-sqlite3 installed, db-helpers.ts created with all exports, helper compiles without TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Write migration-parity and schema-shape tests</name>
  <read_first>
    - tests/data-layer/db-helpers.ts (just created — imports and factory functions)
    - src-tauri/src/lib.rs (lines 1-169 — need to count Migration { entries for D-06)
    - src-tauri/migrations/020_workflow_metadata.sql (columns to verify in D-13)
    - src-tauri/migrations/022_paintless_steps.sql (paint_id nullability to verify in D-13)
    - src-tauri/migrations/023_session_section_fk.sql (recipe_section_id column to verify in D-13)
  </read_first>
  <files>tests/data-layer/migration-parity.test.ts, tests/data-layer/schema-shape.test.ts</files>
  <action>
    Create migration-parity.test.ts (per D-04, D-05, D-06) with `// @vitest-environment node` header:

    describe("migration parity"):
    - Test "all 23 hobbyforge migrations execute without errors" (D-04): call createHobbyforgeDb(), close db. The function throws if any migration SQL fails.
    - Test "all 3 rules migrations execute without errors" (D-05): call createRulesDb(), close db.
    - Test "lib.rs migration count matches helper count" (D-06): read src-tauri/src/lib.rs via readFileSync, count occurrences of regex /Migration\s*\{/g. The total should equal HOBBYFORGE_MIGRATION_COUNT + RULES_MIGRATION_COUNT (26). Import repoRoot calculation or compute it locally.
    - Test "PRAGMA foreign_keys is ON after migration chain" (verifies migration 022 FK toggle is properly restored): call createHobbyforgeDb(), run db.pragma("foreign_keys"), expect result[0].foreign_keys === 1.

    Create schema-shape.test.ts (per D-12, D-13, D-14) with `// @vitest-environment node` header:

    Define a ColumnInfo interface: { cid: number; name: string; type: string; notnull: number; dflt_value: string | null; pk: number }.

    describe("schema shape"):
    - Test "expected tables exist after full migration chain" (D-12): run createHobbyforgeDb(), query `SELECT name FROM sqlite_master WHERE type = 'table' AND name NOT LIKE '_sqlx%' ORDER BY name`. Assert the result contains at minimum these tables: factions, units, paints, painting_recipes, recipe_paints, army_lists, army_list_units, unit_strategy_notes, battle_logs, image_assets, painting_sessions, recipe_steps, recipe_step_paints, recipe_step_photos, recipe_sections, unit_recipe_assignments, unit_recipe_step_progress. Use expect(tableNames).toContain() for each.
    - Test "recipe_sections has workflow metadata columns" (D-13 partial — migration 020): run PRAGMA table_info(recipe_sections), assert columns section_type, technique, execution_mode, applies_to exist and all have notnull === 0 (nullable).
    - Test "recipe_step_paints.paint_id is nullable" (D-13 partial — migration 022): run PRAGMA table_info(recipe_step_paints), find paint_id column, assert notnull === 0.
    - Test "painting_sessions.recipe_section_id exists" (D-13 partial — migration 023): run PRAGMA table_info(painting_sessions), find recipe_section_id column, assert it exists and notnull === 0 (nullable FK).
    - Test "rules DB tables exist" (D-12 for rules): run createRulesDb(), query sqlite_master, assert datasheets table exists (core rules table).

    Use PRAGMA table_info() for all column introspection (per D-14). Cast pragma results as ColumnInfo[].
  </action>
  <verify>
    <automated>pnpm test -- tests/data-layer/migration-parity.test.ts tests/data-layer/schema-shape.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - migration-parity.test.ts has 4 tests: hobbyforge chain (D-04), rules chain (D-05), count match (D-06), FK pragma check
    - schema-shape.test.ts has 5 tests: table existence (D-12), workflow metadata columns, paint_id nullable, recipe_section_id exists, rules tables
    - All 9 tests pass with `pnpm test -- tests/data-layer/`
    - Both files start with // @vitest-environment node
    - D-06 test reads lib.rs from disk and counts Migration { occurrences
  </acceptance_criteria>
  <done>Migration parity tests verify all 26 migrations run cleanly; schema shape tests verify tables and columns from migrations 020, 022, 023 exist with correct constraints; all tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code -> filesystem | Tests read migration SQL files from src-tauri/migrations/ via readFileSync |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | T (Tampering) | Migration SQL files read from disk | accept | Tests run in dev environment only; migration files are version-controlled. No production impact. |
</threat_model>

<verification>
All tests in tests/data-layer/ pass:
```
pnpm test -- tests/data-layer/
```
</verification>

<success_criteria>
- better-sqlite3 installed as devDependency
- db-helpers.ts provides createHobbyforgeDb and createRulesDb with FK enforcement
- 4 migration parity tests pass (D-04, D-05, D-06, FK check)
- 5 schema shape tests pass (D-12, D-13 x3, rules tables)
</success_criteria>

<output>
After completion, create `.planning/phases/72-data-layer-test-suite/72-01-SUMMARY.md`
</output>

---
phase: 77-data-health-page-backup-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
  - src/db/queries/diagnostics.ts
  - src/hooks/useDiagnostics.ts
  - src/app/router.tsx
  - src/components/common/AppSidebar.tsx
autonomous: true
requirements:
  - DX-01
  - DX-02
  - DX-03
  - BK-01
  - BK-02

must_haves:
  truths:
    - "A Rust backup_database command exists and compiles"
    - "Diagnostic queries return typed results for table counts, orphaned rows, ambiguous matches, and stale sync"
    - "React Query hooks expose table counts, diagnostic flags, schema versions, and backup status independently"
    - "The /data-health route is registered and the sidebar has a Data Health link"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "backup_database Tauri command using VACUUM INTO"
      contains: "fn backup_database"
    - path: "src-tauri/capabilities/default.json"
      provides: "dialog:allow-save permission"
      contains: "dialog:allow-save"
    - path: "src/db/queries/diagnostics.ts"
      provides: "getTableCounts, getOrphanedProgressRows, getAmbiguousPointMatches, getSchemaVersions"
      exports: ["getTableCounts", "getOrphanedProgressRows", "getAmbiguousPointMatches", "getSchemaVersions"]
    - path: "src/hooks/useDiagnostics.ts"
      provides: "useTableCounts, useDiagnosticFlags, useBackupStatus, useSchemaVersions"
      exports: ["useTableCounts", "useDiagnosticFlags", "useBackupStatus", "useSchemaVersions"]
    - path: "src/app/router.tsx"
      provides: "/data-health route"
      contains: "dataHealthRoute"
    - path: "src/components/common/AppSidebar.tsx"
      provides: "Data Health sidebar link"
      contains: "data-health"
  key_links:
    - from: "src/hooks/useDiagnostics.ts"
      to: "src/db/queries/diagnostics.ts"
      via: "queryFn imports"
      pattern: "import.*diagnostics"
    - from: "src/app/router.tsx"
      to: "src/app/data-health/page.tsx"
      via: "lazy component import"
      pattern: "data-health"
---

<objective>
Build the backend backup command, diagnostics data layer, React Query hooks, and route/navigation wiring for the Data Health page.

Purpose: Establish all data plumbing and infrastructure so that Plan 02 can focus purely on assembling UI components against working hooks and a functional backup command.
Output: Rust backup_database command, diagnostics.ts query module, useDiagnostics.ts hooks, route registration, sidebar link.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-data-health-page-backup-export/77-CONTEXT.md
@.planning/phases/77-data-health-page-backup-export/77-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/db/client.ts:
```typescript
export async function getDb(): Promise<Database>;
```

From src/db/rules-client.ts:
```typescript
export async function getRulesDb(): Promise<Database>;
```

From src/lib/syncFreshness.ts:
```typescript
export type SyncFreshness = "fresh" | "aging" | "stale" | "never";
export function getSyncFreshness(lastSyncAt: string | null): SyncFreshness;
export function getSyncAgeLabel(lastSyncAt: string | null): string;
export const FRESHNESS_DOT_CLASS: Record<SyncFreshness, string>;
```

From src/hooks/useDatasheet.ts:
```typescript
export const RULES_SYNC_META_KEY = ["rules-sync-meta"] as const;
export function useRulesSyncMeta();
```

From src/hooks/useSyncErrors.ts:
```typescript
export const SYNC_ERRORS_KEY = ["sync-errors"] as const;
export function useRulesSyncErrors();
```

From src/features/dashboard/StatCard.tsx:
```typescript
export interface StatCardProps {
  value: number | string;
  label: string;
  animate?: boolean;
  icon?: ComponentType<{ size?: number; className?: string }>;
  trend?: { value: number; label: string };
  progress?: number;
  to?: string;
}
export function StatCard(props: StatCardProps);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rust backup_database command + capability permission</name>
  <files>src-tauri/src/lib.rs, src-tauri/capabilities/default.json</files>
  <read_first>
    - src-tauri/src/lib.rs (current state -- bulk_sync_rules pattern to replicate, invoke_handler macro at bottom)
    - src-tauri/capabilities/default.json (current permissions list)
    - .planning/phases/77-data-health-page-backup-export/77-RESEARCH.md (Pattern 1: Rust Command, Pitfall 1+2)
  </read_first>
  <action>
    **lib.rs changes (per D-03):**
    Add a new `backup_database` async Tauri command above the `run()` function. Follow the exact connection pattern from `bulk_sync_rules`:
    - Signature: `async fn backup_database(app: tauri::AppHandle, destination: String) -> Result<(), String>`
    - Use `sqlx::sqlite::SqliteConnectOptions`, `ConnectOptions`, `Connection` from sqlx (same imports as bulk_sync_rules)
    - Resolve app_data_dir via `app.path().app_data_dir()`, build db_url as `sqlite:{app_data_dir}/hobbyforge.db`
    - Create SqliteConnectOptions with `.create_if_missing(false)` -- no WAL mode or busy_timeout needed for a read-only VACUUM
    - Connect via `opts.connect().await`
    - Handle file-already-exists: before executing VACUUM INTO, attempt `std::fs::remove_file(&destination)` and ignore NotFound errors (the save dialog on Windows confirms overwrite, so we remove the old file to avoid VACUUM INTO's "file already exists" error per Pitfall 2)
    - Execute `VACUUM INTO` using string interpolation with single-quote escaping: `format!("VACUUM INTO '{}'", destination.replace('\'', "''"))` -- NOTE: sqlx parameterized binding may not work for VACUUM INTO's filename argument per Assumption A2
    - Map all errors to String via `.map_err(|e| format!("..."))`
    - Return `Ok(())`

    Register the command in the `invoke_handler` macro: change `tauri::generate_handler![bulk_sync_rules]` to `tauri::generate_handler![bulk_sync_rules, backup_database]`

    **capabilities/default.json changes (per D-05):**
    Add `"dialog:allow-save"` to the permissions array, after the existing `"dialog:allow-open"` entry.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && cd src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/lib.rs contains `async fn backup_database(app: tauri::AppHandle, destination: String) -> Result<(), String>`
    - The function body includes `VACUUM INTO` string with single-quote escaping
    - The function body includes `std::fs::remove_file` before VACUUM INTO
    - invoke_handler macro includes both `bulk_sync_rules` and `backup_database`
    - src-tauri/capabilities/default.json permissions array contains `"dialog:allow-save"`
    - `cargo check` succeeds with no errors
  </acceptance_criteria>
  <done>backup_database command compiles and is registered; dialog:allow-save permission is declared</done>
</task>

<task type="auto">
  <name>Task 2: Diagnostics query module + React Query hooks + route/nav wiring</name>
  <files>src/db/queries/diagnostics.ts, src/hooks/useDiagnostics.ts, src/app/router.tsx, src/components/common/AppSidebar.tsx, src/app/data-health/page.tsx</files>
  <read_first>
    - src/db/queries/datasheets.ts (query module pattern -- SELECT syntax, getDb/getRulesDb usage, return types)
    - src/hooks/useDatasheet.ts (hook pattern -- KEY constants, useQuery options, staleTime)
    - src/hooks/useSyncErrors.ts (hook pattern -- simple hook wrapping query)
    - src/lib/syncFreshness.ts (getSyncFreshness, getSyncAgeLabel for stale detection reuse)
    - src/app/router.tsx (current route tree -- import pattern, createRoute, routeTree.addChildren)
    - src/components/common/AppSidebar.tsx (MANAGEMENT_NAV array, NavItem icon pattern)
    - .planning/phases/77-data-health-page-backup-export/77-CONTEXT.md (D-07 through D-15)
  </read_first>
  <action>
    **src/db/queries/diagnostics.ts (new file, per D-07 through D-11, D-13):**

    Export interfaces:
    - `TableCounts` with fields: `units: number`, `painting_recipes: number`, `unit_recipe_assignments: number`, `unit_recipe_step_progress: number`, `synced_unit_points: number`
    - `DiagnosticFlag` with fields: `type: string`, `count: number`, `description: string`, `severity: "warning" | "info"`
    - `SchemaVersions` with fields: `hobbyforge: number`, `rules: number`

    Export functions:
    - `getTableCounts(): Promise<TableCounts>` -- five separate `SELECT COUNT(*) as c FROM {table}` queries via `getDb()`, one per table listed in D-11 (units, painting_recipes, unit_recipe_assignments, unit_recipe_step_progress, synced_unit_points)
    - `getSchemaVersions(): Promise<SchemaVersions>` -- run `PRAGMA user_version` on both `getDb()` and `getRulesDb()`, return `{ hobbyforge: result.user_version, rules: result.user_version }` per D-13. Note: the column name from PRAGMA may vary -- if `user_version` key is not present, try reading first key of the result object
    - `getOrphanedProgressRows(): Promise<DiagnosticFlag | null>` -- per D-08: `SELECT COUNT(*) as c FROM unit_recipe_step_progress p LEFT JOIN recipe_steps rs ON rs.id = p.recipe_step_id WHERE rs.id IS NULL`. Return null when count is 0, otherwise return flag with type `"orphaned_progress"`, severity `"warning"`, description `"{n} orphaned progress rows -- tracking completion for steps that no longer exist"`
    - `getAmbiguousPointMatches(): Promise<DiagnosticFlag | null>` -- per D-09: query `getDb()` for units with synced_unit_points (`SELECT u.id, u.name FROM units u JOIN synced_unit_points sup ON sup.unit_id = u.id`), query `getRulesDb()` for all `rw_datasheet_points` entries, compare in JavaScript: count units whose name (case-insensitive) matches zero OR more than one datasheet_name. Return null when count is 0, otherwise severity `"warning"`, type `"ambiguous_points"`, description `"{n} units have ambiguous or missing point matches"`
    - `getDiagnosticFlags(): Promise<DiagnosticFlag[]>` -- calls getOrphanedProgressRows and getAmbiguousPointMatches, filters nulls, returns the array. Stale sync detection is handled by reusing `useRulesSyncMeta` in the UI layer per D-10/D-14 rather than duplicating the query here

    **src/hooks/useDiagnostics.ts (new file, per D-12):**

    Export query key constants:
    - `TABLE_COUNTS_KEY = ["diagnostics", "table-counts"] as const`
    - `DIAGNOSTIC_FLAGS_KEY = ["diagnostics", "flags"] as const`
    - `SCHEMA_VERSIONS_KEY = ["diagnostics", "schema-versions"] as const`

    Export hooks:
    - `useTableCounts()` -- `useQuery({ queryKey: TABLE_COUNTS_KEY, queryFn: getTableCounts })`
    - `useDiagnosticFlags()` -- `useQuery({ queryKey: DIAGNOSTIC_FLAGS_KEY, queryFn: getDiagnosticFlags })`
    - `useSchemaVersions()` -- `useQuery({ queryKey: SCHEMA_VERSIONS_KEY, queryFn: getSchemaVersions })`
    - `useBackupStatus()` -- per D-06: reads `localStorage.getItem("lastBackup")`, parses JSON, returns `{ date: string; path: string; success: boolean } | null`. This is NOT a useQuery hook -- it is a simple function that returns the parsed value. Export the `BACKUP_STORAGE_KEY = "lastBackup"` constant as well for use by the backup action.

    **src/app/data-health/page.tsx (new stub file, per D-01):**
    Create a minimal placeholder component `DataHealthPage` that renders a `div` with text "Data Health" -- Plan 02 will replace this with the full UI. Export as named export `DataHealthPage`.

    **src/app/router.tsx changes (per D-01):**
    Import `DataHealthPage` from `"./data-health/page"`. Create `dataHealthRoute` using `createRoute({ getParentRoute: () => rootRoute, path: "/data-health", component: DataHealthPage })`. Add `dataHealthRoute` to the `routeTree.addChildren` array.

    **src/components/common/AppSidebar.tsx changes (per D-02):**
    Import `HeartPulse` from `"lucide-react"`. Add `{ to: "/data-health", label: "Data Health", icon: HeartPulse }` as the last item in the `MANAGEMENT_NAV` array.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/db/queries/diagnostics.ts exports: getTableCounts, getSchemaVersions, getOrphanedProgressRows, getAmbiguousPointMatches, getDiagnosticFlags, TableCounts, DiagnosticFlag, SchemaVersions
    - getOrphanedProgressRows uses LEFT JOIN recipe_steps WHERE rs.id IS NULL
    - getAmbiguousPointMatches queries both getDb() and getRulesDb() separately and compares in JS
    - src/hooks/useDiagnostics.ts exports: useTableCounts, useDiagnosticFlags, useSchemaVersions, useBackupStatus, TABLE_COUNTS_KEY, DIAGNOSTIC_FLAGS_KEY, SCHEMA_VERSIONS_KEY, BACKUP_STORAGE_KEY
    - useBackupStatus reads from localStorage key "lastBackup"
    - src/app/data-health/page.tsx exports DataHealthPage
    - src/app/router.tsx imports DataHealthPage and registers /data-health route in routeTree children
    - src/components/common/AppSidebar.tsx MANAGEMENT_NAV includes { to: "/data-health", label: "Data Health", icon: HeartPulse }
    - TypeScript compiles with no errors (npx tsc --noEmit)
  </acceptance_criteria>
  <done>Diagnostics data layer, hooks, route, and sidebar link all compile and are wired; Plan 02 can build UI against these contracts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User file path -> VACUUM INTO SQL | File path from save dialog enters SQL statement |
| DB query results -> UI | Diagnostic counts rendered in page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Tampering | backup_database VACUUM INTO path | mitigate | Single-quote escaping in format! string; save dialog constrains path to valid OS locations |
| T-77-02 | Tampering | diagnostics.ts SQL queries | accept | All queries are read-only SELECTs with no user input parameters; no injection surface |
| T-77-03 | Information Disclosure | backup file contents | accept | Local desktop app, single user; backup file contains same data user already has access to |
</threat_model>

<verification>
1. `cargo check` passes in src-tauri (Rust compiles)
2. `npx tsc --noEmit` passes (TypeScript compiles)
3. Navigating to /data-health shows placeholder page
4. Sidebar Management group includes "Data Health" link with HeartPulse icon
</verification>

<success_criteria>
- backup_database Rust command compiles and is registered in invoke_handler
- dialog:allow-save permission is declared in capabilities
- Diagnostics query module provides typed functions for table counts, schema versions, orphaned rows, ambiguous matches
- React Query hooks load independently (separate query keys)
- /data-health route is registered and navigable
- Sidebar Management group shows Data Health link
</success_criteria>

<output>
After completion, create `.planning/phases/77-data-health-page-backup-export/77-01-SUMMARY.md`
</output>

---
phase: 74-applied-recipe-identity-hardening
plan: 02
type: execute
wave: 2
depends_on: [74-01]
files_modified:
  - src/hooks/useRecipeAssignments.ts
  - src/features/recipes/AssignmentChecklist.tsx
  - src/features/dashboard/LogSessionSheet.tsx
  - tests/lib/computeAssignmentProgress.test.ts
  - tests/applied-recipes/assignmentChecklist.test.tsx
autonomous: true
requirements: [DI-01, DI-02]

must_haves:
  truths:
    - "useToggleStepProgress mutation passes recipeStepId instead of orderIndex"
    - "AssignmentChecklist builds completedSet from recipe_step_id and passes step.id to toggle"
    - "LogSessionSheet passes step.id instead of step.order_index when auto-marking a step"
    - "Tests for computeAssignmentProgress use step.id and recipe_step_id fixtures"
    - "Tests for AssignmentChecklist assert recipeStepId in toggle call"
    - "Full test suite passes"
  artifacts:
    - path: "src/hooks/useRecipeAssignments.ts"
      provides: "Updated mutation params"
      contains: "recipeStepId"
    - path: "src/features/recipes/AssignmentChecklist.tsx"
      provides: "Updated checklist UI"
      contains: "step.id"
    - path: "src/features/dashboard/LogSessionSheet.tsx"
      provides: "Updated session logging"
      contains: "recipeStepId"
    - path: "tests/lib/computeAssignmentProgress.test.ts"
      provides: "Updated pure function tests"
      contains: "recipe_step_id"
    - path: "tests/applied-recipes/assignmentChecklist.test.tsx"
      provides: "Updated component tests"
      contains: "recipeStepId"
  key_links:
    - from: "src/features/recipes/AssignmentChecklist.tsx"
      to: "src/hooks/useRecipeAssignments.ts"
      via: "useToggleStepProgress mutation call"
      pattern: "recipeStepId"
    - from: "src/features/dashboard/LogSessionSheet.tsx"
      to: "src/hooks/useRecipeAssignments.ts"
      via: "toggleStepProgress.mutateAsync call"
      pattern: "recipeStepId"
    - from: "src/features/recipes/AssignmentChecklist.tsx"
      to: "src/lib/computeAssignmentProgress.ts"
      via: "computeAssignmentProgress(steps, stepProgressRows)"
      pattern: "computeAssignmentProgress"
---

<objective>
Update the hook, UI consumer, and test layers to use recipe_step_id instead of order_index, completing the full identity hardening from database through UI.

Purpose: After this plan, every layer that reads or writes step progress uses recipe_step_id as the key, making the system resilient to step reordering.

Output: Updated hook, two UI consumers (AssignmentChecklist, LogSessionSheet), and two test files updated to match new signatures.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-applied-recipe-identity-hardening/74-CONTEXT.md
@.planning/phases/74-applied-recipe-identity-hardening/74-01-SUMMARY.md

<interfaces>
<!-- Post-plan-01 types (the executor will see these after plan 01 completes) -->

From src/types/recipeAssignment.ts (after plan 01):
```typescript
export interface StepProgress {
  id: number;
  assignment_id: number;
  recipe_step_id: number;   // WAS order_index
  completed: number;
  completed_at: string | null;
}
```

From src/db/queries/recipeAssignments.ts (after plan 01):
```typescript
export async function upsertStepProgress(
  assignmentId: number,
  recipeStepId: number,     // WAS orderIndex
  completed: boolean,
): Promise<void>;
```

From src/lib/computeAssignmentProgress.ts (after plan 01):
```typescript
export function computeAssignmentProgress(
  steps: ReadonlyArray<{ id: number; section_id: number | null }>,       // WAS order_index
  progress: ReadonlyArray<{ recipe_step_id: number; completed: number }>, // WAS order_index
): AssignmentProgress;
```

From src/types/recipePaint.ts (unchanged):
```typescript
export interface RecipeStep {
  id: number;
  recipe_id: number;
  order_index: number;
  section_id: number | null;
  step_name: string;
  // ...
}
```

From src/hooks/useRecipeAssignments.ts (CURRENT -- to be modified):
```typescript
export function useToggleStepProgress() {
  // mutation type: { assignmentId: number; orderIndex: number; completed: boolean }
  // calls upsertStepProgress(assignmentId, orderIndex, completed)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update hook and UI consumers</name>
  <files>src/hooks/useRecipeAssignments.ts, src/features/recipes/AssignmentChecklist.tsx, src/features/dashboard/LogSessionSheet.tsx</files>
  <read_first>
    src/hooks/useRecipeAssignments.ts
    src/features/recipes/AssignmentChecklist.tsx
    src/features/dashboard/LogSessionSheet.tsx
    src/types/recipeAssignment.ts
    src/db/queries/recipeAssignments.ts
  </read_first>
  <action>
    Update three files per D-09, D-12, and Research Pitfall 3:

    1. src/hooks/useRecipeAssignments.ts -- useToggleStepProgress (line 91-99):
       - Change mutation type from { assignmentId: number; orderIndex: number; completed: boolean } to { assignmentId: number; recipeStepId: number; completed: boolean }
       - Change mutationFn destructure from ({ assignmentId, orderIndex, completed }) to ({ assignmentId, recipeStepId, completed })
       - Change call from upsertStepProgress(assignmentId, orderIndex, completed) to upsertStepProgress(assignmentId, recipeStepId, completed)
       - The onSuccess invalidation remains unchanged (still invalidates STEP_PROGRESS_KEY)

    2. src/features/recipes/AssignmentChecklist.tsx (per D-12):
       - Line 31: Change completedSet from mapping p.order_index to mapping p.recipe_step_id
       - Line 53: Change handleToggle parameter from (orderIndex: number, checked: boolean) to (recipeStepId: number, checked: boolean)
       - Line 54-58: Change mutation call from { assignmentId, orderIndex, completed } to { assignmentId, recipeStepId, completed }
       - Line 88 (key): Change key={step.order_index} to key={step.id} in the sectioned accordion step divs
       - Lines 92-93: Change completedSet.has(step.order_index) to completedSet.has(step.id)
       - Lines 93-95: Change handleToggle(step.order_index, ...) to handleToggle(step.id, ...)
       - Lines 99-100: Change completedSet.has(step.order_index) to completedSet.has(step.id) in className
       - Same pattern for the flat list branch (lines 116-131): key, completedSet.has, handleToggle all switch from step.order_index to step.id

    3. src/features/dashboard/LogSessionSheet.tsx (Research Pitfall 3):
       - Around line 206-209: Change the toggleStepProgress.mutateAsync call from { assignmentId, orderIndex: step.order_index, completed: true } to { assignmentId, recipeStepId: step.id, completed: true }
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - useToggleStepProgress mutation type contains recipeStepId: number (not orderIndex)
    - useToggleStepProgress mutationFn calls upsertStepProgress(assignmentId, recipeStepId, completed)
    - AssignmentChecklist completedSet maps p.recipe_step_id (not p.order_index)
    - AssignmentChecklist handleToggle parameter is recipeStepId (not orderIndex)
    - AssignmentChecklist JSX uses step.id for key, completedSet.has, and handleToggle calls (not step.order_index)
    - LogSessionSheet line ~208 passes recipeStepId: step.id (not orderIndex: step.order_index)
    - No remaining references to order_index in AssignmentChecklist.tsx or the useToggleStepProgress function
    - pnpm build succeeds
  </acceptance_criteria>
  <done>All three UI/hook consumers use recipe_step_id/step.id for step progress identity. TypeScript build passes with no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update tests</name>
  <files>tests/lib/computeAssignmentProgress.test.ts, tests/applied-recipes/assignmentChecklist.test.tsx</files>
  <read_first>
    tests/lib/computeAssignmentProgress.test.ts
    tests/applied-recipes/assignmentChecklist.test.tsx
    src/lib/computeAssignmentProgress.ts
    src/types/recipeAssignment.ts
  </read_first>
  <action>
    Update two test files to match the new type signatures:

    1. tests/lib/computeAssignmentProgress.test.ts:
       - TestStep interface: replace order_index: number with id: number (keep section_id: number | null)
       - TestProgress interface: replace order_index: number with recipe_step_id: number (keep completed: number)
       - All test fixture data: replace order_index values with id values in TestStep arrays (e.g., { order_index: 0, section_id: null } becomes { id: 1, section_id: null }). Use step ids 1,2,3,4... instead of order indices 0,1,2,3...
       - All test fixture data: replace order_index values with recipe_step_id values in TestProgress arrays (e.g., { order_index: 0, completed: 1 } becomes { recipe_step_id: 1, completed: 1 }). The recipe_step_id values must match the corresponding step ids.
       - The "ignores progress records for order_index values not in steps" test becomes "ignores progress records for recipe_step_id values not in steps" -- stale progress with recipe_step_id 99 or 100 (not matching any step id)
       - Update test descriptions from "order_index" references to "step id" or "recipe_step_id" where they appear in test names

    2. tests/applied-recipes/assignmentChecklist.test.tsx:
       - mockSteps array entries: add id field (e.g., { id: 1, order_index: 0, section_id: 1, step_name: "Base Silver" }). Note: order_index stays because RecipeStep still has it (it's just no longer used for progress keying). But id must be added since the component now uses step.id.
       - makeStepProgress factory: replace order_index field with recipe_step_id field in the default values and the StepProgress type
       - mockStepProgress setup in beforeEach: use recipe_step_id matching the step ids (e.g., makeStepProgress({ id: 1, recipe_step_id: 1, completed: 1 }))
       - The "calls toggleStep on checkbox click" test assertion: change from expect(mockToggleMutate).toHaveBeenCalledWith({ assignmentId: 1, orderIndex: 0, completed: false }) to expect(mockToggleMutate).toHaveBeenCalledWith({ assignmentId: 1, recipeStepId: 1, completed: false }) where 1 is the id of the first step ("Base Silver")
  </action>
  <verify>
    <automated>pnpm test -- tests/lib/computeAssignmentProgress.test.ts tests/applied-recipes/assignmentChecklist.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TestStep interface in computeAssignmentProgress.test.ts has id: number (not order_index)
    - TestProgress interface has recipe_step_id: number (not order_index)
    - All step fixture arrays use id field with values 1,2,3... (not order_index 0,1,2...)
    - All progress fixture arrays use recipe_step_id matching step ids
    - makeStepProgress factory in assignmentChecklist.test.tsx returns recipe_step_id (not order_index)
    - mockSteps entries include id field
    - Assertion in "calls toggleStep on checkbox click" expects recipeStepId (not orderIndex)
    - Both test files pass: pnpm test -- tests/lib/computeAssignmentProgress.test.ts tests/applied-recipes/assignmentChecklist.test.tsx
    - Full suite passes: pnpm test
  </acceptance_criteria>
  <done>Both test files updated to use recipe_step_id/step.id fixtures and assertions. All tests pass, confirming the full identity hardening is correct end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click -> mutation -> DB | Checkbox toggle sends recipe_step_id to upsert; must be a valid step id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-04 | Tampering | upsertStepProgress | mitigate | FK constraint on recipe_step_id REFERENCES recipe_steps(id) prevents writing progress for non-existent steps; UNIQUE(assignment_id, recipe_step_id) prevents duplicates |
| T-74-05 | Repudiation | Step toggle | accept | completed_at timestamp records when toggle occurred; sufficient for hobby app audit trail |
</threat_model>

<verification>
1. pnpm build passes (full TypeScript compilation)
2. pnpm test passes (all unit and component tests)
3. No references to orderIndex or order_index remain in useToggleStepProgress, AssignmentChecklist, or LogSessionSheet step progress paths
4. grep -r "orderIndex" src/hooks/useRecipeAssignments.ts returns no matches
5. grep -r "order_index" src/features/recipes/AssignmentChecklist.tsx returns no matches (aside from RecipeStep type which retains order_index for display ordering)
</verification>

<success_criteria>
All UI consumers and hooks use recipe_step_id. All tests updated and passing. Full test suite green. Zero references to order_index in the step progress data path.
</success_criteria>

<output>
After completion, create `.planning/phases/74-applied-recipe-identity-hardening/74-02-SUMMARY.md`
</output>

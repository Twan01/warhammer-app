---
phase: 75-transactional-recipe-graph-save
plan: 02
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - src/features/recipes/RecipeFormSheet.tsx
autonomous: true
requirements:
  - DI-03
  - DI-04

must_haves:
  truths:
    - "D-06: RecipeFormSheet onSubmit calls saveRecipeGraph() directly as its sole DB operation"
    - "No individual mutation hooks (createRecipe, updateRecipe, addRecipePaint) are called in onSubmit"
    - "D-08: React Query cache is invalidated once after the transaction succeeds, not per-operation"
    - "D-04: The form stays open with data intact on error so the user can retry"
    - "D-07: Existing CRUD hooks remain available for non-form callers"
  artifacts:
    - path: "src/features/recipes/RecipeFormSheet.tsx"
      provides: "Refactored onSubmit using saveRecipeGraph()"
      contains: "saveRecipeGraph"
  key_links:
    - from: "src/features/recipes/RecipeFormSheet.tsx"
      to: "src/db/queries/recipes.ts"
      via: "import saveRecipeGraph"
      pattern: "import.*saveRecipeGraph.*recipes"
    - from: "src/features/recipes/RecipeFormSheet.tsx"
      to: "React Query cache"
      via: "qc.invalidateQueries after saveRecipeGraph resolves"
      pattern: "qc\\.invalidateQueries"
---

<objective>
Wire `RecipeFormSheet.tsx` `onSubmit` to call `saveRecipeGraph()` instead of the
current chain of individual mutation hooks and direct CRUD calls, followed by a
single batch of React Query cache invalidations.

Purpose: Completes the transactional save integration — the UI now triggers atomic
saves and receives consistent cache state after success.

Output: Refactored RecipeFormSheet.tsx with simplified onSubmit.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-transactional-recipe-graph-save/75-CONTEXT.md
@.planning/phases/75-transactional-recipe-graph-save/75-PATTERNS.md
@.planning/phases/75-transactional-recipe-graph-save/75-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->

From src/db/queries/recipes.ts (created in Plan 01):
- export async function saveRecipeGraph(recipeId: number | null, formValues: RecipeFormValues, sections: DraftSection[], existingSections: RecipeSection[], existingSteps: RecipeStep[]): Promise<number>

From src/hooks/useRecipes.ts:
- export const RECIPES_KEY = ["recipes"] as const
- export const RECIPE_KEY = (id: number) => ["recipes", id] as const

From src/hooks/useRecipeSections.ts:
- export const RECIPE_SECTIONS_KEY = (recipeId: number) => ["recipe-sections", recipeId] as const
- export const SECTION_COUNTS_KEY = ["section-counts"] as const

From src/hooks/useRecipePaints.ts:
- export const RECIPE_PAINTS_KEY = (recipeId: number) => ["recipe-paints", recipeId] as const
- export const STEP_COUNTS_KEY = ["recipe-step-counts"] as const
- export const RECIPE_AVAILABILITY_KEY = ["recipe-availability"] as const
- export const RECIPE_SWATCH_KEY = ["recipe-swatch"] as const
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor RecipeFormSheet onSubmit to use saveRecipeGraph</name>
  <files>src/features/recipes/RecipeFormSheet.tsx</files>
  <read_first>
    - src/features/recipes/RecipeFormSheet.tsx (full file — understand current onSubmit at lines 215-415, all imports, which hooks are used only in onSubmit vs elsewhere in the component)
    - src/db/queries/recipes.ts (verify saveRecipeGraph export exists from Plan 01)
    - src/hooks/useRecipes.ts (RECIPES_KEY, RECIPE_KEY exports)
    - src/hooks/useRecipeSections.ts (RECIPE_SECTIONS_KEY, SECTION_COUNTS_KEY exports)
    - src/hooks/useRecipePaints.ts (RECIPE_PAINTS_KEY, STEP_COUNTS_KEY, RECIPE_AVAILABILITY_KEY, RECIPE_SWATCH_KEY exports)
  </read_first>
  <action>
    Refactor RecipeFormSheet.tsx per D-06, D-07, D-08.

    Replace the entire onSubmit body (lines 215-415) with:
    1. Call `saveRecipeGraph(recipe?.id ?? null, values, sections, existingSections, existingSteps)` — returns `finalRecipeId`
    2. On success, batch-invalidate 10 React Query keys: RECIPES_KEY, RECIPE_KEY(finalRecipeId), RECIPE_SECTIONS_KEY(finalRecipeId), RECIPE_PAINTS_KEY(finalRecipeId), STEP_COUNTS_KEY, RECIPE_AVAILABILITY_KEY, RECIPE_SWATCH_KEY, SECTION_COUNTS_KEY, ["kanban-enrichment"], ["recipes", "by-unit"]
    3. Show toast.success and call onClose()
    4. In catch block: toast.error("Failed to save recipe. Changes were not saved.") — form stays open per D-04

    Add import: `import { saveRecipeGraph } from "@/db/queries/recipes"` (keep existing `RECIPES_KEY`, `RECIPE_KEY` imports from useRecipes)

    Remove imports that are NO LONGER used anywhere in the component after onSubmit refactor. Audit each carefully:
    - Remove `createRecipeSection`, `deleteRecipeSection`, `updateRecipeSection` from `@/db/queries/recipeSections` IF only used in onSubmit
    - Remove `updateRecipeStep`, `removeRecipePaint` (aliased as removeRecipeStep) from `@/db/queries/recipePaints` IF only used in onSubmit
    - Remove `useCreateRecipe`, `useUpdateRecipe` hook calls IF only used in onSubmit
    - Remove `useAddRecipePaint` hook call IF only used in onSubmit
    - KEEP `computeSectionDiff`, `computeStepDiff`, `buildSectionIdMap` ONLY if used elsewhere in the component (they should NOT be needed in onSubmit anymore since saveRecipeGraph calls them internally)
    - KEEP `computeOrderIndex` ONLY if used elsewhere
    - KEEP all non-submit hook usages (useRecipeSections, useRecipeSteps query hooks for loading data)
    - KEEP `useQueryClient` — still needed for batch invalidation

    Per D-07: Do NOT remove or modify the hook definitions in useRecipes.ts, useRecipeSections.ts, or useRecipePaints.ts — only remove their usage from THIS component's onSubmit.

    The existing `existingSections` and `existingSteps` variables used in onSubmit (loaded via useRecipeSections and useRecipeSteps hooks) must still be available to pass to saveRecipeGraph. Verify these are populated from the query hooks' data properties.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - RecipeFormSheet.tsx contains `import { saveRecipeGraph } from "@/db/queries/recipes"`
    - onSubmit function body calls saveRecipeGraph exactly once as its sole DB operation
    - onSubmit does NOT call createRecipe.mutateAsync, updateRecipe.mutateAsync, addRecipePaint.mutateAsync, createRecipeSection, deleteRecipeSection, updateRecipeSection, updateRecipeStep, or removeRecipeStep
    - After saveRecipeGraph resolves, qc.invalidateQueries is called for all 10 keys listed in PATTERNS.md Pattern 3
    - The catch block calls toast.error and does NOT call onClose (form stays open)
    - pnpm build exits 0 (no TypeScript errors, no unused imports)
    - pnpm test exits 0 (existing tests still pass)
  </acceptance_criteria>
  <done>RecipeFormSheet onSubmit is a ~25-line function calling saveRecipeGraph + batch invalidation; the full test suite passes; build succeeds with no unused-import errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form values -> saveRecipeGraph | Zod-validated form data passed to query layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03 | Tampering | Form submission | mitigate | Zod schema validates all form values before onSubmit fires; saveRecipeGraph uses parameterized SQL |
</threat_model>

<verification>
pnpm build
pnpm test
</verification>

<success_criteria>
RecipeFormSheet.tsx onSubmit calls saveRecipeGraph as its sole DB operation, batch-invalidates
React Query cache after success, and both build and full test suite pass.
</success_criteria>

<output>
After completion, create `.planning/phases/75-transactional-recipe-graph-save/75-02-SUMMARY.md`
</output>

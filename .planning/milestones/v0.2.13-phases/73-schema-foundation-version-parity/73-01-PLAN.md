---
phase: 73-schema-foundation-version-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/026_unit_rules_mapping.sql
  - src-tauri/migrations/027_battle_log_after_action.sql
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - DI-05
must_haves:
  truths:
    - "Migration 026 creates the unit_rules_mapping table with correct schema"
    - "Migration 027 adds four after-action columns to battle_logs"
    - "Both migrations are registered in lib.rs and will run automatically on app launch"
  artifacts:
    - path: "src-tauri/migrations/026_unit_rules_mapping.sql"
      provides: "unit_rules_mapping table DDL"
      contains: "CREATE TABLE IF NOT EXISTS unit_rules_mapping"
    - path: "src-tauri/migrations/027_battle_log_after_action.sql"
      provides: "battle_logs after-action columns"
      contains: "ALTER TABLE battle_logs ADD COLUMN forgotten_rules"
    - path: "src-tauri/src/lib.rs"
      provides: "Migration registration for versions 26 and 27"
      contains: "version: 27"
  key_links:
    - from: "src-tauri/migrations/026_unit_rules_mapping.sql"
      to: "src-tauri/src/lib.rs"
      via: "include_str! macro in get_migrations()"
      pattern: 'include_str!\("../migrations/026_unit_rules_mapping.sql"\)'
    - from: "src-tauri/migrations/027_battle_log_after_action.sql"
      to: "src-tauri/src/lib.rs"
      via: "include_str! macro in get_migrations()"
      pattern: 'include_str!\("../migrations/027_battle_log_after_action.sql"\)'
---

<objective>
Create two SQL migrations (026 for unit_rules_mapping table, 027 for battle_log after-action columns) and register them in the Rust entry point.

Purpose: Extend the schema for Phase 76 (unit-to-rules mapping) and Phase 78 (Game Day after-action) without touching any query/hook/UI layers.
Output: Two migration SQL files and updated lib.rs with versions 26+27 registered.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-schema-foundation-version-parity/73-CONTEXT.md

<interfaces>
<!-- Existing migration registration pattern in lib.rs (versions 1-25) -->
<!-- Each entry follows this exact shape: -->
From src-tauri/src/lib.rs:
```rust
Migration {
    version: 25,
    description: "tactical_role",
    sql: include_str!("../migrations/025_tactical_role.sql"),
    kind: MigrationKind::Up,
}
```

<!-- The vec lives inside fn get_migrations() -> Vec<Migration>, currently has 25 entries -->
<!-- New entries must be appended BEFORE the closing ] of the vec -->

From src/types/battleLog.ts (downstream reference - DO NOT modify in this plan):
```typescript
export interface BattleLog {
  id: number;
  army_list_id: number | null;
  battle_date: string;
  opponent: string | null;
  opponent_faction: string;
  mission: string;
  points_played: number | null;
  result: BattleLogResult;
  my_score: number | null;
  opponent_score: number | null;
  mvp_unit_id: number | null;
  underperforming_unit_id: number | null;
  lessons_learned: string | null;
  changes_next_time: string | null;
  notes: string | null;
  created_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 026 (unit_rules_mapping) and 027 (battle_log after-action)</name>
  <files>src-tauri/migrations/026_unit_rules_mapping.sql, src-tauri/migrations/027_battle_log_after_action.sql</files>
  <read_first>
    src-tauri/migrations/024_points_import_history.sql (cross-DB TEXT reference pattern)
    src-tauri/migrations/025_tactical_role.sql (latest ALTER TABLE pattern)
    src-tauri/migrations/001_core_schema.sql lines 142-162 (battle_logs table definition, FK patterns)
  </read_first>
  <action>
    Create 026_unit_rules_mapping.sql per D-01, D-02, D-03:
    - CREATE TABLE IF NOT EXISTS unit_rules_mapping with columns: id (INTEGER PRIMARY KEY AUTOINCREMENT), unit_id (INTEGER NOT NULL UNIQUE REFERENCES units(id) ON DELETE CASCADE), rules_datasheet_id (TEXT, nullable), match_status (TEXT NOT NULL DEFAULT 'auto'), source (TEXT, nullable), created_at (TEXT NOT NULL DEFAULT (datetime('now'))), updated_at (TEXT NOT NULL DEFAULT (datetime('now')))
    - The UNIQUE constraint on unit_id is inline on the column definition (per D-02)
    - No FK to rules.db; rules_datasheet_id is a plain TEXT column (per D-03, same cross-DB pattern as synced_unit_points.datasheet_id in migration 024)

    Create 027_battle_log_after_action.sql per D-04, D-05, D-06:
    - Four ALTER TABLE battle_logs ADD COLUMN statements, one per column:
      forgotten_rules TEXT (nullable, stores JSON array of strings per D-05)
      mvp_notes TEXT (nullable, free-text alongside existing mvp_unit_id FK per D-06)
      underperformer_notes TEXT (nullable, free-text alongside existing underperforming_unit_id FK per D-06)
      promoted_to_reminder INTEGER NOT NULL DEFAULT 0 (boolean 0|1 pattern)
    - Do NOT use IF NOT EXISTS with ALTER TABLE (SQLite does not support it)
    - Do NOT modify existing columns or FKs (per D-06)
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS unit_rules_mapping" src-tauri/migrations/026_unit_rules_mapping.sql &amp;&amp; grep -c "ALTER TABLE battle_logs ADD COLUMN" src-tauri/migrations/027_battle_log_after_action.sql</automated>
  </verify>
  <acceptance_criteria>
    - 026_unit_rules_mapping.sql contains CREATE TABLE IF NOT EXISTS unit_rules_mapping with all 7 columns (id, unit_id, rules_datasheet_id, match_status, source, created_at, updated_at)
    - unit_id column has both UNIQUE and REFERENCES units(id) ON DELETE CASCADE
    - match_status has DEFAULT 'auto'
    - 027_battle_log_after_action.sql contains exactly 4 ALTER TABLE statements adding forgotten_rules, mvp_notes, underperformer_notes, promoted_to_reminder
    - promoted_to_reminder has INTEGER NOT NULL DEFAULT 0
    - forgotten_rules, mvp_notes, underperformer_notes are nullable TEXT
  </acceptance_criteria>
  <done>Both SQL migration files exist with correct DDL matching decisions D-01 through D-06</done>
</task>

<task type="auto">
  <name>Task 2: Register migrations 26+27 in lib.rs</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>
    src-tauri/src/lib.rs (full file — identify exact insertion point after version 25 entry, before closing bracket of get_migrations vec)
  </read_first>
  <action>
    Append two Migration entries to the get_migrations() vec in lib.rs, after the version 25 entry and before the closing ].

    Entry for version 26:
    - version: 26
    - description: "unit_rules_mapping"
    - sql: include_str!("../migrations/026_unit_rules_mapping.sql")
    - kind: MigrationKind::Up

    Entry for version 27:
    - version: 27
    - description: "battle_log_after_action"
    - sql: include_str!("../migrations/027_battle_log_after_action.sql")
    - kind: MigrationKind::Up

    Follow the exact formatting of existing entries (8-space indentation for Migration {, 12-space for fields, trailing comma after closing brace).
  </action>
  <verify>
    <automated>grep -c "version: 27" src-tauri/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - lib.rs get_migrations() vec contains exactly 27 Migration entries (was 25)
    - Version 26 entry references "../migrations/026_unit_rules_mapping.sql"
    - Version 27 entry references "../migrations/027_battle_log_after_action.sql"
    - Both entries use MigrationKind::Up
    - Rust compilation succeeds: the file is syntactically valid (no unmatched braces or missing commas)
  </acceptance_criteria>
  <done>lib.rs registers both new migrations; version 27 is the highest version in get_migrations()</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. This plan creates database DDL and modifies Rust source — all local compile-time artifacts with no user input paths.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | Migration SQL files | accept | Migrations are local files bundled at compile time; no runtime injection vector |
</threat_model>

<verification>
1. Both .sql files exist in src-tauri/migrations/
2. lib.rs compiles (pnpm tauri build would verify, but pnpm build is sufficient for TS side)
3. grep confirms version 26 and 27 are registered
</verification>

<success_criteria>
- Migration 026 creates unit_rules_mapping with UNIQUE unit_id, CASCADE delete, TEXT cross-DB reference
- Migration 027 adds 4 columns to battle_logs (forgotten_rules, mvp_notes, underperformer_notes, promoted_to_reminder)
- lib.rs registers both as versions 26 and 27 in get_migrations()
</success_criteria>

<output>
After completion, create `.planning/phases/73-schema-foundation-version-parity/73-01-SUMMARY.md`
</output>

---
phase: 76-points-resolver-unit-rules-mapping-split-warnings
plan: 02
type: execute
wave: 2
depends_on: [76-01]
files_modified:
  - src/features/army-lists/PointsSourceChip.tsx
  - src/features/army-lists/MatchStatusIndicator.tsx
  - src/features/army-lists/RulesMappingSheet.tsx
  - src/features/army-lists/ArmyListUnitRow.tsx
  - src/features/army-lists/ArmyListSummaryBar.tsx
autonomous: false
requirements:
  - PV-02
  - PV-03
  - PV-04
  - PV-05
  - PV-06
  - PV-07

must_haves:
  truths:
    - "Each unit row shows a source chip with points and provenance label"
    - "Each unit row shows a match status indicator icon"
    - "Clicking the match indicator opens a Sheet for confirm/override/remove"
    - "List-level warnings appear once in the summary bar, not per-unit"
    - "Unit-level warnings remain on individual unit rows"
    - "Ambiguous matches show a count and destructive-colored indicator"
  artifacts:
    - path: "src/features/army-lists/PointsSourceChip.tsx"
      provides: "Source label display component"
      exports: ["PointsSourceChip"]
    - path: "src/features/army-lists/MatchStatusIndicator.tsx"
      provides: "Match status icon button"
      exports: ["MatchStatusIndicator"]
    - path: "src/features/army-lists/RulesMappingSheet.tsx"
      provides: "Confirm/override Sheet for unit-to-rules mapping"
      exports: ["RulesMappingSheet"]
  key_links:
    - from: "src/features/army-lists/ArmyListUnitRow.tsx"
      to: "src/lib/resolveUnitPoints.ts"
      via: "import resolveUnitPoints, pass row fields"
      pattern: "resolveUnitPoints"
    - from: "src/features/army-lists/ArmyListUnitRow.tsx"
      to: "src/features/army-lists/PointsSourceChip.tsx"
      via: "renders PointsSourceChip with resolved source"
      pattern: "PointsSourceChip"
    - from: "src/features/army-lists/ArmyListSummaryBar.tsx"
      to: "src/lib/computeUnitWarnings.ts"
      via: "imports computeListWarnings for list-level warning display"
      pattern: "computeListWarnings"
    - from: "src/features/army-lists/RulesMappingSheet.tsx"
      to: "src/hooks/useUnitRulesMapping.ts"
      via: "useUpsertUnitRulesMapping + useDeleteUnitRulesMapping mutations"
      pattern: "useUpsertUnitRulesMapping"
---

<objective>
Build the UI integration layer for Phase 76: PointsSourceChip, MatchStatusIndicator, RulesMappingSheet components, and integrate them into ArmyListUnitRow and ArmyListSummaryBar.

Purpose: Makes the data layer from Plan 01 visible to users. Every unit row shows where its points come from, whether its rules mapping is confirmed, and list-level warnings appear exactly once in the summary bar.

Output: 3 new components + 2 modified components, all rendering correctly with full interaction flows.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-points-resolver-unit-rules-mapping-split-warnings/76-CONTEXT.md
@.planning/phases/76-points-resolver-unit-rules-mapping-split-warnings/76-UI-SPEC.md
@.planning/phases/76-points-resolver-unit-rules-mapping-split-warnings/76-PATTERNS.md
@.planning/phases/76-points-resolver-unit-rules-mapping-split-warnings/76-01-SUMMARY.md

<interfaces>
<!-- Contracts created by Plan 01 that this plan consumes -->

From src/lib/resolveUnitPoints.ts (created in Plan 01):
```typescript
export type PointsSource = "override" | "synced" | "user-override" | "base" | "unknown";
export interface ResolvedPoints { points: number; source: PointsSource; }
export function resolveUnitPoints(row: {
  points_override: number | null;
  synced_points: number | null;
  override_points: number | null;
  unit_points: number | null;
}): ResolvedPoints;
```

From src/types/unitRulesMapping.ts (created in Plan 01):
```typescript
export type MatchStatus = "auto" | "confirmed" | "manual";
export interface UnitRulesMapping {
  id: number; unit_id: number; rules_datasheet_id: string | null;
  match_status: MatchStatus; source: string | null;
  created_at: string; updated_at: string;
}
export type UpsertUnitRulesMappingInput = {
  unit_id: number; rules_datasheet_id: string | null;
  match_status: MatchStatus; source?: string | null;
};
```

From src/hooks/useUnitRulesMapping.ts (created in Plan 01):
```typescript
export const UNIT_RULES_MAPPING_KEY = (unitId: number) => ["unit-rules-mapping", unitId] as const;
export function useUnitRulesMapping(unitId: number | undefined);
export function useUpsertUnitRulesMapping();
export function useDeleteUnitRulesMapping();
```

From src/db/queries/unitRulesMapping.ts (created in Plan 01):
```typescript
export async function findMatchingDatasheets(unitName: string, factionId: number | null): Promise<Array<{ unit_name: string; faction_id: string | null; points: number }>>;
export async function findRulesDatasheets(searchTerm: string): Promise<Array<{ id: string; name: string; faction_id: string | null }>>;
```

From src/lib/computeUnitWarnings.ts (modified in Plan 01):
```typescript
export function computeListWarnings(context: WarningContext): UnitWarnings;
export function computeUnitWarnings(unit, context): UnitWarnings; // no longer contains list-level warnings
```

From src/types/armyList.ts (modified in Plan 01):
```typescript
export interface ArmyListUnitRow extends ArmyListUnit {
  // ... existing fields ...
  synced_points: number | null;  // NEW
  override_points: number | null; // NEW
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PointsSourceChip, MatchStatusIndicator, and RulesMappingSheet components</name>
  <files>
    src/features/army-lists/PointsSourceChip.tsx,
    src/features/army-lists/MatchStatusIndicator.tsx,
    src/features/army-lists/RulesMappingSheet.tsx
  </files>
  <read_first>
    src/features/army-lists/PointsFreshnessBadge.tsx,
    src/components/ui/status-badge.tsx,
    src/features/factions/FactionSheet.tsx,
    src/hooks/useUnitRulesMapping.ts,
    src/db/queries/unitRulesMapping.ts,
    src/lib/resolveUnitPoints.ts,
    src/types/unitRulesMapping.ts,
    .planning/phases/76-points-resolver-unit-rules-mapping-split-warnings/76-UI-SPEC.md
  </read_first>
  <action>
    Create PointsSourceChip.tsx per UI-SPEC Component 1 and D-03. Props: { points: number | null; source: PointsSource }. Render inline-flex with items-center gap-1.5. Colored dot (h-1.5 w-1.5 rounded-full) using SOURCE_DOT_CLASS map: synced -> bg-emerald-500, override -> bg-violet-500, user-override -> bg-amber-500, base -> bg-blue-500, unknown -> bg-muted-foreground/50. Text label: text-xs text-muted-foreground. Format: "{points} pts" or "--" when source is unknown. Wrap dot in Tooltip with source name as content. Add aria-label "{N} points, source: {source}".

    Create MatchStatusIndicator.tsx per UI-SPEC Component 2 and D-09/D-10. Props: { unitId: number; matchStatus: MatchStatus | null; ambiguousCount?: number; onClick: () => void }. Render Button variant="ghost" size="icon" className="h-6 w-6". Icon selection: confirmed -> Check (text-emerald-500), auto -> LinkIcon (text-muted-foreground), manual -> Pencil (text-blue-500), null (no mapping) -> AlertTriangle (text-amber-500), ambiguous (ambiguousCount > 1) -> AlertTriangle (text-destructive). Wrap in Tooltip with labels from UI-SPEC copywriting contract. The ambiguous state takes priority over null when ambiguousCount > 1.

    Create RulesMappingSheet.tsx per UI-SPEC Component 3 and D-09/D-04. Props: { open: boolean; unitId: number; unitName: string; factionId: number; onClose: () => void }. Use Sheet side="right". Three sections: (1) Current Match card showing current mapping from useUnitRulesMapping(unitId), with "Confirm Match" Button (calls useUpsertUnitRulesMapping with match_status "confirmed") and "Remove Mapping" destructive ghost Button (calls useDeleteUnitRulesMapping). (2) Alternative Matches section with search Input (aria-label="Search datasheets") that calls findRulesDatasheets on change (debounce 300ms via setTimeout). Display results in scrollable list (max-h-64 overflow-y-auto) with "Select" outline Button per candidate that upserts with match_status "manual" and new rules_datasheet_id. (3) Empty state when no rules data synced: centered muted text per UI-SPEC copywriting. Toast notifications on success/error per UI-SPEC copywriting contract. Close sheet after successful confirm/select/remove.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - PointsSourceChip renders a colored dot (h-1.5 w-1.5 rounded-full) with Tooltip and text label
    - PointsSourceChip has 5 entries in SOURCE_DOT_CLASS: synced/emerald, override/violet, user-override/amber, base/blue, unknown/muted-foreground
    - MatchStatusIndicator renders Button variant="ghost" size="icon" with conditional icon based on matchStatus
    - MatchStatusIndicator shows AlertTriangle text-destructive when ambiguousCount > 1
    - RulesMappingSheet uses Sheet with SheetContent, SheetHeader, SheetTitle, SheetDescription
    - RulesMappingSheet imports and calls useUpsertUnitRulesMapping and useDeleteUnitRulesMapping
    - RulesMappingSheet has search Input with aria-label="Search datasheets"
    - RulesMappingSheet calls toast.success on confirm/select/remove actions
    - pnpm build completes without errors
  </acceptance_criteria>
  <done>Three new components created matching UI-SPEC visual spec, all type-checking correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate components into ArmyListUnitRow and ArmyListSummaryBar</name>
  <files>
    src/features/army-lists/ArmyListUnitRow.tsx,
    src/features/army-lists/ArmyListSummaryBar.tsx
  </files>
  <read_first>
    src/features/army-lists/ArmyListUnitRow.tsx,
    src/features/army-lists/ArmyListSummaryBar.tsx,
    src/features/army-lists/PointsSourceChip.tsx,
    src/features/army-lists/MatchStatusIndicator.tsx,
    src/features/army-lists/RulesMappingSheet.tsx,
    src/lib/resolveUnitPoints.ts,
    src/lib/computeUnitWarnings.ts,
    src/hooks/useUnitRulesMapping.ts,
    src/db/queries/unitRulesMapping.ts,
    .planning/phases/76-points-resolver-unit-rules-mapping-split-warnings/76-UI-SPEC.md
  </read_first>
  <action>
    Per D-09 and UI-SPEC Component 5: Modify ArmyListUnitRow.tsx to integrate the new components. Import resolveUnitPoints from @/lib/resolveUnitPoints, PointsSourceChip, MatchStatusIndicator, RulesMappingSheet. Add local state for Sheet open (useState boolean). Compute resolved = resolveUnitPoints({ points_override: unit.points_override, synced_points: unit.synced_points, override_points: unit.override_points, unit_points: unit.unit_points }). For match status: use useUnitRulesMapping(unit.unit_id) to get current mapping, and call findMatchingDatasheets with unit.unit_name and unit.faction_id via useQuery to get ambiguousCount (number of results). Add MatchStatusIndicator between the existing warning icon and unit name, with onClick opening the RulesMappingSheet. Add PointsSourceChip below the points override Input in the points TableCell, passing resolved.points and resolved.source. Render RulesMappingSheet conditionally when sheet state is open, passing unitId, unitName (unit.unit_name), factionId (unit.faction_id), onClose.

    Per D-10: For ambiguity detection, create a useQuery hook inline in ArmyListUnitRow (or a separate small hook) that calls findMatchingDatasheets(unit.unit_name, unit.faction_id) and counts results. Pass ambiguousCount to MatchStatusIndicator. When count > 1 and matchStatus is "auto" or null, the indicator shows the destructive state.

    Per D-13 and UI-SPEC Component 4: Modify ArmyListSummaryBar.tsx. Import computeListWarnings from @/lib/computeUnitWarnings and Badge from @/components/ui/badge. Compute listWarnings = computeListWarnings(context) where context is the same WarningContext already constructed in the component. Render list-level warnings as inline badges: hard warnings get Badge variant="destructive", soft warnings get Badge variant="outline". Display them in a flex-wrap row with gap-2 below the existing freshness row, only when listWarnings has entries. Update the warning count tooltip text from "N critical, M informational" to "{N} list warnings, {M} unit warnings" per UI-SPEC copywriting. For the counts: list warning count = listWarnings.hard.length + listWarnings.soft.length. Unit warning count = stats.hardWarningCount + stats.softWarningCount - listWarnings.hard.length - listWarnings.soft.length (since computeListHealthStats now includes list counts in its totals). Alternatively, compute unit warnings separately by iterating units with computeUnitWarnings and summing.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - ArmyListUnitRow imports and renders PointsSourceChip below the points Input
    - ArmyListUnitRow imports and renders MatchStatusIndicator between warning icon and unit name
    - ArmyListUnitRow manages RulesMappingSheet open state and renders it conditionally
    - ArmyListUnitRow calls resolveUnitPoints with unit.points_override, unit.synced_points, unit.override_points, unit.unit_points
    - ArmyListSummaryBar imports and calls computeListWarnings
    - ArmyListSummaryBar renders hard list warnings as Badge variant="destructive"
    - ArmyListSummaryBar renders soft list warnings as Badge variant="outline"
    - ArmyListSummaryBar tooltip text contains "list warnings" and "unit warnings"
    - pnpm build completes without errors
  </acceptance_criteria>
  <done>Source chips and match indicators visible on unit rows; list-level warnings displayed once in summary bar; all interactions wired to data layer; build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 76 UI components integrated: PointsSourceChip showing point source provenance on each unit row, MatchStatusIndicator showing rules mapping status with click-to-open Sheet, RulesMappingSheet for confirming/overriding unit-to-rules mappings, warning split with list-level warnings in summary bar and unit-level warnings on rows.
  </what-built>
  <how-to-verify>
    1. Run `pnpm tauri dev` to launch the app
    2. Navigate to an army list with units
    3. Verify each unit row shows a source chip below the points input (e.g., "95 pts . synced" or "75 pts . base")
    4. Verify each unit row has a small status icon to the left of the unit name (checkmark, link, pencil, or warning triangle)
    5. Click a match status icon -- verify a Sheet opens showing current mapping with "Confirm Match" button
    6. In the Sheet, type in the search field -- verify datasheet results appear
    7. Click "Confirm Match" -- verify toast "Rules mapping confirmed" and icon changes to green checkmark
    8. Look at the summary bar -- verify list-level warnings (like "Points exceeded") appear as badges in the summary bar, not repeated on every unit row
    9. Verify the tooltip on the warnings count shows "N list warnings, M unit warnings"
    10. Verify unit-level warnings (Not painted, Not assembled) still appear as warning triangles on individual unit rows
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User search input -> rules.db query | Search term in RulesMappingSheet flows to findRulesDatasheets SQL |
| rules.db data -> UI display | Datasheet names from rules.db rendered in Sheet list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04 | Tampering | RulesMappingSheet search | mitigate | Search term passed via parameterized $1 to findRulesDatasheets, LIMIT 20 |
| T-76-05 | Denial of Service | Ambiguity detection query | mitigate | Query runs once per unit row mount with React Query caching (staleTime prevents re-fetch) |
| T-76-06 | Information Disclosure | Datasheet names in Sheet | accept | Rules data is non-sensitive game content, single-user desktop app |
</threat_model>

<verification>
1. `pnpm build` -- no TypeScript errors
2. `pnpm test` -- full test suite passes
3. Visual verification of source chips, match indicators, warning split in running app
</verification>

<success_criteria>
- PointsSourceChip renders correct dot color and label for all 5 source types
- MatchStatusIndicator renders correct icon and color for all match states
- RulesMappingSheet allows confirm, select alternative, and remove mapping flows
- ArmyListUnitRow shows source chip and match indicator
- ArmyListSummaryBar shows list-level warnings as badges, not per-unit
- Warning tooltip distinguishes list vs unit warning counts
- All interactions produce correct toast notifications
</success_criteria>

<output>
After completion, create `.planning/phases/76-points-resolver-unit-rules-mapping-split-warnings/76-02-SUMMARY.md`
</output>

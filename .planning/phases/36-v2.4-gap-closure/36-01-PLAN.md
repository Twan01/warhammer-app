---
phase: 36-v0.2.4-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useRecipes.ts
  - .planning/phases/34-visual-polish/34-VERIFICATION.md
  - .planning/phases/32-army-readiness-card/32-01-SUMMARY.md
  - .planning/phases/33-data-intelligence/33-01-SUMMARY.md
autonomous: true
requirements:
  - DATA-06
gap_closure: true

must_haves:
  truths:
    - "Recipe mutations (create/update/delete) invalidate the ['recipes', 'by-unit'] query key so CurrentFocusCard recipe name refreshes immediately"
    - "34-VERIFICATION.md status reads 'passed' (not 'gaps_found')"
    - "32-01-SUMMARY.md frontmatter includes requirements_completed with PANEL-04 and PANEL-05"
    - "33-01-SUMMARY.md frontmatter includes requirements_completed with DATA-01"
  artifacts:
    - path: "src/hooks/useRecipes.ts"
      provides: "Recipe mutation cache invalidation including by-unit queries"
      contains: "recipes.*by-unit"
    - path: ".planning/phases/34-visual-polish/34-VERIFICATION.md"
      provides: "Accurate verification status reflecting code at HEAD"
      contains: "status: passed"
    - path: ".planning/phases/32-army-readiness-card/32-01-SUMMARY.md"
      provides: "Accurate requirements_completed frontmatter"
      contains: "requirements_completed"
    - path: ".planning/phases/33-data-intelligence/33-01-SUMMARY.md"
      provides: "Accurate requirements_completed frontmatter"
      contains: "requirements_completed"
  key_links:
    - from: "src/hooks/useRecipes.ts"
      to: "DashboardPage.tsx inline useQuery"
      via: "queryKey prefix match ['recipes', 'by-unit']"
      pattern: "invalidateQueries.*recipes.*by-unit"
---

<objective>
Close all tech debt items from the v0.2.4 milestone audit: fix recipe cache invalidation so CurrentFocusCard recipe name refreshes immediately after recipe mutations, and update three stale documentation files.

Purpose: Eliminate the last remaining cache staleness gap (DATA-06) and bring verification/summary docs in sync with actual shipped code.
Output: Patched useRecipes.ts with by-unit invalidation + 3 corrected documentation files.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.2.4-MILESTONE-AUDIT.md

<interfaces>
<!-- Key exports from useRecipes.ts that the executor needs to understand -->

From src/hooks/useRecipes.ts:
```typescript
export const RECIPES_KEY = ["recipes"] as const;
export const RECIPE_KEY = (id: number) => ["recipes", id] as const;

// Three mutation hooks that need the by-unit invalidation added:
export function useCreateRecipe()  // onSuccess invalidates RECIPES_KEY + ["kanban-enrichment"]
export function useUpdateRecipe()  // onSuccess invalidates RECIPES_KEY + RECIPE_KEY(id) + ["kanban-enrichment"]
export function useDeleteRecipe()  // onSuccess invalidates RECIPES_KEY only
```

From src/features/dashboard/DashboardPage.tsx (consumer):
```typescript
// Line 83-87: inline useQuery that reads recipe data for focus unit
const { data: focusRecipes } = useQuery({
  queryKey: ["recipes", "by-unit", focusUnitId ?? 0],
  queryFn: () => getRecipeNamesByUnitIds([focusUnitId!]),
  enabled: focusUnitId !== null,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix recipe cache invalidation for DATA-06 freshness</name>
  <files>src/hooks/useRecipes.ts</files>
  <read_first>
    - src/hooks/useRecipes.ts (current mutation hooks and their onSuccess handlers)
    - src/features/dashboard/DashboardPage.tsx lines 81-87 (the consumer query using ["recipes", "by-unit", focusUnitId])
  </read_first>
  <action>
Add `qc.invalidateQueries({ queryKey: ["recipes", "by-unit"] })` to the onSuccess handler of all three recipe mutation hooks in src/hooks/useRecipes.ts:

1. In `useCreateRecipe` onSuccess (after the existing `qc.invalidateQueries({ queryKey: ["kanban-enrichment"] })` line): add `qc.invalidateQueries({ queryKey: ["recipes", "by-unit"] });`

2. In `useUpdateRecipe` onSuccess (after the existing `qc.invalidateQueries({ queryKey: ["kanban-enrichment"] })` line): add `qc.invalidateQueries({ queryKey: ["recipes", "by-unit"] });`

3. In `useDeleteRecipe` onSuccess (after the existing `qc.invalidateQueries({ queryKey: RECIPES_KEY })` line): add `qc.invalidateQueries({ queryKey: ["recipes", "by-unit"] });`

This ensures that any query whose key starts with `["recipes", "by-unit"]` (including `["recipes", "by-unit", 42]` for specific unit IDs) is invalidated when recipes change. The DashboardPage inline useQuery for focus card recipe name will immediately refetch instead of showing stale data for up to 5 minutes.

Do NOT add a new exported constant for this key — it is an inline query key used only in DashboardPage, and a raw array literal in invalidateQueries is clearer here.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep for `"recipes", "by-unit"` in src/hooks/useRecipes.ts returns exactly 3 matches (one per mutation hook)
    - useCreateRecipe onSuccess contains `qc.invalidateQueries({ queryKey: ["recipes", "by-unit"] })`
    - useUpdateRecipe onSuccess contains `qc.invalidateQueries({ queryKey: ["recipes", "by-unit"] })`
    - useDeleteRecipe onSuccess contains `qc.invalidateQueries({ queryKey: ["recipes", "by-unit"] })`
    - `pnpm build` passes with 0 TypeScript errors
    - `pnpm test` passes with 0 failures
  </acceptance_criteria>
  <done>All three recipe mutation hooks invalidate the ["recipes", "by-unit"] query prefix so CurrentFocusCard recipe name refreshes immediately after any recipe create/update/delete</done>
</task>

<task type="auto">
  <name>Task 2: Update stale verification and summary documentation</name>
  <files>
    .planning/phases/34-visual-polish/34-VERIFICATION.md
    .planning/phases/32-army-readiness-card/32-01-SUMMARY.md
    .planning/phases/33-data-intelligence/33-01-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/34-visual-polish/34-VERIFICATION.md (current frontmatter with status: gaps_found)
    - .planning/phases/32-army-readiness-card/32-01-SUMMARY.md (current frontmatter missing requirements_completed)
    - .planning/phases/33-data-intelligence/33-01-SUMMARY.md (current frontmatter missing requirements_completed)
    - .planning/v0.2.4-MILESTONE-AUDIT.md (source of truth for what needs updating)
  </read_first>
  <action>
Three documentation updates:

**1. Update 34-VERIFICATION.md frontmatter and body:**
- Change frontmatter `status: gaps_found` to `status: passed`
- Change frontmatter `score: 5/6 must-haves verified` to `score: 6/6 must-haves verified`
- Remove the entire `gaps:` block from frontmatter (the block starting with `gaps:` through the two `missing:` items ending with `"...hover:shadow-md'"`)
- In the Observable Truths table (row 5), change `PARTIAL` to `VERIFIED` and update the Evidence column to: `All 6 card components verified: StatCard, CurrentFocusCard, HobbyPipeline, RecentActivityFeed (both branches), ArmyReadinessCard (all 3 branches) — all have transition-shadow duration-150 hover:shadow-md`
- In the Required Artifacts table for RecentActivityFeed.tsx, change status from `STUB` to `VERIFIED` and update Details to: `Both empty (line 47) and populated (line 59) branches have transition-shadow duration-150 hover:shadow-md`
- In the Required Artifacts table for ArmyReadinessCard.tsx, change status from `PARTIAL` to `VERIFIED` and update Details to: `All 3 branches (loading, empty, populated) have transition-shadow duration-150 hover:shadow-md`
- In the Requirements Coverage table for VIS-03, change status from `PARTIALLY BLOCKED` to `SATISFIED` and update Evidence to: `All 6 card types have transition-shadow duration-150 hover:shadow-md on all render branches`
- Remove the Anti-Patterns Found section entirely (both entries are resolved)
- Remove the Gaps Summary section entirely (gaps are closed)
- Add a note at the bottom: `Re-verified: 2026-05-06 — VIS-03 gap fixed in commit e4a221c; document updated in Phase 36 gap closure`

**2. Add requirements_completed to 32-01-SUMMARY.md frontmatter:**
- The file already has `requirements: [PANEL-04, PANEL-05]` at line 38. Add a new line directly after it:
  `requirements_completed: [PANEL-04, PANEL-05]`

**3. Add requirements_completed to 33-01-SUMMARY.md frontmatter:**
- Add `requirements_completed: [DATA-01]` as a new field in the frontmatter block, after the existing `metrics:` block (after the `tests_added: 21` line, before the closing `---`).
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - grep for `status: passed` in .planning/phases/34-visual-polish/34-VERIFICATION.md returns exactly 1 match
    - grep for `status: gaps_found` in .planning/phases/34-visual-polish/34-VERIFICATION.md returns 0 matches
    - grep for `score: 6/6` in .planning/phases/34-visual-polish/34-VERIFICATION.md returns exactly 1 match
    - grep for `gaps:` in the frontmatter of .planning/phases/34-visual-polish/34-VERIFICATION.md returns 0 matches
    - grep for `PARTIAL` in .planning/phases/34-visual-polish/34-VERIFICATION.md returns 0 matches
    - grep for `STUB` in .planning/phases/34-visual-polish/34-VERIFICATION.md returns 0 matches
    - grep for `PARTIALLY BLOCKED` in .planning/phases/34-visual-polish/34-VERIFICATION.md returns 0 matches
    - grep for `Anti-Patterns Found` in .planning/phases/34-visual-polish/34-VERIFICATION.md returns 0 matches
    - grep for `Gaps Summary` in .planning/phases/34-visual-polish/34-VERIFICATION.md returns 0 matches
    - grep for `requirements_completed: \[PANEL-04, PANEL-05\]` in .planning/phases/32-army-readiness-card/32-01-SUMMARY.md returns exactly 1 match
    - grep for `requirements_completed: \[DATA-01\]` in .planning/phases/33-data-intelligence/33-01-SUMMARY.md returns exactly 1 match
  </acceptance_criteria>
  <done>34-VERIFICATION.md reflects passed status matching code at HEAD, 32-01-SUMMARY.md and 33-01-SUMMARY.md both have accurate requirements_completed frontmatter</done>
</task>

</tasks>

<verification>
1. `pnpm build` passes — TypeScript compilation confirms no type errors from useRecipes.ts changes
2. `pnpm test` passes — no regressions from cache invalidation additions
3. grep confirms all 3 recipe mutation hooks invalidate `["recipes", "by-unit"]`
4. grep confirms 34-VERIFICATION.md status is `passed` with no remaining gap indicators
5. grep confirms 32-01-SUMMARY.md has `requirements_completed: [PANEL-04, PANEL-05]`
6. grep confirms 33-01-SUMMARY.md has `requirements_completed: [DATA-01]`
</verification>

<success_criteria>
- Recipe mutations immediately invalidate the by-unit query cache — CurrentFocusCard recipe name refreshes without waiting for 5-min stale timeout
- All three stale documentation files reflect accurate state matching code at HEAD
- Full test suite passes with 0 failures
- TypeScript build passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/36-v0.2.4-gap-closure/36-01-SUMMARY.md`
</output>

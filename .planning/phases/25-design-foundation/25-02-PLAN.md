---
phase: 25-design-foundation
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/features/dashboard/DashboardPage.tsx
  - src/features/units/CollectionPage.tsx
  - src/features/painting-projects/PaintingProjectsPage.tsx
  - src/features/paints/PaintsPage.tsx
  - src/features/recipes/RecipesPage.tsx
  - src/features/army-lists/ArmyListsPage.tsx
  - src/features/battle-log/BattleLogPage.tsx
  - src/features/spending/SpendingPage.tsx
  - src/features/factions/FactionsPage.tsx
autonomous: false
requirements: [DSFD-02]

must_haves:
  truths:
    - "Every one of the 9 main pages renders its title via the shared `<PageHeader />` component — no inline `text-3xl font-semibold tracking-tight` h1 strings remain in any page file."
    - "Dashboard has PageHeader applied in all 4 render branches (loading, error, populated empty, populated with data)."
    - "FactionsPage gets upgraded from its existing `text-xl font-semibold` inline header to the locked `text-3xl font-semibold tracking-tight` PageHeader pattern (no subtitle — per UI-SPEC verification note)."
    - "Action buttons (Add Unit, Add Faction, Add Paint, Add Recipe, Log Game, view-mode toggles, etc.) continue to render in the right slot exactly where they did before."
    - "`pnpm build` passes."
    - "On manual smoke test: Dashboard, Collection, Painting Projects, Paints, Recipes, Army Lists, Battle Log, Spending, and Factions each show a 28px page title with a thin border-bottom separator and the correct subtitle (where applicable)."
  artifacts:
    - path: "src/features/dashboard/DashboardPage.tsx"
      provides: "Dashboard with PageHeader in all branches"
      contains: "PageHeader"
    - path: "src/features/units/CollectionPage.tsx"
      provides: "Collection page with PageHeader + view-mode toggles + Add Unit in actions slot"
      contains: "PageHeader"
    - path: "src/features/painting-projects/PaintingProjectsPage.tsx"
      provides: "Painting Projects page with PageHeader"
      contains: "PageHeader"
    - path: "src/features/paints/PaintsPage.tsx"
      provides: "Paints page with PageHeader"
      contains: "PageHeader"
    - path: "src/features/recipes/RecipesPage.tsx"
      provides: "Recipes page with PageHeader"
      contains: "PageHeader"
    - path: "src/features/army-lists/ArmyListsPage.tsx"
      provides: "Army Lists page with PageHeader"
      contains: "PageHeader"
    - path: "src/features/battle-log/BattleLogPage.tsx"
      provides: "Battle Log page with PageHeader + Log Game action"
      contains: "PageHeader"
    - path: "src/features/spending/SpendingPage.tsx"
      provides: "Spending page with PageHeader"
      contains: "PageHeader"
    - path: "src/features/factions/FactionsPage.tsx"
      provides: "Factions page upgraded to standard PageHeader"
      contains: "PageHeader"
  key_links:
    - from: "9 page components"
      to: "src/components/common/PageHeader.tsx"
      via: "named import"
      pattern: "import \\{ PageHeader \\} from \"@/components/common/PageHeader\""
---

<objective>
Wire the `PageHeader` component (shipped by Plan 01) into all 9 main pages, replacing the duplicated inline header markup with a single consistent call.

Purpose: DSFD-02 requires that every main page use the shared component — this plan does the mechanical replacement. Eight of the nine pages already use the `text-3xl` pattern verbatim, so adoption is byte-equivalent except for the import. The ninth (FactionsPage) is the one outlier still on the legacy `text-xl` pattern — it gets upgraded to the canonical pattern as part of this plan.

Output: 9 page files, each with one inline header `<div>` block replaced by `<PageHeader title="..." subtitle="..." actions={...} />`. DashboardPage gets the replacement applied to all 4 of its render branches. No layout changes outside the header. No new features.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-design-foundation/25-CONTEXT.md
@.planning/phases/25-design-foundation/25-UI-SPEC.md
@.planning/phases/25-design-foundation/25-01-SUMMARY.md

@src/components/common/PageHeader.tsx
@src/features/dashboard/DashboardPage.tsx
@src/features/units/CollectionPage.tsx
@src/features/painting-projects/PaintingProjectsPage.tsx
@src/features/paints/PaintsPage.tsx
@src/features/recipes/RecipesPage.tsx
@src/features/army-lists/ArmyListsPage.tsx
@src/features/battle-log/BattleLogPage.tsx
@src/features/spending/SpendingPage.tsx
@src/features/factions/FactionsPage.tsx

<interfaces>
<!-- Contract from Plan 01 — use these directly. -->

From src/components/common/PageHeader.tsx:
```typescript
export interface PageHeaderProps {
  title: string;
  subtitle?: string;
  actions?: ReactNode;
}
export function PageHeader({ title, subtitle, actions }: PageHeaderProps): JSX.Element;
```

The PageHeader internally wraps `actions` in `<div className="flex items-center gap-2">`,
so call sites must pass the inner action nodes directly — NOT wrap them in their own
`<div className="flex items-center gap-2">` (that would double-wrap).
</interfaces>

<copy_table>
<!-- Locked title + subtitle copy per UI-SPEC §Copywriting Contract. -->

| Page | Title | Subtitle |
|------|-------|----------|
| Dashboard | "Dashboard" | "Your hobby command center at a glance" |
| Collection | "Collection" | "All units you own, tracked and filterable" |
| Painting Projects | "Painting Projects" | "Active units being worked on right now" |
| Paints | "Paints" | "Your paint collection, linked to recipes" |
| Recipes | "Recipes" | "Documented paint schemes for your models" |
| Army Lists | "Army Lists" | "Points-tracked lists for the tabletop" |
| Battle Log | "Battle Log" | "Every game you've played, win or lose." |
| Spending | "Spending" | "Total hobby spend tracked to the penny" |
| Factions | "Factions" | (none) |
</copy_table>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace inline headers in the 7 already-canonical pages</name>
  <files>
    src/features/units/CollectionPage.tsx,
    src/features/painting-projects/PaintingProjectsPage.tsx,
    src/features/paints/PaintsPage.tsx,
    src/features/recipes/RecipesPage.tsx,
    src/features/army-lists/ArmyListsPage.tsx,
    src/features/battle-log/BattleLogPage.tsx,
    src/features/spending/SpendingPage.tsx
  </files>
  <action>
For each of the 7 files listed, perform the following surgical replacement:

1. Add this import near the other top-level imports (alongside other `@/components/...` imports — order doesn't matter to the compiler but keep grouping consistent):
   ```ts
   import { PageHeader } from "@/components/common/PageHeader";
   ```

2. Locate the existing inline header block, which has the exact shape:
   ```tsx
   <div className="flex items-center justify-between pb-6 border-b border-border/40">
     <div>
       <h1 className="text-3xl font-semibold tracking-tight">{TITLE}</h1>
       <p className="text-sm text-muted-foreground mt-1">{SUBTITLE}</p>
     </div>
     <div className="flex items-center gap-2">
       {ACTIONS_JSX}
     </div>
   </div>
   ```

3. Replace it with:
   ```tsx
   <PageHeader
     title="{TITLE}"
     subtitle="{SUBTITLE}"
     actions={
       <>
         {ACTIONS_JSX}
       </>
     }
   />
   ```

   If `ACTIONS_JSX` is a single JSX element (e.g. `<Button onClick={openCreate}>...</Button>` in BattleLogPage), pass it directly without a fragment:
   ```tsx
   actions={<Button onClick={openCreate}>...</Button>}
   ```

   If there are no actions in the original (verify per file), omit the `actions` prop entirely.

4. Remove the no-longer-used inner `<div className="flex items-center gap-2">` wrapper — `PageHeader` adds this wrapper internally for the actions slot.

Per-file specifics — copy strings come from the `<copy_table>` in the context section above:

**CollectionPage.tsx** (line ~135):
- title="Collection", subtitle="All units you own, tracked and filterable"
- actions = the existing fragment of two ghost icon buttons (`Table view`, `Gallery view`) plus the `Add Unit` Button. Pass them inside a fragment `<>...</>` since there are 3 sibling action elements.

**PaintingProjectsPage.tsx** (line ~23):
- title="Painting Projects", subtitle="Active units being worked on right now"
- actions: read the file to see the existing right-slot content; pass through unchanged.

**PaintsPage.tsx** (line ~97):
- title="Paints", subtitle="Your paint collection, linked to recipes"
- actions: read the file to see the existing right-slot content; pass through unchanged.

**RecipesPage.tsx** (line ~133):
- title="Recipes", subtitle="Documented paint schemes for your models"
- actions: read the file to see the existing right-slot content; pass through unchanged.

**ArmyListsPage.tsx** (line ~65):
- title="Army Lists", subtitle="Points-tracked lists for the tabletop"
- actions: read the file to see the existing right-slot content; pass through unchanged.

**BattleLogPage.tsx** (line ~61):
- title="Battle Log", subtitle="Every game you've played, win or lose."
- actions={<Button onClick={openCreate}><Swords className="mr-2 h-4 w-4" /> Log Game</Button>} — the existing button is already a single element, no fragment needed.

**SpendingPage.tsx** (line ~62):
- title="Spending", subtitle="Total hobby spend tracked to the penny"
- actions: read the file to see the existing right-slot content; pass through unchanged.
- IMPORTANT: SpendingPage inserts its header INSIDE a `max-w-3xl` wrapper (Phase 16 Pitfall 1). Keep the PageHeader inside the same wrapper — do NOT lift it out. Border-b on PageHeader will span only the narrow content column, which is the intended Phase 16 behavior.

After all 7 files are edited, do NOT remove any imports until after the file is fully edited and you have verified `pnpm build` passes — sometimes the inline header used `Button` only for an action, and `Button` is still needed.

Subtitle strings must be copied byte-for-byte from the copy table (Battle Log subtitle ends with a period; the others do not — preserve exactly).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    - Each of the 7 files imports `PageHeader` from `@/components/common/PageHeader`.
    - Each of the 7 files contains exactly one `<PageHeader ...>` element (grep should return 7 matches across these files).
    - None of the 7 files still contains the literal string `text-3xl font-semibold tracking-tight` (grep on the 7 files returns 0 matches for that exact string).
    - All previous action buttons (view toggles, Add buttons, etc.) still render in the right slot.
    - `pnpm build` passes — no TypeScript errors, no unused imports.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace all inline header branches in DashboardPage</name>
  <files>src/features/dashboard/DashboardPage.tsx</files>
  <action>
DashboardPage has the inline header pattern duplicated in multiple branches. Verified by grep against the working tree — 4 occurrences in DashboardPage.tsx around lines 108 (error), 126 (loading), 200 (empty), 233 (populated). Re-read the file before editing in case anything has shifted; replace all occurrences.

Each occurrence has the same shape:
```tsx
<div className="flex items-center justify-between pb-6 border-b border-border/40">
  <div>
    <h1 className="text-3xl font-semibold tracking-tight">Dashboard</h1>
    <p className="text-sm text-muted-foreground mt-1">Your hobby command center at a glance</p>
  </div>
  <div className="flex items-center gap-2" />
</div>
```

For each occurrence:

1. Add (once, at the top of the file with other imports):
   ```ts
   import { PageHeader } from "@/components/common/PageHeader";
   ```

2. Replace each occurrence with:
   ```tsx
   <PageHeader
     title="Dashboard"
     subtitle="Your hobby command center at a glance"
   />
   ```

   Do NOT pass an empty `actions={<></>}` — omit the prop entirely. The original empty `<div className="flex items-center gap-2" />` is a placeholder reserved for Phase 26 (DASH-02 will add Quick Add + Log Session buttons here). Phase 25 ships without it.

3. Verify the file still has the same number of distinct render returns (error / loading / empty / populated) and that each one still wraps content in `<div className="flex flex-col gap-12 p-6">` — only the inner header div is being replaced, not the page-level wrapper.

After replacing all branches, run `pnpm build`. If TypeScript flags the `<div className="flex items-center gap-2" />` as unused/dead JSX in any branch you missed, fix that branch.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    - `src/features/dashboard/DashboardPage.tsx` imports `PageHeader`.
    - The file no longer contains the literal string `text-3xl font-semibold tracking-tight` (grep returns 0 matches in this file).
    - The file contains 4 occurrences of `<PageHeader title="Dashboard" subtitle="Your hobby command center at a glance" />` (or formatted across multiple lines — count by JSX tag, not by exact whitespace).
    - All render branches (error, loading, empty, populated) still render with their existing layout — verified by reading the diff.
    - `pnpm build` passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Upgrade FactionsPage from text-xl legacy header to canonical PageHeader</name>
  <files>src/features/factions/FactionsPage.tsx</files>
  <action>
FactionsPage is the lone outlier — it currently uses an older smaller-title pattern that predates the Phase 16 design overhaul. Per UI-SPEC §Component Inventory, FactionsPage is one of the 9 pages that must adopt PageHeader.

Existing block (around lines 71–77):
```tsx
<div className="flex items-center justify-between">
  <h1 className="text-xl font-semibold">Factions</h1>
  <Button onClick={openCreate}>
    <Plus className="mr-2 h-4 w-4" /> Add Faction
  </Button>
</div>
```

Replace with:
```tsx
<PageHeader
  title="Factions"
  actions={
    <Button onClick={openCreate}>
      <Plus className="mr-2 h-4 w-4" /> Add Faction
    </Button>
  }
/>
```

Add this import near the other top-level imports:
```ts
import { PageHeader } from "@/components/common/PageHeader";
```

Notes on this specific replacement:
- No `subtitle` prop — UI-SPEC §Copywriting Contract lists Factions title only ("to verify inline" — the existing code has no subtitle, so we ship without one).
- The title is upgraded from `text-xl` (20px) to `text-3xl` (28px) — this is intentional, per UI-SPEC §Typography "Display = 28px page titles (PageHeader h1)". This is a deliberate visual change that aligns Factions with every other page.
- The Add Faction button stays in the right slot, identical visual content.
- The outer page wrapper `<div className="flex flex-col gap-6 p-6">` is unchanged — only the inner header div changes.
- After this change, the page layout will gain `pb-6 border-b border-border/40` separator under the title — matches the design system, no extra adjustments needed.
- Existing `Plus` and `Button` imports stay — they are still used inside the actions prop.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    - `src/features/factions/FactionsPage.tsx` imports `PageHeader`.
    - The file contains `<PageHeader title="Factions" ...>` and the Add Faction Button is passed via the `actions` prop.
    - The file no longer contains the literal `text-xl font-semibold` (grep returns 0 matches in this file for that string).
    - `pnpm build` passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual smoke test — verify PageHeader renders correctly on all 9 pages</name>
  <files>(checkpoint — no files modified by this task)</files>
  <action>
Run the manual smoke test below in the live Tauri app. This task is a checkpoint — Claude does not modify any files. The user (or developer) runs the app, verifies each page header, and types "approved" to resume execution.

Steps:

1. Run `pnpm tauri dev` (full desktop app). Wait for the window to open.

2. Click each sidebar entry in turn and verify the page header looks correct against this table:

   | Page | Title (28px, semibold, tight tracking) | Subtitle (14px, muted) | Right-slot actions |
   |------|----------------------------------------|------------------------|---------------------|
   | Dashboard | Dashboard | "Your hobby command center at a glance" | (empty in Phase 25) |
   | Collection | Collection | "All units you own, tracked and filterable" | view-toggle icons + Add Unit |
   | Painting Projects | Painting Projects | "Active units being worked on right now" | (existing right-slot content) |
   | Paints | Paints | "Your paint collection, linked to recipes" | (existing right-slot content) |
   | Recipes | Recipes | "Documented paint schemes for your models" | (existing right-slot content) |
   | Army Lists | Army Lists | "Points-tracked lists for the tabletop" | (existing right-slot content) |
   | Battle Log | Battle Log | "Every game you've played, win or lose." | Log Game button |
   | Spending | Spending | "Total hobby spend tracked to the penny" | (existing right-slot content) |
   | Factions | Factions | (no subtitle) | Add Faction button |

3. For each page, confirm:
   - Title is `text-3xl` (visibly the same large size — compare against other pages).
   - Title is followed by a thin border-bottom separator (`border-b border-border/40` produces a faint line under the header block).
   - Subtitle (where present) is small muted text directly below the title (`mt-1` spacing).
   - Action buttons / icon toggles still work — clicking Add Unit / Log Game / Add Faction opens the corresponding Sheet.

4. Special check on Dashboard:
   - Force the loading state by hard-refreshing — header should appear identically while skeletons render.
   - Force the empty state if you have a fresh DB / fixture — header should still appear above the DashboardEmptyState.
   - Populated state — header above the 4-card stat row.

5. Special check on Factions: the title is now visibly larger than before this phase (28px vs the previous 20px). This is the intentional alignment to the design system — confirm it does not break the rest of the FactionsPage layout.

6. Special check on Spending: the header bottom-border should span only the narrow `max-w-3xl` content column (NOT the full window width) — preserves the Phase 16 Pitfall 1 layout.

7. If anything looks wrong (wrong subtitle, missing button, layout shift, double border, etc.), report which page and what is wrong. Otherwise, type "approved".
  </action>
  <verify>
    <automated>echo "Manual checkpoint — automated verification covered by Tasks 1–3 pnpm build"</automated>
  </verify>
  <done>
    - User has navigated to all 9 main pages in `pnpm tauri dev` and confirmed correct title, subtitle, and action-slot rendering against the table above.
    - User has typed "approved" (or described any issues for follow-up).
  </done>
  <what-built>
    All 9 main pages now render their header via the shared `PageHeader` component:
    Dashboard (4 branches), Collection, Painting Projects, Paints, Recipes,
    Army Lists, Battle Log, Spending, and Factions.
  </what-built>
  <how-to-verify>
    See the numbered steps in &lt;action&gt; above. Run `pnpm tauri dev`, walk through all 9 pages, verify against the rendering table.
  </how-to-verify>
  <resume-signal>Type "approved" when all 9 page headers render correctly, or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
After all 4 tasks complete:

1. `pnpm build` passes (TypeScript + Vite production build succeed).
2. Grep across the 9 modified files for the literal string `text-3xl font-semibold tracking-tight` returns 0 matches inside `<h1>` tags — only the canonical occurrence in `src/components/common/PageHeader.tsx` should remain. (Note: `src/features/dashboard/DashboardEmptyState.tsx` has the class on a `<span>` for the HobbyForge wordmark and that is intentionally untouched.)
3. Grep for `import { PageHeader }` returns 9 matches (one per modified page file).
4. Manual smoke test (Task 4) approved by user.
</verification>

<success_criteria>
- DSFD-02 fully satisfied: every main page renders via `<PageHeader />` with the locked title/subtitle copy and the appropriate actions slot.
- Visual consistency: all 9 pages share identical header geometry — 28px title, optional 14px muted subtitle, optional right-aligned actions, thin border-bottom separator with 24px gap below.
- Zero regressions: existing buttons, view toggles, and click handlers still work because the original action JSX is passed through unchanged via the `actions` prop.
- `pnpm build` passes.
- User has visually confirmed all 9 pages on the running Tauri app.
</success_criteria>

<output>
After completion, create `.planning/phases/25-design-foundation/25-02-SUMMARY.md` capturing:
- Confirmation that all 9 pages were updated.
- Note that DashboardPage gets PageHeader applied to all 4 render branches (error/loading/empty/populated).
- Note that FactionsPage was visually upgraded from `text-xl` (20px) to `text-3xl` (28px) — the only page where header sizing changed.
- Note that Phase 25 leaves Dashboard's actions slot empty — Phase 26 (DASH-02) will populate it with Quick Add + Log Session.
- Note that StatusBadge (Plan 01) is built but not yet applied — Phase 28 (COLL-02) wires it into UnitTable rows and UnitGallery cards.
- Confirmation that DSFD-01 (tokens), DSFD-02 (PageHeader applied), DSFD-03 (StatCard enriched), DSFD-04 (StatusBadge built) are all satisfied at the artifact level.
- Any visual surprises noted during the human-verify checkpoint.
</output>

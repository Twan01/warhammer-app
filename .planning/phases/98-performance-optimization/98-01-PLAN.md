---
phase: 98-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/router.tsx
  - src/features/painting-projects/KanbanCard.tsx
  - src/features/army-lists/ArmyListUnitRow.tsx
  - src/features/dashboard/CurrentFocusCard.tsx
  - tests/performance/lazyRoutes.test.ts
  - tests/performance/reactMemo.test.ts
autonomous: true
requirements:
  - PERF-01
  - PERF-04

must_haves:
  truths:
    - "Route pages load as separate chunks on first navigation (visible as dynamic import in Network tab)"
    - "A Suspense spinner appears while a lazy route chunk is downloading"
    - "KanbanCard, ArmyListUnitRow, and CurrentFocusCard are wrapped with React.memo"
  artifacts:
    - path: "src/app/router.tsx"
      provides: "Lazy route imports + Suspense boundaries"
      contains: "React.lazy"
    - path: "src/features/painting-projects/KanbanCard.tsx"
      provides: "Memoized KanbanCard"
      contains: "memo(function KanbanCard"
    - path: "src/features/army-lists/ArmyListUnitRow.tsx"
      provides: "Memoized ArmyListUnitRow"
      contains: "memo(function ArmyListUnitRow"
    - path: "src/features/dashboard/CurrentFocusCard.tsx"
      provides: "Memoized CurrentFocusCard"
      contains: "memo(function CurrentFocusCard"
    - path: "tests/performance/lazyRoutes.test.ts"
      provides: "Lazy route verification tests"
    - path: "tests/performance/reactMemo.test.ts"
      provides: "React.memo verification tests"
  key_links:
    - from: "src/app/router.tsx"
      to: "React.lazy dynamic imports"
      via: "lazy(() => import('./page').then(m => ({ default: m.PageName })))"
      pattern: "lazy\\("
---

<objective>
Convert all 16 route page imports to React.lazy dynamic imports with Suspense fallback, and wrap 3 high-frequency components with React.memo.

Purpose: Reduce initial bundle size by splitting each route into its own chunk (PERF-01), and prevent unnecessary re-renders on list/board card components (PERF-04).
Output: Modified router.tsx with lazy imports + Suspense, 3 memoized component files, 2 test files.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-performance-optimization/98-CONTEXT.md
@.planning/phases/98-performance-optimization/98-RESEARCH.md
@.planning/phases/98-performance-optimization/98-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lazy route imports + Suspense boundaries in router.tsx</name>
  <files>src/app/router.tsx, tests/performance/lazyRoutes.test.ts</files>
  <read_first>
    - src/app/router.tsx (current state -- all 16 static page imports, layoutRoute and bareLayoutRoute component definitions)
    - .planning/phases/98-performance-optimization/98-PATTERNS.md (Pattern: React.lazy route conversion, Suspense boundary placement, named export adapter)
  </read_first>
  <action>
    Replace lines 14-29 (the 16 static page imports) with React.lazy dynamic imports using the named-export adapter pattern per D-01 and D-03. Each import becomes:
    `const PageName = lazy(() => import("./path/page").then(m => ({ default: m.PageName })));`

    All 16 page names and their module paths:
    - DashboardPage from "./dashboard/page"
    - CollectionPage from "./collection/page"
    - PaintingProjectsPage from "./painting-projects/page"
    - RecipesPage from "./recipes/page"
    - PaintsPage from "./paints/page"
    - SettingsPage from "./settings/page"
    - FactionsPage from "./factions/page"
    - ArmyListsPage from "./army-lists/page"
    - SpendingPage from "./spending/page"
    - BattleLogPage from "./battle-log/page"
    - WishlistPage from "./wishlist/page"
    - GoalsPage from "./goals/page"
    - RulesHubPageShell from "./rules-hub/page"
    - GameDayPageShell from "./game-day/page"
    - DataHealthPage from "./data-health/page"
    - PaintingModePage from "./painting-mode/page"

    Add `import { lazy, Suspense } from "react";` at top. Import `Loader2` from `lucide-react` for the spinner.

    Per D-02, add Suspense wrapping the Outlet inside both layout routes:
    1. In layoutRoute component (line 52-58): wrap `<Outlet />` with `<Suspense fallback={...}>` INSIDE ActiveFactionProvider, not outside AppLayout.
    2. In bareLayoutRoute component (line 69-77): wrap `<Outlet />` with `<Suspense fallback={...}>` INSIDE TooltipProvider, above Toaster.

    The Suspense fallback is: `<div className="flex h-full items-center justify-center text-muted-foreground"><Loader2 className="h-6 w-6 animate-spin" /></div>`

    Create test file `tests/performance/lazyRoutes.test.ts` that verifies the router exports a valid router object, and verifies the module no longer statically imports page components (by importing the router module and checking it loads without errors).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - router.tsx contains `import { lazy, Suspense } from "react"` at top
    - router.tsx contains exactly 16 `lazy(() => import(` calls, one per page
    - router.tsx contains zero static imports from `./dashboard/page`, `./collection/page`, etc.
    - layoutRoute component contains `<Suspense fallback=` wrapping `<Outlet />`
    - bareLayoutRoute component contains `<Suspense fallback=` wrapping `<Outlet />`
    - `pnpm build` succeeds (TypeScript check + Vite bundle)
    - tests/performance/lazyRoutes.test.ts exists and passes via `pnpm test -- tests/performance/lazyRoutes.test.ts`
  </acceptance_criteria>
  <done>All 16 route page imports are lazy-loaded via React.lazy with named-export adapters, both layout routes have Suspense boundaries wrapping Outlet, and build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: React.memo on KanbanCard, ArmyListUnitRow, CurrentFocusCard</name>
  <files>src/features/painting-projects/KanbanCard.tsx, src/features/army-lists/ArmyListUnitRow.tsx, src/features/dashboard/CurrentFocusCard.tsx, tests/performance/reactMemo.test.ts</files>
  <read_first>
    - src/features/painting-projects/KanbanCard.tsx (current export signature + imports)
    - src/features/army-lists/ArmyListUnitRow.tsx (current export signature + imports -- note: has internal hooks)
    - src/features/dashboard/CurrentFocusCard.tsx (current export signature + imports)
    - .planning/phases/98-performance-optimization/98-PATTERNS.md (Pattern: React.memo Named Export Wrap)
  </read_first>
  <action>
    Per D-09 and D-10, wrap exactly 3 components with React.memo using shallow comparison (default behavior).

    For each component file:
    1. Add `import { memo } from "react";` (or add `memo` to existing React import)
    2. Change `export function ComponentName(` to `export const ComponentName = memo(function ComponentName(`
    3. Add closing `});` after the function body
    4. Add `ComponentName.displayName = "ComponentName";` after the declaration

    Files and their export names:
    - KanbanCard.tsx: `export function KanbanCard({...}: KanbanCardProps)` becomes `export const KanbanCard = memo(function KanbanCard({...}: KanbanCardProps) { ... });`
    - ArmyListUnitRow.tsx: `export function ArmyListUnitRow({...}: ArmyListUnitRowProps)` becomes `export const ArmyListUnitRow = memo(function ArmyListUnitRow({...}: ArmyListUnitRowProps) { ... });`
    - CurrentFocusCard.tsx: `export function CurrentFocusCard({...}: CurrentFocusCardProps)` becomes `export const CurrentFocusCard = memo(function CurrentFocusCard({...}: CurrentFocusCardProps) { ... });`

    Create test file `tests/performance/reactMemo.test.ts` that verifies all 3 components are wrapped with React.memo by checking `$$typeof` contains "memo" symbol on the exported component.
  </action>
  <verify>
    <automated>pnpm test -- tests/performance/reactMemo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - KanbanCard.tsx contains `export const KanbanCard = memo(function KanbanCard(`
    - KanbanCard.tsx contains `KanbanCard.displayName = "KanbanCard"`
    - ArmyListUnitRow.tsx contains `export const ArmyListUnitRow = memo(function ArmyListUnitRow(`
    - ArmyListUnitRow.tsx contains `ArmyListUnitRow.displayName = "ArmyListUnitRow"`
    - CurrentFocusCard.tsx contains `export const CurrentFocusCard = memo(function CurrentFocusCard(`
    - CurrentFocusCard.tsx contains `CurrentFocusCard.displayName = "CurrentFocusCard"`
    - tests/performance/reactMemo.test.ts passes: all 3 components have `$$typeof` matching memo symbol
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>KanbanCard, ArmyListUnitRow, and CurrentFocusCard are wrapped with React.memo with displayName set, verified by automated tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan modifies only import statements and component wrappers -- no new data inputs or API surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | Dynamic imports (lazy) | accept | Vite bundles chunks from local source; no external CDN or runtime code injection. CSP is already off per tauri.conf.json. Desktop app -- no network-served chunks. |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `pnpm build` succeeds (TypeScript + Vite bundle produces separate chunks)
- `pnpm test -- tests/performance` passes all new tests
- `pnpm test` full suite passes (no regressions)
</verification>

<success_criteria>
- 16 route pages lazy-loaded via React.lazy (PERF-01)
- Suspense boundaries on both layout routes showing spinner during chunk load
- 3 components wrapped with React.memo (PERF-04)
- All tests pass, build succeeds
</success_criteria>

<output>
Create `.planning/phases/98-performance-optimization/98-01-SUMMARY.md` when done
</output>

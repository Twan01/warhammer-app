---
phase: 98-performance-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/queries/syncedUnitPoints.ts
  - src/db/queries/bsdataExtended.ts
  - tests/performance/batchInsert.test.ts
autonomous: true
requirements:
  - DBH-04

must_haves:
  truths:
    - "Sync operations insert rows using multi-row VALUES statements, not individual INSERT loops"
    - "Each replace* function produces at most ceil(N/200) INSERT calls instead of N calls"
    - "Empty row arrays are handled gracefully without generating invalid SQL"
  artifacts:
    - path: "src/db/queries/syncedUnitPoints.ts"
      provides: "Batched INSERT for replaceSyncedUnitPoints and replaceSyncedUnitPointTiers"
      contains: "VALUES ${placeholders}"
    - path: "src/db/queries/bsdataExtended.ts"
      provides: "Batched INSERT for 4 replace* functions"
      contains: "VALUES ${placeholders}"
    - path: "tests/performance/batchInsert.test.ts"
      provides: "Tests verifying single INSERT call for N rows"
  key_links:
    - from: "src/db/queries/syncedUnitPoints.ts"
      to: "db.execute"
      via: "multi-row VALUES with positional params"
      pattern: "\\$\\{base \\+ 1\\}"
    - from: "src/db/queries/bsdataExtended.ts"
      to: "db.execute"
      via: "multi-row VALUES with positional params"
      pattern: "\\$\\{base \\+ 1\\}"
---

<objective>
Convert all 6 replace* sync functions from per-row INSERT loops to multi-row INSERT VALUES statements with chunking.

Purpose: Reduce SQLite round-trips during sync/import operations from N individual INSERTs to ceil(N/200) batched INSERTs (DBH-04).
Output: Modified syncedUnitPoints.ts (2 functions) and bsdataExtended.ts (4 functions), plus test file.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-performance-optimization/98-CONTEXT.md
@.planning/phases/98-performance-optimization/98-RESEARCH.md
@.planning/phases/98-performance-optimization/98-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batched INSERT in syncedUnitPoints.ts</name>
  <files>src/db/queries/syncedUnitPoints.ts, tests/performance/batchInsert.test.ts</files>
  <read_first>
    - src/db/queries/syncedUnitPoints.ts (current per-row INSERT loops in replaceSyncedUnitPoints and replaceSyncedUnitPointTiers)
    - .planning/phases/98-performance-optimization/98-PATTERNS.md (Pattern: Multi-row VALUES placeholder builder, Transaction wrapper, Batch INSERT test patterns)
  </read_first>
  <action>
    Per D-11 and D-12, convert the two replace* functions from per-row INSERT loops to multi-row VALUES batched INSERT.

    For `replaceSyncedUnitPoints` (4 columns: unit_name, faction_id, points, synced_at):
    1. Keep the BEGIN TRANSACTION / DELETE / COMMIT / ROLLBACK structure
    2. After DELETE, add early return with COMMIT if `rows.length === 0`
    3. Replace the for-of loop with a chunked batch loop: BATCH_SIZE = 200, COL_COUNT = 4
    4. For each chunk: build placeholders using `batch.map((_, i) => { const base = i * 4; return "($" + (base+1) + ... })`, flatMap params as `[row.unit_name, row.faction_id, row.points, syncedAt]`
    5. Execute single INSERT per chunk: `INSERT INTO synced_unit_points (unit_name, faction_id, points, synced_at) VALUES ${placeholders}`

    For `replaceSyncedUnitPointTiers` (5 columns: unit_name, faction_id, model_count, points, synced_at):
    Same pattern with COL_COUNT = 5 and params `[row.unit_name, row.faction_id, row.model_count, row.points, syncedAt]`

    Create `tests/performance/batchInsert.test.ts`:
    - Mock `getDb` to return `{ execute: executeMock, select: vi.fn() }`
    - Test `replaceSyncedUnitPoints` with 2 rows: assert exactly 1 INSERT call (not 2), verify SQL contains `VALUES ($1, $2, $3, $4), ($5, $6, $7, $8)`, verify params length is 8
    - Test empty array: assert no INSERT call (only BEGIN + DELETE + COMMIT)
    - Test `replaceSyncedUnitPointTiers` similarly with COL_COUNT=5
  </action>
  <verify>
    <automated>pnpm test -- tests/performance/batchInsert.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - replaceSyncedUnitPoints no longer contains `for (const row of rows)` followed by individual INSERT
    - replaceSyncedUnitPoints contains `VALUES ${placeholders}` with dynamic placeholder building
    - replaceSyncedUnitPoints contains `const BATCH_SIZE = 200` (or equivalent chunking constant)
    - replaceSyncedUnitPointTiers follows the same batched pattern with COL_COUNT = 5
    - Both functions still wrap in BEGIN TRANSACTION / COMMIT / ROLLBACK
    - Tests verify: 2 rows produce 1 INSERT call, empty array produces 0 INSERT calls
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>replaceSyncedUnitPoints and replaceSyncedUnitPointTiers use multi-row batched INSERT with 200-row chunking, verified by tests</done>
</task>

<task type="auto">
  <name>Task 2: Batched INSERT in bsdataExtended.ts</name>
  <files>src/db/queries/bsdataExtended.ts</files>
  <read_first>
    - src/db/queries/bsdataExtended.ts (current per-row INSERT loops in all 4 replace* functions)
    - src/db/queries/syncedUnitPoints.ts (reference: the batched pattern from Task 1)
    - .planning/phases/98-performance-optimization/98-PATTERNS.md (Column counts per function table, boolean cast for replaceSyncedLoadoutOptions)
  </read_first>
  <action>
    Apply the same multi-row INSERT pattern from Task 1 to all 4 replace* functions in bsdataExtended.ts per D-11.

    Column counts and param mappings for each function:

    1. `replaceSyncedEnhancements` -- COL_COUNT = 5, params per row: `[row.name, row.faction_id, row.detachment_name, row.points, syncedAt]`

    2. `replaceSyncedLoadoutOptions` -- COL_COUNT = 7, params per row: `[row.unit_name, row.faction_id, row.group_name, row.option_name, row.is_default ? 1 : 0, row.is_exclusive ? 1 : 0, syncedAt]` -- IMPORTANT: preserve the boolean-to-integer cast `? 1 : 0` for is_default and is_exclusive

    3. `replaceSyncedModelCounts` -- COL_COUNT = 5, params per row: `[row.unit_name, row.faction_id, row.min_models, row.max_models, syncedAt]`

    4. `replaceSyncedLeaderTargets` -- COL_COUNT = 4, params per row: `[row.leader_name, row.faction_id, row.target_name, syncedAt]`

    For each function: keep BEGIN TRANSACTION / DELETE / COMMIT / ROLLBACK structure, add empty array guard, replace for-of loop with BATCH_SIZE=200 chunked multi-row INSERT.

    Add tests for at least one bsdataExtended function (replaceSyncedEnhancements) to `tests/performance/batchInsert.test.ts` confirming the same single-call pattern.
  </action>
  <verify>
    <automated>pnpm test -- tests/performance/batchInsert.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 replace* functions in bsdataExtended.ts no longer contain `for (const row of rows)` followed by individual INSERT
    - All 4 functions contain `VALUES ${placeholders}` with dynamic placeholder building
    - replaceSyncedLoadoutOptions params flatMap contains `row.is_default ? 1 : 0` and `row.is_exclusive ? 1 : 0`
    - All 4 functions still wrap in BEGIN TRANSACTION / COMMIT / ROLLBACK
    - `pnpm build` succeeds
    - `pnpm test -- tests/performance/batchInsert.test.ts` passes
  </acceptance_criteria>
  <done>All 4 bsdataExtended replace* functions use multi-row batched INSERT with 200-row chunking, boolean casts preserved for loadout options</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | SQL changes use same parameterized queries with same data -- no new input paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-02 | Injection | Multi-row VALUES SQL | mitigate | Placeholders are positional `$N` params built from array index math, not string interpolation of user data. All values pass through parameterized binding. |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `pnpm build` succeeds
- `pnpm test -- tests/performance/batchInsert.test.ts` passes
- `pnpm test` full suite passes
</verification>

<success_criteria>
- 6 replace* functions across 2 files use multi-row batched INSERT (DBH-04)
- 200-row chunking guards against SQLite parameter limits
- Empty array handling prevents invalid SQL
- Boolean casts preserved in loadout options
- All tests pass, build succeeds
</success_criteria>

<output>
Create `.planning/phases/98-performance-optimization/98-02-SUMMARY.md` when done
</output>

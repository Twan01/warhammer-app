---
phase: 119-stratagems-enhancements-import
plan: 02
type: execute
wave: 2
depends_on: [119-01]
files_modified:
  - src-tauri/src/lib.rs
autonomous: true
requirements: [STR-01, STR-02, ENH-01]

must_haves:
  truths:
    - "Rust importer deserializes stratagems and enhancements arrays from unit_database.json"
    - "udb_stratagems and udb_enhancements tables are populated after import"
    - "Universal stratagems with null faction_id are inserted without FK constraint errors"
    - "Migrations 042 and 043 are both registered and execute on app startup"
    - "Import result reports stratagem and enhancement counts"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "Extended Rust importer with stratagems + enhancements support"
      contains: "udb_stratagems"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/data/unit_database.json"
      via: "serde deserialization of UnitDatabasePayload"
      pattern: "stratagems.*Vec.*JsRow"
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/migrations/043_udb_stratagems_enhancements.sql"
      via: "include_str! in get_migrations()"
      pattern: "include_str.*043_udb_stratagems"
---

<objective>
Extend the Rust importer to handle stratagems and enhancements: register migrations 042+043, extend payload/result structs, add DELETE and INSERT blocks in correct FK order.

Purpose: Complete the data pipeline so stratagems and enhancements flow from JSON into SQLite tables at app startup.
Output: Working Rust import of stratagems and enhancements; app boots without migration errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-stratagems-enhancements-import/119-CONTEXT.md
@.planning/phases/119-stratagems-enhancements-import/119-RESEARCH.md
@.planning/phases/119-stratagems-enhancements-import/119-PATTERNS.md
@.planning/phases/119-stratagems-enhancements-import/119-01-SUMMARY.md

<interfaces>
<!-- From src-tauri/src/lib.rs — existing Rust patterns -->
From src-tauri/src/lib.rs:

fn str_val(row: &JsRow, key: &str) -> Option<String>
  // Returns None for empty strings — binds as SQL NULL via sqlx

fn i64_val(row: &JsRow, key: &str) -> Option<i64>
  // Handles both numeric JSON values and string-encoded numbers

struct UnitDatabasePayload {
    // ... existing fields ...
    #[serde(default)] detachments: Vec<JsRow>,
    #[serde(default)] detachment_abilities: Vec<JsRow>,
    // stratagems and enhancements to be added here
}

struct UdbImportResult {
    pub factions: u64, pub units: u64, pub models: u64, pub weapons: u64,
    pub abilities: u64, pub keywords: u64, pub points: u64, pub composition: u64,
    pub detachments: u64, pub detachment_abilities: u64,
    // stratagems and enhancements to be added here
}

// get_migrations() currently ends at version 41 — need to add 42 and 43

// DELETE loop iterates over table name list — add udb_stratagems, udb_enhancements before udb_detachment_abilities
// INSERT section — add stratagems/enhancements blocks after detachment_abilities block
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register migrations and extend Rust structs</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/lib.rs (find get_migrations() ending at version 41, UnitDatabasePayload struct around line 476, UdbImportResult struct around line 505, both UdbImportResult { initializations around lines 562 and 578)
    - .planning/phases/119-stratagems-enhancements-import/119-PATTERNS.md (exact Rust patterns for struct extension and migration registration)
  </read_first>
  <action>
    In src-tauri/src/lib.rs:

    1. Register migration 042 in get_migrations() after version 41: Migration { version: 42, description: "udb_detachments", sql: include_str!("../migrations/042_udb_detachments.sql"), kind: MigrationKind::Up }. This is CRITICAL — 042 exists on disk but was never registered. Without it, migration 043 will fail (FK to udb_detachments).

    2. Register migration 043 after 042: Migration { version: 43, description: "udb_stratagems_enhancements", sql: include_str!("../migrations/043_udb_stratagems_enhancements.sql"), kind: MigrationKind::Up }. Per D-18.

    3. Add two new fields to UnitDatabasePayload after detachment_abilities: #[serde(default)] stratagems: Vec<JsRow> and #[serde(default)] enhancements: Vec<JsRow>. Per D-15.

    4. Add two new fields to UdbImportResult after detachment_abilities: pub stratagems: u64 and pub enhancements: u64. Per D-17.

    5. CRITICAL — Update BOTH UdbImportResult literal initializations (Pitfall 3): the early-return path (version-match, around line 562) and the normal counts initializer (around line 578). Add stratagems: 0, enhancements: 0 to both.

    6. Add "udb_stratagems" and "udb_enhancements" to the DELETE table list, positioned before "udb_detachment_abilities" (FK child before parent convention). Per D-16.

    7. Add INSERT block for stratagems after the detachment_abilities INSERT block. Bind 9 columns: id, faction_id (str_val — nullable Option<String>), detachment_id (str_val — nullable), name (unwrap_or_default), type (str_val — nullable), cp_cost (i64_val unwrap_or(0)), turn (str_val — nullable), phase (str_val — nullable), description (unwrap_or_default). Increment counts.stratagems. Per D-16.

    8. Add INSERT block for enhancements after stratagems block. Bind 6 columns: id, faction_id (unwrap_or_default — NOT NULL), detachment_id (str_val — nullable), name (unwrap_or_default), cost (i64_val unwrap_or(0)), description (unwrap_or_default). Increment counts.enhancements. Per D-16.

    IMPORTANT for nullable FK binding:
    - Stratagems faction_id and detachment_id: use .bind(str_val(row, "field")) — NOT unwrap_or_default. str_val returns None for empty strings, which sqlx binds as SQL NULL.
    - Enhancements faction_id: use .bind(str_val(row, "faction_id").unwrap_or_default()) — NOT NULL column.
    - Enhancements detachment_id: use .bind(str_val(row, "detachment_id")) — nullable.
  </action>
  <acceptance_criteria>
    - get_migrations() contains version 42 entry with include_str!("../migrations/042_udb_detachments.sql")
    - get_migrations() contains version 43 entry with include_str!("../migrations/043_udb_stratagems_enhancements.sql")
    - UnitDatabasePayload has stratagems: Vec<JsRow> and enhancements: Vec<JsRow> with #[serde(default)]
    - UdbImportResult has pub stratagems: u64 and pub enhancements: u64
    - Both UdbImportResult { literal blocks contain stratagems: 0, enhancements: 0
    - DELETE list contains "udb_stratagems" and "udb_enhancements" before "udb_detachment_abilities"
    - INSERT block for udb_stratagems binds faction_id via str_val(row, "faction_id") (no unwrap) for nullable FK
    - INSERT block for udb_enhancements binds faction_id via str_val(row, "faction_id").unwrap_or_default() for NOT NULL
    - cargo check passes cleanly (no compile errors)
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Rust importer extended with migrations 042+043 registered, structs extended, DELETE list updated, and INSERT blocks added for stratagems (9 columns, nullable FKs) and enhancements (6 columns, NOT NULL faction_id). cargo check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Commit rebuilt JSON and verify full pipeline</name>
  <files>src-tauri/data/unit_database.json</files>
  <read_first>
    - src-tauri/data/unit_database.json (verify stratagems and enhancements arrays exist from Plan 01 rebuild)
  </read_first>
  <action>
    1. Run pnpm build to verify TypeScript compilation passes with the new types and build script changes.
    2. Verify unit_database.json contains stratagems array with 1300+ entries and enhancements array with 800+ entries.
    3. Verify at least one universal stratagem has faction_id: null in the JSON (confirming STR-02 null FK handling).
    4. Stage and commit the rebuilt unit_database.json alongside the lib.rs changes if not already committed.

    Discretion decisions applied:
    - HTML descriptions kept as-is (consistent with Phase 118 detachment abilities pattern)
    - cp_cost parsed with parseInt || 0 (handles 0-cost stratagems and any non-numeric edge cases)
    - No FK reference validation during build (warning not needed — the Rust importer handles FK enforcement at import time)
  </action>
  <acceptance_criteria>
    - pnpm build completes without TypeScript errors
    - unit_database.json stratagems array length is 1300+
    - unit_database.json enhancements array length is 800+
    - At least one stratagem in JSON has faction_id: null (universal/core stratagem)
    - All enhancements in JSON have non-empty faction_id string
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && node -e "const d=JSON.parse(require('fs').readFileSync('src-tauri/data/unit_database.json','utf8')); const s=d.stratagems.length; const e=d.enhancements.length; const u=d.stratagems.filter(x=>x.faction_id===null).length; const badEnh=d.enhancements.filter(x=>!x.faction_id).length; console.log('Stratagems:',s,'Enhancements:',e,'Universal:',u,'BadEnh:',badEnh); if(s<1300||e<800||u<1||badEnh>0){process.exit(1)}; console.log('Pipeline OK')"</automated>
  </verify>
  <done>Full pipeline verified: TypeScript builds, JSON contains 1300+ stratagems (with universal null-FK entries) and 800+ enhancements. Rust compiles. Ready for app startup testing in Phase 120.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON artifact to Rust deserializer | Build artifact shipped in binary; no runtime user input |
| Rust INSERT to SQLite | Parameterized queries via sqlx (? placeholders); no string interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-04 | Injection | Rust INSERT blocks | mitigate | All SQL uses sqlx parameterized ? placeholders — no string interpolation. str_val/i64_val helpers sanitize types. |
| T-119-05 | Tampering | unit_database.json | accept | Build artifact bundled in app binary. Developer-controlled pipeline. No user-facing modification surface. |
| T-119-06 | Denial of Service | Migration 042+043 registration | accept | One-time DDL execution at app startup. IF NOT EXISTS guards prevent duplicate table creation on re-run. |
</threat_model>

<verification>
1. cargo check passes (Rust compilation)
2. pnpm build passes (TypeScript compilation)
3. unit_database.json has stratagems array of 1300+ entries
4. unit_database.json has enhancements array of 800+ entries
5. Universal stratagems have faction_id: null in JSON
6. All enhancements have non-empty faction_id in JSON
</verification>

<success_criteria>
- Migrations 042 and 043 registered in get_migrations()
- UnitDatabasePayload and UdbImportResult extended with stratagem/enhancement fields
- DELETE list includes udb_stratagems and udb_enhancements before detachment tables
- INSERT blocks use correct nullable/non-nullable binding patterns
- cargo check and pnpm build both pass
</success_criteria>

<output>
Create `.planning/phases/119-stratagems-enhancements-import/119-02-SUMMARY.md` when done
</output>

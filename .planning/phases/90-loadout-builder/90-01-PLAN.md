---
phase: 90-loadout-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/queries/bsdataExtended.ts
  - src/db/queries/syncedUnitPoints.ts
  - src/hooks/useLoadoutOptions.ts
  - src/features/army-lists/ArmyListUnitRow.tsx
  - src/features/army-lists/ArmyListDetailSheet.tsx
  - tests/army-lists/LoadoutBuilderSheet.test.tsx
autonomous: true
requirements:
  - DL-01
  - DL-02

must_haves:
  truths:
    - "getLoadoutOptionsForUnit returns wargear options filtered by unit name and faction"
    - "getTiersByUnitName returns synced point tiers filtered by unit name and faction"
    - "useLoadoutOptionsForUnit hook queries wargear with correct cache key"
    - "useTiersByUnitName hook queries tiers with correct cache key"
    - "ArmyListUnitRow shows a Configure trigger instead of the old inline tier selector"
    - "ArmyListDetailSheet passes onConfigureUnit callback to each ArmyListUnitRow"
  artifacts:
    - path: "src/hooks/useLoadoutOptions.ts"
      provides: "React Query hooks for wargear options and synced tiers by name"
      exports: ["useLoadoutOptionsForUnit", "useTiersByUnitName", "LOADOUT_OPTIONS_KEY", "SYNCED_TIERS_BY_NAME_KEY"]
    - path: "src/db/queries/bsdataExtended.ts"
      provides: "getLoadoutOptionsForUnit query function"
      contains: "getLoadoutOptionsForUnit"
    - path: "src/db/queries/syncedUnitPoints.ts"
      provides: "getTiersByUnitName query function"
      contains: "getTiersByUnitName"
  key_links:
    - from: "src/hooks/useLoadoutOptions.ts"
      to: "src/db/queries/bsdataExtended.ts"
      via: "import getLoadoutOptionsForUnit"
      pattern: "getLoadoutOptionsForUnit"
    - from: "src/hooks/useLoadoutOptions.ts"
      to: "src/db/queries/syncedUnitPoints.ts"
      via: "import getTiersByUnitName"
      pattern: "getTiersByUnitName"
    - from: "src/features/army-lists/ArmyListUnitRow.tsx"
      to: "src/features/army-lists/ArmyListDetailSheet.tsx"
      via: "onConfigure prop callback"
      pattern: "onConfigure"
---

<objective>
Add query functions and hooks for unit-specific wargear/tier lookup, refactor ArmyListUnitRow to replace the inline tier selector with a Configure trigger, and thread the onConfigureUnit callback through ArmyListDetailSheet.

Purpose: Establishes the data layer and row-level UX for the LoadoutBuilderSheet (Plan 02). The inline Phase 24 tier selector that wrote to units.points is removed per D-02/D-04.

Output: Two new query functions, one new hook file, refactored ArmyListUnitRow with Configure button, and ArmyListDetailSheet with onConfigureUnit callback prop.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-loadout-builder/90-CONTEXT.md
@.planning/phases/90-loadout-builder/90-RESEARCH.md
@.planning/phases/90-loadout-builder/90-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/armyList.ts:
```typescript
export interface ArmyListUnitRow extends ArmyListUnit {
  unit_name: string;
  unit_points: number | null;
  effective_points: number;
  faction_id: number | null;
  status_assembly: number | null;
  status_painting: string | null;
  painting_percentage: number | null;
  tactical_role: string | null;
  synced_points: number | null;
  override_points: number | null;
  tier_points: number | null;
}
```

From src/db/queries/bsdataExtended.ts:
```typescript
export interface SyncedLoadoutOptionRow {
  unit_name: string;
  faction_id: string | null;
  group_name: string;
  option_name: string;
  is_default: number;
  is_exclusive: number;
}
export async function getLoadoutOptionsByFaction(factionId: string): Promise<SyncedLoadoutOptionRow[]>;
```

From src/db/queries/syncedUnitPoints.ts:
```typescript
export async function getPointTiersByFaction(factionId: string): Promise<SyncedUnitPointTier[]>;
```

From src/hooks/useArmyLists.ts (lines 347-382):
```typescript
export function useSetSelectedModelCount(): UseMutationResult<void, Error, SetSelectedModelCountVariables>;
export function useClearSelectedModelCount(): UseMutationResult<void, Error, ClearSelectedModelCountVariables>;
```

From src/features/army-lists/ArmyListDetailSheet.tsx:
```typescript
interface ArmyListDetailSheetProps {
  open: boolean;
  list: ArmyList | null;
  onClose: () => void;
  onEdit: (list: ArmyList) => void;
  onDelete: (list: ArmyList) => void;
  onAddUnit: () => void;
}
```

From src/features/army-lists/ArmyListUnitRow.tsx:
```typescript
interface ArmyListUnitRowProps {
  unit: ArmyListUnitRowType;
  totalPoints: number;
  pointsLimit: number | null;
  freshness: SyncFreshness;
  onRemove: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add query functions + hooks for unit-level wargear and tier lookup</name>
  <files>
    src/db/queries/bsdataExtended.ts,
    src/db/queries/syncedUnitPoints.ts,
    src/hooks/useLoadoutOptions.ts
  </files>
  <read_first>
    src/db/queries/bsdataExtended.ts,
    src/db/queries/syncedUnitPoints.ts,
    src/hooks/useUnitPointTiers.ts,
    src/types/armyList.ts
  </read_first>
  <action>
    1. In bsdataExtended.ts, append `getLoadoutOptionsForUnit(unitName: string, factionId: string | null): Promise<SyncedLoadoutOptionRow[]>` after `getLoadoutOptionsByFaction` (line ~145). Query: SELECT group_name, option_name, is_default, is_exclusive FROM synced_loadout_options WHERE unit_name = $1 AND (faction_id IS NULL OR faction_id = $2) ORDER BY group_name, option_name. Parameters: [unitName, factionId]. Per D-07.

    2. In syncedUnitPoints.ts, append `getTiersByUnitName(unitName: string, factionId: string | null): Promise<Array<{ model_count: number; points: number }>>` after `getPointTiersByFaction` (line ~86). Query: SELECT model_count, points FROM synced_unit_point_tiers WHERE unit_name = $1 AND (faction_id IS NULL OR faction_id = $2) ORDER BY model_count ASC. Parameters: [unitName, factionId]. Per D-10 (supports ghost units via name-based lookup).

    3. Create src/hooks/useLoadoutOptions.ts with two hooks and two cache key factories following the useUnitPointTiers.ts pattern:
       - LOADOUT_OPTIONS_KEY = (unitName, factionId) => ["loadout-options", unitName, factionId] as const
       - SYNCED_TIERS_BY_NAME_KEY = (unitName, factionId) => ["synced-tiers-by-name", unitName, factionId] as const
       - useLoadoutOptionsForUnit(unitName: string | undefined, factionId: string | null | undefined) — calls getLoadoutOptionsForUnit, enabled when unitName !== undefined, staleTime 5 min
       - useTiersByUnitName(unitName: string | undefined, factionId: string | null | undefined) — calls getTiersByUnitName, enabled when unitName !== undefined, staleTime 5 min
       Both hooks accept string | null for factionId because synced tables store faction_id as TEXT (Pitfall 1 from RESEARCH.md — never pass a number).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - bsdataExtended.ts exports getLoadoutOptionsForUnit with signature (unitName: string, factionId: string | null) => Promise<SyncedLoadoutOptionRow[]>
    - syncedUnitPoints.ts exports getTiersByUnitName with signature (unitName: string, factionId: string | null) => Promise<Array<{ model_count: number; points: number }>>
    - src/hooks/useLoadoutOptions.ts exports useLoadoutOptionsForUnit, useTiersByUnitName, LOADOUT_OPTIONS_KEY, SYNCED_TIERS_BY_NAME_KEY
    - Both hooks have enabled: unitName !== undefined guard
    - Both hooks use staleTime: 5 * 60 * 1000
    - pnpm build passes with no type errors
  </acceptance_criteria>
  <done>Two new query functions and two new hooks exist, TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ArmyListUnitRow + ArmyListDetailSheet for Configure trigger</name>
  <files>
    src/features/army-lists/ArmyListUnitRow.tsx,
    src/features/army-lists/ArmyListDetailSheet.tsx
  </files>
  <read_first>
    src/features/army-lists/ArmyListUnitRow.tsx,
    src/features/army-lists/ArmyListDetailSheet.tsx,
    src/hooks/useArmyLists.ts,
    src/features/army-lists/ArmyListsPage.tsx
  </read_first>
  <action>
    **ArmyListUnitRow.tsx refactor (per D-02, D-04):**
    1. Add onConfigure: () => void to ArmyListUnitRowProps interface.
    2. Remove the entire inline tier selector block (lines 269-312): the hasTiers conditional, the Select with pendingTierId, and the Confirm button.
    3. Remove the pendingTierId state (useState), candidatePoints memo (useMemo), and the useUpdateUnit import + hook call — these are no longer needed since tier selection moves to the Sheet.
    4. Remove the useUnitPointTiers import and hook call if only used for the tier selector.
    5. Add import for Settings2 from lucide-react.
    6. After the PointsSourceChip (line 268), add a compact Configure trigger button:
       - Compute tierLabel: if unit.selected_model_count !== null && unit.tier_points !== null, show "{selected_model_count} models + {tier_points}pts"; otherwise show "Configure"
       - Render a Button variant="outline" size="sm" className="h-7 text-xs mt-1" with Settings2 icon (h-3 w-3 mr-1) and tierLabel text
       - onClick calls onConfigure prop
       - aria-label="Configure loadout for {unit.unit_name}"

    **ArmyListDetailSheet.tsx refactor:**
    1. Add onConfigureUnit: (armyListUnitId: number) => void to ArmyListDetailSheetProps interface.
    2. Destructure onConfigureUnit from props in the function signature.
    3. In the ArmyListUnitRow render loop (line ~229-236), add onConfigure={() => onConfigureUnit(alu.id)} prop.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - ArmyListUnitRowProps includes onConfigure: () => void
    - No pendingTierId state, candidatePoints memo, or useUpdateUnit/useUnitPointTiers hook calls remain in ArmyListUnitRow.tsx (unless useUnitPointTiers is used elsewhere in the file)
    - Lines 269-312 (inline tier selector) are fully removed
    - A Button with Settings2 icon and tierLabel text exists after the PointsSourceChip
    - The tierLabel logic uses unit.selected_model_count and unit.tier_points (not pendingTierId)
    - ArmyListDetailSheetProps includes onConfigureUnit: (armyListUnitId: number) => void
    - Each ArmyListUnitRow in the render loop receives onConfigure={() => onConfigureUnit(alu.id)}
    - pnpm build passes (note: ArmyListsPage will have a type error on the missing onConfigureUnit prop — this is expected and resolved in Plan 02)
  </acceptance_criteria>
  <done>ArmyListUnitRow shows Configure trigger instead of inline tier selector; ArmyListDetailSheet threads onConfigureUnit callback; old Phase 24 tier-to-units.points write path removed</done>
</task>

<task type="auto">
  <name>Task 3: Create test scaffold for LoadoutBuilderSheet</name>
  <files>
    tests/army-lists/LoadoutBuilderSheet.test.tsx
  </files>
  <read_first>
    tests/army-lists/ArmyListSummaryBar.test.tsx,
    src/types/armyList.ts,
    src/hooks/useLoadoutOptions.ts
  </read_first>
  <action>
    Create tests/army-lists/LoadoutBuilderSheet.test.tsx with test scaffolding for Plan 02. This file establishes the test infrastructure:

    1. Set up vi.mock blocks:
       - vi.mock("@/hooks/useArmyLists") mocking useSetSelectedModelCount returning { mutate: vi.fn(), isPending: false } and useClearSelectedModelCount returning { mutate: vi.fn(), isPending: false }
       - vi.mock("@/hooks/useLoadoutOptions") mocking useTiersByUnitName and useLoadoutOptionsForUnit returning configurable mock data

    2. Create makeUnit factory function matching ArmyListUnitRow interface with sensible defaults (id: 1, list_id: 1, unit_id: 1, ghost_unit_name: null, is_warlord: 0, selected_model_count: null, leader_attached_to_id: null, points_override: null, notes: null, created_at: "2024-01-01", unit_name: "Intercessors", unit_points: 100, faction_id: 1, status_assembly: 1, status_painting: "Completed", synced_points: null, override_points: null, tier_points: null, painting_percentage: 100, effective_points: 100, tactical_role: null). Accepts Partial overrides.

    3. Create mockTiers array: [{ model_count: 5, points: 90 }, { model_count: 10, points: 180 }]

    4. Create mockWargearOptions array using SyncedLoadoutOptionRow shape: [{ group_name: "Ranged Weapons", option_name: "Bolt Rifle", is_default: 1, is_exclusive: 0 }, { group_name: "Ranged Weapons", option_name: "Stalker Bolt Rifle", is_default: 0, is_exclusive: 1 }, { group_name: "Melee Weapons", option_name: "Astartes Chainsword", is_default: 1, is_exclusive: 0 }]

    5. Write placeholder test stubs (describe + it blocks with TODO comments) for:
       - "renders tier selector with available tiers from synced data" (DL-01)
       - "selecting a tier calls useSetSelectedModelCount with correct args" (DL-01)
       - "selecting Default calls useClearSelectedModelCount" (DL-01)
       - "renders wargear options grouped by group_name" (DL-02)
       - "shows Default badge for is_default=1 options" (DL-02)
       - "shows Exclusive badge for is_exclusive=1 options" (DL-02)
       - "shows empty state when no wargear options" (DL-02)
       - "shows Planned badge for ghost units" (DL-10/DL-11)
       - "shows points override warning when points_override is set" (Pitfall 6)
  </action>
  <verify>
    <automated>pnpm test -- tests/army-lists/LoadoutBuilderSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tests/army-lists/LoadoutBuilderSheet.test.tsx exists
    - File has vi.mock blocks for useArmyLists and useLoadoutOptions
    - makeUnit factory function exists with all ArmyListUnitRow fields
    - At least 9 test stubs (it blocks) are present
    - File compiles and vitest recognizes it (tests may skip/todo but file parses)
  </acceptance_criteria>
  <done>Test scaffold with mock factories and 9+ test stubs ready for Plan 02 to implement alongside the LoadoutBuilderSheet component</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> SQLite | User-selected tier values passed to parameterized queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Tampering | getLoadoutOptionsForUnit / getTiersByUnitName | accept | Read-only queries against local SQLite; no user-modifiable input beyond unit name and faction ID already in DB. Parameterized queries prevent injection. |
| T-90-02 | Information Disclosure | synced_loadout_options | accept | Local desktop app, single user, no network exposure. Wargear data is public game information. |
</threat_model>

<verification>
pnpm build passes with no type errors
pnpm test -- tests/army-lists/LoadoutBuilderSheet.test.tsx runs without crashes
</verification>

<success_criteria>
- getLoadoutOptionsForUnit and getTiersByUnitName query functions exist and are exported
- useLoadoutOptionsForUnit and useTiersByUnitName hooks exist in src/hooks/useLoadoutOptions.ts
- ArmyListUnitRow shows a Configure button instead of the inline tier selector
- ArmyListDetailSheet accepts and threads onConfigureUnit callback
- Test scaffold for LoadoutBuilderSheet is ready with mock factories
</success_criteria>

<output>
Create `.planning/phases/90-loadout-builder/90-01-SUMMARY.md` when done
</output>

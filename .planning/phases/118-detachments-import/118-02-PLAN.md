---
phase: 118-detachments-import
plan: 02
type: execute
wave: 2
depends_on: [118-01]
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/data/unit_database.json
autonomous: true
requirements: [DET-01, DET-02]

must_haves:
  truths:
    - "Rust importer inserts detachments and detachment_abilities from JSON into SQLite"
    - "DELETE order is correct: udb_detachment_abilities before udb_detachments before udb_factions"
    - "INSERT order is correct: udb_detachments before udb_detachment_abilities"
    - "UdbImportResult reports detachments and detachment_abilities counts"
    - "Old unit_database.json without detachments keys still deserializes (serde default)"
    - "App starts successfully with the new schema and populated data"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "Extended Rust importer with detachment support"
      contains: "udb_detachments"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/data/unit_database.json"
      via: "UnitDatabasePayload deserialization"
      pattern: "detachments.*Vec.*JsRow"
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/migrations/042_udb_detachments.sql"
      via: "INSERT INTO udb_detachments / udb_detachment_abilities"
      pattern: "INSERT INTO udb_detachments"
---

<objective>
Extend the Rust importer to handle detachments and detachment abilities from the JSON payload.

Purpose: The TypeScript build script (Plan 01) produces a JSON with detachments and detachment_abilities arrays. This plan extends the Rust side to deserialize those arrays and INSERT them into the SQLite tables created by migration 042.

Output: Updated lib.rs with detachment support in UnitDatabasePayload, UdbImportResult, DELETE list, and INSERT blocks. Rebuilt unit_database.json committed.
</objective>

<execution_context>
@.planning/phases/118-detachments-import/118-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/118-detachments-import/118-RESEARCH.md
@.planning/phases/118-detachments-import/118-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Rust structs and DELETE list</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/lib.rs (UnitDatabasePayload struct at ~line 476, UdbImportResult struct at ~line 501, early-return at ~line 556, counts initializer at ~line 571, DELETE loop at ~line 577)
    - .planning/phases/118-detachments-import/118-PATTERNS.md (exact Rust patterns for struct additions, DELETE order, INSERT blocks)
  </read_first>
  <action>
    Four changes to lib.rs per D-10, D-11, D-12:

    1. UnitDatabasePayload struct (at ~line 476): Add two fields after composition — detachments: Vec of JsRow with #[serde(default)], and detachment_abilities: Vec of JsRow with #[serde(default)]. The serde(default) attribute is critical so old JSON files without these keys still deserialize.

    2. UdbImportResult struct (at ~line 501): Add detachments: u64 and detachment_abilities: u64 after composition.

    3. Early-return path (at ~line 556) and counts initializer (at ~line 571): Add detachments: 0 and detachment_abilities: 0 to both UdbImportResult literal constructions.

    4. DELETE list (at ~line 577): Insert "udb_detachment_abilities" and "udb_detachments" into the table array AFTER "udb_units" and BEFORE "udb_factions". Child (abilities) must appear before parent (detachments) in the delete list.
  </action>
  <acceptance_criteria>
    - UnitDatabasePayload has detachments and detachment_abilities fields, both Vec of JsRow with #[serde(default)]
    - UdbImportResult has detachments: u64 and detachment_abilities: u64 fields
    - Both UdbImportResult literal constructions (early-return and counts init) include the new fields set to 0
    - DELETE list contains "udb_detachment_abilities" before "udb_detachments", both before "udb_factions"
    - cargo check (via pnpm tauri build --no-bundle or equivalent) compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App\src-tauri" && cargo check</automated>
  </verify>
  <done>Rust structs extended. DELETE list updated with correct child-before-parent order. Code compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add Rust INSERT blocks and commit rebuilt JSON</name>
  <files>src-tauri/src/lib.rs, src-tauri/data/unit_database.json</files>
  <read_first>
    - src-tauri/src/lib.rs (INSERT factions block at ~line 594 for TEXT PK insert pattern, INSERT composition block for the last existing insert, the udb_meta INSERT location)
    - .planning/phases/118-detachments-import/118-PATTERNS.md (exact INSERT block code for detachments and detachment_abilities)
  </read_first>
  <action>
    Add two INSERT blocks in import_unit_database_inner per D-11. Place them after the composition INSERT block and before the udb_meta INSERT.

    INSERT detachments block: Loop over payload.detachments, extract id via str_val. Skip if empty. INSERT INTO udb_detachments (id, faction_id, name) VALUES (?, ?, ?). Bind id, faction_id, name via str_val. Increment counts.detachments.

    INSERT detachment_abilities block: Loop over payload.detachment_abilities, extract id via str_val. Skip if empty. INSERT INTO udb_detachment_abilities (id, detachment_id, faction_id, name, description) VALUES (?, ?, ?, ?, ?). Bind id, detachment_id, faction_id, name via str_val (unwrap_or_default), description via str_val (no unwrap — nullable). Increment counts.detachment_abilities.

    Follow the exact error format from existing blocks: map_err with format!("insert detachment {id}: {e}") and format!("insert detachment_ability {id}: {e}").

    Stage and commit the rebuilt unit_database.json from Plan 01 along with these Rust changes.
  </action>
  <acceptance_criteria>
    - Two new INSERT blocks exist in import_unit_database_inner for udb_detachments and udb_detachment_abilities
    - Detachments INSERT uses 3 bind params (id, faction_id, name)
    - Detachment abilities INSERT uses 5 bind params (id, detachment_id, faction_id, name, description)
    - counts.detachments and counts.detachment_abilities are incremented
    - cargo check compiles without errors
    - pnpm build succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App\src-tauri" && cargo check && cd .. && pnpm build</automated>
  </verify>
  <done>Rust importer handles detachments end-to-end. INSERT blocks follow the TEXT PK pattern. Both pnpm build and cargo check pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON artifact to Rust importer | Build artifact embedded in app bundle; not user input |
| Rust INSERT to SQLite | Parameterized queries prevent SQL injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-03 | Injection | Rust INSERT queries | mitigate | All values bound via parameterized ? placeholders through sqlx — no string interpolation in SQL |
| T-118-04 | Denial of Service | Large JSON payload | accept | JSON is a build artifact with ~300 rows; not user-controllable. Bounded size. |
</threat_model>

<verification>
1. cargo check passes with the struct and INSERT changes
2. pnpm build passes (TypeScript + Vite)
3. Full end-to-end: pnpm tauri dev starts the app without errors (manual check if needed)
</verification>

<success_criteria>
Rust importer deserializes detachments and detachment_abilities from JSON, DELETEs old rows in correct FK order, INSERTs new rows with TEXT PKs, and reports counts. App compiles and the data pipeline is complete from CSV through JSON to SQLite.
</success_criteria>

<output>
Create `.planning/phases/118-detachments-import/118-02-SUMMARY.md` when done
</output>

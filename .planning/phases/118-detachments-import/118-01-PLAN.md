---
phase: 118-detachments-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/042_udb_detachments.sql
  - scripts/lib/types.ts
  - scripts/build-unit-db.ts
  - scripts/update-unit-database.ts
autonomous: true
requirements: [DET-01, DET-02]

must_haves:
  truths:
    - "udb_detachments and udb_detachment_abilities tables exist in schema with TEXT PKs"
    - "Running pnpm build:udb produces unit_database.json with detachments and detachment_abilities arrays"
    - "Detachments are deduplicated by detachment_id — one row per unique detachment"
    - "Detachment abilities reference their parent detachment via detachment_id"
    - "Both build scripts (build-unit-db.ts and update-unit-database.ts) parse Detachment_abilities.csv"
    - "The legend column is NOT used as a boolean filter — it contains lore text in this CSV"
    - "Content hash includes detachments and detachment_abilities for re-import triggering"
  artifacts:
    - path: "src-tauri/migrations/042_udb_detachments.sql"
      provides: "DDL for udb_detachments and udb_detachment_abilities tables"
      contains: "CREATE TABLE IF NOT EXISTS udb_detachments"
    - path: "scripts/lib/types.ts"
      provides: "UdbDetachmentRow and UdbDetachmentAbilityRow interfaces"
      contains: "UdbDetachmentRow"
    - path: "scripts/build-unit-db.ts"
      provides: "Detachment parsing step in main build pipeline"
      contains: "Detachment_abilities.csv"
    - path: "scripts/update-unit-database.ts"
      provides: "Detachment parsing step in update pipeline"
      contains: "Detachment_abilities.csv"
    - path: "src-tauri/data/unit_database.json"
      provides: "Rebuilt JSON with detachments and detachment_abilities arrays"
      contains: "detachments"
  key_links:
    - from: "scripts/build-unit-db.ts"
      to: "scripts/lib/types.ts"
      via: "import UdbDetachmentRow, UdbDetachmentAbilityRow"
      pattern: "UdbDetachmentRow"
    - from: "scripts/build-unit-db.ts"
      to: "src-tauri/data/unit_database.json"
      via: "JSON output includes detachments and detachment_abilities"
      pattern: "detachment_abilities"
---

<objective>
Create the SQLite migration for detachment tables, add TypeScript types, and extend both build scripts to parse Detachment_abilities.csv into the unit database JSON.

Purpose: This is the data pipeline half of Phase 118. The migration defines the schema, the types enforce the data shape, and the build scripts extract detachments and their abilities from the Wahapedia CSV into the JSON artifact that the Rust importer will consume.

Output: Migration 042, two new TypeScript interfaces, extended UnitDatabaseJson, and a rebuilt unit_database.json containing detachments and detachment_abilities arrays.
</objective>

<execution_context>
@.planning/phases/118-detachments-import/118-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-detachments-import/118-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration and TypeScript types</name>
  <files>src-tauri/migrations/042_udb_detachments.sql, scripts/lib/types.ts</files>
  <read_first>
    - src-tauri/migrations/038_udb_schema.sql (DDL pattern: TEXT PK, FK references, ON DELETE CASCADE, indexes)
    - scripts/lib/types.ts (existing UdbUnitAbilityRow pattern, UnitDatabaseJson interface)
  </read_first>
  <action>
    Create migration file 042_udb_detachments.sql per D-13. Two tables, DDL-only (no INSERTs per boot-loop precedent).

    udb_detachments: id TEXT PRIMARY KEY (Wahapedia detachment_id per D-02), faction_id TEXT NOT NULL REFERENCES udb_factions(id), name TEXT NOT NULL, updated_at TEXT NOT NULL DEFAULT (datetime('now')).

    udb_detachment_abilities: id TEXT PRIMARY KEY (Wahapedia ability row id per D-03), detachment_id TEXT NOT NULL REFERENCES udb_detachments(id) ON DELETE CASCADE (per D-03), faction_id TEXT NOT NULL REFERENCES udb_factions(id) (per D-04), name TEXT NOT NULL, description TEXT.

    Two indexes per D-05: idx_udb_detachments_faction_id ON udb_detachments(faction_id), idx_udb_detachment_abilities_detachment_id ON udb_detachment_abilities(detachment_id).

    In scripts/lib/types.ts per D-09: Add UdbDetachmentRow interface (id: string, faction_id: string, name: string) and UdbDetachmentAbilityRow interface (id: string, detachment_id: string, faction_id: string, name: string, description: string). Append detachments: UdbDetachmentRow[] and detachment_abilities: UdbDetachmentAbilityRow[] to UnitDatabaseJson interface per D-08.
  </action>
  <acceptance_criteria>
    - File src-tauri/migrations/042_udb_detachments.sql exists and contains exactly 2 CREATE TABLE and 2 CREATE INDEX statements
    - No INSERT statements in the migration file
    - UdbDetachmentRow exported from scripts/lib/types.ts with fields id, faction_id, name (all string)
    - UdbDetachmentAbilityRow exported from scripts/lib/types.ts with fields id, detachment_id, faction_id, name, description (all string)
    - UnitDatabaseJson interface includes detachments: UdbDetachmentRow[] and detachment_abilities: UdbDetachmentAbilityRow[]
    - pnpm build succeeds (TypeScript strict mode catches any type inconsistencies)
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App" && pnpm build</automated>
  </verify>
  <done>Migration 042 creates both tables with TEXT PKs and FK indexes. TypeScript types are exported and UnitDatabaseJson is extended.</done>
</task>

<task type="auto">
  <name>Task 2: Extend both build scripts to parse detachments</name>
  <files>scripts/build-unit-db.ts, scripts/update-unit-database.ts</files>
  <read_first>
    - scripts/build-unit-db.ts (REQUIRED_CSVs array, factionIds set, Step 3 Legends+faction-warn pattern, Step 7 dedup-via-Set pattern, hash computation at ~line 776, output assembly at ~lines 779-793)
    - scripts/update-unit-database.ts (REQUIRED_CSVs, buildUnitDatabase function, hash computation, return value)
    - scripts/lib/types.ts (newly added UdbDetachmentRow, UdbDetachmentAbilityRow, extended UnitDatabaseJson)
    - .planning/phases/118-detachments-import/118-PATTERNS.md (exact code patterns for the new step)
  </read_first>
  <action>
    In both build scripts, apply 4 changes per D-07 and D-08:

    1. Add "Detachment_abilities.csv" to the REQUIRED_CSVs array.

    2. Add import of UdbDetachmentRow and UdbDetachmentAbilityRow from ./lib/types.ts (build-unit-db.ts) or from the same relative path (update-unit-database.ts).

    3. Add a new numbered step after the existing steps (before the completeness check / hash). The step reads Detachment_abilities.csv via readCsvFile(DATA_DIR, "Detachment_abilities.csv"). Loop over rows extracting detachment_id, faction_id, id (ability), detachment (name), name (ability name), description. Skip rows with missing required fields. Warn and skip on unknown faction_id (consistent with unit parsing). Do NOT apply Legends boolean filter -- the legend column in this CSV is lore text, not a flag. Collect unique detachments into a UdbDetachmentRow[] using a Set keyed by detachment_id (first occurrence wins). Collect all abilities into UdbDetachmentAbilityRow[]. Log parsed counts.

    4. Add detachments and detachmentAbilities (as detachment_abilities key) to: (a) the SHA-256 hash input object, (b) the UnitDatabaseJson output object. The hash must include these so re-imports trigger on detachment data changes (Pitfall 5 from RESEARCH.md).

    After both scripts are updated, run pnpm build:udb to rebuild unit_database.json.
  </action>
  <acceptance_criteria>
    - Both scripts include "Detachment_abilities.csv" in REQUIRED_CSVs
    - Running pnpm build:udb completes without error
    - Console output includes a line matching "Parsed \d+ detachments, \d+ detachment abilities"
    - src-tauri/data/unit_database.json contains a "detachments" array with length > 200
    - src-tauri/data/unit_database.json contains a "detachment_abilities" array with length > 200
    - Each detachment object has id, faction_id, name fields (all non-empty strings)
    - Each detachment_ability object has id, detachment_id, faction_id, name, description fields
    - pnpm build succeeds (TypeScript strict mode validates both scripts against UnitDatabaseJson)
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App" && pnpm build:udb && pnpm build</automated>
  </verify>
  <done>Both build scripts parse Detachment_abilities.csv. unit_database.json contains detachments and detachment_abilities arrays with 200+ entries each. Hash includes new arrays.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV file to build script | Developer-supplied CSV parsed at build time (not user input) |
| JSON artifact to Rust importer | Build artifact consumed by Tauri app startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Tampering | Detachment_abilities.csv | accept | Developer-supplied build-time artifact; not user-facing input. Build is offline-only. |
| T-118-02 | Information Disclosure | description HTML in DB | accept | HTML stored as-is; sanitization is Phase 120 UI concern. No PII in game rules data. |
</threat_model>

<verification>
1. pnpm build:udb succeeds and prints detachment parse counts
2. pnpm build succeeds (TypeScript strict mode)
3. unit_database.json has detachments array with 200+ entries
4. unit_database.json has detachment_abilities array with 200+ entries
</verification>

<success_criteria>
Migration 042 exists with DDL for both tables. TypeScript types are defined and exported. Both build scripts parse Detachment_abilities.csv and emit detachments + detachment_abilities into unit_database.json. The JSON artifact is rebuilt successfully.
</success_criteria>

<output>
Create `.planning/phases/118-detachments-import/118-01-SUMMARY.md` when done
</output>

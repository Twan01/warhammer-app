---
phase: 85-core-execution-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/scroll-area.tsx
  - src/features/painting-mode/StepMetadataRow.tsx
  - src/features/painting-mode/PaintReadinessBanner.tsx
  - tests/painting-mode/StepMetadataRow.test.tsx
  - tests/painting-mode/PaintReadinessBanner.test.tsx
autonomous: true
requirements: [PR-01, PR-02, PR-03]

must_haves:
  truths:
    - "Missing paints are listed in a dismissible amber banner when any step paint is not owned"
    - "Paintless steps produce no false availability warnings in the banner"
    - "Banner can be dismissed and stays hidden for the session"
    - "Step metadata row shows technique, tool, dilution, and time estimate inline"
  artifacts:
    - path: "src/components/ui/scroll-area.tsx"
      provides: "shadcn ScrollArea primitive for section navigator"
    - path: "src/features/painting-mode/StepMetadataRow.tsx"
      provides: "Inline metadata row: technique badge, tool, dilution, time"
      exports: ["StepMetadataRow"]
    - path: "src/features/painting-mode/PaintReadinessBanner.tsx"
      provides: "Dismissible amber banner listing missing paints"
      exports: ["PaintReadinessBanner"]
    - path: "tests/painting-mode/StepMetadataRow.test.tsx"
      provides: "Tests for metadata row rendering"
    - path: "tests/painting-mode/PaintReadinessBanner.test.tsx"
      provides: "Tests for banner display, dismiss, paintless steps"
  key_links:
    - from: "PaintReadinessBanner"
      to: "isPaintMissing"
      via: "import from @/features/recipes/recipeSteps"
      pattern: "isPaintMissing"
---

<objective>
Install ScrollArea shadcn component and build the two leaf presentational components for Painting Mode: StepMetadataRow (technique/tool/dilution/time display) and PaintReadinessBanner (dismissible missing-paint warning). These are standalone components consumed by StepFocalView and PaintingModeView respectively in Plan 03.

Purpose: Deliver the leaf-level building blocks first so Plan 03 can compose them without inline complexity.
Output: 3 source files (1 shadcn + 2 components) + 2 test files.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-core-execution-ui/85-CONTEXT.md
@.planning/phases/85-core-execution-ui/85-RESEARCH.md
@.planning/phases/85-core-execution-ui/85-PATTERNS.md
@.planning/phases/85-core-execution-ui/85-UI-SPEC.md

<interfaces>
<!-- Phase 84 hooks — consumed by PaintingModeView (Plan 03), referenced here for type awareness -->

From src/types/recipePaint.ts:
export interface RecipeStep {
  id: number; recipe_id: number; paint_id: number | null;
  step_name: string; order_index: number; notes: string | null;
  painting_phase: string | null; tool: string | null; technique: string | null;
  dilution: string | null; time_estimate_minutes: number | null;
  step_photo_path: string | null; alt_paint_id: number | null;
  section_id: number | null; created_at: string;
}

From src/types/paint.ts:
export interface Paint {
  id: number; brand: string; name: string; paint_type: PaintType;
  hex_color: string | null; owned: 0 | 1;
  /* other fields omitted */
}

From src/features/recipes/recipeSteps.ts:
export function isPaintMissing(paint: Paint | undefined | null): boolean;
// Returns true if paint is null/undefined OR paint.owned !== 1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install ScrollArea + create StepMetadataRow and PaintReadinessBanner</name>
  <files>
    src/components/ui/scroll-area.tsx
    src/features/painting-mode/StepMetadataRow.tsx
    src/features/painting-mode/PaintReadinessBanner.tsx
  </files>
  <read_first>
    src/features/recipes/RecipeStepTimeline.tsx (lines 75-99 — metadata row pattern with icon+text pairs)
    src/features/game-day/GameDayReadinessPanel.tsx (lines 115-191 — warning panel layout and amber styling)
    src/features/recipes/recipeSteps.ts (isPaintMissing function)
    src/types/recipePaint.ts (RecipeStep interface — source of metadata fields)
    src/types/paint.ts (Paint interface — owned field, hex_color)
    .planning/phases/85-core-execution-ui/85-UI-SPEC.md (typography scale, spacing, color contract)
  </read_first>
  <action>
    Install shadcn ScrollArea: run `npx shadcn@latest add scroll-area` — creates src/components/ui/scroll-area.tsx.

    Create src/features/painting-mode/ directory.

    Create StepMetadataRow.tsx — a pure presentational component. Props: `{ technique: string | null; tool: string | null; dilution: string | null; timeEstimateMinutes: number | null; paintingPhase: string | null }`. Renders a horizontal flex row (`flex flex-wrap items-center gap-4`) with:
    - technique: Badge outline variant, capitalize text
    - tool: Paintbrush icon (h-4 w-4) + text (text-base per UI spec Body 16px)
    - dilution: Droplets icon (h-4 w-4) + text
    - time: Clock icon (h-4 w-4) + "~{N}m" text
    - paintingPhase: Badge outline variant if present
    Each field conditionally renders only when non-null. If all fields are null, render nothing (return null). Use text-base for values (scaled up from RecipeStepTimeline's text-xs per D-04 and UI spec). Import icons from lucide-react: Paintbrush, Droplets, Clock. Export as named function `StepMetadataRow`.

    Create PaintReadinessBanner.tsx — a dismissible warning banner. Props: `{ missingPaints: Array<{ id: number; name: string; brand: string }>; onDismiss: () => void }`. Renders:
    - Outer container: `bg-amber-500/10 border border-amber-500/20 rounded-lg mx-6 mt-4 px-4 py-3` per UI spec
    - Layout: `flex items-start justify-between gap-3`
    - Left: AlertTriangle icon (h-5 w-5, text-amber-500) + message text (text-base): "Some paints are not in your inventory:" followed by comma-joined paint names in font-semibold
    - Right: ghost Button with X icon, aria-label="Dismiss banner", calls onDismiss
    - If missingPaints array is empty, return null (no render)
    Import AlertTriangle, X from lucide-react. Import Button from @/components/ui/button. Export as named function `PaintReadinessBanner`.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/scroll-area.tsx exists and exports ScrollArea, ScrollBar
    - src/features/painting-mode/StepMetadataRow.tsx exports named function StepMetadataRow
    - StepMetadataRow accepts props: technique, tool, dilution, timeEstimateMinutes, paintingPhase (all nullable)
    - StepMetadataRow returns null when all props are null
    - StepMetadataRow uses text-base class for value text, h-4 w-4 for icons
    - StepMetadataRow uses Badge outline variant for technique and paintingPhase
    - src/features/painting-mode/PaintReadinessBanner.tsx exports named function PaintReadinessBanner
    - PaintReadinessBanner accepts props: missingPaints array + onDismiss callback
    - PaintReadinessBanner returns null when missingPaints is empty
    - PaintReadinessBanner uses bg-amber-500/10 container styling
    - PaintReadinessBanner dismiss button has aria-label="Dismiss banner"
    - pnpm build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>ScrollArea installed, StepMetadataRow renders technique/tool/dilution/time with scaled-up typography, PaintReadinessBanner renders dismissible amber warning with missing paint names. Build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests for StepMetadataRow and PaintReadinessBanner</name>
  <files>
    tests/painting-mode/StepMetadataRow.test.tsx
    tests/painting-mode/PaintReadinessBanner.test.tsx
  </files>
  <read_first>
    src/features/painting-mode/StepMetadataRow.tsx (just created — verify exact props and rendering)
    src/features/painting-mode/PaintReadinessBanner.tsx (just created — verify exact props and rendering)
    tests/game-day/GameDayReadinessPanel.test.tsx (test pattern: render helper, userEvent for clicks)
    tests/painting-mode/paintingModeState.test.ts (existing painting-mode test — import/setup pattern)
  </read_first>
  <behavior>
    StepMetadataRow tests:
    - Renders technique as Badge when provided
    - Renders tool with Paintbrush icon text when provided
    - Renders dilution with Droplets icon text when provided
    - Renders time estimate as "~{N}m" when provided
    - Returns null / renders nothing when all props are null
    - Renders only non-null fields (partial props)

    PaintReadinessBanner tests:
    - Renders missing paint names when array is non-empty (PR-01)
    - Renders "Some paints are not in your inventory:" heading text
    - Dismiss button calls onDismiss callback when clicked (PR-02)
    - Returns null / renders nothing when missingPaints is empty (PR-03 — no false warnings)
  </behavior>
  <action>
    Create tests/painting-mode/StepMetadataRow.test.tsx. Import render, screen from @testing-library/react. Import StepMetadataRow. Write describe block with tests for: all fields present renders each one, all null renders nothing (container not in document), partial props render only present fields. Use screen.getByText for value assertions, screen.queryByText for absence.

    Create tests/painting-mode/PaintReadinessBanner.test.tsx. Import render, screen from @testing-library/react, userEvent from @testing-library/user-event. Import PaintReadinessBanner. Write tests for: non-empty array renders paint names and heading text, empty array renders nothing, clicking dismiss calls onDismiss (use vi.fn() spy, await userEvent.click on button with aria-label "Dismiss banner").
  </action>
  <verify>
    <automated>pnpm test -- tests/painting-mode/StepMetadataRow.test.tsx tests/painting-mode/PaintReadinessBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tests/painting-mode/StepMetadataRow.test.tsx has at least 4 test cases
    - tests/painting-mode/PaintReadinessBanner.test.tsx has at least 4 test cases
    - All tests pass on pnpm test run
    - PaintReadinessBanner test verifies onDismiss callback is called exactly once on dismiss click
    - PaintReadinessBanner test verifies empty array renders nothing (queryByText returns null)
    - StepMetadataRow test verifies null-props case renders nothing
  </acceptance_criteria>
  <done>StepMetadataRow and PaintReadinessBanner have passing test suites covering all rendering variants and user interactions. PR-01, PR-02, PR-03 requirements have test coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed in this plan. All data originates from local SQLite via parameterized queries. Components are pure presentational — no user text input, no external API calls, no file system writes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Spoofing | n/a | accept | No auth in desktop app — single local user |
| T-85-02 | Tampering | ScrollArea install | accept | Official shadcn registry, Radix UI transitive dep already present |
</threat_model>

<verification>
pnpm build succeeds
pnpm test -- tests/painting-mode/StepMetadataRow.test.tsx passes
pnpm test -- tests/painting-mode/PaintReadinessBanner.test.tsx passes
</verification>

<success_criteria>
- ScrollArea component installed and importable from @/components/ui/scroll-area
- StepMetadataRow renders technique/tool/dilution/time with scaled-up typography per UI spec
- PaintReadinessBanner renders dismissible amber warning with missing paint names
- All test suites green
- Build clean with no TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/85-core-execution-ui/85-01-SUMMARY.md` when done
</output>

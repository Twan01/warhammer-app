---
phase: 145-integration-pass
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/db/queries/recipeTechniqueSlotMaps.ts
  - src/hooks/useSlotResolutionMap.ts
  - src/hooks/useTechniqueInstances.ts
  - tests/data-layer/recipe-duplication-live-link.test.ts
  - tests/data-layer/unfilled-slot-count.test.ts
  - tests/data-layer/availability-effective-paint.test.ts
autonomous: true
requirements: [INTG-02, INTG-05, INTG-06]
must_haves:
  truths:
    - "getUnfilledSlotCount(recipeId) returns the count of unfilled colour slots across non-detached technique instances"
    - "getStepSlotIdMap(recipeId) maps each technique recipe_step.id to its colour_slot_id"
    - "Duplicating a recipe produces independent technique instances + slot maps with technique_step_id preserved (live link intact)"
    - "A technique step with a filled slot counts toward owned/missing exactly as a plain step; an unfilled slot is never counted as missing"
    - "useUpdateSlotMap invalidates UNFILLED_SLOT_COUNT_KEY so the readiness banner refreshes after an inline reassignment"
  artifacts:
    - path: "src/db/queries/recipeTechniqueSlotMaps.ts"
      provides: "getUnfilledSlotCount + getStepSlotIdMap queries"
      contains: "export async function getUnfilledSlotCount"
    - path: "src/hooks/useSlotResolutionMap.ts"
      provides: "useUnfilledSlotCount + useStepSlotIdMap hooks + UNFILLED_SLOT_COUNT_KEY + STEP_SLOT_ID_MAP_KEY"
      contains: "export function useUnfilledSlotCount"
    - path: "tests/data-layer/recipe-duplication-live-link.test.ts"
      provides: "INTG-05 duplication live-link invariant test"
    - path: "tests/data-layer/unfilled-slot-count.test.ts"
      provides: "INTG-06 unfilled-slot count correctness test"
    - path: "tests/data-layer/availability-effective-paint.test.ts"
      provides: "INTG-02 effectivePaintId availability no-undercounting test"
  key_links:
    - from: "src/hooks/useTechniqueInstances.ts"
      to: "UNFILLED_SLOT_COUNT_KEY"
      via: "useUpdateSlotMap onSuccess invalidation"
      pattern: "UNFILLED_SLOT_COUNT_KEY\\(variables.recipeId\\)"
    - from: "src/hooks/useSlotResolutionMap.ts"
      to: "getUnfilledSlotCount"
      via: "useQuery queryFn"
      pattern: "getUnfilledSlotCount\\(recipeId\\)"
---

<objective>
Build and prove the data-layer foundation for the integration pass: two new read
queries (`getUnfilledSlotCount`, `getStepSlotIdMap`), their hooks, the
`UNFILLED_SLOT_COUNT_KEY` invalidation wiring, and three data-layer tests that
encode the phase's honesty invariant — **no silent undercounting**.

Purpose: This is Wave 0. Every UI plan in this phase consumes these hooks. The
tests prove (a) recipe duplication preserves the live link, (b) the unfilled-slot
count is correct, and (c) availability resolution via `effectivePaintId()` counts
filled technique slots and never miscounts unfilled slots as missing — BEFORE any
UI surface depends on them.

Output: Two query functions, two hooks + two query-key factories, one extended
invalidation list, and three green data-layer test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-integration-pass/145-RESEARCH.md
@.planning/phases/145-integration-pass/145-PATTERNS.md
@.planning/phases/145-integration-pass/145-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

From src/db/queries/recipeTechniqueSlotMaps.ts (existing — getSlotResolutionMap JOIN chain to reuse):
The file already exports getSlotResolutionMap(recipeId), getSlotMapByInstance(instanceId),
updateSlotMap(instanceId, slotFills). All use `const db = await getDb()`, NO BEGIN/COMMIT.
getSlotResolutionMap keys on rs.id (recipe_step PK) via this JOIN:
  recipe_steps rs
  JOIN recipe_sections rsec ON rsec.id = rs.section_id
  JOIN recipe_technique_instances rti ON rti.id = rsec.technique_instance_id
  JOIN technique_steps ts ON ts.id = rs.technique_step_id
  LEFT JOIN recipe_technique_slot_maps sm ON sm.instance_id = rti.id AND sm.slot_id = ts.colour_slot_id
  WHERE rs.recipe_id = $1 AND rs.technique_step_id IS NOT NULL

From src/lib/effectivePaintId.ts:
  export interface PaintResolvableStep { id: number; paint_id: number | null; technique_step_id?: number | null; }
  export type SlotResolutionMap = ReadonlyMap<number, number | null>;
  export function effectivePaintId(step: PaintResolvableStep, slotMap: SlotResolutionMap): number | null;
  // technique step (technique_step_id != null) → slotMap.get(step.id) ?? null; else step.paint_id

From src/hooks/useSlotResolutionMap.ts (existing exports to mirror):
  export const SLOT_RESOLUTION_MAP_KEY = (recipeId: number) => ["slot-resolution-map", recipeId] as const;
  export const SLOT_MAP_BY_INSTANCE_KEY = (instanceId: number) => ["slot-map-by-instance", instanceId] as const;
  export function useSlotResolutionMap(recipeId: number | undefined): UseQueryResult<Map<number, number|null>>;
  // enabled-by-id pattern: queryKey falls back to bare ["slot-resolution-map"], queryFn resolves empty Map

From src/hooks/useTechniqueInstances.ts (useUpdateSlotMap onSuccess currently invalidates):
  SLOT_RESOLUTION_MAP_KEY(recipeId), SLOT_MAP_BY_INSTANCE_KEY(instanceId),
  RECIPE_PAINTS_KEY(recipeId), RECIPE_SWATCH_KEY, RECIPE_AVAILABILITY_KEY

Schema (migration 051):
  recipe_technique_instances(id, recipe_id, technique_id, detached INTEGER NOT NULL DEFAULT 0, ...)
  technique_colour_slots(id, technique_id, name, role_hint, order_index)
  recipe_technique_slot_maps(instance_id, slot_id, paint_id)  UNIQUE(instance_id, slot_id)
  recipe_steps(id, recipe_id, section_id, paint_id, technique_step_id, ...)  — technique steps: paint_id NULL
  technique_steps(id, technique_section_id, colour_slot_id, ...)

Test harness (tests/data-layer/db-helpers.ts):
  createHobbyforgeDb(): Database  — in-memory, all migrations applied, FK ON
  createTestRecipe(db): number
  createDbBridge(db)  — Tauri-compatible async select/execute; mock getDb with it
  Pattern: vi.mock("@/db/client", () => ({ getDb: vi.fn() })); vi.mocked(getDb).mockResolvedValue(createDbBridge(db) as never)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getUnfilledSlotCount + getStepSlotIdMap queries</name>
  <files>src/db/queries/recipeTechniqueSlotMaps.ts</files>
  <read_first>
    - src/db/queries/recipeTechniqueSlotMaps.ts (the file — getSlotResolutionMap lines 53-76 is the JOIN-chain analog; getSlotMapByInstance lines 90-105 is the count/select shape analog)
    - .planning/phases/145-integration-pass/145-PATTERNS.md lines 58-104 (exact SQL for both new functions)
    - .planning/phases/145-integration-pass/145-RESEARCH.md lines 338-368, 396-413 (query rationale, LEFT JOIN pitfall, detached filter)
    - src-tauri/migrations/051_technique_library_foundation.sql line 61 (detached column DEFAULT 0 — filter IS safe)
  </read_first>
  <behavior>
    - getUnfilledSlotCount: returns 0 when a recipe has no technique instances
    - getUnfilledSlotCount: counts a slot with no recipe_technique_slot_maps row as unfilled
    - getUnfilledSlotCount: counts a slot with a row where paint_id IS NULL as unfilled
    - getUnfilledSlotCount: does NOT count a slot with a row where paint_id is set
    - getUnfilledSlotCount: excludes instances where detached = 1
    - getStepSlotIdMap: returns Map<recipe_step.id, colour_slot_id|null> for technique steps only (technique_step_id IS NOT NULL)
  </behavior>
  <action>
    Add two exported async functions to recipeTechniqueSlotMaps.ts after getSlotMapByInstance,
    both using `const db = await getDb()` with NO BEGIN/COMMIT (file convention).

    getUnfilledSlotCount(recipeId: number): Promise&lt;number&gt; — SELECT COUNT(*) AS unfilled_count
    FROM recipe_technique_instances rti JOIN technique_colour_slots tcs ON tcs.technique_id = rti.technique_id
    LEFT JOIN recipe_technique_slot_maps sm ON sm.instance_id = rti.id AND sm.slot_id = tcs.id
    WHERE rti.recipe_id = $1 AND rti.detached = 0 AND (sm.instance_id IS NULL OR sm.paint_id IS NULL).
    Use LEFT JOIN on recipe_technique_slot_maps (NOT INNER — Pitfall 2, missing rows = unfilled).
    INCLUDE the `AND rti.detached = 0` filter (the detached column exists with DEFAULT 0 per migration 051 line 61).
    Return rows[0]?.unfilled_count ?? 0. Use positional $1 param. Type the select row as
    `{ unfilled_count: number }[]`.

    getStepSlotIdMap(recipeId: number): Promise&lt;Map&lt;number, number | null&gt;&gt; — reuse the
    getSlotResolutionMap JOIN chain but SELECT rs.id AS recipe_step_id, ts.colour_slot_id AS slot_id
    (no slot-map LEFT JOIN needed). WHERE rs.recipe_id = $1 AND rs.technique_step_id IS NOT NULL.
    Build and return Map keyed on recipe_step_id, value `row.slot_id ?? null`. Mirror the
    Map-build loop from getSlotResolutionMap exactly.

    Add doc comments matching the file's existing JSDoc style.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "recipeTechniqueSlotMaps" || echo "TYPECHECK_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function getUnfilledSlotCount" src/db/queries/recipeTechniqueSlotMaps.ts` returns 1
    - `grep -c "export async function getStepSlotIdMap" src/db/queries/recipeTechniqueSlotMaps.ts` returns 1
    - getUnfilledSlotCount SQL contains `LEFT JOIN recipe_technique_slot_maps` and `rti.detached = 0` (verify: `grep "rti.detached = 0" src/db/queries/recipeTechniqueSlotMaps.ts` matches)
    - getUnfilledSlotCount SQL does NOT use `INNER JOIN recipe_technique_slot_maps`
    - getStepSlotIdMap SQL selects `ts.colour_slot_id` and filters `rs.technique_step_id IS NOT NULL`
    - No BEGIN/COMMIT/ROLLBACK added (verify: `grep -ci "BEGIN" src/db/queries/recipeTechniqueSlotMaps.ts` returns 0)
    - `npx tsc --noEmit` reports no errors in this file
  </acceptance_criteria>
  <done>Both query functions exist, typecheck clean, LEFT JOIN + detached filter present, no transaction statements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useUnfilledSlotCount + useStepSlotIdMap hooks + UNFILLED_SLOT_COUNT_KEY invalidation</name>
  <files>src/hooks/useSlotResolutionMap.ts, src/hooks/useTechniqueInstances.ts</files>
  <read_first>
    - src/hooks/useSlotResolutionMap.ts (the file — useSlotResolutionMap lines 24-36 is the enabled-by-id hook analog; existing KEY factories lines 8-12)
    - src/hooks/useTechniqueInstances.ts lines 118-130 (useUpdateSlotMap onSuccess — extend this invalidation list)
    - .planning/phases/145-integration-pass/145-PATTERNS.md lines 108-184 (exact hook bodies + invalidation one-liner)
  </read_first>
  <behavior>
    - useUnfilledSlotCount(undefined) is disabled and resolves to 0
    - useUnfilledSlotCount(recipeId) calls getUnfilledSlotCount under UNFILLED_SLOT_COUNT_KEY(recipeId)
    - useStepSlotIdMap(undefined) is disabled and resolves to an empty Map
    - useStepSlotIdMap(recipeId) calls getStepSlotIdMap under STEP_SLOT_ID_MAP_KEY(recipeId)
    - After a slot reassignment, the unfilled-count cache entry for that recipe is invalidated
  </behavior>
  <action>
    In src/hooks/useSlotResolutionMap.ts: extend the import from
    "@/db/queries/recipeTechniqueSlotMaps" to add getUnfilledSlotCount and getStepSlotIdMap.
    Export UNFILLED_SLOT_COUNT_KEY = (recipeId: number) =&gt; ["unfilled-slot-count", recipeId] as const
    and STEP_SLOT_ID_MAP_KEY = (recipeId: number) =&gt; ["step-slot-id-map", recipeId] as const.
    Add useUnfilledSlotCount(recipeId: number | undefined) and useStepSlotIdMap(recipeId: number | undefined)
    following the enabled-by-id pattern of useSlotResolutionMap: queryKey falls back to the bare
    key array when recipeId is undefined; queryFn resolves Promise.resolve(0) (count) or
    Promise.resolve(new Map&lt;number, number | null&gt;()) (step-slot map) when disabled;
    `enabled: recipeId !== undefined`.

    In src/hooks/useTechniqueInstances.ts: add UNFILLED_SLOT_COUNT_KEY to the existing import from
    "@/hooks/useSlotResolutionMap". In useUpdateSlotMap onSuccess (lines 122-128), append one line:
    qc.invalidateQueries({ queryKey: UNFILLED_SLOT_COUNT_KEY(variables.recipeId) }). Do not remove
    any existing invalidation (SLOT_RESOLUTION_MAP_KEY, SLOT_MAP_BY_INSTANCE_KEY, RECIPE_PAINTS_KEY,
    RECIPE_SWATCH_KEY, RECIPE_AVAILABILITY_KEY all stay).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "useSlotResolutionMap|useTechniqueInstances" || echo "TYPECHECK_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function useUnfilledSlotCount" src/hooks/useSlotResolutionMap.ts` returns 1
    - `grep -c "export function useStepSlotIdMap" src/hooks/useSlotResolutionMap.ts` returns 1
    - `grep -c "UNFILLED_SLOT_COUNT_KEY" src/hooks/useSlotResolutionMap.ts` returns at least 2 (factory + usage)
    - `grep -c "STEP_SLOT_ID_MAP_KEY" src/hooks/useSlotResolutionMap.ts` returns at least 2
    - useTechniqueInstances.ts useUpdateSlotMap onSuccess invalidates UNFILLED_SLOT_COUNT_KEY (verify: `grep "UNFILLED_SLOT_COUNT_KEY(variables.recipeId)" src/hooks/useTechniqueInstances.ts` matches)
    - All five pre-existing invalidation calls remain in useUpdateSlotMap onSuccess (SLOT_RESOLUTION_MAP_KEY, SLOT_MAP_BY_INSTANCE_KEY, RECIPE_PAINTS_KEY, RECIPE_SWATCH_KEY, RECIPE_AVAILABILITY_KEY)
    - `npx tsc --noEmit` reports no errors in these files
  </acceptance_criteria>
  <done>Two new hooks + two key factories exported; useUpdateSlotMap invalidates the unfilled-count key alongside the existing five; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Three data-layer tests — duplication live-link, unfilled-count, availability no-undercounting</name>
  <files>tests/data-layer/recipe-duplication-live-link.test.ts, tests/data-layer/unfilled-slot-count.test.ts, tests/data-layer/availability-effective-paint.test.ts</files>
  <read_first>
    - tests/data-layer/apply-technique.test.ts (full file — fixture builder lines 42-85, assertion patterns; the canonical analog for all three tests)
    - tests/data-layer/db-helpers.ts lines 43-99, 118-166 (createHobbyforgeDb, createTestRecipe, createDbBridge)
    - src/db/queries/recipes.ts lines 148-271 (duplicateRecipe — what the live-link test asserts against)
    - src/lib/effectivePaintId.ts (the resolver the availability test exercises)
    - .planning/phases/145-integration-pass/145-PATTERNS.md lines 601-686 (test scaffolds + assertion lists)
    - .planning/phases/145-integration-pass/145-VALIDATION.md lines 51-57 (Wave 0 requirements)
  </read_first>
  <behavior>
    recipe-duplication-live-link.test.ts (INTG-05):
    - Copy has distinct recipe_technique_instances rows (new ids, same technique_id) vs original
    - Copy's recipe_technique_slot_maps are independent (same slot→paint, distinct instance_id)
    - Copy's recipe_steps carry technique_step_id (live link intact, not flattened)
    - Copy's recipe_sections carry technique_instance_id pointing to the copy's instances
    - Original recipe's instances are unchanged after duplication
    unfilled-slot-count.test.ts (INTG-06):
    - Returns 0 for a recipe with no technique instances
    - Returns slot count when no slot maps exist (all unfilled)
    - Decrements as slots are filled; returns 0 when all slots filled
    - A slot-map row with paint_id NULL counts as unfilled
    - A detached instance's slots are excluded from the count
    availability-effective-paint.test.ts (INTG-02):
    - effectivePaintId resolves a filled technique step to the slot's paint (counts toward owned/missing)
    - effectivePaintId resolves an unfilled technique step to null (NOT counted as missing)
    - effectivePaintId on a plain step returns step.paint_id unchanged (FND-04 fallback)
  </behavior>
  <action>
    Create three node-environment data-layer test files (`// @vitest-environment node` header)
    mirroring apply-technique.test.ts structure: vi.mock("@/db/client") before imports;
    createHobbyforgeDb() in beforeEach; seed a technique (techniques → technique_sections →
    technique_colour_slots → technique_steps) and a recipe via createTestRecipe; seed paint rows
    (INSERT OR IGNORE INTO paints with explicit ids) for slot-map FK satisfaction; wire getDb via
    createDbBridge; db.close() + vi.clearAllMocks() in afterEach.

    recipe-duplication-live-link.test.ts: apply a technique to the original recipe (call applyTechnique
    directly on the bridged db, or insert instance/section/step/slot-map rows directly), then call
    duplicateRecipe(originalId, "Copy"). Assert the five behaviors above by querying the in-memory db
    directly with db.prepare(...).all/get.

    unfilled-slot-count.test.ts: seed a technique with 2+ colour slots, apply to a recipe, then assert
    getUnfilledSlotCount across states — no fills, partial fills, all fills, an explicit-NULL fill, and a
    detached instance (UPDATE recipe_technique_instances SET detached = 1). Import getUnfilledSlotCount
    from "@/db/queries/recipeTechniqueSlotMaps".

    availability-effective-paint.test.ts: build a SlotResolutionMap via getSlotResolutionMap(recipeId)
    after applying a technique with one filled and one unfilled slot; call effectivePaintId(step, slotMap)
    for a filled technique step (expect the slot paint id), an unfilled technique step (expect null), and a
    plain step with a paint_id (expect that paint_id). This proves the no-silent-undercounting invariant at
    the resolver level. Import effectivePaintId from "@/lib/effectivePaintId" and getSlotResolutionMap from
    "@/db/queries/recipeTechniqueSlotMaps".

    Do NOT inline production-code implementations — only test setup, calls, and assertions.
  </action>
  <verify>
    <automated>pnpm test -- tests/data-layer/recipe-duplication-live-link.test.ts tests/data-layer/unfilled-slot-count.test.ts tests/data-layer/availability-effective-paint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three files start with `// @vitest-environment node`
    - All three files exist and run green: `pnpm test -- tests/data-layer/recipe-duplication-live-link.test.ts tests/data-layer/unfilled-slot-count.test.ts tests/data-layer/availability-effective-paint.test.ts` exits 0 with 0 failures
    - recipe-duplication-live-link.test.ts asserts copy steps carry technique_step_id (verify: `grep -c "technique_step_id" tests/data-layer/recipe-duplication-live-link.test.ts` >= 1)
    - unfilled-slot-count.test.ts asserts a detached instance is excluded (verify: `grep -c "detached" tests/data-layer/unfilled-slot-count.test.ts` >= 1)
    - availability-effective-paint.test.ts calls effectivePaintId and asserts an unfilled slot resolves to null (verify: `grep -c "effectivePaintId" tests/data-layer/availability-effective-paint.test.ts` >= 1)
  </acceptance_criteria>
  <done>Three node-env data-layer tests exist and pass, encoding the duplication live-link, unfilled-count, and no-undercounting invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component → DB query | recipeId from UI state crosses into SQL ($1 positional param — no string interpolation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01 | Information disclosure (data-integrity) | getUnfilledSlotCount | mitigate | LEFT JOIN + `(sm.instance_id IS NULL OR sm.paint_id IS NULL)` ensures slots with no map row are counted — prevents silent undercount of unfilled slots |
| T-145-02 | Tampering (miscount) | getUnfilledSlotCount | mitigate | `AND rti.detached = 0` excludes detached instances so the count cannot be inflated by Phase-146 detached rows |
| T-145-03 | Information disclosure (undercount) | duplicateRecipe live-link | mitigate | Test asserts copy preserves technique_step_id — proves duplication does not silently flatten/drop the live link |
</threat_model>

<verification>
- `pnpm test -- tests/data-layer/` runs all three new tests green
- `npx tsc --noEmit` clean for the two modified source files
- `grep -c "BEGIN" src/db/queries/recipeTechniqueSlotMaps.ts` returns 0 (flat inline SQL constraint, FND-05)
</verification>

<success_criteria>
- getUnfilledSlotCount + getStepSlotIdMap exist with correct LEFT JOIN and detached filter
- useUnfilledSlotCount + useStepSlotIdMap hooks exported with key factories
- useUpdateSlotMap invalidates UNFILLED_SLOT_COUNT_KEY without removing existing invalidation
- Three data-layer tests green, proving duplication live-link, unfilled-count, and no-undercounting
</success_criteria>

<output>
Create `.planning/phases/145-integration-pass/145-01-SUMMARY.md` when done
</output>

---
phase: 18-battle-log
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified: []
autonomous: false
requirements: [BATTLE-01, BATTLE-02, BATTLE-03, BATTLE-04, BATTLE-05]

must_haves:
  truths:
    - "All 5 BATTLE-01..05 success criteria verified by hand in the live Tauri app"
    - "User confirms the page matches the UI-SPEC contract (badges, summary bar, hover actions, inline expand)"
    - "User confirms no regressions on /army-lists, /spending, sidebar navigation"
  artifacts: []
  key_links: []
---

<objective>
Wave 3: Manual smoke-test checkpoint. Plans 00-02 are fully automated and unit-tested, but the only end-to-end verification of BATTLE-01..05 against a real SQLite database, real Tauri window, and real UI is human verification in the running app.

Purpose: Catch any IPC, persistence, animation, focus-trap, or styling regressions that unit tests cannot reach.
Output: User confirmation that the phase is shippable, or a written list of issues to gap-close in a follow-up plan.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-battle-log/18-VALIDATION.md
@.planning/phases/18-battle-log/18-UI-SPEC.md
@.planning/phases/18-battle-log/18-CONTEXT.md
@.planning/phases/18-battle-log/18-00-SUMMARY.md
@.planning/phases/18-battle-log/18-01-SUMMARY.md
@.planning/phases/18-battle-log/18-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Smoke-test the Battle Log feature in the live Tauri app</name>
  <read_first>
    - .planning/phases/18-battle-log/18-VALIDATION.md §"Manual-Only Verifications" (5 manual checks)
    - .planning/phases/18-battle-log/18-UI-SPEC.md §"Copywriting Contract" (exact strings to expect)
    - .planning/phases/18-battle-log/18-CONTEXT.md §"List entry display" (compact 2-line row + inline expand)
  </read_first>
  <action>
    Claude will start the Tauri dev server, then pause for the user to walk through every step below. The user types "approved" if all pass, or describes failures.

    Pre-flight (Claude runs):
    1. Run `pnpm tauri dev` in the project root (background). Wait until the window opens.
    2. Inform the user the app is running and ready for verification.

    User then performs these 11 steps in the running app:
  </action>
  <what-built>
    Phase 18 produced:
    - /battle-log route reachable via sidebar
    - Battle Log page with header ("Battle Log" + "Every game you've played, win or lose." + Log Game button)
    - Summary bar ("N games · XW YL ZD · Z% win rate") visible when logs exist
    - Compact 2-line row per log with WIN/LOSS/DRAW badge, opponent_faction · mission · points, army list name + battle_date
    - Hover-revealed Edit (pencil) and Delete (trash) icon buttons on each row
    - Click row -> inline expand showing scores, opponent name, MVP unit, underperformed unit, lessons learned, changes next time
    - Sheet form for create + edit (4 grouped sections: Required, Game Details, Linked Records, Post-Game Notes)
    - Delete confirmation Dialog (sibling portal — never nested)
    - Empty state: Swords icon in muted-pill + "No games logged yet" + Log Game CTA
  </what-built>
  <how-to-verify>
    Walk through every step. The expected outcome is in parentheses.

    **Step 1 — Sidebar entry & navigation (BATTLE-04 nav)**
    1. Look at the sidebar's Tracking section. (Expected: three entries — Army Lists, Battle Log, Spending — in that order. Battle Log uses the Swords icon, distinct from the Sword wordmark icon at the top.)
    2. Click "Battle Log".
       (Expected: page navigates to /battle-log; URL bar reflects this; sidebar entry shows active highlight.)

    **Step 2 — Empty state (BATTLE-04)**
    On first visit (assuming no battle_logs rows):
    - Page shows the heading "Battle Log" + subtitle "Every game you've played, win or lose." + a "Log Game" button (top-right).
    - Below the header: an empty-state block with Swords icon in a muted-pill, "No games logged yet" heading, body text "Record your first game to start tracking wins, losses, and lessons learned.", and a "Log Game" button.
    - No summary bar visible (it's hidden until at least one log exists).

    **Step 3 — Create a Win (BATTLE-01, BATTLE-02, BATTLE-03)**
    1. Click "Log Game" (either button).
       (Expected: a Sheet slides in from the right with title "Log Game" and description "Record a new game — result, opponent, mission, and notes.".)
    2. Verify the form has 4 grouped sections separated by `<Separator />` and small uppercase labels: (a) the unlabelled required block at the top, (b) "Game Details", (c) "Linked Records", (d) "Post-Game Notes".
    3. Verify `Date` field defaults to today's date in YYYY-MM-DD format.
    4. Fill in: Date = today, Opponent Faction = "Tau Empire", Mission = "Take and Hold", Result = "Win". Optionally fill Opponent Name = "Alex", Points = 2000, My VP = 85, Opponent VP = 60. Select an existing army list (if one exists). Add Lessons Learned = "Aggressive deployment paid off." and Changes Next Time = "Hold reserves longer.".
    5. Click "Log Game" (submit button).
       (Expected: Sheet closes; toast "Game logged."; new row appears at the TOP of the list with green WIN badge, "Tau Empire · Take and Hold · 2000pts" line 1, and army list name + today's date line 2.)

    **Step 4 — Summary bar appears (BATTLE-04)**
    Above the list (and below the header): "1 games · 1W 0L 0D · 100% win rate". Wins=green-500, losses=red-500, draws=muted, all numerals tabular-nums.

    **Step 5 — Add a Loss + a Draw (BATTLE-01)**
    Repeat Step 3 twice with Loss and Draw results.
       (Expected: rows appear in date-DESC order; summary updates to "3 games · 1W 1L 1D · 33% win rate".)

    **Step 6 — Inline expand (BATTLE-03)**
    1. Click the body of the WIN row (anywhere except the right-side icon area).
       (Expected: row expands inline; gray panel shows MyVP/OpponentVP, "Opponent Alex", "Lessons Learned" + the text, "Changes Next Time" + the text. Animation is acceptable instant or animated.)
    2. Click again to collapse.
       (Expected: panel hides.)

    **Step 7 — Hover actions (BATTLE-05)**
    1. Hover over any row.
       (Expected: Edit pencil + Delete trash icons appear at the right side, and remain hidden when the cursor leaves.)
    2. Click the Edit pencil.
       (Expected: Sheet opens with title "Edit Game" and ALL fields pre-filled with that log's values; row does NOT expand simultaneously.)
    3. Change Mission to "Scorched Earth", click "Update Game".
       (Expected: Sheet closes; toast "Game updated."; row line 1 updates to show "Scorched Earth"; cache invalidates and re-fetches without flicker.)

    **Step 8 — Pitfall 5 verified — clear an FK (BATTLE-02)**
    1. Edit a log that currently has an army_list_id set.
    2. Change "Army List Used" to "No army list".
    3. Save.
       (Expected: Sheet closes; the row's line 2 now shows "No army list · {date}" instead of the army list name. This proves updateBattleLog uses full-replacement UPDATE, not COALESCE.)

    **Step 9 — Deleted army list (BATTLE-02 fallback)**
    1. Recreate a log linked to a real army list.
    2. Navigate to /army-lists, delete that army list.
    3. Navigate back to /battle-log, find the affected row.
       (Expected: line 2 shows "(Army list deleted)" in italic muted text, NOT the empty/no-link fallback. The log itself remains visible — ON DELETE SET NULL preserved the row but cleared army_list_id.)

    **Step 10 — Delete a log (BATTLE-05)**
    1. Hover any row, click the red trash icon.
       (Expected: Dialog opens with title "Delete this game log?" and body "This will permanently delete the game against {opponent_faction} on {battle_date}. This cannot be undone.".)
    2. Click "Cancel".
       (Expected: Dialog closes; row remains in the list.)
    3. Re-open and click "Delete".
       (Expected: Dialog closes; toast "Game log deleted."; row vanishes from list immediately; summary bar updates.)

    **Step 11 — Persistence**
    1. Close the Tauri window completely.
    2. Re-launch with `pnpm tauri dev`.
    3. Navigate to /battle-log.
       (Expected: all remaining logs still present, in date-DESC order, with all fields preserved.)

    **Cross-page regression check**
    - Click Army Lists in sidebar: page loads, list visible, no console errors.
    - Click Spending in sidebar: page loads, totals visible.
    - Click Dashboard: loads without flicker.
  </how-to-verify>
  <resume-signal>
    Type "approved" if every expected outcome above held. Otherwise list the failed step numbers + a one-sentence description of what went wrong, and Claude will route to gap closure (`/gsd:plan-phase 18 --gaps`).
  </resume-signal>
  <acceptance_criteria>
    - User explicitly types "approved" OR provides a list of failures
    - If failures exist, they are recorded verbatim in 18-03-SUMMARY.md
    - If approved, .planning/STATE.md is updated to mark Phase 18 complete and to advance current_phase to 19
  </acceptance_criteria>
  <done>User has approved the manual smoke test or has provided a written failure list for gap closure.</done>
</task>

</tasks>

<verification>
On user "approved" signal:
- Phase 18 success criteria are all met (BATTLE-01..05 verified)
- Full vitest suite green (Plans 00-02 already enforce this)
- Production build succeeds (Plan 02 enforces)
- Manual end-to-end verified
</verification>

<success_criteria>
- All 11 verification steps pass per UI-SPEC contract
- No console errors, no Tauri IPC failures, no React warnings about nested portals
- Cross-page navigation works (no breakage on /army-lists, /spending, /)
- Phase 18 ROADMAP.md status flips to Complete after this plan
</success_criteria>

<output>
After user approval, create `.planning/phases/18-battle-log/18-03-SUMMARY.md` capturing:
- Approval timestamp + outcome
- Any issues found during verification (if not approved, list them for gap-closure planning)
- Confirmation that Phase 18 BATTLE-01..05 success criteria are all verified end-to-end
- Hand-off note for Phase 19 (Analytics Core depends on Phase 17, NOT Phase 18 — Phase 18 has no downstream blocker)
</output>

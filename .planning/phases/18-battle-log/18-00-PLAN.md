---
phase: 18-battle-log
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/battle-log/battleLogQueries.test.ts
  - tests/battle-log/computeBattleLogSummary.test.ts
autonomous: true
requirements: [BATTLE-01, BATTLE-02, BATTLE-03, BATTLE-04, BATTLE-05]

must_haves:
  truths:
    - "tests/battle-log/ directory exists with two .test.ts stub files"
    - "All test stubs use it.skip — full suite is GREEN with stubs deferred"
    - "Stubs name every BATTLE-01..05 requirement they will eventually verify"
  artifacts:
    - path: "tests/battle-log/battleLogQueries.test.ts"
      provides: "BATTLE-01,02,03,05 — query module SQL contract stubs"
      contains: "describe(\"battleLogs queries"
    - path: "tests/battle-log/computeBattleLogSummary.test.ts"
      provides: "BATTLE-04 — pure aggregation function stubs"
      contains: "describe(\"computeBattleLogSummary"
  key_links:
    - from: "tests/battle-log/battleLogQueries.test.ts"
      to: "src/db/queries/battleLogs.ts"
      via: "import statements (will resolve once Plan 01 creates module)"
      pattern: "from \"@/db/queries/battleLogs\""
    - from: "tests/battle-log/computeBattleLogSummary.test.ts"
      to: "src/features/battle-log/computeBattleLogSummary.ts"
      via: "import statement (will resolve once Plan 01 creates module)"
      pattern: "from \"@/features/battle-log/computeBattleLogSummary\""
---

<objective>
Wave 0: Create failing test stubs for the entire Battle Log phase.

Purpose: Establish the test contract before implementation. Every BATTLE-01..05 requirement is named in a stub `it.skip` block, so Plan 01 can flip them to active and watch them go green. Mirrors the Wave-0 pattern used by Phase 14 (`tests/spending/`) and Phase 15 (`tests/datasheet/`).

Output: Two test files with `it.skip` blocks (full suite stays GREEN), each block names exactly which requirement and behavior it will verify when activated.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-battle-log/18-CONTEXT.md
@.planning/phases/18-battle-log/18-RESEARCH.md
@.planning/phases/18-battle-log/18-VALIDATION.md
@tests/foundation/armyListQueries.test.ts
@tests/spending/computeSpendingStats.test.ts

<interfaces>
<!-- Stubs reference symbols that Plan 01 will create. Stubs use `import` statements so a TypeScript "module not found" error is the SECOND-stage signal (after `it.skip`). The test file remains parseable. -->

Symbols Plan 01 will export from `src/db/queries/battleLogs.ts`:
- getBattleLogs(): Promise<BattleLog[]>
- getBattleLogSummary(): Promise<{ result: string; count: number }[]>
- createBattleLog(input: CreateBattleLogInput): Promise<number>
- updateBattleLog(input: UpdateBattleLogInput): Promise<void>
- deleteBattleLog(id: number): Promise<void>

Symbols Plan 01 will export from `src/features/battle-log/computeBattleLogSummary.ts`:
- computeBattleLogSummary(rows: { result: string; count: number }[]): BattleLogSummary
- interface BattleLogSummary { total: number; wins: number; losses: number; draws: number; winRate: number }

Test mock pattern (from tests/foundation/armyListQueries.test.ts):
```typescript
const selectMock = vi.fn();
const executeMock = vi.fn();
vi.mock("@/db/client", () => ({
  getDb: async () => ({ select: selectMock, execute: executeMock }),
}));
```

NOTE: Wave 0 stubs use `it.skip(...)` — they do NOT import the not-yet-existing modules at the top level (that would fail TS compilation). Use TYPE-ONLY imports or wrap imports in dynamic `await import()` inside the skipped tests, OR simply omit the imports for now and add a TODO comment naming the symbol. Pattern used in tests/datasheet/* Wave 0: top-level imports OMITTED, replaced by `// TODO: import { ... } once Plan 01 creates the module`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create battleLogQueries.test.ts stub (BATTLE-01,02,03,05)</name>
  <files>tests/battle-log/battleLogQueries.test.ts</files>
  <read_first>
    - tests/foundation/armyListQueries.test.ts (mock pattern, describe/it structure, SQL regex assertions)
    - tests/spending/computeSpendingStats.test.ts (vitest import + describe layout)
    - .planning/phases/18-battle-log/18-RESEARCH.md §"battleLogs.ts query module skeleton" (target SQL strings to match)
    - .planning/phases/18-battle-log/18-VALIDATION.md (test file paths and naming)
  </read_first>
  <action>
    Create `tests/battle-log/battleLogQueries.test.ts` (mkdir tests/battle-log if missing).

    File header:
    ```typescript
    /**
     * Phase 18 — Battle Log query module SQL contract tests.
     *
     * Wave 0: stubs only (it.skip). Plan 01 flips these to active by removing
     * .skip and adding the actual mock setup once src/db/queries/battleLogs.ts
     * is created.
     *
     * Mocks getDb() because tauri-plugin-sql IPC cannot run in jsdom.
     * Mirrors tests/foundation/armyListQueries.test.ts.
     *
     * Covers BATTLE-01 (create with required cols), BATTLE-02 (army_list_id
     * nullable + full-replacement UPDATE), BATTLE-03 (notes columns +
     * mvp/underperforming unit FKs), BATTLE-05 (delete by id).
     */
    import { describe, it } from "vitest";
    // TODO Plan 01: replace this comment with:
    //   import { vi, expect, beforeEach } from "vitest";
    //   const selectMock = vi.fn();
    //   const executeMock = vi.fn();
    //   vi.mock("@/db/client", () => ({
    //     getDb: async () => ({ select: selectMock, execute: executeMock }),
    //   }));
    //   import { getBattleLogs, createBattleLog, updateBattleLog, deleteBattleLog }
    //     from "@/db/queries/battleLogs";
    //   beforeEach(() => { selectMock.mockReset(); executeMock.mockReset(); });
    ```

    Then add EXACTLY these `describe`/`it.skip` blocks (do not write bodies — leave each `it.skip` with a single empty arrow function `() => {}`):

    ```typescript
    describe("battleLogs queries — getBattleLogs (BATTLE-04)", () => {
      it.skip("calls db.select with 'SELECT * FROM battle_logs ORDER BY battle_date DESC, created_at DESC'", () => {});
    });

    describe("battleLogs queries — getBattleLogSummary (BATTLE-04)", () => {
      it.skip("calls db.select with 'SELECT result, COUNT(*) AS count FROM battle_logs GROUP BY result'", () => {});
    });

    describe("battleLogs queries — createBattleLog (BATTLE-01, BATTLE-02, BATTLE-03)", () => {
      it.skip("INSERTs all 14 columns in the documented order and returns lastInsertId", () => {});
      it.skip("passes null for nullable fields when input properties are undefined (army_list_id, opponent, points_played, my_score, opponent_score, mvp_unit_id, underperforming_unit_id, lessons_learned, changes_next_time, notes)", () => {});
    });

    describe("battleLogs queries — updateBattleLog (BATTLE-02 — Pitfall 5: full-replacement UPDATE for nullable FKs)", () => {
      it.skip("uses full-replacement UPDATE (SET army_list_id = $2) — does NOT use COALESCE for army_list_id, mvp_unit_id, or underperforming_unit_id", () => {});
      it.skip("includes WHERE id = $1 and binds id as the first positional parameter", () => {});
      it.skip("does NOT touch updated_at column (battle_logs schema has no updated_at)", () => {});
    });

    describe("battleLogs queries — deleteBattleLog (BATTLE-05)", () => {
      it.skip("runs DELETE FROM battle_logs WHERE id = $1 with the given id", () => {});
    });
    ```

    Total: 8 it.skip stubs. Do NOT write test bodies — Plan 01 fills them in when flipping.
  </action>
  <verify>
    <automated>pnpm vitest run tests/battle-log/battleLogQueries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/battle-log/battleLogQueries.test.ts
    - File contains the literal string `describe("battleLogs queries — getBattleLogs (BATTLE-04)"`
    - File contains the literal string `describe("battleLogs queries — createBattleLog (BATTLE-01, BATTLE-02, BATTLE-03)"`
    - File contains the literal string `describe("battleLogs queries — updateBattleLog (BATTLE-02 — Pitfall 5: full-replacement UPDATE for nullable FKs)"`
    - File contains the literal string `describe("battleLogs queries — deleteBattleLog (BATTLE-05)"`
    - File contains exactly 8 occurrences of `it.skip(`
    - File contains zero occurrences of `it(` (active, non-skip)
    - File contains zero occurrences of `expect(` (no assertions yet — Plan 01 adds them)
    - `pnpm vitest run tests/battle-log/battleLogQueries.test.ts` exits 0 with all 8 tests reported as skipped (not failed)
  </acceptance_criteria>
  <done>Stub file exists with 8 it.skip blocks naming every BATTLE-01..05 query contract; vitest reports the file as 8 skipped + 0 failed.</done>
</task>

<task type="auto">
  <name>Task 2: Create computeBattleLogSummary.test.ts stub (BATTLE-04)</name>
  <files>tests/battle-log/computeBattleLogSummary.test.ts</files>
  <read_first>
    - tests/spending/computeSpendingStats.test.ts (pure-function fixture pattern, describe naming)
    - .planning/phases/18-battle-log/18-RESEARCH.md §"computeBattleLogSummary pure function" (target signature + return shape)
  </read_first>
  <action>
    Create `tests/battle-log/computeBattleLogSummary.test.ts`.

    File header:
    ```typescript
    /**
     * Phase 18 — computeBattleLogSummary pure-function tests.
     *
     * Wave 0: stubs only (it.skip). Plan 01 flips these to active.
     *
     * Covers BATTLE-04 (chronological list summary bar): the W/L/D counts and
     * win-rate percent shown above the log list are derived in JS from the
     * GROUP BY rows returned by getBattleLogSummary().
     *
     * Pure function — no mocks needed. Mirrors tests/spending/computeSpendingStats.test.ts.
     */
    import { describe, it } from "vitest";
    // TODO Plan 01: replace this comment with:
    //   import { expect } from "vitest";
    //   import { computeBattleLogSummary } from "@/features/battle-log/computeBattleLogSummary";
    ```

    Then add EXACTLY these `describe`/`it.skip` blocks (empty bodies):

    ```typescript
    describe("computeBattleLogSummary — BATTLE-04 (summary bar aggregation)", () => {
      it.skip("returns total=0, wins=0, losses=0, draws=0, winRate=0 for an empty rows array", () => {});
      it.skip("counts wins, losses, draws separately by exact string match on row.result ('Win', 'Loss', 'Draw')", () => {});
      it.skip("treats missing result categories as 0 (rows array missing a 'Loss' entry returns losses=0, not undefined)", () => {});
      it.skip("computes winRate as Math.round((wins / total) * 100) — integer percent, no decimal", () => {});
      it.skip("returns winRate=0 when total=0 (does NOT throw or return NaN from divide-by-zero)", () => {});
      it.skip("ignores rows whose result string does not match 'Win' | 'Loss' | 'Draw' exactly (case-sensitive — 'win' is ignored)", () => {});
    });
    ```

    Total: 6 it.skip stubs. Do NOT write test bodies.
  </action>
  <verify>
    <automated>pnpm vitest run tests/battle-log/computeBattleLogSummary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/battle-log/computeBattleLogSummary.test.ts
    - File contains the literal string `describe("computeBattleLogSummary — BATTLE-04 (summary bar aggregation)"`
    - File contains exactly 6 occurrences of `it.skip(`
    - File contains zero occurrences of `it(` (active, non-skip)
    - File contains zero occurrences of `expect(`
    - File contains the literal string `winRate as Math.round((wins / total) * 100)`
    - `pnpm vitest run tests/battle-log/computeBattleLogSummary.test.ts` exits 0 with all 6 tests reported as skipped
  </acceptance_criteria>
  <done>Stub file exists with 6 it.skip blocks naming every BATTLE-04 aggregation contract; vitest reports the file as 6 skipped + 0 failed.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm vitest run tests/battle-log/` exits 0 with 14 skipped + 0 passed + 0 failed
2. `pnpm vitest run` (full suite) exits 0 — no regressions
3. `tests/battle-log/` directory contains exactly 2 files
</verification>

<success_criteria>
- 14 it.skip stubs total across 2 files
- Every BATTLE-01..05 requirement is named in at least one stub describe/it block
- Full vitest suite is GREEN (no fails introduced)
- Plan 01 can begin work immediately
</success_criteria>

<output>
After completion, create `.planning/phases/18-battle-log/18-00-SUMMARY.md` capturing:
- Stub count per file (8 + 6 = 14)
- Requirement coverage map (BATTLE-01..05 → stubs)
- Activation checklist for Plan 01 ("flip these stubs in this order")
</output>

---
phase: 144-live-link-re-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/052_technique_resync.sql
  - src-tauri/src/lib.rs
  - tests/data-layer/technique-resync.test.ts
  - src/db/queries/recipeTechniqueResync.ts
autonomous: true
requirements: [LINK-01]
user_setup: []

must_haves:
  truths:
    - "Migration 052 adds recipe_sections.technique_section_id (nullable FK) and an index on recipe_steps(technique_step_id); pnpm check:version 3-leg gate passes"
    - "resyncTechniqueInstances(db, techniqueId) propagates a technique's section/step structure to every non-detached recipe instance using a single passed-in db handle (no internal getDb, no BEGIN)"
    - "A surviving technique step keeps its materialised recipe_steps.id PK across a resync (reorder = UPDATE order_index only) so its unit_recipe_step_progress row is untouched"
    - "A removed technique step causes its recipe_steps row to be DELETEd and its progress to cascade-delete; an added technique step inserts a new uncompleted recipe_steps row (paint_id NULL)"
    - "Resync skips detached=1 instances and applies to all detached=0 instances of the technique"
  artifacts:
    - path: "src-tauri/migrations/052_technique_resync.sql"
      provides: "recipe_sections.technique_section_id column + idx_recipe_steps_technique_step_id"
      contains: "ALTER TABLE recipe_sections ADD COLUMN technique_section_id"
    - path: "src/db/queries/recipeTechniqueResync.ts"
      provides: "resyncTechniqueInstances + getNonDetachedInstanceCount"
      exports: ["resyncTechniqueInstances", "getNonDetachedInstanceCount"]
    - path: "tests/data-layer/technique-resync.test.ts"
      provides: "LINK-01 reorder/add/remove/slot/multi-recipe/cross-section + teeth counter-case"
      min_lines: 200
  key_links:
    - from: "src/db/queries/recipeTechniqueResync.ts"
      to: "recipe_steps.technique_step_id"
      via: "UPDATE-by-PK for survivors / DELETE for removed / INSERT for added"
      pattern: "technique_step_id"
    - from: "src-tauri/src/lib.rs"
      to: "migrations/052_technique_resync.sql"
      via: "Migration{ version: 52, include_str! }"
      pattern: "052_technique_resync"
---

<objective>
Build the live-link resync engine and its schema support, test-first. This is the
milestone's highest-risk novel code and the Phase 144 Critical Gate: the data-layer
tests written here must pass before any UI depends on the resync.

Three deliverables:
1. Migration 052 — adds `recipe_sections.technique_section_id` (exact section identity,
   avoids positional ambiguity) + an index on `recipe_steps(technique_step_id)`; lib.rs
   registration; `pnpm check:version` 3-leg gate stays green.
2. `tests/data-layer/technique-resync.test.ts` — covers all LINK-01 cases (reorder, add,
   remove, slot CASCADE, multi-recipe, cross-section move) PLUS the DELETE+INSERT "teeth"
   counter-assertion proving the identity-preserving path is the correct one.
3. `src/db/queries/recipeTechniqueResync.ts` — `resyncTechniqueInstances(db, techniqueId)`
   (single passed-in handle, flat inline SQL) + `getNonDetachedInstanceCount(techniqueId)`.

Purpose: LINK-01 — editing a technique's step structure propagates to every recipe using
it while each recipe keeps its own slot colours and existing step-completion progress.
Output: migration 052, resync module, passing data-layer test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-live-link-re-sync/144-RESEARCH.md
@.planning/phases/144-live-link-re-sync/144-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase — no exploration required. -->

From src/db/queries/recipeTechniqueInstances.ts (lines 19-141) — applyTechnique is the
materialisation that resync re-applies; mirror its INSERT column list exactly:
  import { getDb } from "@/db/client";
  import type { TechniqueSection, TechniqueStep } from "@/types/technique";
  // recipe_steps INSERT columns (14): recipe_id, paint_id, step_name, order_index, notes,
  //   painting_phase, tool, technique, dilution, time_estimate_minutes,
  //   step_photo_path, alt_paint_id, section_id, technique_step_id
  // For added steps: paint_id NULL, step_photo_path NULL, alt_paint_id NULL, technique_step_id = technique_steps.id
  // recipe_sections INSERT columns (7): recipe_id, name, surface, optional, order_index, notes, technique_instance_id
  //   (052 adds technique_section_id as an 8th column — populate it on INSERT)

From src/db/queries/techniques.ts (saveTechniqueGraph, lines 313-356) — the single-db-handle
contract (verbatim explanation lives at lines 319-324):
  // tauri-plugin-sql uses sqlx::Pool<Sqlite>; explicit BEGIN/COMMIT is broken across calls.
  // Auto-commit per statement in WAL mode. resync receives `db` from saveTechniqueGraph —
  // NEVER call getDb() inside resyncTechniqueInstances.

From src-tauri/migrations/051_technique_library_foundation.sql:
  // recipe_sections has technique_instance_id (line 75) but NO technique_section_id
  // recipe_steps has technique_step_id (line 77), ON DELETE SET NULL — NOT cascade
  // recipe_technique_instances.detached INTEGER NOT NULL DEFAULT 0 (line ~61)
From src-tauri/migrations/028_step_progress_identity.sql:
  // unit_recipe_step_progress.recipe_step_id REFERENCES recipe_steps(id) ON DELETE CASCADE (line 17)

From src-tauri/src/lib.rs (line ~309) — migration registration shape:
  Migration { version: 51, description: "...", sql: include_str!("../migrations/051_technique_library_foundation.sql"), kind: MigrationKind::Up },

From tests/data-layer/db-helpers.ts (lines 28-59) — createHobbyforgeDb() reads ALL .sql files
from src-tauri/migrations/ in numeric order, so migration 052 reaches the test DB automatically.
Helpers available: createHobbyforgeDb, createTestRecipe, createTestFaction, createTestUnit,
createTestSection, createDbBridge.
</interfaces>

@tests/data-layer/technique-progress-identity.test.ts
@src/db/queries/recipeTechniqueInstances.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 052 — technique_section_id column + resync index + lib.rs registration</name>
  <files>src-tauri/migrations/052_technique_resync.sql, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/migrations/051_technique_library_foundation.sql lines 41-78 (ALTER TABLE nullable-FK pattern; confirms technique_section_id does NOT yet exist on recipe_sections)
    - src-tauri/src/lib.rs lines 305-315 (the Migration{ version, description, sql: include_str!, kind } registration block for 051)
    - scripts/check-version.mjs (the 3-leg gate: version parity, fileCount === lib.rs Migration{} count, no CR bytes)
    - .claude/skills/new-migration (project migration skill)
  </read_first>
  <action>
    Create migration `src-tauri/migrations/052_technique_resync.sql` containing exactly two
    statements: `ALTER TABLE recipe_sections ADD COLUMN technique_section_id INTEGER REFERENCES
    technique_sections(id) ON DELETE SET NULL;` and `CREATE INDEX IF NOT EXISTS
    idx_recipe_steps_technique_step_id ON recipe_steps(technique_step_id);`. Add a header comment
    explaining the column enables exact (non-positional) section identity during resync and the
    index speeds the per-step lookup. The file MUST be LF-only (no CR/0x0D bytes) — CRLF breaks the
    _sqlx_migrations SHA-384 check (check-version.mjs Leg 3). Register it in src-tauri/src/lib.rs by
    adding a `Migration { version: 52, description: "technique_resync", sql:
    include_str!("../migrations/052_technique_resync.sql"), kind: MigrationKind::Up }` entry
    immediately after the version-51 entry. Do NOT edit migration 051. HOBBYFORGE_MIGRATION_COUNT in
    db-helpers.ts is disk-derived (auto-counts files) — no manual bump needed.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm check:version</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check:version` exits 0 with `[version] OK`, `[migration-count] OK: 52 .sql files === 52 Migration{} entries`, `[cr-byte] OK`
    - File `src-tauri/migrations/052_technique_resync.sql` exists and contains the string `ALTER TABLE recipe_sections ADD COLUMN technique_section_id`
    - `src-tauri/src/lib.rs` contains `include_str!("../migrations/052_technique_resync.sql")` with `version: 52`
    - `grep -c $'\r' src-tauri/migrations/052_technique_resync.sql` returns 0 (LF-only)
  </acceptance_criteria>
  <done>Migration 052 exists, is LF-only, is registered in lib.rs, and the 3-leg check:version gate passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — technique-resync data-layer test scaffold covering all LINK-01 cases + teeth counter-case</name>
  <files>tests/data-layer/technique-resync.test.ts</files>
  <read_first>
    - tests/data-layer/technique-progress-identity.test.ts lines 1-27 (file header: `// @vitest-environment node`, imports, vi.mock pattern), lines 55-165 (beforeEach materialisation fixture), lines 189-419 (the exact reorder/add/remove SQL operations + the lines 360-419 teeth counter-case to mirror)
    - tests/data-layer/db-helpers.ts lines 38-130 (createHobbyforgeDb, createDbBridge, createTestRecipe/Faction/Unit/Section factory signatures)
    - tests/data-layer/apply-technique.test.ts lines 1-30 (createDbBridge + vi.mock("@/db/client") wiring for testing query functions)
    - .planning/phases/144-PATTERNS.md "tests/data-layer/technique-resync.test.ts" section (lines 406-484) for the fixture variable layout and the reorder-test assertion shape
  </read_first>
  <behavior>
    - reorder: after mutating technique_steps order_index and calling resyncTechniqueInstances, the surviving recipe_steps.id is unchanged, its order_index reflects the new technique order, and the completed unit_recipe_step_progress row for that recipe_step_id still exists with completed=1
    - add: a new technique_step inserted then resynced produces exactly one new recipe_steps row with technique_step_id set, paint_id NULL, and zero unit_recipe_step_progress rows
    - remove: deleting a technique_step then resyncing DELETEs the matching recipe_steps row and its unit_recipe_step_progress row is gone (count 0)
    - slot remove: deleting a technique_colour_slot cascades recipe_technique_slot_maps rows to 0 (CASCADE, no resync code needed) — assert the orphan count is 0
    - multi-recipe: two non-detached instances both receive the change; a detached=1 instance is left untouched (its recipe_steps unchanged)
    - cross-section move: a technique step whose technique_section_id changes keeps its recipe_steps.id and progress (UPDATE section_id, not DELETE+INSERT)
    - teeth counter-case: an explicit `DELETE FROM recipe_steps WHERE id = ?` + re-INSERT of a surviving step loses the progress row — assert progress becomes count 0, proving the UPDATE-by-PK path is required
  </behavior>
  <action>
    Create `tests/data-layer/technique-resync.test.ts` beginning with `// @vitest-environment node`.
    Mirror the fixture in technique-progress-identity.test.ts: `createHobbyforgeDb()` in beforeEach,
    `db.close()` in afterEach, `vi.mock("@/db/client", () => ({ getDb: vi.fn() }))`,
    `const bridge = createDbBridge(db); vi.mocked(getDb).mockResolvedValue(bridge as never)`. Build a
    technique with one section and three steps (s1,s2,s3), apply it to a recipe to materialise an
    instance (populate recipe_sections.technique_instance_id AND the new technique_section_id from
    052), complete s2's recipe_step in unit_recipe_step_progress. Write the seven `it(...)` cases per
    <behavior>, each calling `await resyncTechniqueInstances(bridge, techniqueId)` (except the teeth
    case which performs raw DELETE+INSERT to prove the failure mode). Import
    `resyncTechniqueInstances` from `@/db/queries/recipeTechniqueResync` — it does not exist yet, so
    the suite fails to import/run (RED). Do NOT write the implementation in this task.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/data-layer/technique-resync.test.ts 2>&1 | grep -iE "fail|error|cannot find module" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/data-layer/technique-resync.test.ts` exists, starts with `// @vitest-environment node`, and contains seven `it(` cases (reorder, add, remove, slot remove, multi-recipe, cross-section, teeth)
    - The test imports `resyncTechniqueInstances` from `@/db/queries/recipeTechniqueResync`
    - Running the suite fails RED (module not found or assertions fail) — this is the expected pre-implementation state
    - The teeth case performs a raw `DELETE FROM recipe_steps WHERE id` + re-INSERT and asserts progress count becomes 0
  </acceptance_criteria>
  <done>Test file exists with all seven cases and fails RED because the resync module is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — implement resyncTechniqueInstances + getNonDetachedInstanceCount</name>
  <files>src/db/queries/recipeTechniqueResync.ts</files>
  <read_first>
    - src/db/queries/recipeTechniqueInstances.ts lines 19-141 (applyTechnique: exact recipe_sections / recipe_steps INSERT column lists, ORDER BY order_index queries, lastInsertId chaining)
    - src/db/queries/techniques.ts lines 319-324 (single-db-handle / no-BEGIN contract) and lines 556-637 (computeTechniqueStepDiff usage + UPDATE-by-PK survivor pattern)
    - tests/data-layer/technique-progress-identity.test.ts lines 192-315 (the exact REORDER UPDATE, ADD INSERT, REMOVE DELETE SQL the resync must produce)
    - .planning/phases/144-PATTERNS.md "src/db/queries/recipeTechniqueResync.ts" section (lines 27-154) for the full per-instance algorithm including the Pitfall-5 instance-wide step lookup
    - .planning/phases/144-RESEARCH.md Pitfall 5 (lines 497-507) cross-section move and Pitfall 7 (lines 530-543) FK cascade two-step path
  </read_first>
  <action>
    Create `src/db/queries/recipeTechniqueResync.ts`. Export
    `async function resyncTechniqueInstances(db: Awaited<ReturnType<typeof import("@/db/client").getDb>>, techniqueId: number): Promise<void>`
    — the db handle is passed in; NEVER call getDb() inside it; no BEGIN. Algorithm: (1) SELECT
    non-detached instances `WHERE technique_id = $1 AND detached = 0 ORDER BY id ASC`; (2) SELECT the
    technique's technique_sections ORDER BY order_index; (3) per instance: SELECT its recipe_sections
    `WHERE technique_instance_id = $1`, match each technique_section to a recipe_section by the new
    `technique_section_id` column (insert a recipe_sections row with technique_instance_id +
    technique_section_id for added sections, DELETE recipe_sections whose technique_section_id no
    longer exists, UPDATE order_index for survivors); (4) build an instance-WIDE lookup of all
    recipe_steps for the instance keyed by technique_step_id (query across all sections — Pitfall 5)
    so a cross-section move is UPDATEd not DELETE+INSERTed; (5) for each technique_step: if a survivor
    exists UPDATE recipe_steps SET order_index + section_id + content fields WHERE id = survivor.id
    (NEVER DELETE+INSERT — FND-03); if new, INSERT a recipe_steps row (14-column list, paint_id NULL,
    technique_step_id set, section_id = the resolved recipe_section); (6) DELETE recipe_steps whose
    technique_step_id is no longer in the technique (progress cascade-deletes via migration 028). All
    SQL uses $1,$2 positional params; detached compared as integer `= 0`. Also export
    `async function getNonDetachedInstanceCount(techniqueId: number): Promise<number>` — this
    standalone query MAY call getDb() (it is not embedded in saveTechniqueGraph) and returns
    `SELECT COUNT(*) AS n FROM recipe_technique_instances WHERE technique_id = $1 AND detached = 0`.
    Do not wire into saveTechniqueGraph in this plan — that call site is plan 03's concern via the
    confirmation flow; this plan proves the engine in isolation.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/data-layer/technique-resync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All seven cases in tests/data-layer/technique-resync.test.ts pass GREEN
    - `src/db/queries/recipeTechniqueResync.ts` exports `resyncTechniqueInstances` and `getNonDetachedInstanceCount`
    - `resyncTechniqueInstances` contains NO call to `getDb(` (grep returns 0 matches inside the function body) and NO `BEGIN`
    - Survivor handling uses `UPDATE recipe_steps ... WHERE id =` (never DELETE+INSERT for a step that still exists in the technique)
    - `pnpm test -- tests/data-layer/technique-progress-identity.test.ts` still passes (no FND-03 regression)
  </acceptance_criteria>
  <done>resyncTechniqueInstances + getNonDetachedInstanceCount implemented; all resync tests and the existing progress-identity tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → resyncTechniqueInstances | techniqueId is a DB-internal integer supplied by saveTechniqueGraph, not user input |
| SQL params → SQLite | all values bound via $1,$2 positional params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-01 | Tampering | resync DELETE/INSERT path | mitigate | Survivors matched by technique_step_id and UPDATEd by PK; DELETE only for steps absent from the technique — proven by the data-layer suite incl. teeth counter-case (prevents silent progress loss) |
| T-144-02 | Information disclosure | unit_recipe_step_progress integrity | mitigate | FK ON DELETE CASCADE (migration 028) cleans progress only after an explicit recipe_steps DELETE; no orphaned/dangling progress |
| T-144-03 | Tampering | detached instances | mitigate | Resync WHERE detached = 0 — detached instances (Phase 146) are never mutated; asserted by the multi-recipe test case |
| T-144-04 | Tampering | SQL injection on techniqueId | accept | Integer DB-internal id, parameterised; no user-supplied string reaches SQL |
</threat_model>

<verification>
- `pnpm check:version` green (migration 052 + lib.rs parity, LF-only)
- `pnpm test -- tests/data-layer/technique-resync.test.ts` — all seven cases green
- `pnpm test -- tests/data-layer/technique-progress-identity.test.ts` — no FND-03 regression
</verification>

<success_criteria>
- Migration 052 adds recipe_sections.technique_section_id + idx_recipe_steps_technique_step_id; 3-leg version gate passes
- resyncTechniqueInstances propagates structure to all non-detached instances on a single passed-in handle, preserving surviving recipe_step.id and progress
- All LINK-01 data-layer cases (reorder/add/remove/slot/multi-recipe/cross-section) plus the teeth counter-case pass
</success_criteria>

<output>
Create `.planning/phases/144-live-link-re-sync/144-01-SUMMARY.md` when done
</output>

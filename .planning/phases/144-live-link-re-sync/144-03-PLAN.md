---
phase: 144-live-link-re-sync
plan: 03
type: execute
wave: 3
depends_on: ["144-01", "144-02"]
files_modified:
  - src/features/techniques/TechniqueFormSheet.tsx
autonomous: false
requirements: [LINK-02, LINK-03]
user_setup: []

must_haves:
  truths:
    - "Saving a structural technique edit on a technique used by ≥1 non-detached recipe shows a confirmation dialog before any DB write"
    - "The dialog title shows 'Update X recipe(s)?' and the body shows the aggregated change summary ('adds N steps, removes M steps, reorders K') plus a note that step completion progress is preserved"
    - "Cancel aborts the entire save with zero DB writes; Confirm proceeds with the save + resync"
    - "A pure metadata edit (rename/notes) or a technique with zero non-detached instances saves directly with no dialog"
  artifacts:
    - path: "src/features/techniques/TechniqueFormSheet.tsx"
      provides: "confirmation-dialog intercept in onSubmit + executeSave helper"
      contains: "previewTechniqueResyncDiff"
  key_links:
    - from: "src/features/techniques/TechniqueFormSheet.tsx"
      to: "src/lib/techniquePreviewDiff.ts"
      via: "previewTechniqueResyncDiff(orderedSections, existingSections, existingSteps)"
      pattern: "previewTechniqueResyncDiff"
    - from: "src/features/techniques/TechniqueFormSheet.tsx"
      to: "src/db/queries/recipeTechniqueResync.ts"
      via: "getNonDetachedInstanceCount(technique.id)"
      pattern: "getNonDetachedInstanceCount"
---

<objective>
Add the affected-recipes confirmation dialog to the technique edit form: before committing a
structural change, intercept the submit, compute the pure preview diff, count affected recipes,
and show an honest change-summary dialog (LINK-02, LINK-03). Confirm proceeds with save + resync;
Cancel aborts with no writes. Metadata-only edits and zero-instance techniques save directly.

Purpose: LINK-02 ("X recipes will be affected" warning) + LINK-03 (change summary, not a bare count).
Output: confirmation intercept wired into TechniqueFormSheet, verified by a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-live-link-re-sync/144-PATTERNS.md
@.planning/phases/144-live-link-re-sync/144-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

From plan 144-02 — src/lib/techniquePreviewDiff.ts:
  export interface TechniqueResyncPreview { stepAdds; stepRemoves; stepReorders; sectionAdds; sectionRemoves; isStructural: boolean }
  export function previewTechniqueResyncDiff(draftSections, existingSections, existingSteps): TechniqueResyncPreview;

From plan 144-01 — src/db/queries/recipeTechniqueResync.ts:
  export async function getNonDetachedInstanceCount(techniqueId: number): Promise<number>;

From src/features/techniques/TechniqueFormSheet.tsx (current state):
  - line 110: const [sections, setSections] = useState<DraftTechniqueSection[]>([...])
  - line 113: const [slots, setSlots] = useState<DraftTechniqueSlot[]>([])
  - lines 116-117: const { data: existingSections = EMPTY } = useTechniqueSections(technique?.id); const { data: existingSteps = EMPTY } = useTechniqueSteps(technique?.id);
  - lines 198-250: onSubmit — validation (203-213), orderedSlots/orderedSections recompute (216-221), try/catch with updateTechnique/createTechnique.mutateAsync (223-249)
  - isEdit and technique are in scope

From src/features/techniques/TechniqueDeleteDialog.tsx (lines 1-69) — the canonical Dialog
open/onOpenChange/DialogContent/Header/Title/Description/Footer + Button(variant outline / default)
pattern to mirror for the confirmation dialog.
</interfaces>

@src/features/techniques/TechniqueFormSheet.tsx
@src/features/techniques/TechniqueDeleteDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirmation-dialog intercept in TechniqueFormSheet.onSubmit</name>
  <files>src/features/techniques/TechniqueFormSheet.tsx</files>
  <read_first>
    - src/features/techniques/TechniqueFormSheet.tsx lines 1-30 (imports — add useRef, Dialog primitives, previewTechniqueResyncDiff, getNonDetachedInstanceCount), lines 108-120 (state + existingSections/existingSteps hooks), lines 198-250 (the onSubmit to refactor), lines 252-end (the JSX return / Sheet — where the Dialog mounts)
    - src/features/techniques/TechniqueDeleteDialog.tsx lines 1-69 (Dialog structure + Button variants + onOpenChange close pattern)
    - .planning/phases/144-PATTERNS.md "TechniqueFormSheet.tsx MODIFICATION" (lines 239-357) — the exact state shape (confirmDialog useState + pendingSubmitRef useRef), the onSubmit intercept ordering, the executeSave extraction, and the Dialog JSX with the change-summary join logic
    - .planning/phases/144-RESEARCH.md Pattern 3 (lines 353-387) for the intercept-vs-mutate ordering
  </read_first>
  <action>
    Refactor `onSubmit` in TechniqueFormSheet. Add state `const [confirmDialog, setConfirmDialog] =
    useState<{ open: boolean; preview: TechniqueResyncPreview; count: number } | null>(null)` and
    `const pendingSubmitRef = useRef<{ values; orderedSlots; orderedSections } | null>(null)`. Extract
    the existing try/catch save block (current lines 223-249) into `async function executeSave(values,
    orderedSlots, orderedSections)`. In onSubmit, keep the existing validation and order_index
    recompute, then: if `isEdit && technique`, call `previewTechniqueResyncDiff(orderedSections,
    existingSections, existingSteps)`; if `preview.isStructural`, `await
    getNonDetachedInstanceCount(technique.id)` and if count > 0, store the pending values in
    pendingSubmitRef, `setConfirmDialog({ open: true, preview, count })`, and `return` (suspend before
    any write). Otherwise call `await executeSave(...)` directly (covers create path, metadata-only
    edits, and zero-instance techniques). Mount a confirmation `Dialog` (mirroring
    TechniqueDeleteDialog) inside the return: title `Update {count} recipe{count===1?"":"s"}?`; body
    aggregates non-zero counts into a sentence via a `[adds N steps, removes M steps, reorders K]
    .filter(Boolean).join(", ")` + the literal reassurance "Step completion progress is preserved.";
    Cancel button calls `setConfirmDialog(null)` (no write); the confirm button ("Update Recipes")
    clears the dialog, reads pendingSubmitRef, clears it, and `await executeSave(...)`. Pluralise
    "step"/"recipe" correctly. Do NOT change saveTechniqueGraph or the resync (already wired in 144-02).
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build 2>&1 | grep -iE "error" | head -10; echo "exit-marker"</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/techniques/TechniqueFormSheet.tsx` imports `previewTechniqueResyncDiff` and `getNonDetachedInstanceCount`
    - onSubmit calls `previewTechniqueResyncDiff` and, when `isStructural` && count > 0, sets confirmDialog and returns BEFORE any mutateAsync
    - An `executeSave` helper holds the create/update mutateAsync + toast + onClose logic; the create path and non-structural edits route through it directly (no dialog)
    - The Dialog body joins only non-zero change counts and includes the text "progress is preserved"
    - `pnpm build` reports no TypeScript errors (no `error` lines before exit-marker)
  </acceptance_criteria>
  <done>Structural edits on a used technique show the change-summary confirmation; Cancel writes nothing; Confirm runs save + resync; metadata/zero-instance edits save directly. Build is clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The affected-recipes confirmation dialog in the technique edit form (LINK-02/03). When you save a
    structural change (add/remove/reorder a step) to a technique that is applied in one or more
    recipes, a dialog now appears before the save commits, showing how many recipes are affected and
    an itemised change summary. Confirm propagates the change (resync) to every recipe; Cancel writes
    nothing. The data-layer resync correctness was already proven by automated tests in plan 01.
  </what-built>
  <how-to-verify>
    1. Run the app: `pnpm tauri dev`.
    2. Pick (or create) a technique and apply it to at least one recipe (Workshop → Recipes → a recipe
       → add technique). Mark one of its technique-sourced steps complete in that recipe.
    3. Open the technique in the technique library and edit its structure — add one step and reorder another.
    4. Save. EXPECT: a dialog titled "Update 1 recipe?" (or N) with a summary like "adds 1 step,
       reorders 1 step" and the line "Step completion progress is preserved."
    5. Click Cancel → reopen the affected recipe → EXPECT no structural change (the edit did not apply).
    6. Edit + save again, this time click "Update Recipes" → reopen the recipe → EXPECT the new step is
       present, the reordered step moved, and the previously-completed step is STILL marked complete.
    7. Now make a metadata-only edit (rename a step, change a note) and save → EXPECT NO dialog (direct save).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what differed (dialog wording, missing reassurance line, dialog appearing on metadata edit, lost completion, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form submit → confirmation gate | structural saves are intercepted before any DB write |
| confirm/cancel → executeSave | Cancel guarantees zero writes; Confirm is the only path to mutateAsync for structural edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-08 | Repudiation | silent propagation without warning | mitigate | LINK-02/03: structural edits on used techniques cannot save without the user seeing the affected-recipe count + change summary and explicitly confirming |
| T-144-09 | Tampering | partial write on Cancel | mitigate | onSubmit returns before any mutateAsync when the dialog opens; Cancel only calls setConfirmDialog(null) — no DB call reachable |
| T-144-10 | Denial of service | dialog fatigue on metadata edits | accept | isStructural excludes rename/notes so routine edits skip the dialog; human checkpoint confirms behaviour |
</threat_model>

<verification>
- `pnpm build` clean
- Human checkpoint confirms: dialog appears on structural edit with correct summary; Cancel is a no-op; Confirm propagates and preserves completion; metadata edit shows no dialog
- `pnpm test` full suite green (no regressions from the form change)
</verification>

<success_criteria>
- Structural technique edits on a used technique require an honest change-summary confirmation (LINK-02, LINK-03)
- Cancel writes nothing; Confirm saves + resyncs while preserving step completion
- Metadata-only / zero-instance edits save without a dialog
</success_criteria>

<output>
Create `.planning/phases/144-live-link-re-sync/144-03-SUMMARY.md` when done
</output>

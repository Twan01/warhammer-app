---
phase: 120-ui-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 120-01
files_modified:
  - src/features/game-day/StrategemsTab.tsx
  - src/features/rules-hub/RulesHubPage.tsx
  - src/features/army-lists/DetachmentPicker.tsx
  - src/features/army-lists/EnhancementPickerSheet.tsx
  - src/features/units/PlaybookTab.tsx
  - src/features/units/PlaybookDetachmentAbilities.tsx
autonomous: true
requirements:
  - STR-03
  - STR-04
  - ENH-02
  - ENH-03
  - DET-03
  - DET-04

must_haves:
  truths:
    - "Game Day page shows real faction stratagems grouped by battle phase when a detachment is selected"
    - "Rules Hub stratagems tab shows real stratagem data with working search and faction filter"
    - "Army list enhancement picker displays description and points cost from udb_enhancements"
    - "Enhancement points are resolved from the canonical database cost column without manual input"
    - "Army list detachment picker shows real detachment names from udb_detachments"
    - "PlaybookTab shows detachment abilities section for units with a canonical faction link"
  artifacts:
    - path: "src/features/game-day/StrategemsTab.tsx"
      provides: "Game Day stratagems with real data"
      contains: "useStratagemsByDetachment"
    - path: "src/features/rules-hub/RulesHubPage.tsx"
      provides: "Rules Hub with real stratagems and detachments"
      contains: "useStratagemsByFaction"
    - path: "src/features/army-lists/DetachmentPicker.tsx"
      provides: "Detachment picker with real data"
      contains: "useDetachmentsByFaction"
    - path: "src/features/army-lists/EnhancementPickerSheet.tsx"
      provides: "Enhancement picker with canonical data"
      contains: "useEnhancementsByDetachment"
    - path: "src/features/units/PlaybookDetachmentAbilities.tsx"
      provides: "Detachment abilities section for PlaybookTab"
      contains: "useDetachmentAbilities"
  key_links:
    - from: "src/features/game-day/StrategemsTab.tsx"
      to: "src/hooks/useGameData.ts"
      via: "import useStratagemsByDetachment"
      pattern: "import.*useStratagemsByDetachment.*useGameData"
    - from: "src/features/army-lists/EnhancementPickerSheet.tsx"
      to: "src/hooks/useGameData.ts"
      via: "import useEnhancementsByDetachment"
      pattern: "import.*useEnhancementsByDetachment.*useGameData"
    - from: "src/features/units/PlaybookTab.tsx"
      to: "src/features/units/PlaybookDetachmentAbilities.tsx"
      via: "component render"
      pattern: "PlaybookDetachmentAbilities"
---

<objective>
Wire all page-level components to real canonical data by replacing stub hooks with imports from useGameData.ts and migrating the enhancement picker from BSData to udb_enhancements.

Purpose: This is the final plan — after completion, all six requirements (STR-03, STR-04, ENH-02, ENH-03, DET-03, DET-04) are satisfied and users see real game data everywhere.

Output: 5 updated page components + 1 new PlaybookDetachmentAbilities component. All stubs eliminated.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-ui-wiring/120-CONTEXT.md
@.planning/phases/120-ui-wiring/120-RESEARCH.md
@.planning/phases/120-ui-wiring/120-PATTERNS.md
@.planning/phases/120-ui-wiring/120-UI-SPEC.md
@.planning/phases/120-ui-wiring/120-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From src/types/gameData.ts:
  export interface UdbStratagem { id: string; faction_id: string | null; detachment_id: string | null; name: string; type: string | null; cp_cost: number; turn: string | null; phase: string | null; description: string; }
  export interface UdbEnhancement { id: string; faction_id: string; detachment_id: string | null; name: string; cost: number; description: string; }
  export interface UdbDetachment { id: string; faction_id: string; name: string; }
  export interface UdbDetachmentAbility { id: string; detachment_id: string; faction_id: string; name: string; description: string | null; }
  export interface UdbDetachmentAbilityWithDetachment extends UdbDetachmentAbility { detachment_name: string; }

From src/hooks/useGameData.ts:
  export function useStratagemsByDetachment(detachmentId: string | undefined): UseQueryResult<UdbStratagem[]>;
  export function useStratagemsByFaction(factionId: string | undefined): UseQueryResult<UdbStratagem[]>;
  export function useEnhancementsByDetachment(detachmentId: string | undefined): UseQueryResult<UdbEnhancement[]>;
  export function useDetachmentsByFaction(factionId: string | undefined): UseQueryResult<UdbDetachment[]>;
  export function useDetachmentAbilities(factionId: string | null): UseQueryResult<UdbDetachmentAbilityWithDetachment[]>;

<!-- Existing types the components use -->
From src/types/armyList.ts:
  export interface ArmyList { id: number; faction_id: number | null; detachment_id: string | null; detachment_name: string | null; ... }
  export interface ArmyListUnitRow { id: number; unit_name: string; faction_id: number | null; udb_unit_id: string | null; ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire StrategemsTab, RulesHubPage, and DetachmentPicker to real data</name>
  <files>src/features/game-day/StrategemsTab.tsx, src/features/rules-hub/RulesHubPage.tsx, src/features/army-lists/DetachmentPicker.tsx</files>
  <read_first>
    src/features/game-day/StrategemsTab.tsx
    src/features/rules-hub/RulesHubPage.tsx
    src/features/army-lists/DetachmentPicker.tsx
    src/hooks/useGameData.ts
    src/types/gameData.ts
    src/features/rules-hub/applyRulesHubFilters.ts
  </read_first>
  <action>
    Replace stub hooks in three page components with real imports from @/hooks/useGameData (per D-04, D-07, D-13).

    **StrategemsTab.tsx:**
    - Delete the local stub function useStratagemsByDetachment (lines 10-14) and its comment
    - Add import: useStratagemsByDetachment from @/hooks/useGameData
    - Add import: UdbStratagem from @/types/gameData
    - Change the grouped useMemo Map type from Map<string, RwStratagem[]> to Map<string, UdbStratagem[]> (line 56)
    - Remove the import of RwStratagem from @/types/datasheet (line 19) — no longer used
    - The phase grouping logic in the useMemo reads s.phase which exists on UdbStratagem — no logic changes needed
    - The PHASE_ORDER matching logic reads s.phase as string — this works with UdbStratagem.phase which contains values like "Command phase", "Shooting phase". Note: UdbStratagem.phase values include the word "phase" (e.g. "Shooting phase"). The existing PHASE_ORDER array contains bare names ("Command", "Movement", etc.). The grouping check uses PHASE_ORDER.includes(s.phase as ...) which will NOT match "Shooting phase" against "Shooting". Fix by extracting the first word: map s.phase to its first word before comparing, or update the includes check to use s.phase?.split(" ")[0]. Alternatively, adjust the rendering to show the full phase name. The simplest fix: normalize incoming phase by stripping trailing " phase" suffix before grouping.

    **RulesHubPage.tsx:**
    - Delete the three stub functions (lines 20-28) and the import of RwDetachmentAbility, RwStratagem from @/types/datasheet (line 19)
    - Add imports: useStratagemsByFaction, useDetachmentsByFaction from @/hooks/useGameData
    - The useSharedAbilitiesByFaction stub must remain — shared abilities are NOT part of Phase 120 scope. Keep it as a local stub returning empty array but remove the RwDetachmentAbility type reference. Use a minimal inline type: { id: string; name: string; description: string | null; legend?: string | null }[]
    - Update the hook call sites (lines 54-56): replace the stub calls with imported hook calls for stratagems and detachments
    - The filteredDetachments useMemo (line 78-82) casts detachments as { name: string } — this works with UdbDetachment which has name: string
    - The DetachmentCard component receives detachment prop — this now correctly types as UdbDetachment (updated in Plan 01)
    - Add detachment filter dropdown for stratagems tab per D-05: a Select component below the faction selector that filters stratagems by detachment. Use useDetachmentsByFaction(selectedFactionId) to populate the options. Filter the stratagems list client-side by matching detachment_id before passing to applyStratagemFilters.

    **DetachmentPicker.tsx:**
    - Delete the local stub function useDetachmentsByFaction (lines 14-17)
    - Add import: useDetachmentsByFaction from @/hooks/useGameData
    - Update the emptyMessage: change "Sync rules from the Rules Hub to load detachments." to "Import unit database to load detachments." when !rulesSynced
    - No type changes needed — UdbDetachment has { id: string; name: string } which matches the existing usage
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - StrategemsTab.tsx has no local stub function, imports useStratagemsByDetachment from @/hooks/useGameData
    - StrategemsTab.tsx has no import of RwStratagem from @/types/datasheet
    - StrategemsTab.tsx handles "Shooting phase" -> "Shooting" normalization for PHASE_ORDER grouping
    - RulesHubPage.tsx imports useStratagemsByFaction and useDetachmentsByFaction from @/hooks/useGameData
    - RulesHubPage.tsx retains useSharedAbilitiesByFaction as local stub (out of scope)
    - RulesHubPage.tsx has no import of RwDetachmentAbility or RwStratagem from @/types/datasheet
    - RulesHubPage.tsx stratagems tab has a detachment filter Select dropdown per D-05
    - DetachmentPicker.tsx has no local stub, imports useDetachmentsByFaction from @/hooks/useGameData
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Game Day shows real stratagems grouped by phase. Rules Hub shows real stratagems with faction/detachment/search/phase/CP filters. DetachmentPicker shows real detachment names from canonical DB.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate EnhancementPickerSheet to udb_enhancements and add PlaybookTab detachment abilities</name>
  <files>src/features/army-lists/EnhancementPickerSheet.tsx, src/features/units/PlaybookTab.tsx, src/features/units/PlaybookDetachmentAbilities.tsx</files>
  <read_first>
    src/features/army-lists/EnhancementPickerSheet.tsx
    src/features/units/PlaybookTab.tsx
    src/hooks/useGameData.ts
    src/types/gameData.ts
    src/types/armyList.ts
    src/db/queries/bsdataExtended.ts
  </read_first>
  <action>
    Migrate the enhancement picker from BSData to canonical udb_enhancements (per D-08, D-09, D-10, D-11) and add detachment abilities to PlaybookTab (per D-14, D-15).

    **EnhancementPickerSheet.tsx:**
    - Remove imports: useQuery from @tanstack/react-query, getEnhancementsByFaction and SyncedEnhancementRow from @/db/queries/bsdataExtended
    - Add imports: useEnhancementsByDetachment from @/hooks/useGameData, UdbEnhancement from @/types/gameData
    - Remove the factionIdStr coercion block (lines 45-50) — no longer needed
    - Remove the useQuery for factionEnhancements (lines 53-58) — replaced by hook
    - Remove the detachmentEnhancements useMemo that filters by detachment_name (lines 67-72) — filtering now happens in SQL via detachment_id
    - Add the new hook call: const { data: detachmentEnhancements = [] } = useEnhancementsByDetachment(list?.detachment_id ?? undefined)
    - Update guard conditions throughout: replace list?.detachment_name checks with list?.detachment_id for data-flow guards (lines 68, 106, 113, 120). Keep list.detachment_name for display text in SheetDescription (line 91)
    - Update field references: enhancement.points -> enhancement.cost in Badge (line 161), and enhancement_points: enhancement.points -> enhancement_points: enhancement.cost in addEnhancement.mutate call (line 223)
    - Add description rendering per D-10: after the Badge row inside the enhancement card div, add a div with dangerouslySetInnerHTML for enhancement.description, className "text-xs text-muted-foreground [&_b]:font-semibold [&_.kwb]:text-foreground mt-1"
    - Update the points badge: show "Free" when enhancement.cost === 0, otherwise show cost + " pts"
    - Preserve all existing validation logic unchanged per D-11 (isMaxed, isDuplicate, isEpicHero checks)

    **PlaybookDetachmentAbilities.tsx** (NEW FILE):
    - Create a new component file per D-14, D-15
    - Import useDetachmentAbilities from @/hooks/useGameData
    - Import UdbDetachmentAbilityWithDetachment from @/types/gameData
    - Import Collapsible, CollapsibleTrigger, CollapsibleContent from @/components/ui/collapsible
    - Import ChevronDown from lucide-react
    - Import useMemo from react
    - Props: { factionId: string }
    - Call useDetachmentAbilities(factionId) to get all abilities for the faction
    - Group abilities by detachment_name using a Map in useMemo (per D-15: PlaybookTab always shows all detachments for faction)
    - Return null if isLoading or abilities.length === 0 (hide section when no data)
    - Render a Collapsible with defaultOpen={false}, trigger text "Detachment Abilities" with ChevronDown icon
    - Inside CollapsibleContent, iterate grouped entries: each detachment name as a text-xs font-semibold text-muted-foreground uppercase tracking-wider header, then each ability with name (text-sm font-semibold) and description via dangerouslySetInnerHTML guarded by && check (description is nullable per schema)
    - Description wrapper className: "text-sm text-muted-foreground [&_b]:font-semibold [&_.kwb]:text-foreground"

    **PlaybookTab.tsx:**
    - Add import: PlaybookDetachmentAbilities from @/features/units/PlaybookDetachmentAbilities
    - Insert the component after the PlaybookRules line (after line 234, before TierManager): {wahapediaFactionId && <PlaybookDetachmentAbilities factionId={wahapediaFactionId} />}
    - wahapediaFactionId is already available in PlaybookTab from the existing useWahapediaFactionId hook call
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - EnhancementPickerSheet.tsx has no import of getEnhancementsByFaction or SyncedEnhancementRow from bsdataExtended
    - EnhancementPickerSheet.tsx imports useEnhancementsByDetachment from @/hooks/useGameData
    - EnhancementPickerSheet.tsx uses list?.detachment_id (not list?.detachment_name) for data fetch guards
    - EnhancementPickerSheet.tsx references enhancement.cost (not enhancement.points) in Badge and mutate call
    - EnhancementPickerSheet.tsx renders enhancement.description via dangerouslySetInnerHTML
    - PlaybookDetachmentAbilities.tsx exists with useDetachmentAbilities hook call and grouped-by-detachment rendering
    - PlaybookTab.tsx renders PlaybookDetachmentAbilities when wahapediaFactionId is truthy
    - PlaybookDetachmentAbilities section is positioned after PlaybookRules and before TierManager
    - TypeScript compiles with no errors
    - pnpm build succeeds
  </acceptance_criteria>
  <done>Enhancement picker shows descriptions and points from canonical DB. PlaybookTab shows detachment abilities grouped by detachment name. All six requirements (STR-03, STR-04, ENH-02, ENH-03, DET-03, DET-04) are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> UI | Enhancement/ability descriptions rendered via dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-03 | Information Disclosure | Enhancement cost display | accept | Points data is public game information, no PII |
| T-120-04 | Tampering | EnhancementPickerSheet data source migration | mitigate | Parameterized queries via $1 positional params; validation logic preserved per D-11 |
</threat_model>

<verification>
- pnpm build succeeds with no TypeScript errors
- No stub hooks remain in StrategemsTab.tsx, RulesHubPage.tsx (except useSharedAbilitiesByFaction which is out of scope), DetachmentPicker.tsx, DetachmentCard.tsx
- No imports from bsdataExtended remain in EnhancementPickerSheet.tsx
- PlaybookDetachmentAbilities component renders in PlaybookTab
</verification>

<success_criteria>
- All six stub hooks replaced with real data hooks
- Enhancement picker migrated from BSData to canonical udb_enhancements
- PlaybookTab shows detachment abilities section
- pnpm build succeeds
- All phase 120 requirements (STR-03, STR-04, ENH-02, ENH-03, DET-03, DET-04) met
</success_criteria>

<output>
Create `.planning/phases/120-ui-wiring/120-02-SUMMARY.md` when done
</output>

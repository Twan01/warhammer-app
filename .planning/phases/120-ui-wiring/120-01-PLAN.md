---
phase: 120-ui-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/gameData.ts
  - src/db/queries/udbGameData.ts
  - src/hooks/useGameData.ts
  - src/features/rules-hub/applyRulesHubFilters.ts
  - src/features/rules-hub/StratagemCard.tsx
  - src/features/game-day/GameDayStratagemCard.tsx
  - src/features/rules-hub/DetachmentCard.tsx
autonomous: true
requirements:
  - STR-03
  - STR-04
  - ENH-02
  - ENH-03
  - DET-03
  - DET-04

must_haves:
  truths:
    - "TypeScript interfaces exist for UdbStratagem, UdbEnhancement, UdbDetachment, UdbDetachmentAbility, UdbDetachmentAbilityWithDetachment"
    - "Query functions exist for getStratagemsByDetachment, getStratagemsByFaction, getEnhancementsByDetachment, getDetachmentsByFaction, getDetachmentAbilitiesByFaction, getDetachmentAbilitiesByDetachment"
    - "React Query hooks exist for all six query functions with staleTime Infinity and disabled-when-null guards"
    - "applyStratagemFilters accepts UdbStratagem[] and coerces integer cp_cost for string comparison"
    - "StratagemCard and GameDayStratagemCard accept UdbStratagem props with number cp_cost"
    - "DetachmentCard accepts UdbDetachment props and fetches abilities from udb_detachment_abilities"
    - "All card components render HTML descriptions via dangerouslySetInnerHTML"
  artifacts:
    - path: "src/types/gameData.ts"
      provides: "TypeScript interfaces for game data entities"
      exports: ["UdbStratagem", "UdbEnhancement", "UdbDetachment", "UdbDetachmentAbility", "UdbDetachmentAbilityWithDetachment"]
    - path: "src/db/queries/udbGameData.ts"
      provides: "SQLite query functions for game data"
      exports: ["getStratagemsByDetachment", "getStratagemsByFaction", "getEnhancementsByDetachment", "getDetachmentsByFaction", "getDetachmentAbilitiesByFaction", "getDetachmentAbilitiesByDetachment"]
    - path: "src/hooks/useGameData.ts"
      provides: "React Query hooks for game data"
      exports: ["useStratagemsByDetachment", "useStratagemsByFaction", "useEnhancementsByDetachment", "useDetachmentsByFaction", "useDetachmentAbilities", "useDetachmentAbilitiesByDetachment"]
  key_links:
    - from: "src/hooks/useGameData.ts"
      to: "src/db/queries/udbGameData.ts"
      via: "import queryFn"
      pattern: "import.*from.*udbGameData"
    - from: "src/hooks/useGameData.ts"
      to: "src/types/gameData.ts"
      via: "import type"
      pattern: "import type.*from.*gameData"
---

<objective>
Create the complete data layer (types, queries, hooks) for game data entities and update all self-contained card/filter components to accept the new UdbStratagem/UdbDetachment types.

Purpose: Establishes the foundation that Plan 02 will wire into page-level components. Card components and the filter function are updated here because they are self-contained (no stub swaps needed) and would cause type errors if deferred.

Output: 3 new files (types, queries, hooks) + 4 updated components ready for consumption by Plan 02.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-ui-wiring/120-CONTEXT.md
@.planning/phases/120-ui-wiring/120-RESEARCH.md
@.planning/phases/120-ui-wiring/120-PATTERNS.md
@.planning/phases/120-ui-wiring/120-UI-SPEC.md

<interfaces>
<!-- Migration 042 schema (udb_detachments + udb_detachment_abilities) -->
From src-tauri/migrations/042_udb_detachments.sql:
  CREATE TABLE udb_detachments (
    id TEXT PRIMARY KEY,
    faction_id TEXT NOT NULL REFERENCES udb_factions(id),
    name TEXT NOT NULL
  );
  CREATE TABLE udb_detachment_abilities (
    id TEXT PRIMARY KEY,
    detachment_id TEXT NOT NULL REFERENCES udb_detachments(id),
    faction_id TEXT NOT NULL REFERENCES udb_factions(id),
    name TEXT NOT NULL,
    description TEXT
  );

<!-- Migration 043 schema (udb_stratagems + udb_enhancements) -->
From src-tauri/migrations/043_udb_stratagems_enhancements.sql:
  CREATE TABLE udb_stratagems (
    id TEXT PRIMARY KEY,
    faction_id TEXT,
    detachment_id TEXT REFERENCES udb_detachments(id),
    name TEXT NOT NULL,
    type TEXT,
    cp_cost INTEGER NOT NULL DEFAULT 0,
    turn TEXT,
    phase TEXT,
    description TEXT NOT NULL
  );
  CREATE TABLE udb_enhancements (
    id TEXT PRIMARY KEY,
    faction_id TEXT NOT NULL REFERENCES udb_factions(id),
    detachment_id TEXT REFERENCES udb_detachments(id),
    name TEXT NOT NULL,
    cost INTEGER NOT NULL DEFAULT 0,
    description TEXT NOT NULL
  );

<!-- Existing hook pattern -->
From src/hooks/useUnitDatabase.ts:
  export const UDB_FACTIONS_KEY = (locale: Locale) => ["udb-factions", locale] as const;
  export function useUdbFactions() { return useQuery({ queryKey: ..., queryFn: ..., staleTime: Infinity }); }
  export function useUdbUnits(factionId: string | null) { return useQuery({ ..., enabled: !!factionId, staleTime: Infinity }); }

<!-- Existing query pattern -->
From src/db/queries/unitDatabase.ts:
  import { getDb } from "@/db/client";
  export async function getUdbFactions(locale?: "en" | "fr"): Promise<UdbFaction[]> {
    const db = await getDb();
    return db.select<UdbFaction[]>("SELECT id, name, short_name FROM udb_factions ORDER BY name ASC");
  }

<!-- Old types being replaced -->
From src/types/datasheet.ts:
  export interface RwStratagem { id: string; name: string; legend: string | null; phase: string | null; cp_cost: string | null; description: string | null; ... }
  export interface RwDetachment { id: string; name: string; faction_id: string; }
  export interface RwDetachmentAbility { id: string; name: string; description: string | null; legend: string | null; detachment_id: string; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create types, query functions, and React Query hooks</name>
  <files>src/types/gameData.ts, src/db/queries/udbGameData.ts, src/hooks/useGameData.ts</files>
  <read_first>
    src/hooks/useUnitDatabase.ts
    src/db/queries/unitDatabase.ts
    src-tauri/migrations/042_udb_detachments.sql
    src-tauri/migrations/043_udb_stratagems_enhancements.sql
    src/db/client.ts
  </read_first>
  <action>
    Create three new files following exact established patterns (per D-16, D-17, D-18).

    **src/types/gameData.ts** — Define five interfaces matching SQLite column names exactly:
    - UdbStratagem: id (string), faction_id (string | null), detachment_id (string | null), name (string), type (string | null), cp_cost (number), turn (string | null), phase (string | null), description (string)
    - UdbEnhancement: id (string), faction_id (string), detachment_id (string | null), name (string), cost (number), description (string)
    - UdbDetachment: id (string), faction_id (string), name (string)
    - UdbDetachmentAbility: id (string), detachment_id (string), faction_id (string), name (string), description (string | null)
    - UdbDetachmentAbilityWithDetachment: extends UdbDetachmentAbility with detachment_name (string)

    **src/db/queries/udbGameData.ts** — Six async query functions using getDb() from @/db/client with $1 positional params:
    - getStratagemsByDetachment(detachmentId: string): SELECT from udb_stratagems WHERE detachment_id = $1 OR (faction_id IS NULL AND detachment_id IS NULL) ORDER BY phase, name — per D-02/D-04, includes universal stratagems
    - getStratagemsByFaction(factionId: string): SELECT from udb_stratagems WHERE faction_id = $1 OR (faction_id IS NULL AND detachment_id IS NULL) ORDER BY phase, name — per D-07
    - getEnhancementsByDetachment(detachmentId: string): SELECT from udb_enhancements WHERE detachment_id = $1 ORDER BY name — per D-09
    - getDetachmentsByFaction(factionId: string): SELECT from udb_detachments WHERE faction_id = $1 ORDER BY name — per D-12
    - getDetachmentAbilitiesByFaction(factionId: string): SELECT with JOIN on udb_detachments for detachment_name WHERE a.faction_id = $1 ORDER BY d.name, a.name — per D-14
    - getDetachmentAbilitiesByDetachment(detachmentId: string): SELECT from udb_detachment_abilities WHERE detachment_id = $1 ORDER BY name — for DetachmentCard per-card use

    **src/hooks/useGameData.ts** — Seven React Query hooks with staleTime: Infinity:
    - Query key factories: STRATAGEMS_BY_DETACHMENT_KEY, STRATAGEMS_BY_FACTION_KEY, ENHANCEMENTS_BY_DETACHMENT_KEY, DETACHMENTS_BY_FACTION_KEY, DETACHMENT_ABILITIES_KEY, DETACHMENT_ABILITIES_BY_DETACHMENT_KEY
    - useStratagemsByDetachment(detachmentId: string | undefined): enabled: !!detachmentId, disabled key uses ["udb-stratagems-detachment", "disabled"]
    - useStratagemsByFaction(factionId: string | undefined): enabled: !!factionId
    - useEnhancementsByDetachment(detachmentId: string | undefined): enabled: !!detachmentId
    - useDetachmentsByFaction(factionId: string | undefined): enabled: !!factionId
    - useDetachmentAbilities(factionId: string | null): enabled: !!factionId — for PlaybookTab
    - useDetachmentAbilitiesByDetachment(detachmentId: string): always enabled, no null guard — for DetachmentCard per-card
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/types/gameData.ts exports UdbStratagem with cp_cost: number (not string)
    - src/types/gameData.ts exports UdbEnhancement with cost: number (not points)
    - src/db/queries/udbGameData.ts imports getDb from "@/db/client" (not getRulesDb)
    - src/db/queries/udbGameData.ts getStratagemsByDetachment SQL contains "OR (faction_id IS NULL AND detachment_id IS NULL)"
    - src/hooks/useGameData.ts every hook uses staleTime: Infinity
    - src/hooks/useGameData.ts useStratagemsByDetachment has enabled: !!detachmentId guard
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Three new files exist, export all specified interfaces/functions/hooks, and TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Migrate card components and filter function to UdbStratagem/UdbDetachment types</name>
  <files>src/features/rules-hub/applyRulesHubFilters.ts, src/features/rules-hub/StratagemCard.tsx, src/features/game-day/GameDayStratagemCard.tsx, src/features/rules-hub/DetachmentCard.tsx</files>
  <read_first>
    src/features/rules-hub/applyRulesHubFilters.ts
    src/features/rules-hub/StratagemCard.tsx
    src/features/game-day/GameDayStratagemCard.tsx
    src/features/rules-hub/DetachmentCard.tsx
    src/types/gameData.ts
    src/hooks/useGameData.ts
  </read_first>
  <action>
    Update four files to consume UdbStratagem/UdbDetachment types instead of RwStratagem/RwDetachment (per D-03, D-06, D-19).

    **applyRulesHubFilters.ts:**
    - Change import from RwStratagem to UdbStratagem from @/types/gameData
    - Update function signature: applyStratagemFilters(stratagems: UdbStratagem[], options: StratagemFilterOptions): UdbStratagem[]
    - Fix cp_cost comparison (Pitfall 1): change `s.cp_cost === options.cpFilter` to `String(s.cp_cost) === options.cpFilter`
    - Fix search: replace `(s.legend ?? "").toLowerCase()` with `(s.type ?? "").toLowerCase()` since UdbStratagem has no legend field

    **StratagemCard.tsx:**
    - Change import from RwStratagem to UdbStratagem from @/types/gameData
    - Update StratagemCardProps interface: stratagem: UdbStratagem
    - Update cpLabel function signature from (cost: string | null) to (cost: number). Body: if (cost === 0) return "Free"; return cost + " CP"
    - Remove stratagem.legend rendering block (UdbStratagem has no legend field)
    - Change description rendering from plain text `<p>{stratagem.description}</p>` to dangerouslySetInnerHTML div with className "text-sm text-muted-foreground [&_b]:font-semibold [&_.kwb]:text-foreground"
    - Add turn badge after phase badge in CollapsibleTrigger: Badge variant="outline" showing stratagem.turn when truthy, className "shrink-0 border-transparent bg-muted text-muted-foreground text-xs"

    **GameDayStratagemCard.tsx:**
    - Change import from RwStratagem to UdbStratagem from @/types/gameData
    - Update GameDayStratagemCardProps interface: stratagem: UdbStratagem
    - Update cpLabel function same as StratagemCard
    - Replace parseInt line (line 39) with direct assignment: const cost = stratagem.cp_cost
    - Remove stratagem.legend rendering block
    - Change description rendering to dangerouslySetInnerHTML same as StratagemCard
    - Add turn badge after phase badge per D-03

    **DetachmentCard.tsx:**
    - Remove local stub function useDetachmentAbilitiesByDetachment (lines 9-12)
    - Add import: useDetachmentAbilitiesByDetachment from @/hooks/useGameData
    - Change import from RwDetachment, RwDetachmentAbility to UdbDetachment, UdbDetachmentAbility from @/types/gameData
    - Update DetachmentAbilityRowProps: ability: UdbDetachmentAbility
    - Update DetachmentCardProps: detachment: UdbDetachment
    - Remove ability.legend rendering (line 69-71) — UdbDetachmentAbility has no legend field
    - Change ability.description rendering from plain text to dangerouslySetInnerHTML div with className "text-muted-foreground mt-0.5 [&_b]:font-semibold [&_.kwb]:text-foreground"
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - applyRulesHubFilters.ts imports UdbStratagem (not RwStratagem)
    - applyRulesHubFilters.ts contains String(s.cp_cost) for the cpFilter comparison
    - StratagemCard.tsx prop type is UdbStratagem, cpLabel accepts number
    - GameDayStratagemCard.tsx prop type is UdbStratagem, no parseInt call for cp_cost
    - GameDayStratagemCard.tsx and StratagemCard.tsx both render stratagem.turn as a Badge
    - DetachmentCard.tsx has no local stub function, imports useDetachmentAbilitiesByDetachment from @/hooks/useGameData
    - All four files use dangerouslySetInnerHTML for description rendering
    - No references to RwStratagem, RwDetachment, or RwDetachmentAbility remain in these four files
    - TypeScript compiles with no errors (expect temporary errors in StrategemsTab.tsx, RulesHubPage.tsx — resolved in Plan 02)
  </acceptance_criteria>
  <done>All four component/filter files consume new UdbStratagem/UdbDetachment types with correct cp_cost handling, HTML rendering, and turn badges</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> UI | Data from hobbyforge.db rendered via dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Information Disclosure | dangerouslySetInnerHTML in card components | accept | Data originates from controlled Wahapedia CSV import pipeline, not user input. No PII. Consistent with existing codebase pattern. |
| T-120-02 | Tampering | SQL queries in udbGameData.ts | mitigate | All queries use $1/$2 positional parameterized syntax via Tauri plugin-sql — no string interpolation |
</threat_model>

<verification>
TypeScript compiles cleanly. All new files export the specified interfaces, functions, and hooks. Card components accept new types and render HTML descriptions.
</verification>

<success_criteria>
- 3 new files created (types, queries, hooks) with all specified exports
- 4 existing files updated to consume UdbStratagem/UdbDetachment types
- No RwStratagem/RwDetachment imports remain in updated files
- cp_cost handled as number throughout
- dangerouslySetInnerHTML used for all description rendering
- TypeScript compiles (may have transient errors in files updated in Plan 02)
</success_criteria>

<output>
Create `.planning/phases/120-ui-wiring/120-01-SUMMARY.md` when done
</output>

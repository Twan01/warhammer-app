---
phase: 19-analytics-core
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/analytics/computeHobbyAnalytics.test.ts
  - tests/analytics/analyticsQueries.test.ts
  - tests/analytics/useHobbyAnalytics.test.ts
autonomous: true
requirements: [ANLY-04, ANLY-05, ANLY-06, ANLY-07]

must_haves:
  truths:
    - "tests/analytics/ directory exists with three .test.ts stub files"
    - "All test stubs use it.skip — full vitest suite is GREEN with stubs deferred"
    - "Stubs name every ANLY-04..07 requirement they will eventually verify"
    - "Plan 01 can flip stubs to active in one pass without writing new it() blocks"
  artifacts:
    - path: "tests/analytics/computeHobbyAnalytics.test.ts"
      provides: "ANLY-04 (velocity), ANLY-05 (streak), ANLY-06 (monthlyData) — pure aggregation stubs"
      contains: "describe(\"computeHobbyAnalytics"
    - path: "tests/analytics/analyticsQueries.test.ts"
      provides: "ANLY-07 — getAnalyticsData SQL contract (NULL purchase_date excluded)"
      contains: "describe(\"analytics queries"
    - path: "tests/analytics/useHobbyAnalytics.test.ts"
      provides: "ANLY-04/05 — HOBBY_ANALYTICS_KEY cache key contract"
      contains: "HOBBY_ANALYTICS_KEY"
  key_links:
    - from: "tests/analytics/computeHobbyAnalytics.test.ts"
      to: "src/features/dashboard/computeHobbyAnalytics.ts"
      via: "import statements added by Plan 01"
      pattern: "from \"@/features/dashboard/computeHobbyAnalytics\""
    - from: "tests/analytics/analyticsQueries.test.ts"
      to: "src/db/queries/analytics.ts"
      via: "vi.mock(@/db/client) — wired by Plan 01"
      pattern: "from \"@/db/queries/analytics\""
    - from: "tests/analytics/useHobbyAnalytics.test.ts"
      to: "src/hooks/useHobbyAnalytics.ts"
      via: "import statement added by Plan 01"
      pattern: "from \"@/hooks/useHobbyAnalytics\""
---

<objective>
Wave 0: Create failing test stubs for the entire Analytics Core phase.

Purpose: Establish the test contract before implementation. Every ANLY-04..07 requirement is named in a stub `it.skip` block, so Plan 01 can flip them to active and watch them go green. Mirrors the Wave-0 pattern used by Phases 14, 15, and 18.

Output: Three test files with `it.skip` blocks (full suite stays GREEN), each block names exactly which requirement and behaviour it will verify when activated.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-analytics-core/19-CONTEXT.md
@.planning/phases/19-analytics-core/19-RESEARCH.md
@.planning/phases/19-analytics-core/19-VALIDATION.md
@tests/battle-log/computeBattleLogSummary.test.ts
@tests/battle-log/battleLogQueries.test.ts
@tests/spending/useSpendingStats.test.ts

<interfaces>
<!-- Stubs reference symbols that Plan 01 will create. Stubs use it.skip with empty bodies. -->
<!-- Top-level imports are OMITTED (replaced by TODO comments) so TypeScript does not fail on missing modules — pattern from tests/battle-log/* Wave 0. -->

Symbols Plan 01 will export from `src/features/dashboard/computeHobbyAnalytics.ts`:
- computeHobbyAnalytics(sessions, rawMonthlySpend): HobbyAnalytics
- interface HobbyAnalytics { velocityString: string; streakString: string; monthlyData: { month: string; pence: number }[] }

Symbols Plan 01 will export from `src/db/queries/analytics.ts`:
- getAnalyticsData(): Promise<RawAnalyticsData>
- interface RawAnalyticsData { sessions: { unit_id: number; session_date: string }[]; monthlySpend: { month: string; pence: number }[] }

Symbols Plan 01 will export from `src/hooks/useHobbyAnalytics.ts`:
- HOBBY_ANALYTICS_KEY = ["hobby-analytics"] as const
- useHobbyAnalytics(): UseQueryResult<HobbyAnalytics, Error>

Test mock pattern (from tests/foundation/armyListQueries.test.ts):
```typescript
const selectMock = vi.fn();
const executeMock = vi.fn();
vi.mock("@/db/client", () => ({
  getDb: async () => ({ select: selectMock, execute: executeMock }),
}));
```

Cache-key contract pattern (from tests/spending/useSpendingStats.test.ts):
```typescript
import { describe, it, expect } from "vitest";
import { SPENDING_STATS_KEY } from "@/hooks/useSpendingStats";
describe("...", () => {
  it("equals ['spending-stats'] literal", () => {
    expect(SPENDING_STATS_KEY).toEqual(["spending-stats"]);
  });
});
```

NOTE: Wave 0 stubs use `it.skip(...)` — top-level imports of not-yet-existing modules are OMITTED. Replace with TODO comments naming the symbols. Wave 1 (Plan 01) will add the imports and flip .skip → active.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create computeHobbyAnalytics.test.ts stub (ANLY-04, ANLY-05, ANLY-06)</name>
  <files>tests/analytics/computeHobbyAnalytics.test.ts</files>
  <read_first>
    - tests/battle-log/computeBattleLogSummary.test.ts (Wave 0 stub structure — describe naming, it.skip with empty body, top-level import omission pattern)
    - tests/spending/computeSpendingStats.test.ts (pure function fixture conventions)
    - .planning/phases/19-analytics-core/19-RESEARCH.md §"computeHobbyAnalytics Pure Function" (target shape: HobbyAnalytics + 3 fields)
    - .planning/phases/19-analytics-core/19-RESEARCH.md §"Pitfall 2: Velocity Division by Zero" (single-day floor edge case)
    - .planning/phases/19-analytics-core/19-RESEARCH.md §"Pitfall 6: X-Axis Label Crossing Calendar Year Boundary" (label format expectations)
    - .planning/phases/19-analytics-core/19-VALIDATION.md §"Wave 0 Requirements" (target test list)
  </read_first>
  <action>
    Create `tests/analytics/computeHobbyAnalytics.test.ts` (mkdir tests/analytics if missing).

    Use this EXACT file body. Wave 0 stubs only — empty `() => {}` bodies for every `it.skip`.

    ```typescript
    /**
     * Phase 19 — computeHobbyAnalytics pure-function tests.
     *
     * Wave 0: stubs only (it.skip). Plan 01 flips these to active by removing
     * .skip and adding the actual assertions once src/features/dashboard/
     * computeHobbyAnalytics.ts is created.
     *
     * Pure function — no mocks needed. Mirrors tests/spending/computeSpendingStats.test.ts.
     *
     * Covers:
     *   ANLY-04 — velocity: distinct unit_ids ÷ months between first/last session date,
     *             "0.0" when empty, single-day session floors to 1 month (Pitfall 2).
     *   ANLY-05 — streak: consecutive calendar days ending today via dates.ts,
     *             "0 days" when empty.
     *   ANLY-06 — monthlyData: 12 entries always, gaps filled with pence=0,
     *             month labels use "Jan" abbreviation or "Jan '25" when crossing year (Pitfall 6).
     */
    import { describe, it } from "vitest";
    // TODO Plan 01: replace this comment with:
    //   import { expect, beforeEach, afterEach, vi } from "vitest";
    //   import { computeHobbyAnalytics } from "@/features/dashboard/computeHobbyAnalytics";

    describe("computeHobbyAnalytics — ANLY-04 velocity (units/month average)", () => {
      it.skip("returns velocityString='0.0' when sessions array is empty", () => {});
      it.skip("returns velocityString='1.0' when one session exists (single-day floor: months=1, units=1)", () => {});
      it.skip("returns velocityString='3.0' when 6 distinct units have sessions across 2 months (6 ÷ 2 = 3.0)", () => {});
      it.skip("returns velocityString='3.1' with 1 decimal place (Math: 31 ÷ 10 = 3.1, never integer-rounded to '3')", () => {});
      it.skip("does NOT return 'Infinity' when all sessions occurred on the same date (Pitfall 2: monthsDiff=0 floored to 1)", () => {});
      it.skip("counts DISTINCT unit_ids (3 sessions all on unit 1 contribute 1 to numerator, not 3)", () => {});
    });

    describe("computeHobbyAnalytics — ANLY-05 streak (consecutive calendar days)", () => {
      it.skip("returns streakString='0 days' when sessions array is empty", () => {});
      it.skip("returns streakString='1 days' when there is exactly one session dated today", () => {});
      it.skip("returns streakString='3 days' when sessions exist on today, today-1, today-2 (no gaps)", () => {});
      it.skip("returns streakString='0 days' when most recent session is yesterday but not today (streak does not include today)", () => {});
      it.skip("stops counting at first calendar-day gap (sessions today + today-2 → streak=1, not 2)", () => {});
      it.skip("uses dates.ts (todayISO + parseLocalDate) — never raw new Date().toISOString() (Pitfall 5: timezone safety)", () => {});
    });

    describe("computeHobbyAnalytics — ANLY-06 monthlyData (12-month rolling window)", () => {
      it.skip("always returns exactly 12 entries (length === 12) regardless of input rawMonthlySpend length", () => {});
      it.skip("fills missing months with pence=0 — input [] produces 12 zero entries (Pitfall 3: SQL gaps padded in JS)", () => {});
      it.skip("merges SQL rows by YYYY-MM key — input [{month:'2026-03',pence:5500}] surfaces 5500 in the matching slot", () => {});
      it.skip("month labels use 3-character abbreviation when window stays in current year (e.g. 'Jan', 'Feb', 'Mar')", () => {});
      it.skip("month labels use Jan '25 style suffix for months belonging to a year before the current year (Pitfall 6)", () => {});
      it.skip("entries are ordered oldest-first (entry 0 = 11 months ago, entry 11 = current month)", () => {});
    });
    ```

    Total: 18 it.skip stubs. Do NOT write test bodies — Plan 01 fills them in.
  </action>
  <verify>
    <automated>pnpm vitest run tests/analytics/computeHobbyAnalytics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/analytics/computeHobbyAnalytics.test.ts
    - File contains the literal string `describe("computeHobbyAnalytics — ANLY-04 velocity (units/month average)"`
    - File contains the literal string `describe("computeHobbyAnalytics — ANLY-05 streak (consecutive calendar days)"`
    - File contains the literal string `describe("computeHobbyAnalytics — ANLY-06 monthlyData (12-month rolling window)"`
    - File contains the literal string `does NOT return 'Infinity'` (Pitfall 2 named in stub)
    - File contains the literal string `Pitfall 5: timezone safety` (Pitfall 5 named in stub)
    - File contains the literal string `Pitfall 6` (year boundary named in stub)
    - File contains exactly 18 occurrences of `it.skip(`
    - File contains zero occurrences of `it(` (active, non-skip)
    - File contains zero occurrences of `expect(` (assertions deferred to Plan 01)
    - File contains the literal TODO marker `TODO Plan 01: replace this comment`
    - `pnpm vitest run tests/analytics/computeHobbyAnalytics.test.ts` exits 0 with all 18 tests reported as skipped
  </acceptance_criteria>
  <done>Stub file exists with 18 it.skip blocks naming every ANLY-04, ANLY-05, ANLY-06 contract; vitest reports the file as 18 skipped + 0 failed.</done>
</task>

<task type="auto">
  <name>Task 2: Create analyticsQueries.test.ts stub (ANLY-07)</name>
  <files>tests/analytics/analyticsQueries.test.ts</files>
  <read_first>
    - tests/foundation/armyListQueries.test.ts (vi.mock(@/db/client) pattern — Plan 01 will reference this for the active mock setup)
    - tests/battle-log/battleLogQueries.test.ts (Wave 0 SQL-contract stub structure)
    - .planning/phases/19-analytics-core/19-RESEARCH.md §"SQL: Monthly Spend Bucketing (UNION, NULL excluded)" (target SQL string components Plan 01 will assert against)
    - .planning/phases/19-analytics-core/19-RESEARCH.md §"Pitfall 4: purchase_date on Units vs Paints Schema Difference" (rationale)
  </read_first>
  <action>
    Create `tests/analytics/analyticsQueries.test.ts`.

    Use this EXACT file body. Wave 0 stubs only — empty `() => {}` bodies.

    ```typescript
    /**
     * Phase 19 — analytics query module SQL contract tests.
     *
     * Wave 0: stubs only (it.skip). Plan 01 flips these to active by removing
     * .skip and adding the vi.mock setup once src/db/queries/analytics.ts exists.
     *
     * Mocks getDb() because tauri-plugin-sql IPC cannot run in jsdom.
     * Mirrors tests/foundation/armyListQueries.test.ts.
     *
     * Covers:
     *   ANLY-07 — getAnalyticsData monthly spend SQL must filter
     *             "WHERE purchase_date IS NOT NULL" for BOTH units AND paints in the
     *             UNION ALL subquery — NULL purchase_date rows must NOT be bucketed
     *             to 1970-01 epoch.
     *   ANLY-04/05 — getAnalyticsData also returns sessions = DISTINCT (unit_id, session_date)
     *                ORDER BY session_date ASC for compute layer to derive velocity + streak.
     */
    import { describe, it } from "vitest";
    // TODO Plan 01: replace this comment with the active mock setup:
    //   import { vi, expect, beforeEach } from "vitest";
    //   const selectMock = vi.fn();
    //   const executeMock = vi.fn();
    //   vi.mock("@/db/client", () => ({
    //     getDb: async () => ({ select: selectMock, execute: executeMock }),
    //   }));
    //   import { getAnalyticsData } from "@/db/queries/analytics";
    //   beforeEach(() => { selectMock.mockReset(); executeMock.mockReset(); });

    describe("analytics queries — getAnalyticsData (ANLY-04/05/06/07)", () => {
      it.skip("calls db.select twice — once for sessions, once for monthlySpend (Promise.all parallel)", () => {});
      it.skip("sessions query selects DISTINCT unit_id, session_date FROM painting_sessions ORDER BY session_date ASC", () => {});
      it.skip("monthlySpend query uses UNION ALL combining units and paints with purchase_price_pence + purchase_date", () => {});
      it.skip("monthlySpend query filters 'WHERE purchase_date IS NOT NULL' for the units source (ANLY-07)", () => {});
      it.skip("monthlySpend query filters 'WHERE purchase_date IS NOT NULL' for the paints source (ANLY-07)", () => {});
      it.skip("monthlySpend query also filters 'AND purchase_price_pence IS NOT NULL' for both sources (avoid NULL pence in SUM)", () => {});
      it.skip("monthlySpend query restricts the rolling window via WHERE month >= strftime('%Y-%m', date('now', '-11 months'))", () => {});
      it.skip("monthlySpend query GROUPs BY month and ORDERs BY month ASC (matches compute-layer expectation)", () => {});
      it.skip("returns shape { sessions: {...}[], monthlySpend: {...}[] } so compute function can consume it directly", () => {});
    });
    ```

    Total: 9 it.skip stubs. Do NOT write test bodies.
  </action>
  <verify>
    <automated>pnpm vitest run tests/analytics/analyticsQueries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/analytics/analyticsQueries.test.ts
    - File contains the literal string `describe("analytics queries — getAnalyticsData (ANLY-04/05/06/07)"`
    - File contains the literal string `'WHERE purchase_date IS NOT NULL' for the units source (ANLY-07)`
    - File contains the literal string `'WHERE purchase_date IS NOT NULL' for the paints source (ANLY-07)`
    - File contains the literal string `UNION ALL` (named in describe text or stub label)
    - File contains the literal string `strftime('%Y-%m'` (rolling window contract)
    - File contains exactly 9 occurrences of `it.skip(`
    - File contains zero occurrences of `it(` (non-skip)
    - File contains zero occurrences of `expect(`
    - `pnpm vitest run tests/analytics/analyticsQueries.test.ts` exits 0 with all 9 tests reported as skipped
  </acceptance_criteria>
  <done>Stub file exists with 9 it.skip blocks naming every ANLY-07 SQL contract requirement (NULL purchase_date exclusion for both sources); vitest reports the file as 9 skipped + 0 failed.</done>
</task>

<task type="auto">
  <name>Task 3: Create useHobbyAnalytics.test.ts stub (HOBBY_ANALYTICS_KEY contract)</name>
  <files>tests/analytics/useHobbyAnalytics.test.ts</files>
  <read_first>
    - tests/spending/useSpendingStats.test.ts (cache-key contract test pattern — exact equality assertion structure to mirror)
    - .planning/STATE.md §Architecture constraints (confirms `["hobby-analytics"]` cache key documented as architecture constraint)
    - .planning/phases/19-analytics-core/19-RESEARCH.md §"Hook: useHobbyAnalytics" (target export const HOBBY_ANALYTICS_KEY)
  </read_first>
  <action>
    Create `tests/analytics/useHobbyAnalytics.test.ts`.

    Use this EXACT file body. Wave 0 stubs only.

    ```typescript
    /**
     * Phase 19 — HOBBY_ANALYTICS_KEY cache-key contract test.
     *
     * Wave 0: stub only (it.skip). Plan 01 flips this to active by removing
     * .skip and importing HOBBY_ANALYTICS_KEY once src/hooks/useHobbyAnalytics.ts
     * is created.
     *
     * The Dashboard HOBBY HEALTH section (Plan 02) and SpendingPage Monthly Trend
     * chart (Plan 02) BOTH consume this key. Forward-compat: future session +
     * spend mutations may invalidate ['hobby-analytics']. If a future refactor
     * renames this key, the hook contract test fails — alerting the developer.
     *
     * Mirrors tests/spending/useSpendingStats.test.ts exactly.
     */
    import { describe, it } from "vitest";
    // TODO Plan 01: replace this comment with:
    //   import { expect } from "vitest";
    //   import { HOBBY_ANALYTICS_KEY } from "@/hooks/useHobbyAnalytics";

    describe("ANLY-04/05/06/07 HOBBY_ANALYTICS_KEY contract", () => {
      it.skip("equals ['hobby-analytics'] literal — must match any future session/spend invalidation key", () => {});
      it.skip("is a single-element readonly tuple (not a deeper key path)", () => {});
    });
    ```

    Total: 2 it.skip stubs. Do NOT write test bodies.
  </action>
  <verify>
    <automated>pnpm vitest run tests/analytics/useHobbyAnalytics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/analytics/useHobbyAnalytics.test.ts
    - File contains the literal string `describe("ANLY-04/05/06/07 HOBBY_ANALYTICS_KEY contract"`
    - File contains the literal string `equals ['hobby-analytics'] literal`
    - File contains exactly 2 occurrences of `it.skip(`
    - File contains zero occurrences of `it(` (non-skip)
    - File contains zero occurrences of `expect(`
    - `pnpm vitest run tests/analytics/useHobbyAnalytics.test.ts` exits 0 with both tests reported as skipped
  </acceptance_criteria>
  <done>Stub file exists with 2 it.skip blocks; vitest reports the file as 2 skipped + 0 failed.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `pnpm vitest run tests/analytics/` exits 0 with 29 skipped + 0 passed + 0 failed (18 + 9 + 2 = 29)
2. `pnpm vitest run` (full suite) exits 0 — no regressions
3. `tests/analytics/` directory contains exactly 3 files
4. `pnpm tsc --noEmit` exits 0 (no TypeScript errors — top-level imports omitted, only describe/it imported)
</verification>

<success_criteria>
- 29 it.skip stubs total across 3 files
- Every ANLY-04..07 requirement is named in at least one stub describe/it block
- Pitfalls 2, 5, 6 (the three named in 19-RESEARCH.md affecting compute logic) are explicitly named in stub text
- Full vitest suite is GREEN (no fails introduced)
- Plan 01 can immediately flip stubs to active in a single pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-analytics-core/19-00-SUMMARY.md` capturing:
- Stub count per file (18 + 9 + 2 = 29)
- Requirement coverage map (ANLY-04..07 → stubs)
- Pitfall coverage (Pitfall 2 / 5 / 6 named in stub text)
- Activation checklist for Plan 01: "flip stubs in this order → 1) computeHobbyAnalytics (uses pure-function fixtures), 2) analyticsQueries (uses vi.mock), 3) useHobbyAnalytics (single import + equality)"
</output>

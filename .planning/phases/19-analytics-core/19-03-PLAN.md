---
phase: 19-analytics-core
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified: []
autonomous: false
requirements: [ANLY-04, ANLY-05, ANLY-06, ANLY-07]

must_haves:
  truths:
    - "All 4 ANLY-04..07 success criteria verified by hand in the live Tauri app"
    - "User confirms HOBBY HEALTH section + Monthly Trend chart match UI-SPEC contract"
    - "User confirms cache invalidation works — logging a session updates Dashboard velocity/streak; editing a purchase_date updates chart"
    - "User confirms no regressions on /, /spending, /collection, /paints, /battle-log"
  artifacts: []
  key_links: []
---

<objective>
Wave 3: Manual smoke-test checkpoint. Plans 00-02 are fully automated and unit-tested, but the only end-to-end verification of ANLY-04..07 against a real SQLite database, real Recharts SVG rendering, real Tauri window, and real cache invalidation is human verification in the running app.

Purpose: Catch any IPC, animation, layout, currency-formatting, year-boundary, or invalidation regressions that unit tests cannot reach (jsdom does not render real SVG; vitest does not run Tauri IPC).

Output: User confirmation that the phase is shippable, or a written list of issues to gap-close in a follow-up plan.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-analytics-core/19-VALIDATION.md
@.planning/phases/19-analytics-core/19-UI-SPEC.md
@.planning/phases/19-analytics-core/19-CONTEXT.md
@.planning/phases/19-analytics-core/19-RESEARCH.md
@.planning/phases/19-analytics-core/19-00-SUMMARY.md
@.planning/phases/19-analytics-core/19-01-SUMMARY.md
@.planning/phases/19-analytics-core/19-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Smoke-test the Analytics Core feature in the live Tauri app</name>
  <read_first>
    - .planning/phases/19-analytics-core/19-VALIDATION.md §"Manual-Only Verifications" (5 manual checks the validation plan flagged as un-jsdom-able)
    - .planning/phases/19-analytics-core/19-UI-SPEC.md §"Copywriting Contract" (exact strings to expect: "Hobby Health" label, "Hobby Velocity · units/month" StatCard label, "Painting Streak" StatCard label, "Monthly Trend" h2, "Add purchase dates to units and paints to see trends" muted note)
    - .planning/phases/19-analytics-core/19-CONTEXT.md §"Hobby Velocity display" + §"Painting Streak display" (locked: 0.0 / 0 days fallback, 1-decimal velocity)
    - .planning/phases/19-analytics-core/19-RESEARCH.md §"Pitfall 6" (Year-boundary label format check — "Jan '25" style for prior years)
  </read_first>
  <action>
    Claude will start the Tauri dev server, then pause for the user to walk through every step below. The user types "approved" if all pass, or describes failures.

    Pre-flight (Claude runs):
    1. Run `pnpm tauri dev` in the project root (background). Wait until the window opens.
    2. Inform the user the app is running and ready for verification.

    User then performs these 12 steps in the running app.
  </action>
  <what-built>
    Phase 19 produced:
    - Dashboard HOBBY HEALTH section between PROGRESS and BY FACTION rows
    - Two new StatCards: Hobby Velocity (e.g. "3.1" with "Hobby Velocity · units/month" label) and Painting Streak (e.g. "14 days" with "Painting Streak" label) — both animate=false (string values)
    - SpendingPage Monthly Trend section between hero card and Breakdown table (only in !isEmpty branch)
    - SpendTrendChart component: Recharts BarChart inside shadcn ChartContainer, 12 bars rolling 12-month window, Y-axis + tooltip via formatCurrency
    - Zero-state muted note "Add purchase dates to units and paints to see trends" below chart when all 12 bars are zero
    - Bar fill = `var(--color-pence)` mapping to `hsl(var(--chart-1))` — stable across faction theme changes
    - Cache invalidation chain: 2 painting-session mutations + 3 unit mutations + 3 paint mutations all invalidate ['hobby-analytics']
    - shadcn chart.tsx component installed (with recharts + react-is ^19.0.0 pnpm override)
  </what-built>
  <how-to-verify>
    Walk through every step. The expected outcome is in parentheses.

    **Step 1 — Dashboard HOBBY HEALTH section visible (ANLY-04, ANLY-05)**
    1. Click Dashboard in the sidebar (or the HobbyForge wordmark) to navigate to /.
       (Expected: page loads.)
    2. Scroll the Dashboard down so all sections are visible.
       (Expected: section order from top to bottom is — top stat row [Total Models, Fully Painted, Battle-Ready Points, Active Projects] → PROGRESS [Painting %, Assembly %, Basing %] → **HOBBY HEALTH** [Hobby Velocity · units/month, Painting Streak] → BY FACTION → Lists section.)
    3. Verify the HOBBY HEALTH section label.
       (Expected: uppercase "HOBBY HEALTH" in muted-foreground color, same typographic weight + tracking as "PROGRESS" and "BY FACTION".)
    4. Verify there are exactly 2 StatCards in the HOBBY HEALTH grid (grid-cols-2 — full width, larger cards than PROGRESS row).
       (Expected: Hobby Velocity card on the left, Painting Streak on the right. Both same height as PROGRESS StatCards (h-24).)

    **Step 2 — Empty fallback state (no journal sessions)**
    Pre-condition: confirm the app has zero painting_sessions rows (fresh DB or all sessions deleted).
    - Hobby Velocity card displays exactly `0.0` as the big number with `Hobby Velocity · units/month` as the label.
    - Painting Streak card displays exactly `0 days` as the big number with `Painting Streak` as the label.
    - Neither card animates (text appears immediately, no count-up).

    **Step 3 — Log a session today (ANLY-04, ANLY-05 reactivity)**
    1. Click Collection in the sidebar.
    2. Click any unit row to open UnitDetailSheet.
    3. Switch to the Journal tab.
    4. Log a session: Date = today (default), Duration = 60 minutes, Notes = "test phase 19".
    5. Click "Log session" (or the equivalent button).
       (Expected: session row appears in the list newest-first.)
    6. Click Dashboard in the sidebar.
       (Expected: HOBBY HEALTH cards have updated WITHOUT needing a manual refresh:
        - Hobby Velocity now shows `1.0` (1 distinct unit ÷ 1-month floor = 1.0).
        - Painting Streak now shows `1 days`.
        — This proves the cache invalidation patch from Plan 02 Task 4 is working.)

    **Step 4 — Streak with consecutive days (ANLY-05)**
    Optional — only if the user can fake yesterday's session via DevTools or wants to simulate via the SQL CLI. Skip if not feasible. The automated tests verify multi-day streaks; this manual check is a sanity sample only.

    **Step 5 — Velocity with multiple units (ANLY-04)**
    1. From Dashboard, navigate back to Collection. Open a SECOND unit (different from the one in Step 3).
    2. Switch to its Journal tab and log another session today (Duration = 30 min).
    3. Return to Dashboard.
       (Expected: Hobby Velocity now shows `2.0` — 2 distinct unit_ids ÷ 1-month floor = 2.0. Painting Streak still shows `1 days` (today only — both sessions on same date).)

    **Step 6 — SpendingPage chart visible (ANLY-06)**
    Pre-condition: at least one unit OR paint has both `purchase_date` and `purchase_price_pence` set (otherwise SpendingPage shows the empty state — no chart).
    1. If needed: open any unit in Collection, set its `purchase_date` to a date within the last 12 months and `purchase_price_pence` to e.g. `1500` (£15.00). Save.
    2. Click Spending in the sidebar.
       (Expected: page renders. Section order from top to bottom — page header → hero "Total Hobby Spend" card (with the faction-accent ring) → **Monthly Trend** section → Breakdown table.)
    3. Verify the Monthly Trend section.
       (Expected: `Monthly Trend` heading in `text-base font-semibold` matching the Breakdown heading style. Below it: a bar chart roughly 240px tall, with 12 bars stacked along the X-axis. Y-axis ticks formatted as currency (e.g. £0, £25.00, £50.00 — depending on data). The bar where you set the date has visible height matching its pence value; other bars are zero-height (just the X-axis line).)
    4. Hover the populated bar.
       (Expected: tooltip appears showing the month label (e.g. "May") + the spend amount formatted as currency (e.g. "£15.00") — NEVER as raw pence (1500).)

    **Step 7 — Chart bar color is stable across factions (ANLY-06 Pitfall: anti-pattern check)**
    1. Click any faction card on the Dashboard to set it as active (e.g. Tau Empire).
       (Expected: faction-accent color flips throughout the app — sidebar highlights, hero ring on SpendingPage if visible, etc.)
    2. Navigate back to /spending.
       (Expected: bar fill color does NOT change with the faction theme. The bars remain the chart-1 token color (a neutral muted blue/teal). This proves the chart uses `var(--color-chart-1)` and NOT `var(--faction-accent)` per UI-SPEC anti-pattern.)
    3. Reset the active faction (click the same faction card again to deactivate).

    **Step 8 — Zero-state muted note (ANLY-07 + UI-SPEC)**
    Pre-condition: no unit AND no paint has BOTH purchase_date AND purchase_price_pence set, OR all such entries fall outside the last 12 months. If your test DB has populated purchase data, simulate by editing all such units/paints to clear their purchase_date.
    1. With NO purchase data in the last 12 months, navigate to /spending.
       (Expected if isEmpty=false but chart is empty: the chart still renders with 12 zero-height bars + a centered muted note BELOW the chart reading exactly "Add purchase dates to units and paints to see trends". This text MUST appear verbatim per UI-SPEC.)
       (Expected if isEmpty=true: the spending empty-state is shown — Receipt icon + "No spend logged yet" — and the chart is NOT rendered. This is the correct behaviour.)
    2. Restore your test purchase data (if changed).

    **Step 9 — NULL purchase_date excluded from chart (ANLY-07)**
    1. Open a unit in Collection. Set its `purchase_price_pence` to `2500` (£25.00) but LEAVE its `purchase_date` blank/null. Save.
    2. Navigate to /spending.
       (Expected: the unit you just edited does NOT add a bar for any month. No 1970-01 / "Jan '70" bar appears. The chart remains stable. This proves the SQL filter `WHERE purchase_date IS NOT NULL` is enforced on the units source.)
    3. Repeat for a paint: open Paints, edit a paint, set purchase_price_pence but leave purchase_date null.
       (Expected: same — no bar added for the unset paint. ANLY-07 verified for the paints source.)

    **Step 10 — Year-boundary label disambiguation (Pitfall 6)**
    Conditional — only if today's date is in a month where the 12-month rolling window crosses a calendar year (this is true any time UNLESS today is December — December's window is Jan..Dec of the same year).
    1. Inspect the X-axis labels on the chart.
       (Expected: months belonging to the prior year carry a year suffix like "Jan '25" while months belonging to the current year are bare "Jan", "Feb", etc. There MUST NOT be two identical labels (e.g. two unsuffixed "Jan"s) on the same chart.)
    2. If today is December, you should see Jan..Dec all bare (no suffix). The Pitfall 6 fix is unverifiable manually until next year — skip this check.

    **Step 11 — Spend chart loading skeleton independence (Pitfall 7)**
    1. Navigate away from Spending (e.g. click Dashboard, wait 1 second).
    2. Click Spending again.
       (Expected: hero card and Breakdown render at full speed — they are powered by useSpendingStats. The Monthly Trend chart slot may show a brief Skeleton (h-60) BEFORE the chart renders — this skeleton MUST appear in-place where the chart will be, and MUST NOT cover the entire page or block the hero / Breakdown from showing.)

    **Step 12 — Cross-page regression check**
    - Click Dashboard in sidebar: page loads, HOBBY HEALTH still visible, no console errors.
    - Click Collection: filters work, gallery toggle works.
    - Click Paints: list and filters work.
    - Click Recipes: list works.
    - Click Painting Projects: kanban renders.
    - Click Army Lists: page loads.
    - Click Battle Log: page loads.
    - Click Spending: page loads, Monthly Trend visible (or empty state).
    - Open browser/Tauri DevTools console: no React errors, no Recharts warnings about peer deps, no "react-is" type errors.
  </how-to-verify>
  <resume-signal>
    Type "approved" if every expected outcome above held. Otherwise list the failed step numbers + a one-sentence description of what went wrong, and Claude will route to gap closure (`/gsd:plan-phase 19 --gaps`).
  </resume-signal>
  <acceptance_criteria>
    - User explicitly types "approved" OR provides a list of failures
    - If failures exist, they are recorded verbatim in 19-03-SUMMARY.md
    - If approved, .planning/STATE.md is updated to mark Phase 19 complete and to advance current_phase to 20
    - Phase 19 ROADMAP.md status flips to Complete after this plan
  </acceptance_criteria>
  <done>User has approved the manual smoke test or has provided a written failure list for gap closure.</done>
</task>

</tasks>

<verification>
On user "approved" signal:
- Phase 19 success criteria are all met (ANLY-04..07 verified)
- Full vitest suite green (Plans 00-02 already enforce this)
- Production build succeeds (Plan 02 enforces)
- Manual end-to-end verified
</verification>

<success_criteria>
- All 12 verification steps pass per UI-SPEC contract
- No console errors, no Recharts peer-dep warnings, no React-is type errors
- Cache invalidation works — Dashboard reactively updates when sessions are logged
- Chart bar color stable across faction changes (anti-pattern not present)
- ANLY-07 NULL purchase_date exclusion verified manually for both units AND paints
- Phase 19 ROADMAP.md status flips to Complete after this plan
</success_criteria>

<output>
After user approval, create `.planning/phases/19-analytics-core/19-03-SUMMARY.md` capturing:
- Approval timestamp + outcome
- Any issues found during verification (if not approved, list them for gap-closure planning)
- Confirmation that Phase 19 ANLY-04..07 success criteria are all verified end-to-end
- Note for Phase 20: Wishlist phase depends on Phase 17 (not 19) — Phase 19 has no downstream blocker
</output>

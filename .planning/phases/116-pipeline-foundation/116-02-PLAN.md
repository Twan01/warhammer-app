---
phase: 116-pipeline-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-unit-db.ts
  - scripts/update-unit-database.ts
autonomous: true
requirements: [PF-03, PF-04]

must_haves:
  truths:
    - "Units with legend=1 or legend=true in Datasheets.csv are excluded from validUnitIds before any downstream parsing"
    - "Build summary logs how many Legends units were excluded"
    - "After Legends filtering, remaining name+faction duplicates produce a console.warn (not silent discard)"
    - "Both build-unit-db.ts and update-unit-database.ts apply identical Legends filter and dedup logic"
    - "Sub-faction assignment via BSData loop is unchanged in Phase 116"
  artifacts:
    - path: "scripts/build-unit-db.ts"
      provides: "Legends filter + dedup in Step 3"
      contains: "legendsSkipped"
    - path: "scripts/update-unit-database.ts"
      provides: "Mirror Legends filter + dedup in Step 3"
      contains: "legendsSkipped"
  key_links:
    - from: "scripts/build-unit-db.ts"
      to: "scripts/update-unit-database.ts"
      via: "identical Step 3 legend filter + dedup logic"
      pattern: "isLegend"
---

<objective>
Add Legends unit filtering and name+faction deduplication to both build scripts.

Purpose: Wahapedia exports contain Legends (deprecated) units that pollute the canonical database. Filtering them at Step 3 (before validUnitIds population) automatically excludes them from all downstream parsing steps (models, weapons, abilities, keywords). After filtering, remaining duplicates are warned about rather than silently discarded (D-06).

Output: Both build-unit-db.ts and update-unit-database.ts filter Legends units and warn on duplicates during Step 3.
</objective>

<execution_context>
@.planning/phases/116-pipeline-foundation/116-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-pipeline-foundation/116-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Legends filter and dedup to build-unit-db.ts</name>
  <files>scripts/build-unit-db.ts</files>
  <read_first>
    - scripts/build-unit-db.ts (lines 195-225 — Step 3 unit parse loop with validUnitIds.add and units.push; lines 380-385 — BSData sub_faction assignment to confirm it stays untouched)
    - scripts/lib/parseCsv.ts (confirm parseWahapediaCsv returns Record<string, string>[] — legend column accessed as row["legend"])
  </read_first>
  <action>
    Modify Step 3 unit parse loop in scripts/build-unit-db.ts (around lines 197-220) per D-05 and D-06:

    PF-03 (Legends filter): Declare let legendsSkipped = 0 before the for loop. Inside the loop, after extracting id/factionId/name and before the factionIds check, add legend detection: const isLegend = row["legend"] === "1" || row["legend"] === "true". If isLegend, increment legendsSkipped and continue (skip adding to validUnitIds and units array). This ensures all downstream steps (models, weapons, abilities, keywords) automatically skip Legends units.

    Update the Step 3 summary log from "Parsed N units" to include legends count: use template literal showing units.length and legendsSkipped.

    PF-04 (dedup with warning): After the unit parse loop ends (after the for block), add a dedup pass. Create a Map keyed by name.toLowerCase() + ":" + faction_id. Iterate units array: if key already exists in map, emit console.warn with format matching existing warning pattern (2-space indent, WARNING prefix, unit name, faction_id, discarded id). If key is new, add to map and push to deduped array. Replace the units array reference with the deduped result for all subsequent code. Log dedup count if any duplicates were found.

    DO NOT modify: the BSData sub_faction assignment at lines 382-384 (stays for Phase 116 per research recommendation). DO NOT modify: the REQUIRED_CSVs array (stays at 6 entries per Pitfall 5).
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/build-unit-db.ts 2>&1 | findstr /C:"units" /C:"Legends" /C:"Parsed"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/build-unit-db.ts contains const isLegend = row["legend"] === "1" || row["legend"] === "true"
    - scripts/build-unit-db.ts contains legendsSkipped variable declaration and increment
    - scripts/build-unit-db.ts contains a dedup pass after the unit parse loop using a Map keyed by name+faction
    - scripts/build-unit-db.ts console.warn calls for duplicates include "WARNING" prefix and unit name
    - scripts/build-unit-db.ts Step 3 log includes legendsSkipped count
    - REQUIRED_CSVs array still contains exactly 6 entries (unchanged)
    - BSData sub_faction assignment block (lines ~382-384) is unchanged
    - pnpm build:udb completes without error (build pipeline still works)
  </acceptance_criteria>
  <done>build-unit-db.ts filters Legends units in Step 3, warns on remaining name+faction duplicates, and produces a clean unit list for downstream steps</done>
</task>

<task type="auto">
  <name>Task 2: Mirror Legends filter and dedup to update-unit-database.ts</name>
  <files>scripts/update-unit-database.ts</files>
  <read_first>
    - scripts/update-unit-database.ts (lines 120-155 — Step 3 unit parse loop, same shape as build-unit-db.ts; lines 242-250 — BSData sub_faction assignment to confirm it stays untouched)
    - scripts/build-unit-db.ts (the modified version from Task 1 — use its Step 3 as the reference implementation for identical changes)
  </read_first>
  <action>
    Apply identical PF-03/PF-04 changes to scripts/update-unit-database.ts Step 3 loop (around lines 122-153) as were applied to build-unit-db.ts in Task 1. Per Pitfall 4 from RESEARCH.md, these two scripts share lib functions but each has its own copy of the Step 3 loop — both must be updated.

    PF-03: Add legendsSkipped counter, isLegend check using row["legend"] === "1" || row["legend"] === "true", skip+increment on legend units.

    PF-04: Add dedup pass after the loop using Map keyed by name.toLowerCase() + ":" + faction_id. Warn on duplicates with same console.warn format as build-unit-db.ts. Replace units array with deduped result.

    Update Step 3 summary log to include legendsSkipped count.

    DO NOT modify: BSData sub_faction assignment at lines ~244-249. DO NOT modify: any REQUIRED_CSVs or file check logic.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/update-unit-database.ts 2>&1 | findstr /C:"units" /C:"Legends" /C:"Parsed"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/update-unit-database.ts contains const isLegend = row["legend"] === "1" || row["legend"] === "true"
    - scripts/update-unit-database.ts contains legendsSkipped variable and dedup Map logic
    - scripts/update-unit-database.ts dedup warning format matches build-unit-db.ts (2-space indent, WARNING prefix)
    - BSData sub_faction assignment block is unchanged
    - node --experimental-strip-types scripts/update-unit-database.ts completes without error
  </acceptance_criteria>
  <done>update-unit-database.ts has identical Legends filter and dedup logic as build-unit-db.ts; both scripts produce consistent output</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wahapedia CSV data -> build output | CSV field values (legend column) drive filtering logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-03 | Tampering | legend column in CSV | accept | Dev-side build script; if legend field is malformed, filter is a no-op (units pass through unfiltered — safe default); log line shows legendsSkipped=0 as indicator |
| T-116-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
- pnpm build:udb completes without error
- Build output log includes Legends exclusion count
- Both build scripts contain identical legend filter and dedup logic
- REQUIRED_CSVs array unchanged (6 entries) in both scripts
</verification>

<success_criteria>
- Legends units are excluded from the canonical database when fresh CSVs with legend=1 are present
- Name+faction duplicates after Legends filter produce console warnings
- Both build-unit-db.ts and update-unit-database.ts are in sync with identical Step 3 changes
- Sub-faction assignment via BSData is preserved unchanged
</success_criteria>

<output>
Create `.planning/phases/116-pipeline-foundation/116-02-SUMMARY.md` when done
</output>

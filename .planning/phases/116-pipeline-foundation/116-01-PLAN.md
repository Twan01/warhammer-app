---
phase: 116-pipeline-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/parseCsv.ts
  - tests/build-pipeline/parseCsv.test.ts
  - scripts/download-wahapedia.ts
  - package.json
autonomous: true
requirements: [PF-01, PF-02]

must_haves:
  truths:
    - "parseWahapediaCsv strips UTF-8 BOM from CSV input before parsing headers"
    - "pnpm download:wahapedia fetches all 10 required CSVs from wahapedia.ru to scripts/data/"
    - "Download script skips existing files unless --force is passed"
    - "Download script does not silently write empty files on HTTP failure"
  artifacts:
    - path: "scripts/lib/parseCsv.ts"
      provides: "BOM-safe CSV parser"
      contains: "replace(/^\\uFEFF/, '')"
    - path: "scripts/download-wahapedia.ts"
      provides: "Wahapedia CSV auto-download"
      contains: "wahapedia.ru/wh40k10ed/"
    - path: "tests/build-pipeline/parseCsv.test.ts"
      provides: "BOM strip test coverage"
      contains: "PF-01"
  key_links:
    - from: "package.json"
      to: "scripts/download-wahapedia.ts"
      via: "download:wahapedia script entry"
      pattern: "download:wahapedia"
---

<objective>
Fix UTF-8 BOM handling in the CSV parser and create the Wahapedia auto-download command.

Purpose: BOM corruption silently breaks header parsing for new CSV files, and developers currently must manually download and place CSVs. These two fixes are prerequisites for all subsequent pipeline work.

Output: BOM-safe parseWahapediaCsv function, new scripts/download-wahapedia.ts, pnpm download:wahapedia command, and test coverage for BOM behavior.
</objective>

<execution_context>
@.planning/phases/116-pipeline-foundation/116-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-pipeline-foundation/116-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BOM fix in parseWahapediaCsv with test-first approach</name>
  <files>scripts/lib/parseCsv.ts, tests/build-pipeline/parseCsv.test.ts</files>
  <read_first>
    - scripts/lib/parseCsv.ts (full source — 21 lines; see current parseWahapediaCsv signature and trim/split logic)
    - tests/build-pipeline/parseCsv.test.ts (existing test structure, describe block name, import path)
  </read_first>
  <behavior>
    - Test 1: BOM-prefixed CSV string "﻿id|name|\nSM|Space Marines|\n" produces records where key is "id" not "﻿id" (PF-01)
    - Test 2: Non-BOM CSV string "id|name|\nSM|Space Marines|\n" still produces correct records with key "id" (regression guard)
    - Test 3: Empty string with BOM "﻿" returns empty array (edge case)
  </behavior>
  <action>
    RED: Add three test cases to tests/build-pipeline/parseCsv.test.ts inside a new describe block "BOM handling (PF-01)". Import parseWahapediaCsv from ../../scripts/lib/parseCsv.ts. Run tests — expect failures on Test 1 and Test 3 (BOM in key name).

    GREEN: In scripts/lib/parseCsv.ts, add `const cleaned = raw.replace(/^﻿/, "");` as the first line of parseWahapediaCsv, before the existing `const lines = raw.trim().split("\n")`. Change `raw.trim()` to `cleaned.trim()`. Per D-01, this is the centralized fix covering all CSV callers.

    Run tests — all three must pass.
  </action>
  <verify>
    <automated>pnpm test -- tests/build-pipeline/parseCsv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/lib/parseCsv.ts contains `raw.replace(/^﻿/, "")` before the trim/split line
    - tests/build-pipeline/parseCsv.test.ts contains a describe block with "PF-01" in its name
    - tests/build-pipeline/parseCsv.test.ts contains at least 3 test cases for BOM handling
    - pnpm test -- tests/build-pipeline/parseCsv.test.ts exits 0
  </acceptance_criteria>
  <done>BOM-prefixed CSV input produces records with clean header keys; test suite passes with 3 new BOM test cases</done>
</task>

<task type="auto">
  <name>Task 2: Create download-wahapedia script and package.json command</name>
  <files>scripts/download-wahapedia.ts, package.json</files>
  <read_first>
    - package.json (current scripts section — find "build:udb" entry pattern for node --experimental-strip-types invocation)
    - scripts/build-unit-db.ts (lines 24-26 and 49-52 — import pattern for node:fs, node:path, __dirname setup)
  </read_first>
  <action>
    Create scripts/download-wahapedia.ts per D-02, D-03, D-04:
    - Import writeFileSync, existsSync from node:fs; join, dirname from node:path; fileURLToPath from node:url
    - Set __dirname via fileURLToPath(import.meta.url), DATA_DIR via join(__dirname, "data"), BASE_URL as "https://wahapedia.ru/wh40k10ed/"
    - Define CSV_FILES array with all 10 filenames from D-02: Factions.csv, Datasheets.csv, Datasheets_models.csv, Datasheets_abilities.csv, Datasheets_keywords.csv, Datasheets_wargear.csv, Datasheets_models_cost.csv, Stratagems.csv, Enhancements.csv, Detachment_abilities.csv
    - Parse --force flag from process.argv
    - Loop over CSV_FILES: skip if file exists and no --force, otherwise fetch with User-Agent header "HobbyForge-UDB-Builder/1.0", check res.ok before reading body (throw on HTTP error per Pitfall 6), write Buffer.from(await res.arrayBuffer()) to dest
    - Log summary: banner line "=== Wahapedia CSV Download ===" at start, per-file status (skip/download/size), final count at end
    - Top-level await is fine — Node 24 supports it natively

    Add to package.json scripts section:
    "download:wahapedia": "node --experimental-strip-types scripts/download-wahapedia.ts"
    Place it adjacent to the existing "build:udb" entry.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/download-wahapedia.ts --help 2>&1 || echo "script loads without syntax error"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/download-wahapedia.ts exists and contains CSV_FILES array with exactly 10 entries
    - scripts/download-wahapedia.ts contains "wahapedia.ru/wh40k10ed/" as BASE_URL
    - scripts/download-wahapedia.ts contains res.ok check before res.arrayBuffer()
    - scripts/download-wahapedia.ts contains User-Agent header in fetch call
    - scripts/download-wahapedia.ts contains --force flag parsing from process.argv
    - package.json contains "download:wahapedia" script entry using node --experimental-strip-types
    - node --experimental-strip-types scripts/download-wahapedia.ts parses without syntax errors (may fail on network but script loads)
  </acceptance_criteria>
  <done>pnpm download:wahapedia command exists and the script successfully loads; fetches all 10 CSVs when network is available</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wahapedia.ru -> local filesystem | Downloaded CSV data crosses from external domain to local build artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Tampering | scripts/download-wahapedia.ts | accept | CSVs are data-only (not executed); existing build validation (unit count >= 100) provides sanity check; dev-side only script |
| T-116-02 | Denial of Service | scripts/download-wahapedia.ts | accept | Dev-side script; if wahapedia.ru is down, developer uses manual CSV placement (existing workflow) |
| T-116-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
- pnpm test -- tests/build-pipeline/parseCsv.test.ts exits 0 (BOM tests pass)
- scripts/download-wahapedia.ts loads without syntax errors
- package.json contains download:wahapedia script entry
</verification>

<success_criteria>
- BOM-prefixed CSV input produces clean header keys in parseWahapediaCsv
- pnpm download:wahapedia command is registered and script is syntactically valid
- Download script handles --force flag, HTTP errors, and 10 CSV files
</success_criteria>

<output>
Create `.planning/phases/116-pipeline-foundation/116-01-SUMMARY.md` when done
</output>

---
phase: 100-query-layer-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/037_override_flags.sql
  - src/types/recipeSection.ts
  - src/types/unit.ts
  - src/db/queries/units.ts
  - src/features/recipes/AssignmentChecklist.tsx
autonomous: true
requirements:
  - SAD-03
  - SAD-04
must_haves:
  truths:
    - "Override columns exist in units table after migration 037 runs"
    - "SECTION_TYPES contains assembly, basing, and varnish values"
    - "Unit TypeScript interface includes the three override fields"
    - "updateUnit() accepts and persists override flag values"
    - "Toggling assembly checkbox in AssignmentChecklist sets status_assembly_override = 1"
  artifacts:
    - path: "src-tauri/migrations/037_override_flags.sql"
      provides: "Three override columns on units table"
      contains: "status_assembly_override"
    - path: "src/types/recipeSection.ts"
      provides: "Extended SECTION_TYPES with assembly, basing, varnish"
      contains: "assembly"
    - path: "src/types/unit.ts"
      provides: "Unit interface with override fields"
      contains: "status_assembly_override"
    - path: "src/db/queries/units.ts"
      provides: "updateUnit with override params at $24-$26"
      contains: "status_assembly_override"
    - path: "src/features/recipes/AssignmentChecklist.tsx"
      provides: "Override flag write on manual assembly toggle"
      contains: "status_assembly_override"
  key_links:
    - from: "src/features/recipes/AssignmentChecklist.tsx"
      to: "src/db/queries/units.ts"
      via: "updateUnit mutation with override flag"
      pattern: "status_assembly_override.*1"
---

<objective>
Create the schema foundation and type contracts for Phase 100 automation: migration for override columns, SECTION_TYPES vocabulary extension, Unit interface update, updateUnit() param extension, and AssignmentChecklist override wiring.

Purpose: Establishes the database schema, TypeScript types, and UI override wiring that Plan 02's automation logic depends on. Without override columns, auto-derivation cannot be safely shipped (Pitfall 1).

Output: Migration file, updated types, extended updateUnit(), and AssignmentChecklist wired to set override flags on manual assembly toggle.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-query-layer-automation/100-CONTEXT.md
@.planning/phases/100-query-layer-automation/100-RESEARCH.md
@.planning/phases/100-query-layer-automation/100-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/unit.ts:
- Unit interface has fields: status_assembly (0|1), status_basing (0|1), status_varnished (0|1), is_active_project (0|1)
- CreateUnitInput = Omit<Unit, "id" | "created_at" | "updated_at">
- UpdateUnitInput = Partial<CreateUnitInput> & { id: number }

From src/types/recipeSection.ts:
- SECTION_TYPES currently has 7 values: "prep", "basecoat", "shade", "layer", "detail", "effect", "finishing"
- SectionType = typeof SECTION_TYPES[number]

From src/db/queries/units.ts:
- updateUnit(input: UpdateUnitInput): Promise<void>
- SQL uses positional params $1-$23, COALESCE pattern for partial updates
- Last param is $23 (undercoat)

From src/features/recipes/AssignmentChecklist.tsx:
- handleToggleAssembly(checked: boolean) calls updateUnit.mutate({ id: unitId, status_assembly: checked ? 1 : 0 })
- This is the ONLY manual toggle for status_assembly in the codebase
- status_basing and status_varnished have NO manual toggle UI currently
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + Types Foundation</name>
  <files>
    src-tauri/migrations/037_override_flags.sql,
    src/types/recipeSection.ts,
    src/types/unit.ts
  </files>
  <read_first>
    src-tauri/migrations/008_enrichment.sql,
    src/types/recipeSection.ts,
    src/types/unit.ts
  </read_first>
  <action>
    Create migration file `src-tauri/migrations/037_override_flags.sql` per D-01: three ALTER TABLE ADD COLUMN statements adding `status_assembly_override INTEGER NOT NULL DEFAULT 0`, `status_basing_override INTEGER NOT NULL DEFAULT 0`, `status_varnished_override INTEGER NOT NULL DEFAULT 0` to the units table. Follow the header comment pattern from 008_enrichment.sql.

    Extend `SECTION_TYPES` in `src/types/recipeSection.ts` per D-04: append `"assembly"`, `"basing"`, `"varnish"` to the const array (10 total values). The `SectionType` union type auto-widens via `typeof SECTION_TYPES[number]`. D-05: keep `"finishing"` as-is; do NOT repurpose it.

    Add three fields to the `Unit` interface in `src/types/unit.ts` after `status_varnished`: `status_assembly_override: 0 | 1`, `status_basing_override: 0 | 1`, `status_varnished_override: 0 | 1`. Add a comment referencing migration 037 and SAD-04. Because `CreateUnitInput` and `UpdateUnitInput` are derived via Omit/Partial, the new fields propagate automatically. SQLite DEFAULT 0 handles new rows without explicit INSERT values.

    Before extending SECTION_TYPES, grep for exhaustive switch/case on SectionType to confirm none exist (research says dropdown-only usage).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - File `src-tauri/migrations/037_override_flags.sql` exists with exactly 3 ALTER TABLE statements
    - `SECTION_TYPES` array in `src/types/recipeSection.ts` has exactly 10 elements and includes "assembly", "basing", "varnish"
    - `Unit` interface in `src/types/unit.ts` includes `status_assembly_override: 0 | 1`, `status_basing_override: 0 | 1`, `status_varnished_override: 0 | 1`
    - `pnpm build` passes with no type errors
  </acceptance_criteria>
  <done>Migration file created, SECTION_TYPES extended to 10 values, Unit interface includes 3 override fields, TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Extend updateUnit() and Wire AssignmentChecklist Override</name>
  <files>
    src/db/queries/units.ts,
    src/features/recipes/AssignmentChecklist.tsx
  </files>
  <read_first>
    src/db/queries/units.ts,
    src/features/recipes/AssignmentChecklist.tsx,
    src/types/unit.ts
  </read_first>
  <action>
    Extend `updateUnit()` in `src/db/queries/units.ts` per D-02/Pitfall 6: add three new positional params `$24`, `$25`, `$26` to the UPDATE SQL for `status_assembly_override`, `status_basing_override`, `status_varnished_override`. Use the existing COALESCE pattern so callers that do not pass override flags leave them unchanged. Add the three corresponding entries to the params array after `input.undercoat ?? null`: `input.status_assembly_override ?? null`, `input.status_basing_override ?? null`, `input.status_varnished_override ?? null`.

    Wire override flag in `src/features/recipes/AssignmentChecklist.tsx` per D-02: in `handleToggleAssembly`, change the mutation call from `{ id: unitId, status_assembly: checked ? 1 : 0 }` to `{ id: unitId, status_assembly: checked ? 1 : 0, status_assembly_override: 1 }`. This marks the status as user-controlled so that `syncDerivedStatuses()` (Plan 02) will skip auto-derivation for this unit's assembly status.

    Note: `status_basing` and `status_varnished` have no manual toggle UI currently. The override columns exist and are wired in `updateUnit()` for future use, but no UI component needs changes for those two fields today.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `updateUnit()` SQL in `src/db/queries/units.ts` contains `status_assembly_override = COALESCE($24, status_assembly_override)`, `status_basing_override = COALESCE($25, status_basing_override)`, `status_varnished_override = COALESCE($26, status_varnished_override)`
    - Params array in `updateUnit()` has exactly 26 entries (was 23)
    - `handleToggleAssembly` in AssignmentChecklist.tsx passes `status_assembly_override: 1` in the mutation payload
    - `pnpm build` passes with no type errors
  </acceptance_criteria>
  <done>updateUnit() accepts 3 override params at $24-$26, AssignmentChecklist sets status_assembly_override=1 on manual toggle, TypeScript compiles clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Query Layer | Boolean values from checkbox toggles cross into SQL params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Tampering | updateUnit params | mitigate | All values pass through COALESCE with positional $N params -- no SQL injection vector; boolean values are 0 or 1 integers |
| T-100-02 | Information Disclosure | override columns | accept | Override flags are non-sensitive boolean metadata with no PII |
</threat_model>

<verification>
- `pnpm build` passes (TypeScript strict mode catches type mismatches)
- Migration file exists at correct path with correct filename ordering (037)
- SECTION_TYPES has 10 values including assembly, basing, varnish
- Unit interface includes 3 override fields
- updateUnit SQL has 26 positional params
- AssignmentChecklist passes override flag on assembly toggle
</verification>

<success_criteria>
Schema foundation complete: migration, types, query function, and UI wiring all in place for Plan 02 automation logic to build against.
</success_criteria>

<output>
Create `.planning/phases/100-query-layer-automation/100-01-SUMMARY.md` when done
</output>

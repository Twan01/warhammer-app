---
phase: 124-data-management-tab
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/settings/DataManagementTab.tsx
  - src/app/settings/page.tsx
  - tests/settings/DataManagementTab.test.tsx
autonomous: true
requirements:
  - DAT-01
  - DAT-02
  - DAT-03
  - DAT-04
must_haves:
  truths:
    - "Data tab shows a clickable link that navigates to /data-health"
    - "User can trigger factory reset with multi-step confirmation (type RESET)"
    - "User can export preferences to a JSON file via native save dialog"
    - "User can import preferences from a JSON file via native open dialog"
    - "Import validates entire JSON before writing any settings"
    - "Import invalidates APP_SETTINGS_KEY cache after success"
    - "Malformed/invalid JSON shows error toast without modifying settings"
  artifacts:
    - path: "src/features/settings/DataManagementTab.tsx"
      provides: "Data Management tab content with 4 card sections"
      exports: ["DataManagementTab"]
    - path: "src/app/settings/page.tsx"
      provides: "Settings page with DataManagementTab wired into Data tab"
      contains: "DataManagementTab"
    - path: "tests/settings/DataManagementTab.test.tsx"
      provides: "Unit tests for all DAT-01 through DAT-04 behaviors"
      min_lines: 80
  key_links:
    - from: "src/features/settings/DataManagementTab.tsx"
      to: "/data-health"
      via: "useNavigate from TanStack Router"
      pattern: "navigate.*data-health"
    - from: "src/features/settings/DataManagementTab.tsx"
      to: "invoke(\"factory_reset\")"
      via: "Tauri invoke API"
      pattern: "invoke.*factory_reset"
    - from: "src/features/settings/DataManagementTab.tsx"
      to: "getAppSettings"
      via: "direct query import for export"
      pattern: "getAppSettings"
    - from: "src/features/settings/DataManagementTab.tsx"
      to: "upsertAppSetting"
      via: "direct query import for import"
      pattern: "upsertAppSetting"
    - from: "src/features/settings/DataManagementTab.tsx"
      to: "APP_SETTINGS_KEY"
      via: "React Query cache invalidation after import"
      pattern: "invalidateQueries.*APP_SETTINGS_KEY"
    - from: "src/app/settings/page.tsx"
      to: "src/features/settings/DataManagementTab.tsx"
      via: "component import"
      pattern: "import.*DataManagementTab"
---

<objective>
Build the DataManagementTab component with four card sections (Data Health link, Export, Import, Factory Reset) and wire it into the Settings page Data tab, replacing the placeholder.

Purpose: Delivers all four DAT requirements — the user gets navigation to Data Health, preference export/import, and factory reset with safety confirmation.
Output: DataManagementTab.tsx, updated page.tsx, DataManagementTab.test.tsx
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-data-management-tab/124-CONTEXT.md
@.planning/phases/124-data-management-tab/124-RESEARCH.md
@.planning/phases/124-data-management-tab/124-PATTERNS.md
@.planning/phases/124-data-management-tab/124-UI-SPEC.md

<interfaces>
<!-- From src/db/queries/appSettings.ts -->
export type AppSettingsMap = Record<string, string>;
export async function getAppSettings(): Promise<AppSettingsMap>;
export async function upsertAppSetting(key: string, value: string): Promise<void>;

<!-- From src/hooks/useAppSettings.ts -->
export const APP_SETTINGS_KEY = ["app-settings"] as const;
export function useAppSettings(): UseQueryResult<AppSettingsMap>;
export function useUpdateSetting(): UseMutationResult<void, Error, { key: string; value: string }>;

<!-- From src/app/settings/page.tsx (current state) -->
- Data tab placeholder at lines 33-38: TabsContent value="data" with h2 "Data Management" + placeholder paragraph
- Import to add: DataManagementTab from @/features/settings/DataManagementTab

<!-- Tauri plugin APIs (all already installed) -->
import { save, open } from "@tauri-apps/plugin-dialog";
import { writeTextFile, readTextFile } from "@tauri-apps/plugin-fs";
import { invoke } from "@tauri-apps/api/core";
import { relaunch } from "@tauri-apps/plugin-process";

<!-- shadcn components (all already installed) -->
Card, CardContent from @/components/ui/card
Button from @/components/ui/button
AlertDialog, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogCancel from @/components/ui/alert-dialog
Input from @/components/ui/input
Label from @/components/ui/label
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DataManagementTab component</name>
  <files>src/features/settings/DataManagementTab.tsx</files>
  <read_first>
    - src/features/data-health/BackupCard.tsx (save/open dialog, invoke, relaunch, toast patterns, AlertDialog confirmation)
    - src/features/army-lists/ArmyListDetailSheet.tsx (save dialog + writeTextFile pattern, lines 238-254)
    - src/db/queries/appSettings.ts (getAppSettings, upsertAppSetting signatures)
    - src/hooks/useAppSettings.ts (APP_SETTINGS_KEY export)
    - .planning/phases/124-data-management-tab/124-UI-SPEC.md (full visual/interaction spec)
    - .planning/phases/124-data-management-tab/124-PATTERNS.md (all code patterns to follow)
  </read_first>
  <action>
Create `src/features/settings/DataManagementTab.tsx` exporting a named function `DataManagementTab`.

The component renders a vertical stack (`space-y-4`) of four Card sections per UI-SPEC layout contract. Each Card uses `CardContent` with `className="p-6"` and a flex row layout: icon + title/description on left, action button on right.

**Section 1 — Data Health Link (per D-01):**
- Icon: `Database` from lucide-react, `h-4 w-4 text-muted-foreground`
- Title: "Data Health" (`text-sm font-semibold`)
- Description: "View diagnostics, backup & restore" (`text-sm text-muted-foreground`)
- Button: "Open Data Health" `variant="outline"`, onClick calls `navigate({ to: "/data-health" })` via `useNavigate` from `@tanstack/react-router`

**Section 2 — Preference Export (per D-07, D-08):**
- Icon: `Download` from lucide-react
- Title: "Export Preferences"
- Description: "Save your current settings to a JSON file for safekeeping"
- Button: "Export Preferences" `variant="outline"`, loading state shows Loader2 spinner + "Exporting..."
- Handler `handleExport`: calls `getAppSettings()` (imported directly from `@/db/queries/appSettings`), builds JSON payload `{ version: 1, exported_at: new Date().toISOString(), settings }`, opens `save()` dialog with `defaultPath: hobbyforge-preferences-YYYY-MM-DD.json` and `filters: [{ name: "JSON", extensions: ["json"] }]`, writes via `writeTextFile(destination, JSON.stringify(payload, null, 2))`, shows `toast.success("Preferences exported")`. If save dialog returns null, return silently. Wrap in try/catch with `toast.error`.

**Section 3 — Preference Import (per D-09, D-10, D-11):**
- Icon: `Upload` from lucide-react
- Title: "Import Preferences"
- Description: "Restore settings from a previously exported JSON file"
- Button: "Import Preferences" `variant="outline"`, loading state shows Loader2 spinner + "Importing..."
- Handler `handleImport`: opens `open()` dialog (cast result to `string | null` per Pitfall 1), reads via `readTextFile(path)`, parses JSON, validates entire structure BEFORE writing anything per D-11: check `typeof payload === "object"`, `payload !== null`, `payload.version === 1`, `typeof payload.settings === "object"`. If invalid, `toast.error("Invalid preferences file - missing version or settings")` and return. If JSON.parse throws, `toast.error("Could not read file - is it a valid JSON file?")` and return. On valid: iterate `Object.entries(payload.settings)`, call `upsertAppSetting(key, value)` for each entry where `typeof value === "string"` (skip non-strings per Pitfall 6). After all writes, call `qc.invalidateQueries({ queryKey: APP_SETTINGS_KEY })` via `useQueryClient()`. Show `toast.success("Imported N setting(s)")` per D-10.

**Section 4 — Factory Reset (per D-02, D-03, D-04, D-05, D-06):**
- Icon: `RotateCcw` from lucide-react
- Title: "Factory Reset"
- Description: "Permanently delete all your data and start fresh. This cannot be undone."
- Button: "Factory Reset" `variant="destructive"`, onClick sets `open` state to true
- AlertDialog controlled by `open` state + `onOpenChange` handler that resets `phrase` to `""` when closing (Pitfall 2), prevents close when `isResetting` is true
- AlertDialog title: "Factory Reset - Are you absolutely sure?"
- AlertDialog description line 1: "This will permanently delete all your collection, paints, army lists, recipes, battle logs, and photos."
- AlertDialog description line 2: "A safety backup will be created first. The app will restart automatically."
- Label: 'Type "RESET" to confirm' with `htmlFor="reset-confirm"`
- Input with `id="reset-confirm"`, `value={phrase}`, `onChange`, `placeholder="RESET"`, `autoComplete="off"`
- Cancel button: "Keep My Data" (AlertDialogCancel, disabled when isResetting)
- Confirm button: "Reset App" `variant="destructive"`, disabled when `phrase !== "RESET" || isResetting`, loading state shows Loader2 + "Resetting..."
- Add `onEscapeKeyDown={(e) => isResetting && e.preventDefault()}` to AlertDialogContent per accessibility contract
- Handler `handleFactoryReset`: set `isResetting(true)`, call `invoke("factory_reset")`, then `relaunch()`. On error: `toast.error` with message, set `isResetting(false)`.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exports `DataManagementTab` as a named function component
    - `grep -c "useNavigate" src/features/settings/DataManagementTab.tsx` returns 1
    - `grep -c "invoke.*factory_reset" src/features/settings/DataManagementTab.tsx` returns 1
    - `grep -c "getAppSettings" src/features/settings/DataManagementTab.tsx` returns at least 1
    - `grep -c "upsertAppSetting" src/features/settings/DataManagementTab.tsx` returns at least 1
    - `grep -c "APP_SETTINGS_KEY" src/features/settings/DataManagementTab.tsx` returns at least 1
    - `grep -c "invalidateQueries" src/features/settings/DataManagementTab.tsx` returns at least 1
    - `grep -c 'version.*1' src/features/settings/DataManagementTab.tsx` returns at least 1 (export payload version check)
    - `grep -c "relaunch" src/features/settings/DataManagementTab.tsx` returns at least 1
    - `grep -c "RESET" src/features/settings/DataManagementTab.tsx` returns at least 2 (phrase constant + placeholder)
    - `grep -c "toast\\.success\\|toast\\.error" src/features/settings/DataManagementTab.tsx` returns at least 4 (export success, import success, import error x2, reset error)
    - Four Card components rendered (Data Health, Export, Import, Factory Reset)
    - AlertDialog has onOpenChange that resets phrase state on close
    - pnpm build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>DataManagementTab component renders 4 card sections with all interaction handlers for Data Health link, preference export, preference import, and factory reset with multi-step confirmation</done>
</task>

<task type="auto">
  <name>Task 2: Wire DataManagementTab into Settings page + add tests</name>
  <files>src/app/settings/page.tsx, tests/settings/DataManagementTab.test.tsx</files>
  <read_first>
    - src/app/settings/page.tsx (current placeholder at lines 33-38 to replace)
    - tests/settings/SettingsPage.test.tsx (existing test patterns for Settings)
    - tests/data-health/backupCard.test.tsx (Tauri API mock patterns — vi.mock for plugin-dialog, plugin-fs, plugin-process, api/core)
    - src/features/settings/DataManagementTab.tsx (the component just created in Task 1)
  </read_first>
  <action>
**page.tsx update:**
Replace the Data tab placeholder (lines 33-38) in `src/app/settings/page.tsx`. Add import: `import { DataManagementTab } from "@/features/settings/DataManagementTab"`. Replace the `TabsContent value="data"` block contents (remove the h2 and placeholder p) with just `<DataManagementTab />`. The h2 heading "Data Management" is NOT needed in page.tsx anymore — it is rendered by the DataManagementTab component internally if the UI-SPEC calls for it, OR the four cards are self-descriptive without a heading (per UI-SPEC the layout is just 4 cards in a vertical stack with no separate heading — the section titles are inside each card). Keep the `TabsContent` wrapper with `className="mt-4"`.

**Test file:**
Create `tests/settings/DataManagementTab.test.tsx` following the mock setup pattern from `tests/data-health/backupCard.test.tsx` (PATTERNS.md test section). Set up mocks for:
- `@tauri-apps/plugin-dialog` (save, open)
- `@tauri-apps/plugin-fs` (writeTextFile, readTextFile)
- `@tauri-apps/plugin-process` (relaunch)
- `@tauri-apps/api/core` (invoke)
- `sonner` (toast.success, toast.error)
- `@tanstack/react-router` (useNavigate returning mockNavigate)
- `@/db/queries/appSettings` (getAppSettings returning `{ theme: "dark", language: "en" }`, upsertAppSetting)
- `@tanstack/react-query` (useQueryClient returning `{ invalidateQueries: vi.fn() }`)

Import `describe, it, expect, vi, beforeEach` from vitest, `render, screen, waitFor` from testing-library, `userEvent` from `@testing-library/user-event`.

Write tests covering:

DAT-01:
- "renders Data Health section with Open Data Health button" — assert button with text "Open Data Health" is present
- "clicking Open Data Health calls navigate" — click button, assert mockNavigate was called

DAT-02:
- "renders Factory Reset button" — assert button with text "Factory Reset" is present
- "Factory Reset opens confirmation dialog" — click Factory Reset, assert "Are you absolutely sure" text appears
- "Reset App button is disabled until RESET is typed" — open dialog, assert Reset App button is disabled, type "RESET" in input, assert button becomes enabled
- "confirming reset calls invoke and relaunch" — open dialog, type "RESET", click Reset App, await waitFor, assert mockInvoke called with "factory_reset", assert mockRelaunch called
- "reset error shows toast" — mockInvoke rejects, open dialog, type "RESET", click Reset App, await waitFor, assert toast.error was called

DAT-03:
- "Export calls save dialog and writeTextFile" — mockSave resolves to "test.json", click Export Preferences, await waitFor, assert mockSave called, assert mockWriteTextFile called with "test.json" and a string containing `"version":1`
- "Export cancelled when save returns null" — mockSave resolves to null, click Export, assert mockWriteTextFile NOT called

DAT-04:
- "Import reads file and upserts settings" — mockOpen resolves to "prefs.json", mockReadTextFile resolves to `JSON.stringify({ version: 1, exported_at: "2026-01-01", settings: { lang: "fr" } })`, click Import Preferences, await waitFor, assert upsertAppSetting called with "lang", "fr"
- "Import shows error for malformed JSON" — mockReadTextFile resolves to "not json", click Import, await waitFor, assert toast.error called
- "Import shows error for missing version" — mockReadTextFile resolves to `JSON.stringify({ settings: {} })`, click Import, await waitFor, assert toast.error called

Use `beforeEach(() => vi.clearAllMocks())`.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/settings/DataManagementTab.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DataManagementTab" src/app/settings/page.tsx` returns at least 2 (import + usage)
    - The Data tab placeholder paragraph "coming in the next update" is removed from page.tsx
    - `pnpm test -- tests/settings/DataManagementTab.test.tsx` passes all tests
    - Test file has at least 10 test cases (it/test blocks) covering DAT-01 through DAT-04
    - All Tauri APIs are mocked (no real native calls in test)
    - pnpm build succeeds
  </acceptance_criteria>
  <done>Settings page Data tab shows DataManagementTab component instead of placeholder; all DAT-01 through DAT-04 behaviors verified by passing unit tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> import validation | JSON file from disk crosses trust boundary into app state |
| User confirmation -> Rust invoke | Factory reset requires typed phrase before destructive invoke call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-03 | Tampering | Preference import JSON | mitigate | Validate entire JSON structure (version field, settings object type) before any DB write per D-11 |
| T-124-04 | Tampering | Import non-string values | mitigate | Skip entries where typeof value !== "string" per Pitfall 6 |
| T-124-05 | Elevation of Privilege | Factory reset without confirmation | mitigate | Three-step flow: button -> AlertDialog -> typed "RESET" phrase -> action per D-03 |
| T-124-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
- pnpm build succeeds (TypeScript compilation)
- pnpm test -- tests/settings/DataManagementTab.test.tsx passes all tests
- pnpm test passes full suite (no regressions)
</verification>

<success_criteria>
Data tab in Settings shows four functional sections: Data Health navigation, preference export, preference import, and factory reset with multi-step confirmation. All behaviors verified by unit tests.
</success_criteria>

<output>
Create `.planning/phases/124-data-management-tab/124-02-SUMMARY.md` when done
</output>

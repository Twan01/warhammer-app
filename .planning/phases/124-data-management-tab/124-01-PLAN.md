---
phase: 124-data-management-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - DAT-02
must_haves:
  truths:
    - "factory_reset Rust command exists and is registered in invoke_handler"
    - "Safety backup is created before any destructive operation"
    - "hobbyforge.db and sidecar files are deleted"
    - "Image files in appDataDir are cleaned up"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "factory_reset Tauri command"
      contains: "async fn factory_reset"
  key_links:
    - from: "factory_reset"
      to: "create_safety_backup"
      via: "internal Rust function call"
      pattern: "create_safety_backup\\(app\\.clone\\(\\)\\)"
---

<objective>
Add the `factory_reset` Rust Tauri command that creates a safety backup, deletes hobbyforge.db (plus WAL/SHM/journal sidecars), and removes image files from appDataDir.

Purpose: Backend support for DAT-02 factory reset feature. The JS frontend (Plan 02) calls this via `invoke("factory_reset")` then relaunches.
Output: `factory_reset` command registered in lib.rs invoke_handler.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-data-management-tab/124-CONTEXT.md
@.planning/phases/124-data-management-tab/124-RESEARCH.md
@.planning/phases/124-data-management-tab/124-PATTERNS.md

<interfaces>
<!-- From src-tauri/src/lib.rs — existing command patterns -->
From src-tauri/src/lib.rs:
- `create_safety_backup(app: tauri::AppHandle) -> Result<String, String>` — creates zip in backups/ dir
- `restore_from_backup(app: tauri::AppHandle, path: String) -> Result<(), String>` — safety backup + sidecar delete + extract zip
- `app.path().app_data_dir()` — resolves to platform-specific app data directory
- `tauri::generate_handler![...]` at line 1355 — handler registration macro

Photo storage: flat UUID files (e.g. "a3f8c1d2-xxxx.jpg") directly in appDataDir, NOT in subdirectories.
Image extensions used in codebase: jpg, jpeg, png, webp, gif.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add factory_reset Rust command</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/lib.rs (lines 1118-1222 for create_safety_backup and restore_from_backup patterns; lines 1355-1365 for invoke_handler registration)
  </read_first>
  <action>
Add a new async Tauri command `factory_reset` that takes `app: tauri::AppHandle` and returns `Result<(), String>`. Place it after the `restore_from_backup` function (around line 1222). Per D-04 and D-06:

1. Call `create_safety_backup(app.clone()).await?` — abort if this fails (safety guarantee per D-04).
2. Resolve `app_data_dir` via `app.path().app_data_dir()`.
3. Delete sidecar files (`-wal`, `-shm`, `-journal`) with NotFound tolerance — same pattern as `restore_from_backup` lines 1193-1201.
4. Delete `hobbyforge.db` main file — do NOT tolerate NotFound for the main file (Pitfall 4 from RESEARCH.md). Use `std::fs::remove_file` and propagate errors.
5. Delete image files: enumerate `app_data_dir` with `std::fs::read_dir`, filter by extensions `["jpg", "jpeg", "png", "webp", "gif"]` (case-insensitive via `.to_lowercase()`), delete each with `std::fs::remove_file` (best-effort, ignore individual file errors). This handles the flat UUID photo storage pattern confirmed in PATTERNS.md.
6. Return `Ok(())`.

Add `factory_reset` to the `tauri::generate_handler![]` macro at line 1355, after `ack_successful_launch`.

Add a doc comment: "Delete hobbyforge.db and all user photos after creating a safety backup. The JS caller must call relaunch() after this succeeds — migration plugin recreates the DB on startup."
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async fn factory_reset" src-tauri/src/lib.rs` returns 1
    - `grep -c "factory_reset" src-tauri/src/lib.rs` returns at least 2 (function + handler registration)
    - `grep "create_safety_backup" src-tauri/src/lib.rs` shows the call inside factory_reset
    - `cargo check --manifest-path src-tauri/Cargo.toml` succeeds with no errors
    - The sidecar deletion loop uses the same NotFound tolerance pattern as restore_from_backup
    - The main hobbyforge.db deletion does NOT tolerate NotFound
    - Image file enumeration filters by lowercase extension match against jpg/jpeg/png/webp/gif
  </acceptance_criteria>
  <done>factory_reset command compiles, is registered in invoke_handler, creates safety backup before delete, removes DB + sidecars + image files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS -> Rust invoke | Frontend calls factory_reset — must not be triggerable without user confirmation (handled in Plan 02 UI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | factory_reset command | mitigate | Safety backup created before any file deletion; abort if backup fails |
| T-124-02 | Denial of Service | factory_reset deletes DB | accept | Intentional destructive operation; multi-step UI confirmation in Plan 02 |
| T-124-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
cargo check succeeds for the Tauri project after adding factory_reset command
</verification>

<success_criteria>
factory_reset Rust command exists, compiles, is registered, and follows the established safety-backup-first pattern
</success_criteria>

<output>
Create `.planning/phases/124-data-management-tab/124-01-SUMMARY.md` when done
</output>

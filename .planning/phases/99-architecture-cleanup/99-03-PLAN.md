---
phase: 99-architecture-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/units/UnitSheet.tsx
  - src/features/units/UnitFormRequired.tsx
  - src/features/units/UnitFormOptional.tsx
autonomous: true
requirements:
  - ARCH-03

must_haves:
  truths:
    - "Creating a new unit via UnitSheet works identically to before decomposition"
    - "Editing an existing unit via UnitSheet works identically to before decomposition"
    - "Each new sub-component file is under 200 lines"
    - "UnitSheet.tsx orchestrator is under 250 lines"
  artifacts:
    - path: "src/features/units/UnitFormRequired.tsx"
      provides: "Required form fields: name, faction, category/role, base size, points, quantity"
      exports: ["UnitFormRequired"]
    - path: "src/features/units/UnitFormOptional.tsx"
      provides: "Collapsible optional fields: painting status, assembly, basing, priority, notes, dates"
      exports: ["UnitFormOptional"]
    - path: "src/features/units/UnitSheet.tsx"
      provides: "Sheet wrapper, useForm setup, buildDefaultValues, submit logic"
  key_links:
    - from: "src/features/units/UnitSheet.tsx"
      to: "src/features/units/UnitFormRequired.tsx"
      via: "import + JSX inside Form"
      pattern: "UnitFormRequired"
    - from: "src/features/units/UnitSheet.tsx"
      to: "src/features/units/UnitFormOptional.tsx"
      via: "import + JSX inside Form"
      pattern: "UnitFormOptional"
    - from: "src/features/units/UnitFormRequired.tsx"
      to: "react-hook-form"
      via: "useFormContext<UnitFormValues>"
      pattern: "useFormContext"
---

<objective>
Decompose UnitSheet.tsx from 688 lines into 2 form section sub-components plus a slim orchestrator, each under 200 lines (ARCH-03).

Purpose: The 688-line form file is unwieldy. Splitting into required fields and optional fields sub-components improves maintainability while keeping the create/edit workflow identical.

Output: UnitFormRequired.tsx, UnitFormOptional.tsx, slimmed UnitSheet.tsx. Create and edit workflows unchanged.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-architecture-cleanup/99-RESEARCH.md
@.planning/phases/99-architecture-cleanup/99-PATTERNS.md

<interfaces>
From src/components/ui/form.tsx:
  const Form = FormProvider  // shadcn Form IS FormProvider -- useFormContext works inside <Form>

From src/features/units/unitSchema.ts:
  export const unitSchema = z.object({ ... })
  export type UnitFormValues = z.infer<typeof unitSchema>

From src/features/units/UnitSheet.tsx (current structure):
  - Lines 1-40: imports
  - Lines 42-80: UnitSheetProps, buildDefaultValues
  - Lines 82-175: UnitSheet component (useForm, useEffect syncs, onSubmit, hooks)
  - Lines 177-233: Required fields JSX (name, faction, category)
  - Lines 235-250: Separator + collapsible toggle
  - Lines 250-672: Optional fields JSX (painting status, assembly, basing, etc.)
  - Lines 672-688: Sheet footer (submit button)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract UnitFormRequired and UnitFormOptional, slim UnitSheet (D-09 through D-11)</name>
  <files>
    src/features/units/UnitFormRequired.tsx,
    src/features/units/UnitFormOptional.tsx,
    src/features/units/UnitSheet.tsx
  </files>
  <read_first>
    src/features/units/UnitSheet.tsx,
    src/features/units/unitSchema.ts,
    src/components/ui/form.tsx,
    src/features/units/LoadoutSection.tsx,
    src/features/units/CategoryCombobox.tsx
  </read_first>
  <action>
Per D-09: Split UnitSheet.tsx into three files.

Create UnitFormRequired.tsx: Extract the required fields section (name, faction select, category/role combobox, base size, points, quantity). Use useFormContext&lt;UnitFormValues&gt;() to access the form control -- since shadcn's Form component IS FormProvider, useFormContext works automatically inside the Form wrapper in UnitSheet. Define UnitFormRequiredProps for data the component cannot derive from form context: factions array, factionsLoading boolean, and any other data needed for select options (hasTiers, tiers for points field if it appears in the required section). Follow the existing FormField render pattern from UnitSheet.tsx lines 177-233.

Create UnitFormOptional.tsx: Extract the collapsible optional details section. This component owns its local expanded/collapsed state (useState for the toggle is local interaction state per D-07). Use useFormContext&lt;UnitFormValues&gt;() for form field access. Define UnitFormOptionalProps for any external data needed (e.g., hasTiers, tiers if points is in this section). Contains the Collapsible trigger, painting status checkboxes, priority input, notes textarea, date fields, and all other optional fields from lines 250-672.

Per D-10: Both sub-components use useFormContext (preferred over prop drilling the form object). Import useFormContext from "react-hook-form" and type it with UnitFormValues.

Per D-11: Keep buildDefaultValues in UnitSheet.tsx since it is used in useForm initialization.

Slim UnitSheet.tsx to contain:
- buildDefaultValues helper function
- UnitSheet component with useForm setup, zodResolver, useEffect syncs for edit mode, onSubmit handler, React Query mutation hooks
- JSX: Sheet wrapper, SheetContent, SheetHeader, Form component (which IS FormProvider), then renders UnitFormRequired and UnitFormOptional inside the form, followed by SheetFooter with submit button

Target: UnitSheet.tsx under 250 lines, UnitFormRequired.tsx under 200 lines, UnitFormOptional.tsx under 200 lines.
  </action>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App" &amp;&amp; pnpm build</automated>
    Then manual: Open the app, create a new unit (verify all fields work), edit an existing unit (verify fields populate correctly).
  </verify>
  <acceptance_criteria>
    - src/features/units/UnitFormRequired.tsx exists and exports UnitFormRequired function
    - src/features/units/UnitFormOptional.tsx exists and exports UnitFormOptional function
    - UnitFormRequired.tsx contains useFormContext&lt;UnitFormValues&gt;() call
    - UnitFormOptional.tsx contains useFormContext&lt;UnitFormValues&gt;() call
    - Neither sub-component contains useQuery, useMutation, useForm, or useCreateUnit/useUpdateUnit
    - UnitSheet.tsx contains buildDefaultValues (per D-11)
    - UnitSheet.tsx imports and renders both UnitFormRequired and UnitFormOptional inside a Form wrapper
    - wc -l on UnitSheet.tsx returns under 250
    - wc -l on UnitFormRequired.tsx returns under 200
    - wc -l on UnitFormOptional.tsx returns under 200
    - pnpm build exits 0 (TypeScript compiles cleanly)
  </acceptance_criteria>
  <done>UnitSheet decomposed into 3 files, each under target line count. Form context flows via useFormContext. Create/edit workflow unchanged. ARCH-03 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Pure form component decomposition with no new inputs or data flows.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-03 | N/A | All | accept | Pure UI refactoring -- no new attack surface |
</threat_model>

<verification>
1. pnpm build passes (TypeScript compilation)
2. wc -l on UnitSheet.tsx under 250, UnitFormRequired.tsx under 200, UnitFormOptional.tsx under 200
3. grep -c "useFormContext" in both sub-components returns >= 1
4. grep -c "useQuery\|useMutation\|useCreateUnit\|useUpdateUnit" in sub-components returns 0
</verification>

<success_criteria>
- UnitSheet.tsx decomposed from 688 lines to under 250 lines
- 2 new sub-component files, each under 200 lines
- Sub-components use useFormContext for form access (D-10)
- buildDefaultValues stays in UnitSheet (D-11)
- Create/edit unit workflow unchanged
- pnpm build passes
</success_criteria>

<output>
Create `.planning/phases/99-architecture-cleanup/99-03-SUMMARY.md` when done
</output>

---
phase: 146-detach-safety-rails
plan: 03
type: execute
wave: 2
depends_on: ["146-01"]
files_modified:
  - src/hooks/useTechniques.ts
  - src/features/techniques/TechniqueDeleteDialog.tsx
  - src/features/techniques/TechniqueLibraryTab.tsx
  - tests/techniques/TechniqueDeleteDialog.test.tsx
autonomous: true
requirements: [SAFE-03]

must_haves:
  truths:
    - "Deleting a technique routes through detachAllAndDeleteTechnique — live instances are auto-detached (paints baked) before the technique is removed, so no recipe content is lost"
    - "The delete dialog surfaces the non-detached live-instance count and labels the confirm button with the consequence ('Detach N recipe(s) & delete')"
    - "When no live instances exist, the dialog keeps the existing simple 'permanently remove' copy and 'Delete' label"
    - "After delete-with-auto-detach, recipe-level + technique-level query keys are invalidated so every recipe surface refreshes"
  artifacts:
    - path: "src/hooks/useTechniques.ts"
      provides: "useDeleteTechnique rewired to detachAllAndDeleteTechnique with extended recipe-level prefix invalidation"
      contains: "detachAllAndDeleteTechnique"
    - path: "src/features/techniques/TechniqueDeleteDialog.tsx"
      provides: "Two-case copy + consequence-labelled confirm button driven by liveInstanceCount"
      contains: "liveInstanceCount"
    - path: "src/features/techniques/TechniqueLibraryTab.tsx"
      provides: "Passes the non-detached live-instance count (via getNonDetachedInstanceCount query) to the dialog"
      contains: "TechniqueDeleteDialog"
    - path: "tests/techniques/TechniqueDeleteDialog.test.tsx"
      provides: "Updated component tests for the liveInstanceCount prop + case-B copy/label"
      contains: "liveInstanceCount"
  key_links:
    - from: "src/hooks/useTechniques.ts"
      to: "detachAllAndDeleteTechnique"
      via: "useDeleteTechnique mutationFn replacement"
      pattern: "detachAllAndDeleteTechnique"
    - from: "src/features/techniques/TechniqueDeleteDialog.tsx"
      to: "liveInstanceCount"
      via: "case-A/case-B copy + confirm button label"
      pattern: "liveInstanceCount > 0"
    - from: "src/features/techniques/TechniqueLibraryTab.tsx"
      to: "getNonDetachedInstanceCount"
      via: "live query feeding liveInstanceCount prop (not stale usage_count)"
      pattern: "getNonDetachedInstanceCount|liveInstanceCount"
---

<objective>
Make technique deletion honest and lossless (the v0.6.0 principle, SAFE-03). Rewire `useDeleteTechnique` to call `detachAllAndDeleteTechnique` (Plan 01) so any live (non-detached) instances are auto-detached — baking their resolved colours into plain recipe content — BEFORE the technique is deleted. Extend `TechniqueDeleteDialog` to surface the non-detached live-instance count and name the consequence on the confirm button ("Detach N recipe(s) & delete"). Feed that count from a live `getNonDetachedInstanceCount` query rather than the legacy total `usage_count`, which counts detached instances too.

Purpose: SAFE-03 — deleting a technique with live recipe instances surfaces the affected-recipe count, requires explicit confirmation, and preserves all recipe content.
Output: modified `useTechniques.ts`, `TechniqueDeleteDialog.tsx`, `TechniqueLibraryTab.tsx`, updated `tests/techniques/TechniqueDeleteDialog.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-detach-safety-rails/146-CONTEXT.md
@.planning/phases/146-detach-safety-rails/146-RESEARCH.md
@.planning/phases/146-detach-safety-rails/146-PATTERNS.md
@.planning/phases/146-detach-safety-rails/146-UI-SPEC.md
@.planning/phases/146-detach-safety-rails/146-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase + Plan 01 output. -->

From Plan 01 — src/db/queries/recipeTechniqueDetach.ts:
- `export async function detachAllAndDeleteTechnique(techniqueId: number): Promise<void>` — auto-detach loop on one db handle, then DELETE FROM techniques.
- `export async function getNonDetachedInstanceCount(techniqueId: number): Promise<number>` — re-exported from detach module; counts `detached = 0` instances only.

Current useDeleteTechnique — src/hooks/useTechniques.ts (lines 147-155):
- `mutationFn: deleteTechnique` (bare DELETE). onSuccess only calls `invalidateTechniqueKeys(qc)` (technique-level keys only).
- `useUpdateTechnique` onSuccess (lines 128-142) is the analog for the EXTENDED recipe-level prefix invalidation breadth: it invalidates `["recipe-sections"]`, `["recipe-steps"]`, `["recipe-paints"]`, `["slot-resolution-map"]`, `STEP_COUNTS_KEY`, `RECIPE_SWATCH_KEY`, `RECIPE_AVAILABILITY_KEY`.
- `STEP_COUNTS_KEY, RECIPE_SWATCH_KEY, RECIPE_AVAILABILITY_KEY` already imported at top of useTechniques.ts (lines 21-24).

Current TechniqueDeleteDialog — src/features/techniques/TechniqueDeleteDialog.tsx (full file, 69 lines):
- Props: `{ open, technique: Technique | null, usageCount: number, onClose }`.
- Description branch lines 47-52 (usageCount > 0 → "is used by N recipes…"; else "permanently remove").
- Confirm button lines 58-64: `disabled={deleteTechnique.isPending}`, label `{deleteTechnique.isPending ? "Deleting…" : "Delete"}`.
- Success toast line 33: `toast.success("Technique deleted.")`.
- Cancel button "Keep Technique" (line 55-57) — unchanged.

Call site — src/features/techniques/TechniqueLibraryTab.tsx (lines 176-182):
- `<TechniqueDeleteDialog open={deleteOpen} technique={deleting} usageCount={deleting?.usage_count ?? 0} onClose={closeDelete} />`.
- `deleting` is a TechniqueWithCounts (has `.id`, `.usage_count`). NOTE: `usage_count` (from getTechniquesWithCounts) counts ALL instances, NOT just non-detached — so it is the WRONG source for the honest live count. Replace with a live `getNonDetachedInstanceCount(deleting.id)` query.

Existing test — tests/techniques/TechniqueDeleteDialog.test.tsx (passes usageCount={0|1|2}). These props must be renamed to `liveInstanceCount` and the case-B copy/label assertions updated to match UI-SPEC.

UI-SPEC copy (LOCKED):
- Case A (liveInstanceCount === 0): description `This will permanently remove "{name}" and all its steps. This cannot be undone.`; confirm label "Delete".
- Case B (liveInstanceCount > 0): description `"{name}" is live-linked to {N} recipe{N===1?"":"s"}. Detaching will bake the current colours into those recipes before removing the technique. No recipe content will be lost.`; confirm label `Detach {N} recipe{N===1?"":"s"} & delete`; loading label "Detaching & deleting…".
- Cancel (both): "Keep Technique".
- Success toast case B: `Technique detached from {N} recipe{N===1?"":"s"} and deleted.`; case A: "Technique deleted." (unchanged).
- Error toast: "Failed to delete technique. Please try again." (unchanged).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire useDeleteTechnique to auto-detach + extend invalidation</name>
  <files>src/hooks/useTechniques.ts</files>
  <read_first>
    - src/hooks/useTechniques.ts (useDeleteTechnique lines 147-155; useUpdateTechnique recipe-level invalidation lines 128-142; existing imports lines 1-24)
    - src/db/queries/recipeTechniqueDetach.ts (detachAllAndDeleteTechnique signature from Plan 01)
    - .planning/phases/146-detach-safety-rails/146-PATTERNS.md (useDeleteTechnique replacement + prefix-invalidation block)
  </read_first>
  <action>
    In `src/hooks/useTechniques.ts`, import `detachAllAndDeleteTechnique` from "@/db/queries/recipeTechniqueDetach". Replace the `useDeleteTechnique` `mutationFn` from `deleteTechnique` to `detachAllAndDeleteTechnique` (signature is identical — `(techniqueId: number) => Promise<void>`). Extend its `onSuccess` to keep `invalidateTechniqueKeys(qc)` AND add the recipe-level PREFIX invalidations (no recipeId — N recipes affected) mirroring `useUpdateTechnique` lines 134-140: `["recipe-sections"]`, `["recipe-steps"]`, `["recipe-paints"]`, `["slot-resolution-map"]`, `["technique-instances"]`, `STEP_COUNTS_KEY`, `RECIPE_SWATCH_KEY`, `RECIPE_AVAILABILITY_KEY`. Leave the existing `deleteTechnique` import in place ONLY if it still has callers; if `useDeleteTechnique` was its sole caller, remove the now-unused `deleteTechnique` import to satisfy `noUnusedLocals` (verify with grep before removing).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `useDeleteTechnique.mutationFn` is `detachAllAndDeleteTechnique` (grep confirms; `deleteTechnique` no longer referenced as the delete mutationFn).
    - `onSuccess` calls `invalidateTechniqueKeys(qc)` PLUS prefix invalidations for `["recipe-sections"]`, `["recipe-steps"]`, `["recipe-paints"]`, `["slot-resolution-map"]`, `["technique-instances"]`, and the three imported keys.
    - `pnpm build` typechecks clean — no unused-import error for `deleteTechnique` (removed if orphaned).
  </acceptance_criteria>
  <done>Deleting a technique auto-detaches live instances first (preserving content) and refreshes every affected recipe surface via prefix invalidation.</done>
</task>

<task type="auto">
  <name>Task 2: Extend TechniqueDeleteDialog (two-case copy) + wire live count + update tests</name>
  <files>src/features/techniques/TechniqueDeleteDialog.tsx, src/features/techniques/TechniqueLibraryTab.tsx, tests/techniques/TechniqueDeleteDialog.test.tsx</files>
  <read_first>
    - src/features/techniques/TechniqueDeleteDialog.tsx (full file — props, description branch lines 47-52, confirm button lines 58-64, toast line 33)
    - src/features/techniques/TechniqueLibraryTab.tsx (call site lines 176-182, deleting/deleteOpen/closeDelete state)
    - tests/techniques/TechniqueDeleteDialog.test.tsx (existing usageCount-based tests to migrate to liveInstanceCount)
    - src/db/queries/recipeTechniqueDetach.ts (getNonDetachedInstanceCount from Plan 01)
    - .planning/phases/146-detach-safety-rails/146-UI-SPEC.md (Surface 4 two-case copy contract, Copywriting Contract table)
  </read_first>
  <action>
    In `TechniqueDeleteDialog.tsx`: rename the prop `usageCount` to `liveInstanceCount` in `TechniqueDeleteDialogProps` and the destructure. Replace the description branch with the two LOCKED cases (case B when `liveInstanceCount > 0`: live-linked count + bake-colours reassurance + "No recipe content will be lost."; case A otherwise: existing permanent-remove copy). Replace the confirm button label logic: when pending → `liveInstanceCount > 0 ? "Detaching & deleting…" : "Deleting…"`; when idle → `liveInstanceCount > 0 ? \`Detach ${liveInstanceCount} recipe${liveInstanceCount === 1 ? "" : "s"} & delete\` : "Delete"`. Update the success toast in `handleConfirm` to be count-aware: case B → `Technique detached from ${liveInstanceCount} recipe${liveInstanceCount === 1 ? "" : "s"} and deleted.`, case A → "Technique deleted." Keep "Keep Technique" cancel and the error toast unchanged.

    In `TechniqueLibraryTab.tsx`: replace the `usageCount={deleting?.usage_count ?? 0}` prop with a live non-detached count. Add a `useQuery` (queryKey `["technique-nondetached-count", deleting?.id]`, enabled when `deleting != null`, queryFn `() => getNonDetachedInstanceCount(deleting!.id)`) — import `getNonDetachedInstanceCount` from "@/db/queries/recipeTechniqueDetach" and `useQuery` from @tanstack/react-query if not already imported. Pass `liveInstanceCount={nonDetachedCountQuery.data ?? 0}` to the dialog. This guarantees the count reflects detaches done in the editor (Pitfall 5: no stale prop).

    In `tests/techniques/TechniqueDeleteDialog.test.tsx`: rename every `usageCount={…}` prop to `liveInstanceCount={…}`. Update the case-B assertion (count > 0) to expect the new copy ("live-linked", "No recipe content will be lost") and the new confirm label ("Detach 2 recipes & delete" / singular "Detach 1 recipe & delete"). Keep the case-A (count 0) assertions on the existing "permanently remove" copy + "Delete" label.
  </action>
  <verify>
    <automated>pnpm test -- tests/techniques/TechniqueDeleteDialog.test.tsx && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `TechniqueDeleteDialog` prop is `liveInstanceCount` (no remaining `usageCount` references in the file).
    - Case B (count > 0) renders the locked "live-linked / bake the current colours / No recipe content will be lost" copy and a confirm button labelled "Detach {N} recipe(s) & delete" (pluralised) with loading text "Detaching & deleting…".
    - Case A (count === 0) renders the existing "permanently remove" copy and the "Delete" label, unchanged.
    - `TechniqueLibraryTab.tsx` feeds `liveInstanceCount` from a live `getNonDetachedInstanceCount` query (not `deleting.usage_count`).
    - `tests/techniques/TechniqueDeleteDialog.test.tsx` uses `liveInstanceCount`, asserts case-B copy + pluralised consequence label, and is GREEN.
    - `pnpm test` full suite GREEN; `pnpm build` typechecks clean.
  </acceptance_criteria>
  <done>The delete dialog honestly names the consequence, counts only non-detached live instances from a live query, and the auto-detach path preserves recipe content end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user delete action (technique library) → useDeleteTechnique → detachAllAndDeleteTechnique → local SQLite | A confirmed destructive cross-surface action (technique → many recipes). No network/auth/untrusted input. Threat surface is data-integrity: silent recipe-content loss on technique delete, and an inaccurate/misleading affected-recipe count. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-08 | Tampering (silent recipe-content loss) | useDeleteTechnique mutationFn | mitigate | Route through `detachAllAndDeleteTechnique` so live instances are detached (paints baked → plain content) BEFORE the technique DELETE cascades. Replaces the legacy bare DELETE that would CASCADE-destroy instances + slot maps. Proven by Plan 01 data-layer test (auto-detach preserves content). |
| T-146-09 | Repudiation (misleading consequence) | TechniqueDeleteDialog copy/count | mitigate | Count sourced from live `getNonDetachedInstanceCount` (excludes already-detached/plain instances); confirm button names the exact consequence ("Detach N recipe(s) & delete"). Honest by construction (v0.6.0). |
| T-146-10 | Information integrity (stale count) | TechniqueLibraryTab count prop | mitigate | Use a live query rather than the stale total `usage_count`, so editor-side detaches are reflected when the delete dialog opens (Pitfall 5). |
| T-146-11 | Spoofing / classic web threats | — | accept | Not applicable: single-user local desktop app, no network/auth surface. |
</threat_model>

<verification>
- `pnpm test -- tests/techniques/TechniqueDeleteDialog.test.tsx` GREEN; `pnpm test` full suite GREEN; `pnpm build` clean.
- Manual (UAT, see VALIDATION.md): delete a technique used by ≥1 recipe; confirm the count + consequence label show, then verify the recipe content survives (now plain) after delete.
- Grep confirms `useDeleteTechnique` uses `detachAllAndDeleteTechnique` and the dialog has no remaining `usageCount` reference.
</verification>

<success_criteria>
- Deleting a technique with live instances auto-detaches them first; no recipe content lost (SAFE-03).
- Delete dialog surfaces the non-detached live-instance count and names the consequence on the confirm button (SAFE-03).
- Case A (no live instances) keeps the existing simple copy + "Delete" label.
- Recipe-level + technique-level caches invalidated after delete.
</success_criteria>

<output>
Create `.planning/phases/146-detach-safety-rails/146-03-SUMMARY.md` when done.
</output>

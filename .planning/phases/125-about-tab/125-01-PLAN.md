---
phase: 125-about-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/settings/AboutTab.tsx
  - src/app/settings/page.tsx
  - tests/settings/AboutTab.test.tsx
  - tests/settings/SettingsPage.test.tsx
autonomous: true
requirements: [ABT-01, ABT-02, ABT-03]

must_haves:
  truths:
    - "D-01: Three stacked sections in vertical flow — App Identity, Data Stats, Credits"
    - "D-02: No card wrappers — simple heading + content pairs"
    - "D-03: Display unit count, faction count, and Wahapedia data date only"
    - "D-04: Show 'Not imported yet' when udb_meta has no row"
    - "D-05: Wahapedia attribution with no clickable links"
    - "D-06: Tech stack list — Tauri 2, React, TypeScript, SQLite"
    - "D-07: HobbyForge app name and one-line description at top"
    - "D-08: Purpose-built AboutTab component, reuse hooks not VersionInfoCard"
    - "D-09: Single file src/features/settings/AboutTab.tsx"
    - "About tab displays the current app version from tauri.conf.json (ABT-01)"
    - "About tab shows unit count, faction count, and Wahapedia data date (ABT-02)"
    - "About tab displays Wahapedia attribution and tech stack list (ABT-03)"
    - "Loading skeletons appear while async data is fetching"
  artifacts:
    - path: "src/features/settings/AboutTab.tsx"
      provides: "About tab component with three sections"
      exports: ["AboutTab"]
    - path: "tests/settings/AboutTab.test.tsx"
      provides: "Unit tests for ABT-01, ABT-02, ABT-03"
  key_links:
    - from: "src/app/settings/page.tsx"
      to: "src/features/settings/AboutTab.tsx"
      via: "import and render in TabsContent value='about'"
      pattern: "AboutTab"
    - from: "src/features/settings/AboutTab.tsx"
      to: "@tauri-apps/api/app"
      via: "getVersion() in useEffect"
      pattern: "getVersion"
    - from: "src/features/settings/AboutTab.tsx"
      to: "src/hooks/useUdbMeta.ts"
      via: "useUdbMeta() hook call"
      pattern: "useUdbMeta"
---

<objective>
Build the About tab for the Settings page, replacing the placeholder content with a read-only informational display showing app version, data statistics, and credits/attribution.

Purpose: Completes the About tab (ABT-01, ABT-02, ABT-03) -- the final tab of the Settings page, giving users visibility into app identity, data provenance, and attribution.
Output: Working AboutTab component with tests, integrated into the Settings page.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-about-tab/125-CONTEXT.md
@.planning/phases/125-about-tab/125-PATTERNS.md
@.planning/phases/125-about-tab/125-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/hooks/useUdbMeta.ts:
```typescript
export interface UdbMeta {
  version: string;
  built_at: string;
  game_system: string;
  unit_count: number | null;
  faction_count: number | null;
}
// Hook returns UseQueryResult where data is UdbMeta | null
export function useUdbMeta(): UseQueryResult<UdbMeta | null>;
```

From @tauri-apps/api/app:
```typescript
export function getVersion(): Promise<string>;
```

From src/components/ui/skeleton:
```typescript
export function Skeleton({ className }: { className?: string }): JSX.Element;
```

From src/app/settings/page.tsx (lines 39-44 -- placeholder to replace):
```tsx
<TabsContent value="about" className="mt-4">
  <h2 className="text-lg font-semibold">About HobbyForge</h2>
  <p className="text-muted-foreground text-sm">
    App version, data statistics, and attribution — coming in the next update.
  </p>
</TabsContent>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AboutTab tests and component</name>
  <files>tests/settings/AboutTab.test.tsx, src/features/settings/AboutTab.tsx</files>
  <read_first>
    - src/features/data-health/VersionInfoCard.tsx (analog: getVersion pattern lines 50-57, formatBuiltAt lines 32-43, three-branch udbMeta render lines 93-100)
    - tests/data-health/versionInfoCard.test.tsx (analog: mock patterns for getVersion and useUdbMeta)
    - src/hooks/useUdbMeta.ts (hook interface, UdbMeta type)
    - .planning/phases/125-about-tab/125-PATTERNS.md (all code excerpts and test patterns)
    - .planning/phases/125-about-tab/125-UI-SPEC.md (copywriting contract, layout contract, typography)
  </read_first>
  <behavior>
    - Test: renders "HobbyForge" heading (h2) and app description text "Your personal Warhammer hobby command center." (ABT-01/D-07)
    - Test: renders app version "0.4.14" after getVersion resolves (ABT-01)
    - Test: shows Skeleton while appVersion is null (before getVersion resolves) (ABT-01)
    - Test: renders unit count and faction count from udbMeta — "500 units across 25 factions" (ABT-02/D-03)
    - Test: renders formatted data date from udbMeta.built_at (ABT-02/D-03)
    - Test: shows "Not imported yet" when udbMeta data is null (ABT-02/D-04)
    - Test: shows Skeleton elements when udbMetaLoading is true (ABT-02)
    - Test: renders Wahapedia attribution text containing "Wahapedia" (ABT-03/D-05)
    - Test: renders tech stack text containing "Tauri 2" and "React" and "TypeScript" and "SQLite" (ABT-03/D-06)
  </behavior>
  <action>
    RED: Create tests/settings/AboutTab.test.tsx with vi.mock blocks for "@tauri-apps/api/app" (getVersion returns "0.4.14") and "@/hooks/useUdbMeta" (useUdbMeta as vi.fn). Use the mock pattern from 125-PATTERNS.md: beforeEach sets mockUseUdbMeta to return data with unit_count 500, faction_count 25, built_at "2026-05-20T10:00:00Z", game_system "40k-10th", version "2026.05.20", isLoading false. Write all 9 test cases per the behavior block. Run tests -- all must fail (component does not exist yet).

    GREEN: Create src/features/settings/AboutTab.tsx as a named export function. Per D-01, render three stacked sections in a div with className "space-y-6" (per UI-SPEC spacing). Per D-02, no Card wrappers -- use heading + content pairs only.

    Section 1 (App Identity per D-07): h2 "HobbyForge" with text-lg font-semibold. Paragraph "Your personal Warhammer hobby command center." in text-sm text-muted-foreground. Version line: "Version" label followed by Skeleton (inline-block w-16 h-4 align-middle) when appVersion is null, or span with font-mono class showing appVersion string when loaded. Use the useState/useEffect pattern from VersionInfoCard: useState<string | null>(null), useEffect calls getVersion().then(setAppVersion).catch(() => setAppVersion("unknown")).

    Section 2 (Data Stats per D-03): h3 "Unit Database" with text-sm font-semibold text-muted-foreground uppercase tracking-wider. Three-branch render: udbMetaLoading shows stacked Skeletons (h-4 w-40 and h-4 w-32 in a space-y-1 div), udbMeta truthy shows "{unit_count ?? 0} units across {faction_count ?? 0} factions" and "Data date: {formatBuiltAt(built_at)}" in text-sm, udbMeta null shows "Not imported yet" in text-sm text-muted-foreground. Include inline formatBuiltAt helper (copy from VersionInfoCard lines 32-43: try new Date(iso).toLocaleDateString with year numeric, month short, day numeric; catch returns iso).

    Section 3 (Credits per D-05/D-06): h3 "Credits" with same sub-heading style. Attribution paragraph per UI-SPEC copywriting contract: "Unit rules data is sourced from Wahapedia (wahapedia.ru), an independent fan-made database. HobbyForge is not affiliated with Games Workshop or Wahapedia." in text-sm text-muted-foreground. "Built with" micro-label (text-xs font-semibold uppercase tracking-wider) followed by "Tauri 2, React, TypeScript, SQLite" in text-sm text-muted-foreground. No clickable links per D-05.

    Imports: useEffect and useState from react, getVersion from @tauri-apps/api/app, Skeleton from @/components/ui/skeleton, useUdbMeta from @/hooks/useUdbMeta.

    Run tests -- all must pass.
  </action>
  <verify>
    <automated>pnpm test -- tests/settings/AboutTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tests/settings/AboutTab.test.tsx exists with 9+ test cases covering ABT-01/02/03
    - src/features/settings/AboutTab.tsx exists with named export `AboutTab`
    - AboutTab imports getVersion from "@tauri-apps/api/app" and useUdbMeta from "@/hooks/useUdbMeta"
    - Component renders three sections: App Identity (h2 "HobbyForge"), Data Stats (h3 "Unit Database"), Credits (h3 "Credits")
    - No Card/CardHeader/CardContent imports -- D-02 no card wrappers
    - formatBuiltAt function defined locally in AboutTab.tsx (not imported from VersionInfoCard)
    - Three-branch render for udbMeta: loading -> Skeleton, null -> "Not imported yet", data -> formatted stats
    - appVersion null -> Skeleton, string -> rendered in font-mono span
    - All tests pass: pnpm test -- tests/settings/AboutTab.test.tsx exits 0
  </acceptance_criteria>
  <done>AboutTab component renders all three sections with correct async loading states; all unit tests pass covering version display, data stats with null/loading/loaded states, and credits content.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate AboutTab into SettingsPage and fix test regressions</name>
  <files>src/app/settings/page.tsx, tests/settings/SettingsPage.test.tsx</files>
  <read_first>
    - src/app/settings/page.tsx (current placeholder at lines 39-44)
    - tests/settings/SettingsPage.test.tsx (existing mocks and test structure)
    - src/features/settings/AboutTab.tsx (just created -- verify export name)
    - .planning/phases/125-about-tab/125-PATTERNS.md (integration point section and SettingsPage test update section)
  </read_first>
  <action>
    In src/app/settings/page.tsx: Add import for AboutTab from "@/features/settings/AboutTab" at top of file (after existing imports). Replace the placeholder content inside TabsContent value="about" (lines 40-43: the h2 and p elements) with a single element: AboutTab component. Keep the TabsContent wrapper and its className="mt-4" unchanged.

    In tests/settings/SettingsPage.test.tsx: Add two vi.mock blocks immediately after the existing useAppSettings mock block (after line 7). First mock: vi.mock("@tauri-apps/api/app", () => ({ getVersion: vi.fn(() => Promise.resolve("0.4.14")) })). Second mock: vi.mock("@/hooks/useUdbMeta", () => ({ useUdbMeta: vi.fn().mockReturnValue({ data: null, isLoading: false }) })). This prevents AboutTab async hooks from throwing in the SettingsPage test environment (per RESEARCH.md Pitfall 4).

    Run full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>pnpm test -- tests/settings/SettingsPage.test.tsx tests/settings/AboutTab.test.tsx</automated>
    <human-check>Open the Settings page in the app (pnpm tauri dev), click the About tab, verify: (1) "HobbyForge" heading and description visible, (2) version number shown, (3) data stats or "Not imported yet" shown, (4) Wahapedia attribution and tech stack visible, (5) no card borders or wrappers around sections.</human-check>
  </verify>
  <acceptance_criteria>
    - src/app/settings/page.tsx imports AboutTab from "@/features/settings/AboutTab"
    - TabsContent value="about" contains only AboutTab component (no placeholder h2/p)
    - tests/settings/SettingsPage.test.tsx has vi.mock for "@tauri-apps/api/app" and "@/hooks/useUdbMeta"
    - pnpm test -- tests/settings/SettingsPage.test.tsx exits 0 (all 5 existing tests pass)
    - pnpm test -- tests/settings/AboutTab.test.tsx exits 0 (all tests still pass)
    - pnpm build succeeds (TypeScript compilation clean)
  </acceptance_criteria>
  <done>AboutTab integrated into Settings page replacing placeholder; all settings tests pass including regressions from AboutTab async hooks; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. This tab is read-only, renders no user input, makes no mutations, and accesses only internal app data (version string from Tauri config, counts from pre-built SQLite table).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Information Disclosure | AboutTab version display | accept | App version is not sensitive; it is already visible in window title bar and Tauri config |
| T-125-02 | Information Disclosure | AboutTab data stats | accept | Unit/faction counts are aggregate numbers from a pre-built public-knowledge database; no PII |
</threat_model>

<verification>
- `pnpm test` -- full test suite passes (no regressions)
- `pnpm build` -- TypeScript compilation succeeds
- About tab renders all three sections when navigated to in the app
</verification>

<success_criteria>
- ABT-01: About tab shows app version matching tauri.conf.json via getVersion()
- ABT-02: About tab shows unit count, faction count, and data date from useUdbMeta(); shows "Not imported yet" when no data
- ABT-03: About tab shows Wahapedia attribution text and tech stack list (Tauri 2, React, TypeScript, SQLite)
- All tests pass, build clean, no regressions in existing SettingsPage tests
</success_criteria>

<output>
Create `.planning/phases/125-about-tab/125-01-SUMMARY.md` when done
</output>

---
phase: 16
slug: design-overhaul
status: draft
shadcn_initialized: true
preset: none
created: 2026-05-04
---

# Phase 16 — UI Design Contract

> Visual and interaction contract for the Design Overhaul phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (new-york style, zinc base) |
| Preset | not applicable — no preset string; config detected from components.json |
| Component library | radix-ui (confirmed via `npx shadcn info`) |
| Icon library | lucide-react 0.460.0 (existing project dependency) |
| Font | Geist Sans via `@fontsource-variable/geist` — variable weight, installed as new npm dep; `@import` in `src/styles/globals.css`; applied as `font-family: 'Geist Variable', ui-sans-serif, system-ui, sans-serif` on `body` |

**Source:** CONTEXT.md §Typography ("Geist Sans is the recommended choice — used by Linear and Vercel, available via `@fontsource-variable/geist`"). Tailwind v4 integration: declare `--font-sans: 'Geist Variable', ui-sans-serif, system-ui, sans-serif` inside `@theme inline {}` in `src/styles/globals.css`, alongside the existing faction-accent token. No Tailwind config file needed (v4 CSS-first).

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding, inline chip spacing |
| sm | 8px | Compact element spacing, nav item icon-to-label gap |
| md | 16px | Default element spacing, input padding, card internal padding |
| lg | 24px | Section padding (current page `p-6` baseline preserved) |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Empty-state vertical padding (current `py-16` maps to 64px — see exception) |
| 3xl | 64px | Page-level spacing / hero section vertical breathing room |

**Exceptions:**
- Empty state containers: `py-16` (64px top/bottom) — intentional, provides visual weight at page center. This is not a spacing token; it is a layout override for a specific structural pattern.
- Touch/click targets: minimum 32px tall for all interactive nav items and buttons (current `py-1.5` on NavItem = 28px + 4px border = 32px — acceptable, do not reduce).
- Page outer wrapper: keep current `flex flex-col gap-6 p-6` (`24px padding, 24px gap`). Phase 16 upgrades typography and content within pages; it does not restructure the outer wrapper padding.

**Source:** CONTEXT.md §Code Context ("All page files use `flex flex-col gap-6 p-6` as top-level wrapper — spacing refinements apply here"). Standard 8-point scale applied as default.

---

## Typography

| Role | Size | Weight | Line Height | Class |
|------|------|--------|-------------|-------|
| Body | 14px | 400 (regular) | 1.5 | `text-sm font-normal` |
| Label / UI text | 14px | 600 (semibold) | 1.4 | `text-sm font-semibold` |
| Card title / sub-heading | 16px | 600 (semibold) | 1.4 | `text-base font-semibold` |
| Page heading | 28px | 600 (semibold) | 1.2 | `text-3xl font-semibold tracking-tight` |

**Only two weights are used across the entire spec: 400 (font-normal) for body text and 600 (font-semibold) for all UI labels, card titles, and headings. Do not introduce font-medium (500) or font-bold (700) anywhere in Phase 16.**

**Numeric values:** Add `tabular-nums` class to all numeric displays: painted percentages, points totals, stat block values (M/T/Sv/W/Ld/OC), spend amounts formatted by `formatCurrency`. This is a single class addition at the render site.

**Heading upgrade rule:** Every `<h1 className="text-xl font-semibold">` in all `*Page.tsx` files becomes `<h1 className="text-3xl font-semibold tracking-tight">`. This is the single highest-impact change. Card titles (`CardTitle`, section headers within cards) remain at 16px / semibold to preserve hierarchy.

**Source:** CONTEXT.md §Typography ("upgrade from current `text-xl font-semibold` to clearly dominant headings — `text-2xl` or `text-3xl`, `tracking-tight`"; "`text-3xl` chosen here over text-2xl to match Linear's heading weight contrast at desktop densities"; "add `tabular-nums` Tailwind class to all numeric values"). Body at 14px is the existing shadcn new-york default; kept consistent. Weights collapsed from 4 to 2 per checker revision (2026-05-04): font-medium (500) removed in favour of font-semibold (600) for labels; font-bold (700) replaced by font-semibold (600) for headings — tracking-tight carries the hierarchy at 28px without needing 700.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `hsl(240 10% 3.9%)` — `--background` | Page background — all `*Page.tsx` root backgrounds |
| Secondary (30%) | `hsl(240 10% 5.9%)` — `--card` | Cards, sidebar (`--sidebar`), nav background, sheet panels |
| Accent (10%) | `var(--faction-accent)` — runtime-mutable, default zinc-500 `#71717a` | See reserved-for list below |
| Destructive | `hsl(0 72.2% 50.6%)` — `--destructive` | Delete buttons, delete confirmation dialogs only |

**Accent reserved for:**
1. Primary button background (`variant="default"`) — already using `bg-faction-accent` from Phase 10
2. Active nav item background in sidebar (`bg-faction-accent`) — already applied in NavItem
3. Faction summary card active ring on Dashboard (`ring-faction-accent`)
4. Painting-status ring stroke for the "active project" indicator on UnitGallery cards
5. Active faction badge on FactionSummaryCard

**NOT used for:** table row hover states, card hover borders, input focus rings (these use `--ring` from the zinc token set), status badges (those encode painting state independently), or any passive text.

**Card elevation:** Replace current flat `border border-border` cards with a subtle layered treatment: `bg-card border border-border/60 shadow-sm` — adds 1px border at 60% opacity and a `shadow-sm` (2px blur, not heavy). This distinguishes cards from the page background without high contrast. Apply to all `<Card>` shadcn components used on the content pages.

**Source:** CONTEXT.md §Claude's Discretion ("Card elevation / surface depth decisions (subtle shadow improvements, border treatments)"). Color values from `src/styles/globals.css` `.dark` block — dark-mode-first confirmed by PROJECT.md.

---

## Sidebar Polish Contract

The sidebar structure, routing, and collapse mechanics from Phase 10 are locked. This phase polishes the expanded-state visual presentation only.

### App wordmark area
- Top of the sidebar, above the nav list: render `"HobbyForge"` in `text-base font-semibold tracking-tight` with a small inline icon (recommend lucide `Sword` — 16×16, `text-faction-accent`).
- Container: `flex items-center gap-2 px-3 py-4 border-b border-border/40` — a subtle separator line divides the wordmark from the nav items.
- In collapsed mode: show only the icon (16×16), centered. The wordmark text gets `sr-only` (same pattern as NavItem label in collapsed mode).

### Nav section grouping
- Group nav items with a `text-xs font-semibold text-muted-foreground uppercase tracking-widest` section label:
  - **Manage** — Dashboard, Collection, Painting Projects
  - **Inventory** — Paints, Recipes
  - **Tracking** — Army Lists, Spending
- Section labels: `px-3 pt-4 pb-1` spacing. In collapsed mode: hide section labels entirely (collapsed sidebar shows only icons).

### Active nav item
- Current: `bg-faction-accent font-semibold text-white` (from Phase 10) — keep this.
- Polish: add `rounded-md` (already present) and ensure `px-2 py-2` (increase from current `py-1.5` to `py-2` for more visual weight on the active state).

### Icon-to-label spacing
- Current NavItem gap: `gap-2` (8px). Upgrade to `gap-4` (16px) for the expanded state. This matches Linear's nav density.

**Source:** CONTEXT.md §Sidebar Polish ("app logo/wordmark area at the top of the sidebar", "better icon-to-label spacing", "active item highlight using bg-faction-accent", "visual nav section grouping"). Phase 10 mechanics (`useSidebarCollapsed`) must not be touched. Gap revised from gap-3 (12px) to gap-4 (16px) per checker revision (2026-05-04): 12px is not in the approved spacing scale {4, 8, 16, 24, 32, 48, 64}.

---

## Page Header Contract

Every page header follows this exact pattern (replaces current `flex items-center justify-between` + `text-xl font-semibold`):

```
<div className="flex items-center justify-between pb-6 border-b border-border/40">
  <div>
    <h1 className="text-3xl font-semibold tracking-tight">{Page Title}</h1>
    <p className="text-sm text-muted-foreground mt-1">{Page subtitle}</p>
  </div>
  <div className="flex items-center gap-2">{CTA buttons}</div>
</div>
```

**Page subtitles (one line each):**

| Page | Subtitle |
|------|---------|
| Dashboard | "Your hobby command center at a glance" |
| Collection | "All units you own, tracked and filterable" |
| Painting Projects | "Active units being worked on right now" |
| Paints | "Your paint collection, linked to recipes" |
| Army Lists | "Points-tracked lists for the tabletop" |
| Recipes | "Documented paint schemes for your models" |
| Spending | "Total hobby spend tracked to the penny" |

The subtitle is `text-sm text-muted-foreground` — it disappears naturally and provides breathing room between the bold heading and the page content.

**Source:** CONTEXT.md §Typography ("Every page title should be visually stronger than any card title"; "clear visual break (border or spacing) separated from content"). Standard `border-b border-border/40` gives a Linear-style hairline separator.

---

## Empty State Contract

All empty state components follow this exact layout. No illustrated art, no elaborate visuals — icon + strong headline + specific helper + CTA.

### Structural pattern (all pages except Dashboard welcome)

```
<div className="flex flex-col items-center justify-center gap-3 py-16 text-center">
  <div className="rounded-xl bg-muted/40 p-4">
    <{Icon} className="h-8 w-8 text-muted-foreground" />
  </div>
  <div className="space-y-1">
    <p className="text-base font-semibold">{Headline}</p>
    <p className="text-sm text-muted-foreground max-w-xs">{Helper text}</p>
  </div>
  <Button className="mt-2">{CTA label}</Button>
</div>
```

The icon gets a `bg-muted/40 rounded-xl p-4` container — a subtle pill background that elevates the icon above the plain muted treatment currently used (just `text-muted-foreground` at 48×48). Icon size reduces to `h-8 w-8` (32×32) inside the container — same visual weight, more refined treatment.

Note: `gap-3` (12px) in the empty-state structural pattern above is an internal layout gap for the flex column within this self-contained component. The approved spacing scale governs page-level and component-to-component spacing; this internal 12px gap is an accepted layout exception for this pattern only.

### Empty state copy per page

| Page | Icon | Headline | Helper text | CTA label |
|------|------|----------|-------------|-----------|
| Dashboard (has-units, no active faction) | `Star` | "No faction selected" | "Select a faction from the cards below to personalise your command center." | — (no CTA, action is inline) |
| Collection (no-data) | `ShieldOff` | "No units yet" | "Add your first unit to start tracking what you own and how far along it is." | "Add unit" |
| Collection (filtered, no results) | `FilterX` | "No units match" | "Your current filters returned nothing. Clear a filter to see more units." | "Clear filters" |
| Painting Projects | `Layers` | "No active projects" | "Mark a unit as an active project from Collection to see it here." | "Go to Collection" |
| Paints | `Palette` | "No paints yet" | "Add the paints you own to link them to recipes and track what you're running low on." | "Add paint" |
| Army Lists | `Swords` | "No army lists yet" | "Build a list to track points, units, and your battle-ready percentage." | "New list" |
| Recipes | `BookOpen` | "No recipes yet" | "Document a paint scheme to keep your colour choices consistent across models." | "New recipe" |
| Spending | `Receipt` | "No spend logged yet" | "Add purchase prices to units and paints from their detail sheets to track your spend here." | — (no CTA — spend is logged from other pages) |

### Dashboard welcome screen (first-run: `stats.hasUnits === false`)

Full-page welcome experience. Replaces the current `DashboardEmptyState`.

```
<div className="flex flex-col items-center justify-center gap-6 py-20 text-center">
  <div className="flex items-center gap-3">
    <Sword className="h-8 w-8 text-faction-accent" />
    <span className="text-2xl font-semibold tracking-tight">HobbyForge</span>
  </div>
  <div className="space-y-2 max-w-sm">
    <p className="text-xl font-semibold">Your collection is empty</p>
    <p className="text-sm text-muted-foreground">
      HobbyForge tracks what you own, what's painted, and what's ready to play.
      Add your first unit to get started.
    </p>
  </div>
  <Button size="lg" onClick={() => navigate({ to: "/collection" })}>
    Add your first unit
  </Button>
</div>
```

**Source:** CONTEXT.md §Empty States ("icon + polished copy", "Dashboard special case: welcome screen — first-run experience — app name/wordmark, clear CTA to add first unit, brief one-line explanation of what the app does").

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — add unit | "Add unit" |
| Primary CTA — add paint | "Add paint" |
| Primary CTA — new army list | "New list" |
| Primary CTA — new recipe | "New recipe" |
| Primary CTA — first-run welcome | "Add your first unit" |
| Empty state — no units (headline) | "No units yet" |
| Empty state — no units (helper) | "Add your first unit to start tracking what you own and how far along it is." |
| Empty state — filtered no results (headline) | "No units match" |
| Empty state — filtered no results (helper) | "Your current filters returned nothing. Clear a filter to see more units." |
| Empty state — no projects (headline) | "No active projects" |
| Empty state — no projects (helper) | "Mark a unit as an active project from Collection to see it here." |
| Empty state — no paints (headline) | "No paints yet" |
| Empty state — no paints (helper) | "Add the paints you own to link them to recipes and track what you're running low on." |
| Empty state — no army lists (headline) | "No army lists yet" |
| Empty state — no army lists (helper) | "Build a list to track points, units, and your battle-ready percentage." |
| Empty state — no recipes (headline) | "No recipes yet" |
| Empty state — no recipes (helper) | "Document a paint scheme to keep your colour choices consistent across models." |
| Empty state — no spend (headline) | "No spend logged yet" |
| Empty state — no spend (helper) | "Add purchase prices to units and paints from their detail sheets to track your spend here." |
| Error state | "Something went wrong loading your {resource}. Try restarting the app." |
| Destructive — delete unit | Confirmation dialog: "Delete {unit name}? This will also remove {N} photo(s) from disk. This cannot be undone." |
| Destructive — delete paint | Confirmation dialog: "Delete {paint name}? Any recipes that reference this paint will lose the link. This cannot be undone." |
| Destructive — delete army list | Confirmation dialog: "Delete {list name}? This cannot be undone." |
| Destructive — delete recipe | Confirmation dialog: "Delete {recipe name}? This cannot be undone." |
| Destructive — delete painting session | No confirmation dialog — inline trash icon with optimistic rollback (established in Phase 13 — do not change). |

**Copy tone:** Direct, lowercase CTAs ("Add unit" not "ADD UNIT"), British English spelling (the user is Belgian/European; "colour" in recipe helper text above is intentional), present-tense action verbs.

**Source:** CONTEXT.md §Empty States (icon + strong headline + specific helper text pattern). Destructive confirmations from existing Phase 8 / Phase 13 patterns in STATE.md accumulated context.

---

## Loading State Contract

Skeleton loading states use the existing `<Skeleton>` shadcn component. Phase 16 standardises the visual pattern:

- Page heading skeleton: `<Skeleton className="h-9 w-48" />` (matches upgraded `text-3xl` heading height)
- Subtitle skeleton: `<Skeleton className="h-4 w-64 mt-1" />`
- Card skeleton: `<Skeleton className="h-32 w-full rounded-xl" />`
- Table row skeleton: `<Skeleton className="h-10 w-full" />` (unchanged from existing pattern)

Skeleton bg uses `bg-muted` (already the shadcn Skeleton default). No custom shimmer animation — the existing Tailwind `animate-pulse` from shadcn Skeleton is correct.

---

## Component Inventory (Phase 16 touch list)

All files to be polished. No new components created for this phase — existing components are upgraded in-place.

### Page files (heading upgrade + subtitle addition)
- `src/features/dashboard/DashboardPage.tsx`
- `src/features/units/CollectionPage.tsx`
- `src/features/painting-projects/PaintingProjectsPage.tsx`
- `src/features/paints/PaintsPage.tsx`
- `src/features/army-lists/ArmyListsPage.tsx`
- `src/features/recipes/RecipesPage.tsx`
- `src/features/spending/SpendingPage.tsx` (Phase 14 output)

### Empty state components (full icon + copy upgrade)
- `src/features/dashboard/DashboardEmptyState.tsx`
- `src/features/units/CollectionEmptyState.tsx`
- `src/features/painting-projects/KanbanEmptyState.tsx`
- `src/features/paints/PaintsEmptyState.tsx`
- `src/features/army-lists/ArmyListsEmptyState.tsx`
- `src/features/recipes/RecipeEmptyState.tsx`

### Sidebar components (visual polish)
- `src/components/AppSidebar.tsx` — wordmark area + nav section grouping
- `src/components/common/NavItem.tsx` — `gap-2` → `gap-4`, `py-1.5` → `py-2`

### Global styles (font + token addition)
- `src/styles/globals.css` — `@import "@fontsource-variable/geist"` + `--font-sans` in `@theme inline` + `font-family` on `body`

### Numeric display sites (tabular-nums addition)
- `src/features/dashboard/StatCard.tsx` — stat numbers
- `src/features/units/UnitGallery.tsx` — painting percentage per card
- `src/features/units/UnitDetailSheet.tsx` — spend display (Phase 14)
- `src/features/spending/SpendingPage.tsx` — all currency figures (Phase 14)
- `src/features/units/UnitPlaybook.tsx` (or PlaybookTab) — stat block values (M/T/Sv/W/Ld/OC)
- `src/features/army-lists/ArmyListCard.tsx` — points totals

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | badge, button, card, command, dialog, drawer, form, input, label, popover, progress, select, separator, sheet, sidebar, skeleton, sonner, table, tabs, tooltip | not required |

No third-party registries declared. Registry safety gate: not applicable for this phase.

**Source:** `npx shadcn info` output — 2026-05-04. All 20 installed components are from the shadcn official registry only.

---

## Interaction Contract

### Hover states
- Nav items (inactive): `hover:bg-accent hover:text-accent-foreground` — already in NavItem, keep.
- Cards: `hover:shadow-md transition-shadow duration-150` — add to interactive cards (ArmyListCard, FactionSummaryCard, UnitGallery card). Static display cards (StatCard) do not get hover treatment.
- Table rows: `hover:bg-muted/50` — keep existing pattern.

### Focus states
- All interactive elements: use existing `focus-visible:ring-2 focus-visible:ring-ring` from shadcn defaults. Do not change.

### Transition durations
- All `transition-*` utilities: `duration-150` (fast — matches Linear's snappy feel). Do not use `duration-300` or longer for interactive element state changes.

### Reduced motion
- Phase 11's `useCountUp` hook already gates animation behind `prefers-reduced-motion`. No new animation introduced in Phase 16.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

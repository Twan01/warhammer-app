---
phase: 16-design-overhaul
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/features/dashboard/DashboardPage.tsx
  - src/features/dashboard/StatCard.tsx
  - src/features/units/CollectionPage.tsx
  - src/features/units/UnitGallery.tsx
  - src/features/units/UnitDetailSheet.tsx
autonomous: true
requirements:
  - PAGE-HEADER-DASHBOARD
  - PAGE-HEADER-COLLECTION
  - TABULAR-NUMS-DASHBOARD
  - TABULAR-NUMS-COLLECTION
  - CARD-ELEVATION-STATCARD
must_haves:
  truths:
    - "Dashboard page header h1 reads 'Dashboard' in text-3xl font-semibold tracking-tight, with the subtitle 'Your hobby command center at a glance' directly below"
    - "Collection page header h1 reads 'Collection' in text-3xl font-semibold tracking-tight, with the subtitle 'All units you own, tracked and filterable' directly below"
    - "Both page headers are separated from their page content by a hairline border-b border-border/40 with pb-6"
    - "Every numeric value rendered in StatCard, UnitGallery card percentages, and UnitDetailSheet purchase price uses the tabular-nums class — column alignment is clean"
    - "StatCard uses bg-card border border-border/60 shadow-sm (replaces flat border) — subtle depth without hover treatment (StatCard is static)"
    - "UnitGallery cards use bg-card border border-border/60 shadow-sm hover:shadow-md transition-shadow duration-150 (interactive cards get hover treatment)"
    - "Existing DashboardPage and UnitGallery tests continue to pass without modification"
  artifacts:
    - path: "src/features/dashboard/DashboardPage.tsx"
      provides: "Dashboard page with upgraded header"
      contains: "text-3xl font-semibold tracking-tight"
    - path: "src/features/dashboard/StatCard.tsx"
      provides: "StatCard with tabular-nums and shadow-sm elevation"
      contains: "tabular-nums"
    - path: "src/features/units/CollectionPage.tsx"
      provides: "Collection page with upgraded header"
      contains: "text-3xl font-semibold tracking-tight"
    - path: "src/features/units/UnitGallery.tsx"
      provides: "Gallery cards with tabular-nums and shadow elevation"
      contains: "tabular-nums"
    - path: "src/features/units/UnitDetailSheet.tsx"
      provides: "Detail sheet with tabular-nums on currency display"
      contains: "tabular-nums"
  key_links:
    - from: "DashboardPage h1"
      to: "Phase 16 typography contract"
      via: "className='text-3xl font-semibold tracking-tight'"
      pattern: "text-3xl\\s+font-semibold\\s+tracking-tight"
    - from: "CollectionPage h1"
      to: "Phase 16 typography contract"
      via: "className='text-3xl font-semibold tracking-tight'"
      pattern: "text-3xl\\s+font-semibold\\s+tracking-tight"
    - from: "page header div"
      to: "page content"
      via: "border-b border-border/40 pb-6 hairline separator"
      pattern: "pb-6\\s+border-b\\s+border-border/40"
---

<objective>
Upgrade the Dashboard and Collection cluster to the Phase 16 visual contract: page headers (h1 → text-3xl, subtitle line, hairline border separator), tabular-nums on every numeric display, and card elevation (shadow-sm + bg-card + border-border/60) for StatCard and UnitGallery cards. UnitDetailSheet's purchase price display also gets tabular-nums.

Purpose: Dashboard is the landing page on app launch — its header is the first thing the user reads. Collection is the most-used page. The header upgrade alone produces an immediately visible improvement; the tabular-nums + card elevation make every numeric column look clean and every card feel intentionally elevated above the page background.

Output: Five files modified — DashboardPage.tsx (header upgrade), StatCard.tsx (tabular-nums + shadow-sm), CollectionPage.tsx (header upgrade), UnitGallery.tsx (tabular-nums + card hover shadow), UnitDetailSheet.tsx (tabular-nums on currency). No DashboardEmptyState, CollectionEmptyState, or UnitFilters changes (those are scoped to plans 16-06 / 16-07).
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-design-overhaul/16-CONTEXT.md
@.planning/phases/16-design-overhaul/16-RESEARCH.md
@.planning/phases/16-design-overhaul/16-UI-SPEC.md

<interfaces>
<!-- Universal page header pattern (UI-SPEC §Page Header Contract) -->
<!-- Replaces: <div className="flex items-center justify-between"><h1 className="text-xl font-semibold">Title</h1><Button>CTA</Button></div> -->
<!-- With: -->
```tsx
<div className="flex items-center justify-between pb-6 border-b border-border/40">
  <div>
    <h1 className="text-3xl font-semibold tracking-tight">{Page Title}</h1>
    <p className="text-sm text-muted-foreground mt-1">{subtitle}</p>
  </div>
  <div className="flex items-center gap-2">{CTA buttons}</div>
</div>
```

Subtitles per UI-SPEC §Page Header Contract:
- Dashboard: "Your hobby command center at a glance"
- Collection: "All units you own, tracked and filterable"

Card elevation classes (UI-SPEC §Color):
- Static cards (StatCard): `bg-card border border-border/60 shadow-sm`
- Interactive cards (UnitGallery card, ArmyListCard): `bg-card border border-border/60 shadow-sm hover:shadow-md transition-shadow duration-150`

Tabular-nums sites:
- StatCard.tsx: stat number span (currently `text-3xl font-semibold` — add `tabular-nums`)
- UnitGallery.tsx: painted percentage display, models · pts row
- UnitDetailSheet.tsx: purchase price display via `formatCurrency()` (added in Phase 14-02)

Existing test files (must continue to pass):
- tests/dashboard/DashboardPage.test.tsx — renders without error, checks stat values
- tests/collection/UnitGallery.test.tsx — renders card grid, painting ring percentage
- tests/collection/CollectionPage.test.tsx — renders, view-toggle integration
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Upgrade DashboardPage header + StatCard tabular-nums and elevation</name>
  <files>src/features/dashboard/DashboardPage.tsx, src/features/dashboard/StatCard.tsx</files>
  <read_first>
    - src/features/dashboard/DashboardPage.tsx (read entire file — locate the existing h1 and its surrounding flex container)
    - src/features/dashboard/StatCard.tsx (read entire file — locate the stat number span and the Card root className)
    - tests/dashboard/DashboardPage.test.tsx (verify what selectors the tests use — typically getByRole('heading') by name)
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md §Page Header Contract (subtitle copy verbatim)
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md §Color (card elevation rule for static StatCard — shadow-sm WITHOUT hover treatment per Pitfall in RESEARCH.md)
    - .planning/phases/16-design-overhaul/16-RESEARCH.md §Pattern 4 (tabular-nums) and §Pattern 5 (card elevation — StatCard does NOT get hover:shadow-md)
  </read_first>
  <action>
    Edit A — DashboardPage.tsx page header. Locate the existing div containing `<h1 className="text-xl font-semibold">Dashboard</h1>` (the page-level header, NOT any section h2). Replace the entire wrapping div + h1 + CTA structure with:
    ```tsx
    <div className="flex items-center justify-between pb-6 border-b border-border/40">
      <div>
        <h1 className="text-3xl font-semibold tracking-tight">Dashboard</h1>
        <p className="text-sm text-muted-foreground mt-1">Your hobby command center at a glance</p>
      </div>
      <div className="flex items-center gap-2">
        {/* existing CTA buttons go here verbatim — preserve every button currently in the right slot */}
      </div>
    </div>
    ```
    Preserve any CTA buttons (or lack thereof) currently rendered on the right side of the header — read the source first and copy them inside the new `<div className="flex items-center gap-2">` slot. If there are no CTA buttons currently, render the empty `<div className="flex items-center gap-2">` (or omit it — but DO keep the outer flex container with `justify-between` so the structure is identical to all other pages).

    Edit B — StatCard.tsx stat number tabular-nums. Locate the span/div that renders the stat value (currently `text-3xl font-semibold` — per UI-SPEC §Component Inventory and RESEARCH §Pattern 4). Add the `tabular-nums` class. The full className for the value display becomes:
    ```tsx
    <span className="text-3xl font-semibold tabular-nums">{displayValue}</span>
    ```
    If StatCard already imports the AnimatedNumber sub-component (Phase 11 wiring), apply `tabular-nums` to the wrapping span that contains AnimatedNumber output. Do NOT add tabular-nums to label text or icon containers — only to the numeric display.

    Edit C — StatCard.tsx Card elevation. Locate the root `<Card className="...">` element. Update className using `cn()` (already imported in StatCard) so the final class set INCLUDES:
    `bg-card border border-border/60 shadow-sm`

    Important: StatCard is a STATIC display card per RESEARCH §Pattern 5 anti-pattern note — DO NOT add `hover:shadow-md` or any hover transition. Replace any existing `border border-border` (without opacity) with `border border-border/60`.

    Concretely: if the Card root currently has `className={cn("...existing...", "border border-border")}` or similar, change the existing border specifier to `border border-border/60` and append `bg-card shadow-sm` to the class set. Do NOT remove existing flexbox/padding/gap classes.

    Do NOT modify:
    - StatCard's animation logic, AnimatedNumber sub-component, or useCountUp wiring (Phase 11 contract)
    - DashboardPage's stats fetching, FactionSummaryCard rendering, or active-faction logic
    - The `Skeleton` loading state (handled in plan 16-08 manual review)
  </action>
  <verify>
    <automated>npx vitest run tests/dashboard/DashboardPage.test.tsx tests/dashboard/StatCard.test.tsx --reporter=verbose 2>&1 | tail -20</automated>

    Manual checks:
    1. `Get-Content src/features/dashboard/DashboardPage.tsx | Select-String -Pattern 'text-3xl font-semibold tracking-tight'` returns at least one match (page h1)
    2. `Get-Content src/features/dashboard/DashboardPage.tsx | Select-String -Pattern 'Your hobby command center at a glance'` returns exactly one match
    3. `Get-Content src/features/dashboard/DashboardPage.tsx | Select-String -Pattern 'pb-6 border-b border-border/40'` returns at least one match
    4. `Get-Content src/features/dashboard/DashboardPage.tsx | Select-String -Pattern 'text-xl font-semibold'` (the OLD h1 size) returns ZERO matches OR only matches that are clearly card titles (NOT the page h1)
    5. `Get-Content src/features/dashboard/StatCard.tsx | Select-String -Pattern 'tabular-nums'` returns at least one match
    6. `Get-Content src/features/dashboard/StatCard.tsx | Select-String -Pattern 'shadow-sm'` returns at least one match
    7. `Get-Content src/features/dashboard/StatCard.tsx | Select-String -Pattern 'hover:shadow'` returns ZERO matches (StatCard is static — no hover)
  </verify>
  <acceptance_criteria>
    - DashboardPage.tsx contains exactly one `<h1>` with className `text-3xl font-semibold tracking-tight` and inner text `Dashboard`
    - DashboardPage.tsx contains the literal subtitle string `Your hobby command center at a glance`
    - DashboardPage.tsx page-header wrapper div className contains `pb-6 border-b border-border/40`
    - DashboardPage.tsx no longer contains the page-level `text-xl font-semibold` h1 (any remaining `text-xl` matches must be section h2 elements, not the page h1 — verify by reading the file)
    - StatCard.tsx contains `tabular-nums` in the className of the numeric display span
    - StatCard.tsx Card root className contains `bg-card`, `border border-border/60`, `shadow-sm` (in any order, possibly via cn() composition)
    - StatCard.tsx does NOT contain `hover:shadow-md` (static card per Pattern 5)
    - StatCard.tsx does NOT contain `hover:shadow-sm` or `transition-shadow` on the Card root
    - `npx vitest run tests/dashboard/DashboardPage.test.tsx tests/dashboard/StatCard.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Dashboard page renders new header (text-3xl h1 + subtitle + hairline border), StatCard renders stat numbers with tabular-nums and a subtle bg-card + shadow-sm elevation. No hover treatment on StatCard. Existing dashboard tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Upgrade CollectionPage header + UnitGallery tabular-nums and card hover elevation</name>
  <files>src/features/units/CollectionPage.tsx, src/features/units/UnitGallery.tsx</files>
  <read_first>
    - src/features/units/CollectionPage.tsx (read entire file — locate the page header structure, may include search input, filter bar, view toggle alongside the h1)
    - src/features/units/UnitGallery.tsx (read entire file — find the painted percentage display and the model_count/points row, plus the Card root className for each unit card)
    - tests/collection/UnitGallery.test.tsx (verify how painted percentage and card structure are queried — Phase 12 tests assert ring rendering and card grid layout)
    - tests/collection/CollectionPage.test.tsx (verify the page header text query — typically getByRole('heading', { name: 'Collection' }))
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md §Page Header Contract (Collection subtitle verbatim)
    - .planning/phases/16-design-overhaul/16-RESEARCH.md §Pattern 5 (UnitGallery cards are INTERACTIVE — they DO get hover:shadow-md)
  </read_first>
  <action>
    Edit A — CollectionPage.tsx page header. Locate the existing h1 with `text-xl font-semibold` and inner text `Collection`. Replace its surrounding flex container + h1 with:
    ```tsx
    <div className="flex items-center justify-between pb-6 border-b border-border/40">
      <div>
        <h1 className="text-3xl font-semibold tracking-tight">Collection</h1>
        <p className="text-sm text-muted-foreground mt-1">All units you own, tracked and filterable</p>
      </div>
      <div className="flex items-center gap-2">
        {/* preserve every existing CTA button (Add unit / Import / etc.) verbatim */}
        {/* if the view toggle currently lives here, preserve it; if it lives elsewhere, leave that alone */}
      </div>
    </div>
    ```
    Read CollectionPage.tsx first to identify which buttons currently sit on the right of the h1 and copy them inside `<div className="flex items-center gap-2">`. The view toggle (table/gallery, from Phase 12) typically sits in the filter bar — leave it in place if so.

    Do NOT modify the search input, UnitFilters component, view-toggle wiring, UnitTable rendering, UnitGallery rendering, UnitDetailSheet portal mount, or onPhotoClick lightbox wiring.

    Edit B — UnitGallery.tsx tabular-nums on numeric displays. UnitGallery renders unit cards with:
    1. A painting percentage (e.g., `{percentage}%` near the PaintingRing)
    2. A "{model_count ?? "—"} models · {points ?? "—"} pts" row

    Add `tabular-nums` to BOTH numeric displays. Locate the percentage span (it has `text-sm` or similar) and add `tabular-nums` to its className. Locate the models/pts span and add `tabular-nums`. After this edit:
    - The painting percentage span className should match: `text-sm tabular-nums` (with any other existing classes preserved)
    - The models/pts span className should match: `text-sm text-muted-foreground tabular-nums`

    Edit C — UnitGallery.tsx card elevation with hover treatment. Locate the root `<Card className="...">` of each unit card (typically inside the `.map((unit) => ...)` render). Update its className to include:
    ```
    bg-card border border-border/60 shadow-sm hover:shadow-md transition-shadow duration-150
    ```
    Replace any existing `hover:bg-muted/50` on the Card root with the new hover-shadow approach (per RESEARCH §Pattern 5: "hover:bg-muted/50 on the interactive cards (ArmyListCard, UnitGallery card) is replaced by hover:shadow-md transition-shadow duration-150"). Preserve `cursor-pointer` and any onClick / role / tabIndex attributes — only the elevation/hover classes change.

    Do NOT modify:
    - PaintingRing rendering (Phase 12 contract — stroke='currentColor' + className='text-primary' must stay)
    - Faction badge rendering or color
    - Card grid layout (`grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3` etc.)
    - Click handlers, keyboard handlers, or aria-labels
  </action>
  <verify>
    <automated>npx vitest run tests/collection/UnitGallery.test.tsx tests/collection/CollectionPage.test.tsx --reporter=verbose 2>&1 | tail -25</automated>

    Manual checks:
    1. `Get-Content src/features/units/CollectionPage.tsx | Select-String -Pattern 'text-3xl font-semibold tracking-tight'` returns at least one match
    2. `Get-Content src/features/units/CollectionPage.tsx | Select-String -Pattern 'All units you own, tracked and filterable'` returns exactly one match
    3. `Get-Content src/features/units/UnitGallery.tsx | Select-String -Pattern 'tabular-nums'` returns at least two matches (percentage and models/pts row)
    4. `Get-Content src/features/units/UnitGallery.tsx | Select-String -Pattern 'hover:shadow-md'` returns at least one match
    5. `Get-Content src/features/units/UnitGallery.tsx | Select-String -Pattern 'transition-shadow duration-150'` returns at least one match
    6. `Get-Content src/features/units/UnitGallery.tsx | Select-String -Pattern 'border border-border/60'` returns at least one match
    7. `Get-Content src/features/units/UnitGallery.tsx | Select-String -Pattern 'hover:bg-muted/50'` returns ZERO matches (replaced by hover:shadow-md)
  </verify>
  <acceptance_criteria>
    - CollectionPage.tsx contains exactly one page-level h1 with className `text-3xl font-semibold tracking-tight` and inner text `Collection`
    - CollectionPage.tsx contains the literal subtitle string `All units you own, tracked and filterable`
    - CollectionPage.tsx page-header wrapper className contains `pb-6 border-b border-border/40`
    - UnitGallery.tsx painted-percentage span className contains `tabular-nums`
    - UnitGallery.tsx models/pts row span className contains `tabular-nums`
    - UnitGallery.tsx unit Card root className contains `bg-card`, `border border-border/60`, `shadow-sm`, `hover:shadow-md`, `transition-shadow duration-150`
    - UnitGallery.tsx does NOT contain `hover:bg-muted/50` on a Card element (replaced by shadow hover)
    - PaintingRing component file is NOT modified (Phase 12 contract preserved)
    - `npx vitest run tests/collection/UnitGallery.test.tsx tests/collection/CollectionPage.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Collection page header upgraded to text-3xl + subtitle + hairline border. UnitGallery cards render tabular-nums on percentages and pts/models, and have shadow-sm with hover:shadow-md interactive elevation. Phase 12 tests pass without modification.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add tabular-nums to UnitDetailSheet purchase price display</name>
  <files>src/features/units/UnitDetailSheet.tsx</files>
  <read_first>
    - src/features/units/UnitDetailSheet.tsx (read entire file — locate the formatCurrency() call site for purchase_price_pence display; per STATE.md "Phase 14 Plan 02: UnitDetailSheet had inline pence math `(/ 100).toFixed(2)` — replaced with formatCurrency()")
    - src/lib/formatCurrency.ts (or wherever formatCurrency is exported — check the import in UnitDetailSheet.tsx to find the path; this is for context only, NOT modified)
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md §Component Inventory (UnitDetailSheet listed as a tabular-nums target site)
    - .planning/phases/16-design-overhaul/16-RESEARCH.md §Pattern 4 (tabular-nums at render site)
  </read_first>
  <action>
    Locate the JSX node that renders the purchase price (the call site for `formatCurrency(unit.purchase_price_pence)` — added in Phase 14-02). It is wrapped in a span, p, or dd element with a className like `text-sm` or `text-base`.

    Add the `tabular-nums` class to that element's className. The minimal edit looks like:
    ```tsx
    // Before:
    <span className="text-sm">{formatCurrency(unit.purchase_price_pence)}</span>
    // After:
    <span className="text-sm tabular-nums">{formatCurrency(unit.purchase_price_pence)}</span>
    ```

    If the formatCurrency call site is rendered inside a description-list (`<dl>`/`<dt>`/`<dd>`) or a labeled row, apply `tabular-nums` to the element that wraps the currency string itself — NOT the surrounding label.

    Do NOT modify:
    - formatCurrency's signature or implementation
    - The purchase price form input (that lives in UnitSheet, not UnitDetailSheet)
    - The Tabs structure (Stats / Painting / Playbook / Journal — Phase 9 + 13 contract)
    - The onPhotoClick / sibling lightbox Dialog wiring (Phase 13 contract)
    - Any other text in UnitDetailSheet
  </action>
  <verify>
    <automated>npx vitest run tests/collection/UnitDetailSheet.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
    (If UnitDetailSheet.test.tsx does not exist as a standalone file, run the broader collection suite: `npx vitest run tests/collection/ --reporter=verbose 2>&1 | tail -20`)

    Manual checks:
    1. `Get-Content src/features/units/UnitDetailSheet.tsx | Select-String -Pattern 'tabular-nums'` returns at least one match
    2. `Get-Content src/features/units/UnitDetailSheet.tsx | Select-String -Pattern 'formatCurrency'` confirms the formatCurrency call site exists
    3. The line containing `formatCurrency(` should have `tabular-nums` in the same JSX element's className OR in the immediate parent element wrapping the currency display
  </verify>
  <acceptance_criteria>
    - `src/features/units/UnitDetailSheet.tsx` contains the literal substring `tabular-nums`
    - The element wrapping the `formatCurrency(...)` call has `tabular-nums` in its className
    - `formatCurrency` is still imported and called the same way (no API change)
    - `npx vitest run tests/collection/` exits 0 with all tests passing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    UnitDetailSheet renders the purchase price using `tabular-nums` so digits align cleanly. No structural changes to the sheet — only a class addition at the render site. Existing tests pass.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite ≥ 279 passing, 0 failing
- `npx tsc --noEmit` — no TS errors
- Manual visual check (Plan 16-08): Dashboard h1 visually dominant; Collection h1 + subtitle visible; StatCard has subtle elevation but no hover; UnitGallery cards lift on hover; UnitDetailSheet currency aligns cleanly
</verification>

<success_criteria>
- DashboardPage h1: `text-3xl font-semibold tracking-tight` + subtitle "Your hobby command center at a glance" + hairline border
- StatCard: `tabular-nums` on stat number; `bg-card border border-border/60 shadow-sm`; NO hover treatment
- CollectionPage h1: `text-3xl font-semibold tracking-tight` + subtitle "All units you own, tracked and filterable" + hairline border
- UnitGallery: `tabular-nums` on percentage and models/pts; cards use `bg-card border border-border/60 shadow-sm hover:shadow-md transition-shadow duration-150`
- UnitDetailSheet: `tabular-nums` on formatCurrency render site
- All Phase 11 (StatCard animation) and Phase 12 (PaintingRing, view toggle) contracts preserved
- Full test suite passes; no test modifications needed
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-overhaul/16-03-SUMMARY.md` documenting:
- Exact before/after of each page header div
- Where tabular-nums was added in StatCard, UnitGallery, UnitDetailSheet (with line numbers post-edit)
- Confirmation that StatCard has shadow-sm but NO hover (Pattern 5 anti-pattern check)
- Confirmation that UnitGallery has hover:shadow-md
- Vitest pass count
</output>

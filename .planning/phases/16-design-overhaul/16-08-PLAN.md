---
phase: 16-design-overhaul
plan: 08
type: execute
wave: 3
depends_on: ["16-01", "16-02", "16-03", "16-04", "16-05", "16-06", "16-07"]
files_modified: []
autonomous: false
requirements:
  - VISUAL-REVIEW-FINAL
must_haves:
  truths:
    - "All 7 pages render with text-3xl page headings + subtitle + hairline border in the live Tauri app"
    - "Sidebar wordmark, three section labels (Manage / Inventory / Tracking), and gap-4 nav density all render correctly when expanded; collapse mode hides section labels and renders icons only"
    - "Geist Variable font is the visible body font on every page (no system-font fallback)"
    - "Empty states across all pages match UI-SPEC: Dashboard welcome screen, Collection (both modes), Painting Projects, Paints, Army Lists, Recipes, Spending"
    - "Tabular-nums visibly aligns numbers in StatCard, UnitGallery, ArmyListCard, SpendingPage, UnitDetailSheet, PlaybookTab"
    - "Card elevation (bg-card + border-border/60 + shadow-sm) is visible on StatCard (no hover) and ArmyListCard / UnitGallery cards (with hover:shadow-md)"
    - "Phase 4 Kanban drag-and-drop, Phase 8 sibling-portal pattern, Phase 10 sidebar collapse, Phase 11 stat-counter animation, Phase 12 view toggle, Phase 13 Journal tab + photo timeline, and Phase 14 Spending data flow all still work end-to-end"
  artifacts:
    - path: ".planning/phases/16-design-overhaul/16-08-SUMMARY.md"
      provides: "Final manual smoke-test sign-off and Phase 16 completion record"
      contains: "All 7 pages reviewed"
  key_links:
    - from: "Manual smoke test"
      to: "Phase 16 visual review completion criterion"
      via: "User approves all 7 pages and shared components"
      pattern: "approved"
---

<objective>
Final visual review checkpoint for Phase 16. Phase 16 has no formal requirement IDs — the goal is "significantly improved visual design across all pages" — and the completion criterion per CONTEXT.md is "every page reviewed and polished systematically; done when all pages pass a visual review." This plan executes that review in the live Tauri app.

Purpose: Phase 16 is purely visual. Vitest covers regressions. The actual quality gate is the user looking at the running app and confirming each page meets the Linear-inspired polish bar. This is the final ship gate before /gsd:verify-work.

Output: One human-verify checkpoint that walks through every page and shared component. On approval, the executor writes a SUMMARY.md documenting the review. On any failure, the user reports issues and a gap-closure plan is created via /gsd:plan-phase 16 --gaps.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-design-overhaul/16-CONTEXT.md
@.planning/phases/16-design-overhaul/16-RESEARCH.md
@.planning/phases/16-design-overhaul/16-UI-SPEC.md
@.planning/phases/16-design-overhaul/16-VALIDATION.md
@.planning/phases/16-design-overhaul/16-01-PLAN.md
@.planning/phases/16-design-overhaul/16-02-PLAN.md
@.planning/phases/16-design-overhaul/16-03-PLAN.md
@.planning/phases/16-design-overhaul/16-04-PLAN.md
@.planning/phases/16-design-overhaul/16-05-PLAN.md
@.planning/phases/16-design-overhaul/16-06-PLAN.md
@.planning/phases/16-design-overhaul/16-07-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run final regression suite + tsc + build</name>
  <files>(no files modified — gating commands only)</files>
  <read_first>
    - .planning/phases/16-design-overhaul/16-VALIDATION.md (sampling rate: full suite must remain ≥ 279 passing)
  </read_first>
  <action>
    Run the three gating commands in sequence and capture output. Do NOT proceed to Task 2 if any command fails.

    1. `npx vitest run --reporter=verbose 2>&1 | tail -30` — full vitest regression suite
    2. `npx tsc --noEmit 2>&1 | tail -20` — TypeScript compile check
    3. `npm run build 2>&1 | tail -30` — Vite production build (catches any runtime CSS or import errors that vitest jsdom misses, especially the @fontsource-variable/geist import)

    All three must exit 0. Vitest must show ≥ 279 passing, 0 failing. TypeScript must produce no errors. The Vite build must finish with a `dist/` output and no error/warning about font imports or missing modules.

    If any of these fails:
    - DO NOT proceed to Task 2 (manual review)
    - Pause and report the failure to the user with the exact error output
    - Wait for instruction (the user may direct a fix or split the issue into a new gap-closure plan)
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with line `Tests` showing `passed` count ≥ 279 and `failed` count = 0
    - `npx tsc --noEmit` exits 0 with no error output
    - `npm run build` exits 0 and produces `dist/` directory; output contains the line(s) referencing `@fontsource-variable/geist` WOFF2 chunk(s) being bundled (or no font-related errors at minimum)
    - No commands report missing modules, type errors, or unresolved imports
  </acceptance_criteria>
  <done>
    Phase 16's full automated gate is green: 279+ tests passing, no TypeScript errors, Vite production build succeeds. The build artifact reflects all Phase 16 changes ready for the live-app review in Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual visual review of all 7 pages and shared components in the live Tauri app</name>
  <files>(no files modified — visual review only)</files>
  <read_first>
    - All Phase 16 SUMMARY files (16-01 through 16-07) — verify what each plan claimed to ship
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md (the visual contract being reviewed)
  </read_first>
  <action>
    This is a checkpoint:human-verify task. After Task 1's automated gates pass, launch the live Tauri app via `npm run tauri dev` and walk through the 24-step visual review described below. Capture the user's PASS/FAIL/notes per step. Do not proceed to Task 3 until the user types "approved" or describes any issues found.
  </action>
  <verify>
    <automated>MISSING — manual visual review; no automated check possible. Pass criterion is the user typing "approved" after the 24-step walkthrough.</automated>
  </verify>
  <done>
    User has explicitly typed "approved" OR has described all issues found per page. The 24-step review log is captured in conversation history for transcription into Task 3's SUMMARY.md.
  </done>
  <acceptance_criteria>
    - Live Tauri app has been launched and is running on the user's machine during review
    - All 24 steps below have been walked through with the user, in order
    - Each step has an explicit PASS / FAIL / NOTES outcome captured in conversation history
    - User has either typed exactly "approved" (success path) OR has described every failing step (gap-closure path)
  </acceptance_criteria>
  <what-built>
    Phase 16 design overhaul end-to-end:
    - Wave 1 (Plan 16-01): Geist Variable font + globals.css token
    - Wave 2 (Plans 16-02 through 16-07, all parallel):
      - Sidebar: Sword wordmark + Manage/Inventory/Tracking section grouping + gap-4 NavItem density
      - Page headers: text-3xl + subtitle + hairline border on all 7 pages
      - Tabular-nums: applied across StatCard, UnitGallery, UnitDetailSheet, ArmyListCard, SpendingPage, PlaybookTab
      - Card elevation: bg-card + border-border/60 + shadow-sm on StatCard (static), with hover:shadow-md on ArmyListCard and UnitGallery cards (interactive)
      - Empty states: 7 components upgraded — Dashboard welcome screen (full replacement), Collection (both modes), Painting Projects, Paints, Army Lists, Recipes; Spending inline empty state too
  </what-built>
  <how-to-verify>
    Run the app: `npm run tauri dev` (or `npm run dev` for web-only preview if Tauri shell is unavailable).

    Walk through each step. For each, confirm the visible result OR report what's wrong.

    --- Foundation (Wave 1 + Wave 2 sidebar) ---

    Step 1 — Font: open the app on the Dashboard. Confirm body text renders in Geist Sans (clean, narrow, modern — not the default system font). The tell: lowercase 'a' has a single-storey form; numbers feel slightly narrower than Helvetica/Arial.

    Step 2 — Sidebar wordmark: top of the sidebar shows a small singular sword icon (text-faction-accent colour, NOT muted) followed by the word "HobbyForge" in semibold tracking-tight. A hairline `border-b` separates the wordmark from the nav.

    Step 3 — Sidebar grouping: in expanded mode (default), confirm three uppercase section labels in sequence:
    - "MANAGE" above Dashboard / Factions / Collection / Painting Projects
    - "INVENTORY" above Paints / Recipes
    - "TRACKING" above Army Lists / Spending
    Settings remains pinned at the bottom outside any group.

    Step 4 — Sidebar collapse: click the collapse toggle at the bottom of the sidebar. Confirm:
    - Sidebar shrinks to icon-only (≈48px width)
    - All section labels (Manage / Inventory / Tracking) disappear (only icons remain)
    - Wordmark text "HobbyForge" disappears (only the Sword icon remains, centred)
    - Hovering each nav icon shows a tooltip with the nav label (Phase 10 contract preserved)
    Click again to expand. Confirm everything restores.

    Step 5 — Active nav item: navigate to any page (e.g., Collection). Confirm the active NavItem uses `bg-faction-accent` colour AND font-semibold (NOT the previous font-medium — text should look slightly bolder).

    --- Page headers (Wave 2 plans 16-03, 16-04, 16-05) ---

    Step 6 — Dashboard header: navigate to "/". Confirm:
    - h1 reads "Dashboard" in clearly larger size than card titles (28px vs 16px)
    - Subtitle line "Your hobby command center at a glance" sits below the h1 in muted-foreground colour
    - A hairline `border-b border-border/40` runs along the bottom of the header div
    - StatCards in the hero have a subtle elevated look (faint shadow + bg-card border) and stat numbers use tabular-nums (the digits have equal width; if you compare "8" and "1" side-by-side they don't shift)

    Step 7 — Collection header: navigate to "/collection". Confirm h1 "Collection" + subtitle "All units you own, tracked and filterable" + hairline border. Toggle to gallery view (Phase 12 contract). Confirm UnitGallery cards have shadow elevation and lift on hover (shadow-md). Numbers (model count / pts / painting %) use tabular-nums.

    Step 8 — Painting Projects header: navigate to "/painting-projects". Confirm h1 "Painting Projects" + subtitle "Active units being worked on right now" + hairline border. Drag a Kanban card to a different column to confirm Phase 4 drag-drop still works.

    Step 9 — Paints header: navigate to "/paints". Confirm h1 "Paints" + subtitle "Your paint collection, linked to recipes" + hairline border. Filter bar still works.

    Step 10 — Army Lists header: navigate to "/army-lists". Confirm h1 "Army Lists" + subtitle "Points-tracked lists for the tabletop" + hairline border. Cards visibly elevate on hover (shadow-md). Points totals use tabular-nums.

    Step 11 — Recipes header: navigate to "/recipes". Confirm h1 "Recipes" + subtitle "Documented paint schemes for your models" + hairline border.

    Step 12 — Spending header: navigate to "/spending". Confirm h1 "Spending" + subtitle "Total hobby spend tracked to the penny" + hairline border IS contained inside the narrow `max-w-3xl` column (NOT spanning full window — Pitfall 1). Hero card total uses tabular-nums. The "Breakdown" section heading is visibly smaller (text-base) than the page h1 (text-3xl) — clearly a sub-section, not a peer of the page title. Per-faction spend rows use tabular-nums.

    --- Empty states (Wave 2 plan 16-06 + 16-07 + 16-05 inline) ---

    To test empty states, you may need to temporarily delete data or filter to no-results. Alternatively, use a fresh database OR check the empty-state JSX renders correctly by inspection.

    Step 13 — Dashboard welcome: if no units exist, the dashboard shows the welcome screen:
    - Singular Sword icon (faction-accent colour) + "HobbyForge" wordmark side-by-side at text-3xl
    - Sub-headline "Your collection is empty" in text-base font-semibold
    - Helper paragraph mentions tracking what you own / what's painted / what's ready to play
    - Large "Add your first unit" button navigates to /collection on click
    Confirm: NO icon-pill container (welcome screen does NOT use the bg-muted/40 pattern — Pitfall 3)

    Step 14 — Collection empty (no-data): with no units, Collection shows ShieldOff icon inside a rounded-xl bg-muted/40 p-4 container, headline "No units yet", helper "Add your first unit to start tracking what you own and how far along it is.", "Add unit" CTA.

    Step 15 — Collection empty (filtered): apply filters that return zero results. Confirm FilterX icon in same container style, headline "No units match", helper "Your current filters returned nothing. Clear a filter to see more units.", "Clear filters" CTA.

    Step 16 — Painting Projects empty: with no active projects, confirm Layers icon-pill, "No active projects", "Mark a unit as an active project from Collection to see it here.", "Go to Collection" CTA.

    Step 17 — Paints empty: with no paints, confirm Palette icon-pill, "No paints yet", "Add the paints you own to link them to recipes and track what you're running low on.", "Add paint" CTA.

    Step 18 — Army Lists empty: with no army lists, confirm Swords (crossed-blades, plural) icon-pill, "No army lists yet", "Build a list to track points, units, and your battle-ready percentage.", "New list" CTA.

    Step 19 — Recipes empty: with no recipes, confirm BookOpen icon-pill, "No recipes yet", "Document a paint scheme to keep your colour choices consistent across models." (note British "colour" spelling), "New recipe" CTA.

    Step 20 — Spending empty: with no spend logged, confirm Receipt icon-pill, "No spend logged yet", "Add purchase prices to units and paints from their detail sheets to track your spend here.", and importantly NO CTA button (Spending empty state has no inline action).

    --- Regression sweep ---

    Step 21 — UnitDetailSheet: open any unit. Cycle through tabs: Stats, Painting, Playbook, Journal. Confirm:
    - Phase 9 Playbook stat block (M/T/Sv/W/Ld/OC) uses tabular-nums
    - Phase 13 Journal tab still renders sessions + photos
    - Phase 14 purchase price (if set) displays as formatted currency with tabular-nums

    Step 22 — Phase 11 dashboard counters: refresh Dashboard. Confirm stat counters animate from 0 to their target value (Phase 11 useCountUp contract intact, plus tabular-nums on the final number).

    Step 23 — Phase 13 photo timeline: open a unit with photos. Click a photo. Confirm sibling-portal lightbox opens (NOT nested portal). Photo timeline renders thumbnails with stage labels.

    Step 24 — Phase 8 ArmyListDetailSheet: open an army list. Confirm sheet opens, unit picker dialog opens as a sibling portal.

    --- Decision ---

    If ALL steps pass: respond "approved" and Task 3 (write SUMMARY.md) proceeds.

    If ANY step fails: describe the issue precisely (page, what was expected, what you saw). The user can then choose:
    - "Fix in-place" — small issue, executor patches now
    - "Plan a gap closure" — significant issue, run `/gsd:plan-phase 16 --gaps` to create a follow-up plan
    - "Defer" — accept the issue as-is and document in SUMMARY.md as known
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe issues found per page</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write Phase 16 final SUMMARY documenting the visual review and shipping the phase</name>
  <files>.planning/phases/16-design-overhaul/16-08-SUMMARY.md</files>
  <read_first>
    - .planning/phases/16-design-overhaul/16-01-SUMMARY.md
    - .planning/phases/16-design-overhaul/16-02-SUMMARY.md
    - .planning/phases/16-design-overhaul/16-03-SUMMARY.md
    - .planning/phases/16-design-overhaul/16-04-SUMMARY.md
    - .planning/phases/16-design-overhaul/16-05-SUMMARY.md
    - .planning/phases/16-design-overhaul/16-06-SUMMARY.md
    - .planning/phases/16-design-overhaul/16-07-SUMMARY.md
    - .planning/phases/16-design-overhaul/16-UI-SPEC.md (cross-check sign-off dimensions)
    - .planning/phases/16-design-overhaul/16-VALIDATION.md (cross-check validation sign-off)
  </read_first>
  <action>
    Create `.planning/phases/16-design-overhaul/16-08-SUMMARY.md` with the following structure (replace the placeholder values with actual results from Task 1 and Task 2):

    Frontmatter:
    - phase: 16-design-overhaul
    - plan: 08
    - completed: YYYY-MM-DD (the actual date Task 2 was approved)
    - tests_passing: <number from Task 1's vitest output>

    Body sections (in this order):
    1. `# Phase 16 Final Smoke-Test Summary` heading
    2. `## Phase 16 Completion Status` — paragraph describing what shipped (font, sidebar, page headers, empty states, tabular-nums, card elevation)
    3. `## Manual Review Steps Completed` — markdown table with 24 rows, columns Step / Page-Component / Result. Each Result cell is "PASS" or "FAIL — <note>". The 24 rows correspond exactly to the 24 steps from Task 2's how-to-verify section (Step 1 Geist font, Step 2 sidebar wordmark, ..., Step 24 ArmyListDetailSheet sibling portal).
    4. `## Final Test Counts` — Vitest passing count, TypeScript errors (0), Vite build status
    5. `## Phase 16 Plans Reference` — bullet list naming each plan (16-01 through 16-07) and its scope
    6. `## Known Issues / Deferred` — list any issues identified during review that the user chose to defer; otherwise write "None"
    7. `## Phase 16 Status` — final sentence: either "Complete — ready for /gsd:verify-work and STATE.md update." OR "Issues found — gap-closure plan needed."

    After writing the SUMMARY, update STATE.md `last_activity` to reflect Phase 16 completion (do NOT mark Phase 16 complete yet — the verify-work step does that).
  </action>
  <verify>
    <automated>node "C:/Users/antoi/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate ".planning/phases/16-design-overhaul/16-08-SUMMARY.md" --schema summary 2>&1 | tail -10</automated>

    Manual checks:
    1. SUMMARY.md exists at the correct path
    2. Frontmatter has `phase: 16-design-overhaul`, `plan: 08`, `completed: <date>`
    3. The 24-row review table has explicit PASS/FAIL/notes for each step (not all blank)
    4. Final test counts are populated with actual numbers
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/16-design-overhaul/16-08-SUMMARY.md` exists at the exact path
    - Frontmatter contains keys `phase` (value `16-design-overhaul`), `plan` (value `08`), `completed` (a YYYY-MM-DD date)
    - Body contains a 24-row markdown table covering every manual review step with explicit PASS or FAIL results — no placeholder strings like `<result>` or `TBD` remain
    - Body lists actual test counts from Task 1 (vitest passing count is a number, not a placeholder)
    - Body either confirms `Complete` status OR lists known deferred issues clearly
    - Frontmatter validates against the summary schema (`gsd-tools.cjs frontmatter validate ... --schema summary` returns `valid: true`)
  </acceptance_criteria>
  <done>
    Phase 16 final summary written, all 24 manual review results recorded, regression numbers captured, phase ready to ship via /gsd:verify-work.
  </done>
</task>

</tasks>

<verification>
- All previous Phase 16 plans (16-01 through 16-07) merged and tested
- Vitest ≥ 279 passing, 0 failing
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0 with dist/ output
- Manual review approval recorded in SUMMARY.md
- All 24 review steps have explicit PASS/FAIL outcomes
</verification>

<success_criteria>
- 16-08-SUMMARY.md exists with frontmatter, full 24-step review table, regression counts
- All Phase 16 plans (01-07) merged and visible in git log
- No outstanding visual issues OR all issues clearly documented as deferred
- Phase 16 ready for `/gsd:verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-overhaul/16-08-SUMMARY.md` (Task 3 IS the output — this is the phase-completion record). No further plan files are created. Recommend the user run `/gsd:verify-work 16` next.
</output>

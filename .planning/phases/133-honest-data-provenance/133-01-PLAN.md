---
phase: 133-honest-data-provenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/computeUnitWarnings.ts
  - src/features/army-lists/ArmyListSummaryBar.tsx
  - src/features/army-lists/ArmyListDetailPage.tsx
  - src/features/game-day/GameDayPage.tsx
  - src/features/game-day/GameDayReadinessPanel.tsx
  - tests/lib/computeUnitWarnings.test.ts
  - tests/army-lists/ArmyListSummaryBar.test.tsx
  - tests/game-day/GameDayReadinessPanel.test.tsx
autonomous: true
requirements: [HON-02]

must_haves:
  truths:
    - "computeListHealthStats no longer accepts a freshness parameter"
    - "WarningContext has no freshness field"
    - "ArmyListSummaryBar and GameDayReadinessPanel have no freshness prop"
    - "ArmyListDetailPage and GameDayPage no longer derive or pass freshness"
    - "pnpm build stays green after this plan (no dangling SyncFreshness type imports in the warnings layer or its callers)"
  artifacts:
    - path: "src/lib/computeUnitWarnings.ts"
      provides: "Warnings logic with freshness parameter removed from WarningContext and computeListHealthStats"
      contains: "computeListHealthStats"
    - path: "src/features/army-lists/ArmyListSummaryBar.tsx"
      provides: "Summary bar without freshness prop"
    - path: "src/features/game-day/GameDayReadinessPanel.tsx"
      provides: "Readiness panel without freshness prop"
  key_links:
    - from: "src/features/army-lists/ArmyListSummaryBar.tsx"
      to: "computeListHealthStats"
      via: "call without freshness arg"
      pattern: "computeListHealthStats\\(units, pointsLimit"
    - from: "src/features/game-day/GameDayReadinessPanel.tsx"
      to: "computeListHealthStats"
      via: "call without freshness arg"
      pattern: "computeListHealthStats\\(units, pointsLimit\\)"
---

<objective>
Excise the threaded `freshness: SyncFreshness` parameter from the warnings layer and every component whose type signature carries it — the pure-logic and signature-propagation portion of the fake-freshness removal (HON-02).

This is the FIRST wave: by removing `freshness` from `computeUnitWarnings.ts` first and then updating each call site in the same plan, `pnpm build` stays green at the end of this plan. The `src/lib/syncFreshness.ts` module is NOT deleted here (Plan 02 deletes it after the display-layer references are also gone); this plan only stops the warnings layer and its four signature-consuming callers from importing the `SyncFreshness` type or passing the `freshness` argument.

Purpose: Make the signature-level cut cleanly so the type has zero remaining references in the warnings/army-list/game-day layer, satisfying the "no dangling imports, no unused parameters" half of HON-02.
Output: `computeUnitWarnings.ts` + 4 component files + 3 test files updated; `pnpm build` green; warnings-layer tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-honest-data-provenance/133-RESEARCH.md
@.planning/phases/133-honest-data-provenance/133-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from RESEARCH/PATTERNS (codebase-verified). Use directly — no exploration needed. -->

computeUnitWarnings.ts (current, the field/param to remove):
- WarningContext interface currently has: totalPoints: number; pointsLimit: number | null; freshness: SyncFreshness;
- computeListHealthStats signature: (units, pointsLimit, freshness: SyncFreshness, enhancementTotal = 0)
- context construction: const context: WarningContext = { totalPoints, pointsLimit, freshness };
- computeListWarnings already has its stale-data branch removed (only a comment remains); freshness in WarningContext is the only remaining use.
- KEEP unchanged: computeListWarnings, computeUnitWarnings, ListHealthStats, UnitWarnings, all BATTLELINE/TRANSPORT/EPIC HERO logic.

ArmyListSummaryBar.tsx (current call sites):
- computeListHealthStats(units, pointsLimit, freshness, enhancementTotal) → drop freshness arg
- computeListWarnings({ totalPoints: stats.totalPoints, pointsLimit, freshness }, units) → drop freshness key
- PointsFreshnessBadge is rendered here — DO NOT TOUCH it in this plan (Plan 02 handles its display).

GameDayReadinessPanel.tsx (current call site):
- computeListHealthStats(units, pointsLimit, freshness) → computeListHealthStats(units, pointsLimit)
- PointsFreshnessBadge is rendered here — DO NOT TOUCH it in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove freshness from the warnings layer and update its unit test</name>
  <files>src/lib/computeUnitWarnings.ts, tests/lib/computeUnitWarnings.test.ts</files>
  <read_first>
    - src/lib/computeUnitWarnings.ts (the file being modified)
    - tests/lib/computeUnitWarnings.test.ts (the test being modified)
    - .planning/phases/133-honest-data-provenance/133-RESEARCH.md (computeUnitWarnings.ts call-graph entry + Pitfall 4)
    - .planning/phases/133-honest-data-provenance/133-PATTERNS.md (computeUnitWarnings.ts pattern assignment + computeUnitWarnings.test.ts factory cleanup)
  </read_first>
  <behavior>
    - computeListHealthStats(units, pointsLimit, enhancementTotal) still returns the same ListHealthStats as before for any input — removing freshness changes no output, because the freshness branch was already dead.
    - The three existing tests asserting "Stale points data" does NOT appear still pass when makeContext() is called with no freshness key.
    - makeContext() with no overrides produces a valid WarningContext (totalPoints, pointsLimit only).
  </behavior>
  <action>In src/lib/computeUnitWarnings.ts: delete the `import type { SyncFreshness } from "@/lib/syncFreshness";` line; delete the `freshness: SyncFreshness;` field from the WarningContext interface; delete the `freshness: SyncFreshness` parameter from the computeListHealthStats signature (it sits between pointsLimit and enhancementTotal); change the context construction from `{ totalPoints, pointsLimit, freshness }` to `{ totalPoints, pointsLimit }`. Do NOT change computeListWarnings, computeUnitWarnings, ListHealthStats, UnitWarnings, or any BATTLELINE/TRANSPORT/EPIC HERO logic. In tests/lib/computeUnitWarnings.test.ts: remove `freshness: "fresh"` from the makeContext factory defaults; remove the `freshness: "stale"` (and any other freshness) override from the three "does NOT return Stale points data" tests so they call makeContext() with no freshness key; remove any `SyncFreshness` import if present. The "does NOT return Stale points data" assertions stay — the code never produces that warning.</action>
  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test -- tests/lib/computeUnitWarnings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "freshness" src/lib/computeUnitWarnings.ts` returns no matches (case-sensitive identifier gone; note backupFreshness is in a different file).
    - `grep -n "SyncFreshness" src/lib/computeUnitWarnings.ts tests/lib/computeUnitWarnings.test.ts` returns no matches.
    - `pnpm build` exits 0.
    - `pnpm test -- tests/lib/computeUnitWarnings.test.ts` exits 0.
  </acceptance_criteria>
  <done>computeUnitWarnings.ts has no freshness field/param and no SyncFreshness import; its unit test passes with the freshness override removed; pnpm build green.</done>
</task>

<task type="auto">
  <name>Task 2: Drop freshness prop/derivation from the 4 signature-consuming components and update their tests</name>
  <files>src/features/army-lists/ArmyListSummaryBar.tsx, src/features/army-lists/ArmyListDetailPage.tsx, src/features/game-day/GameDayPage.tsx, src/features/game-day/GameDayReadinessPanel.tsx, tests/army-lists/ArmyListSummaryBar.test.tsx, tests/game-day/GameDayReadinessPanel.test.tsx</files>
  <read_first>
    - src/features/army-lists/ArmyListSummaryBar.tsx
    - src/features/army-lists/ArmyListDetailPage.tsx
    - src/features/game-day/GameDayPage.tsx
    - src/features/game-day/GameDayReadinessPanel.tsx
    - tests/army-lists/ArmyListSummaryBar.test.tsx
    - tests/game-day/GameDayReadinessPanel.test.tsx
    - .planning/phases/133-honest-data-provenance/133-PATTERNS.md (pattern assignments for these 4 files + the "Unused-hook removal pattern" table)
    - .planning/phases/133-honest-data-provenance/133-RESEARCH.md (Pitfall 2 and Pitfall 5 — useUdbMeta removal in GameDayPage and ArmyListDetailPage)
  </read_first>
  <action>ArmyListSummaryBar.tsx: remove the `import type { SyncFreshness } from "@/lib/syncFreshness";` line; remove `freshness: SyncFreshness;` from ArmyListSummaryBarProps; remove `freshness` from the destructuring; change the computeListHealthStats call to `computeListHealthStats(units, pointsLimit, enhancementTotal)` and update its useMemo deps array to drop freshness; change the computeListWarnings context object to `{ totalPoints: stats.totalPoints, pointsLimit }` and drop freshness from its deps array. Leave the rendered PointsFreshnessBadge untouched. ArmyListDetailPage.tsx: remove `import { getSyncFreshness } from "@/lib/syncFreshness";`; remove the `const freshness = useMemo(() => getSyncFreshness(udbMeta?.built_at ?? null), [udbMeta?.built_at]);` memo; since udbMeta is used ONLY for that memo (verified A1), remove the `const { data: udbMeta } = useUdbMeta();` call and the `useUdbMeta` import; remove the `freshness={freshness}` prop from the rendered ArmyListSummaryBar. Do NOT remove useUdbMeta if a fresh read shows any other udbMeta usage — if so, keep the hook and only drop the memo and prop. GameDayPage.tsx: remove `import { getSyncFreshness } from "@/lib/syncFreshness";`; remove `const freshness = getSyncFreshness(udbMeta?.built_at ?? null);`; since udbMeta is used ONLY for freshness (verified A2), remove the `const { data: udbMeta } = useUdbMeta();` call and the `useUdbMeta` import; remove `freshness={freshness}` from the rendered GameDayReadinessPanel. GameDayReadinessPanel.tsx: remove `import type { SyncFreshness } from "@/lib/syncFreshness";`; remove `freshness: SyncFreshness;` from GameDayReadinessPanelProps; remove `freshness` from destructuring; change the call to `computeListHealthStats(units, pointsLimit)` and drop freshness from its deps array. Leave the rendered PointsFreshnessBadge untouched. tests/army-lists/ArmyListSummaryBar.test.tsx: remove the `SyncFreshness` import; remove the `freshness` parameter from the renderBar() helper and the `freshness={...}` prop it passes; update every `renderBar(units, limit, "fresh")` call to `renderBar(units, limit)`. tests/game-day/GameDayReadinessPanel.test.tsx: remove the `SyncFreshness` import; remove `freshness` from defaultProps and any renderPanel override type; rewrite the test that passes `freshness: "stale"` to drop that override (its warnings still appear from points/battleline conditions).</action>
  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test -- tests/army-lists/ArmyListSummaryBar.test.tsx tests/game-day/GameDayReadinessPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "SyncFreshness\|getSyncFreshness\|\.freshness\|freshness=" src/features/army-lists/ArmyListSummaryBar.tsx src/features/army-lists/ArmyListDetailPage.tsx src/features/game-day/GameDayPage.tsx src/features/game-day/GameDayReadinessPanel.tsx` returns no matches.
    - `grep -rn "SyncFreshness" tests/army-lists/ArmyListSummaryBar.test.tsx tests/game-day/GameDayReadinessPanel.test.tsx` returns no matches.
    - `pnpm build` exits 0 (noUnusedLocals/noUnusedParameters catch any missed udbMeta/useUdbMeta dangle).
    - `pnpm test -- tests/army-lists/ArmyListSummaryBar.test.tsx tests/game-day/GameDayReadinessPanel.test.tsx` exits 0.
    - src/lib/backupFreshness.ts is unchanged (not in files_modified).
  </acceptance_criteria>
  <done>All four components compile with no freshness prop/param/import; ArmyListDetailPage and GameDayPage no longer call useUdbMeta; both updated test files pass; pnpm build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan removes display/logic dead code and tightens pure-function signatures. No new untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | (none applicable) | computeUnitWarnings.ts + 4 components | accept | No security-sensitive surface: no auth, session, input validation, crypto, or access control is touched. Changes are pure signature removal + prop removal on already-trusted in-memory army-list data. RESEARCH §Security Domain confirms ASVS V2–V6 not applicable. |

No package installs in this plan, so the supply-chain (T-133-SC) threat is not applicable here.
</threat_model>

<verification>
- `pnpm build` exits 0 (the HON-02 enforcement gate — strict TS flags any missed SyncFreshness import or unused udbMeta/param).
- `pnpm test -- tests/lib/computeUnitWarnings.test.ts tests/army-lists/ArmyListSummaryBar.test.tsx tests/game-day/GameDayReadinessPanel.test.tsx` exits 0.
- `grep -rn "SyncFreshness\|getSyncFreshness" src/lib/computeUnitWarnings.ts src/features/army-lists/ArmyListSummaryBar.tsx src/features/army-lists/ArmyListDetailPage.tsx src/features/game-day/GameDayPage.tsx src/features/game-day/GameDayReadinessPanel.tsx` returns nothing.
- `src/lib/syncFreshness.ts` still exists (deleted in Plan 02, not here) — Plan 02 depends on this plan finishing first.
</verification>

<success_criteria>
- The `freshness` parameter is gone from WarningContext and computeListHealthStats.
- The four signature-consuming components no longer reference SyncFreshness or pass a freshness prop.
- ArmyListDetailPage and GameDayPage no longer call useUdbMeta (only-use was freshness, verified).
- pnpm build green; the three warnings-layer test files green.
- backupFreshness.ts and its consumers untouched.
</success_criteria>

<output>
Create `.planning/phases/133-honest-data-provenance/133-01-SUMMARY.md` when done.
</output>

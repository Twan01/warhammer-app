---
phase: 133-honest-data-provenance
plan: 02
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - src/features/army-lists/PointsFreshnessBadge.tsx
  - src/features/dashboard/ReadyToPlayCard.tsx
  - src/features/dashboard/DataHealthSummaryCard.tsx
  - src/features/data-health/DiagnosticsCard.tsx
  - src/lib/syncFreshness.ts
  - src/features/army-lists/StaleDataBanner.tsx
  - tests/army-list/StaleDataBanner.test.tsx
  - tests/army-list/ArmyListDetailNotFound.test.tsx
  - tests/army-list/ArmyListNotesNoOp.test.tsx
  - tests/feedback/FBK-02-GameDayErrorState.test.tsx
autonomous: false
requirements: [HON-01, HON-02]

must_haves:
  truths:
    - "PointsFreshnessBadge renders honest text-only v{version} with a tooltip and no traffic-light dot"
    - "DataHealthSummaryCard shows an honest 'Data {udbMeta.version}' row and no fake sync dot"
    - "ReadyToPlayCard has no sync/Clock row and no dead 'Sync stale' branch"
    - "No UI on any touched surface offers a 'sync'/'refresh data' action or implies bundled data is stale"
    - "The real backup-staleness warning (BACKUP_FRESHNESS_DOT_CLASS dot + '(outdated)' block) in DataHealthSummaryCard is preserved unchanged"
    - "src/lib/syncFreshness.ts and src/features/army-lists/StaleDataBanner.tsx no longer exist"
    - "pnpm build green with zero SyncFreshness references in src/; pnpm test green"
  artifacts:
    - path: "src/features/army-lists/PointsFreshnessBadge.tsx"
      provides: "Honest text-only data-version badge (the provenance surface seed, D-01)"
      contains: "udbMeta"
    - path: "src/features/dashboard/DataHealthSummaryCard.tsx"
      provides: "Honest 'Data {version}' sync row + preserved backup row"
      contains: "BACKUP_FRESHNESS_DOT_CLASS"
  key_links:
    - from: "src/features/army-lists/PointsFreshnessBadge.tsx"
      to: "useUdbMeta"
      via: "version + built_at display"
      pattern: "udbMeta"
    - from: "src/features/dashboard/DataHealthSummaryCard.tsx"
      to: "udbMeta.version"
      via: "honest provenance text row"
      pattern: "udbMeta"
---

<objective>
Replace the fake sync-freshness UI with the honest data-provenance surface (HON-01), delete the dead `syncFreshness.ts` stub and the orphaned `StaleDataBanner.tsx` (HON-02), and finish the affected test suite — while preserving the REAL backup-staleness warning untouched (success criterion 3).

This is the SECOND wave. It runs after Plan 01 has removed every `freshness` reference from the warnings layer and the four signature-consuming components, so the remaining references to `syncFreshness.ts` live only in the three display files cleaned here; once those are gone the module can be deleted. The honest provenance text uses `udb_meta.version` (which already encodes the build's content hash as `1.0.0+{sha256[:8]}` — RESEARCH §Content Hash Finding, D-02), so no migration or build-script change is needed.

Purpose: Make the dashboard/Game-Day/army-list UI state the truth about bundled data and remove the always-green dots, dead "Sync stale" branch, and `StaleDataBanner`.
Output: 4 display files updated, 2 source files deleted, 4 test files updated/deleted; `pnpm build` green with zero `SyncFreshness` references in `src/`; `pnpm test` green (~2744 tests, net −5 from the deleted StaleDataBanner suite).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-honest-data-provenance/133-RESEARCH.md
@.planning/phases/133-honest-data-provenance/133-PATTERNS.md
@.planning/phases/133-honest-data-provenance/133-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Codebase-verified in RESEARCH/PATTERNS/UI-SPEC. -->

useUdbMeta() returns { data: udbMeta } where udbMeta has: version (string, e.g. "1.0.0+a3f7bc21"), built_at, game_system, unit_count, faction_count. staleTime/gcTime Infinity.

Honest provenance seed (PATTERNS §Seed Pattern — the canonical "after" for PointsFreshnessBadge):
- displayLabel = udbMeta ? `v${udbMeta.version}` : "No data"
- tooltipText = udbMeta ? `Data version ${udbMeta.version} (built ${udbMeta.built_at})` : "Unit database not imported"
- Tooltip wraps a single <span className="text-xs text-muted-foreground">{displayLabel}</span> — no dot, no outer flex wrapper needed.
- Keep the existing isLoading Skeleton path.

DO-NOT-TOUCH boundary (D-09 — backup staleness is REAL):
- Never modify src/lib/backupFreshness.ts, src/features/data-health/BackupCard.tsx.
- In DataHealthSummaryCard.tsx, the backup row (BACKUP_FRESHNESS_DOT_CLASS dot + getBackupAgeLabel + the hasVersionMismatch "(outdated)" block, approx lines 60–69) and its line-9 backupFreshness import MUST stay byte-for-byte.
- Target identifiers for removal: SyncFreshness, getSyncFreshness, getSyncAgeLabel, FRESHNESS_DOT_CLASS, the syncFreshness module path. Never touch BackupFreshness, getBackupFreshness, getBackupAgeLabel, hasVersionMismatch, BACKUP_FRESHNESS_DOT_CLASS, backupFreshness.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make PointsFreshnessBadge, DataHealthSummaryCard, ReadyToPlayCard, and DiagnosticsCard honest</name>
  <files>src/features/army-lists/PointsFreshnessBadge.tsx, src/features/dashboard/DataHealthSummaryCard.tsx, src/features/dashboard/ReadyToPlayCard.tsx, src/features/data-health/DiagnosticsCard.tsx</files>
  <read_first>
    - src/features/army-lists/PointsFreshnessBadge.tsx (the honest-provenance seed — read first; it defines the target text/tooltip pattern)
    - src/features/dashboard/DataHealthSummaryCard.tsx (note the backup row is the do-not-touch boundary)
    - src/features/dashboard/ReadyToPlayCard.tsx
    - src/features/data-health/DiagnosticsCard.tsx
    - .planning/phases/133-honest-data-provenance/133-UI-SPEC.md (surface-by-surface before/after contract §1–§3, §6)
    - .planning/phases/133-honest-data-provenance/133-PATTERNS.md (pattern assignments for these 4 files + Seed Pattern + grep guard)
  </read_first>
  <action>PointsFreshnessBadge.tsx: remove `import { getSyncFreshness, FRESHNESS_DOT_CLASS } from "@/lib/syncFreshness";`; remove `const freshness = getSyncFreshness(udbMeta?.built_at ?? null);`; remove the colored dot `<span>` that uses FRESHNESS_DOT_CLASS[freshness]; render the component as a Tooltip wrapping a single `<span className="text-xs text-muted-foreground">` showing `v{udbMeta.version}` (fallback "No data"), with TooltipContent `Data version {version} (built {built_at})` (fallback "Unit database not imported"); keep the existing isLoading Skeleton branch; drop the now-unneeded outer flex wrapper and the `cn` import if it becomes unused. DataHealthSummaryCard.tsx: delete the line-8 `import { getSyncFreshness, getSyncAgeLabel, FRESHNESS_DOT_CLASS } from "@/lib/syncFreshness";` (KEEP the line-9 backupFreshness import verbatim); remove the `const freshness = getSyncFreshness(...)` and `const syncLabel = getSyncAgeLabel(...)` lines; replace the sync dot row (the FRESHNESS_DOT_CLASS dot + syncLabel) with an honest row `<span className="text-xs text-muted-foreground">{udbMeta ? \`Data ${udbMeta.version}\` : "Data version unavailable"}</span>`, keeping the existing syncLoading Skeleton wrapper around it; KEEP the `const { data: udbMeta, isLoading: syncLoading } = useUdbMeta();` hook (still needed for the version); DO NOT modify the backup row (BACKUP_FRESHNESS_DOT_CLASS dot + label + the hasVersionMismatch "(outdated)" block) or any backupFreshness reference. ReadyToPlayCard.tsx: change the Lucide import from `{ Shield, Clock }` to `{ Shield }` (Clock becomes unused); remove `import { getSyncFreshness, getSyncAgeLabel, FRESHNESS_DOT_CLASS } from "@/lib/syncFreshness";`; in ReadyToPlayCardInner remove `const freshness = ...`, `const syncLabel = ...`, the `const { data: udbMeta } = useUdbMeta();` call and the `useUdbMeta` import (udbMeta only fed freshness, verified A3); delete the Clock+dot+syncLabel row entirely; collapse the dead branch `{(unpaintedCount > 0 || freshness === "stale" || freshness === "aging") && (...)}` to `{unpaintedCount > 0 && (...)}` rendering only the "{unpaintedCount} unpainted" amber badge (keep that amber badge's existing className). DiagnosticsCard.tsx: update the inline comment that references syncFreshness to `// data version is sourced from udb_meta (udb_meta.version contains the content hash)` — comment-only, no import or code change.</action>
  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test -- tests/data-health/backupFreshness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "SyncFreshness\|getSyncFreshness\|getSyncAgeLabel\|FRESHNESS_DOT_CLASS" src/features/army-lists/PointsFreshnessBadge.tsx src/features/dashboard/DataHealthSummaryCard.tsx src/features/dashboard/ReadyToPlayCard.tsx` returns no matches.
    - `grep -n "BACKUP_FRESHNESS_DOT_CLASS\|backupFreshness" src/features/dashboard/DataHealthSummaryCard.tsx` still returns matches (backup row preserved).
    - `grep -n "Clock" src/features/dashboard/ReadyToPlayCard.tsx` returns no matches (unused import removed).
    - `grep -rn "Sync stale\|getSyncAgeLabel\|syncLabel" src/features/dashboard/` returns no matches.
    - src/lib/backupFreshness.ts and src/features/data-health/BackupCard.tsx are unchanged (not in files_modified).
    - `pnpm build` exits 0; `pnpm test -- tests/data-health/backupFreshness.test.ts` exits 0 (backup staleness still works).
  </acceptance_criteria>
  <done>The four display files show honest version text (or nothing) with no fake sync dot, no Clock row, no "Sync stale" branch; the backup row is untouched; pnpm build green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete syncFreshness.ts + StaleDataBanner.tsx and finish the test suite</name>
  <files>src/lib/syncFreshness.ts, src/features/army-lists/StaleDataBanner.tsx, tests/army-list/StaleDataBanner.test.tsx, tests/army-list/ArmyListDetailNotFound.test.tsx, tests/army-list/ArmyListNotesNoOp.test.tsx, tests/feedback/FBK-02-GameDayErrorState.test.tsx</files>
  <read_first>
    - src/lib/syncFreshness.ts (confirm it only exports SyncFreshness/getSyncFreshness/getSyncAgeLabel/FRESHNESS_DOT_CLASS before deleting)
    - src/features/army-lists/StaleDataBanner.tsx (confirm deletion target)
    - tests/army-list/ArmyListDetailNotFound.test.tsx (the vi.mock block to remove)
    - tests/army-list/ArmyListNotesNoOp.test.tsx (the vi.mock block to remove)
    - tests/feedback/FBK-02-GameDayErrorState.test.tsx (the vi.mock block to remove)
    - .planning/phases/133-honest-data-provenance/133-RESEARCH.md (Tests Disposition Table + Files to DELETE)
    - .planning/phases/133-honest-data-provenance/133-PATTERNS.md (Test Pattern Assignments — vi.mock removal)
  </read_first>
  <action>Before deleting, confirm no remaining references: run `grep -rn "SyncFreshness\|getSyncFreshness\|getSyncAgeLabel\|FRESHNESS_DOT_CLASS\|@/lib/syncFreshness" src/ tests/` and ensure the only hits left are in the files being deleted/cleaned in this task. Delete the file src/lib/syncFreshness.ts. Delete the file src/features/army-lists/StaleDataBanner.tsx (zero importers, confirmed D-05). Delete the file tests/army-list/StaleDataBanner.test.tsx (it tests the now-deleted component, 5 cases). In tests/army-list/ArmyListDetailNotFound.test.tsx, tests/army-list/ArmyListNotesNoOp.test.tsx, and tests/feedback/FBK-02-GameDayErrorState.test.tsx: remove the entire `vi.mock("@/lib/syncFreshness", () => ({ getSyncFreshness: () => ... }))` block in each (the source files they exercise no longer import syncFreshness). Make no other change to those three test files.</action>
  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/syncFreshness.ts does not exist.
    - src/features/army-lists/StaleDataBanner.tsx does not exist.
    - tests/army-list/StaleDataBanner.test.tsx does not exist.
    - `grep -rn "SyncFreshness\|getSyncFreshness\|getSyncAgeLabel\|FRESHNESS_DOT_CLASS\|@/lib/syncFreshness" src/ tests/` returns no matches.
    - `grep -rn "backupFreshness\|BACKUP_FRESHNESS_DOT_CLASS" src/lib/backupFreshness.ts` still returns matches (backup module intact).
    - `pnpm build` exits 0.
    - `pnpm test` exits 0 (full suite green; net −5 tests from the deleted StaleDataBanner suite).
  </acceptance_criteria>
  <done>Both source files and the StaleDataBanner test deleted; the three vi.mock blocks removed; zero SyncFreshness references remain in src/ or tests/; pnpm build and full pnpm test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify honest provenance UI and preserved backup warning</name>
  <action>Blocking human-verify checkpoint — confirm the honest-provenance UI replaced the fake sync freshness across all touched surfaces and the real backup-staleness warning still renders. No automation; pause for the user.</action>
  <what-built>Removed the fake sync-freshness UI and replaced it with honest data-version text across the dashboard, Game Day, army-list summary, and points badge, while preserving the real backup-staleness warning.</what-built>
  <how-to-verify>
    1. Run `pnpm tauri dev` (or `pnpm dev` for UI-only) and open the app.
    2. Dashboard — ReadyToPlayCard: confirm there is NO Clock/dot "sync" row, no "Sync stale" badge; an "{N} unpainted" amber badge appears only when there are unpainted units.
    3. Dashboard — DataHealthSummaryCard: confirm the sync line now reads "Data {version}" (e.g. "Data 1.0.0+a3f7bc21") with NO traffic-light dot; confirm the BACKUP row below it STILL shows its colored dot, backup age, and "(outdated)" badge when a version mismatch exists (this is the real warning and must remain).
    4. Army list detail / Game Day: confirm the points badge shows "v{version}" as plain muted text with a tooltip "Data version {version} (built {built_at})" and NO colored dot.
    5. Across all of the above: confirm NO label, button, or tooltip says "sync", "refresh data", "stale", "out of date", or "30 days old" about bundled unit data. ("(outdated)" on the BACKUP row is legitimate and should remain.)
  </how-to-verify>
  <verify>
    <human-check>Honest version text shown on all touched surfaces; no sync/refresh/stale copy; backup warning intact.</human-check>
  </verify>
  <done>User confirms honest provenance UI and preserved backup warning, or reports a specific surface needing fixes.</done>
  <resume-signal>Type "approved" or describe any surface that still shows a fake freshness dot, a sync/refresh affordance, stale-data copy, or a missing/broken backup warning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan removes display-only dead code and deletes two unused modules. No new untrusted input crosses any boundary; the honest text reads an already-trusted local `udb_meta` row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-02 | (none applicable) | display components + module deletions | accept | No security-sensitive surface: no auth, session, input validation, crypto, or access control touched. The only externally-derived value displayed is `udb_meta.version` (a bundled-build content hash, not user input). RESEARCH §Security Domain confirms ASVS V2–V6 not applicable. |
| T-133-DEL | Tampering | accidental deletion of real backup-staleness code | mitigate | Explicit do-not-touch boundary (D-09): backupFreshness.ts / BackupCard.tsx / the DataHealthSummaryCard backup row are excluded from files_modified; acceptance criteria grep-assert BACKUP_FRESHNESS_DOT_CLASS survives and backupFreshness.test.ts stays green; the human-verify checkpoint confirms the backup warning still renders. |

No package installs in this plan, so the supply-chain (T-133-SC) threat is not applicable here.
</threat_model>

<verification>
- `pnpm build` exits 0 — zero `SyncFreshness` references remain in `src/` (strict TS would otherwise fail on the deleted module's imports).
- `pnpm test` exits 0 — full suite green; StaleDataBanner suite (5 cases) deleted; three vi.mock blocks removed; backupFreshness.test.ts unchanged and green.
- `grep -rn "SyncFreshness\|getSyncFreshness\|getSyncAgeLabel\|FRESHNESS_DOT_CLASS\|@/lib/syncFreshness" src/ tests/` returns nothing.
- `grep -rn "BACKUP_FRESHNESS_DOT_CLASS\|backupFreshness" src/lib/backupFreshness.ts src/features/data-health/BackupCard.tsx src/features/dashboard/DataHealthSummaryCard.tsx` still returns matches (real backup staleness preserved — success criterion 3).
- Human-verify checkpoint: no sync/refresh/stale copy on any touched surface; backup warning intact.
</verification>

<success_criteria>
- StaleDataBanner and the dead "stale points" branch are gone; an honest version surface (built from `udb_meta.version`, which carries the content hash) replaces them (HON-01 SC #1).
- No UI offers a "sync"/"refresh data" action; no stale-data copy about bundled data remains (HON-01 SC #2).
- All former syncFreshness consumers compile cleanly with no dead branches/dangling imports/unused exports; syncFreshness.ts deleted (HON-02).
- The real backup-staleness warning is preserved unchanged (success criterion 3 / D-09).
- pnpm build green; pnpm test green.
</success_criteria>

<output>
Create `.planning/phases/133-honest-data-provenance/133-02-SUMMARY.md` when done.
</output>

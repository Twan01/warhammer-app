---
phase: 59-session-section-cascade
plan: 02
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - src/features/dashboard/LogSessionSheet.tsx
  - tests/painting/logSessionSheet.test.tsx
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - SESS-05

must_haves:
  truths:
    - "Section selector appears between recipe and step selectors when recipe has 2+ sections"
    - "Section selector is hidden when recipe has 0 or 1 section"
    - "Selecting a section filters the step dropdown to only that section's steps"
    - "Changing recipe clears both section and step selections"
    - "Changing section clears step selection only"
    - "All three selectors remain optional — user can log with any combination"
    - "Selected section_name is saved on the painting_session record"
  artifacts:
    - path: "src/features/dashboard/LogSessionSheet.tsx"
      provides: "3-level cascade selector (recipe -> section -> step)"
      contains: "useRecipeSections"
    - path: "tests/painting/logSessionSheet.test.tsx"
      provides: "SESS-01 through SESS-05 component tests"
      contains: "SESS-01"
  key_links:
    - from: "src/features/dashboard/LogSessionSheet.tsx"
      to: "@/hooks/useRecipeSections"
      via: "import and reactive call"
      pattern: "useRecipeSections"
    - from: "src/features/dashboard/LogSessionSheet.tsx"
      to: "createSession.mutateAsync"
      via: "section_name in submit payload"
      pattern: "section_name.*values\\.section_name"
    - from: "src/features/dashboard/LogSessionSheet.tsx"
      to: "filteredSteps"
      via: "useMemo filtering recipeSteps by watchedSectionId"
      pattern: "filteredSteps"
---

<objective>
Wire the 3-level cascading selector (recipe -> section -> step) into LogSessionSheet with reset chains, step filtering, and section_name submission. Add component tests for SESS-01 through SESS-05.

Purpose: Users can log painting sessions with section-level granularity. The section selector appears conditionally when a multi-section recipe is selected, filtering steps to the chosen section.
Output: Updated LogSessionSheet.tsx with cascade logic; extended logSessionSheet.test.tsx with cascade tests.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-session-section-cascade/59-CONTEXT.md
@.planning/phases/59-session-section-cascade/59-RESEARCH.md
@.planning/phases/59-session-section-cascade/59-PATTERNS.md
@.planning/phases/59-session-section-cascade/59-UI-SPEC.md
@.planning/phases/59-session-section-cascade/59-01-SUMMARY.md

<interfaces>
From src/features/dashboard/LogSessionSheet.tsx (current state):
- Line 22: import { useEffect, useMemo } from "react" — needs useState added
- Line 54: import { useRecipePaints } from "@/hooks/useRecipePaints" — add useRecipeSections import nearby
- Lines 69-79: buildDefaultValues — add section_name: null
- Line 126: watchedRecipeId = form.watch("recipe_id")
- Lines 128-130: useRecipePaints(watchedRecipeId) — mirror pattern for useRecipeSections
- Lines 132-135: existing useEffect clearing recipe_step_id — extend to also clear section
- Lines 142-151: onSubmit mutateAsync call — add section_name to payload
- Lines 264-304: recipe_id FormField block — section block goes after this
- Lines 306-348: recipe_step_id FormField block — replace recipeSteps with filteredSteps at line 330

From src/hooks/useRecipeSections.ts:
- useRecipeSections(recipeId: number | undefined) returns { data: RecipeSection[] }
- RecipeSection has id, name, order_index, section_type, technique, execution_mode, applies_to

From src/types/recipePaint.ts:
- RecipeStep has section_id: number | null

From tests/painting/logSessionSheet.test.tsx:
- Hook mocks at lines 11-47: useUnitsMock, useRecipesMock, useRecipePaintsMock — add useRecipeSectionsMock
- beforeEach at lines 105-110: clearAllMocks + defaults — add useRecipeSectionsMock default
- Test data: units at line 94, recipes at line 100
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cascade selector to LogSessionSheet</name>
  <files>src/features/dashboard/LogSessionSheet.tsx</files>
  <read_first>
    - src/features/dashboard/LogSessionSheet.tsx
    - src/features/dashboard/logSessionSchema.ts
    - src/hooks/useRecipeSections.ts
    - src/types/recipePaint.ts
    - .planning/phases/59-session-section-cascade/59-PATTERNS.md
  </read_first>
  <action>
Modify LogSessionSheet.tsx with these changes, per D-01 through D-19:

**Imports (line 22):** Add `useState` to the React import: `import { useEffect, useMemo, useState } from "react"`. Add `import { useRecipeSections } from "@/hooks/useRecipeSections"` after the useRecipePaints import (line 54).

**buildDefaultValues (line 69-79):** Add `section_name: null` after `recipe_step_id: null` per D-17.

**Hook declarations (after line 130):** Add three new declarations per D-12, D-15, D-09:
- `useState<number | null>(null)` for `watchedSectionId` — local state, not a form field (per D-15)
- `useRecipeSections(watchedRecipeId != null ? watchedRecipeId : undefined)` destructured as `{ data: sections = [] }` — same enabled-guard pattern as useRecipePaints (per D-12, D-13)
- `useMemo` computing `filteredSteps`: when `watchedSectionId` is null return `recipeSteps` unchanged; when set, filter `recipeSteps` to entries where `section_id === watchedSectionId` — deps are `[recipeSteps, watchedSectionId]` (per D-09, D-10, D-11)

**Reset useEffect chain 1 (lines 132-135):** Extend the existing useEffect that clears `recipe_step_id` to also call `form.setValue("section_name", null)` and `setWatchedSectionId(null)` per D-05, D-06.

**Reset useEffect chain 2 (new, after chain 1):** Add a new useEffect with dependency `[watchedSectionId, form]` that calls `form.setValue("recipe_step_id", null)` per D-07. Note: double-firing when recipe changes is idempotent and harmless (RESEARCH.md Pitfall 1).

**Form open reset (lines 117-119):** Add `setWatchedSectionId(null)` inside the `if (open)` block per RESEARCH.md Pattern 6.

**Section selector FormField (between lines 304 and 306):** Insert a new conditional block: `{watchedRecipeId != null && sections.length >= 2 && (...)}` per D-03. Inside, render a FormField for "section_name" using FormField+Controller+Select nesting identical to the recipe_step_id block. The Select value uses `watchedSectionId != null ? String(watchedSectionId) : "__none__"`. The `onValueChange` handler sets both `setWatchedSectionId(numId)` and `ctrl.onChange(resolvedName)` simultaneously per D-15, D-16. FormLabel text is "Section" per UI-SPEC. Placeholder and null option text is "No section". SelectItems map over `sections.slice().sort((a, b) => a.order_index - b.order_index)` displaying `s.name` with value `String(s.id)`. Include the hidden input pattern for unused render arg suppression. Full pattern is in PATTERNS.md lines 140-188.

**Step selector (line 330):** Replace `recipeSteps` with `filteredSteps` in the SelectContent map per D-10. The `.slice().sort()` chain stays.

**onSubmit (lines 142-151):** Add `section_name: values.section_name ?? null` to the `createSession.mutateAsync` call payload per D-16.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - LogSessionSheet.tsx imports useState from "react" and useRecipeSections from "@/hooks/useRecipeSections"
    - buildDefaultValues returns an object with section_name: null
    - useState for watchedSectionId declared at component top level (before any returns)
    - useRecipeSections called with the same enabled-guard pattern as useRecipePaints
    - filteredSteps derived via useMemo from recipeSteps and watchedSectionId
    - Existing useEffect at line 132 now also clears section_name form field and watchedSectionId local state
    - New useEffect with [watchedSectionId, form] deps clears recipe_step_id
    - Form open useEffect calls setWatchedSectionId(null)
    - Section FormField rendered conditionally when watchedRecipeId != null AND sections.length >= 2
    - Section Select onValueChange sets both watchedSectionId (number) and ctrl.onChange (string name)
    - Step selector iterates filteredSteps instead of recipeSteps
    - createSession.mutateAsync payload includes section_name
    - pnpm build succeeds
  </acceptance_criteria>
  <done>LogSessionSheet renders a 3-level cascade: recipe -> section (conditional, 2+ sections) -> step (filtered by section). Section name saved on submit.</done>
</task>

<task type="auto">
  <name>Task 2: Add SESS-01 through SESS-05 component tests</name>
  <files>tests/painting/logSessionSheet.test.tsx</files>
  <read_first>
    - tests/painting/logSessionSheet.test.tsx
    - src/features/dashboard/LogSessionSheet.tsx
    - .planning/phases/59-session-section-cascade/59-PATTERNS.md
  </read_first>
  <action>
Extend logSessionSheet.test.tsx with a useRecipeSections mock and a new SESS describe block.

**Mock setup (after line 42):** Add a `useRecipeSectionsMock` vi.fn() and vi.mock for `@/hooks/useRecipeSections` — same pattern as useRecipePaintsMock at lines 39-42. The mock factory returns `{ useRecipeSections: (recipeId) => useRecipeSectionsMock(recipeId) }`.

**beforeEach (extend lines 105-110):** Add `useRecipeSectionsMock.mockReturnValue({ data: [] })` as the default (no sections).

**Test data:** Add a `sections` array with 2+ items for multi-section tests: `[{ id: 10, name: "Armour", order_index: 0 }, { id: 11, name: "Details", order_index: 1 }]`. Add a `recipeSteps` array with steps referencing section_id values matching the sections: e.g. `[{ id: 100, step_name: "Base Blue", section_id: 10, order_index: 0, painting_phase: "Base" }, { id: 101, step_name: "Gold Trim", section_id: 11, order_index: 1, painting_phase: "Detail" }]`.

**New describe block:** `"LogSessionSheet — SESS-01 through SESS-05 (section cascade)"` after the existing INTEG-01 block. Include these test cases:

1. SESS-01: "does not render Section selector when recipe has 0 sections" — mock useRecipeSections returning empty array, render with open=true, expect screen.queryByText("Section") not in document
2. SESS-01: "does not render Section selector when recipe has 1 section" — mock useRecipeSections returning array with 1 section, expect Section label not rendered
3. SESS-01: "renders Section selector between Recipe and Step when recipe has 2+ sections" — mock useRecipeSections returning sections array with 2 items, set watchedRecipeId by selecting a recipe, expect "Section" label in document
4. SESS-02: "filters step selector to selected section's steps" — mock with sections and recipeSteps, select a recipe, select a section, verify only that section's steps appear
5. SESS-03: "clears section when recipe changes" — verify section reset on recipe change
6. SESS-04: "changing section clears step selection only" — verify step reset on section change
7. SESS-05: "submits with section_name null when no section selected" — verify form submits without section

Note: Some of these tests require user interaction (selecting from dropdowns). Use `@testing-library/react` `fireEvent` or `userEvent` to open SelectContent and click SelectItems. If SelectContent rendering in jsdom is limited (Radix portal), focus on testing that the correct elements are rendered given mock state, rather than full interaction flows. The INTEG-01 block at lines 171-208 shows the established pattern — follow it.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting/logSessionSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - logSessionSheet.test.tsx contains vi.mock for @/hooks/useRecipeSections
    - useRecipeSectionsMock default return value set in beforeEach
    - New describe block "SESS-01 through SESS-05" contains at least 5 test cases
    - Test for 0 sections: Section label not in document
    - Test for 1 section: Section label not in document
    - Test for 2+ sections with recipe selected: Section label in document
    - All existing tests (defaultUnitId, INTEG-01) still pass
    - pnpm test -- tests/painting/logSessionSheet.test.tsx exits 0
  </acceptance_criteria>
  <done>Component tests verify section cascade visibility, filtering, reset chains, and optionality for SESS-01 through SESS-05</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form input -> DB | section_name resolved from sections array, not user free text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02 | Tampering | section_name submission | accept | Value sourced from DB-fetched sections array via onValueChange; not user-editable free text; Zod validates as string|null |
| T-59-03 | Information Disclosure | step filtering | accept | Client-side filter over already-fetched steps; no additional data exposure |
</threat_model>

<verification>
pnpm test -- tests/painting/logSessionSheet.test.tsx
pnpm build
</verification>

<success_criteria>
- Section selector renders between recipe and step when recipe has 2+ sections
- Section selector hidden for 0 or 1 section recipes
- Step dropdown filters by selected section
- Recipe change clears section and step
- Section change clears step only
- Form submits section_name to createSession
- All component tests pass
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/59-session-section-cascade/59-02-SUMMARY.md`
</output>

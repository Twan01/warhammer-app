---
phase: 88-polish-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/painting-mode/PaintingModeView.integration.test.tsx
autonomous: true
requirements: [TS-05, TS-06]

must_haves:
  truths:
    - "Paintless steps (paint_id = null) do not trigger the PaintReadinessBanner"
    - "A mix of painted and paintless steps only reports actually missing paints in the banner"
    - "Unowned paints are listed by name in the PaintReadinessBanner"
    - "When all referenced paints are owned, the banner does not appear"
  artifacts:
    - path: "tests/painting-mode/PaintingModeView.integration.test.tsx"
      provides: "TS-05 paintless steps and TS-06 missing paint warning integration tests"
      contains: "TS-05: paintless steps"
  key_links:
    - from: "tests/painting-mode/PaintingModeView.integration.test.tsx"
      to: "src/features/painting-mode/PaintingModeView.tsx"
      via: "component import"
      pattern: "import.*PaintingModeView"
    - from: "src/features/painting-mode/PaintingModeView.tsx"
      to: "src/features/recipes/recipeSteps.ts"
      via: "isPaintMissing import"
      pattern: "isPaintMissing"
---

<objective>
Create a new integration test file covering paintless step handling (TS-05) and missing paint warning accuracy (TS-06) in PaintingModeView.

Purpose: Verify that the missingPaints derivation in PaintingModeView correctly skips paint_id=null steps and accurately identifies unowned paints for the banner.
Output: New integration test file with TS-05 and TS-06 describe blocks, all tests passing.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-polish-test-coverage/88-RESEARCH.md
@.planning/phases/88-polish-test-coverage/88-PATTERNS.md

<interfaces>
<!-- PaintingModeView props and missingPaints logic -->
From src/features/painting-mode/PaintingModeView.tsx:
- Props: { state: PaintingModeState, onMarkDone: () => void, onMarkDoneWithSession: (duration: number, notes: string | null) => void, recipeId: number, isMutating: boolean }
- missingPaints derivation (line 43-53): iterates state.orderedSteps, SKIPS steps where step.paint_id == null, looks up paint in paintMap, calls isPaintMissing(paint), collects { id, name, brand }
- showBanner = !bannerDismissed && missingPaints.length > 0
- PaintReadinessBanner renders text "Some paints are not in your inventory" and lists paint names as "{brand} {name}"

From src/features/recipes/recipeSteps.ts:
- isPaintMissing(paint): returns true when paint.owned !== 1

From src/features/painting-mode/PaintReadinessBanner.tsx:
- Props: { missingPaints: Array<{ id: number; name: string; brand: string }>, onDismiss: () => void }
- Renders each missing paint as "{brand} {name}" text

From hooks:
- usePaints(): returns { data: Paint[], isLoading: boolean }
- useRecipeSections(recipeId): returns { data: RecipeSection[], isLoading: boolean }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PaintingModeView.integration.test.tsx with TS-05 and TS-06</name>
  <files>tests/painting-mode/PaintingModeView.integration.test.tsx</files>
  <read_first>
    - tests/painting-mode/PaintingModeView.test.tsx (exact template — copy mock setup, factory helpers, createWrapper, renderView, setDefaultMocks patterns verbatim)
    - src/features/painting-mode/PaintingModeView.tsx (missingPaints derivation at lines 43-53 — confirms paint_id == null skip logic and isPaintMissing call)
    - src/features/painting-mode/PaintReadinessBanner.tsx (banner text pattern — "Some paints are not in your inventory", paint name format "{brand} {name}")
  </read_first>
  <action>
    Create new file per D-03. Copy the exact mock setup, factory helpers (makeStep, makeSection, makePaint), createWrapper, renderView, and setDefaultMocks functions from PaintingModeView.test.tsx. The import of PaintingModeView must come AFTER all vi.mock calls (same as analog, line 56).

    Mock setup must include all 6 vi.mock calls from the analog: @/hooks/usePaints, @/hooks/useRecipeSections, @tauri-apps/api/path, @tauri-apps/api/core, @/features/recipes/recipeSteps (isPaintMissing), @/lib/dates. Per D-18.

    The isPaintMissing mock per D-14 should match real behavior: return true if paint is null/undefined, return paint.owned !== 1 otherwise. This is the same mock already in the analog (lines 45-50).

    describe("TS-05: paintless steps") block with 3 tests per D-08, D-09, D-10:

    1. "paintless steps do not trigger PaintReadinessBanner" — Set mockUsePaints to return empty data array (no paints in inventory). Build orderedSteps with 2 steps both having paint_id: null. Render. Assert screen.queryByText(/Some paints are not in your inventory/) is NOT in the document. This proves the skip logic works.

    2. "paintless step renders without paint swatch in StepFocalView" — Build state with one step having paint_id: null, step_name: "Varnish coat". Render. Assert screen.getByTestId("step-name") has text content "Varnish coat". This confirms the step renders cleanly.

    3. "mixed painted and paintless steps — banner only reports actually missing paints" — per D-10. Build orderedSteps with: step 1 (paint_id: 10, an owned paint), step 2 (paint_id: null), step 3 (paint_id: 30, an unowned paint). Set mockUsePaints to return paint id=10 (owned: 1) and paint id=30 (name: "Mephiston Red", brand: "Citadel", owned: 0). Render. Assert banner IS present (/Some paints are not in your inventory/). Assert "Citadel Mephiston Red" text is in the document. Assert "Citadel Abaddon Black" text is NOT (it is owned). The paintless step should not cause any additional banner entry.

    describe("TS-06: missing paint warning") block with 3 tests per D-11, D-12, D-13:

    1. "banner renders and lists specific unowned paint names" — per D-11, D-12. Set mockUsePaints with paint id=10 (name: "Abaddon Black", brand: "Citadel", owned: 0) and paint id=20 (name: "Nuln Oil", brand: "Citadel", owned: 0). Build orderedSteps referencing both paint_ids. Render. Assert banner text present. Assert "Citadel Abaddon Black" and "Citadel Nuln Oil" are both in the document.

    2. "banner absent when all paints are owned" — per D-13. Use setDefaultMocks (which sets all paints owned: 1). Render with defaults. Assert screen.queryByText(/Some paints are not in your inventory/) is NOT in the document.

    3. "banner absent when only paintless steps exist and no paints returned" — Edge case combining TS-05 and TS-06. mockUsePaints returns empty array, all steps have paint_id: null. Assert no banner.

    Use beforeEach to call vi.clearAllMocks() and setDefaultMocks() — same pattern as analog.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting-mode/PaintingModeView.integration.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/painting-mode/PaintingModeView.integration.test.tsx
    - Contains describe("TS-05: paintless steps") with 3 tests
    - Contains describe("TS-06: missing paint warning") with 3 tests
    - TS-05 test 1 asserts banner is NOT present when all steps have paint_id: null
    - TS-05 test 3 asserts banner only lists unowned paints, not paintless step entries
    - TS-06 test 1 asserts specific paint names ("Citadel Abaddon Black", "Citadel Nuln Oil") appear in banner
    - TS-06 test 2 asserts banner absent when all paints owned
    - All 6 tests pass
    - Existing PaintingModeView.test.tsx tests still pass
  </acceptance_criteria>
  <done>New PaintingModeView.integration.test.tsx with 6 tests across TS-05 and TS-06 describe blocks. All painting-mode tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this is a pure test-writing plan with no production code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-02 | N/A | Test files only | accept | No production code modified; no new attack surface |
</threat_model>

<verification>
All painting-mode tests pass: `pnpm test -- tests/painting-mode/`
Full test suite passes: `pnpm test`
</verification>

<success_criteria>
- PaintingModeView.integration.test.tsx exists with TS-05 and TS-06 describe blocks
- 6 new tests pass (3 for TS-05, 3 for TS-06)
- No existing tests broken
- `pnpm test -- tests/painting-mode/` exits 0
</success_criteria>

<output>
Create `.planning/phases/88-polish-test-coverage/88-02-SUMMARY.md` when done
</output>

---
phase: 88-polish-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/painting-mode/SectionNavigator.test.tsx
  - tests/painting-mode/PaintingSessionSheet.test.tsx
autonomous: true
requirements: [TS-04, TS-07]

must_haves:
  truths:
    - "Optional sections in SectionNavigator allow step navigation without breaking"
    - "Optional sections show completion icon when all steps are done"
    - "Session prefill values match the exact prop strings passed to PaintingSessionSheet"
    - "Null sectionName is handled gracefully in session prefill"
  artifacts:
    - path: "tests/painting-mode/SectionNavigator.test.tsx"
      provides: "TS-04 optional sections test block"
      contains: "TS-04: optional sections"
    - path: "tests/painting-mode/PaintingSessionSheet.test.tsx"
      provides: "TS-07 session prefill context test block"
      contains: "TS-07: session prefill context"
  key_links:
    - from: "tests/painting-mode/SectionNavigator.test.tsx"
      to: "src/features/painting-mode/SectionNavigator.tsx"
      via: "component import"
      pattern: "import.*SectionNavigator"
    - from: "tests/painting-mode/PaintingSessionSheet.test.tsx"
      to: "src/features/painting-mode/PaintingSessionSheet.tsx"
      via: "component import"
      pattern: "import.*PaintingSessionSheet"
---

<objective>
Add test coverage for optional section handling (TS-04) and session prefill value correctness (TS-07) by augmenting two existing test files with new describe blocks.

Purpose: Verify that optional sections render correctly and do not break navigation, and that session prefill displays the exact context values passed as props.
Output: Two augmented test files with new TS-04 and TS-07 describe blocks, all tests passing.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-polish-test-coverage/88-RESEARCH.md

<interfaces>
<!-- SectionNavigator props (from source) -->
From src/features/painting-mode/SectionNavigator.tsx:
- Props: { sections: RecipeSection[], orderedSteps: RecipeStep[], completedSet: Set<number>, currentStepId: number | null, sectionProgressMap: Map<number, { completed: number; total: number; name: string }>, goToStep: (id: number) => void }
- Optional sections have `optional: 1` which renders an "Optional" Badge
- Collapsible sections: only the section containing currentStepId is open by default
- Completion: when completed === total, renders Check icon with data-testid="section-complete"

From src/features/painting-mode/PaintingSessionSheet.tsx:
- Props: { open: boolean, onClose: () => void, unitName: string, recipeName: string, stepName: string, sectionName: string | null, onSubmit: (duration: number, notes: string | null) => void, isPending: boolean }
- Renders context block showing unitName, recipeName, sectionName (if non-null), stepName as visible text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TS-04 optional sections tests in SectionNavigator.test.tsx</name>
  <files>tests/painting-mode/SectionNavigator.test.tsx</files>
  <read_first>
    - tests/painting-mode/SectionNavigator.test.tsx (current state — 177 lines, has renderNavigator helper, makeStep, makeSection factories)
    - src/features/painting-mode/SectionNavigator.tsx (component under test — optional badge, collapsible, goToStep, section-complete testid)
  </read_first>
  <action>
    Add a new describe("TS-04: optional sections") block at the end of the top-level describe("SectionNavigator") block (before the closing brace at line 177), per D-02.

    Per D-06: Add test "clicking step inside optional section calls goToStep correctly". Create an optional section with `makeSection({ id: 1, name: "Weathering", optional: 1 })` and two steps inside it (section_id: 1). Set currentStepId to the first step's ID so the collapsible is open (Pitfall 1 from RESEARCH). Click the second step's button. Assert goToStep was called with the second step's ID.

    Per D-07: Add test "optional section with all steps complete shows check icon". Create an optional section (optional: 1) with sectionProgressMap showing completed === total. Assert screen.getByTestId("section-complete") is in the document. Assert the numeric badge text (e.g. "2/2") is NOT in the document — same behavior as required sections (existing test at line 127-137 is the template).

    Use the existing renderNavigator helper and makeStep/makeSection factories. Use userEvent.setup() for the click test. Follow the existing patterns exactly (import userEvent is already at line 3).
  </action>
  <verify>
    <automated>pnpm test -- tests/painting-mode/SectionNavigator.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains describe("TS-04: optional sections") block
    - Test "clicking step inside optional section calls goToStep correctly" exists and asserts goToStep called with correct step ID
    - Test "optional section with all steps complete shows check icon" exists and asserts getByTestId("section-complete") present and numeric badge absent
    - All tests in the file pass (existing + new)
  </acceptance_criteria>
  <done>SectionNavigator.test.tsx has a TS-04 describe block with 2 tests verifying optional section navigation and completion icon. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: TS-07 session prefill context tests in PaintingSessionSheet.test.tsx</name>
  <files>tests/painting-mode/PaintingSessionSheet.test.tsx</files>
  <read_first>
    - tests/painting-mode/PaintingSessionSheet.test.tsx (current state — 110 lines, has defaultProps with unitName/recipeName/stepName/sectionName)
    - src/features/painting-mode/PaintingSessionSheet.tsx (component under test — renders context block with props)
  </read_first>
  <action>
    Add a new describe("TS-07: session prefill context") block at the end of the top-level describe("PaintingSessionSheet") block (before the closing brace at line 110), per D-04.

    Per D-15: Add test "renders all four context values matching exact prop strings". Pass DIFFERENT values from defaultProps (e.g. unitName: "Blood Angels Intercessors", recipeName: "Flesh Tearers Scheme", sectionName: "Highlights", stepName: "Edge highlight pauldrons") to prove the component renders dynamic values, not hardcoded strings. Assert each string is in the document with screen.getByText.

    Per D-16: Add test "omits section label when sectionName is null with different values". Pass unitName: "Plague Marines", recipeName: "Death Guard Scheme", sectionName: null, stepName: "Prime black". Assert unitName, recipeName, stepName are present. Assert "Plague Marines" is in the document. Assert section-related label does NOT appear (use queryByText for "Section" label if applicable, or simply verify the sectionName value is absent).

    Per D-17: The existing tests at lines 37-44 and 46-51 already check presence — the new tests add value by using different prop values to confirm dynamic rendering. No need to duplicate the existing tests.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting-mode/PaintingSessionSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains describe("TS-07: session prefill context") block
    - Test uses prop values different from defaultProps (e.g. "Blood Angels Intercessors", "Flesh Tearers Scheme")
    - Test for null sectionName uses different prop values from the existing SL-01 null test
    - All tests in the file pass (existing + new)
  </acceptance_criteria>
  <done>PaintingSessionSheet.test.tsx has a TS-07 describe block with 2 tests verifying exact value-matching of session prefill props. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this is a pure test-writing plan with no production code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | N/A | Test files only | accept | No production code modified; no new attack surface |
</threat_model>

<verification>
All painting-mode tests pass: `pnpm test -- tests/painting-mode/`
Full test suite passes: `pnpm test`
</verification>

<success_criteria>
- SectionNavigator.test.tsx contains TS-04 describe block with 2 passing tests
- PaintingSessionSheet.test.tsx contains TS-07 describe block with 2 passing tests
- No existing tests broken
- `pnpm test -- tests/painting-mode/` exits 0
</success_criteria>

<output>
Create `.planning/phases/88-polish-test-coverage/88-01-SUMMARY.md` when done
</output>

---
phase: 134-no-dead-ends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/rules-hub/RulesHubPage.tsx
  - tests/rules-hub/RulesHubSharedAbilities.test.tsx
autonomous: true
requirements: [HON-03]

must_haves:
  truths:
    - "Opening the Shared Abilities tab for a faction with canonical abilities shows real ability names and descriptions, not a blank panel"
    - "A faction with zero canonical abilities shows an explicit 'No shared abilities for this faction in the canonical database.' message"
    - "Searching with no match shows 'No shared abilities match your search.' (distinct from the no-data message)"
    - "The useSharedAbilitiesByFaction stub no longer exists and pnpm build is green (no unused stub/import/branch)"
  artifacts:
    - path: "src/features/rules-hub/RulesHubPage.tsx"
      provides: "Shared Abilities tab wired to useDetachmentAbilities via toRwAbility adapter; honest two-state empty messages"
      contains: "useDetachmentAbilities"
    - path: "tests/rules-hub/RulesHubSharedAbilities.test.tsx"
      provides: "HON-03 unit coverage: real-data render + both empty states"
      exists: true
  key_links:
    - from: "src/features/rules-hub/RulesHubPage.tsx"
      to: "useDetachmentAbilities"
      via: "hook call with selectedFactionId ?? null"
      pattern: "useDetachmentAbilities\\(selectedFactionId"
    - from: "RulesHubPage toRwAbility adapter"
      to: "SharedAbilityCard"
      via: "legend = detachment_name mapping"
      pattern: "legend:\\s*a\\.detachment_name"
---

<objective>
Remove the permanently-empty "Shared Abilities" tab dead end in the Rules Hub (HON-03).

Replace the local `useSharedAbilitiesByFaction` stub (which always returns `[]`) in
`RulesHubPage.tsx` with the EXISTING `useDetachmentAbilities(selectedFactionId ?? null)`
hook plus an inline `toRwAbility` adapter that maps the faction-scoped
`udb_detachment_abilities` rows onto the `RwAbility` shape `SharedAbilityCard` already
consumes (`detachment_name → legend`). Add the honest two-state empty message from the
UI-SPEC so a faction with genuinely-zero abilities reads as "no data", not "no search match".

Purpose: The tab currently leads the user nowhere — it is the un-stubbing planned in CONTEXT.md D-01.
Output: Real canonical ability data on the Shared Abilities tab, honest empty states, green build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-no-dead-ends/134-CONTEXT.md
@.planning/phases/134-no-dead-ends/134-PATTERNS.md
@.planning/phases/134-no-dead-ends/134-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration required. -->

Existing hook (src/hooks/useGameData.ts) — DO NOT recreate, call it:
  useDetachmentAbilities(factionId: string | null): { data: UdbDetachmentAbilityWithDetachment[]; isLoading: boolean; ... }
  // enabled: !!factionId; staleTime/gcTime Infinity. Passing null → disabled, data stays [].

Source type (src/types/gameData.ts):
  interface UdbDetachmentAbilityWithDetachment {
    id: string; faction_id: string; name: string; description: string | null;
    detachment_name: string;   // NO `legend` field
  }

Target prop type (src/types/datasheet.ts) consumed by SharedAbilityCard:
  interface RwAbility {
    id: string; name: string; legend: string | null;
    faction_id: string | null; description: string | null;
  }

Stub to DELETE (RulesHubPage.tsx ~lines 21-24):
  function useSharedAbilitiesByFaction(_factionId: string | undefined) {
    return { data: [...] as {...}[], isLoading: false };
  }

Favorites/notes wiring already present at the SharedAbilityCard map site — key pattern
is `<id>:shared_ability` against favoritesMap / notesMap. Do NOT change it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — RulesHubSharedAbilities test stub (RED)</name>
  <files>tests/rules-hub/RulesHubSharedAbilities.test.tsx</files>
  <read_first>
    - tests/rules-hub/RulesHubPage.test.tsx (existing test structure + vi.mock shape for @/hooks/useGameData)
    - tests/units/PlaybookDetachmentAbilities.test.tsx (QueryClient wrapper + renderComponent helper pattern)
    - .planning/phases/134-no-dead-ends/134-VALIDATION.md (Wave 0 requirements + sampling)
    - .planning/phases/134-no-dead-ends/134-UI-SPEC.md (Copywriting Contract → HON-03 exact empty-state strings)
  </read_first>
  <action>
    Create the new test file mirroring tests/rules-hub/RulesHubPage.test.tsx infrastructure
    (Tauri SQL already mocked via tests/setup.ts; ResizeObserver/scrollIntoView polyfilled).
    Mock `@/hooks/useGameData` so `useDetachmentAbilities` is a `vi.fn`, alongside the
    existing `useStratagemsByFaction` and `useDetachmentsByFaction` mocks. Write these
    cases (initially RED — they describe the post-implementation behavior):
    (1) when `useDetachmentAbilities` returns abilities for a selected faction, the Shared
    Abilities tab renders at least one ability NAME from the fixture (proves real data, not stub);
    (2) when it returns `[]` and the search box is empty, the exact string
    "No shared abilities for this faction in the canonical database." is present;
    (3) when it returns data but the active search matches nothing, the exact string
    "No shared abilities match your search." is present.
    Use fixture rows shaped as `UdbDetachmentAbilityWithDetachment` (id, faction_id, name,
    description, detachment_name). Drive faction selection the same way RulesHubPage.test.tsx does.
  </action>
  <verify>
    <automated>pnpm test -- tests/rules-hub/RulesHubSharedAbilities.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File tests/rules-hub/RulesHubSharedAbilities.test.tsx exists and is picked up by Vitest.
    - It mocks `@/hooks/useGameData` including `useDetachmentAbilities`.
    - Three cases assert: real ability name rendered; exact no-data string; exact no-search-match string.
    - Tests run (RED is acceptable at this task — they fail because the stub still returns []).
    - No reliance on a live DB; Tauri SQL stays mocked.
  </acceptance_criteria>
  <done>Test file exists, runs under Vitest, and asserts the three HON-03 behaviors using the exact UI-SPEC strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap the stub for useDetachmentAbilities + adapter + honest empty states (GREEN)</name>
  <files>src/features/rules-hub/RulesHubPage.tsx</files>
  <behavior>
    - Selecting a faction with canonical detachment abilities renders SharedAbilityCard rows with real names/descriptions.
    - Faction with zero abilities + empty search → "No shared abilities for this faction in the canonical database."
    - Data present + non-matching search → "No shared abilities match your search."
    - Loading state shows the same 3× Skeleton h-[80px] pattern the Stratagems/Detachments tabs use.
  </behavior>
  <read_first>
    - src/features/rules-hub/RulesHubPage.tsx (the stub at ~21-24; the Detachments tab block ~253-278 and its loading-skeleton/empty-state analogs; the shared-abilities TabsContent ~281-307; favorites/notes map wiring)
    - src/features/rules-hub/SharedAbilityCard.tsx (RwAbility prop contract; legend rendered as badge; sanitizeRulesHtml on description)
    - .planning/phases/134-no-dead-ends/134-PATTERNS.md (RulesHubPage section: exact import lines, toRwAbility adapter, hook-call line, empty-state JSX, skeleton JSX)
    - src/hooks/useGameData.ts (useDetachmentAbilities signature: string | null)
  </read_first>
  <action>
    Delete the `useSharedAbilitiesByFaction` stub function entirely (no dead code may remain
    — strict TS `noUnusedLocals` will fail the build otherwise). Add `useDetachmentAbilities`
    to the existing `@/hooks/useGameData` import, and import `UdbDetachmentAbilityWithDetachment`
    from `@/types/gameData` and `RwAbility` from `@/types/datasheet`. Add a module-scope
    `toRwAbility(a: UdbDetachmentAbilityWithDetachment): RwAbility` adapter that maps
    id/name/description/faction_id straight through and sets `legend: a.detachment_name`.
    Replace the stub hook call with `useDetachmentAbilities(selectedFactionId ?? null)`
    (the hook takes `string | null`, so `?? null` — NOT `?? undefined`), capturing
    `data: rawAbilities` and `isLoading: sharedAbilitiesLoading`. Map
    `rawAbilities.map(toRwAbility)` into the array the tab already filters/renders, keeping the
    existing favorites/notes wiring (`a.id + ':shared_ability'`) unchanged. Reuse the
    Detachments tab's loading skeleton verbatim (3× `Skeleton h-[80px] w-full rounded-lg`).
    Update the empty state to the two-state form: when `searchText` is truthy →
    "No shared abilities match your search."; otherwise →
    "No shared abilities for this faction in the canonical database." Keep the existing
    count label and tab trigger copy unchanged. Per D-01/D-02: no new query, no migration —
    only the existing `getDetachmentAbilitiesByFaction`-backed hook.
  </action>
  <verify>
    <automated>pnpm test -- tests/rules-hub/RulesHubSharedAbilities.test.tsx && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - The `useSharedAbilitiesByFaction` stub is fully removed; grep for it returns nothing in src/.
    - `useDetachmentAbilities(selectedFactionId ?? null)` is the data source (Task-1 test case 1 passes — a real ability name renders).
    - `toRwAbility` maps `legend: a.detachment_name`.
    - Empty-state strings exactly match the UI-SPEC: the no-data string and the no-search-match string both present and used in their correct branches.
    - All three Wave-0 tests are GREEN.
    - `pnpm build` exits 0 — strict TS (`noUnusedLocals`/`noUnusedParameters`) confirms no leftover stub, unused import, or dead branch (HON-02-style proof).
  </acceptance_criteria>
  <done>Shared Abilities tab renders real canonical abilities with honest empty states; stub gone; Wave-0 tests green; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → local SQLite (read-only) | Faction-scoped read via the existing `getDetachmentAbilitiesByFaction` query |

This is a local, offline, single-user Tauri desktop app. No network calls, no auth, no
external input in this plan's scope. HON-03 is a read-only display change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-01 | Tampering | `getDetachmentAbilitiesByFaction` faction filter | accept | Query already uses `$1` parameterization (existing, unchanged here); no string concatenation introduced by this plan |
| T-134-02 | Information disclosure | `SharedAbilityCard` `dangerouslySetInnerHTML` on ability description | accept | `sanitizeRulesHtml` already sanitizes the HTML before render (existing path, unchanged); data is bundled canonical content, not user/network input |

No network, auth, or injection surface is created by this plan.
</threat_model>

<verification>
- `pnpm test -- tests/rules-hub/RulesHubSharedAbilities.test.tsx` — all three HON-03 cases green.
- `pnpm test -- tests/rules-hub` — no regression in sibling Rules Hub tests.
- `pnpm build` — strict TS + vite green; no unused stub/import/branch remains.
</verification>

<success_criteria>
- The Rules Hub "Shared Abilities" tab displays real faction shared/army-rule abilities from the canonical database (ROADMAP success criterion 1 / HON-03).
- A faction with zero abilities shows the honest no-data message; a non-matching search shows the search message.
- The `useSharedAbilitiesByFaction` stub is removed and `pnpm build` is green.
</success_criteria>

<output>
Create `.planning/phases/134-no-dead-ends/134-01-SUMMARY.md` when done.
</output>

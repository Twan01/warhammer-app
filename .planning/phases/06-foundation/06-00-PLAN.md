---
phase: 06-foundation
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/foundation/migration004.test.ts
  - tests/foundation/armyListQueries.test.ts
  - tests/foundation/strategyNoteQueries.test.ts
  - tests/foundation/usePaints.test.ts
autonomous: true
requirements:
  - STRAT-06
must_haves:
  truths:
    - "All four Wave-0 test files exist under tests/foundation/"
    - "Each test file declares describe blocks matching VALIDATION.md -t filter strings (migration, armyLists, strategyNotes, paints, usePaints)"
    - "All tests use it.skip() with a placeholder body so pnpm test exits 0 with skipped tests reported"
    - "pnpm test discovers the new files and runs them without errors"
  artifacts:
    - path: "tests/foundation/migration004.test.ts"
      provides: "STRAT-06 migration content stubs"
      contains: 'describe("migration"'
    - path: "tests/foundation/armyListQueries.test.ts"
      provides: "Army list query mock stubs"
      contains: 'describe("armyLists"'
    - path: "tests/foundation/strategyNoteQueries.test.ts"
      provides: "Strategy note upsert stubs"
      contains: 'describe("strategyNotes"'
    - path: "tests/foundation/usePaints.test.ts"
      provides: "Paint hook double-invalidation stubs"
      contains: 'describe("usePaints"'
  key_links:
    - from: "tests/foundation/*"
      to: "vitest.config.ts include pattern"
      via: "tests/**/*.test.ts glob"
      pattern: "tests/foundation"
---

<objective>
Create Wave-0 test scaffolds for all four foundation test files identified in 06-VALIDATION.md.

Purpose: Establish the test contract before implementation so Wave 1 and 2 tasks can fill in test bodies in-place (per the 03-00 stub pattern decision in STATE.md). This satisfies the Nyquist rule — every later task has an `<automated>` command pointing at a real file.
Output: 4 stub test files under `tests/foundation/` with `describe` blocks named to match VALIDATION.md `-t` filter strings, plus `it.skip()` placeholders for each behavior. `pnpm test` runs green with skipped tests reported.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-foundation/06-CONTEXT.md
@.planning/phases/06-foundation/06-RESEARCH.md
@.planning/phases/06-foundation/06-VALIDATION.md

@vitest.config.ts
@tests/setup.ts

<interfaces>
<!-- Test infrastructure already in place from Phase 3 plan 03-00 -->

vitest.config.ts include pattern:
```
include: ["tests/**/*.test.ts", "tests/**/*.test.tsx"]
```

Stub pattern from STATE.md (03-00 decision): use `it.skip()` with VALIDATION.md `-t` filter strings as describe-block names. Globals enabled in vitest.config.ts (`globals: true`) — no need to import describe/it/expect.

Existing test layout uses subdirectories:
- tests/collection/
- tests/painting/
- tests/dashboard/

Phase 6 adds tests/foundation/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create four Wave-0 test stub files under tests/foundation/</name>
  <files>tests/foundation/migration004.test.ts, tests/foundation/armyListQueries.test.ts, tests/foundation/strategyNoteQueries.test.ts, tests/foundation/usePaints.test.ts</files>
  <read_first>
    - tests/setup.ts (current global polyfills — ResizeObserver, scrollIntoView, cleanup)
    - vitest.config.ts (include glob pattern, globals:true)
    - tests/dashboard/useDashboardStats.test.ts (reference for describe-block naming + literal-equality assertion style)
    - .planning/phases/06-foundation/06-VALIDATION.md (Wave 0 Requirements section — exact behaviors to stub)
    - .planning/phases/06-foundation/06-RESEARCH.md (Pattern 1 + Pattern 8 + Pattern 6 — concrete describe targets)
  </read_first>
  <action>
Create exactly 4 files with the contents below verbatim. Each file uses `describe(...)` and `it.skip(...)` only — no imports of describe/it/expect (vitest globals:true). No mocks, no real assertions yet — Wave 1 and 2 tasks fill these in.

**File 1: tests/foundation/migration004.test.ts**
```ts
/**
 * STRAT-06 — Migration 004 file content stubs.
 *
 * Wave 0: skip-stub. 06-01 fills in real assertions that read
 * src-tauri/migrations/004_unit_playbook_stats.sql as a string and
 * verify ALTER-TABLE-only / column list / save INTEGER.
 */
describe("migration 004 unit_playbook_stats — file content", () => {
  it.skip("contains only ALTER TABLE ADD COLUMN statements (no DROP, no CREATE TABLE)", () => {});
  it.skip("declares all 8 new columns: move, toughness, save, wounds, leadership, objective_control, keywords, abilities", () => {});
  it.skip("declares save column as INTEGER (not TEXT)", () => {});
  it.skip("declares move/toughness/wounds/leadership/objective_control as INTEGER", () => {});
  it.skip("declares keywords and abilities as TEXT", () => {});
});

describe("migration 004 — lib.rs registration", () => {
  it.skip("src-tauri/src/lib.rs contains version: 4 entry referencing 004_unit_playbook_stats.sql", () => {});
});
```

**File 2: tests/foundation/armyListQueries.test.ts**
```ts
/**
 * STRAT-06 (Phase 6 Success Criteria 4) — Army list query function stubs.
 *
 * Wave 0: skip-stub. 06-03 fills these in by mocking getDb() and asserting
 * SQL strings + parameter arrays. tauri-plugin-sql IPC cannot run in jsdom,
 * so all tests mock the client.
 */
describe("armyLists queries — getArmyLists / getArmyListWithUnits", () => {
  it.skip("getArmyLists() calls db.select with 'SELECT * FROM army_lists ORDER BY name ASC'", () => {});
  it.skip("getArmyListWithUnits(listId) JOINs units and computes COALESCE(points_override, u.points, 0) AS effective_points", () => {});
});

describe("armyLists queries — createArmyList / updateArmyList / deleteArmyList", () => {
  it.skip("createArmyList INSERTs name, faction_id, points_limit, list_type, notes and returns lastInsertId", () => {});
  it.skip("updateArmyList uses COALESCE partial-update pattern matching updateUnit/updatePaint", () => {});
  it.skip("deleteArmyList runs DELETE FROM army_lists WHERE id = $1", () => {});
});

describe("armyLists queries — addUnitToList / removeUnitFromList", () => {
  it.skip("addUnitToList INSERTs list_id, unit_id, points_override, notes (allows duplicate unit_id for same list_id)", () => {});
  it.skip("removeUnitFromList runs DELETE FROM army_list_units WHERE id = $1", () => {});
});

describe("armyLists queries — updateArmyListUnit (NULL-passthrough)", () => {
  it.skip("UPDATE statement is 'SET points_override=$2, notes=$3' — NOT COALESCE — so points_override can be cleared to NULL", () => {});
  it.skip("passing { id, points_override: null, notes: null } sets both columns to NULL in the DB", () => {});
});
```

**File 3: tests/foundation/strategyNoteQueries.test.ts**
```ts
/**
 * STRAT-06 (Phase 6 Success Criteria 4) — Strategy note query stubs.
 *
 * Wave 0: skip-stub. 06-03 fills these in by mocking getDb().
 * The upsert uses select-then-insert/update because no UNIQUE INDEX exists
 * on unit_strategy_notes.unit_id (06-CONTEXT discretion: do NOT add it).
 */
describe("strategyNotes queries — getStrategyNote", () => {
  it.skip("returns null when no row exists for the given unit_id", () => {});
  it.skip("returns the row with all 18 columns (10 existing + 8 new from migration 004) when one exists", () => {});
});

describe("strategyNotes queries — upsertStrategyNote (select-then-insert/update)", () => {
  it.skip("INSERT path: when SELECT returns no row, runs INSERT with all 17 fields including new stat columns", () => {});
  it.skip("UPDATE path: when SELECT returns a row, runs UPDATE WHERE unit_id=$1 with all 16 fields + datetime('now') for updated_at", () => {});
  it.skip("save column accepts integer (3) and stores as INTEGER — never as string '3+'", () => {});
});
```

**File 4: tests/foundation/usePaints.test.ts**
```ts
/**
 * Phase 6 Success Criteria 5 — usePaints mutations invalidate both
 * PAINTS_KEY (['paints']) and PAINTS_WITH_RECIPES_KEY (['paints-with-recipes']).
 *
 * Wave 0: skip-stub. 06-04 fills these in using QueryClient + renderHook
 * + a spy on queryClient.invalidateQueries.
 */
describe("usePaints — PAINTS_WITH_RECIPES_KEY constant", () => {
  it.skip("PAINTS_WITH_RECIPES_KEY equals ['paints-with-recipes'] literal", () => {});
});

describe("usePaints — useCreatePaint onSuccess invalidations", () => {
  it.skip("invalidates PAINTS_KEY (['paints'])", () => {});
  it.skip("invalidates PAINTS_WITH_RECIPES_KEY (['paints-with-recipes'])", () => {});
});

describe("usePaints — useUpdatePaint onSuccess invalidations", () => {
  it.skip("invalidates PAINTS_KEY", () => {});
  it.skip("invalidates PAINT_KEY(id) for the updated paint", () => {});
  it.skip("invalidates PAINTS_WITH_RECIPES_KEY", () => {});
});

describe("usePaints — useDeletePaint onSuccess invalidations", () => {
  it.skip("invalidates PAINTS_KEY", () => {});
  it.skip("invalidates PAINTS_WITH_RECIPES_KEY", () => {});
});

describe("usePaintsWithRecipeCount", () => {
  it.skip("queries with key PAINTS_WITH_RECIPES_KEY and queryFn getPaintsWithRecipeCount", () => {});
});
```

Each file is pure stubs — no imports needed beyond what vitest globals provide. No production code touched in this task.
  </action>
  <verify>
    <automated>pnpm test -- --run tests/foundation</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/foundation/migration004.test.ts` exists
    - File `tests/foundation/armyListQueries.test.ts` exists
    - File `tests/foundation/strategyNoteQueries.test.ts` exists
    - File `tests/foundation/usePaints.test.ts` exists
    - `tests/foundation/migration004.test.ts` contains literal string `describe("migration 004 unit_playbook_stats — file content"`
    - `tests/foundation/armyListQueries.test.ts` contains literal string `describe("armyLists queries — getArmyLists / getArmyListWithUnits"`
    - `tests/foundation/armyListQueries.test.ts` contains literal string `describe("armyLists queries — updateArmyListUnit (NULL-passthrough)"`
    - `tests/foundation/strategyNoteQueries.test.ts` contains literal string `describe("strategyNotes queries — upsertStrategyNote (select-then-insert/update)"`
    - `tests/foundation/usePaints.test.ts` contains literal string `describe("usePaints — PAINTS_WITH_RECIPES_KEY constant"`
    - `tests/foundation/usePaints.test.ts` contains literal string `describe("usePaints — useDeletePaint onSuccess invalidations"`
    - `pnpm test -- --run tests/foundation` exits with code 0
    - `pnpm test -- --run tests/foundation` reports skipped tests (no real assertions yet)
    - No new production code files created (only test stubs)
  </acceptance_criteria>
  <done>All four stub test files exist under tests/foundation/, pnpm test runs them as skipped suites, exit code 0.</done>
</task>

</tasks>

<verification>
- All four files present under tests/foundation/
- `pnpm test -- --run tests/foundation` exits 0 with skipped tests
- No imports outside vitest globals (no `import { describe, it } from "vitest"` lines)
- No production code touched (lib.rs, migrations/, src/db/, src/hooks/, src/types/ unchanged)
</verification>

<success_criteria>
- Wave-0 test scaffold complete: 4 files, all describe blocks named per VALIDATION.md `-t` filter strings
- Subsequent plans (06-01, 06-03, 06-04) can replace `it.skip(...)` with real assertions in-place without restructuring
- pnpm test runs green
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation/06-00-SUMMARY.md` summarizing the stub structure (file paths + describe blocks created).
</output>

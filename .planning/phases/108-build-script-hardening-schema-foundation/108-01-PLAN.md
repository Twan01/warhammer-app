---
phase: 108-build-script-hardening-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/parseCsv.ts
  - scripts/lib/parseXml.ts
  - scripts/lib/normalize.ts
  - scripts/lib/factionMap.ts
  - scripts/lib/types.ts
  - scripts/data/aliases.json
  - scripts/build-unit-db.ts
  - scripts/update-unit-database.ts
autonomous: true
requirements: [DQ-02, DQ-03, DQ-04, DQ-07]

must_haves:
  truths:
    - "Running the build script twice on the same input data produces identical output"
    - "Shared parsing logic lives in scripts/lib/ and is imported by both build scripts"
    - "Name normalization handles lowercase, special chars, whitespace, smart quotes"
    - "An aliases.json file exists for manual name mapping fallbacks"
  artifacts:
    - path: "scripts/lib/parseCsv.ts"
      provides: "Wahapedia CSV parser extracted from build-unit-db.ts"
      exports: ["parseWahapediaCsv"]
    - path: "scripts/lib/parseXml.ts"
      provides: "BSData XML parser extracted from build-unit-db.ts"
      exports: ["parseCatXml", "extractTiers", "extractModelCounts"]
    - path: "scripts/lib/normalize.ts"
      provides: "Name normalization and alias loading"
      exports: ["normalizeName", "loadAliases"]
    - path: "scripts/lib/factionMap.ts"
      provides: "FACTION_MAP and SUB_FACTION_MAP constants"
      exports: ["FACTION_MAP", "SUB_FACTION_MAP"]
    - path: "scripts/lib/types.ts"
      provides: "Shared TypeScript interfaces for build pipeline"
      exports: ["PointsTier", "BsdataUnitPoints", "BsdataModelCount"]
    - path: "scripts/data/aliases.json"
      provides: "Manual Wahapedia-to-BSData name mappings"
  key_links:
    - from: "scripts/build-unit-db.ts"
      to: "scripts/lib/parseCsv.ts"
      via: "import"
      pattern: "import.*parseCsv"
    - from: "scripts/update-unit-database.ts"
      to: "scripts/lib/parseCsv.ts"
      via: "import"
      pattern: "import.*parseCsv"
---

<objective>
Extract shared parsing logic to scripts/lib/, add deterministic file sorting, implement name normalization, and create the alias table -- establishing the shared library foundation that both build scripts depend on.

Purpose: DQ-02 (determinism), DQ-03 (normalization), DQ-04 (aliases), DQ-07 (shared lib) are all prerequisites for the multi-pass matching and coverage reporting in Plan 02.
Output: Five shared library modules in scripts/lib/, an aliases.json data file, and both build scripts refactored to import from the shared library with sorted file reads.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-build-script-hardening-schema-foundation/108-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/lib/ shared modules and aliases.json</name>
  <files>scripts/lib/types.ts, scripts/lib/parseCsv.ts, scripts/lib/parseXml.ts, scripts/lib/normalize.ts, scripts/lib/factionMap.ts, scripts/data/aliases.json</files>
  <read_first>
    - scripts/build-unit-db.ts (full file -- source of extraction)
    - scripts/update-unit-database.ts (lines 1-200 -- verify duplicated functions)
  </read_first>
  <action>
    Create the scripts/lib/ directory with five modules extracted from build-unit-db.ts:

    1. **scripts/lib/types.ts** -- Extract interfaces PointsTier (lines 101-104), BsdataUnitPoints (lines 106-111), BsdataModelCount (lines ~192-197) from build-unit-db.ts. Export all three.

    2. **scripts/lib/parseCsv.ts** -- Extract function parseWahapediaCsv (lines 41-51) verbatim. Import nothing from lib (pure function). Export it as a named export. Also export a readCsv helper that wraps readFileSync + parseWahapediaCsv for convenience.

    3. **scripts/lib/parseXml.ts** -- Extract functions extractTiers (lines 113-136), parseCatXml (lines 138-187), and extractModelCounts (lines ~199+) from build-unit-db.ts. Import types from ./types. These functions rely on globalThis.DOMParser which is polyfilled in the entry-point scripts, NOT in this module. Add a JSDoc note about that.

    4. **scripts/lib/normalize.ts** -- New capability per D-01/D-03. Create normalizeName(name: string): string that lowercases, replaces smart quotes with straight apostrophe, strips non-alphanumeric chars (keeping apostrophes and spaces), collapses whitespace, trims. Create loadAliases(filePath: string): Record<string, string> that reads and JSON.parses aliases.json, returning empty object if file missing.

    5. **scripts/lib/factionMap.ts** -- Extract the FACTION_MAP constant (lines 57-96) from build-unit-db.ts verbatim. Add SUB_FACTION_MAP per D-09 with entries for SM chapters (Black Templars, Blood Angels, Dark Angels, Deathwatch, Imperial Fists, Iron Hands, Raven Guard, Salamanders, Space Wolves, Ultramarines, White Scars), CSM warbands (Death Guard, Thousand Sons, World Eaters, Emperor's Children), and Aeldari (Drukhari, Ynnari). Keys are BSData catalogue names like "Imperium - Black Templars", values are the sub-faction label like "Black Templars".

    6. **scripts/data/aliases.json** -- Create an initial empty object {} as the alias table skeleton per D-02/D-04. This will be populated after running the build and inspecting unmatched units.

    All scripts/lib/ files use Node.js import syntax (import from "node:fs" etc.) with NO Vite aliases.
  </action>
  <verify>
    <automated>node --experimental-strip-types -e "import { parseWahapediaCsv } from './scripts/lib/parseCsv.ts'; import { normalizeName } from './scripts/lib/normalize.ts'; import { FACTION_MAP, SUB_FACTION_MAP } from './scripts/lib/factionMap.ts'; console.log('parseCsv:', typeof parseWahapediaCsv); console.log('normalize:', normalizeName('Intercessor   Squad')); console.log('FACTION_MAP keys:', Object.keys(FACTION_MAP).length); console.log('SUB_FACTION_MAP keys:', Object.keys(SUB_FACTION_MAP).length);"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/lib/types.ts exports PointsTier, BsdataUnitPoints, BsdataModelCount interfaces
    - scripts/lib/parseCsv.ts exports parseWahapediaCsv function that splits pipe-delimited CSV into Record array
    - scripts/lib/parseXml.ts exports extractTiers, parseCatXml, extractModelCounts
    - scripts/lib/normalize.ts exports normalizeName that returns "intercessor squad" for input "Intercessor   Squad"
    - scripts/lib/normalize.ts exports loadAliases that returns {} for empty/missing file
    - scripts/lib/factionMap.ts exports FACTION_MAP with same entries as build-unit-db.ts lines 57-96
    - scripts/lib/factionMap.ts exports SUB_FACTION_MAP with entries for SM chapters, CSM warbands, Aeldari sub-factions
    - scripts/data/aliases.json exists and contains {}
    - All files importable via node --experimental-strip-types without errors
  </acceptance_criteria>
  <done>All five shared library modules exist and are importable. Alias table skeleton exists.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor build-unit-db.ts and update-unit-database.ts to use shared lib</name>
  <files>scripts/build-unit-db.ts, scripts/update-unit-database.ts</files>
  <read_first>
    - scripts/build-unit-db.ts (full file -- identify all inlined functions to replace)
    - scripts/update-unit-database.ts (full file -- identify all inlined functions to replace)
    - scripts/lib/parseCsv.ts (Task 1 output -- import targets)
    - scripts/lib/parseXml.ts (Task 1 output -- import targets)
    - scripts/lib/factionMap.ts (Task 1 output -- import targets)
    - scripts/lib/types.ts (Task 1 output -- import targets)
  </read_first>
  <action>
    Refactor both scripts to import from scripts/lib/ instead of inlining:

    **build-unit-db.ts:**
    1. Remove inlined parseWahapediaCsv (lines 41-51), FACTION_MAP (lines 57-96), PointsTier/BsdataUnitPoints/BsdataModelCount interfaces, extractTiers, parseCatXml, extractModelCounts functions.
    2. Add imports: parseWahapediaCsv from ./lib/parseCsv, parseCatXml/extractTiers/extractModelCounts from ./lib/parseXml, FACTION_MAP/SUB_FACTION_MAP from ./lib/factionMap, types from ./lib/types.
    3. Per D-06: add .sort() call on every readdirSync result before processing. Specifically: the catFiles array from readdirSync(bsdataDir) and any Wahapedia CSV file listing.
    4. Keep the DOMParser polyfill in the entry point (lines 25-27). Do NOT move it to lib/.

    **update-unit-database.ts:**
    1. Same extraction: remove inlined parseWahapediaCsv, FACTION_MAP, extractTiers, parseCatXml, extractModelCounts, and type definitions.
    2. Add same imports from ./lib/ modules.
    3. Per D-06: add .sort() on all readdirSync results.
    4. Keep the DOMParser polyfill in the entry point.

    After refactoring, both scripts should have ~300-500 fewer lines each, with all shared logic imported from scripts/lib/.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/build-unit-db.ts 2>&1 | Select-String -Pattern "error|Error" -NotMatch | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - build-unit-db.ts has import statements for parseCsv, parseXml, factionMap, types from ./lib/
    - build-unit-db.ts does NOT contain the inlined parseWahapediaCsv function definition
    - build-unit-db.ts does NOT contain the inlined FACTION_MAP constant definition
    - build-unit-db.ts calls .sort() on readdirSync results before processing
    - update-unit-database.ts has matching import statements from ./lib/
    - update-unit-database.ts does NOT contain duplicated parsing functions
    - update-unit-database.ts calls .sort() on readdirSync results before processing
    - Running build-unit-db.ts produces unit_database.json without errors
    - Running build-unit-db.ts twice produces identical output (byte-for-byte determinism per D-06)
  </acceptance_criteria>
  <done>Both build scripts use shared lib modules. File reads are sorted. No duplicated parsing code remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/data/ files -> build pipeline | Wahapedia CSV and BSData XML are untrusted external data parsed by the build scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Tampering | scripts/data/ CSV/XML files | accept | Dev-side build tool only; files are manually placed by developer; no runtime exposure |
| T-108-02 | Information Disclosure | aliases.json | accept | Contains only unit name mappings, no PII or secrets |
</threat_model>

<verification>
- `node --experimental-strip-types scripts/build-unit-db.ts` completes without error
- Output unit_database.json is valid JSON
- Running build twice produces identical output
- All scripts/lib/ modules importable independently
</verification>

<success_criteria>
- Shared lib modules in scripts/lib/ eliminate ~500 lines of duplication (DQ-07)
- Both scripts use sorted file reads for determinism (DQ-02)
- normalizeName function handles smart quotes, special chars, whitespace (DQ-03)
- aliases.json skeleton exists for manual fallback mapping (DQ-04)
</success_criteria>

<output>
Create `.planning/phases/108-build-script-hardening-schema-foundation/108-01-SUMMARY.md` when done
</output>

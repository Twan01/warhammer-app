---
phase: 70-non-destructive-recipe-save
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/recipes/recipeSteps.ts
  - src/features/recipes/recipeSection.ts
  - src/types/recipePaint.ts
  - src/db/queries/recipePaints.ts
  - tests/painting/recipeSteps.test.ts
  - tests/painting/recipeSection.pure.test.ts
autonomous: true
requirements: [REC-02]

must_haves:
  truths:
    - "DraftStep interface has a dbId field of type number | null"
    - "DraftSection interface has a dbId field of type number | null"
    - "makeDraftStep() returns dbId: null"
    - "makeDraftSection() returns dbId: null"
    - "buildDraftSections populates dbId from the DB row id field on both sections and steps"
    - "updateRecipeStep function exists in recipePaints.ts and UPDATEs all 13 mutable step columns"
    - "UpdateRecipeStepInput type exists in types/recipePaint.ts"
  artifacts:
    - path: "src/features/recipes/recipeSteps.ts"
      provides: "DraftStep with dbId + makeDraftStep with dbId: null"
      contains: "dbId: number | null"
    - path: "src/features/recipes/recipeSection.ts"
      provides: "DraftSection with dbId + buildDraftSections populating dbId from DB"
      contains: "dbId: s.id"
    - path: "src/types/recipePaint.ts"
      provides: "UpdateRecipeStepInput type"
      contains: "UpdateRecipeStepInput"
    - path: "src/db/queries/recipePaints.ts"
      provides: "updateRecipeStep query function"
      contains: "export async function updateRecipeStep"
  key_links:
    - from: "src/features/recipes/recipeSection.ts"
      to: "src/features/recipes/recipeSteps.ts"
      via: "DraftStep import — DraftSection.steps uses DraftStep with dbId"
      pattern: "dbId: st\\.id"
    - from: "src/db/queries/recipePaints.ts"
      to: "src/types/recipePaint.ts"
      via: "UpdateRecipeStepInput import"
      pattern: "import type.*UpdateRecipeStepInput"
---

<objective>
Add `dbId: number | null` tracking to DraftStep and DraftSection interfaces, populate it in buildDraftSections from DB row IDs, create the UpdateRecipeStepInput type, and add the updateRecipeStep query function. Extend existing tests to cover the new field.

Purpose: These are the contracts and data-layer functions that Plan 02 needs to implement the three-way diff in RecipeFormSheet.onSubmit. Without dbId, the save flow cannot match draft items to their DB rows.

Output: Modified recipeSteps.ts, recipeSection.ts, recipePaint.ts (type), recipePaints.ts (query), extended test files.
</objective>

<execution_context>
@.planning/phases/70-non-destructive-recipe-save/70-RESEARCH.md
@.planning/phases/70-non-destructive-recipe-save/70-PATTERNS.md
@.planning/phases/70-non-destructive-recipe-save/70-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/features/recipes/recipeSteps.ts:
- DraftStep interface (lines 3-15): localId, step_name, paint_id, notes, painting_phase, tool, technique, dilution, time_estimate_minutes, step_photo_path, alt_paint_id
- makeDraftStep() factory (lines 17-31): returns DraftStep with all null fields
- computeOrderIndex(steps) (lines 33-37): spread-maps steps with order_index

From src/features/recipes/recipeSection.ts:
- DraftSection interface (lines 18-32): localId, name, surface, optional, notes, section_type, technique, execution_mode, applies_to, steps: DraftStep[]
- makeDraftSection(name?) factory (lines 38-51): returns empty DraftSection
- buildDraftSections(sections, steps) (lines 67-104): converts DB RecipeSection[] + RecipeStep[] to DraftSection[]; currently discards s.id and st.id

From src/types/recipePaint.ts:
- RecipeStep interface: id, recipe_id, paint_id, step_name, order_index, notes, painting_phase, tool, technique, dilution, time_estimate_minutes, step_photo_path, alt_paint_id, section_id, created_at
- CreateRecipeStepInput = Omit<RecipeStep, "id" | "created_at">

From src/types/recipeSection.ts:
- UpdateRecipeSectionInput = Partial<Omit<CreateRecipeSectionInput, "recipe_id">> & { id: number }

From src/db/queries/recipePaints.ts:
- addRecipePaint(input: CreateRecipeStepInput): Promise<number>
- removeRecipePaint(id: number): Promise<void>
- Line 38 comment: "No updateRecipePaint — links are immutable; to change, remove + re-add."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dbId to DraftStep and DraftSection + populate in buildDraftSections</name>
  <files>src/features/recipes/recipeSteps.ts, src/features/recipes/recipeSection.ts, tests/painting/recipeSteps.test.ts, tests/painting/recipeSection.pure.test.ts</files>
  <read_first>
    - src/features/recipes/recipeSteps.ts (full file — 43 lines)
    - src/features/recipes/recipeSection.ts (full file — 104 lines)
    - tests/painting/recipeSteps.test.ts (full file — 125 lines)
    - tests/painting/recipeSection.pure.test.ts (full file — 235 lines)
  </read_first>
  <behavior>
    - Test: makeDraftStep().dbId === null
    - Test: makeDraftSection().dbId === null
    - Test: buildDraftSections populates dbId from section.id on each DraftSection (e.g., SECTION_1.id=1 -> result[0].dbId === 1)
    - Test: buildDraftSections populates dbId from step.id on each nested DraftStep (e.g., STEP_A.id=101 -> result[0].steps[0].dbId === 101)
    - Test: computeOrderIndex preserves dbId through spread (input with dbId:42 -> output has dbId:42)
    - Test: existing step() helper in recipeSteps.test.ts must include dbId: null (update helper to avoid TS errors)
  </behavior>
  <action>
    Per D-01, D-02, D-03:

    In recipeSteps.ts:
    - Add `dbId: number | null` to DraftStep interface immediately after `localId` field
    - Add `dbId: null` to makeDraftStep() return object immediately after `localId`

    In recipeSection.ts:
    - Add `dbId: number | null` to DraftSection interface immediately after `localId` field
    - Add `dbId: null` to makeDraftSection() return object immediately after `localId`
    - In buildDraftSections, add `dbId: st.id` to the step map (line 77 area, after localId)
    - In buildDraftSections, add `dbId: s.id` to the section return (line 92 area, after localId)

    In recipeSteps.test.ts:
    - Update the `step()` helper function to include `dbId: null` field after `localId`
    - Add test for makeDraftStep().dbId === null
    - Add test for computeOrderIndex preserving dbId through spread

    In recipeSection.pure.test.ts:
    - Add test for makeDraftSection().dbId === null (extend Test 1)
    - Add test for buildDraftSections populating dbId from section.id
    - Add test for buildDraftSections populating dbId from step.id (nested steps)
  </action>
  <verify>
    <automated>pnpm test -- tests/painting/recipeSteps.test.ts tests/painting/recipeSection.pure.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - DraftStep interface in recipeSteps.ts contains `dbId: number | null` between localId and step_name
    - DraftSection interface in recipeSection.ts contains `dbId: number | null` between localId and name
    - makeDraftStep() returns object with dbId: null
    - makeDraftSection() returns object with dbId: null
    - buildDraftSections output sections have dbId matching input section.id (e.g., section with id=1 produces DraftSection with dbId=1)
    - buildDraftSections output steps have dbId matching input step.id (e.g., step with id=101 produces DraftStep with dbId=101)
    - step() helper in recipeSteps.test.ts includes dbId: null
    - All tests in both files pass
  </acceptance_criteria>
  <done>DraftStep.dbId and DraftSection.dbId exist, factory functions set null, buildDraftSections populates from DB row IDs, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add UpdateRecipeStepInput type and updateRecipeStep query function</name>
  <files>src/types/recipePaint.ts, src/db/queries/recipePaints.ts</files>
  <read_first>
    - src/types/recipePaint.ts (full file — 31 lines)
    - src/db/queries/recipePaints.ts (full file — 145 lines)
    - src/types/recipeSection.ts (lines 43-44 — UpdateRecipeSectionInput pattern to follow)
    - src/db/queries/recipeSections.ts (lines 50-78 — updateRecipeSection as template)
  </read_first>
  <behavior>
    - UpdateRecipeStepInput type includes id: number plus all mutable step fields as optional
    - updateRecipeStep issues UPDATE recipe_steps SET ... WHERE id = $1 covering paint_id, step_name, order_index, notes, painting_phase, tool, technique, dilution, time_estimate_minutes, step_photo_path, alt_paint_id, section_id (13 columns)
    - updateRecipeStep uses direct assignment (no COALESCE) per D-09
  </behavior>
  <action>
    Per D-07, D-08, D-09:

    In src/types/recipePaint.ts:
    - Add `export type UpdateRecipeStepInput = Partial<Omit<CreateRecipeStepInput, "recipe_id">> & { id: number };` after CreateRecipeStepInput (line 29 area). This follows the exact pattern of UpdateRecipeSectionInput in types/recipeSection.ts line 44.

    In src/db/queries/recipePaints.ts:
    - Update the import on line 2 to add UpdateRecipeStepInput: `import type { RecipeStep, CreateRecipeStepInput, UpdateRecipeStepInput } from "@/types/recipePaint";`
    - Replace the line 38 comment ("No updateRecipePaint — links are immutable...") with the new `updateRecipeStep` function
    - The function signature: `export async function updateRecipeStep(input: UpdateRecipeStepInput): Promise<void>`
    - SQL: `UPDATE recipe_steps SET paint_id = $2, step_name = $3, order_index = $4, notes = $5, painting_phase = $6, tool = $7, technique = $8, dilution = $9, time_estimate_minutes = $10, step_photo_path = $11, alt_paint_id = $12, section_id = $13 WHERE id = $1`
    - All direct assignment (no COALESCE wrappers) — D-09 specifies full-row update
    - Parameter array: [input.id, input.paint_id ?? null, input.step_name, input.order_index, input.notes ?? null, input.painting_phase ?? null, input.tool ?? null, input.technique ?? null, input.dilution ?? null, input.time_estimate_minutes ?? null, input.step_photo_path ?? null, input.alt_paint_id ?? null, input.section_id ?? null]
    - Note: step_name and order_index are required fields (not nullable) so they pass directly without ?? null
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/types/recipePaint.ts exports UpdateRecipeStepInput type
    - src/db/queries/recipePaints.ts exports updateRecipeStep function
    - updateRecipeStep SQL string contains 13 SET clauses (paint_id through section_id) with no COALESCE wrappers
    - The old "No updateRecipePaint — links are immutable" comment on line 38 is replaced
    - Import line in recipePaints.ts includes UpdateRecipeStepInput
    - `pnpm build` passes (TypeScript type checking confirms type correctness)
  </acceptance_criteria>
  <done>UpdateRecipeStepInput type exported, updateRecipeStep function exported, TypeScript build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan modifies internal form-state types and adds a DB query function. All inputs pass through existing Zod validation in recipeSchema before reaching onSubmit.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Tampering | updateRecipeStep SQL | accept | Parameterized query ($1-$13) prevents SQL injection. Input comes from validated form state. Same pattern as existing addRecipePaint/updateRecipeSection. |
</threat_model>

<verification>
- `pnpm test -- tests/painting/recipeSteps.test.ts tests/painting/recipeSection.pure.test.ts` passes
- `pnpm build` passes
- DraftStep and DraftSection both have dbId field
- buildDraftSections populates dbId from DB row IDs
- updateRecipeStep function exists and compiles
</verification>

<success_criteria>
- All existing tests still pass (no regressions from adding dbId field)
- New tests verify dbId population in both factory functions and buildDraftSections
- UpdateRecipeStepInput type and updateRecipeStep query function are available for Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/70-non-destructive-recipe-save/70-01-SUMMARY.md`
</output>

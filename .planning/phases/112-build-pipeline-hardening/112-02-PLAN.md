---
phase: 112-build-pipeline-hardening
plan: 02
type: execute
wave: 2
depends_on: [112-01]
files_modified:
  - scripts/build-unit-db.ts
  - scripts/lib/types.ts
autonomous: true
requirements: [BPH-01, BPH-02, BPH-04, BPH-05]

must_haves:
  truths:
    - "D-01: Running pnpm build:udb prints a per-faction table with Matched/Exact/Norm/Alias columns"
    - "D-02: Running pnpm build:udb prints unmatched unit names grouped by faction"
    - "D-03: Two consecutive pnpm build:udb runs from identical inputs produce byte-identical unit_database.json"
    - "D-07, D-08: Running with a stale alias entry prints a warning identifying the unused alias; summary line shows used/unused/unknown counts"
    - "D-09, D-10: Setting MIN_COVERAGE_PCT above current coverage causes exit code 1; threshold is overall, not per-faction"
  artifacts:
    - path: "scripts/build-unit-db.ts"
      provides: "Enhanced coverage table, deterministic sorting, alias validation, coverage threshold"
      contains: "MIN_COVERAGE_PCT"
    - path: "scripts/lib/types.ts"
      provides: "FactionCoverage with match method fields"
      contains: "matched_exact"
  key_links:
    - from: "scripts/build-unit-db.ts"
      to: "scripts/data/coverage-report.json"
      via: "writeFileSync with enhanced FactionCoverage data"
      pattern: "writeFileSync.*COVERAGE_PATH"
    - from: "scripts/build-unit-db.ts"
      to: "process.exit"
      via: "coverage threshold check"
      pattern: "MIN_COVERAGE_PCT"
---

<objective>
Enhance build-unit-db.ts with four production-grade features: per-faction match method reporting (BPH-01), deterministic output sorting (BPH-02), alias validation (BPH-04), and coverage failure threshold (BPH-05).

Purpose: Makes the build pipeline observable (developers see exactly what matched and how), reproducible (byte-identical output), self-validating (stale aliases flagged), and regression-proof (coverage drops caught).
Output: Enhanced build-unit-db.ts with all four features; updated coverage-report.json schema.
</objective>

<execution_context>
@.planning/phases/112-build-pipeline-hardening/112-CONTEXT.md
@.planning/phases/112-build-pipeline-hardening/112-RESEARCH.md
@.planning/phases/112-build-pipeline-hardening/112-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From scripts/lib/bsdata.ts (created in Plan 01):
- matchUnit(bsdataName, factionId, aliases, unitMap) -> MatchResult | undefined
- MatchResult = { unit: UdbUnitRow; method: "exact" | "normalized" | "alias" }
- MatchMethod = "exact" | "normalized" | "alias"
- readCsvFile(dataDir, filename) -> Record<string, string>[]
- readBsdataCatFiles(bsdataDir) -> Array<{xml, factionId, catalogueName}>
- parseBsdataModelCounts(catFiles) -> BsdataModelCount[]

From scripts/lib/types.ts (extended in Plan 01):
- FactionCoverage with fields: faction_id, faction_name, total_units, units_with_points, coverage_pct, matched_exact?, matched_normalized?, matched_alias?, unmatched_names?
- CoverageReport with factions: FactionCoverage[] and unmatched_units array

From scripts/lib/normalize.ts:
- loadAliases(path) -> Record<string, string>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BPH-01 enhanced coverage report + BPH-02 deterministic output</name>
  <read_first>
    - scripts/build-unit-db.ts (full file — especially: matching loop ~lines 396-448 for per-faction tracking, coverage report ~lines 530-611, JSON assembly ~lines 688-714)
    - scripts/lib/types.ts (FactionCoverage and CoverageReport interfaces)
    - scripts/lib/bsdata.ts (matchUnit return type MatchResult)
  </read_first>
  <files>scripts/build-unit-db.ts, scripts/lib/types.ts</files>
  <action>
    BPH-01 — Per-faction match method tracking (per D-01, D-02):

    1. Before the BSData matching loop, create a per-faction counter map: const factionMatchStats = new Map of string to {exact: number, normalized: number, alias: number}. Initialize an entry for each faction with zeros.

    2. In the matching loop, after matchUnit returns a MatchResult, increment the appropriate counter in factionMatchStats keyed by the matched unit's faction_id. Use result.method to determine which counter ("exact", "normalized", or "alias").

    3. In the coverage report section (Step 10), when building factionCoverages array, populate the new FactionCoverage fields: matched_exact, matched_normalized, matched_alias from factionMatchStats.get(faction.id). Also populate unmatched_names from the unmatched units for that faction.

    4. Replace the coverage table header and row format. New header columns:
       Faction (padEnd 40), Units (padStart 7), Matched (padStart 9), Exact (padStart 7), Norm (padStart 6), Alias (padStart 7), Coverage (padStart 10)
       Where Matched = matched_exact + matched_normalized + matched_alias for that faction.

    5. After the summary table, for each faction with unmatched_names.length > 0, print the faction name and unmatched count, then each unmatched name indented with "    - " prefix. Per D-02, this lets Phase 113 auditor see exactly which units lack BSData matches.

    6. Update the CoverageReport JSON written to coverage-report.json to include the new FactionCoverage fields (they are already optional in types.ts from Plan 01, so existing consumers are not broken).

    BPH-02 — Deterministic output sorting (per D-03):

    7. BEFORE the content hash computation (the createHash call), add sort calls for all 8 output arrays:
       - factions.sort by id (localeCompare)
       - units.sort by id (localeCompare)
       - models.sort by (unit_id localeCompare, then line_order numeric)
       - weapons.sort by (unit_id localeCompare, then weapon_group numeric, then line_order numeric)
       - abilities.sort by (unit_id localeCompare, then line_order numeric)
       - keywords.sort by (unit_id localeCompare, then keyword localeCompare)
       - points.sort by (unit_id localeCompare, then model_count numeric)
       - composition.sort by unit_id (localeCompare)

    8. The hash computation MUST come AFTER these sorts (per Pitfall 1 from RESEARCH.md) so the hash itself is deterministic.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build:udb 2>&1 | findstr /C:"Exact" /C:"Norm" /C:"Alias" /C:"Matched"</automated>
  </verify>
  <acceptance_criteria>
    - Coverage table stdout includes columns: Faction, Units, Matched, Exact, Norm, Alias, Coverage
    - After the summary table, unmatched unit names are printed grouped by faction with indented "    - " prefix
    - coverage-report.json FactionCoverage entries include matched_exact, matched_normalized, matched_alias, unmatched_names fields
    - All 8 output arrays (factions, units, models, weapons, abilities, keywords, points, composition) are sorted before JSON.stringify
    - Content hash (createHash) is computed AFTER the sort calls
    - pnpm build:udb runs successfully (exit 0)
  </acceptance_criteria>
  <done>Coverage table shows match method breakdown per faction. Unmatched unit names printed per faction. Two identical builds produce byte-identical output JSON.</done>
</task>

<task type="auto">
  <name>Task 2: BPH-04 alias validation + BPH-05 coverage threshold</name>
  <read_first>
    - scripts/build-unit-db.ts (aliases loading at ~line 374, BSData matching loop for collecting BSData names, coverage computation for overallCoveragePct)
    - scripts/data/aliases.json (current 44 entries — understand key/value format)
  </read_first>
  <files>scripts/build-unit-db.ts</files>
  <action>
    BPH-04 — Alias validation (per D-07, D-08):

    1. During the BSData matching loop (Step 8a), accumulate a Set of all BSData datasheet_name values seen across all .cat files: const allBsdataNames = new Set of string. Add bsdataUnit.datasheet_name for EVERY bsdataUnit, regardless of whether matchUnit succeeds.

    2. After the BSData matching loop completes (after Step 8 ends), add a new validation step. Create a validateAliases function (local to build-unit-db.ts, not shared lib) that accepts: aliases (Record of string to string), allBsdataNames (Set of string), unitByNameFaction (Map of string to UdbUnitRow). Returns: { used: number; unused: string[]; unknownTargets: string[] }.

    3. Validation logic: For each entry in aliases:
       - An alias is "unused" if its key (BSData name) is NOT in allBsdataNames
       - An alias has "unknown target" if its value (Wahapedia name) does not match any key in unitByNameFaction when checked as value.toLowerCase() + ":" + factionId for any factionId. Use: [...unitByNameFaction.keys()].some(key => key.startsWith(wahapediaName.toLowerCase() + ":"))
       - An alias is "used" if it is in allBsdataNames and target exists

    4. Print validation results:
       - If unused.length > 0: console.warn with "  UNUSED aliases (BSData name not in any .cat file):" followed by each name
       - If unknownTargets.length > 0: console.warn with "  UNKNOWN TARGET aliases (Wahapedia name not found):" followed by each entry formatted as '"bsdataName" -> "wahapediaName"'
       - Always print summary line per D-08: console.log("  Alias validation: N used, M unused, K unknown target")
       - Warnings do NOT fail the build per D-07.

    BPH-05 — Coverage failure threshold (per D-09, D-10):

    5. Add a constant at the top of build-unit-db.ts (after imports, before DATA_DIR): const MIN_COVERAGE_PCT = 55 with a JSDoc comment explaining current coverage is ~60%, threshold should be raised after Phase 113/114 audit work improves coverage, target is 90% after all faction audits. Initial value is 55 (safely below current 60.1%) per RESEARCH.md Pitfall 5 recommendation.

    6. After the coverage table is printed (after Step 10 console output), add: if (overallCoveragePct < MIN_COVERAGE_PCT) { console.error("ERROR: Overall coverage " + overallCoveragePct.toFixed(1) + "% is below minimum threshold of " + MIN_COVERAGE_PCT + "%"); console.error("This indicates a regression. Check BSData .cat files or Wahapedia CSV freshness."); process.exit(1); }

    7. The check uses the overall (all factions combined) percentage per D-10, not per-faction.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build:udb 2>&1 | findstr /C:"Alias validation"</automated>
  </verify>
  <acceptance_criteria>
    - Build stdout includes "Alias validation: N used, M unused, K unknown target" summary line
    - Unused aliases (if any) are printed with console.warn listing each BSData name
    - Unknown target aliases (if any) are printed with console.warn showing "bsdataName" -> "wahapediaName"
    - MIN_COVERAGE_PCT constant exists near top of build-unit-db.ts with value 55
    - Comment on MIN_COVERAGE_PCT explains it should be raised after audit (target 90%)
    - If overallCoveragePct < MIN_COVERAGE_PCT, script calls process.exit(1) with descriptive error
    - Current build succeeds (60.1% > 55% threshold)
    - allBsdataNames Set collects names from ALL parsed BSData units (not just matched ones) per Pitfall 4
  </acceptance_criteria>
  <done>Build validates aliases and reports problems. Build fails loudly if coverage regresses below threshold. Current build passes at 60.1% with 55% floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable — dev-side build scripts only. No user input, no network, no runtime changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | aliases.json | mitigate | BPH-04 validates all entries at build time; warns on stale/unknown |
| T-112-02 | Denial | MIN_COVERAGE_PCT | accept | Developer-controlled constant; intentionally blocks builds on regression |
| T-112-SC | Tampering | npm installs | accept | No new npm dependencies in this phase |
</threat_model>

<verification>
1. Run `pnpm build:udb` — produces enhanced coverage table with match method columns, prints unmatched names, validates aliases, passes threshold check.
2. Run `pnpm build:udb` twice — compare content hashes in output; they must be identical.
3. Run `pnpm build` — TypeScript check passes.
</verification>

<success_criteria>
- Coverage table shows Matched/Exact/Norm/Alias columns per faction (BPH-01)
- Unmatched unit names printed per faction (BPH-01, D-02)
- Two builds produce identical content hash (BPH-02)
- Alias validation summary printed with used/unused/unknown counts (BPH-04)
- MIN_COVERAGE_PCT = 55 with explanatory comment; process.exit(1) if below (BPH-05)
- Build succeeds end-to-end with all features active
</success_criteria>

<output>
Create `.planning/phases/112-build-pipeline-hardening/112-02-SUMMARY.md` when done
</output>

---
phase: 128-feedback-hardening-form-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/game-day/GameDayPage.tsx
  - src/features/rules-hub/RuleNoteEditor.tsx
  - src/features/units/PlaybookTab.tsx
autonomous: true
requirements: [FBK-02, FBK-05, FBK-06, FBK-07]

must_haves:
  truths:
    - "D-02: GameDayPage shows a centered error block with retry button when the army list query fails"
    - "D-05: RuleNoteEditor shows a subtle 'Saved' indicator that appears after auto-save and fades after 2 seconds"
    - "D-06: PlaybookTab disabled save button shows a tooltip explaining why it is disabled"
    - "D-07: PlaybookTab error state includes an inline Retry button that refetches the datasheet"
  artifacts:
    - path: "src/features/game-day/GameDayPage.tsx"
      provides: "Error state with retry"
      contains: "AlertCircle"
    - path: "src/features/rules-hub/RuleNoteEditor.tsx"
      provides: "Saved indicator"
      contains: "Saved"
    - path: "src/features/units/PlaybookTab.tsx"
      provides: "Tooltip on disabled save and Retry on error"
      contains: "TooltipContent"
  key_links:
    - from: "GameDayPage.tsx"
      to: "useArmyList"
      via: "isError and refetch destructured from hook"
      pattern: "isError.*refetch"
    - from: "PlaybookTab.tsx"
      to: "useDatasheet"
      via: "refetch destructured from hook"
      pattern: "refetch.*refetchDatasheet"
    - from: "RuleNoteEditor.tsx"
      to: "upsertNote.mutate"
      via: "onSuccess callback sets saved state"
      pattern: "onSuccess.*setSaved"
---

<objective>
Add structural JSX improvements: GameDayPage error state, RuleNoteEditor saved indicator, PlaybookTab tooltip on disabled save, and PlaybookTab error retry button.

Purpose: Four requirements (FBK-02, FBK-05, FBK-06, FBK-07) each need new JSX blocks or component wiring beyond simple attribute/text changes. Grouping them keeps structural work together.
Output: 3 files updated with new error states, feedback indicators, and interactive affordances.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-feedback-hardening-form-ux/128-CONTEXT.md
@.planning/phases/128-feedback-hardening-form-ux/128-PATTERNS.md
@.planning/phases/128-feedback-hardening-form-ux/128-UI-SPEC.md

<interfaces>
<!-- Reference pattern: RecipesPage centered error state -->
From src/features/recipes/RecipesPage.tsx:
  if (isError) return centered div with AlertCircle h-12 w-12 text-destructive,
  h2 text-lg font-semibold "Failed to load recipes",
  p text-sm text-muted-foreground mt-1,
  Button variant="outline" onClick refetch

<!-- Tooltip imports (from JournalTab.tsx) -->
From @/components/ui/tooltip:
  Tooltip, TooltipContent, TooltipProvider, TooltipTrigger

<!-- useArmyList hook signature -->
From src/hooks/useArmyLists.ts:
  useArmyList(id) returns { data, isLoading, isError, refetch, ... }

<!-- useDatasheet hook signature -->
From src/hooks/useDatasheet.ts:
  useDatasheet(unitId) returns { data, error, refetch, ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GameDayPage error state (FBK-02)</name>
  <files>src/features/game-day/GameDayPage.tsx</files>
  <read_first>
    src/features/game-day/GameDayPage.tsx,
    src/features/recipes/RecipesPage.tsx
  </read_first>
  <action>
    Per D-02: Add an error state to GameDayPage matching the RecipesPage centered error pattern.

    Step 1: Update the useArmyList destructure from `{ data: list, isLoading: listLoading }` to `{ data: list, isLoading: listLoading, isError: listError, refetch: refetchList }`.

    Step 2: Add `AlertCircle` to the lucide-react import statement (existing imports are Swords, Users, ClipboardList or similar).

    Step 3: Add the error branch AFTER the `listLoading` guard and BEFORE the `!list` (not found) guard. This ordering is critical per RESEARCH.md Pitfall 6 — when a query fails, `data` is undefined, so both `isError` and `!list` are truthy. If error branch comes after the not-found check, errors would show the wrong message.

    The error block structure: a centered flex column (py-12 gap-4) with AlertCircle h-12 w-12 text-destructive, heading "Failed to load game day data" (text-lg font-semibold), description "Check your connection and try again." (text-sm text-muted-foreground mt-1), and a Button variant="outline" labeled "Try Again" that calls refetchList().

    Also import Button from @/components/ui/button if not already imported.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - GameDayPage.tsx destructures `isError` and `refetch` (aliased as listError and refetchList) from useArmyList
    - GameDayPage.tsx imports AlertCircle from lucide-react
    - The `if (listError)` branch appears BEFORE any `if (!list)` branch in the source code
    - The error block contains "Failed to load game day data" as heading text
    - The error block contains a Button with onClick calling refetchList
    - pnpm build succeeds
  </acceptance_criteria>
  <done>GameDayPage shows user-friendly error with retry when army list query fails, not a blank screen</done>
</task>

<task type="auto">
  <name>Task 2: RuleNoteEditor saved indicator and PlaybookTab tooltip + retry (FBK-05, FBK-06, FBK-07)</name>
  <files>
    src/features/rules-hub/RuleNoteEditor.tsx,
    src/features/units/PlaybookTab.tsx
  </files>
  <read_first>
    src/features/rules-hub/RuleNoteEditor.tsx,
    src/features/units/PlaybookTab.tsx,
    src/features/units/JournalTab.tsx
  </read_first>
  <action>
    **RuleNoteEditor — Per D-05:**

    Add a `useState` boolean `showSaved` initialized to false. In the debounced `upsertNote.mutate()` call inside the change handler (NOT in the cleanup useEffect's mutate call — that fires on unmount and would update state on an unmounted component), add an `onSuccess` callback object as the second argument to `mutate()`. The callback sets `showSaved` to true and starts a 2-second setTimeout that sets it back to false.

    Add a span element after the textarea element. The span always renders (for layout stability) but uses opacity classes to fade: `text-xs text-muted-foreground transition-opacity duration-300` with conditional `opacity-100` when showSaved is true, `opacity-0` when false. Content: "Saved".

    **PlaybookTab Tooltip — Per D-06:**

    Add Tooltip imports (Tooltip, TooltipContent, TooltipProvider, TooltipTrigger from @/components/ui/tooltip) — same import block used in JournalTab.tsx.

    Extract the disabled condition into a `saveDisabled` variable: `!isDirty || isLoading || upsert.isPending`. Create a `tooltipMessage` variable: `isLoading ? "Loading..." : "No changes to save"`.

    Wrap the existing save Button in TooltipProvider > Tooltip > TooltipTrigger(asChild) > span > Button. The span wrapper is mandatory because disabled buttons do not fire pointer events — without it the tooltip never appears. Give the span `className="w-full"` when saveDisabled is true. Only render TooltipContent when saveDisabled is true.

    **PlaybookTab Retry — Per D-07:**

    Update the useDatasheet destructure from `{ data: datasheet, error: datasheetError }` to `{ data: datasheet, error: datasheetError, refetch: refetchDatasheet }`. Do NOT add a second useDatasheet call.

    Modify the existing error block (the div with class border-destructive/50 bg-destructive/10): add `flex items-center justify-between gap-2` to its className. Wrap the existing error text in a span element. Add a Button (variant="outline", size="sm") with label "Retry" and onClick calling `refetchDatasheet()`. Button is already imported in PlaybookTab.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - RuleNoteEditor.tsx contains `useState` for showSaved (or equivalent naming like `saved`)
    - RuleNoteEditor.tsx contains `onSuccess` callback in a mutate() call that sets the saved flag
    - RuleNoteEditor.tsx contains a span with text "Saved" and classes including `text-xs text-muted-foreground transition-opacity`
    - RuleNoteEditor.tsx does NOT have onSuccess in the cleanup useEffect's mutate call
    - PlaybookTab.tsx imports Tooltip, TooltipContent, TooltipProvider, TooltipTrigger from @/components/ui/tooltip
    - PlaybookTab.tsx contains TooltipContent with "No changes to save" text
    - PlaybookTab.tsx contains TooltipContent with "Loading..." text
    - PlaybookTab.tsx contains a span wrapper around the save Button inside TooltipTrigger
    - PlaybookTab.tsx destructures refetchDatasheet from useDatasheet
    - PlaybookTab.tsx error block contains a Button with label "Retry" and onClick calling refetchDatasheet
    - pnpm build succeeds
  </acceptance_criteria>
  <done>RuleNoteEditor shows subtle "Saved" indicator after auto-save, PlaybookTab disabled save has tooltip, PlaybookTab error has Retry button</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries are crossed. Error states display static messages. Retry buttons call existing refetch functions. No user-provided data is processed differently.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-02 | N/A | All changes | accept | UI-only feedback improvements — no security surface |
</threat_model>

<verification>
pnpm build succeeds with zero TypeScript errors.
GameDayPage has isError branch before !list branch.
RuleNoteEditor has "Saved" span with opacity transition.
PlaybookTab has Tooltip wrapping save button with span for disabled state.
PlaybookTab error block has inline Retry button.
</verification>

<success_criteria>
- GameDayPage shows centered error state with AlertCircle and "Try Again" button when query fails
- RuleNoteEditor shows "Saved" text that fades in after auto-save and fades out after 2 seconds
- PlaybookTab disabled save button shows context-aware tooltip ("No changes to save" or "Loading...")
- PlaybookTab error block includes inline Retry button that refetches datasheet
- pnpm build green
</success_criteria>

<output>
Create `.planning/phases/128-feedback-hardening-form-ux/128-02-SUMMARY.md` when done
</output>

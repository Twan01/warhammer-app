---
phase: 143-apply-flow-slot-fill-system
plan: 04
type: execute
wave: 3
depends_on: [143-02, 143-03]
files_modified:
  - src/features/recipes/SectionedTimeline.tsx
  - src/features/recipes/RecipeDetailSheet.tsx
autonomous: false
requirements: [APPLY-05, SLOT-06]
must_haves:
  truths:
    - "In the recipe detail view, technique-owned step swatches resolve through effectivePaintId() (not step.paint_id), so filled slots show the right paint"
    - "Technique-sourced sections in the detail view show an interactive 'from technique X' badge that navigates to the technique in the library tab"
    - "Each technique-sourced section in the detail view exposes an 'Edit colours' button"
    - "'Edit colours' reopens the slot-fill dialog pre-populated with the instance's current slot→paint mappings and saves via updateSlotMap"
    - "Technique-owned steps render read-only in the detail view (no per-step paint edit)"
  artifacts:
    - path: "src/features/recipes/SectionedTimeline.tsx"
      provides: "effectivePaintId() wiring via a slotMap prop"
      contains: "effectivePaintId"
    - path: "src/features/recipes/RecipeDetailSheet.tsx"
      provides: "badge + 'Edit colours' + read-only technique steps + slotMap supply"
      contains: "Edit colours"
  key_links:
    - from: "RecipeDetailSheet"
      to: "useSlotResolutionMap(recipeId)"
      via: "passes slotMap into SectionedTimeline"
      pattern: "useSlotResolutionMap"
    - from: "SectionedTimeline step swatch"
      to: "effectivePaintId(step, slotMap)"
      via: "resolution spine — no direct step.paint_id read for technique steps"
      pattern: "effectivePaintId"
    - from: "RecipeDetailSheet 'Edit colours'"
      to: "SlotFillDialog (pre-populated via useSlotMapByInstance) → updateSlotMap"
      via: "useUpdateSlotMap mutation"
      pattern: "useUpdateSlotMap"
---

<objective>
Complete the cross-surface half of the phase: make `effectivePaintId()` the resolution spine for the recipe detail view, and let the user view/change slot colours straight from the detail view (APPLY-05) — not only from the full edit form. Technique-owned steps render read-only with their real paint resolved through the slot map; the "from technique X" badge becomes an interactive link to the library tab; an "Edit colours" button reopens the (Plan 03) slot-fill dialog pre-populated with the instance's current mappings, saving via `updateSlotMap`.

Purpose: Without this, technique step swatches in the detail view always show "no paint" (because technique-owned `recipe_steps` carry `paint_id = NULL`) — the SectionedTimeline currently reads `step.paint_id` directly (RESEARCH Pitfall 6). This wires the resolution spine and delivers the only in-scope cross-surface edit (APPLY-05).
Output: 2 extended detail-view components. Ends with a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-apply-flow-slot-fill-system/143-PATTERNS.md
@.planning/phases/143-apply-flow-slot-fill-system/143-UI-SPEC.md

<interfaces>
<!-- Executor uses these directly. -->

effectivePaintId — src/lib/effectivePaintId.ts (verified lines 34–61): effectivePaintId(step: {paint_id; technique_step_id?}, slotMap: SlotResolutionMap) → number|null. slotMap keyed by technique_step_id.

Resolution map hooks (Plan 02, src/hooks/useSlotResolutionMap.ts):
  useSlotResolutionMap(recipeId) → Map<technique_step_id, paint_id|null> (for swatch resolution).
  useSlotMapByInstance(instanceId) → Map<slot_id, paint_id|null> (for Edit-colours pre-population).

updateSlotMap mutation (Plan 02, src/hooks/useTechniqueInstances.ts): useUpdateSlotMap() → mutateAsync({ instanceId, recipeId, slotFills: Map<slot_id, paint_id|null> }); invalidates SLOT_RESOLUTION_MAP_KEY + resolved-paint keys.

SlotFillDialog (Plan 03, src/features/recipes/SlotFillDialog.tsx): created for the apply flow. This plan REUSES it in "edit" mode. Read its current props — it currently calls useApplyTechnique. To support edit mode, extend its props with an optional `mode?: "apply" | "edit"`, `instanceId?`, and `initialSlotFills?: Map<slot_id, paint_id|null>`; in edit mode the CTA "Apply technique" becomes a save that calls useUpdateSlotMap instead and seeds slotFills from initialSlotFills. (If extending SlotFillDialog risks regressing Plan 03's apply flow, instead add a thin wrapper or a sibling EditColoursDialog reusing SlotFillRow — pick whichever keeps the apply path untouched; document the choice in the SUMMARY.)

TechniqueSectionBadge (Plan 03, src/features/recipes/TechniqueSectionBadge.tsx): pass onNavigate to render it as an interactive `<button>` that navigates to the technique in the library tab.

SectionedTimeline — src/features/recipes/SectionedTimeline.tsx (verified lines 40–54): sectionAvailability useMemo currently reads `step.paint_id` directly at ~line 43 (BROKEN for technique steps). Props interface lines 10–15.

RecipeDetailSheet — src/features/recipes/RecipeDetailSheet.tsx: detail Sheet that renders SectionedTimeline; Badge already imported (line 13). This is where the slotMap is fetched and passed down, and where "Edit colours" mounts.

Navigation to technique library tab: the technique library lives under the Recipes page Tabs (Phase 142, no new sidebar entry). Use the existing tab/route mechanism the library uses — read RecipeDetailSheet / the Recipes page to find how a tab is selected (likely a search param or Tabs value); do not invent a new route.

Copywriting (UI-SPEC): "Edit colours" button label = "Edit colours". Slot-map update toasts: success "Colours updated.", failure "Failed to update colours. Please try again.". Badge tooltip "from {technique.name} — click to view in library". Read-only step tooltip "This step is part of a live-linked technique. Edit via 'Edit colours'.".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire effectivePaintId() into SectionedTimeline via a slotMap prop (Pitfall 6, SLOT-06)</name>
  <files>src/features/recipes/SectionedTimeline.tsx</files>
  <read_first>
    - src/features/recipes/SectionedTimeline.tsx (full file — props interface lines 10–15, sectionAvailability useMemo lines 40–54 reading step.paint_id at ~43, plus the per-step swatch render path)
    - src/lib/effectivePaintId.ts (resolver + SlotResolutionMap type)
    - .planning/phases/143-apply-flow-slot-fill-system/143-RESEARCH.md "Pitfall 6" + "Anti-Patterns" (effectivePaintId wiring scope: detail-view swatch is in scope for 143; full availability-count fix is 145)
  </read_first>
  <action>
Add an optional `slotMap?: SlotResolutionMap` prop to SectionedTimelineProps (import SlotResolutionMap from "@/lib/effectivePaintId"; plain recipes pass undefined). Import effectivePaintId. At every place the component derives a step's paint for SWATCH DISPLAY, replace the direct `step.paint_id` read with `effectivePaintId(step, slotMap ?? new Map())` so technique-owned steps resolve through the slot map; a technique step whose slot is unfilled resolves null and renders the dashed/empty swatch (treated paintless, SLOT-05/06). Apply the same resolution in the sectionAvailability useMemo (~line 43) so technique-step swatches are consistent. Keep the change scoped to swatch/paint resolution — do not refactor unrelated timeline logic. (The deeper paint-availability count integration across all surfaces is Phase 145 and out of scope.)
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - SectionedTimelineProps includes `slotMap?: SlotResolutionMap`.
    - No remaining direct `step.paint_id` read drives a technique-step swatch — swatch/paint resolution goes through `effectivePaintId(step, slotMap ?? new Map())` (grep confirms effectivePaintId import + usage).
    - Plain recipes (slotMap undefined) still render correctly (effectivePaintId falls back to step.paint_id for non-technique steps).
    - `pnpm build` (tsc) clean.
  </acceptance_criteria>
  <done>SectionedTimeline resolves technique-step swatches via effectivePaintId; slotMap prop added; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: RecipeDetailSheet — slotMap supply + interactive badge + read-only steps + 'Edit colours' (APPLY-05)</name>
  <files>src/features/recipes/RecipeDetailSheet.tsx</files>
  <read_first>
    - src/features/recipes/RecipeDetailSheet.tsx (full file — where SectionedTimeline is rendered, how sections/steps are loaded, the Tabs/route mechanism for navigating to the library tab, existing Badge import line 13)
    - src/features/recipes/TechniqueSectionBadge.tsx (Plan 03 — pass onNavigate to make it interactive)
    - src/features/recipes/SlotFillDialog.tsx (Plan 03 — confirm its current props before reusing/extending for edit mode)
    - src/hooks/useSlotResolutionMap.ts (Plan 02 — useSlotResolutionMap + useSlotMapByInstance)
    - src/hooks/useTechniqueInstances.ts (Plan 02 — useUpdateSlotMap + getInstancesForRecipe/useTechniqueInstances to map a section's technique_instance_id → technique name + instance id)
    - .planning/phases/143-apply-flow-slot-fill-system/143-UI-SPEC.md "Recipe section with technique badge" (detail) + "Copywriting Contract"
  </read_first>
  <action>
In RecipeDetailSheet: (1) call `useSlotResolutionMap(recipeId)` and pass the resulting map as the `slotMap` prop into SectionedTimeline. (2) For each section whose `technique_instance_id != null`, render `<TechniqueSectionBadge techniqueName={...} onNavigate={...} />` where onNavigate selects the Technique Library tab on the Recipes page for that technique (use the existing tab/route mechanism — do not invent a new route) and add the badge tooltip copy. (3) Render the technique-owned steps read-only — no per-step paint edit control; their swatch resolves via effectivePaintId/slotMap (already handled by SectionedTimeline); add the read-only step tooltip copy "This step is part of a live-linked technique. Edit via 'Edit colours'.". (4) Add an "Edit colours" Button (label exactly "Edit colours") in each technique-sourced section header. Clicking it opens the slot-fill dialog in EDIT mode for that section's instance: pre-populate via `useSlotMapByInstance(instanceId)` (Map<slot_id, paint_id|null>), and on save call useUpdateSlotMap().mutateAsync({ instanceId, recipeId, slotFills }) with success toast "Colours updated." / failure toast "Failed to update colours. Please try again.". Resolve each section's technique name + instance id by joining the section's technique_instance_id to the recipe's instances (useTechniqueInstances/getInstancesForRecipe) and the technique list.

If reusing SlotFillDialog requires the edit-mode extension described in the interfaces block, make that change minimal and confirm Plan 03's apply path still builds; otherwise create a sibling EditColoursDialog reusing SlotFillRow. Document the choice in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - RecipeDetailSheet calls useSlotResolutionMap(recipeId) and passes slotMap into SectionedTimeline (grep confirms).
    - Technique-sourced sections render an interactive TechniqueSectionBadge (onNavigate provided) that selects the library tab for the technique — reuses the existing tab mechanism, no new route invented.
    - Each technique-sourced section header has an "Edit colours" button that opens a slot-fill dialog pre-populated from useSlotMapByInstance and saves via useUpdateSlotMap with the exact "Colours updated." / failure toasts.
    - Technique-owned steps render read-only (no editable paint control) with the read-only tooltip copy.
    - `pnpm build` clean; the full suite still green: `pnpm test` passes (Plan 01/02 data-layer tests unaffected).
  </acceptance_criteria>
  <done>Detail view resolves via slotMap, badge links to library, Edit colours pre-populates + saves via updateSlotMap, steps read-only; build + tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The recipe detail view now resolves technique-owned step swatches through effectivePaintId() (filled slots show real paint), shows an interactive "from technique X" badge that jumps to the library tab, renders technique steps read-only, and exposes an "Edit colours" button that reopens the slot-fill dialog pre-populated so the user can change slot colours straight from the detail view (APPLY-05).
  </what-built>
  <how-to-verify>
1. Run `pnpm tauri dev`.
2. Open a recipe that has an applied technique (apply one first via the editor if needed). Open its detail view.
3. Confirm the technique section shows a "from technique X" badge; click it and confirm it navigates to that technique in the Technique Library tab.
4. Back in the detail view, confirm technique-owned steps that have a filled slot show the correct paint swatch (not empty), and steps with an unfilled slot show an empty/dashed swatch — and that none of these steps are editable (read-only).
5. Click "Edit colours" on the technique section. Confirm the slot-fill dialog opens PRE-POPULATED with the current slot→paint selections.
6. Change one slot's paint and save. Confirm the toast "Colours updated." and that the detail-view swatch updates immediately to the new paint.
7. Confirm a recipe with NO technique still renders normally (no regressions).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. swatch still empty for filled slots, badge does not navigate, Edit colours not pre-populated, steps editable, plain recipe regressed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit-colours form → updateSlotMap mutation | Local slot→paint selections cross into an UPSERT on recipe_technique_slot_maps. Constrained to existing paint ids; risk is data coherence (stale swatch, wrong instance), not untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-08 | Tampering | wrong instance edited (shared slot map) | mitigate | Edit colours keys off the section's specific technique_instance_id; updateSlotMap UPSERTs by (instance_id, slot_id) so editing one instance never touches another (SLOT-03) |
| T-143-09 | Information Disclosure | technique step swatch shows wrong/no paint | mitigate | effectivePaintId() spine resolves via slotMap; no direct step.paint_id read for technique steps (Pitfall 6 closed) |
| T-143-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH: zero new deps) |
</threat_model>

<verification>
- `pnpm build` clean; `pnpm test` full suite green.
- Human-verify checkpoint passed (badge navigation, filled-slot swatch, Edit colours pre-populate + save, no plain-recipe regression).
</verification>

<success_criteria>
- effectivePaintId() is the resolution spine for the detail view (Pitfall 6 closed); technique-step swatches show resolved paint (SLOT-06).
- User can view + change slot colours from the recipe detail view (APPLY-05) via a pre-populated Edit-colours dialog saving through updateSlotMap.
- Badge links to the library tab; technique steps render read-only.
</success_criteria>

<output>
Create `.planning/phases/143-apply-flow-slot-fill-system/143-04-SUMMARY.md` when done.
</output>

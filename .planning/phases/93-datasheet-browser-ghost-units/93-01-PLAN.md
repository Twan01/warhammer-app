---
phase: 93-datasheet-browser-ghost-units
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/army-lists/DatasheetBrowserDialog.tsx
  - src/features/army-lists/ArmyListsPage.tsx
  - src/features/army-lists/ArmyListDetailSheet.tsx
  - tests/army-lists/DatasheetBrowserDialog.test.tsx
autonomous: true
requirements: [BRW-01, BRW-02]

must_haves:
  truths:
    - "User can open a DatasheetBrowserDialog from the army list detail view"
    - "Dialog shows all faction datasheets grouped by role with points"
    - "User can search datasheets by name via Command palette"
    - "Selecting a datasheet adds a ghost unit to the list with the exact canonical name"
    - "Dialog stays open after each add for multi-add UX"
    - "Dialog shows a helpful message when no Wahapedia faction mapping exists"
  artifacts:
    - path: "src/features/army-lists/DatasheetBrowserDialog.tsx"
      provides: "Dialog component for browsing all faction datasheets"
      exports: ["DatasheetBrowserDialog"]
    - path: "tests/army-lists/DatasheetBrowserDialog.test.tsx"
      provides: "Unit tests for DatasheetBrowserDialog"
  key_links:
    - from: "src/features/army-lists/DatasheetBrowserDialog.tsx"
      to: "useDatasheetsByFactionWithPoints"
      via: "React Query hook"
      pattern: "useDatasheetsByFactionWithPoints"
    - from: "src/features/army-lists/DatasheetBrowserDialog.tsx"
      to: "useAddGhostUnitToList"
      via: "mutation hook"
      pattern: "useAddGhostUnitToList"
    - from: "src/features/army-lists/ArmyListsPage.tsx"
      to: "DatasheetBrowserDialog"
      via: "sibling portal rendering"
      pattern: "datasheetBrowserOpen"
    - from: "src/features/army-lists/ArmyListDetailSheet.tsx"
      to: "ArmyListsPage"
      via: "onBrowseDatasheets callback"
      pattern: "onBrowseDatasheets"
---

<objective>
Build the DatasheetBrowserDialog component and wire it into the army list page via the sibling portal architecture, giving users the ability to browse all faction datasheets and add ghost/planned units to their list.

Purpose: Enables list-building with any faction datasheet, not just owned collection units (BRW-01, BRW-02).
Output: DatasheetBrowserDialog.tsx component, sibling portal wiring in ArmyListsPage, "Browse Datasheets" trigger in ArmyListDetailSheet, and tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-datasheet-browser-ghost-units/93-CONTEXT.md
@.planning/phases/93-datasheet-browser-ghost-units/93-RESEARCH.md
@.planning/phases/93-datasheet-browser-ghost-units/93-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/db/queries/datasheets.ts:
export interface DatasheetWithPoints {
  id: string;
  name: string;
  role: string | null;
  points: number | null;
}
export async function getDatasheetsByFactionWithPoints(factionId: string): Promise<DatasheetWithPoints[]>;

From src/hooks/useDatasheet.ts:
export function useDatasheetsByFactionWithPoints(factionId: string | undefined);  // auto-disabled when undefined
export function useWahapediaFactionId(localFactionName: string | undefined);

From src/hooks/useArmyLists.ts:
export function useAddGhostUnitToList();  // returns useMutation with mutate({list_id, ghost_unit_name})

From src/hooks/useFactions.ts:
export function useFactions();  // returns useQuery with Faction[]

From src/types/armyList.ts:
export interface AddGhostUnitToListInput {
  list_id: number;
  ghost_unit_name: string;
  points_override?: number | null;
  notes?: string | null;
}

From src/features/army-lists/ArmyListDetailSheet.tsx (current props):
interface ArmyListDetailSheetProps {
  open: boolean;
  list: ArmyList | null;
  onClose: () => void;
  onEdit: (list: ArmyList) => void;
  onDelete: (list: ArmyList) => void;
  onAddUnit: () => void;
  onConfigureUnit: (armyListUnitId: number) => void;
}

From src/features/army-lists/ArmyListsPage.tsx (current sibling portal state):
const [unitPickerOpen, setUnitPickerOpen] = useState(false);
const [loadoutUnitId, setLoadoutUnitId] = useState<number | null>(null);
// Pattern: add analogous datasheetBrowserOpen state
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DatasheetBrowserDialog component</name>
  <files>src/features/army-lists/DatasheetBrowserDialog.tsx, tests/army-lists/DatasheetBrowserDialog.test.tsx</files>
  <read_first>
    src/features/army-lists/UnitPickerDialog.tsx
    src/hooks/useDatasheet.ts
    src/hooks/useArmyLists.ts
    src/db/queries/datasheets.ts (lines 309-330)
    src/types/armyList.ts (lines 82-93)
  </read_first>
  <action>
Create DatasheetBrowserDialog.tsx by cloning UnitPickerDialog.tsx architecture per D-01.

Props interface: DatasheetBrowserDialogProps with open: boolean, listId: number | null, factionId: number | null (HobbyForge faction_id, not Wahapedia ID), onClose: () => void.

Inside the component:
- Resolve Wahapedia faction ID: call useFactions() to find the faction by factionId, then useWahapediaFactionId(faction?.name) per D-03. This is the same resolution chain used in ArmyListDetailSheet (lines 68-73 of that file).
- Fetch datasheets: useDatasheetsByFactionWithPoints(wahapediaFactionId ?? undefined). The hook auto-disables when undefined.
- Group datasheets by role using useMemo: create a Record keyed by ds.role (null maps to "Other"). Per D-02.
- Add ghost unit mutation: useAddGhostUnitToList(). The handleSelect function receives a DatasheetWithPoints, calls mutate with { list_id: listId, ghost_unit_name: ds.name }. CRITICAL per D-10: pass ds.name (the canonical string), NOT ds.id. Toast success per D-04.
- Dialog stays open after each add (no onClose call in onSuccess) per D-04.
- If wahapediaFactionId is null/undefined, render a message: "Set a faction on this list to browse datasheets." per D-03.
- Use Dialog + Command + CommandInput + CommandList + CommandEmpty + CommandGroup (one per role heading) + CommandItem. Per D-02.
- CommandItem value must be template literal ds.name-ds.id to avoid collisions (Pitfall 5 from RESEARCH.md).
- Each CommandItem shows ds.name as flex-1 span, plus a role Badge (variant="secondary") and points span (text-xs text-muted-foreground, show "pts" suffix, skip if null).
- Import Badge from @/components/ui/badge, use BookOpen or similar icon is not needed in this component (it has no trigger button -- that is in ArmyListDetailSheet).

Create tests/army-lists/DatasheetBrowserDialog.test.tsx:
- Mock useDatasheetsByFactionWithPoints returning 2+ datasheets with different roles (e.g., Character and Battleline).
- Mock useWahapediaFactionId returning a string like "SM".
- Mock useFactions returning [{id: 1, name: "Space Marines"}].
- Mock useAddGhostUnitToList returning { mutate: mockMutate, isPending: false }.
- Test: renders datasheets grouped by role -- assert both role headings visible and both datasheet names visible.
- Test: calls addGhostUnit.mutate with ds.name on selection -- click a CommandItem, assert mockMutate called with { list_id: 1, ghost_unit_name: "Intercessors" }.
- Test: shows empty state when wahapediaFactionId is null -- mock useWahapediaFactionId returning { data: undefined }, assert "Set a faction" message visible.
- Test: dialog stays open after selection -- click item, assert dialog still rendered (open prop unchanged).
- Use QueryClient + QueryClientProvider wrapper like existing tests.
  </action>
  <verify>
    <automated>pnpm test -- tests/army-lists/DatasheetBrowserDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DatasheetBrowserDialog.tsx exports a named function DatasheetBrowserDialog
    - Component uses Dialog (not Sheet) as outer wrapper
    - handleSelect passes ds.name to ghost_unit_name (grep for "ghost_unit_name: ds.name" or equivalent)
    - CommandItem value uses template literal with ds.id to avoid collision
    - No onClose() call inside mutate onSuccess (multi-add stays open)
    - wahapediaFactionId null path renders text containing "Set a faction"
    - All 4+ tests pass in DatasheetBrowserDialog.test.tsx
  </acceptance_criteria>
  <done>DatasheetBrowserDialog renders grouped datasheets, adds ghost units with canonical name, and stays open for multi-add. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DatasheetBrowserDialog into ArmyListsPage and ArmyListDetailSheet</name>
  <files>src/features/army-lists/ArmyListsPage.tsx, src/features/army-lists/ArmyListDetailSheet.tsx</files>
  <read_first>
    src/features/army-lists/ArmyListsPage.tsx
    src/features/army-lists/ArmyListDetailSheet.tsx
    src/features/army-lists/DatasheetBrowserDialog.tsx
  </read_first>
  <action>
Modify ArmyListsPage.tsx per D-01 sibling portal pattern:
- Add import for DatasheetBrowserDialog from "./DatasheetBrowserDialog".
- Add state: const [datasheetBrowserOpen, setDatasheetBrowserOpen] = useState(false);
- Add handlers: openDatasheetBrowser = () => setDatasheetBrowserOpen(true), closeDatasheetBrowser = () => setDatasheetBrowserOpen(false).
- Update closeDetail to also reset datasheetBrowserOpen to false (same pattern as unitPickerOpen reset on line 68).
- Pass onBrowseDatasheets={openDatasheetBrowser} prop to ArmyListDetailSheet.
- Render DatasheetBrowserDialog as a sibling portal AFTER LoadoutBuilderSheet, with props: open={datasheetBrowserOpen}, listId={selectedListId}, factionId={selectedList?.faction_id ?? null}, onClose={closeDatasheetBrowser}.

Modify ArmyListDetailSheet.tsx per D-09:
- Add onBrowseDatasheets: () => void to ArmyListDetailSheetProps interface.
- Destructure onBrowseDatasheets in the component function signature.
- In the "Units" header area (around line 196-206), change the single "Add Unit" button into a flex row with gap-2 containing two buttons: the existing "Add Unit" button (unchanged) and a new "Browse Datasheets" button. The new button uses variant="outline", size="sm", onClick={onBrowseDatasheets}, with BookOpen icon from lucide-react (import it). Text: "Browse Datasheets".
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - ArmyListsPage.tsx contains datasheetBrowserOpen state and renders DatasheetBrowserDialog as sibling portal
    - ArmyListDetailSheet.tsx props interface includes onBrowseDatasheets: () => void
    - ArmyListDetailSheet.tsx renders a "Browse Datasheets" button that calls onBrowseDatasheets on click
    - closeDetail handler in ArmyListsPage resets datasheetBrowserOpen to false
    - pnpm build succeeds with zero TypeScript errors
  </acceptance_criteria>
  <done>"Browse Datasheets" button appears in the army list detail, opens the DatasheetBrowserDialog as a sibling portal, and the app builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. All data reads are from local SQLite databases. Ghost unit creation uses the existing parameterized query with positional $1, $2 syntax.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Tampering | ghost_unit_name input | accept | Name comes from rw_datasheets.name (app-controlled data, not user free-text). Parameterized query prevents injection. |
</threat_model>

<verification>
pnpm test -- tests/army-lists/DatasheetBrowserDialog.test.tsx
pnpm build
</verification>

<success_criteria>
- DatasheetBrowserDialog renders all faction datasheets grouped by role with points
- "Browse Datasheets" button in ArmyListDetailSheet opens the dialog via sibling portal
- Selecting a datasheet creates a ghost unit with the canonical ds.name
- Dialog stays open for multi-add
- Missing faction mapping shows helpful message
- All tests pass, build succeeds
</success_criteria>

<output>
Create `.planning/phases/93-datasheet-browser-ghost-units/93-01-SUMMARY.md` when done
</output>

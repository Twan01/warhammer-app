---
phase: 86-shell-route-keyboard
plan: 02
type: execute
wave: 2
depends_on: [86-01]
files_modified:
  - src/features/painting-mode/SectionNavigator.tsx
  - src/features/painting-mode/StepFocalView.tsx
  - tests/painting-mode/SectionNavigator.test.tsx
  - tests/painting-mode/StepFocalView.test.tsx
  - tests/painting-mode/PaintingModePage.test.tsx
autonomous: true
requirements: [SP-05, PX-06]

must_haves:
  truths:
    - "When all steps in a section are complete, the section shows a green checkmark instead of the progress count badge"
    - "Completed section names are muted to visually distinguish from active sections"
    - "Keyboard shortcut hints appear as small kbd badges on navigation and Mark Done buttons"
    - "Time estimate is displayed per step in the execution view"
  artifacts:
    - path: "src/features/painting-mode/SectionNavigator.tsx"
      provides: "Section completion acknowledgment"
      contains: "isComplete"
    - path: "src/features/painting-mode/StepFocalView.tsx"
      provides: "Keyboard shortcut kbd badges"
      contains: "kbd"
    - path: "tests/painting-mode/PaintingModePage.test.tsx"
      provides: "Keyboard shortcut test coverage"
      min_lines: 30
  key_links:
    - from: "src/features/painting-mode/SectionNavigator.tsx"
      to: "sectionProgressMap"
      via: "completed === total check"
      pattern: "progress.completed === progress.total"
---

<objective>
Add section completion acknowledgment and keyboard shortcut discoverability badges, plus test coverage for the painting mode page shell.

Purpose: Completed sections show a clear visual acknowledgment (green checkmark) so painters know they can move on. Kbd badges teach keyboard shortcuts without cluttering the UI. Tests verify keyboard shortcuts work correctly.

Output: Modified SectionNavigator with SP-05 completion state, modified StepFocalView with kbd badges, new PaintingModePage test file, and updated existing test files.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-shell-route-keyboard/86-CONTEXT.md
@.planning/phases/86-shell-route-keyboard/86-RESEARCH.md
@.planning/phases/86-shell-route-keyboard/86-PATTERNS.md
@.planning/phases/86-shell-route-keyboard/86-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Phase 85 components -->

From src/features/painting-mode/SectionNavigator.tsx:
```typescript
export interface SectionNavigatorProps {
  sections: RecipeSection[];
  orderedSteps: RecipeStep[];
  completedSet: Set<number>;
  currentStepId: number | null;
  sectionProgressMap: Map<number, { completed: number; total: number; name: string }>;
  goToStep: (id: number) => void;
}
```

From src/features/painting-mode/StepFocalView.tsx:
```typescript
export interface StepFocalViewProps {
  currentStep: RecipeStep | undefined;
  paint: Paint | undefined;
  stepPhotoUrl: string | undefined;
  isCompleted: boolean;
  onMarkDone: () => void;
  goPrev: () => void;
  goNext: () => void;
  canGoPrev: boolean;
  canGoNext: boolean;
  currentIndex: number;
  totalSteps: number;
  sectionName: string | null;
  isAllComplete: boolean;
}
```

From src/app/painting-mode/page.tsx (created in Plan 01):
```typescript
export function PaintingModePage(): JSX.Element;
// Registers useHotkeys(' ', ...), useHotkeys('arrowleft', ...), useHotkeys('arrowright', ...), useHotkeys('escape', ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SP-05 section completion acknowledgment + D-10 kbd badges</name>
  <files>src/features/painting-mode/SectionNavigator.tsx, src/features/painting-mode/StepFocalView.tsx</files>
  <read_first>
    src/features/painting-mode/SectionNavigator.tsx
    src/features/painting-mode/StepFocalView.tsx
  </read_first>
  <action>
    **SectionNavigator SP-05 modification (per D-08):**

    1. Inside the sectionEntries.map callback (around line 91), after the existing `progressText` derivation, add an `isComplete` boolean: `const isComplete = progress != null && progress.total > 0 && progress.completed === progress.total`.

    2. Modify the section name span (line 117): conditionally apply `text-sm text-muted-foreground` when `isComplete`, otherwise keep `text-sm`.

    3. Replace the progress Badge in the header button (lines 124-126): when `isComplete`, render a `Check` icon with `className="h-4 w-4 text-green-500"` instead of the `Badge` with `progressText`. The `Check` icon is already imported from lucide-react (line 2). Keep the `ChevronDown` icon adjacent in both branches.

    4. Add `data-testid="section-complete"` to the Check icon for test targeting.

    **StepFocalView D-10 kbd badges:**

    1. Modify the Previous step button (lines 118-126): remove `size="icon"`, add `className="flex items-center gap-1"`. After the `ChevronLeft` icon, add `<kbd className="text-[10px] bg-muted px-1 rounded">←</kbd>`.

    2. Modify the Next step button (lines 133-141): remove `size="icon"`, add `className="flex items-center gap-1"`. Before the `ChevronRight` icon, add `<kbd className="text-[10px] bg-muted px-1 rounded">→</kbd>`.

    3. Modify the Mark Done button (lines 145-153): after the "Mark Done" text, add `<kbd className="ml-2 text-[10px] bg-muted px-1 rounded">Space</kbd>`.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - SectionNavigator renders Check icon (h-4 w-4 text-green-500) when progress.completed === progress.total and total > 0
    - SectionNavigator renders Badge with progressText when section is NOT complete
    - Completed section name has className containing "text-muted-foreground"
    - StepFocalView contains three kbd elements with text content "←", "→", and "Space"
    - Prev/Next buttons no longer have size="icon" prop
    - Mark Done button contains kbd element with "Space" text and ml-2 spacing
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Completed sections show green checkmark, active sections show progress count, keyboard shortcut badges visible on all navigation controls</done>
</task>

<task type="auto">
  <name>Task 2: Test coverage for PaintingModePage keyboard shortcuts + SP-05 + kbd badges</name>
  <files>tests/painting-mode/PaintingModePage.test.tsx, tests/painting-mode/SectionNavigator.test.tsx, tests/painting-mode/StepFocalView.test.tsx</files>
  <read_first>
    src/app/painting-mode/page.tsx
    src/features/painting-mode/SectionNavigator.tsx
    src/features/painting-mode/StepFocalView.tsx
    tests/painting-mode/SectionNavigator.test.tsx
    tests/painting-mode/StepFocalView.test.tsx
    tests/painting-mode/PaintingModeView.test.tsx
  </read_first>
  <action>
    **New file: tests/painting-mode/PaintingModePage.test.tsx**

    Follow the mock pattern from tests/painting-mode/PaintingModeView.test.tsx. Mock these modules with vi.mock:
    - `@tanstack/react-router`: mock `useParams` returning `{ assignmentId: "1" }`, mock `useNavigate` returning a vi.fn()
    - `@/hooks/useRecipeAssignments`: mock `useRecipeAssignment` returning `{ data: { id: 1, unit_id: 10, recipe_id: 20, created_at: "2026-01-01" }, isLoading: false }`, mock `useCompleteStep` returning `{ mutate: vi.fn() }`
    - `@/hooks/usePaintingModeState`: mock returning `{ orderedSteps: [mockStep], currentStepId: 1, currentIndex: 0, completedSet: new Set(), isLoading: false, canGoPrev: false, canGoNext: true, goPrev: vi.fn(), goNext: vi.fn(), goToStep: vi.fn(), sectionProgressMap: new Map() }`
    - `@/hooks/useRecipeSections`: mock returning `{ data: [] }`
    - `@/features/painting-mode/PaintingModeView`: mock as a simple div with data-testid="painting-mode-view"
    - `react-hotkeys-hook`: DO NOT mock — let the real useHotkeys fire on keyboard events

    Write tests using `userEvent.setup()` and `userEvent.keyboard(...)`:
    - PX-02: "Space key calls mark done handler" — render PaintingModePage, fire `user.keyboard(' ')`, assert completeMutation.mutate was called
    - PX-03: "ArrowRight calls goNext" — fire `user.keyboard('{ArrowRight}')`, assert mockGoNext called. "ArrowLeft calls goPrev" with canGoPrev: true — assert mockGoPrev called.
    - PX-04: "Escape calls navigate" — fire `user.keyboard('{Escape}')`, assert mockNavigate was called
    - PX-05: "Shortcuts disabled when input focused" — render an input element alongside PaintingModePage (or within the mock), focus it, fire Space, assert mutate NOT called. Note: react-hotkeys-hook handles this automatically via enableOnFormTags default, but test verifies the behavior.
    - "renders PaintingModeView when assignment loads" — assert data-testid="painting-mode-view" is present
    - "shows skeleton while loading" — mock isLoading: true on useRecipeAssignment, assert Skeleton renders

    **Append to tests/painting-mode/SectionNavigator.test.tsx:**

    Add a new describe block "SP-05: section completion acknowledgment" with tests:
    - "renders Check icon when all steps in section are complete" — pass sectionProgressMap with completed === total, assert `data-testid="section-complete"` renders
    - "renders Badge with count when section is incomplete" — pass sectionProgressMap with completed < total, assert Badge with "1/3" text renders
    - "completed section name has muted styling" — pass complete section, assert section name element has class "text-muted-foreground"

    **Append to tests/painting-mode/StepFocalView.test.tsx:**

    Add tests:
    - "renders kbd badges for keyboard shortcuts" — render StepFocalView with valid step, assert 3 kbd elements exist with text "←", "→", "Space"
    - "PX-06: renders time estimate" — already covered by existing test; verify test name references time estimate display
  </action>
  <verify>
    <automated>pnpm test -- tests/painting-mode/</automated>
  </verify>
  <acceptance_criteria>
    - tests/painting-mode/PaintingModePage.test.tsx exists and contains tests for PX-02, PX-03, PX-04, PX-05
    - Space key test asserts completeMutation.mutate was called
    - ArrowLeft/Right tests assert goPrev/goNext were called
    - Escape test asserts navigate was called
    - Input focus test asserts mutate was NOT called when input is focused
    - SectionNavigator.test.tsx contains SP-05 tests for completion checkmark vs progress badge
    - StepFocalView.test.tsx contains test for kbd badge rendering
    - `pnpm test -- tests/painting-mode/` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Full test coverage for keyboard shortcuts (PX-02 through PX-05), section completion acknowledgment (SP-05), and kbd badge rendering (D-10)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan modifies presentation layer only — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-03 | Information Disclosure | Section progress display | accept | Progress counts are local data, no PII; display is accurate |
</threat_model>

<verification>
1. `pnpm build` exits 0
2. `pnpm test -- tests/painting-mode/` — all tests pass
3. `pnpm test` — full suite green, no regressions
</verification>

<success_criteria>
- Completed sections show green checkmark replacing progress badge
- Kbd badges visible on Prev/Next/Mark Done buttons
- All PX-02 through PX-06 and SP-05 tests pass
- Full test suite passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/86-shell-route-keyboard/86-02-SUMMARY.md` when done
</output>

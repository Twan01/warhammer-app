---
phase: 86-shell-route-keyboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/hooks/useRecipeAssignments.ts
  - src/app/router.tsx
  - src/app/painting-mode/page.tsx
  - src/features/painting-mode/PaintingModeView.tsx
autonomous: true
requirements: [PX-02, PX-03, PX-04, PX-05, PX-06]

must_haves:
  truths:
    - "Pressing Space marks the current step done without mouse interaction"
    - "Pressing ArrowLeft/ArrowRight navigates to previous/next step"
    - "Pressing Escape exits Painting Mode and returns to previous surface"
    - "Keyboard shortcuts are silent when focus is inside a text input"
    - "Painting mode renders without sidebar in a full-page distraction-free layout"
  artifacts:
    - path: "src/app/painting-mode/page.tsx"
      provides: "Route shell for painting mode with keyboard shortcuts"
      min_lines: 40
    - path: "src/app/router.tsx"
      provides: "Layout route nesting with bareLayoutRoute for painting mode"
      contains: "bareLayoutRoute"
    - path: "src/hooks/useRecipeAssignments.ts"
      provides: "useRecipeAssignment single-record hook"
      exports: ["useRecipeAssignment", "ASSIGNMENT_KEY"]
  key_links:
    - from: "src/app/router.tsx"
      to: "src/app/painting-mode/page.tsx"
      via: "paintingModeRoute component"
      pattern: "PaintingModePage"
    - from: "src/app/painting-mode/page.tsx"
      to: "src/hooks/useRecipeAssignments.ts"
      via: "useRecipeAssignment hook"
      pattern: "useRecipeAssignment"
    - from: "src/app/painting-mode/page.tsx"
      to: "src/features/painting-mode/PaintingModeView.tsx"
      via: "renders PaintingModeView"
      pattern: "PaintingModeView"
---

<objective>
Create the full-page painting mode route with keyboard shortcuts for hands-free step execution.

Purpose: Let painters navigate and complete recipe steps without touching the mouse. The route renders painting mode in a sidebar-free layout, and keyboard shortcuts (Space/Arrow/Escape) are wired at the page level with automatic input guard protection.

Output: New route at `/painting-mode/$assignmentId` with layout route nesting in router.tsx, new page shell component, useRecipeAssignment hook, and react-hotkeys-hook integration.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-shell-route-keyboard/86-CONTEXT.md
@.planning/phases/86-shell-route-keyboard/86-RESEARCH.md
@.planning/phases/86-shell-route-keyboard/86-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/hooks/useRecipeAssignments.ts:
```typescript
export const ASSIGNMENTS_KEY = ["recipe-assignments"] as const;
export const STEP_PROGRESS_KEY = (assignmentId: number) => ["recipe-assignments", "progress", assignmentId] as const;
export function useCompleteStep(): UseMutationResult<void, Error, CompleteStepVars>;
```

From src/hooks/usePaintingModeState.ts:
```typescript
export function usePaintingModeState(assignmentId: number, recipeId: number): {
  orderedSteps: RecipeStep[];
  currentStepId: number | null;
  currentIndex: number;
  completedSet: Set<number>;
  isLoading: boolean;
  canGoPrev: boolean;
  canGoNext: boolean;
  goPrev: () => void;
  goNext: () => void;
  goToStep: (id: number) => void;
  sectionProgressMap: Map<number, { completed: number; total: number; name: string }>;
};
```

From src/features/painting-mode/PaintingModeView.tsx (BEFORE refactor — will be changed by Task 2):
```typescript
interface PaintingModeViewProps {
  assignmentId: number;
  recipeId: number;
  unitId: number;
}
export function PaintingModeView(props: PaintingModeViewProps): JSX.Element;
```

After refactor (target state):
```typescript
interface PaintingModeViewProps {
  state: ReturnType<typeof usePaintingModeState>;
  onMarkDone: () => void;
  recipeId: number;
  isMutating: boolean;
}
```

From src/db/queries/recipeAssignments.ts:
```typescript
export async function getAssignment(id: number): Promise<RecipeAssignment | null>;
```

From src/types/recipeAssignment.ts:
```typescript
export interface RecipeAssignment {
  id: number;
  unit_id: number;
  recipe_id: number;
  created_at: string;
}
```

From src/app/game-day/page.tsx (clone pattern):
```typescript
export function GameDayPageShell() {
  const { listId } = useParams({ from: "/game-day/$listId" });
  const listIdNum = Number(listId);
  if (Number.isNaN(listIdNum)) return null;
  return <GameDayPage listId={listIdNum} />;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useRecipeAssignment hook + install react-hotkeys-hook</name>
  <files>src/hooks/useRecipeAssignments.ts, package.json, pnpm-lock.yaml</files>
  <read_first>
    src/hooks/useRecipeAssignments.ts
    src/db/queries/recipeAssignments.ts
    package.json
  </read_first>
  <action>
    1. Install react-hotkeys-hook v5.3.2 via `pnpm add react-hotkeys-hook@5.3.2` (per D-04).

    2. In src/hooks/useRecipeAssignments.ts, add `getAssignment` to the import from `@/db/queries/recipeAssignments`. The function already exists at line 80 of recipeAssignments.ts.

    3. Add a new cache key export `ASSIGNMENT_KEY` returning `["recipe-assignments", "by-id", id] as const`.

    4. Add a new `useRecipeAssignment(id: number | undefined)` hook following the exact pattern of `useAssignmentsByUnit` (lines 36-42): enabled-when-defined, queryKey uses ASSIGNMENT_KEY when id is defined and ASSIGNMENTS_KEY as fallback, queryFn resolves null when id is undefined, return type is `RecipeAssignment | null`. Place it after the existing `useStepProgress` hook.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"react-hotkeys-hook": "5.3.2"` or `"^5.3.2"` in dependencies
    - src/hooks/useRecipeAssignments.ts exports `ASSIGNMENT_KEY` and `useRecipeAssignment`
    - `getAssignment` is in the import list from `@/db/queries/recipeAssignments`
    - `useRecipeAssignment` uses `enabled: id !== undefined` guard
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>react-hotkeys-hook installed, useRecipeAssignment hook added and type-checks</done>
</task>

<task type="auto">
  <name>Task 2: Router layout route nesting + PaintingModePage with keyboard shortcuts</name>
  <files>src/app/router.tsx, src/app/painting-mode/page.tsx, src/features/painting-mode/PaintingModeView.tsx</files>
  <read_first>
    src/app/router.tsx
    src/features/painting-mode/PaintingModeView.tsx
    src/hooks/usePaintingModeState.ts
    src/hooks/useRecipeAssignments.ts
    src/components/common/AppLayout.tsx
    src/app/game-day/page.tsx
  </read_first>
  <action>
    **Router restructuring (per D-01, RESEARCH.md layout route pattern):**

    In src/app/router.tsx:

    1. Change the `rootRoute` component to a thin shell: render only `Outlet` plus the dev tools conditional. Remove `AppLayout` and `ActiveFactionProvider` from the root.

    2. Create `layoutRoute` using `createRoute` with `getParentRoute: () => rootRoute`, `id: 'layout'`. Its component renders `AppLayout` wrapping `ActiveFactionProvider` wrapping `Outlet` — exactly what `rootRoute` currently renders minus the dev tools.

    3. Create `bareLayoutRoute` using `createRoute` with `getParentRoute: () => rootRoute`, `id: 'bare-layout'`. Its component renders `ActiveFactionProvider` wrapping `TooltipProvider` (delayDuration 200) wrapping `Outlet` + `Toaster` (richColors, position bottom-right). Import `TooltipProvider` from `@/components/ui/tooltip` and `Toaster` from `@/components/ui/sonner`.

    4. Create `paintingModeRoute` with `getParentRoute: () => bareLayoutRoute`, path `/painting-mode/$assignmentId`, component `PaintingModePage`. Import `PaintingModePage` from `./painting-mode/page`.

    5. Update ALL 15 existing route definitions: change every `getParentRoute: () => rootRoute` to `getParentRoute: () => layoutRoute`. This applies to: dashboardRoute, factionsRoute, collectionRoute, paintingProjectsRoute, recipesRoute, paintsRoute, armyListsRoute, spendingRoute, wishlistRoute, battleLogRoute, goalsRoute, settingsRoute, rulesHubRoute, gameDayRoute, dataHealthRoute.

    6. Restructure `routeTree`: `rootRoute.addChildren([layoutRoute.addChildren([...all 15 existing routes...]), bareLayoutRoute.addChildren([paintingModeRoute])])`.

    **Refactor PaintingModeView to accept lifted state (per D-05):**

    CRITICAL: `usePaintingModeState` uses `useState` for `currentStepId` (line 76). Two instances will diverge — keyboard shortcuts would update the page shell's state but PaintingModeView would render from its own independent state. The hooks MUST be lifted to the page shell exclusively.

    In src/features/painting-mode/PaintingModeView.tsx:

    1. Change `PaintingModeViewProps` interface: remove `assignmentId`, `recipeId`, `unitId` as standalone props. Instead accept the full painting mode state and handlers:
       - `state` (return type of `usePaintingModeState`) — orderedSteps, currentStepId, currentIndex, completedSet, isLoading, canGoPrev, canGoNext, goPrev, goNext, goToStep, sectionProgressMap
       - `onMarkDone: () => void` — the handleMarkDone callback
       - `recipeId: number` — still needed for useRecipeSections and useRecipePaints internally (read-only data, not navigation state)
       - `isMutating: boolean` — completeMutation.isPending passed from page shell

    2. Remove the `usePaintingModeState(assignmentId, recipeId)` call (line 30) and `useCompleteStep()` call (line 31). Replace all references to `state.xxx` with the `state` prop. Replace `handleMarkDone` function body with a call to `props.onMarkDone()`. Replace `completeMutation.isPending` with `props.isMutating`.

    3. Keep `usePaints`, `useRecipeSections`, `isPaintMissing`, photo URL resolution, `paintMap`, `missingPaints`, `bannerDismissed` state — these are read-only presentation concerns that belong in the view.

    **Create PaintingModePage shell (per D-02, D-03, D-05, D-07):**

    Create src/app/painting-mode/page.tsx:

    1. Clone the GameDayPageShell pattern. Extract `assignmentId` from `useParams({ from: '/painting-mode/$assignmentId' })`. Coerce to number, return null if NaN.

    2. Call `useRecipeAssignment(assignmentIdNum)` to resolve `recipe_id` and `unit_id`. Show `Skeleton` (h-screen w-full) while loading. Show "Assignment not found" div if no data.

    3. Call `usePaintingModeState(assignment.id, assignment.recipe_id)` and `useCompleteStep()` EXCLUSIVELY at the page level — these are the single instances (per D-05).

    4. Derive `currentStep` and `sectionName` from state (same as PaintingModeView lines 96-105). Build `handleMarkDone` callback: call `completeMutation.mutate` with assignmentId, unitId, recipeStepId (from state.currentStepId), and session object (unit_id, session_date via todayISO(), duration_minutes 0, recipe_id, recipe_step_id, section_name, recipe_section_id). On success, call `state.goNext()`. Guard with `if (!currentStep) return`. Import `useRecipeSections` for section name lookup and `todayISO` from `@/lib/dates`.

    5. Register four `useHotkeys` calls from `react-hotkeys-hook`:
       - `useHotkeys(' ', handleMarkDone, { preventDefault: true, enabled })` — Space marks done
       - `useHotkeys('arrowleft', () => state.goPrev(), { preventDefault: true, enabled })` — Left navigates
       - `useHotkeys('arrowright', () => state.goNext(), { preventDefault: true, enabled })` — Right navigates
       - `useHotkeys('escape', handleExit)` — Escape exits
       Where `enabled = !!assignment && !state.isLoading` (Pitfall 3 guard).
       `enableOnFormTags` defaults to false per D-06 — no custom guard code needed for PX-05.

    6. For handleExit (D-07): use `useNavigate` from TanStack Router. If `window.history.length <= 1`, navigate to `/`. Otherwise call `navigate({ to: '/' })` (safe fallback rather than `navigate(-1)` which risks exiting the app).

    7. Render `PaintingModeView` passing `state`, `onMarkDone={handleMarkDone}`, `recipeId={assignment.recipe_id}`, `isMutating={completeMutation.isPending}`. Single source of truth for navigation state — no divergence.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/router.tsx defines rootRoute as thin shell (no AppLayout in root component)
    - src/app/router.tsx defines layoutRoute with id 'layout' containing AppLayout + ActiveFactionProvider
    - src/app/router.tsx defines bareLayoutRoute with id 'bare-layout' containing ActiveFactionProvider + TooltipProvider + Toaster but NO AppLayout
    - All 15 existing routes use getParentRoute: () => layoutRoute
    - paintingModeRoute uses getParentRoute: () => bareLayoutRoute with path '/painting-mode/$assignmentId'
    - routeTree nests layoutRoute and bareLayoutRoute as children of rootRoute
    - src/app/painting-mode/page.tsx exports PaintingModePage
    - PaintingModePage calls useHotkeys for ' ', 'arrowleft', 'arrowright', 'escape'
    - Space and arrow hotkeys have preventDefault: true
    - All hotkeys have enabled guard: !!assignment && !isLoading
    - PaintingModeView no longer calls usePaintingModeState or useCompleteStep internally
    - PaintingModeView accepts state prop (return type of usePaintingModeState) and onMarkDone callback
    - PaintingModePage renders PaintingModeView with state, onMarkDone, recipeId, isMutating props
    - Only ONE instance of usePaintingModeState exists (in PaintingModePage, not PaintingModeView)
    - pnpm build exits 0
    - All existing routes still accessible (no 404s from router refactor)
  </acceptance_criteria>
  <done>Painting mode has its own full-page route without sidebar, keyboard shortcuts are registered at the page level with input guard protection</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params -> component | assignmentId from URL is untrusted string, must be coerced and validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Tampering | URL assignmentId param | mitigate | Number coercion + NaN check + null render; useRecipeAssignment returns null for nonexistent IDs |
| T-86-02 | Denial of Service | Keyboard event flooding | accept | react-hotkeys-hook handles keydown dedup by default; low risk in desktop app |
| T-86-SC | Tampering | npm install react-hotkeys-hook | mitigate | Package legitimacy audit passed (slopcheck OK, no postinstall script, 7yr old package) |
</threat_model>

<verification>
1. `pnpm build` exits 0
2. `pnpm test` full suite passes (no regressions from router refactor)
3. Navigate to `/painting-mode/1` in dev — renders PaintingModeView without sidebar
4. All existing routes (`/`, `/collection`, `/recipes`, etc.) still render with sidebar
</verification>

<success_criteria>
- Painting mode route renders full-page without sidebar
- Space/ArrowLeft/ArrowRight/Escape shortcuts work at document level
- Shortcuts do not fire when typing in form inputs
- All existing routes unaffected by router restructuring
- Build passes with zero TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/86-shell-route-keyboard/86-01-SUMMARY.md` when done
</output>

---
phase: 132-update-trustworthiness
plan: 04
type: execute
wave: 3
depends_on: ["132-01", "132-02", "132-03"]
files_modified: []
autonomous: false
requirements: [REL-06, REL-07, REL-08]
must_haves:
  truths:
    - "REL-06, REL-07, REL-08 are all verified before any merge happens"
    - "The full test suite (pnpm test + cargo test) and pnpm build are green"
    - "The CI test check is green on the Theme-A branch before merge"
    - "Theme A is merged to master only after the above gates pass, as a human-gated decision not an automated git operation (D-11)"
  artifacts: []
  key_links:
    - from: "Theme A branch (fix/update-breaks-app-launch)"
      to: "master"
      via: "gated merge after REL-06/07/08 verified + CI green"
      pattern: "master"
---

<objective>
Perform the gated Theme A → `master` merge (D-11) — the payoff of phases 130-132 — only after REL-06/07/08 are verified and CI is green, per the v0.6.0 sequencing law.

Purpose: the sequencing law requires Theme A (phases 130-132) to land AND merge to `master` before any Theme-B refactor (133+) begins. The reliability fix and the large refactors must not coexist in-flight. This merge is a human-gated decision, not an automated git operation — it is the explicit go/no-go for the whole theme.

Output: a verified, CI-green merge of the Theme-A branch into `master` (or a documented no-go with the blocking reason).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-update-trustworthiness/132-CONTEXT.md
@.planning/phases/132-update-trustworthiness/VERIFICATION.md

<notes>
- STATE.md "v0.6.0 Sequencing Law (CRITICAL)": Theme A must land and merge to master — CI gate green + ONE verified real in-place NSIS update — before Theme-B (133+) begins. Strict order A → B → C → D.
- The CI gate (Phase 131) requires the `test` check green on origin/master; keep master green or PRs red-fail. Theme work is on `fix/update-breaks-app-launch`.
- This plan does NOT modify code. It runs the gate checks and then performs the merge as a human-approved step.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full local gate — pnpm test + cargo test + pnpm build green</name>
  <files></files>
  <read_first>
    - .planning/phases/132-update-trustworthiness/132-VALIDATION.md "Sampling Rate" (phase-gate: full suite + cargo test + build before verify-work and the merge)
    - .planning/STATE.md "v0.6.0 Sequencing Law" (the merge precondition)
  </read_first>
  <action>
    Run the full phase gate locally and capture the results: `pnpm test` (Vitest full run), `cd src-tauri && cargo test` (Rust unit tests including the new `frontend_log` tail-trim + the existing `repair_heals_crlf_era_checksums`), and `pnpm build` (tsc + vite). All three MUST be green. If any fails, STOP — record the failure and do not proceed to the merge checkpoint; the failure is a blocker to route back to the relevant plan. Confirm the REL-06 evidence in `VERIFICATION.md` has its three slots filled (from Plan 03 Task 3) and that `git diff --exit-code src-tauri/tauri.conf.json` is clean.
  </action>
  <verify>
    <automated>pnpm test && cd src-tauri && cargo test 2>&1 | tail -5 && cd .. && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 (full Vitest suite green).
    - `cd src-tauri && cargo test` exits 0 (Rust suite green, including `frontend_log` + `repair_heals_crlf_era_checksums`).
    - `pnpm build` exits 0 (tsc + vite clean).
    - `git diff --exit-code src-tauri/tauri.conf.json` exits 0 (D-03 config-safety holds).
    - VERIFICATION.md three evidence slots are filled (REL-06 captured).
  </acceptance_criteria>
  <done>The full local gate is green and REL-06 evidence is captured — preconditions for the merge are met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Gated Theme A → master merge (D-11)</name>
  <what-built>
    Phases 130-132 are complete: the migration/version parity gates (130), the CI test gate (131), and update trustworthiness (132 — auto-relaunch REL-07, frontend.log REL-08, and the verified real NSIS update REL-06). The full local suite + build are green (Task 1) and REL-06 evidence is captured in VERIFICATION.md.
  </what-built>
  <how-to-verify>
    Confirm the Theme-A go/no-go gate before merging `fix/update-breaks-app-launch` into `master` (per the v0.6.0 sequencing law):
    1. CI: confirm the `test` check is GREEN on the Theme-A branch on the public origin (e.g. `gh pr checks` or the GitHub Actions run for the latest branch commit). Keep master green — a red CI is a hard no-go.
    2. REL-06: confirm `VERIFICATION.md` has all three evidence items filled (vN+1 launch, DB rows preserved, preflight.log repair line) and `git diff --exit-code src-tauri/tauri.conf.json` is clean.
    3. REL-07 + REL-08: confirm Plans 01/02 tasks are green (frontend.log writes + auto-relaunch tests passed; REL-07 also observed live in the REL-06 install).
    4. ONLY after all three pass, merge Theme A into `master` (PR merge or fast-forward, your call) and confirm `master` CI stays green post-merge.
    If any gate is red, DO NOT merge — report the blocker; Theme-B (133+) must not begin until this merge lands.
  </how-to-verify>
  <acceptance_criteria>
    - CI `test` check is green on the Theme-A branch before merge.
    - REL-06 evidence complete in VERIFICATION.md + `git diff --exit-code src-tauri/tauri.conf.json` clean.
    - REL-07 + REL-08 automated suites green.
    - Theme A is merged into `master` and post-merge `master` CI is green (or a documented no-go with the blocking reason).
  </acceptance_criteria>
  <resume-signal>Type "merged" once Theme A is merged to master with CI green, or describe the blocker preventing the merge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme-A branch → master | The merge promotes the reliability fix to the shipping branch; a premature merge of un-verified work breaks the sequencing law |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-11 | Tampering (release integrity) | merging un-verified / red-CI work to master | mitigate | Human-gated merge checkpoint requires CI green + REL-06/07/08 verified before merge; a red gate is a hard no-go (D-11 + sequencing law). |
| T-132-12 | Tampering | local-test config leaking into the merged branch | mitigate | Task 1 re-asserts `git diff --exit-code src-tauri/tauri.conf.json` clean before the merge; throwaway artifacts are gitignored (Plan 03). |
</threat_model>

<verification>
- `pnpm test` + `cargo test` + `pnpm build` all green locally.
- CI `test` check green on the Theme-A branch.
- REL-06 evidence captured; `git diff --exit-code src-tauri/tauri.conf.json` clean.
- Theme A merged to master with post-merge CI green.
</verification>

<success_criteria>
- REL-06, REL-07, REL-08 all verified.
- Full local gate + CI green.
- Theme A merged to `master` (the v0.6.0 sequencing-law payoff), unblocking Theme-B (133+).
</success_criteria>

<output>
Create `.planning/phases/132-update-trustworthiness/132-04-SUMMARY.md` when done.
</output>

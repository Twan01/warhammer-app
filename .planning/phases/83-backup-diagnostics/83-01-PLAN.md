---
phase: 83-backup-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useDiagnostics.ts
  - src/lib/backupFreshness.ts
  - src/features/data-health/BackupCard.tsx
  - tests/data-health/backupStatus.test.ts
  - tests/data-health/backupDiagnostics.test.ts
autonomous: true
requirements:
  - DGN-01
  - DGN-02
  - DGN-03

must_haves:
  truths:
    - "BackupStatus interface includes optional app_version field"
    - "handleBackup stores app_version in localStorage after successful export"
    - "hasVersionMismatch pure function correctly compares stored vs current version"
    - "Existing backups without app_version do not cause errors or false mismatch warnings"
  artifacts:
    - path: "src/hooks/useDiagnostics.ts"
      provides: "BackupStatus with app_version field"
      contains: "app_version"
    - path: "src/lib/backupFreshness.ts"
      provides: "hasVersionMismatch pure function"
      exports: ["hasVersionMismatch"]
    - path: "tests/data-health/backupDiagnostics.test.ts"
      provides: "Version mismatch logic tests"
  key_links:
    - from: "src/features/data-health/BackupCard.tsx"
      to: "src/hooks/useDiagnostics.ts"
      via: "BackupStatus type with app_version"
      pattern: "app_version"
    - from: "src/lib/backupFreshness.ts"
      to: "src/features/data-health/BackupCard.tsx"
      via: "hasVersionMismatch import"
      pattern: "hasVersionMismatch"
---

<objective>
Extend the backup data layer with version mismatch detection and app_version storage.

Purpose: Provide the data foundation (BackupStatus.app_version + hasVersionMismatch pure function) that the UI plan (83-02) will consume to render diagnostic details. Per D-02, D-03, and D-08, this is all localStorage + pure functions — no Rust or DB changes.

Output: Extended BackupStatus interface, hasVersionMismatch function, app_version write in handleBackup, and comprehensive tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-backup-diagnostics/83-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/hooks/useDiagnostics.ts:
```typescript
export const BACKUP_STORAGE_KEY = "lastBackup";
export interface BackupStatus {
  date: string;
  path: string;
  success: boolean;
}
export function useBackupStatus(): BackupStatus | null;
```

From src/lib/backupFreshness.ts:
```typescript
export type BackupFreshness = "healthy" | "recommended" | "overdue" | "never";
export function getBackupFreshness(date: string | null): BackupFreshness;
export function getBackupAgeLabel(date: string | null): string;
export const BACKUP_FRESHNESS_DOT_CLASS: Record<BackupFreshness, string>;
```

From src/features/data-health/BackupCard.tsx:
```typescript
// handleBackup (line 44) writes BackupStatus to localStorage at line 65-70
// Currently writes: { date, path, success }
// Must add: app_version from getVersion()
```

From src/features/data-health/VersionInfoCard.tsx (pattern reference):
```typescript
// getVersion() usage pattern (line 48-49):
const [appVersion, setAppVersion] = useState<string | null>(null);
useEffect(() => {
  getVersion().then(setAppVersion).catch(() => setAppVersion("unknown"));
}, []);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BackupStatus interface + add hasVersionMismatch function</name>
  <files>src/hooks/useDiagnostics.ts, src/lib/backupFreshness.ts</files>
  <read_first>
    - src/hooks/useDiagnostics.ts
    - src/lib/backupFreshness.ts
    - src/features/data-health/VersionInfoCard.tsx (getVersion pattern)
  </read_first>
  <action>
    In src/hooks/useDiagnostics.ts:
    - Add optional `app_version?: string` field to the `BackupStatus` interface (per D-02). Must be optional for backward compatibility with existing localStorage entries that lack the field.
    - No other changes to this file.

    In src/lib/backupFreshness.ts:
    - Add and export a pure function `hasVersionMismatch(backupVersion: string | undefined, currentVersion: string | null): boolean`.
    - Returns `false` if either argument is nullish (missing app_version on old backups should NOT show a mismatch warning).
    - Returns `backupVersion !== currentVersion` when both are present.
    - Per D-04, this is a simple string inequality check — no semver parsing needed.
  </action>
  <verify>
    <automated>pnpm test -- tests/data-health/backupStatus.test.ts tests/data-health/backupDiagnostics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - BackupStatus interface in useDiagnostics.ts has `app_version?: string` field
    - backupFreshness.ts exports `hasVersionMismatch` function
    - hasVersionMismatch(undefined, "0.2.14") returns false
    - hasVersionMismatch("0.2.14", null) returns false
    - hasVersionMismatch("0.2.13", "0.2.14") returns true
    - hasVersionMismatch("0.2.14", "0.2.14") returns false
    - Existing exports from both files remain unchanged
  </acceptance_criteria>
  <done>BackupStatus has app_version field; hasVersionMismatch is exported and tested</done>
</task>

<task type="auto">
  <name>Task 2: Store app_version in handleBackup + write all tests</name>
  <files>src/features/data-health/BackupCard.tsx, tests/data-health/backupStatus.test.ts, tests/data-health/backupDiagnostics.test.ts</files>
  <read_first>
    - src/features/data-health/BackupCard.tsx
    - tests/data-health/backupStatus.test.ts
    - tests/data-health/backupCard.test.tsx
  </read_first>
  <action>
    In src/features/data-health/BackupCard.tsx:
    - Add `import { getVersion } from "@tauri-apps/api/app"` at the top.
    - Add a useState + useEffect for appVersion following the VersionInfoCard pattern: `const [appVersion, setAppVersion] = useState&lt;string | null&gt;(null)` and `useEffect(() => { getVersion().then(setAppVersion).catch(() => setAppVersion("unknown")); }, [])`.
    - In `handleBackup`, after a successful export (line 65-70), include `app_version: appVersion ?? undefined` in the BackupStatus object written to localStorage. If appVersion is null (still loading), omit app_version — do not block the backup on version fetch.

    In tests/data-health/backupDiagnostics.test.ts (NEW file):
    - Import `hasVersionMismatch` from `@/lib/backupFreshness`.
    - Test cases: (1) returns false when backupVersion is undefined, (2) returns false when currentVersion is null, (3) returns false when both undefined/null, (4) returns true when versions differ ("0.2.13" vs "0.2.14"), (5) returns false when versions match ("0.2.14" vs "0.2.14"), (6) returns false when both are undefined/null (no false positive).

    In tests/data-health/backupStatus.test.ts:
    - Add a test case "reads and parses backup data with app_version from localStorage" — store a BackupStatus with `app_version: "0.2.14"` and verify `result!.app_version` equals `"0.2.14"`.
    - Add a test case "handles legacy backup data without app_version field" — store a BackupStatus without app_version and verify `result!.app_version` is `undefined` (not null, not error).
  </action>
  <verify>
    <automated>pnpm test -- tests/data-health/backupStatus.test.ts tests/data-health/backupDiagnostics.test.ts tests/data-health/backupCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BackupCard.tsx imports getVersion from @tauri-apps/api/app
    - BackupCard.tsx has appVersion state with useEffect fetching getVersion()
    - handleBackup writes app_version to localStorage BackupStatus
    - tests/data-health/backupDiagnostics.test.ts exists with 5+ test cases for hasVersionMismatch
    - tests/data-health/backupStatus.test.ts has test for app_version present and test for app_version absent
    - All existing backupCard tests still pass (no regressions)
    - pnpm test -- tests/data-health passes
  </acceptance_criteria>
  <done>handleBackup persists app_version; all version mismatch logic and backward-compat tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage | App reads its own previously-written JSON; no untrusted input |
| Tauri IPC (getVersion) | Built-in Tauri API; returns app version string from tauri.conf.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering | localStorage BackupStatus | accept | localStorage is app-local; user tampering with their own backup metadata has no security impact in a personal desktop app |
| T-83-02 | Information Disclosure | app version in localStorage | accept | App version is not sensitive; already visible in VersionInfoCard UI |
</threat_model>

<verification>
pnpm test -- tests/data-health
</verification>

<success_criteria>
- BackupStatus interface has optional app_version field
- hasVersionMismatch pure function exported from backupFreshness.ts
- handleBackup in BackupCard stores app_version after successful export
- Old localStorage entries without app_version parse without errors
- All data-health tests pass including new version mismatch tests
</success_criteria>

<output>
Create `.planning/phases/83-backup-diagnostics/83-01-SUMMARY.md` when done
</output>

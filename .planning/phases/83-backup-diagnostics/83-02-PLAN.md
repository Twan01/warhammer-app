---
phase: 83-backup-diagnostics
plan: 02
type: execute
wave: 2
depends_on:
  - 83-01
files_modified:
  - src/features/data-health/BackupCard.tsx
  - src/features/dashboard/DataHealthSummaryCard.tsx
  - tests/data-health/backupCard.test.tsx
autonomous: true
requirements:
  - DGN-01
  - DGN-02
  - DGN-03
  - DGN-04

must_haves:
  truths:
    - "User who never backed up sees a 'Never backed up' diagnostic flag on Data Health"
    - "User whose backup is overdue sees an 'Overdue' flag with backup age"
    - "User whose backup was made with a different app version sees a version mismatch warning"
    - "Diagnostic detail is hidden by default and available on expansion"
    - "User with a healthy backup and matching version sees a clean green state"
    - "Dashboard DataHealthSummaryCard shows version mismatch indicator when applicable"
  artifacts:
    - path: "src/features/data-health/BackupCard.tsx"
      provides: "Collapsible diagnostic detail section"
      contains: "CollapsibleContent"
    - path: "src/features/dashboard/DataHealthSummaryCard.tsx"
      provides: "Version mismatch indicator on dashboard"
      contains: "outdated"
    - path: "tests/data-health/backupCard.test.tsx"
      provides: "Tests for collapsible diagnostics and version mismatch display"
  key_links:
    - from: "src/features/data-health/BackupCard.tsx"
      to: "src/lib/backupFreshness.ts"
      via: "hasVersionMismatch import"
      pattern: "hasVersionMismatch"
    - from: "src/features/data-health/BackupCard.tsx"
      to: "src/components/ui/collapsible.tsx"
      via: "Collapsible component import"
      pattern: "Collapsible"
    - from: "src/features/dashboard/DataHealthSummaryCard.tsx"
      to: "src/lib/backupFreshness.ts"
      via: "hasVersionMismatch for dashboard indicator"
      pattern: "hasVersionMismatch"
---

<objective>
Add progressive disclosure diagnostic UI to BackupCard and version mismatch indicator to DataHealthSummaryCard.

Purpose: Surface actionable backup health problems (never backed up, overdue, version mismatch) without overwhelming users who have a healthy recent backup. Per D-01/D-05/D-07, the collapsed state shows the existing tier dot + age label; the expanded state shows diagnostic detail rows. Per D-09, the dashboard gets a version mismatch indicator.

Output: BackupCard with Collapsible diagnostic section, DataHealthSummaryCard with mismatch indicator, and comprehensive tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-backup-diagnostics/83-CONTEXT.md
@.planning/phases/83-backup-diagnostics/83-01-SUMMARY.md

<interfaces>
<!-- Contracts from 83-01 that this plan depends on -->

From src/hooks/useDiagnostics.ts (after 83-01):
```typescript
export interface BackupStatus {
  date: string;
  path: string;
  success: boolean;
  app_version?: string; // Added in 83-01
}
export function useBackupStatus(): BackupStatus | null;
```

From src/lib/backupFreshness.ts (after 83-01):
```typescript
export type BackupFreshness = "healthy" | "recommended" | "overdue" | "never";
export function getBackupFreshness(date: string | null): BackupFreshness;
export function getBackupAgeLabel(date: string | null): string;
export function hasVersionMismatch(backupVersion: string | undefined, currentVersion: string | null): boolean;
export const BACKUP_FRESHNESS_DOT_CLASS: Record<BackupFreshness, string>;
```

From src/components/ui/collapsible.tsx:
```typescript
export { Collapsible, CollapsibleTrigger, CollapsibleContent }
// Collapsible accepts: open, defaultOpen, onOpenChange, children
// CollapsibleTrigger wraps the toggle element
// CollapsibleContent wraps the expandable content
```

From src/features/data-health/BackupCard.tsx (after 83-01):
```typescript
// Already has: appVersion state via useState + useEffect with getVersion()
// Already has: tier, tierLabel, ageLabel computed from backupStatus
// The JSX CardContent (line 135-176) needs Collapsible wrapping
```

From src/features/dashboard/DataHealthSummaryCard.tsx:
```typescript
// Backup row at lines 52-55: tier dot + backupLabel
// Needs: hasVersionMismatch check + conditional mismatch text
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Collapsible diagnostic section to BackupCard</name>
  <files>src/features/data-health/BackupCard.tsx</files>
  <read_first>
    - src/features/data-health/BackupCard.tsx
    - src/components/ui/collapsible.tsx
    - src/lib/backupFreshness.ts
    - src/hooks/useDiagnostics.ts
  </read_first>
  <action>
    In BackupCard.tsx, add the Collapsible diagnostic detail section per D-01, D-05, D-06, D-07:

    Imports to add: `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent` from `@/components/ui/collapsible`, `ChevronDown` from `lucide-react`, `hasVersionMismatch` from `@/lib/backupFreshness`.

    Below the existing status line (the div with tier dot + age label, around line 138-141), add a `Collapsible` wrapper with `defaultOpen={false}`.

    The CollapsibleTrigger should be a small button styled as a subtle "Details" toggle with a ChevronDown icon (h-3 w-3). Use `data-state` CSS to rotate the chevron when open: `transition-transform duration-200 data-[state=open]:rotate-180` on the chevron (the Collapsible from radix-ui sets data-state on the trigger). Place the trigger inline after the tier dot + age label line, not on a separate row.

    The CollapsibleContent contains diagnostic detail rows in a `div` with `pt-2 space-y-1.5`:

    Row 1 — Backup age: Label "Backup age" on the left, value is the exact number of days computed from `backupStatus.date` (use the same `getBackupAgeLabel` output or compute days directly). Show a small dot with color matching the tier.

    Row 2 — App version: Label "App version" on the left. If `backupStatus?.app_version` exists and `appVersion` (current) is loaded, compare them with `hasVersionMismatch`. If match: show "v{currentVersion} (matches backup)" with a green dot. If mismatch: show "Backup: v{backupVersion} / Current: v{currentVersion}" with an amber dot (per D-04 informational, amber not red). If backupStatus has no app_version (legacy backup): show "Version info unavailable" with a muted dot.

    Row 3 — Status summary: Label "Status" on the left. Show the tier name with appropriate color text: "healthy" in green, "recommended" in amber, "overdue" in orange, "never" in muted. For "never" tier specifically, make the text slightly more prominent as a subtle call-to-action: "No backup — export one to protect your data" per the CONTEXT specifics note.

    Each diagnostic row follows the pattern: `div.flex.items-center.justify-between.text-sm` with `span.text-muted-foreground` for label and a `span.flex.items-center.gap-1.5` for value containing a small dot (h-1.5 w-1.5 rounded-full) and text.

    The collapsed state must look identical to the current BackupCard — the Collapsible wrapper should not add visual padding or spacing when closed per D-07.
  </action>
  <verify>
    <automated>pnpm test -- tests/data-health/backupCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BackupCard.tsx imports Collapsible, CollapsibleTrigger, CollapsibleContent from @/components/ui/collapsible
    - BackupCard.tsx imports hasVersionMismatch from @/lib/backupFreshness
    - A Collapsible element wraps the diagnostic detail rows with defaultOpen={false}
    - CollapsibleContent contains at least 3 diagnostic rows (age, version, status)
    - Version mismatch row uses amber dot class (bg-amber-500) not red
    - "Never" state shows "No backup" call-to-action text
    - ChevronDown icon present as the CollapsibleTrigger
    - Existing BackupCard functionality (buttons, backup/restore flows) unchanged
  </acceptance_criteria>
  <done>BackupCard has collapsible diagnostic detail section, collapsed by default, with age/version/status rows</done>
</task>

<task type="auto">
  <name>Task 2: Add version mismatch indicator to DataHealthSummaryCard + write tests</name>
  <files>src/features/dashboard/DataHealthSummaryCard.tsx, tests/data-health/backupCard.test.tsx</files>
  <read_first>
    - src/features/dashboard/DataHealthSummaryCard.tsx
    - tests/data-health/backupCard.test.tsx
    - src/features/data-health/BackupCard.tsx (after Task 1 changes)
  </read_first>
  <action>
    In DataHealthSummaryCard.tsx (per D-09):
    - Add imports: `getVersion` from `@tauri-apps/api/app`, `hasVersionMismatch` from `@/lib/backupFreshness`, `useEffect`, `useState` from `react`, `AlertTriangle` from `lucide-react`.
    - Add appVersion state with the same useEffect pattern from VersionInfoCard: `getVersion().then(setAppVersion).catch(() => setAppVersion("unknown"))`.
    - Compute `versionMismatch = hasVersionMismatch(backup?.app_version, appVersion)`.
    - In the backup status row (lines 52-55), after the existing `backupLabel` span, conditionally render when `versionMismatch` is true: an AlertTriangle icon (size 12, className "text-amber-500") followed by a span with text "(outdated)" in `text-amber-500` class. This should be inside the existing `div.flex.items-center.gap-1.5`.

    In tests/data-health/backupCard.test.tsx:
    - Add mock for `@tauri-apps/api/app`: `vi.mock("@tauri-apps/api/app", () => ({ getVersion: vi.fn().mockResolvedValue("0.2.14") }))`.
    - Add test "DGN-04: diagnostic details are hidden by default" — render BackupCard, assert that text "Backup age" is NOT visible in the document (it is inside collapsed content).
    - Add test "DGN-04: diagnostic details appear on expansion" — render BackupCard, find and click the CollapsibleTrigger button (the chevron/details toggle), then assert "Backup age" text IS visible. Use `screen.getByRole("button")` filtering for the details trigger or query by the chevron's test characteristics.
    - Add test "DGN-01: never-backed-up state shows call-to-action in expanded details" — with useBackupStatus returning null, expand the details, assert text matching /no backup/i is present.
    - Add test "DGN-03: version mismatch shows amber indicator in expanded details" — mock useBackupStatus to return `{ date: "2026-05-19T00:00:00Z", path: "test.zip", success: true, app_version: "0.2.13" }` and getVersion to resolve "0.2.14". Expand details, assert text matching "0.2.13" and "0.2.14" both visible, and an element with class `bg-amber-500` is present.
    - Ensure all existing tests in the file continue to work with the added getVersion mock (the mock should be hoisted at module level like the existing mocks).
  </action>
  <verify>
    <automated>pnpm test -- tests/data-health/backupCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DataHealthSummaryCard.tsx imports hasVersionMismatch and getVersion
    - DataHealthSummaryCard.tsx conditionally renders AlertTriangle icon + "(outdated)" text when version mismatch detected
    - "(outdated)" text uses text-amber-500 class
    - backupCard.test.tsx has test for DGN-04 hidden-by-default behavior
    - backupCard.test.tsx has test for DGN-04 expand behavior
    - backupCard.test.tsx has test for DGN-01 never-backed-up expanded state
    - backupCard.test.tsx has test for DGN-03 version mismatch display
    - All existing tests pass without regression
    - pnpm test passes (full suite)
  </acceptance_criteria>
  <done>DataHealthSummaryCard shows version mismatch indicator; BackupCard diagnostic UI fully tested including expansion, never-state, and version mismatch display</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage | App reads its own previously-written JSON; no untrusted input |
| Tauri IPC (getVersion) | Built-in Tauri API; returns app version from tauri.conf.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-03 | Tampering | localStorage BackupStatus.app_version | accept | Personal desktop app; user tampering with own metadata has no security impact |
| T-83-04 | Information Disclosure | Version comparison in UI | accept | App version is publicly visible in VersionInfoCard; no sensitive data |
</threat_model>

<verification>
pnpm test -- tests/data-health
pnpm test
</verification>

<success_criteria>
- BackupCard shows collapsible diagnostic section, collapsed by default
- Expanding reveals: backup age row, app version comparison row, status summary row
- "Never backed up" state shows prominent call-to-action text in expanded details
- Version mismatch shows amber (not red) indicator with both version numbers
- Healthy state with matching version shows all-green confirmations
- DataHealthSummaryCard shows "(outdated)" with amber AlertTriangle when version mismatch detected
- All tests pass including new DGN-01/02/03/04 coverage
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/83-backup-diagnostics/83-02-SUMMARY.md` when done
</output>

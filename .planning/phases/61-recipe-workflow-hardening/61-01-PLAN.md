---
phase: 61-recipe-workflow-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
  - src/features/recipes/RecipeDetailSheet.tsx
  - src/db/queries/datasheets.ts
  - src-tauri/Cargo.lock
  - src/features/rules-hub/RulesHubPage.tsx
  - src/features/rules-hub/rulesHubFilters.ts
  - src/hooks/useDatasheet.ts
autonomous: true
requirements: [RH-01]

must_haves:
  truths:
    - "Migrations 018, 019, 020 are registered in lib.rs get_migrations()"
    - "cargo check passes clean with all migration SQL files included"
    - "pnpm build passes clean (TypeScript + Vite)"
    - "Pre-existing bug fixes (D-11) are committed and not lost"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "Migration 018/019/020 registration"
      contains: "version: 18"
    - path: "src/features/recipes/RecipeDetailSheet.tsx"
      provides: "React error #185 fix (stepsKey dependency)"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/migrations/018_recipe_sections.sql"
      via: "include_str! macro"
      pattern: "include_str!.*018_recipe_sections"
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/migrations/020_workflow_metadata.sql"
      via: "include_str! macro"
      pattern: "include_str!.*020_workflow_metadata"
---

<objective>
Commit pre-existing bug fixes (D-11) and verify migration integrity for RH-01.

Purpose: The migration registration fix (migrations 018/019/020 in lib.rs) and two other
bug fixes are in uncommitted changes. These must be committed as a clean baseline before
any Phase 61 verification work. Then we verify the build passes, confirming RH-01's
migration integrity requirement.

Output: Committed bug fixes, verified build (cargo check + pnpm build).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-recipe-workflow-hardening/61-CONTEXT.md
@.planning/phases/61-recipe-workflow-hardening/61-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit pre-existing bug fixes (D-11) and verify RH-01 build integrity</name>
  <files>
    src-tauri/src/lib.rs,
    src/features/recipes/RecipeDetailSheet.tsx,
    src/db/queries/datasheets.ts,
    src-tauri/Cargo.lock,
    src/features/rules-hub/RulesHubPage.tsx,
    src/features/rules-hub/rulesHubFilters.ts,
    src/hooks/useDatasheet.ts
  </files>
  <read_first>
    src-tauri/src/lib.rs (verify migrations 18, 19, 20 are registered in get_migrations — look for version: 18, version: 19, version: 20 with include_str! to corresponding .sql files),
    src/features/recipes/RecipeDetailSheet.tsx (verify the React error #185 fix is present — stepsKey string dependency or setStepPhotoUrls guard),
    src/db/queries/datasheets.ts (verify faction resolution fix is present),
    .planning/debug/recipe-save-fails.md (context on migration bug root cause),
    .planning/debug/react-error-185-recipe-click.md (context on infinite loop fix)
  </read_first>
  <action>
    Per D-11, commit all uncommitted bug fixes as a pre-Phase-61 baseline commit.

    Step 1: Read each modified file to verify the fixes are present and correct:
    - src-tauri/src/lib.rs must contain Migration entries for version 18 (recipe_sections),
      version 19 (rules_favorites_notes), version 20 (workflow_metadata) with include_str!
      referencing the corresponding .sql files in ../migrations/
    - src/features/recipes/RecipeDetailSheet.tsx must contain the stepsKey fix that prevents
      React error #185 infinite loop (the useEffect dependency should use a stable string key
      derived from step IDs, not the steps array reference directly)
    - src/db/queries/datasheets.ts must contain the faction resolution fix

    Step 2: Run cargo check --manifest-path src-tauri/Cargo.toml to verify Rust build passes
    with all migration SQL files included via include_str!.

    Step 3: Run pnpm build to verify TypeScript + Vite build passes clean.

    Step 4: Stage all modified files (the uncommitted changes listed in git status) and commit
    with message: "fix: register migrations 018-020, fix React error #185, fix faction resolution"

    Note: Also stage src-tauri/Cargo.lock, src/features/rules-hub/RulesHubPage.tsx,
    src/features/rules-hub/rulesHubFilters.ts, src/hooks/useDatasheet.ts — these are
    part of the same uncommitted batch. Include .planning/debug/*.md files and
    .planning/config.json if they have changes.
  </action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - cargo check exits 0 with no errors
    - pnpm build exits 0 with no errors
    - git log --oneline -1 shows a commit containing "register migrations 018-020"
    - src-tauri/src/lib.rs contains "version: 18" and "version: 19" and "version: 20"
    - src/features/recipes/RecipeDetailSheet.tsx contains the stepsKey or setStepPhotoUrls guard fix
    - git status shows clean working tree (no unstaged modified files from D-11 batch)
  </acceptance_criteria>
  <done>All pre-existing bug fixes committed. cargo check and pnpm build both pass, confirming migration SQL files are valid and included. RH-01 build verification complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test — verify recipe_sections table on dev app startup (RH-01)</name>
  <what-built>Migration registration fix committed and build verified. Per D-02, the final RH-01 verification requires manual dev app startup to confirm the Tauri plugin-sql migration runner creates recipe_sections with all 4 workflow metadata columns.</what-built>
  <how-to-verify>
    1. Run: pnpm tauri dev
    2. Wait for the app to launch and fully load
    3. Navigate to Recipes and create or open an existing recipe
    4. If the recipe has sections, the section form should render without errors
    5. Optional: use a SQLite browser on hobbyforge.db to run PRAGMA table_info(recipe_sections) — confirm columns include section_type, technique, execution_mode, applies_to
    6. Close the app
  </how-to-verify>
  <resume-signal>Type "approved" if recipe_sections table works correctly, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL -> SQLite | SQL files are included via include_str! and executed by tauri-plugin-sql — trusted internal boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | T (Tampering) | Migration SQL files | accept | Files are bundled in binary via include_str!; no runtime file access. Internal-only tool. |
</threat_model>

<verification>
- cargo check --manifest-path src-tauri/Cargo.toml exits 0
- pnpm build exits 0
- All D-11 bug fixes committed (clean git status)
- Manual dev app startup confirms recipe_sections table has workflow metadata columns (Task 2)
</verification>

<success_criteria>
RH-01 is satisfied: fresh install creates recipe_sections table with all 4 workflow metadata columns, verified by build passing and manual smoke test.
</success_criteria>

<output>
After completion, create `.planning/phases/61-recipe-workflow-hardening/61-01-SUMMARY.md`
</output>

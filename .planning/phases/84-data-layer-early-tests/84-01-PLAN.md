---
phase: 84-data-layer-early-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/queries/recipeAssignments.ts
  - src/hooks/useRecipeAssignments.ts
  - tests/painting-mode/completeStepWithSession.test.ts
  - tests/painting-mode/useCompleteStep.test.ts
autonomous: true
requirements: [DL-01, DL-03, TS-02]

must_haves:
  truths:
    - "Marking a step done + creating a session is a single atomic DB operation"
    - "If the session INSERT fails, the step progress upsert is rolled back"
    - "Completing a step refreshes kanban, unit assignments, dashboard, and workflow position caches"
  artifacts:
    - path: "src/db/queries/recipeAssignments.ts"
      provides: "completeStepWithSession transactional function"
      contains: "BEGIN TRANSACTION"
    - path: "src/hooks/useRecipeAssignments.ts"
      provides: "useCompleteStep mutation hook with 6-key invalidation"
      exports: ["useCompleteStep"]
    - path: "tests/painting-mode/completeStepWithSession.test.ts"
      provides: "SQL transaction contract tests"
    - path: "tests/painting-mode/useCompleteStep.test.ts"
      provides: "Hook invalidation contract tests"
  key_links:
    - from: "src/hooks/useRecipeAssignments.ts"
      to: "src/db/queries/recipeAssignments.ts"
      via: "import completeStepWithSession"
      pattern: "completeStepWithSession"
    - from: "src/hooks/useRecipeAssignments.ts"
      to: "React Query cache"
      via: "invalidateQueries x6 in onSuccess"
      pattern: "invalidateQueries"
---

<objective>
Add the atomic `completeStepWithSession` database function and the `useCompleteStep` React Query mutation hook with its 6-key cache invalidation contract. Both are tested.

Purpose: Painting Mode requires marking a step done and logging a session as a single atomic operation. The existing `useToggleStepProgress` only invalidates one cache key and cannot write a session simultaneously.
Output: Two production files modified, two test files created — all tests passing.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-data-layer-early-tests/84-CONTEXT.md
@.planning/phases/84-data-layer-early-tests/84-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/paintingSession.ts:
export interface CreateSessionInput {
  unit_id: number;
  session_date: string;
  duration_minutes: number;
  notes?: string | null;
  recipe_id?: number | null;
  recipe_step_id?: number | null;
  section_name?: string | null;
  recipe_section_id?: number | null;
}

From src/hooks/useRecipeAssignments.ts:
export const STEP_PROGRESS_KEY = (assignmentId: number) => ["recipe-assignments", "progress", assignmentId] as const;
export const UNIT_ASSIGNMENTS_KEY = (unitId: number) => ["recipe-assignments", "by-unit", unitId] as const;

From src/hooks/useKanbanEnrichment.ts:
export const KANBAN_ENRICHMENT_KEY = (unitIds: number[]) => ["kanban-enrichment", ...unitIds.sort((a, b) => a - b)] as const;

From src/hooks/useNextPaintingAction.ts:
export const NEXT_PAINTING_ACTION_KEY = ["next-painting-action"] as const;

From src/hooks/useWorkflowPositions.ts:
export const WORKFLOW_POSITIONS_KEY = (unitIds: number[]) => ["workflow-positions", ...unitIds.sort((a, b) => a - b)] as const;

From src/hooks/useDashboardStats.ts:
export const DASHBOARD_STATS_KEY = ["dashboard-stats"] as const;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add completeStepWithSession + test</name>
  <files>src/db/queries/recipeAssignments.ts, tests/painting-mode/completeStepWithSession.test.ts</files>
  <read_first>
    src/db/queries/recipeAssignments.ts
    src/db/queries/paintingSessions.ts
    src/types/paintingSession.ts
    tests/painting/recipeAssignments.test.ts
    .planning/phases/84-data-layer-early-tests/84-PATTERNS.md
  </read_first>
  <action>
    In `src/db/queries/recipeAssignments.ts`:
    - Add `import type { CreateSessionInput } from "@/types/paintingSession"` at the top alongside existing imports.
    - Add `completeStepWithSession(assignmentId: number, recipeStepId: number, session: CreateSessionInput): Promise<void>` at the end of the file.
    - Per D-01: The function calls `getDb()`, then `db.execute("BEGIN TRANSACTION", [])`, then two flat inline SQL statements (step progress UPSERT and session INSERT), then `db.execute("COMMIT", [])`. The catch block calls `db.execute("ROLLBACK", [])` and re-throws.
    - Per D-01: Do NOT call `upsertStepProgress()` or `createSession()` — inline both SQL statements. Add a comment: "Do not extract to helpers — each getDb() call runs outside this transaction."
    - Step progress SQL: `INSERT INTO unit_recipe_step_progress (assignment_id, recipe_step_id, completed, completed_at) VALUES ($1, $2, $3, $4) ON CONFLICT(assignment_id, recipe_step_id) DO UPDATE SET completed = excluded.completed, completed_at = excluded.completed_at` with params `[assignmentId, recipeStepId, 1, new Date().toISOString()]`.
    - Session INSERT SQL: `INSERT INTO painting_sessions (unit_id, session_date, duration_minutes, notes, recipe_id, recipe_step_id, section_name, recipe_section_id) VALUES ($1, $2, $3, $4, $5, $6, $7, $8)` with params mapping all `CreateSessionInput` fields, using `?? null` coercion for optional fields. The `recipe_step_id` param uses the function argument `recipeStepId`, not `session.recipe_step_id`.

    In `tests/painting-mode/completeStepWithSession.test.ts`:
    - Follow the exact test file structure from `tests/painting/recipeAssignments.test.ts`: top-level `executeMock` and `selectMock`, `vi.mock("@/db/client")` before import, `beforeEach` with `mockReset` + default resolved values.
    - Import `completeStepWithSession` AFTER `vi.mock`.
    - Test 1 ("wraps both writes in BEGIN/COMMIT"): call with valid session data, assert `executeMock` called 4 times, calls[0][0] is `"BEGIN TRANSACTION"`, calls[1][0] contains `ON CONFLICT(assignment_id, recipe_step_id)`, calls[2][0] contains `INSERT INTO painting_sessions`, calls[3][0] is `"COMMIT"`.
    - Test 2 ("passes correct params for step progress upsert"): assert calls[1][1] is `[assignmentId, recipeStepId, 1, expect.stringMatching(/^\d{4}-\d{2}-\d{2}T/)]`.
    - Test 3 ("passes correct params for session INSERT"): assert calls[2][1] maps all session fields with null coercion for optionals.
    - Test 4 ("ROLLBACKs if session INSERT throws"): mock executeMock to resolve BEGIN, resolve upsert, reject session INSERT with Error("FK violation"), resolve ROLLBACK. Assert rejects with "FK violation" and calls[3][0] is `"ROLLBACK"`.
    - Test 5 ("ROLLBACKs if step progress upsert throws"): mock executeMock to resolve BEGIN, reject upsert. Assert rejects and ROLLBACK is called.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting-mode/completeStepWithSession.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/queries/recipeAssignments.ts` exports `completeStepWithSession` function
    - The function contains `BEGIN TRANSACTION`, inline upsert SQL with `ON CONFLICT(assignment_id, recipe_step_id)`, inline `INSERT INTO painting_sessions`, `COMMIT`, and `ROLLBACK` in catch
    - No calls to `upsertStepProgress()` or `createSession()` inside the function body
    - `import type { CreateSessionInput }` present at top of file
    - `tests/painting-mode/completeStepWithSession.test.ts` has 5 passing tests
    - Test file uses `vi.mock("@/db/client")` before the import of `completeStepWithSession`
    - `pnpm test -- tests/painting-mode/completeStepWithSession.test.ts` exits 0
  </acceptance_criteria>
  <done>completeStepWithSession is exported, wraps two SQL writes in BEGIN/COMMIT with ROLLBACK on error, 5 tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Add useCompleteStep hook + invalidation test</name>
  <files>src/hooks/useRecipeAssignments.ts, tests/painting-mode/useCompleteStep.test.ts</files>
  <read_first>
    src/hooks/useRecipeAssignments.ts
    src/hooks/useKanbanEnrichment.ts
    src/hooks/useNextPaintingAction.ts
    src/hooks/useWorkflowPositions.ts
    src/hooks/useDashboardStats.ts
    tests/painting/recipeAssignments.test.ts
    .planning/phases/84-data-layer-early-tests/84-PATTERNS.md
  </read_first>
  <action>
    In `src/hooks/useRecipeAssignments.ts`:
    - Add imports: `completeStepWithSession` from `@/db/queries/recipeAssignments`, `NEXT_PAINTING_ACTION_KEY` from `@/hooks/useNextPaintingAction`, `DASHBOARD_STATS_KEY` from `@/hooks/useDashboardStats`, and `import type { CreateSessionInput } from "@/types/paintingSession"`.
    - Do NOT import `KANBAN_ENRICHMENT_KEY` or `WORKFLOW_POSITIONS_KEY` as functions — use raw string array literals for prefix invalidation per D-06.
    - Define inline type `CompleteStepVars = { assignmentId: number; unitId: number; recipeStepId: number; session: CreateSessionInput }`.
    - Add `useCompleteStep()` function after `useBulkCreateAssignments`. Pattern: `const qc = useQueryClient()`, return `useMutation<void, Error, CompleteStepVars>`.
    - `mutationFn`: destructure `{ assignmentId, recipeStepId, session }` and call `completeStepWithSession(assignmentId, recipeStepId, session)`.
    - `onSuccess`: per D-05, invalidate exactly 6 keys using `variables`:
      (1) `STEP_PROGRESS_KEY(variables.assignmentId)` — exact
      (2) `["kanban-enrichment"]` — prefix, raw literal
      (3) `UNIT_ASSIGNMENTS_KEY(variables.unitId)` — exact
      (4) `NEXT_PAINTING_ACTION_KEY` — exact
      (5) `["workflow-positions"]` — prefix, raw literal
      (6) `DASHBOARD_STATS_KEY` — exact
    - Add comment: "D-05: full 6-key invalidation set — do NOT reuse useToggleStepProgress"
    - Add comment on prefix lines: "D-06: prefix — we don't know active unit ID sets"

    In `tests/painting-mode/useCompleteStep.test.ts`:
    - Mock `@/db/queries/recipeAssignments` so `completeStepWithSession` resolves void.
    - Copy `makeWrapper()` helper from `tests/painting/recipeAssignments.test.ts` (lines 54-60): creates `QueryClient` with `retry: false`, spies on `invalidateQueries`, wraps with `QueryClientProvider`.
    - Import `React`, `renderHook`, `waitFor`, `act` from `@testing-library/react`, `QueryClient`, `QueryClientProvider` from `@tanstack/react-query`.
    - Import `useCompleteStep` from `@/hooks/useRecipeAssignments`.
    - Import `STEP_PROGRESS_KEY`, `UNIT_ASSIGNMENTS_KEY` from `@/hooks/useRecipeAssignments`.
    - Import `NEXT_PAINTING_ACTION_KEY` from `@/hooks/useNextPaintingAction`.
    - Import `DASHBOARD_STATS_KEY` from `@/hooks/useDashboardStats`.
    - Test 1 ("invalidates exactly 6 keys on success"): renderHook useCompleteStep in wrapper, mutateAsync with `{ assignmentId: 5, unitId: 1, recipeStepId: 2, session: { unit_id: 1, session_date: "2026-05-19", duration_minutes: 30 } }`, await waitFor, assert spy calledTimes(6).
    - Test 2 ("invalidates STEP_PROGRESS_KEY(assignmentId)"): extract keys from spy calls via `spy.mock.calls.map(c => c[0]?.queryKey)`, assert `containsEqual(STEP_PROGRESS_KEY(5))`.
    - Test 3 ("invalidates UNIT_ASSIGNMENTS_KEY(unitId)"): assert `containsEqual(UNIT_ASSIGNMENTS_KEY(1))`.
    - Test 4 ("invalidates NEXT_PAINTING_ACTION_KEY"): assert `containsEqual(NEXT_PAINTING_ACTION_KEY)`.
    - Test 5 ("invalidates DASHBOARD_STATS_KEY"): assert `containsEqual(DASHBOARD_STATS_KEY)`.
    - Test 6 ("uses prefix invalidation for kanban-enrichment and workflow-positions"): assert keys `containsEqual(["kanban-enrichment"])` and `containsEqual(["workflow-positions"])`.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting-mode/useCompleteStep.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useRecipeAssignments.ts` exports `useCompleteStep` function
    - The hook's `onSuccess` calls `invalidateQueries` exactly 6 times
    - Prefix invalidation uses `["kanban-enrichment"]` and `["workflow-positions"]` raw literals, NOT the key functions
    - The mutation variables type includes `unitId: number`
    - `tests/painting-mode/useCompleteStep.test.ts` has 6 passing tests
    - `pnpm test -- tests/painting-mode/useCompleteStep.test.ts` exits 0
    - `pnpm build` passes (no TypeScript errors from new imports)
  </acceptance_criteria>
  <done>useCompleteStep hook exported with 6-key invalidation, 6 contract tests passing, TypeScript compiles clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook -> DB query | Mutation variables flow from React to SQLite via completeStepWithSession |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Tampering | completeStepWithSession SQL | mitigate | All SQL uses $1, $2 positional params — no string interpolation |
| T-84-02 | Tampering | Transaction partial commit | mitigate | ROLLBACK in catch block re-throws; caller shows toast on error |
| T-84-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
pnpm test -- tests/painting-mode/completeStepWithSession.test.ts
pnpm test -- tests/painting-mode/useCompleteStep.test.ts
pnpm build
</verification>

<success_criteria>
- completeStepWithSession function exists with BEGIN/COMMIT/ROLLBACK transaction pattern
- useCompleteStep hook invalidates exactly 6 cache keys on success
- 11 tests passing across 2 test files
- TypeScript compiles without errors
</success_criteria>

<output>
Create `.planning/phases/84-data-layer-early-tests/84-01-SUMMARY.md` when done
</output>

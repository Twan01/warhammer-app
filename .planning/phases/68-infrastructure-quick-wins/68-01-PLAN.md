---
phase: 68-infrastructure-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/queries/recipeSections.ts
  - tests/painting/recipeSections.test.ts
  - package.json
  - src-tauri/tauri.conf.json
autonomous: true
requirements:
  - MIG-01
  - MIG-02
  - VER-01
  - REC-03

must_haves:
  truths:
    - "User can set a section metadata field to a value and then clear it back to empty; the cleared state persists after save"
    - "package.json and tauri.conf.json both show version 0.2.11"
    - "All 21 hobbyforge migrations and 3 rules migrations are registered in lib.rs"
  artifacts:
    - path: "src/db/queries/recipeSections.ts"
      provides: "updateRecipeSection with direct assignment for workflow metadata fields"
      contains: "section_type = $7"
    - path: "tests/painting/recipeSections.test.ts"
      provides: "Test asserting workflow metadata uses direct assignment not COALESCE"
      contains: "not.toContain"
    - path: "package.json"
      provides: "Version alignment"
      contains: '"0.2.11"'
    - path: "src-tauri/tauri.conf.json"
      provides: "Version alignment"
      contains: '"0.2.11"'
  key_links:
    - from: "src/db/queries/recipeSections.ts"
      to: "SQLite recipe_sections table"
      via: "db.execute UPDATE with direct assignment"
      pattern: "section_type = \\$7"
---

<objective>
Fix the COALESCE null-clearing bug on 4 workflow metadata fields (REC-03), bump version numbers to 0.2.11 (VER-01), and document verified migration registration completeness (MIG-01, MIG-02).

Purpose: Workflow metadata fields (section_type, technique, execution_mode, applies_to) cannot currently be cleared back to null through the UI because COALESCE blocks null from flowing through. Version numbers are stale at 0.2.7. Migration registration must be confirmed complete for fresh-install confidence.

Output: Fixed updateRecipeSection query, updated tests, aligned version numbers, documented migration verification.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-infrastructure-quick-wins/68-CONTEXT.md
@.planning/phases/68-infrastructure-quick-wins/68-RESEARCH.md
@.planning/phases/68-infrastructure-quick-wins/68-PATTERNS.md

<interfaces>
From src/db/queries/recipeSections.ts (updateRecipeSection — the function being fixed):
- Lines 59-62: COALESCE($7, section_type), COALESCE($8, technique), COALESCE($9, execution_mode), COALESCE($10, applies_to) — these must become direct assignment
- Lines 55, 58: surface = $3, notes = $6 — the existing correct pattern to replicate

From tests/painting/recipeSections.test.ts:
- Line 204: test "UPDATE uses COALESCE for workflow metadata $7-$10" — must be replaced
- Line 226: test "surface and notes use direct assignment (not COALESCE)" — model for new test
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix COALESCE null-clearing bug and update tests (REC-03)</name>
  <files>src/db/queries/recipeSections.ts, tests/painting/recipeSections.test.ts</files>
  <read_first>
    - src/db/queries/recipeSections.ts (full file — see current COALESCE usage on lines 59-62, correct direct assignment pattern on lines 55, 58)
    - tests/painting/recipeSections.test.ts (full file — see Group 3 tests starting at line 192, especially the COALESCE assertion at line 204 and the direct assignment model test at line 226)
    - .planning/phases/68-infrastructure-quick-wins/68-PATTERNS.md (Pattern Assignments section for recipeSections.ts)
  </read_first>
  <action>
In `src/db/queries/recipeSections.ts`, in the `updateRecipeSection` function, change the 4 workflow metadata field assignments from COALESCE to direct assignment per D-01:
- Line 59: `section_type = COALESCE($7, section_type)` becomes `section_type = $7`
- Line 60: `technique = COALESCE($8, technique)` becomes `technique = $8`
- Line 61: `execution_mode = COALESCE($9, execution_mode)` becomes `execution_mode = $9`
- Line 62: `applies_to = COALESCE($10, applies_to)` becomes `applies_to = $10`

No changes to the parameter array — the existing `?? null` coercion at lines 70-73 already handles null correctly per D-02.

In `tests/painting/recipeSections.test.ts`:
1. Remove (or replace) the test at line 204 titled "UPDATE uses COALESCE for workflow metadata $7-$10 and has 10 params total" — it asserts the buggy COALESCE behavior.
2. Add a new test in Group 3 titled "workflow metadata fields use direct assignment (not COALESCE) to allow clearing" that:
   - Creates an UpdateRecipeSectionInput with `id: 5, section_type: null, technique: null, execution_mode: null, applies_to: null`
   - Calls updateRecipeSection
   - Asserts sql does NOT contain `COALESCE($7`, `COALESCE($8`, `COALESCE($9`, `COALESCE($10`
   - Asserts sql DOES contain `section_type = $7`, `technique = $8`, `execution_mode = $9`, `applies_to = $10`
   - Asserts params has length 10
   - Follow the exact assertion style from the existing "surface and notes use direct assignment" test at line 226
  </action>
  <verify>
    <automated>pnpm test -- tests/painting/recipeSections.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/db/queries/recipeSections.ts contains `section_type = $7` (not `COALESCE($7, section_type)`)
    - src/db/queries/recipeSections.ts contains `technique = $8` (not `COALESCE($8, technique)`)
    - src/db/queries/recipeSections.ts contains `execution_mode = $9` (not `COALESCE($9, execution_mode)`)
    - src/db/queries/recipeSections.ts contains `applies_to = $10` (not `COALESCE($10, applies_to)`)
    - tests/painting/recipeSections.test.ts does NOT contain `expect(sql).toContain("COALESCE($7, section_type)")`
    - tests/painting/recipeSections.test.ts DOES contain `expect(sql).toContain("section_type = $7")`
    - `pnpm test -- tests/painting/recipeSections.test.ts` passes with 0 failures
  </acceptance_criteria>
  <done>updateRecipeSection allows null to flow through for all 4 workflow metadata fields; test asserts direct assignment pattern; all recipeSections tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Bump version numbers and verify migration registration (VER-01, MIG-01, MIG-02)</name>
  <files>package.json, src-tauri/tauri.conf.json</files>
  <read_first>
    - package.json (line 3-4 — current version field)
    - src-tauri/tauri.conf.json (line 1-5 — version field at line 3, do NOT touch $schema URL at line 1)
    - src-tauri/src/lib.rs (get_migrations function — verify all 21 versions registered; get_rules_migrations — verify all 3 versions registered)
    - .planning/phases/68-infrastructure-quick-wins/68-RESEARCH.md (Migration Registration Audit section — complete verification table)
  </read_first>
  <action>
Per D-06, update version in both files from `"0.2.7"` to `"0.2.11"`:
- `package.json` line 4: change `"version": "0.2.7"` to `"version": "0.2.11"`
- `src-tauri/tauri.conf.json` line 3: change `"version": "0.2.7"` to `"version": "0.2.11"` — do NOT modify the `$schema` URL at line 1

Per D-07, verify migration registration by reading `src-tauri/src/lib.rs` and confirming:
- `get_migrations()` registers versions 1 through 21 (21 hobbyforge migrations)
- `get_rules_migrations()` registers versions 1 through 3 (3 rules migrations)
- Cross-reference against files in `src-tauri/migrations/` — all 24 SQL files (21 hobbyforge + 3 rules) must have a matching registration entry

Per D-08, MIG-02 fresh install validation is a manual smoke test documented in RESEARCH.md. No automated action needed here — the verification is confirmed by the migration registration audit above. Phase 72 (TST-01) will add automated migration parity tests.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const t=require('./src-tauri/tauri.conf.json'); if(p.version!=='0.2.11')throw new Error('package.json: '+p.version); if(t.version!=='0.2.11')throw new Error('tauri.conf.json: '+t.version); console.log('Both versions: 0.2.11')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"version": "0.2.11"`
    - src-tauri/tauri.conf.json contains `"version": "0.2.11"` at the top-level version key (line 3)
    - src-tauri/tauri.conf.json `$schema` URL at line 1 is unchanged
    - lib.rs get_migrations() contains 21 Migration structs with versions 1-21 (verified by reading, no code change)
    - lib.rs get_rules_migrations() contains 3 Migration structs with versions 1-3 (verified by reading, no code change)
  </acceptance_criteria>
  <done>Both config files show version 0.2.11; migration registration confirmed complete for all 24 SQL files with no gaps</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries are crossed in this plan. All changes are internal SQL query corrections and config file metadata updates.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Tampering | recipeSections.ts UPDATE query | accept | Direct assignment is the correct pattern — already used for surface/notes in the same function. No new attack surface. |
</threat_model>

<verification>
1. `pnpm test -- tests/painting/recipeSections.test.ts` — all tests pass
2. `node -e "..."` version check — both files show 0.2.11
3. `pnpm build` — TypeScript compilation succeeds (no type errors from changes)
</verification>

<success_criteria>
- COALESCE removed from workflow metadata fields in updateRecipeSection
- Tests updated to assert direct assignment pattern
- Version numbers aligned to 0.2.11 in both config files
- Migration registration verified complete (documentation, no code change)
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/68-infrastructure-quick-wins/68-01-SUMMARY.md`
</output>

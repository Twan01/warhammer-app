---
phase: 146.1-resync-section-identity-fix
plan: 01
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - tests/data-layer/technique-resync-apply-path.test.ts
  - src/types/recipeSection.ts
  - src/db/queries/recipeTechniqueInstances.ts
  - src/db/queries/recipes.ts
autonomous: true
requirements: [LINK-01, INTG-05]

must_haves:
  truths:
    - "A regression test drives the REAL applyTechnique path (no manual technique_section_id UPDATE) and FAILS on the pre-fix code, proving the duplication bug"
    - "After applyTechnique then a structural saveTechniqueGraph edit, the count of recipe_sections for that instance is unchanged (no duplicate section set)"
    - "After that resync, a previously-completed step's unit_recipe_step_progress row still exists (FND-03 preserved)"
    - "applyTechnique writes technique_section_id = source technique_sections.id into every recipe_sections row it inserts"
    - "duplicateRecipe copies technique_section_id into the new recipe_sections rows; duplicated technique sections survive a subsequent resync without duplicating"
    - "RecipeSection type declares technique_section_id so it reads back non-undefined under strict TS"
    - "pnpm build is TypeScript-clean and the full vitest suite is green"
  artifacts:
    - path: "tests/data-layer/technique-resync-apply-path.test.ts"
      provides: "Guard-first regression proving applyTechnique populates technique_section_id and resync updates (not duplicates) sections through the production path"
      contains: "applyTechnique"
    - path: "src/db/queries/recipeTechniqueInstances.ts"
      provides: "applyTechnique recipe_sections INSERT now includes technique_section_id"
      contains: "technique_section_id"
    - path: "src/db/queries/recipes.ts"
      provides: "duplicateRecipe recipe_sections INSERT now carries technique_section_id"
      contains: "technique_section_id"
    - path: "src/types/recipeSection.ts"
      provides: "RecipeSection.technique_section_id field"
      contains: "technique_section_id"
  key_links:
    - from: "src/db/queries/recipeTechniqueInstances.ts"
      to: "recipe_sections.technique_section_id"
      via: "INSERT writes section.id (technique_sections PK) into technique_section_id"
      pattern: "technique_section_id"
    - from: "src/db/queries/recipes.ts"
      to: "recipe_sections.technique_section_id"
      via: "duplicateRecipe copies section.technique_section_id ?? null"
      pattern: "technique_section_id"
---

<objective>
Close the v0.7.0 milestone-audit blockers LINK-01 and INTG-05. Their single root
cause: `recipe_sections.technique_section_id` (migration 052) is never written by
the production apply/duplicate paths, so `resyncTechniqueInstances` finds an empty
section-match map and INSERTs duplicate sections on every technique edit instead of
updating the originals. Fix the three write-sites and prove it with a guard-first
regression test that drives the REAL `applyTechnique` path (the original
`technique-resync.test.ts` masked the bug by setting the column via a manual UPDATE).

Output: a regression test (RED on current code, GREEN after fix) plus the
column-write fixes in `applyTechnique`, `duplicateRecipe`, and the `RecipeSection` type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v0.7.0-MILESTONE-AUDIT.md
@.planning/phases/146.1-resync-section-identity-fix-populate-technique-section-id-in/146.1-CONTEXT.md

<interfaces>
<!-- Exact fix sites — no exploration required. -->

src/db/queries/recipeTechniqueInstances.ts (applyTechnique, ~line 80) — current INSERT:
  INSERT INTO recipe_sections
    (recipe_id, name, surface, optional, order_index, notes, technique_instance_id)
    VALUES ($1, $2, $3, $4, $5, $6, $7)
  with values [recipeId, section.name, section.surface ?? null, section.optional,
    insertAfterSectionIndex + si, section.notes ?? null, instanceId].
  `section` is the technique_sections row (has `.id`). FIX: append technique_section_id
  to the column list and `section.id` to the values ($8). Keep ordering: put
  technique_section_id immediately after technique_instance_id.

src/db/queries/recipes.ts (duplicateRecipe, ~line 225) — current section-copy INSERT:
  INSERT INTO recipe_sections
    (recipe_id, name, surface, optional, order_index, notes,
     section_type, technique, execution_mode, applies_to, technique_instance_id)
    VALUES ($1 ... $11)
  `section` comes from `SELECT * FROM recipe_sections` (RecipeSection[]). FIX: append
  technique_section_id column + `section.technique_section_id ?? null` value ($12).
  There is an "AUDIT: add new recipe_sections columns here" comment at the loop —
  this is exactly that case.

src/types/recipeSection.ts (RecipeSection interface, ~line 41) — declares
  `technique_instance_id?: number | null;` but NOT technique_section_id. FIX: add
  `technique_section_id?: number | null;` next to it.

src/db/queries/recipeTechniqueResync.ts (resyncTechniqueInstances, ~lines 163-245) —
  reads recipe_sections.technique_section_id to build recipeSectionByTechSectionId,
  then UPDATEs matched sections and INSERTs only unmatched ones. No change needed here;
  it works once technique_section_id is populated upstream.

migration 052_technique_resync.sql:11 — column already exists (ADD COLUMN
  technique_section_id INTEGER). NO new migration.

Test scaffolding (tests/data-layer/apply-technique.test.ts + technique-resync.test.ts):
  - `// @vitest-environment node` line 1
  - `vi.mock("@/db/client", () => ({ getDb: vi.fn() }));`
  - helpers from `./db-helpers`: createHobbyforgeDb, createTestRecipe, createDbBridge
  - beforeEach builds fixtures + `vi.mocked(getDb).mockResolvedValue(createDbBridge(db) as never)`
  - applyTechnique(recipeId, techniqueId, insertAfterSectionIndex, slotFills) from
    @/db/queries/recipeTechniqueInstances seeds the instance.
  - saveTechniqueGraph from @/db/queries/techniques performs the structural edit that
    triggers resyncTechniqueInstances; inspect its existing tests for the call shape.
  - raw-row assertions: db.prepare("SELECT ... ").get(id) / .all(id).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard-first regression test through the real apply path (RED)</name>
  <files>tests/data-layer/technique-resync-apply-path.test.ts</files>
  <read_first>
    - tests/data-layer/apply-technique.test.ts (fixture builders + createDbBridge + getDb mock pattern)
    - tests/data-layer/technique-resync.test.ts (resync edit + progress-survival assertions; note its line ~133 manual UPDATE — DO NOT replicate it)
    - tests/data-layer/db-helpers.ts (createHobbyforgeDb, createTestRecipe, createDbBridge)
    - src/db/queries/recipeTechniqueInstances.ts (applyTechnique signature)
    - src/db/queries/techniques.ts (saveTechniqueGraph signature + how its tests invoke a structural edit)
  </read_first>
  <action>
    Create `tests/data-layer/technique-resync-apply-path.test.ts` (`// @vitest-environment node` line 1).
    Build a technique with >=1 section and >=2 steps (at least one referencing a colour slot) and a recipe.
    Seed the instance via the REAL `applyTechnique(recipeId, techniqueId, 0, slotFills)` — NOT a manual
    `UPDATE recipe_sections SET technique_section_id`. Mark one materialised recipe_step complete by inserting
    a `unit_recipe_step_progress` row (use the same FK chain as apply-technique.test.ts / the migration-028 schema).
    Then perform a STRUCTURAL technique edit via `saveTechniqueGraph` (e.g. reorder or rename a step / add a step)
    so `resyncTechniqueInstances` runs.
    Assert:
      (a) The count of `recipe_sections WHERE technique_instance_id = <instanceId>` is UNCHANGED vs. immediately
          after apply (no duplicate section set) — this FAILS on current code (RED).
      (b) Every `recipe_sections` row for the instance has a non-NULL `technique_section_id` after apply.
      (c) The previously-completed step's `unit_recipe_step_progress` row still exists (FND-03).
    Add a second `describe` for duplication: apply a technique to a recipe, `duplicateRecipe(originalId)`, and assert
    the duplicated recipe's technique sections have non-NULL `technique_section_id` equal to the source, and that a
    subsequent `saveTechniqueGraph` structural edit updates (does not duplicate) the duplicated recipe's sections too.
    This task is RED until Task 2 — commit it as `test(146.1-01): ...` with a note that it proves the duplication bug.
  </action>
  <acceptance_criteria>
    - tests/data-layer/technique-resync-apply-path.test.ts exists, `// @vitest-environment node` on line 1
    - Test seeds via applyTechnique (grep: file contains `applyTechnique(`) and contains NO `UPDATE recipe_sections SET technique_section_id`
    - Running `pnpm test -- tests/data-layer/technique-resync-apply-path.test.ts` FAILS before Task 2 (section count doubles / technique_section_id NULL) — RED proven
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Populate technique_section_id at all three sites (GREEN)</name>
  <files>src/types/recipeSection.ts, src/db/queries/recipeTechniqueInstances.ts, src/db/queries/recipes.ts</files>
  <read_first>
    - src/types/recipeSection.ts (add the field next to technique_instance_id)
    - src/db/queries/recipeTechniqueInstances.ts (applyTechnique INSERT ~line 80)
    - src/db/queries/recipes.ts (duplicateRecipe section INSERT ~line 225)
    - src/db/queries/recipeTechniqueResync.ts (confirm it consumes technique_section_id — no change needed)
  </read_first>
  <action>
    1. In `src/types/recipeSection.ts`, add `technique_section_id?: number | null;` to the RecipeSection interface
       (beside technique_instance_id).
    2. In `applyTechnique` (recipeTechniqueInstances.ts), add `technique_section_id` to the recipe_sections INSERT
       column list (immediately after technique_instance_id) and append `section.id` as the new positional param.
    3. In `duplicateRecipe` (recipes.ts), add `technique_section_id` to the recipe_sections INSERT column list and
       append `section.technique_section_id ?? null` as the new positional param. Update the param count/placeholders.
    Do NOT add a migration (column exists in 052). Keep flat inline SQL, $1/$2 params, booleans 0|1.
    Run `pnpm test -- tests/data-layer/technique-resync-apply-path.test.ts` (now GREEN), then the full `pnpm test`
    and `pnpm build` (TypeScript-clean).
  </action>
  <acceptance_criteria>
    - src/types/recipeSection.ts contains `technique_section_id`
    - applyTechnique recipe_sections INSERT contains `technique_section_id` and passes `section.id`
    - duplicateRecipe recipe_sections INSERT contains `technique_section_id` and passes `section.technique_section_id`
    - `pnpm test -- tests/data-layer/technique-resync-apply-path.test.ts` is GREEN
    - Full `pnpm test` passes (note: tests/dashboard/recentActivityQuery.test.ts is a known timing flake — re-run in isolation if it trips) and `pnpm build` is TypeScript-clean
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Regression test drives the real applyTechnique path and is GREEN after Task 2 (and was RED before).
- All three write-sites populate technique_section_id; resyncTechniqueInstances updates rather than duplicates sections.
- Full suite green, build clean. LINK-01 and INTG-05 closed.
</verification>

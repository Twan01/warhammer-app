---
phase: 109-sub-faction-filter-ui
plan: 02
type: execute
wave: 2
depends_on: [109-01]
files_modified:
  - src/features/unit-database/UdbFilterBar.tsx
  - src/features/unit-database/DatabaseBrowserPage.tsx
  - src/features/army-lists/UnitPickerDialog.tsx
  - src/features/units/CollectionPage.tsx
  - src/features/units/UnitFilters.tsx
  - src/features/units/collectionFilters.ts
  - src/features/units/applyUnitFilters.ts
autonomous: true
requirements: [SF-03, SF-04, SF-05]

must_haves:
  truths:
    - "Database browser shows sub-faction dropdown when browsing a faction with sub-factions"
    - "Database browser hides sub-faction dropdown for factions without sub-factions"
    - "Selecting a sub-faction in DB browser filters the unit list to matching units only"
    - "Changing faction in DB browser resets the sub-faction filter"
    - "Army list unit picker shows sub-faction dropdown for applicable factions"
    - "Selecting a sub-faction in unit picker narrows the list to matching collection units"
    - "Collection browser shows sub-faction dropdown when exactly one faction with sub-factions is selected"
    - "Collection browser hides sub-faction dropdown when zero or multiple factions are selected"
    - "Selecting a sub-faction in collection filters to collection units linked to matching UDB units"
  artifacts:
    - path: "src/features/unit-database/UdbFilterBar.tsx"
      provides: "Sub-faction Select dropdown before Role dropdown"
      contains: "subFactionFilter"
    - path: "src/features/unit-database/DatabaseBrowserPage.tsx"
      provides: "Sub-faction data wired to filters and filter bar"
      contains: "useUdbSubFactions"
    - path: "src/features/army-lists/UnitPickerDialog.tsx"
      provides: "Sub-faction dropdown for unit picker"
      contains: "subFactionFilter"
    - path: "src/features/units/UnitFilters.tsx"
      provides: "Sub-faction Select in collection filter bar"
      contains: "subFactionFilter"
    - path: "src/features/units/CollectionPage.tsx"
      provides: "Sub-faction filtering wired to collection units"
      contains: "useUdbSubFaction"
  key_links:
    - from: "src/features/unit-database/DatabaseBrowserPage.tsx"
      to: "src/hooks/useUnitDatabase.ts"
      via: "useUdbSubFactions hook"
      pattern: "useUdbSubFactions"
    - from: "src/features/army-lists/UnitPickerDialog.tsx"
      to: "src/hooks/useUnitDatabase.ts"
      via: "useUdbSubFactions + useUdbSubFactionUnitIds"
      pattern: "useUdbSubFaction"
    - from: "src/features/units/CollectionPage.tsx"
      to: "src/hooks/useUnitDatabase.ts"
      via: "useUdbSubFactions + useUdbSubFactionUnitIds"
      pattern: "useUdbSubFaction"
---

<objective>
Wire sub-faction filter dropdowns into three UI surfaces: the database browser filter bar, the army list unit picker dialog, and the collection browser filters.

Purpose: Users can now narrow unit lists by chapter/warband/sub-faction across all browsing surfaces, making it easy to find chapter-specific units in large factions like Space Marines.
Output: Sub-faction Select dropdowns in all three surfaces, conditionally rendered based on data availability.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-sub-faction-filter-ui/109-RESEARCH.md
@.planning/phases/109-sub-faction-filter-ui/109-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/db/queries/unitDatabase.ts (after Plan 01):
- UdbUnitSummary: { id, faction_id, name, role, base_points, min_models, max_models, sub_faction: string | null }
- getDistinctSubFactions(factionId: string): Promise<string[]>
- getUdbUnitIdsBySubFaction(factionId: string, subFaction: string): Promise<string[]>

From src/hooks/useUnitDatabase.ts (after Plan 01):
- useUdbSubFactions(factionId: string | null): useQuery result with data: string[]
- useUdbSubFactionUnitIds(factionId: string | null, subFaction: string | null): useQuery result with data: string[]

From src/features/unit-database/databaseBrowserFilters.ts (after Plan 01):
- subFactionFilter: string | null (in store state)
- setSubFactionFilter: (sf: string | null) => void
- setSelectedFactionId also resets subFactionFilter to null

From src/features/unit-database/applyUdbFilters.ts (after Plan 01):
- UdbFiltersInput now includes subFactionFilter: string | null
- applyUdbFilters filters by sub_faction when subFactionFilter is set

From src/types/faction.ts:
- Faction.wahapedia_faction_id: string | null (line 14) -- maps collection numeric ID to UDB string ID

From src/features/units/collectionFilters.ts:
- factions: number[] -- multi-select faction filter
- clearAll resets all filters

From src/features/units/applyUnitFilters.ts:
- UnitFiltersInput: { search, factions, statuses, categories, activeOnly, battleReady }
- applyUnitFilters<T extends Unit>(units, filters): T[]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database browser sub-faction dropdown (SF-03)</name>
  <files>src/features/unit-database/UdbFilterBar.tsx, src/features/unit-database/DatabaseBrowserPage.tsx</files>
  <read_first>
    src/features/unit-database/UdbFilterBar.tsx
    src/features/unit-database/DatabaseBrowserPage.tsx
    src/features/unit-database/databaseBrowserFilters.ts
    src/hooks/useUnitDatabase.ts
  </read_first>
  <action>
    Per D-04: Modify DatabaseBrowserFilters in UdbFilterBar.tsx to accept a new prop subFactions: string[] (default empty array). Destructure subFactionFilter and setSubFactionFilter from useDatabaseBrowserFilters. Add subFactionFilter !== null to hasActiveFilters check. Add a Select dropdown as the FIRST filter element (before the Role Select), conditionally rendered only when subFactions.length > 0. Pattern: value={subFactionFilter ?? ""}, onValueChange with __clear__ sentinel mapping to null. Placeholder: "Sub-faction". First SelectItem: value="__clear__", text "All sub-factions". Then map subFactions to SelectItem elements.

    Per D-03/D-04: In DatabaseBrowserPage.tsx, import useUdbSubFactions from @/hooks/useUnitDatabase. Call useUdbSubFactions(selectedFactionId) to get sub-faction list. Destructure subFactionFilter from useDatabaseBrowserFilters. Pass subFactions={subFactions} to DatabaseBrowserFilters component. Add subFactionFilter to the applyUdbFilters call: include subFactionFilter in the filters object passed to applyUdbFilters. Add subFactionFilter to the useMemo dependency array for filteredUnits.

    The subFactionFilter auto-reset on faction change is already handled in Plan 01 (setSelectedFactionId resets subFactionFilter to null).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - UdbFilterBar.tsx renders a Select with placeholder "Sub-faction" when subFactions prop has items
    - UdbFilterBar.tsx does NOT render the sub-faction Select when subFactions is empty
    - DatabaseBrowserPage.tsx calls useUdbSubFactions(selectedFactionId)
    - DatabaseBrowserPage.tsx passes subFactionFilter in applyUdbFilters call
    - subFactionFilter is in the useMemo dependency array for filteredUnits
    - pnpm build succeeds
  </acceptance_criteria>
  <done>Database browser shows conditional sub-faction dropdown. Selecting a sub-faction filters the unit list. Changing faction auto-resets the filter.</done>
</task>

<task type="auto">
  <name>Task 2: Army list unit picker sub-faction filter (SF-04)</name>
  <files>src/features/army-lists/UnitPickerDialog.tsx</files>
  <read_first>
    src/features/army-lists/UnitPickerDialog.tsx
    src/hooks/useUnitDatabase.ts
    src/hooks/useFactions.ts
    src/types/faction.ts
  </read_first>
  <action>
    Per D-06/D-07: In UnitPickerDialog.tsx, add imports for useState, Select/SelectContent/SelectItem/SelectTrigger/SelectValue from shadcn, useFactions from @/hooks/useFactions, and useUdbSubFactions plus useUdbSubFactionUnitIds from @/hooks/useUnitDatabase.

    Resolve numeric factionId to UDB string ID per RESEARCH.md Pitfall 3: const { data: collectionFactions = [] } = useFactions(). Derive udbFactionId: if factionId is not null, find the faction in collectionFactions where f.id === factionId, read its wahapedia_faction_id, otherwise null.

    Call useUdbSubFactions(udbFactionId) to get sub-faction list. Add local state: const [subFactionFilter, setSubFactionFilter] = useState<string | null>(null). Call useUdbSubFactionUnitIds(udbFactionId, subFactionFilter) to get the ID set.

    Build a Set from the unit IDs data: const subFactionIdSet = useMemo(() => new Set(subFactionUnitIds ?? []), [subFactionUnitIds]).

    Modify filteredUnits: after the existing faction filter, add sub-faction filter step. If subFactionFilter is not null and subFactionIdSet.size > 0, further filter to units where u.udb_unit_id is not null and subFactionIdSet.has(u.udb_unit_id).

    Add a sub-faction Select dropdown in the DialogContent, positioned between DialogDescription and the budget section (before the TooltipProvider). Conditionally rendered when subFactions array is non-empty. Same __clear__ sentinel pattern. Width: w-full or w-64. Placeholder: "Sub-faction".

    Reset subFactionFilter to null when dialog opens (use useEffect on open prop) or when factionId changes.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - UnitPickerDialog.tsx resolves numeric factionId to wahapedia_faction_id string via useFactions
    - Calls useUdbSubFactions with the resolved UDB faction ID
    - Sub-faction Select rendered only when subFactions has items
    - Filtering uses Set membership: u.udb_unit_id != null && subFactionIdSet.has(u.udb_unit_id)
    - subFactionFilter resets to null when factionId changes or dialog reopens
    - pnpm build succeeds
  </acceptance_criteria>
  <done>Army list unit picker shows sub-faction dropdown for factions with sub-factions. Selecting a sub-faction narrows collection units to those linked to matching UDB entries.</done>
</task>

<task type="auto">
  <name>Task 3: Collection browser sub-faction filter (SF-05)</name>
  <files>src/features/units/collectionFilters.ts, src/features/units/applyUnitFilters.ts, src/features/units/UnitFilters.tsx, src/features/units/CollectionPage.tsx</files>
  <read_first>
    src/features/units/collectionFilters.ts
    src/features/units/applyUnitFilters.ts
    src/features/units/UnitFilters.tsx
    src/features/units/CollectionPage.tsx
    src/hooks/useUnitDatabase.ts
    src/types/faction.ts
  </read_first>
  <action>
    Per D-08/D-09: Add sub-faction filtering to the collection browser, visible only when exactly one faction is selected.

    In collectionFilters.ts: Add subFactionFilter: string | null to the interface and store. Add setSubFactionFilter: (sf: string | null) => void setter. Initial value: null. Add subFactionFilter: null to clearAll reset object. Override toggleFaction to also reset subFactionFilter: null when toggling factions (since changing factions should clear the sub-faction filter).

    In UnitFilters.tsx: Accept a new prop subFactions: string[] (default []). Import Select/SelectContent/SelectItem/SelectTrigger/SelectValue from shadcn. Destructure subFactionFilter and setSubFactionFilter from useCollectionFilters. Add subFactionFilter !== null to hasAny check. Render a Select dropdown after the Category MultiSelectPopover, conditionally when subFactions.length > 0. Same __clear__ sentinel pattern as UdbFilterBar. Placeholder: "Sub-faction". Width: w-48.

    In CollectionPage.tsx: Import useFactions (already imported), useUdbSubFactions and useUdbSubFactionUnitIds from @/hooks/useUnitDatabase. Per D-09: derive the single selected faction: const singleFactionId = factionsSel.length === 1 ? factionsSel[0] : null. Resolve to UDB ID: find the faction in factions where f.id === singleFactionId, read wahapedia_faction_id, or null. Call useUdbSubFactions(udbFactionId). Destructure subFactionFilter from useCollectionFilters. Call useUdbSubFactionUnitIds(udbFactionId, subFactionFilter).

    Build subFactionIdSet from the unit IDs. After the existing applyUnitFilters call, add a second filtering step with useMemo: if subFactionFilter is not null and subFactionIdSet has items, filter filteredUnits to those where u.udb_unit_id is not null and subFactionIdSet.has(u.udb_unit_id). Name the final result displayUnits (or rename existing filteredUnits to preFilteredUnits and final to filteredUnits).

    Pass subFactions={subFactions} prop to UnitFilters component.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - collectionFilters.ts has subFactionFilter: string | null in state with setter
    - toggleFaction resets subFactionFilter to null
    - clearAll resets subFactionFilter to null
    - UnitFilters.tsx renders sub-faction Select only when subFactions prop is non-empty
    - CollectionPage.tsx calls useUdbSubFactions only when exactly 1 faction is selected (singleFactionId)
    - Collection filtering uses Set membership on udb_unit_id
    - pnpm build succeeds
    - pnpm test passes (full suite)
  </acceptance_criteria>
  <done>Collection browser shows sub-faction dropdown when exactly one faction with sub-factions is selected. Filter narrows to collection units linked to matching UDB entries. Multiple-faction selection hides the dropdown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. All filter values come from pre-populated Select options derived from local SQLite data.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-02 | Tampering | Filter values | accept | All dropdown values sourced from DB query results, not user free text |
</threat_model>

<verification>
- pnpm build succeeds (type-check across all modified files)
- pnpm test passes (full suite green)
- Database browser: selecting SM faction shows sub-faction dropdown with chapters; selecting NEC hides it
- Army list picker: sub-faction dropdown appears for SM faction lists; filters collection units by chapter
- Collection browser: sub-faction dropdown appears when exactly 1 faction with sub-factions selected; hidden otherwise
</verification>

<success_criteria>
- Sub-faction dropdown visible in database browser for factions with sub-factions (SF-03)
- Sub-faction dropdown visible in army list unit picker for applicable factions (SF-04)
- Sub-faction dropdown visible in collection browser when exactly one applicable faction selected (SF-05)
- All dropdowns use __clear__ sentinel pattern with "All sub-factions" option
- All dropdowns conditionally hidden when no sub-factions exist
- Faction changes reset sub-faction filter in all surfaces
- pnpm build and pnpm test pass
</success_criteria>

<output>
Create `.planning/phases/109-sub-faction-filter-ui/109-02-SUMMARY.md` when done
</output>

---
phase: 22-hobby-goals
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/goals/goalQueries.test.ts
  - tests/goals/goalSchema.test.ts
  - tests/goals/computeGoalPeriod.test.ts
  - tests/goals/useGoals.test.tsx
  - tests/goals/GoalSheet.test.tsx
  - tests/goals/GoalsPage.test.tsx
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02
  - ANLY-03

must_haves:
  truths:
    - "Six test stub files exist under tests/goals/ with it.skip placeholders"
    - "All stubs pass (skip is not failure) — pnpm test exits 0"
  artifacts:
    - path: "tests/goals/goalQueries.test.ts"
      provides: "ANLY-01 createGoal SQL + ANLY-02 getGoalProgress SQL stubs"
      contains: "it.skip"
    - path: "tests/goals/goalSchema.test.ts"
      provides: "ANLY-01 Zod validation stubs"
      contains: "it.skip"
    - path: "tests/goals/computeGoalPeriod.test.ts"
      provides: "ANLY-02 period boundary math + ANLY-03 deriveGoalStatus ordering stubs"
      contains: "it.skip"
    - path: "tests/goals/useGoals.test.tsx"
      provides: "ANLY-02 invalidation + hook cache behavior stubs"
      contains: "it.skip"
    - path: "tests/goals/GoalSheet.test.tsx"
      provides: "ANLY-01 form submit + edit mode stubs"
      contains: "it.skip"
    - path: "tests/goals/GoalsPage.test.tsx"
      provides: "ANLY-03 section grouping render stubs"
      contains: "it.skip"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all Phase 22 Hobby Goals test files.

Purpose: Establish the test specification contract before any production code is written. Each it.skip stub names the exact behavior the implementation plan must satisfy, serving as a failing-test target for Plans 22-01 and 22-02.
Output: 6 test files under tests/goals/ with it.skip stubs covering ANLY-01, ANLY-02, ANLY-03.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-hobby-goals/22-CONTEXT.md
@.planning/phases/22-hobby-goals/22-RESEARCH.md
@.planning/phases/22-hobby-goals/22-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 Wave 0 test stub files for Hobby Goals</name>
  <files>
    tests/goals/goalQueries.test.ts,
    tests/goals/goalSchema.test.ts,
    tests/goals/computeGoalPeriod.test.ts,
    tests/goals/useGoals.test.tsx,
    tests/goals/GoalSheet.test.tsx,
    tests/goals/GoalsPage.test.tsx
  </files>
  <read_first>
    tests/battle-log/battleLogQueries.test.ts (SQL contract test pattern — vi.mock getDb, selectMock/executeMock),
    tests/battle-log/computeBattleLogSummary.test.ts (pure function test pattern),
    .planning/phases/22-hobby-goals/22-RESEARCH.md (exact test map in Validation Architecture section),
    .planning/phases/22-hobby-goals/22-VALIDATION.md (Wave 0 gaps checklist)
  </read_first>
  <action>
Create the tests/goals/ directory and 6 test stub files. Each file uses `describe` + `it.skip` with TODO markers naming the plan that will fill each body. NO production code imports — stubs reference module paths in TODO comments only.

**File 1: tests/goals/goalQueries.test.ts**
- `describe("goals queries — getGoals (ANLY-01)")` with 1 it.skip: "calls db.select with SELECT * FROM hobby_goals ORDER BY created_at DESC"
- `describe("goals queries — createGoal (ANLY-01)")` with 1 it.skip: "INSERTs name, target_count, timeframe, period columns and returns lastInsertId"
- `describe("goals queries — updateGoal (ANLY-01)")` with 1 it.skip: "UPDATEs name, target_count, timeframe, period WHERE id = $5"
- `describe("goals queries — deleteGoal (ANLY-01)")` with 1 it.skip: "DELETEs by id"
- `describe("goals queries — getGoalProgress (ANLY-02)")` with 2 it.skip stubs:
  - "returns empty Map for empty goals array"
  - "calls db.select with COUNT(DISTINCT unit_id) filtered by startDate/endDate per goal"
- Pattern: use vi.mock("@/db/client") with selectMock/executeMock at top (same as battleLogQueries.test.ts); keep mock setup outside describe blocks. Add TODO comment: `// TODO Plan 22-01: import { getGoals, createGoal, updateGoal, deleteGoal, getGoalProgress } from "@/db/queries/goals"`

**File 2: tests/goals/goalSchema.test.ts**
- `describe("goalSchema (ANLY-01)")` with 4 it.skip stubs:
  - "accepts valid goal with name, target_count >= 1, timeframe 'month'"
  - "accepts valid goal with timeframe 'quarter'"
  - "rejects goal with empty name"
  - "rejects goal with target_count < 1"
- Add TODO comment: `// TODO Plan 22-01: import { goalSchema } from "@/features/goals/goalSchema"`

**File 3: tests/goals/computeGoalPeriod.test.ts**
- `describe("computeGoalPeriod (ANLY-02)")` with 5 it.skip stubs:
  - "returns correct startDate/endDate for month '2026-05'"
  - "returns correct startDate/endDate for quarter '2026-Q2'"
  - "returns correct endDate for Q4 (December 31)"
  - "returns label 'May 2026' for month '2026-05'"
  - "returns label 'Q2 2026' for quarter '2026-Q2'"
- `describe("currentPeriod (ANLY-01)")` with 2 it.skip stubs:
  - "returns 'YYYY-MM' format for timeframe 'month'"
  - "returns 'YYYY-QN' format for timeframe 'quarter'"
- `describe("deriveGoalStatus (ANLY-03)")` with 3 it.skip stubs:
  - "returns 'completed' when progressCount >= targetCount (even if expired)"
  - "returns 'missed' when expired and progress < target"
  - "returns 'active' when not expired and progress < target"
- Add TODO comment: `// TODO Plan 22-01: import { computeGoalPeriod, currentPeriod, deriveGoalStatus } from "@/features/goals/computeGoalPeriod"`

**File 4: tests/goals/useGoals.test.tsx**
- `describe("useGoals hook (ANLY-01)")` with 1 it.skip: "returns goals from getGoals query"
- `describe("useGoalProgress hook (ANLY-02)")` with 2 it.skip stubs:
  - "is disabled when goals are undefined"
  - "is enabled when goals is empty array"
- `describe("useCreateGoal mutation (ANLY-01)")` with 1 it.skip: "invalidates goals and goal-progress keys on success"
- `describe("useDeleteGoal mutation (ANLY-01)")` with 1 it.skip: "invalidates goals and goal-progress keys on success"
- `describe("useCreatePaintingSession invalidation (ANLY-02)")` with 1 it.skip: "invalidates goal-progress key on success"
- Add TODO comment: `// TODO Plan 22-02: import { useGoals, useGoalProgress, useCreateGoal, useDeleteGoal } from "@/hooks/useGoals"`

**File 5: tests/goals/GoalSheet.test.tsx**
- `describe("GoalSheet (ANLY-01)")` with 3 it.skip stubs:
  - "renders Name, Target, and Timeframe fields in create mode"
  - "calls useCreateGoal on submit with name, target_count, timeframe, period"
  - "pre-fills fields when editingGoal is provided"
- Add TODO comment: `// TODO Plan 22-02: import { GoalSheet } from "@/features/goals/GoalSheet"`

**File 6: tests/goals/GoalsPage.test.tsx**
- `describe("GoalsPage (ANLY-03)")` with 4 it.skip stubs:
  - "renders 'Active Goals' section for in-progress goals"
  - "renders 'Completed' section for goals with progress >= target"
  - "renders 'Missed' section for expired goals with progress < target"
  - "renders empty state when no goals exist"
- Add TODO comment: `// TODO Plan 22-02: import { GoalsPage } from "@/features/goals/GoalsPage"`

All stubs use `it.skip("description", () => {})` — empty body. Use `describe` (not `describe.skip`) so the test runner reports the skipped count.
  </action>
  <verify>
    <automated>pnpm test -- tests/goals/</automated>
  </verify>
  <acceptance_criteria>
    - tests/goals/goalQueries.test.ts exists and contains exactly 6 `it.skip` calls
    - tests/goals/goalSchema.test.ts exists and contains exactly 4 `it.skip` calls
    - tests/goals/computeGoalPeriod.test.ts exists and contains exactly 10 `it.skip` calls
    - tests/goals/useGoals.test.tsx exists and contains exactly 6 `it.skip` calls
    - tests/goals/GoalSheet.test.tsx exists and contains exactly 3 `it.skip` calls
    - tests/goals/GoalsPage.test.tsx exists and contains exactly 4 `it.skip` calls
    - Each file has a TODO comment naming the plan that will fill its stubs (Plan 22-01 or Plan 22-02)
    - `pnpm test -- tests/goals/` exits 0 (skipped tests are not failures)
    - `pnpm test` exits 0 (no regressions in existing test suite)
  </acceptance_criteria>
  <done>Six test stub files exist under tests/goals/ with 33 total it.skip stubs covering ANLY-01, ANLY-02, ANLY-03. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `pnpm test -- tests/goals/` exits 0 with 33 skipped tests
- `pnpm test` exits 0 (no regressions)
- Each file contains TODO comment referencing the plan that will activate stubs
</verification>

<success_criteria>
All 6 test files exist with it.skip stubs. Full test suite green. Wave 0 contract established for Plans 22-01 and 22-02.
</success_criteria>

<output>
After completion, create `.planning/phases/22-hobby-goals/22-00-SUMMARY.md`
</output>

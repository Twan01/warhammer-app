---
phase: 22-hobby-goals
plan: 03
type: execute
wave: 3
depends_on:
  - 22-02
files_modified: []
autonomous: false
requirements:
  - ANLY-01
  - ANLY-02
  - ANLY-03

must_haves:
  truths:
    - "User can navigate to /goals via sidebar and see the Goals page"
    - "User can create a goal with name, target, and timeframe — card appears"
    - "Goal progress bar reflects painting sessions logged within the goal's period"
    - "Completed and missed goals are visually distinguished in separate sections"
    - "Full test suite passes and build is clean"
  artifacts: []
  key_links: []
---

<objective>
Manual smoke test of the complete Hobby Goals feature in the live Tauri app.

Purpose: Verify all three ANLY requirements work end-to-end in the real application with actual SQLite persistence — catching integration issues that unit tests cannot detect (migration loading, real query execution, UI rendering, cross-query invalidation).
Output: Verified feature or list of issues to fix.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-hobby-goals/22-CONTEXT.md
@.planning/phases/22-hobby-goals/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification (build + tests)</name>
  <files></files>
  <read_first>
    src/app/router.tsx (confirm goalsRoute is registered),
    src/components/common/AppSidebar.tsx (confirm Goals entry in COMMAND_NAV),
    src-tauri/src/lib.rs (confirm migration version 9 is in vec)
  </read_first>
  <action>
Run the full automated verification suite before the manual smoke test:

1. `pnpm test` — full test suite must pass with zero failures
2. `pnpm build` — TypeScript check + Vite build must succeed
3. Verify migration version count: count `Migration { version:` entries in lib.rs — must be exactly 9
4. Verify test coverage: count `it(` calls (not `it.skip`) in tests/goals/ — must be exactly 33

Report results. If any failures, fix before proceeding to smoke test.
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0
    - `pnpm build` exits 0
    - lib.rs contains exactly 9 Migration entries for hobbyforge.db
    - tests/goals/ contains zero `it.skip` calls
  </acceptance_criteria>
  <done>Full test suite and build pass. All pre-flight checks verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test Hobby Goals in live Tauri app</name>
  <files></files>
  <action>
Present the smoke test checklist to the user and wait for approval. The feature was built in Plans 22-00 through 22-02 — this checkpoint verifies it works end-to-end in the real app.
  </action>
  <what-built>Complete Hobby Goals feature: Goals page with CRUD, progress tracking, section grouping (Active/Completed/Missed), sidebar navigation, and cross-query invalidation when painting sessions are logged.</what-built>
  <how-to-verify>
Run `pnpm tauri dev` and verify the following in the live app:

**ANLY-01 — Goal Creation:**
1. Click "Goals" in the sidebar (Target icon, under Painting Projects in COMMAND section)
2. Verify the Goals page loads with empty state ("No goals set yet" + "Set Goal" button)
3. Click "Set Goal" (or "New Goal" in the header)
4. Fill in: Name = "Paint 5 units this month", Target = 5, Timeframe = "This Month"
5. Click "Create Goal" — verify the card appears with the correct name, target "0 / 5 units", and an empty progress bar
6. Create a second goal: Name = "Quarterly painting target", Target = 10, Timeframe = "This Quarter"
7. Verify both cards appear in the "Active Goals" section

**ANLY-02 — Progress Tracking:**
8. Navigate to Collection, open any unit, go to Journal tab
9. Log a painting session for today
10. Navigate back to Goals — verify the monthly goal progress updated (should show "1 / 5 units" if this unit had no prior session this month)
11. Verify the progress bar width increased proportionally

**ANLY-03 — Section Grouping:**
12. Edit the first goal: change Target to 1 (so it becomes completed with the session just logged)
13. Verify the goal moves to the "Completed" section with battle-gold styling
14. Verify the remaining goal stays in "Active Goals"

**General:**
15. Edit a goal name — verify it updates on the card
16. Delete a goal — verify it disappears
17. Verify no console errors in DevTools
  </how-to-verify>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - User confirms ANLY-01: goal creation works (name, target, timeframe saved and displayed)
    - User confirms ANLY-02: progress bar updates after logging a painting session
    - User confirms ANLY-03: completed goals move to "Completed" section with distinct styling
    - No console errors in DevTools during smoke test
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
  <done>All three ANLY requirements verified in live Tauri app. Human approved smoke test.</done>
</task>

</tasks>

<verification>
- Full test suite green (`pnpm test` exits 0)
- Build clean (`pnpm build` exits 0)
- Manual verification of all 3 ANLY requirements in live Tauri app
</verification>

<success_criteria>
All ANLY-01, ANLY-02, ANLY-03 requirements verified in the live application. Human approves the smoke test.
</success_criteria>

<output>
After completion, create `.planning/phases/22-hobby-goals/22-03-SUMMARY.md`
</output>

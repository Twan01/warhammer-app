---
phase: 123-hobby-defaults-tab
plan: 02
type: execute
wave: 2
depends_on:
  - 123-01
files_modified:
  - src/features/dashboard/HobbyPipeline.tsx
  - src/features/battle-log/BattleLogSheet.tsx
  - src/features/game-day/gameDayStore.ts
  - src/features/game-day/GameDayPage.tsx
  - tests/settings/HobbyPipelineIntegration.test.tsx
autonomous: true
requirements:
  - HOB-01
  - HOB-02
  - HOB-03

must_haves:
  truths:
    - "Dashboard HobbyPipeline shows custom bucket labels from app_settings"
    - "New battle logs pre-fill the mission field from app_settings default_mission_format"
    - "Editing existing battle logs does NOT show the default mission format"
    - "New Game Day sessions use checklist items from app_settings when custom defaults exist"
    - "Existing Game Day sessions are NOT affected by checklist default changes"
  artifacts:
    - path: "src/features/dashboard/HobbyPipeline.tsx"
      provides: "Dynamic bucket labels via getBucketLabel"
      contains: "getBucketLabel"
    - path: "src/features/battle-log/BattleLogSheet.tsx"
      provides: "Mission pre-fill from settings on create mode"
      contains: "default_mission_format"
    - path: "src/features/game-day/gameDayStore.ts"
      provides: "Async checklist defaults from app_settings + getDefaultChecklist and setDefaultChecklist exports"
      exports: ["getDefaultChecklist", "setDefaultChecklist"]
    - path: "src/features/game-day/GameDayPage.tsx"
      provides: "Call site that invokes getDefaultChecklist + setDefaultChecklist on mount for new sessions"
      contains: "getDefaultChecklist"
    - path: "tests/settings/HobbyPipelineIntegration.test.tsx"
      provides: "Integration tests for consumer wiring"
  key_links:
    - from: "src/features/dashboard/HobbyPipeline.tsx"
      to: "src/lib/stageLabel.ts"
      via: "import getBucketLabel + BUCKET_ORDER"
      pattern: "import.*getBucketLabel.*from.*stageLabel"
    - from: "src/features/battle-log/BattleLogSheet.tsx"
      to: "src/hooks/useAppSettings.ts"
      via: "useAppSettings() for mission default"
      pattern: "useAppSettings"
    - from: "src/features/game-day/gameDayStore.ts"
      to: "src/db/queries/appSettings.ts"
      via: "getAppSetting for checklist defaults"
      pattern: "getAppSetting.*default_checklist"
    - from: "src/features/game-day/GameDayPage.tsx"
      to: "src/features/game-day/gameDayStore.ts"
      via: "imports getDefaultChecklist + useGameDayStore for setDefaultChecklist"
      pattern: "getDefaultChecklist|setDefaultChecklist"
---

<objective>
Wire the three hobby default settings into their consuming components: HobbyPipeline reads custom bucket labels, BattleLogSheet pre-fills mission from settings, and gameDayStore reads custom checklist defaults for new sessions — with GameDayPage as the call site that triggers custom checklist initialization.

Purpose: Complete the end-to-end flow so that settings saved in Plan 01's UI actually affect the app's behavior across Dashboard, Battle Logs, and Game Day features.

Output: 4 modified source files + 1 new test file

Scope note (D-05 clarification): Only `HobbyPipeline` is a bucket-label consumer. `UnitFilters`, `KanbanBoard`, and `StatusPopover` display individual `PaintingStatus` values (the 11 statuses), not the 5 bucket labels, so they do NOT need the `getBucketLabel` resolver. ROADMAP SC-1 "Collection filters and Kanban columns" refers to the bucket-level pipeline display on the Dashboard.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-hobby-defaults-tab/123-CONTEXT.md
@.planning/phases/123-hobby-defaults-tab/123-RESEARCH.md
@.planning/phases/123-hobby-defaults-tab/123-PATTERNS.md
@.planning/phases/123-hobby-defaults-tab/123-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/stageLabel.ts (created in Plan 01):
export type PipelineBucket = "Not Started" | "Assembly" | "Painting" | "Finishing" | "Done";
export const BUCKET_ORDER: PipelineBucket[];
export function getBucketLabel(bucket: PipelineBucket, settings: AppSettingsMap): string;

From src/hooks/useAppSettings.ts:
export function useAppSettings(): UseQueryResult<AppSettingsMap>;

From src/db/queries/appSettings.ts:
export type AppSettingsMap = Record<string, string>;
export async function getAppSetting(key: string): Promise<string | null>;

From src/features/game-day/gameDayStore.ts:
export const DEFAULT_CHECKLIST: ChecklistItem[];
export interface ChecklistItem { id: string; text: string; checked: boolean; }
function getListState(state: GameDayStore, listId: number): GameDayListState;
function createDefaultState(): GameDayListState;
export function useGameDayListState(listId: number): GameDayListState;

From src/features/game-day/GameDayPage.tsx:
import { useGameDayStore, useGameDayListState } from "./gameDayStore";  // (via ChecklistTab)
// GameDayPage receives listId as prop, renders ChecklistTab which calls useGameDayListState(listId)
// Currently no useEffect for checklist initialization — relies on synchronous createDefaultState()

From src/features/battle-log/BattleLogSheet.tsx:
function buildDefaultValues(log, prefill?): BattleLogFormValues;
// useEffect resets form when log/prefill change (line 142-144)
// DEFAULT_VALUES.mission = "" (line 58)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire HobbyPipeline to use custom bucket labels</name>
  <files>
    src/features/dashboard/HobbyPipeline.tsx
  </files>
  <read_first>
    src/features/dashboard/HobbyPipeline.tsx,
    src/lib/stageLabel.ts
  </read_first>
  <action>
    Per D-05: Modify `HobbyPipeline.tsx` to display custom bucket labels from app_settings. This is the ONLY bucket-label consumer — UnitFilters, KanbanBoard, and StatusPopover display individual statuses and do not need the label resolver (per D-05 scope clarification).

    **Imports to add:** `import { useAppSettings } from "@/hooks/useAppSettings"` and `import { getBucketLabel, BUCKET_ORDER, type PipelineBucket } from "@/lib/stageLabel"`.

    **Imports to remove:** Delete the local `type Bucket` declaration (line 16) and the local `BUCKET_ORDER` constant (line 18). Replace all references to the local `Bucket` type with `PipelineBucket` from stageLabel. The `BUCKET_GROUPS` and `BUCKET_BUBBLE_CLASS` records should use `PipelineBucket` as their key type instead of `Bucket`.

    **Component body:** Add `const { data: settings = {} } = useAppSettings();` at the top of the `HobbyPipeline` function body.

    **Label render:** Change line 68 from `{bucket}` to `{getBucketLabel(bucket, settings)}`. Also update the `aria-label` on the `<li>` element (line 65) to use the resolved label: `` `${getBucketLabel(bucket, settings)}: ${bucketCounts[bucket]} units` ``.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - HobbyPipeline.tsx imports `getBucketLabel` and `BUCKET_ORDER` from `@/lib/stageLabel`
    - HobbyPipeline.tsx imports `useAppSettings` from `@/hooks/useAppSettings`
    - No local `type Bucket` or local `BUCKET_ORDER` declaration remains in HobbyPipeline.tsx
    - `BUCKET_GROUPS` and `BUCKET_BUBBLE_CLASS` use `PipelineBucket` as key type
    - The JSX renders `getBucketLabel(bucket, settings)` instead of raw `{bucket}`
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>HobbyPipeline renders dynamic bucket labels from app_settings, with fallback to default names when no custom labels are set</done>
</task>

<task type="auto">
  <name>Task 2: Wire BattleLogSheet mission pre-fill, gameDayStore checklist defaults, and GameDayPage call site</name>
  <files>
    src/features/battle-log/BattleLogSheet.tsx,
    src/features/game-day/gameDayStore.ts,
    src/features/game-day/GameDayPage.tsx,
    tests/settings/HobbyPipelineIntegration.test.tsx
  </files>
  <read_first>
    src/features/battle-log/BattleLogSheet.tsx,
    src/features/game-day/gameDayStore.ts,
    src/features/game-day/GameDayPage.tsx,
    src/features/game-day/ChecklistTab.tsx,
    src/db/queries/appSettings.ts,
    src/hooks/useAppSettings.ts,
    tests/settings/SettingsPage.test.tsx
  </read_first>
  <action>
    **src/features/battle-log/BattleLogSheet.tsx** — per D-11: Add `import { useAppSettings } from "@/hooks/useAppSettings"`. Inside the `BattleLogSheet` function body, add `const { data: settings } = useAppSettings();`. Compute `const missionDefault = (!log && settings?.["default_mission_format"]) ? settings["default_mission_format"] : "";`. Modify the `useEffect` (line 142-144) to include `missionDefault` in deps and pass it as a prefill merge: `form.reset(buildDefaultValues(log, { ...prefill, mission: missionDefault || prefill?.mission || "" }))`. The `!log` guard in `missionDefault` computation ensures the setting only applies on create (D-11 / Pitfall 5). When editing (`log !== null`), `missionDefault` is `""` and `buildDefaultValues` uses the log's actual mission value. Add `missionDefault` to the useEffect dep array.

    **src/features/game-day/gameDayStore.ts** — per D-09: Add `import { getAppSetting } from "@/db/queries/appSettings"`. Export a new async function `getDefaultChecklist(): Promise<ChecklistItem[]>` that: calls `await getAppSetting("default_checklist")`, if null returns `DEFAULT_CHECKLIST.map(item => ({ ...item }))`, otherwise parses JSON as `Array<{ text: string }>` inside try-catch, maps each entry to `{ id: crypto.randomUUID(), text: e.text, checked: false }`, and on parse error falls back to `DEFAULT_CHECKLIST.map(item => ({ ...item }))`. IDs are generated fresh each call (D-06). The existing `createDefaultState()` and `getListState()` functions remain unchanged — they still use the hardcoded `DEFAULT_CHECKLIST` as the synchronous fallback. Add a `setDefaultChecklist` action to the Zustand store: `setDefaultChecklist: (listId: number, items: ChecklistItem[]) => void` that sets `listStates[String(listId)]` to `{ ...createDefaultState(), checklistItems: items }` ONLY if `listStates[String(listId)]` is undefined (never overwrite existing sessions per Pitfall 3). Add this to the `GameDayStore` interface and the store implementation.

    **src/features/game-day/GameDayPage.tsx** — per HOB-02 SC-2 ("new Game Day sessions start with the customized checklist"): This is the call site that wires the async checklist defaults into the Zustand store. Add `import { useEffect } from "react"` (already imported as `useMemo, useState` — add `useEffect` to the destructure). Import `getDefaultChecklist` from `./gameDayStore`. Import `useGameDayStore` from `./gameDayStore` (if not already imported). Add a `useEffect` after the existing hooks (around line 30) that runs on mount with `listId` as dependency:

    ```
    useEffect(() => {
      const existing = useGameDayStore.getState().listStates[String(listId)];
      if (existing) return; // existing session — do not overwrite
      getDefaultChecklist().then((items) => {
        useGameDayStore.getState().setDefaultChecklist(listId, items);
      });
    }, [listId]);
    ```

    Key design: Use `useGameDayStore.getState()` (Zustand vanilla access) rather than a hook selector to avoid re-renders. The `setDefaultChecklist` action itself has the guard (`if listStates[key] exists, no-op`), so the double-check in the effect is a performance optimization to skip the async call entirely when a session already exists. This ensures new Game Day sessions get custom checklist items from app_settings, while existing sessions remain untouched.

    **tests/settings/HobbyPipelineIntegration.test.tsx** — Create integration tests for the consumer wiring. Mock `@/hooks/useAppSettings` at module level. Test cases: (1) `getBucketLabel` returns custom label when settings has override — import from `@/lib/stageLabel` directly, (2) `getDefaultChecklist` returns DEFAULT_CHECKLIST items when no setting exists — mock `@/db/queries/appSettings` `getAppSetting` to return null, verify returned items match DEFAULT_CHECKLIST texts, (3) `getDefaultChecklist` returns custom items when setting exists — mock `getAppSetting` to return `JSON.stringify([{text:"Custom item 1"},{text:"Custom item 2"}])`, verify returned items have those texts and unique IDs and `checked: false`, (4) `getDefaultChecklist` falls back to DEFAULT_CHECKLIST on malformed JSON — mock `getAppSetting` to return `"NOT JSON"`, verify fallback. (5) `setDefaultChecklist` does not overwrite existing session — create a store state with a pre-existing listId entry, call `setDefaultChecklist`, verify the existing state is unchanged.
  </action>
  <verify>
    <automated>pnpm test -- tests/settings/HobbyPipelineIntegration.test.tsx && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - BattleLogSheet.tsx imports `useAppSettings` and reads `settings["default_mission_format"]`
    - BattleLogSheet.tsx only applies mission default when `log === null` (create mode)
    - BattleLogSheet.tsx useEffect dep array includes `missionDefault`
    - gameDayStore.ts exports `getDefaultChecklist` async function
    - gameDayStore.ts exports `setDefaultChecklist` Zustand action in the store interface
    - `setDefaultChecklist` does NOT overwrite existing `listStates[key]`
    - `getDefaultChecklist` generates new UUIDs via `crypto.randomUUID()` on each call
    - `getDefaultChecklist` wraps JSON.parse in try-catch with DEFAULT_CHECKLIST fallback
    - GameDayPage.tsx imports `getDefaultChecklist` from `./gameDayStore`
    - GameDayPage.tsx has a useEffect that calls `getDefaultChecklist()` then `setDefaultChecklist(listId, items)` when `listStates[listId]` is undefined
    - GameDayPage.tsx useEffect skips the async call when session already exists
    - tests/settings/HobbyPipelineIntegration.test.tsx has at least 5 test cases
    - `pnpm test -- tests/settings/HobbyPipelineIntegration.test.tsx` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>HobbyPipeline shows custom labels, BattleLogSheet pre-fills mission on create, gameDayStore provides async checklist defaults, GameDayPage wires the call site so new sessions start with custom checklist items, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app_settings JSON -> component display | JSON from SQLite parsed and rendered in HobbyPipeline labels |
| app_settings JSON -> gameDayStore state | JSON parsed to create ChecklistItem array |
| app_settings string -> form default | Raw string used as form field initial value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-04 | Tampering | pipeline_labels JSON in HobbyPipeline | mitigate | getBucketLabel (from Plan 01) wraps JSON.parse in try-catch; malformed values fall back to default bucket names |
| T-123-05 | Tampering | default_checklist JSON in getDefaultChecklist | mitigate | JSON.parse wrapped in try-catch; malformed values fall back to DEFAULT_CHECKLIST |
| T-123-06 | Information Disclosure | default_mission_format string | accept | Free-text string rendered as form input value; no sensitive data; React auto-escapes |
| T-123-07 | Tampering | npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `pnpm build` — TypeScript compilation succeeds
2. `pnpm test -- tests/settings/` — All settings tests pass (including Plan 01 tests)
3. `pnpm test` — Full test suite passes
4. Manual: Dashboard pipeline shows custom labels after saving in Settings
5. Manual: New battle log form has mission pre-filled from Settings value
6. Manual: Editing existing battle log does NOT show settings default in mission field
7. Manual: Navigate to Game Day for a new list — checklist shows custom defaults from Settings (not hardcoded defaults)
8. Manual: Navigate to Game Day for an existing session — checklist retains session-specific items (not overwritten by settings)
</verification>

<success_criteria>
- HobbyPipeline bucket labels update dynamically when pipeline_labels setting changes
- BattleLogSheet pre-fills mission from default_mission_format on create, not on edit
- gameDayStore exports getDefaultChecklist that reads from app_settings with fallback to DEFAULT_CHECKLIST
- gameDayStore has setDefaultChecklist action that initializes new sessions only
- GameDayPage calls getDefaultChecklist on mount and wires it into the store for new sessions
- Only HobbyPipeline consumes bucket labels (per D-05 — UnitFilters/KanbanBoard/StatusPopover show individual statuses, not buckets)
- All consumer integrations are tested
- Full test suite and build pass
</success_criteria>

<output>
Create `.planning/phases/123-hobby-defaults-tab/123-02-SUMMARY.md` when done
</output>

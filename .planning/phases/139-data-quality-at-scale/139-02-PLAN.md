---
phase: 139-data-quality-at-scale
plan: 02
type: execute
wave: 2
depends_on: ["139-01"]
files_modified:
  - scripts/audit-faction.ts
  - package.json
  - tests/data-layer/reimport-preservation.test.ts
  - .planning/phases/139-data-quality-at-scale/reports/
autonomous: true
requirements: [DAT-02, DAT-03]

must_haves:
  truths:
    - "`scripts/audit-faction.ts` accepts any of the 25 known faction IDs and an `--all` batch flag"
    - "`pnpm audit:all` writes one {faction}-audit.md and one {faction}-audit.json per faction under .planning/phases/139-data-quality-at-scale/reports/"
    - "All 25 audit reports exist under the Phase 139 reports directory (not Phase 113's)"
    - "The current systematic_issues status for a representative faction (SM) is captured (resolving RESEARCH Open Question #1: weapon.range / weapon.keywords)"
    - "`tests/data-layer/reimport-preservation.test.ts` proves unit_overrides + rules_favorites + rules_notes survive a simulated DELETE-all+re-INSERT of udb_* tables"
  artifacts:
    - path: "scripts/audit-faction.ts"
      provides: "Batch --all mode, all-25-faction whitelist replacement, Phase 139 reports dir"
      contains: "--all"
    - path: "package.json"
      provides: "audit:all script"
      contains: "audit:all"
    - path: "tests/data-layer/reimport-preservation.test.ts"
      provides: "Re-import survival assertions for user override/favorite/note tables"
      contains: "rules_favorites"
    - path: ".planning/phases/139-data-quality-at-scale/reports/"
      provides: "25 per-faction audit reports (.md + .json)"
  key_links:
    - from: "package.json"
      to: "scripts/audit-faction.ts"
      via: "audit:all script invokes --all"
      pattern: "audit-faction.ts --all"
    - from: "tests/data-layer/reimport-preservation.test.ts"
      to: "tests/data-layer/db-helpers.ts"
      via: "createHobbyforgeDb()"
      pattern: "createHobbyforgeDb"
---

<objective>
Build the DAT-02 audit tooling and the DAT-03 re-import preservation guarantee, then produce the
full 25-faction audit baseline that the per-faction correction+translation plans (03, 04) consume.

This plan:
- Generalizes the existing `scripts/audit-faction.ts` (built for SM/NEC/DG in Phase 113) to all 25
  factions, adds `--all` batch mode, removes the SM/NEC/DG whitelist, and repoints the hardcoded
  Phase-113 reports directory to this phase's reports dir (D-03).
- Adds `pnpm audit:all` and runs it to generate one {faction}-audit.{md,json} for all 25 factions —
  the evidence base for "audited" (D-03 definition) and the triage input for Plans 03/04.
- Resolves RESEARCH Open Question #1: rebuild the artifact and run the SM audit to record whether the
  weapon.range / weapon.keywords systematic bugs from Phase 113 are already fixed.
- Adds the D-07 re-import preservation test, turning "overrides/favorites/notes survive re-import"
  from an assertion into an enforced guarantee.

Purpose: Establish honest, reproducible audit coverage across all factions and lock the user-data
preservation guarantee before any correction/FR churn begins.
Output: an extended `audit-faction.ts`, the `audit:all` script, 25 reports, and
`tests/data-layer/reimport-preservation.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-data-quality-at-scale/139-CONTEXT.md
@.planning/phases/139-data-quality-at-scale/139-RESEARCH.md
@.planning/phases/139-data-quality-at-scale/139-PATTERNS.md

<interfaces>
<!-- Use these directly — no codebase exploration needed. -->

scripts/audit-faction.ts (current shape, verified):
  - main() at lines 202-216: reads process.argv[2].toUpperCase()
  - whitelist guard line 204: `if (!factionId || !["SM","NEC","DG"].includes(factionId))`
  - FACTION_NAMES map lines 192-196: SM/NEC/DG only
  - REPORTS_DIR line 216: hardcoded to .planning/phases/113-priority-faction-data-audit/reports
  - report output (~lines 556-564): writes `${factionId.toLowerCase()}-audit.json` and `-audit.md`
  - comparison logic already implemented: models/weapons/abilities/keywords field diff + systematic-issue detection

All 25 faction IDs (from unit_database.json):
  AC, AdM, AE, AM, AoI, AS, CD, CSM, DG, DRU, EC, GC, GK, LoV, NEC, ORK, QI, QT, SM, TAU, TL, TS, TYR, UN, WE

FACTION_NAMES full map (from 139-PATTERNS.md):
  SM=Space Marines, NEC=Necrons, DG=Death Guard, AC=Adeptus Custodes, AdM=Adeptus Mechanicus,
  AE=Aeldari, AM=Astra Militarum, AoI=Agents of the Imperium, AS=Adepta Sororitas,
  CD=Chaos Daemons, CSM=Chaos Space Marines, DRU=Drukhari, EC=Emperor's Children,
  GC=Genestealer Cults, GK=Grey Knights, LoV=Leagues of Votann, ORK=Orks, QI=Imperial Knights,
  QT=Chaos Knights, TAU=T'au Empire, TL=The Legion of the Damned, TS=Thousand Sons,
  TYR=Tyranids, UN=Unaligned, WE=World Eaters

User-data tables NOT touched by re-import (migrations 017, 019, 039):
  unit_overrides (keyed on units.id INTEGER), rules_favorites (rule_id TEXT + rule_type),
  rules_notes (same keying), units.udb_unit_id (nullable FK ON DELETE SET NULL)

udb_* DELETE-all+INSERT order (src-tauri/src/lib.rs lines 769-790):
  delete leader_targets, unit_keywords, unit_points, unit_composition, unit_abilities,
  unit_weapons, unit_models, units, factions (+ stratagems/enhancements/detachment* tables)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize audit-faction.ts to all 25 factions + --all batch mode + Phase 139 reports dir</name>
  <files>scripts/audit-faction.ts, package.json</files>
  <read_first>
    - scripts/audit-faction.ts (FULL FILE — current CLI main() lines 202-216, whitelist guard line 204, FACTION_NAMES lines 192-196, REPORTS_DIR line 216, report-write block ~556-564, the per-faction comparison/systematic-issue logic; understand current state before editing)
    - scripts/lib/types.ts (UnitDatabaseJson.factions shape — used to derive known faction ids)
    - package.json lines 15-16 (build:udb / download:wahapedia script entries — the analog for the new audit:all entry)
    - 139-PATTERNS.md "scripts/audit-faction.ts — batch mode + path fix (DAT-02)" section (exact before/after edits) and "package.json — audit:all script" section
    - 139-RESEARCH.md Pitfall 1 (hardcoded Phase 113 path) and Pitfall 2 (whitelist guard)
  </read_first>
  <action>
    Edit `scripts/audit-faction.ts` (D-03):
    (1) Replace the SM/NEC/DG whitelist guard (line 204) with a check against the actual faction IDs
        read from unit_database.json: load the artifact once, build a Set of `factions.map(f => f.id)`,
        and reject only IDs not in that set; the usage message should list known IDs and mention `--all`.
    (2) Expand FACTION_NAMES (lines 192-196) to all 25 entries per the interfaces map above (or derive
        the display name from the artifact's faction `name` field — simpler and drift-free; planner's
        discretion, but cover all 25).
    (3) Add `--all` batch mode in main(): when argv[2] (uppercased) is `--ALL`, iterate every faction id
        from the artifact and run the existing per-faction audit logic in a loop; otherwise validate a
        single faction id and run it once. Refactor the per-faction body into a callable function if it
        is not already (keep the comparison/systematic-issue logic untouched — do not re-author it, D-03).
    (4) Repoint REPORTS_DIR (line 216) from the Phase-113 path to
        `.planning/phases/139-data-quality-at-scale/reports`; create the directory if missing
        (mkdirSync recursive). Optionally support a `--output-dir=` flag (discretion), but the DEFAULT
        must be the Phase 139 reports dir.
    Then add a `"audit:all": "node --experimental-strip-types scripts/audit-faction.ts --all"` script to
    package.json immediately after the `download:wahapedia` entry.
    Do NOT change the field-diff comparison logic. Do NOT add a migration. Do NOT hand-edit unit_database.json.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/audit-faction.ts AC >/dev/null 2>&1 && grep -q '"audit:all"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/audit-faction.ts` no longer contains the `["SM", "NEC", "DG"]` whitelist literal (grep returns nothing).
    - Running `node --experimental-strip-types scripts/audit-faction.ts AC` exits 0 and writes ac-audit.md + ac-audit.json under `.planning/phases/139-data-quality-at-scale/reports/`.
    - `scripts/audit-faction.ts` REPORTS_DIR resolves to the Phase 139 reports dir, NOT `113-priority-faction-data-audit`.
    - `package.json` contains an `audit:all` script invoking `scripts/audit-faction.ts --all`.
    - No file is written under `.planning/phases/113-priority-faction-data-audit/reports/` by the new run.
  </acceptance_criteria>
  <done>The audit harness runs for any of the 25 factions and in `--all` batch mode, writing reports to the Phase 139 directory; `pnpm audit:all` is wired.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild artifact, run pnpm audit:all (25 reports), resolve Open Question #1</name>
  <files>.planning/phases/139-data-quality-at-scale/reports/</files>
  <read_first>
    - 139-RESEARCH.md "SM/NEC/DG Audit Findings — Dominant Patterns" (weapon.range / weapon.keywords systematic bugs) and Open Question #1 + Pitfall 6 (always rebuild before auditing) + Assumptions Log A1/A3
    - scripts/lib/weaponMapping.ts (check whether weapon.range uses row["range"] vs row["Range"] and whether keywords reads description — to confirm if the Phase-113 systematic bugs are already fixed)
    - .planning/milestones/v0.4.5-phases/113-priority-faction-data-audit/reports/sm-audit.md (the report format + the systematic_issues section to compare against)
  </read_first>
  <action>
    Run `pnpm build:udb` first (Pitfall 6 — audit must run against the freshest artifact; the Plan-01
    referential gate now guards this build). Then run `pnpm audit:all` to generate all 25
    {faction}-audit.{md,json} reports under the Phase 139 reports directory.
    Resolve RESEARCH Open Question #1: inspect the freshly generated `sm-audit.md` (and `nec-audit.md`,
    `dg-audit.md`) systematic_issues sections and confirm against `scripts/lib/weaponMapping.ts` whether
    the weapon.range (row["Range"] vs row["range"]) and weapon.keywords (reads description) bugs are
    already fixed. Record the finding in the SUMMARY: if systematic_issues is empty for the
    already-audited trio, the 22 remaining factions are expected to show only per-unit residual errors
    and source-limited unmatch (informing Plans 03/04 effort). If systematic bugs persist, note them as
    a pipeline-fix prerequisite for Plans 03/04.
    Do NOT fix per-faction errors in this plan — that triage happens in Plans 03/04. This task produces
    the baseline reports and the systematic-bug status only.
  </action>
  <verify>
    <automated>pnpm build:udb >/dev/null 2>&1 && pnpm audit:all >/dev/null 2>&1 && [ "$(ls .planning/phases/139-data-quality-at-scale/reports/*-audit.md 2>/dev/null | wc -l)" -ge 25 ]</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/139-data-quality-at-scale/reports/*-audit.md | wc -l` is ≥ 25.
    - `ls .planning/phases/139-data-quality-at-scale/reports/*-audit.json | wc -l` is ≥ 25.
    - `pnpm build:udb` exits 0 (referential gate from Plan 01 passes on the rebuilt artifact).
    - The SUMMARY records the systematic_issues status for SM/NEC/DG (Open Question #1 resolved: bugs already fixed, or listed as a prerequisite).
    - No edits to unit_database.json by hand; it is only regenerated via `pnpm build:udb`.
  </acceptance_criteria>
  <done>All 25 factions have a baseline audit report under the Phase 139 reports dir, and the weapon.range / weapon.keywords systematic-bug status is documented to inform per-faction correction effort.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write reimport-preservation.test.ts (DAT-03 D-07)</name>
  <files>tests/data-layer/reimport-preservation.test.ts</files>
  <read_first>
    - tests/data-layer/recipe-persistence.test.ts (the exact analog: createHobbyforgeDb per test, seed → mutate → assert, beforeEach/afterEach DB lifecycle)
    - tests/data-layer/leader-targets.test.ts (FK OFF during seed / ON after, udb_* insert patterns)
    - tests/data-layer/db-helpers.ts (createHobbyforgeDb())
    - src-tauri/migrations/017_unit_overrides.sql, src-tauri/migrations/019_rules_favorites_notes.sql, src-tauri/migrations/039 (units.udb_unit_id FK ON DELETE SET NULL) — exact column names/constraints for rule_id, rule_type, unit_overrides keying
    - src-tauri/src/lib.rs lines 769-790 (the udb_* DELETE order to replicate)
    - 139-RESEARCH.md "D-07: Re-Import Preservation Test" + Pitfall 5 (rule_id must match the Wahapedia-ID format the re-import re-creates)
    - 139-PATTERNS.md "tests/data-layer/reimport-preservation.test.ts" section (seed → delete/re-insert → assert pattern)
  </read_first>
  <action>
    Create `tests/data-layer/reimport-preservation.test.ts` with `// @vitest-environment node`, building a
    fresh in-memory DB via `createHobbyforgeDb()` per test (D-07). For each assertion:
    (1) Seed the udb_* rows needed for FK references (FK OFF), inserting a faction + a unit with a stable
        Wahapedia-style id (e.g. "000000001"); insert child rows only as needed.
    (2) Seed user data in the separate tables: `unit_overrides` (keyed on a units.id INTEGER row you also
        seed in the `units` collection table), `rules_favorites` (rule_id = the Wahapedia id, rule_type per
        migration 019 — match the format the app/import re-creates per Pitfall 5), and `rules_notes`.
    (3) Simulate the re-import: FK OFF, DELETE all udb_* tables in the lib.rs order, then re-INSERT the udb_*
        rows with the SAME Wahapedia ids, FK ON.
    (4) Assert `unit_overrides`, `rules_favorites`, and `rules_notes` rows ALL still exist (the user tables
        are never in the udb_* DELETE list).
    (5) Assert `units.udb_unit_id` still resolves (non-null) after re-import because the udb_units row is
        re-inserted with the same id (ON DELETE SET NULL would null it only if the id disappeared).
    Optionally add an idempotency assertion: running the delete+re-insert a second time leaves user rows
    unchanged (D-07 "safe on second run"). Close the DB in afterEach.
    Do NOT call the Rust importer (native/async) — replicate the DELETE/INSERT directly in better-sqlite3.
  </action>
  <verify>
    <automated>pnpm test -- tests/data-layer/reimport-preservation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/data-layer/reimport-preservation.test.ts` exists and `pnpm test -- tests/data-layer/reimport-preservation.test.ts` exits 0.
    - The test seeds AND re-asserts survival of all three user tables: `unit_overrides`, `rules_favorites`, `rules_notes` (grep finds all three).
    - The test asserts `units.udb_unit_id` resolves to a non-null udb_units row after re-import.
    - The re-import simulation DELETEs udb_* tables in the lib.rs order and re-INSERTs with the same Wahapedia ids.
    - `pnpm test -- tests/data-layer/` exits 0 (no regressions; includes Plan 01's fk-integrity test).
  </acceptance_criteria>
  <done>A data-layer test proves user overrides, favorites, and notes survive a simulated rebuild + re-import, and that the operation is safe on a second run — the DAT-03 preservation guarantee is enforced, not just asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendored Wahapedia CSV → audit/build pipeline | Static vendored inputs; no live input, no end-user data crosses here. |
| Generated artifact → bundled app | unit_database.json regenerated by `pnpm build:udb`, shipped read-only. |
| udb_* re-import vs. user tables | Re-import is DELETE-all+INSERT on udb_* only; user tables are structurally isolated. |

## STRIDE Threat Register

NOTE (per RESEARCH.md §Security Domain): this phase has NO app-runtime surface, no auth, and no
user-facing input. The audit script and re-import are dev-side / data-layer operations over static
inputs. No injection path exists. No high-severity threats apply. The register documents the
assessment honestly.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-03 | Tampering | re-import clobbering user data | mitigate | reimport-preservation.test.ts proves unit_overrides/rules_favorites/rules_notes survive DELETE-all+INSERT (PITFALLS §15). |
| T-139-04 | Repudiation | audit report accuracy | accept | Reports are dev-side evidence files; no integrity-critical trust placed on them at runtime. |
| T-139-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed (RESEARCH §Package Legitimacy Audit). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `node --experimental-strip-types scripts/audit-faction.ts AC` exits 0 and writes to the Phase 139 reports dir.
- `pnpm build:udb` exits 0; `pnpm audit:all` exits 0; ≥25 .md and ≥25 .json reports present.
- `pnpm test -- tests/data-layer/` exits 0 (fk-integrity + reimport-preservation + existing tests).
- No new migration file (`git status --short src-tauri/migrations/` empty).
- No files written under the Phase 113 reports directory.
</verification>

<success_criteria>
DAT-02 tooling is generalized to all 25 factions with a reproducible `pnpm audit:all`, all 25
baseline reports exist under the Phase 139 reports dir, the weapon.range/keywords systematic-bug
status is resolved, and the DAT-03 re-import preservation guarantee is locked by a passing test.
</success_criteria>

<output>
Create `.planning/phases/139-data-quality-at-scale/139-02-SUMMARY.md` when done.
</output>

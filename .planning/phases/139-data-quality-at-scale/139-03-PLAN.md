---
phase: 139-data-quality-at-scale
plan: 03
type: execute
wave: 3
depends_on: ["139-01", "139-02"]
files_modified:
  - scripts/lib/weaponMapping.ts
  - scripts/lib/parseCsv.ts
  - scripts/build-unit-db.ts
  - scripts/data/translations_fr.json
  - src-tauri/data/unit_database.json
  - .planning/phases/139-data-quality-at-scale/reports/
autonomous: true
requirements: [DAT-02, DAT-03]

must_haves:
  truths:
    - "For each Batch-1 faction, the audit report's systematic (non-source-limited) discrepancies are driven to zero via PIPELINE fixes (never hand-edits to unit_database.json)"
    - "translations_fr.json gains FR ability name_fr/description_fr and weapon name_fr entries for the Batch-1 factions, keyed on composite Wahapedia keys"
    - "`pnpm build:udb` exits 0 and logs non-zero French overlay ability/weapon counts after the batch-1 FR additions"
    - "`pnpm audit:all` reruns clean (no systematic regressions) and the Plan-01 referential gate stays green"
  artifacts:
    - path: "scripts/data/translations_fr.json"
      provides: "FR entries (abilities composite-key objects, weapons composite-key strings) for batch-1 factions"
      contains: "abilities"
    - path: ".planning/phases/139-data-quality-at-scale/reports/"
      provides: "Updated batch-1 audit reports with systematic discrepancies at zero"
  key_links:
    - from: "scripts/data/translations_fr.json"
      to: "scripts/build-unit-db.ts"
      via: "step 10.5 overlay merge keyed on ${unit_id}:${name}"
      pattern: "abilities|weapons"
    - from: "scripts/lib/weaponMapping.ts"
      to: "src-tauri/data/unit_database.json"
      via: "pnpm build:udb regenerates artifact from fixed pipeline"
      pattern: "build:udb"
---

<objective>
Batch 1 of the per-faction audit-correction + French-extension work (D-03, D-04, D-05, D-06, D-08).

Process this batch of factions END-TO-END (audit → triage → pipeline fix → rebuild → re-audit →
FR content → rebuild), per D-08's "audit + translate together per faction group" to minimize
re-import churn. Corrections fix the PIPELINE (scripts/lib/, build-unit-db.ts) so one fix corrects
many units; unit_database.json is NEVER hand-edited (D-04). French content extends
translations_fr.json keyed on composite Wahapedia keys (D-05); no schema change, no
machine-translation runtime dependency (D-06).

Batch 1 factions (Xenos + Chaos): TYR, ORK, TAU, AE, DRU, CD, WE, EC, CSM, TS, GC, QI, QT.

Purpose: Drive systematic data discrepancies to zero and add FR ability/weapon strings for this
faction group, with the referential + preservation gates enforcing correctness throughout.
Output: pipeline fixes in scripts/lib/, FR entries in translations_fr.json, a rebuilt
unit_database.json, and updated batch-1 audit reports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-data-quality-at-scale/139-CONTEXT.md
@.planning/phases/139-data-quality-at-scale/139-RESEARCH.md
@.planning/phases/139-data-quality-at-scale/139-PATTERNS.md
@.planning/phases/139-data-quality-at-scale/139-02-SUMMARY.md

<interfaces>
<!-- Use these directly. -->

translations_fr.json sections + key formats (139-PATTERNS.md):
  units:     { "<wahapedia_unit_id>": "<name_fr>" }
  abilities: { "<unit_id>:<ability_name>": { "name_fr": "...", "description_fr": "..."|null } }
  weapons:   { "<unit_id>:<weapon_name>": "<name_fr>" }
  keywords:  { "<keyword>": "<keyword_fr>" }

TranslationsFrOverlay (scripts/lib/types.ts lines 156-162):
  factions?, units?, abilities?(name_fr?/description_fr?), weapons?, keywords?  — all optional records

build-unit-db.ts step 10.5 (lines 641-689) merges on:
  abilities key = `${a.unit_id}:${a.name}`   weapons key = `${w.unit_id}:${w.name}`
  Logs: "French overlay: N factions, N units, N abilities, N weapons, N keywords translated"

Systematic-fix landing zones (D-04):
  scripts/lib/weaponMapping.ts  (weapon field mapping — e.g. range/keywords column-name bugs)
  scripts/lib/parseCsv.ts       (CSV header parsing)
  scripts/build-unit-db.ts      (row construction)

Audit command (from Plan 02): `pnpm audit:all` → reports under
  .planning/phases/139-data-quality-at-scale/reports/{faction}-audit.{md,json}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage batch-1 audit findings and fix systematic discrepancies in the pipeline</name>
  <files>scripts/lib/weaponMapping.ts, scripts/lib/parseCsv.ts, scripts/build-unit-db.ts, src-tauri/data/unit_database.json, .planning/phases/139-data-quality-at-scale/reports/</files>
  <read_first>
    - .planning/phases/139-data-quality-at-scale/reports/ — the batch-1 reports from Plan 02: tyr-audit.md, ork-audit.md, tau-audit.md, ae-audit.md, dru-audit.md, cd-audit.md, we-audit.md, ec-audit.md, csm-audit.md, ts-audit.md, gc-audit.md, qi-audit.md, qt-audit.md (read the systematic_issues + error sections)
    - .planning/phases/139-data-quality-at-scale/139-02-SUMMARY.md (Open Question #1 resolution — whether weapon.range/keywords are already fixed)
    - scripts/lib/weaponMapping.ts (FULL — the weapon field mapping; locate any row["Range"] vs row["range"] / keywords-vs-description mismatch)
    - scripts/lib/parseCsv.ts (CSV header parsing helpers)
    - scripts/build-unit-db.ts lines 250-400 (models/weapons/leader/points/composition row construction)
    - 139-CONTEXT.md D-04 (triage rule: systematic bug → fix in scripts/lib/, one fix corrects all; genuine source gap → document, accept) and 139-RESEARCH.md "SM/NEC/DG Audit Findings" + Pitfall 6 (rebuild before re-audit) + Pitfall 7 (keep Wahapedia IDs stable — never change unit IDs)
  </read_first>
  <action>
    For the 13 batch-1 factions (TYR, ORK, TAU, AE, DRU, CD, WE, EC, CSM, TS, GC, QI, QT): read each
    faction's audit report and triage findings per D-04:
    - SYSTEMATIC parsing/mapping bugs (a field wrong across many units, e.g. a CSV column-name mismatch):
      fix in `scripts/lib/weaponMapping.ts` / `scripts/lib/parseCsv.ts` / `scripts/build-unit-db.ts` so the
      single fix corrects every affected unit across ALL factions. Never hand-edit unit_database.json (D-04).
    - GENUINE source gaps / Wahapedia-side quirks (the CSV itself lacks/contradicts the data): document in
      the report and accept — the audit's job is honesty, not inventing data.
    After each pipeline fix, run `pnpm build:udb` (the Plan-01 referential gate guards it) then
    `pnpm audit:all` and confirm the systematic discrepancies for the batch-1 factions are driven to zero.
    NEVER change a unit's Wahapedia id during corrections (Pitfall 7 — preserves units.udb_unit_id backlinks).
    The regenerated unit_database.json is a build OUTPUT of this task (committed), not a hand-edit.
    If RESEARCH/Plan-02 already confirmed the weapon.range/keywords systematic bugs are fixed, expect mostly
    per-unit residual errors and source-limited unmatch — those without a systematic cause are documented,
    not forced.
  </action>
  <verify>
    <automated>pnpm build:udb >/dev/null 2>&1 && pnpm audit:all >/dev/null 2>&1 && pnpm test -- tests/data-layer/fk-integrity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - For each batch-1 faction report, the systematic_issues section is empty OR every remaining systematic-looking discrepancy is explicitly annotated as a source-limited gap in the report.
    - Any field-mapping correction lives in `scripts/lib/` or `scripts/build-unit-db.ts` — `git diff src-tauri/data/unit_database.json` shows ONLY changes attributable to a rebuild (no manual edits; the file was produced by `pnpm build:udb`).
    - `pnpm build:udb` exits 0 (Plan-01 referential gate passes).
    - `pnpm test -- tests/data-layer/fk-integrity.test.ts` exits 0 against the rebuilt artifact.
    - No unit Wahapedia id was changed (Pitfall 7).
    - No new migration file added.
  </acceptance_criteria>
  <done>Batch-1 systematic data discrepancies are corrected at the pipeline source, the artifact is rebuilt and passes the referential gate, and remaining discrepancies are documented as source-limited.</done>
</task>

<task type="auto">
  <name>Task 2: Add French ability + weapon entries for batch-1 factions to translations_fr.json</name>
  <files>scripts/data/translations_fr.json, src-tauri/data/unit_database.json, .planning/phases/139-data-quality-at-scale/reports/</files>
  <read_first>
    - scripts/data/translations_fr.json (existing abilities entries ~lines 464-490 and weapons entries ~lines 3138-3154 — the exact composite-key object/string shapes to mirror)
    - scripts/lib/types.ts lines 156-162 (TranslationsFrOverlay interface)
    - scripts/build-unit-db.ts lines 641-691 (step 10.5 merge + the "French overlay: ... abilities ... weapons ..." log line)
    - src-tauri/data/unit_database.json (the rebuilt artifact from Task 1 — source of the exact unit_id, ability name, and weapon name strings to build composite keys; FR keys MUST exactly match the EN name strings the build merges on)
    - 139-CONTEXT.md D-05 (ability name_fr/description_fr + weapon name_fr, keyed on composite Wahapedia keys), D-06 (curated content, no MT runtime dep), and 139-RESEARCH.md "French Content Source — Open Question" + 139-02-SUMMARY.md (whether a Wahapedia FR locale source exists)
    - 139-RESEARCH.md Pitfall 4 (name-based keys orphan if Wahapedia renames — accept as the established mechanism, note in reports)
  </read_first>
  <action>
    Extend `scripts/data/translations_fr.json` with French content for the 13 batch-1 factions (D-05):
    for each faction's units, add `abilities["${unit_id}:${ability_name}"] = { name_fr, description_fr }`
    (description_fr may be null where no FR description is curated) and
    `weapons["${unit_id}:${weapon_name}"] = "<name_fr>"`. Optionally add `units["<wahapedia_id>"]` faction
    unit names. Source the composite keys (unit_id + exact EN ability/weapon name) from the rebuilt
    unit_database.json so keys match the step-10.5 merge exactly. Content is curated FR (per D-06, no
    machine-translation runtime dependency); if Plan 02 / 139-02-SUMMARY identified a usable Wahapedia FR
    locale source, use it to source strings, otherwise author curated FR per faction. Keep JSON valid
    (no trailing commas; preserve existing entries — additive only). Document the name-based-key limitation
    (Pitfall 4) in the batch-1 reports.
    Then run `pnpm build:udb` and confirm the "French overlay" log reports non-zero ability and weapon
    counts (i.e., the new keys actually matched rows). Do NOT hand-edit unit_database.json; the overlay is
    applied by the build at step 10.5.
  </action>
  <verify>
    <automated>pnpm build:udb 2>&1 | grep -E "French overlay: .*[1-9][0-9]* abilities" && node -e "JSON.parse(require('fs').readFileSync('scripts/data/translations_fr.json','utf-8'))"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/data/translations_fr.json` parses as valid JSON (the node JSON.parse check exits 0).
    - The file contains new `abilities` composite-key objects and `weapons` composite-key strings for batch-1 factions (additive — existing entries preserved).
    - `pnpm build:udb` logs a "French overlay" line with NON-ZERO abilities AND weapons counts (the new keys matched real rows — keys are correct, not orphaned).
    - `pnpm build:udb` exits 0 (referential gate passes).
    - `pnpm test -- tests/data-layer/` exits 0 (no regressions).
    - The name-based-key limitation (Pitfall 4) is noted in the batch-1 reports/SUMMARY.
  </acceptance_criteria>
  <done>Batch-1 factions have curated FR ability (name + description) and weapon names in the overlay, the build merges them with non-zero counts, and the COALESCE(col_fr, col) read layer will now serve FR strings for those units.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendored Wahapedia CSV → pipeline fixes | Static vendored inputs; corrections fix parsing of static data. |
| translations_fr.json → build step 10.5 → artifact | Curated FR content merged at build time, shipped read-only. |

## STRIDE Threat Register

NOTE (per RESEARCH.md §Security Domain): NO app-runtime surface, no auth, no user input this phase.
The work is dev-side pipeline corrections + curated data authoring over static inputs. No injection
path; no high-severity threats apply. Honest assessment below.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-05 | Tampering | artifact correctness after pipeline fixes | mitigate | Plan-01 referential gate + fk-integrity.test.ts run after every rebuild; `pnpm audit:all` re-verifies. |
| T-139-06 | Tampering | FR overlay key correctness | mitigate | Build logs non-zero overlay match counts; mismatched keys silently no-op (caught by the count check). |
| T-139-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed (RESEARCH §Package Legitimacy Audit). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm build:udb` exits 0 and logs non-zero French overlay ability + weapon counts.
- `pnpm audit:all` reruns; batch-1 systematic discrepancies are zero or documented as source-limited.
- `pnpm test -- tests/data-layer/` exits 0 (fk-integrity + reimport-preservation stay green).
- `git diff src-tauri/data/unit_database.json` reflects a rebuild only (no manual edits).
- No new migration file; no unit Wahapedia id changed.
</verification>

<success_criteria>
The 13 batch-1 factions are corrected end-to-end: systematic discrepancies driven to zero at the
pipeline source, FR ability/weapon content added to the overlay with non-zero merge counts, and the
referential + preservation gates remain green.
</success_criteria>

<output>
Create `.planning/phases/139-data-quality-at-scale/139-03-SUMMARY.md` when done.
</output>

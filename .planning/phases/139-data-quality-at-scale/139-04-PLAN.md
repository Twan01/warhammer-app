---
phase: 139-data-quality-at-scale
plan: 04
type: execute
wave: 4
depends_on: ["139-01", "139-02", "139-03"]
files_modified:
  - scripts/lib/weaponMapping.ts
  - scripts/lib/parseCsv.ts
  - scripts/build-unit-db.ts
  - scripts/data/translations_fr.json
  - src-tauri/data/unit_database.json
  - .planning/phases/139-data-quality-at-scale/reports/
autonomous: true
requirements: [DAT-02, DAT-03]

must_haves:
  truths:
    - "For every remaining (batch-2) faction, systematic (non-source-limited) discrepancies are driven to zero via PIPELINE fixes, never hand-edits"
    - "translations_fr.json gains FR ability name_fr/description_fr and weapon name_fr entries for the batch-2 factions, keyed on composite Wahapedia keys"
    - "ALL 25 factions have an up-to-date audit report and all systematic discrepancies across the whole set are zero or documented as source-limited (DAT-02 complete)"
    - "`pnpm build:udb` exits 0, the Plan-01 referential gate passes, and the full test suite is green"
  artifacts:
    - path: "scripts/data/translations_fr.json"
      provides: "FR entries for batch-2 factions (additive over batch-1)"
      contains: "abilities"
    - path: ".planning/phases/139-data-quality-at-scale/reports/"
      provides: "Final 25 audit reports with systematic discrepancies resolved"
  key_links:
    - from: "scripts/data/translations_fr.json"
      to: "scripts/build-unit-db.ts"
      via: "step 10.5 overlay merge keyed on ${unit_id}:${name}"
      pattern: "abilities|weapons"
    - from: "scripts/lib/weaponMapping.ts"
      to: "src-tauri/data/unit_database.json"
      via: "pnpm build:udb regenerates artifact from fixed pipeline"
      pattern: "build:udb"
---

<objective>
Batch 2 (final) of the per-faction audit-correction + French-extension work, closing DAT-02 and
DAT-03 across all 25 factions (D-03, D-04, D-05, D-06, D-08).

Process the remaining faction group END-TO-END (audit -> triage -> pipeline fix -> rebuild ->
re-audit -> FR content -> rebuild). Corrections fix the PIPELINE so one fix corrects many units;
unit_database.json is NEVER hand-edited (D-04). French content extends translations_fr.json keyed
on composite Wahapedia keys (D-05); curated content, no machine-translation runtime dependency (D-06).

Batch 2 factions (Imperium + remaining): AM, GK, AC, AS, AdM, AoI, LoV, TL, UN.

This is the closing plan: after it, all 25 factions (the 22 beyond SM/NEC/DG plus the prior three)
are audited with systematic discrepancies at zero or documented as source-limited, and the FR
overlay covers the audited factions.

Purpose: Complete the verifiable, broadly-accurate canonical data set with the referential +
preservation gates enforcing correctness.
Output: pipeline fixes (if any), FR entries for batch-2 factions, a rebuilt unit_database.json, and
the final set of 25 audit reports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-data-quality-at-scale/139-CONTEXT.md
@.planning/phases/139-data-quality-at-scale/139-RESEARCH.md
@.planning/phases/139-data-quality-at-scale/139-PATTERNS.md
@.planning/phases/139-data-quality-at-scale/139-02-SUMMARY.md
@.planning/phases/139-data-quality-at-scale/139-03-SUMMARY.md

<interfaces>
<!-- Use these directly. -->

translations_fr.json key formats (139-PATTERNS.md):
  abilities: { "<unit_id>:<ability_name>": { "name_fr": "...", "description_fr": "..."|null } }
  weapons:   { "<unit_id>:<weapon_name>": "<name_fr>" }
  units:     { "<wahapedia_unit_id>": "<name_fr>" }

build-unit-db.ts step 10.5 merge keys (lines 641-689):
  abilities key = `${a.unit_id}:${a.name}`   weapons key = `${w.unit_id}:${w.name}`
  Log: "French overlay: N factions, N units, N abilities, N weapons, N keywords translated"

Systematic-fix landing zones (D-04): scripts/lib/weaponMapping.ts, scripts/lib/parseCsv.ts, scripts/build-unit-db.ts
Audit command (from Plan 02): `pnpm audit:all` -> reports under .planning/phases/139-data-quality-at-scale/reports/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage batch-2 audit findings and fix systematic discrepancies in the pipeline</name>
  <files>scripts/lib/weaponMapping.ts, scripts/lib/parseCsv.ts, scripts/build-unit-db.ts, src-tauri/data/unit_database.json, .planning/phases/139-data-quality-at-scale/reports/</files>
  <read_first>
    - .planning/phases/139-data-quality-at-scale/reports/ — batch-2 reports: am-audit.md, gk-audit.md, ac-audit.md, as-audit.md, adm-audit.md, aoi-audit.md, lov-audit.md, tl-audit.md, un-audit.md (systematic_issues + error sections)
    - .planning/phases/139-data-quality-at-scale/139-03-SUMMARY.md (systematic fixes already landed in batch-1 — avoid re-fixing; a batch-1 systematic fix may have already cleared batch-2 factions)
    - scripts/lib/weaponMapping.ts, scripts/lib/parseCsv.ts (current state AFTER batch-1 edits — read fresh)
    - scripts/build-unit-db.ts lines 250-400 (row construction)
    - 139-CONTEXT.md D-04 (triage rule) and 139-RESEARCH.md Pitfall 6 (rebuild before re-audit), Pitfall 7 (never change unit Wahapedia ids)
  </read_first>
  <action>
    For the 9 batch-2 factions (AM, GK, AC, AS, AdM, AoI, LoV, TL, UN): read each report and triage per D-04.
    SYSTEMATIC bugs -> fix in scripts/lib/ or build-unit-db.ts (one fix corrects all affected units across all
    factions; many batch-2 systematic issues may already be resolved by batch-1 fixes — verify first, don't
    duplicate). GENUINE source gaps -> document and accept. After each fix run `pnpm build:udb` (Plan-01 gate
    guards it) then `pnpm audit:all`; confirm systematic discrepancies are zero across ALL 25 factions (this is
    the closing DAT-02 check). NEVER change a unit's Wahapedia id (Pitfall 7). The rebuilt unit_database.json is
    a build OUTPUT, never hand-edited (D-04). No new migration.
  </action>
  <verify>
    <automated>pnpm build:udb >/dev/null 2>&1 && pnpm audit:all >/dev/null 2>&1 && pnpm test -- tests/data-layer/fk-integrity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - For each batch-2 faction report, systematic_issues is empty OR each remaining systematic-looking discrepancy is annotated as source-limited.
    - Across all 25 reports, no un-annotated systematic discrepancy remains (DAT-02 "corrected" definition met).
    - Field-mapping corrections live only in `scripts/lib/` or `scripts/build-unit-db.ts`; `git diff src-tauri/data/unit_database.json` reflects a rebuild only.
    - `pnpm build:udb` exits 0; `pnpm test -- tests/data-layer/fk-integrity.test.ts` exits 0.
    - No unit Wahapedia id changed; no new migration file.
  </acceptance_criteria>
  <done>Batch-2 systematic discrepancies are corrected at the pipeline source and, combined with batch-1, all 25 factions' systematic discrepancies are zero or documented as source-limited — DAT-02 is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Add French entries for batch-2 factions; final overlay + full-suite verification</name>
  <files>scripts/data/translations_fr.json, src-tauri/data/unit_database.json, .planning/phases/139-data-quality-at-scale/reports/</files>
  <read_first>
    - scripts/data/translations_fr.json (current state AFTER batch-1 additions — read fresh; additions here are additive and must not break batch-1 entries)
    - scripts/lib/types.ts lines 156-162 (TranslationsFrOverlay)
    - scripts/build-unit-db.ts lines 641-691 (step 10.5 merge + overlay log line)
    - src-tauri/data/unit_database.json (the rebuilt artifact from Task 1 — exact unit_id + ability/weapon name strings for batch-2 composite keys)
    - 139-CONTEXT.md D-05 (composite-key FR) and D-06 (curated, no MT runtime dep); 139-RESEARCH.md Pitfall 4 (name-key limitation); 139-03-SUMMARY.md (FR sourcing approach established in batch-1)
  </read_first>
  <action>
    Extend `scripts/data/translations_fr.json` with curated FR content for the 9 batch-2 factions (D-05):
    `abilities["${unit_id}:${ability_name}"] = { name_fr, description_fr|null }` and
    `weapons["${unit_id}:${weapon_name}"] = "<name_fr>"`, optionally `units["<wahapedia_id>"]`. Source composite
    keys (unit_id + exact EN name) from the rebuilt unit_database.json so keys match the step-10.5 merge.
    Additive only — preserve all batch-1 entries; keep JSON valid (no trailing commas). Use the same sourcing
    approach as batch-1 (139-03-SUMMARY). Then run `pnpm build:udb` and confirm the "French overlay" log shows
    ability + weapon counts INCREASED vs. the batch-1-only build (the new keys matched real rows).
    Finally run the FULL suite (`pnpm test`) and `pnpm audit:all` for the closing gate. Do NOT hand-edit
    unit_database.json. No new migration.
  </action>
  <verify>
    <automated>pnpm build:udb 2>&1 | grep -E "French overlay: .*[1-9][0-9]* abilities" && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/data/translations_fr.json` parses as valid JSON and preserves all batch-1 entries (additive).
    - The file contains new batch-2 `abilities` composite-key objects and `weapons` composite-key strings.
    - `pnpm build:udb` logs a "French overlay" line with ability + weapon counts strictly greater than the batch-1-only build (new keys matched real rows).
    - `pnpm build:udb` exits 0 (referential gate passes).
    - `pnpm test` (full suite) exits 0 — fk-integrity + reimport-preservation + all existing tests green.
    - `ls .planning/phases/139-data-quality-at-scale/reports/*-audit.md | wc -l` is ≥ 25 (all factions covered).
  </acceptance_criteria>
  <done>All audited factions have curated FR ability + weapon content, the build merges them with increased non-zero counts, the full test suite is green, and DAT-02 + DAT-03 are complete across all 25 factions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendored Wahapedia CSV -> pipeline fixes | Static vendored inputs; corrections fix parsing of static data. |
| translations_fr.json -> build step 10.5 -> artifact | Curated FR content merged at build time, shipped read-only. |

## STRIDE Threat Register

NOTE (per RESEARCH.md §Security Domain): NO app-runtime surface, no auth, no user input this phase.
The work is dev-side pipeline corrections + curated data authoring over static inputs. No injection
path; no high-severity threats apply. Honest assessment below.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-07 | Tampering | artifact correctness after final pipeline fixes | mitigate | Plan-01 referential gate + fk-integrity.test.ts run after every rebuild; full `pnpm test` + `pnpm audit:all` close the phase. |
| T-139-08 | Tampering | FR overlay key correctness | mitigate | Build logs increasing overlay match counts; mismatched keys silently no-op (caught by the count check). |
| T-139-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed (RESEARCH §Package Legitimacy Audit). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm build:udb` exits 0 and logs increased French overlay ability + weapon counts vs. batch-1.
- `pnpm audit:all` reruns; all 25 factions' systematic discrepancies zero or documented as source-limited.
- `pnpm test` (full suite) exits 0.
- `git diff src-tauri/data/unit_database.json` reflects a rebuild only (no manual edits).
- No new migration file; no unit Wahapedia id changed.
</verification>

<success_criteria>
DAT-02 and DAT-03 are complete across all 25 factions: systematic discrepancies driven to zero at
the pipeline source (or documented as source-limited), FR ability/weapon content added for audited
factions, and the referential + preservation gates plus the full test suite are green.
</success_criteria>

<output>
Create `.planning/phases/139-data-quality-at-scale/139-04-SUMMARY.md` when done.
</output>

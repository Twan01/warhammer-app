---
phase: 82-restore-execution-safety-backups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
autonomous: true
requirements: [RST-06, RST-07, SAF-04]

must_haves:
  truths:
    - "restore_from_backup creates a safety backup before any destructive operation"
    - "restore_from_backup deletes WAL/SHM/journal sidecar files before replacing the database"
    - "restore_from_backup extracts hobbyforge.db from a validated zip and writes it to app_data_dir"
    - "list_safety_backups returns entries from the backups/ directory sorted newest first"
    - "list_safety_backups returns an empty vec when backups/ does not exist"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "restore_from_backup and list_safety_backups Tauri commands + SafetyBackupEntry struct"
      contains: "restore_from_backup"
  key_links:
    - from: "restore_from_backup"
      to: "create_safety_backup"
      via: "internal async call with app.clone()"
      pattern: "create_safety_backup\\(app\\.clone\\(\\)\\)"
    - from: "invoke_handler"
      to: "restore_from_backup, list_safety_backups"
      via: "tauri::generate_handler registration"
      pattern: "restore_from_backup"
---

<objective>
Add two new Rust Tauri commands to lib.rs: `restore_from_backup` (destructive database replacement with internal safety backup) and `list_safety_backups` (directory listing of safety backup files with parsed metadata).

Purpose: Provides the Rust backend API for the restore execution flow (Plan 02) and safety backup listing UI (Plan 03).
Output: Two registered Tauri commands + SafetyBackupEntry struct in src-tauri/src/lib.rs.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-restore-execution-safety-backups/82-CONTEXT.md
@.planning/phases/82-restore-execution-safety-backups/82-RESEARCH.md
@.planning/phases/82-restore-execution-safety-backups/82-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement restore_from_backup and list_safety_backups Rust commands</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/lib.rs (entire file -- read create_safety_backup at ~line 758, vacuum_to_temp at ~line 610, validate_backup at ~line 725, invoke_handler at ~line 829, BackupManifest struct at ~line 577, format_filename_timestamp at ~line 596)
    - .planning/phases/82-restore-execution-safety-backups/82-PATTERNS.md (Pattern 1: restore_from_backup, Pattern 2: list_safety_backups)
  </read_first>
  <action>
    Add `SafetyBackupEntry` struct above `restore_from_backup`. Derive `serde::Serialize` only (no Deserialize -- never received from JS). Fields: `filename: String`, `timestamp: String` (ISO 8601), `size_bytes: u64`.

    Add `restore_from_backup` as an async Tauri command per D-01. Signature: `async fn restore_from_backup(app: tauri::AppHandle, path: String) -> Result<(), String>`. Three-step sequence:
    1. Call `create_safety_backup(app.clone()).await?` -- per D-04/D-05, abort on failure, reuse existing command with `app.clone()` (not app -- AppHandle is Clone not Copy, Pitfall 3).
    2. Delete sidecar files: iterate `["-wal", "-shm", "-journal"]`, build path as `app_data_dir.join(format!("hobbyforge.db{sidecar}"))`, call `std::fs::remove_file`, ignore `ErrorKind::NotFound` per the vacuum_to_temp pattern at ~line 629.
    3. Extract `hobbyforge.db` from the backup zip: open `path` with `std::fs::File::open`, create `zip::ZipArchive::new`, call `by_name("hobbyforge.db")` (exact name -- no path traversal), read bytes with `read_to_end`, write to `app_data_dir.join("hobbyforge.db")` with `std::fs::write`. Add `use std::io::Read;` inside the function body (matches validate_backup pattern).

    Add `list_safety_backups` as a synchronous Tauri command per D-10. Signature: `fn list_safety_backups(app: tauri::AppHandle) -> Vec<SafetyBackupEntry>`. Logic:
    1. Resolve `app_data_dir/backups/` -- return `vec![]` on `app_data_dir()` error (Pitfall 4).
    2. Call `std::fs::read_dir(&backups_dir)` -- return `vec![]` if directory does not exist.
    3. Filter entries to filenames starting with `"safety-"` and ending with `".zip"`.
    4. Parse timestamp from filename: strip `"safety-"` prefix and `".zip"` suffix, split by `'-'` into 4 parts (YYYY, MM, DD, HHMM), validate HHMM length is 4, format as ISO 8601 `"YYYY-MM-DDTHH:MM:00Z"`.
    5. Get `size_bytes` from `e.metadata().ok()?.len()`.
    6. Collect into `Vec<SafetyBackupEntry>`, sort by `timestamp` descending (newest first).

    Register both commands in `invoke_handler` at ~line 829: add `restore_from_backup` and `list_safety_backups` to the `tauri::generate_handler![]` macro.

    Per D-07 and Pitfall 2: `process:allow-restart` is already granted via `process:default` in capabilities/default.json. No capability file change needed.
  </action>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App" && cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `SafetyBackupEntry` struct exists with `#[derive(serde::Serialize)]` and fields `filename: String`, `timestamp: String`, `size_bytes: u64`
    - `restore_from_backup` function has `#[tauri::command]` attribute and `async fn` signature with `app: tauri::AppHandle, path: String` parameters returning `Result<(), String>`
    - `restore_from_backup` calls `create_safety_backup(app.clone()).await?` as its first operation (not `app` -- must be `app.clone()`)
    - `restore_from_backup` deletes three sidecar files (`-wal`, `-shm`, `-journal`) with NotFound tolerance
    - `restore_from_backup` uses `zip::ZipArchive::new` and `.by_name("hobbyforge.db")` for extraction
    - `list_safety_backups` function has `#[tauri::command]` attribute returning `Vec<SafetyBackupEntry>`
    - `list_safety_backups` returns `vec![]` when backups directory does not exist (not an error)
    - `list_safety_backups` parses `"safety-YYYY-MM-DD-HHMM.zip"` filenames into ISO 8601 timestamps
    - `invoke_handler` macro contains both `restore_from_backup` and `list_safety_backups`
    - `cargo build` succeeds with no errors
  </acceptance_criteria>
  <done>Both Rust commands compile, are registered in invoke_handler, and follow established patterns. restore_from_backup performs safety backup + sidecar cleanup + zip extraction. list_safety_backups reads the backups directory and returns parsed entries.</done>
</task>

<task type="auto">
  <name>Task 2: Verify build and existing tests pass</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/lib.rs (the test module at ~line 840 -- verify backup_manifest_serde_roundtrip still compiles)
  </read_first>
  <action>
    Run the full frontend build (`pnpm build`) and the existing Rust tests (`cargo test --manifest-path src-tauri/Cargo.toml`) to confirm no regressions. The existing `backup_manifest_serde_roundtrip` test and all frontend TypeScript checks must still pass. Also run `pnpm test` to verify no frontend test regressions.
  </action>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App" && pnpm build && cargo test --manifest-path src-tauri/Cargo.toml 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0 (TypeScript compilation clean)
    - `cargo test` exits 0 (existing Rust tests pass)
    - No new compiler warnings related to the added functions
  </acceptance_criteria>
  <done>Both Rust and frontend builds pass. Existing tests unaffected by the new commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS -> Rust (invoke) | Frontend passes user-selected file path to restore_from_backup |
| Zip -> filesystem | Backup zip contents extracted and written to app_data_dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Tampering | restore_from_backup zip extraction | mitigate | Use `by_name("hobbyforge.db")` -- exact name lookup prevents zip slip/path traversal; consistent with validate_backup pattern |
| T-82-02 | Tampering | restore_from_backup path parameter | accept | Path comes from Tauri file dialog (OS-controlled) + validated by validate_backup in Phase 81; not user-typed |
| T-82-03 | Denial of Service | list_safety_backups directory listing | accept | Reads only local app_data_dir/backups/ -- not user-controllable path; worst case is many files = slow listing |
| T-82-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase -- all dependencies already present |
</threat_model>

<verification>
- `cargo build` succeeds
- `cargo test` succeeds (existing Rust tests pass)
- `pnpm build` succeeds (no frontend breakage)
- `restore_from_backup` and `list_safety_backups` appear in `invoke_handler`
</verification>

<success_criteria>
Two new Tauri commands registered and compiling: restore_from_backup performs safety-backup-then-swap, list_safety_backups returns parsed directory entries. No regressions in existing builds or tests.
</success_criteria>

<output>
Create `.planning/phases/82-restore-execution-safety-backups/82-01-SUMMARY.md` when done
</output>

---
phase: 82-restore-execution-safety-backups
plan: 03
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - src/hooks/useRulesSync.ts
  - src/features/data-health/SafetyBackupsList.tsx
  - src/features/data-health/DataHealthPage.tsx
  - tests/data-health/SafetyBackupsList.test.tsx
autonomous: true
requirements: [SAF-02, SAF-04]

must_haves:
  truths:
    - "Rules sync creates a safety backup before fetching any CSV data"
    - "Rules sync aborts with an error if the safety backup fails"
    - "Data Health page shows a list of safety backups with timestamps and sizes"
    - "Data Health page shows an empty state when no safety backups exist"
  artifacts:
    - path: "src/hooks/useRulesSync.ts"
      provides: "Pre-sync safety backup invocation"
      contains: "create_safety_backup"
    - path: "src/features/data-health/SafetyBackupsList.tsx"
      provides: "Safety backup listing component"
      contains: "list_safety_backups"
    - path: "src/features/data-health/DataHealthPage.tsx"
      provides: "SafetyBackupsList integration"
      contains: "SafetyBackupsList"
    - path: "tests/data-health/SafetyBackupsList.test.tsx"
      provides: "Tests for safety backup listing"
      contains: "list_safety_backups"
  key_links:
    - from: "useRulesSync.ts"
      to: "create_safety_backup"
      via: "invoke call at top of mutationFn"
      pattern: 'invoke.*create_safety_backup'
    - from: "SafetyBackupsList.tsx"
      to: "list_safety_backups"
      via: "useQuery with invoke"
      pattern: 'invoke.*list_safety_backups'
    - from: "DataHealthPage.tsx"
      to: "SafetyBackupsList"
      via: "component import and render"
      pattern: "SafetyBackupsList"
---

<objective>
Add pre-sync safety backup to useRulesSync, create SafetyBackupsList component, and integrate it into DataHealthPage.

Purpose: Completes SAF-02 (automatic safety backup before rules sync) and SAF-04 (safety backup listing in Data Health).
Output: Modified useRulesSync hook, new SafetyBackupsList component, updated DataHealthPage layout, tests.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-restore-execution-safety-backups/82-CONTEXT.md
@.planning/phases/82-restore-execution-safety-backups/82-RESEARCH.md
@.planning/phases/82-restore-execution-safety-backups/82-PATTERNS.md
@.planning/phases/82-restore-execution-safety-backups/82-UI-SPEC.md
@.planning/phases/82-restore-execution-safety-backups/82-01-SUMMARY.md

<interfaces>
<!-- From useRulesSync.ts -- mutationFn opening (lines 86-91) -->
mutationFn: async () => {
  const [
    factionsRaw, sourcesRaw, dsRaw, modelsRaw, abilitiesRaw, keywordsRaw,
    wargearRaw, sharedAbilitiesRaw, stratagemsRaw, detachmentsRaw,
    detachmentAbilitiesRaw, lastUpdateRaw,
  ] = await Promise.all(RULES_SYNC_FILES.map((f) => fetchCsv(f)));

<!-- From DataHealthPage.tsx -- current layout (lines 18-35) -->
export function DataHealthPage() {
  return (
    <div className="p-6 space-y-6">
      <h1 className="text-xl font-semibold">Data Health</h1>
      <VersionInfoCard />
      <div className="space-y-4">
        <h2 className="text-lg font-semibold">Table Counts</h2>
        <TableCountsGrid />
      </div>
      <DiagnosticsCard />
      <BackupCard />
    </div>
  );
}

<!-- From DiagnosticsCard.tsx -- existing card pattern for reference -->
import { useQuery } from "@tanstack/react-query";
import { Card, CardContent } from "@/components/ui/card";
import { Skeleton } from "@/components/ui/skeleton";

<!-- SafetyBackupEntry shape from Rust (82-01-PLAN) -->
interface SafetyBackupEntry {
  filename: string;
  timestamp: string;   // ISO 8601
  size_bytes: number;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-sync safety backup to useRulesSync</name>
  <files>src/hooks/useRulesSync.ts</files>
  <read_first>
    - src/hooks/useRulesSync.ts (entire file -- understand mutationFn structure, existing invoke import, onError handler)
    - .planning/phases/82-restore-execution-safety-backups/82-PATTERNS.md (Pattern 4: Pre-Sync Safety Backup)
    - .planning/phases/82-restore-execution-safety-backups/82-RESEARCH.md (Anti-Patterns section -- do NOT wrap in try/catch)
  </read_first>
  <action>
    In `useRulesSync.ts`, add a single line at the very top of the `mutationFn` async body (before the `Promise.all` CSV fetch at ~line 87):

    `await invoke("create_safety_backup");`

    Per D-08/D-09: this uses the existing `create_safety_backup` Tauri command. The `invoke` function is already imported in this file (check ~line 11 for `import { invoke } from "@tauri-apps/api/core"`). If invoke is not imported, add it.

    Do NOT wrap the call in try/catch. Per the anti-pattern guidance: if the safety backup fails, the error must bubble up to the mutation's `onError` handler, which will log it and show an error toast. The sync must not proceed.

    Add a comment above the invoke: `// SAF-02: Safety backup before any sync operation — abort sync if fails`
  </action>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `await invoke("create_safety_backup")` appears as the first statement inside `mutationFn` async body, before the `Promise.all` line
    - The invoke call is NOT wrapped in a try/catch block
    - Comment `SAF-02` appears above the invoke call
    - `invoke` is imported from `@tauri-apps/api/core`
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>useRulesSync creates a safety backup before every rules sync. Sync aborts if backup fails.</done>
</task>

<task type="auto">
  <name>Task 2: Create SafetyBackupsList component + integrate into DataHealthPage</name>
  <files>src/features/data-health/SafetyBackupsList.tsx, src/features/data-health/DataHealthPage.tsx</files>
  <read_first>
    - src/features/data-health/DataHealthPage.tsx (entire file -- understand current layout and import pattern)
    - src/features/data-health/DiagnosticsCard.tsx (entire file -- analog for card + useQuery + loading/empty pattern)
    - src/lib/formatBytes.ts (verify formatBytes export exists)
    - .planning/phases/82-restore-execution-safety-backups/82-UI-SPEC.md (SafetyBackupsList component spec -- layout, empty state, loading state, error state)
    - .planning/phases/82-restore-execution-safety-backups/82-PATTERNS.md (Pattern: SafetyBackupsList component)
  </read_first>
  <action>
    **Create `src/features/data-health/SafetyBackupsList.tsx`:**

    Define `SafetyBackupEntry` interface inline: `{ filename: string; timestamp: string; size_bytes: number; }`.

    Export query key: `export const SAFETY_BACKUPS_KEY = ["safety-backups"] as const;`

    Export named function `SafetyBackupsList`. Use inline `useQuery` (no separate hook file -- this is a read-only query consumed only by this component, per Claude's discretion).

    Query config: `queryKey: SAFETY_BACKUPS_KEY`, `queryFn: () => invoke<SafetyBackupEntry[]>("list_safety_backups")`.

    Render inside a `Card` with `CardContent className="p-6"`:
    - Section heading: `<h3 className="text-sm font-semibold mb-3">Safety Backups</h3>`
    - Loading state: 3 `Skeleton` elements with `className="h-4 w-full"` at `py-2` spacing (per UI-SPEC)
    - Empty state: "No safety backups yet" heading in `text-sm text-muted-foreground`, plus body text "Safety backups are created automatically before each restore and rules sync." (per UI-SPEC copywriting contract)
    - Error state: `text-sm text-muted-foreground` with "Could not load safety backups -- refresh the page to try again."
    - Populated state: list container with `divide-y divide-border`. Each row is `flex items-center gap-2 py-2` with:
      - `Shield` icon from lucide-react: `className="h-3.5 w-3.5 text-muted-foreground shrink-0"`
      - Timestamp: `text-sm text-foreground` -- format with `new Date(e.timestamp).toLocaleString()`
      - Size: `text-sm text-muted-foreground ml-auto` -- format with `formatBytes(e.size_bytes)` from `@/lib/formatBytes`

    Imports needed: `useQuery` from `@tanstack/react-query`, `invoke` from `@tauri-apps/api/core`, `Card`/`CardContent` from `@/components/ui/card`, `Skeleton` from `@/components/ui/skeleton`, `Shield` from `lucide-react`, `formatBytes` from `@/lib/formatBytes`.

    **Modify `src/features/data-health/DataHealthPage.tsx`:**

    Add import: `import { SafetyBackupsList } from "./SafetyBackupsList";`

    Add a new section after `<BackupCard />` and before the closing `</div>`:
    ```
    <div className="space-y-4">
      <h2 className="text-lg font-semibold">Safety Backups</h2>
      <SafetyBackupsList />
    </div>
    ```
    This follows the same `space-y-4` + `text-lg font-semibold` heading pattern used by the "Table Counts" section.
  </action>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `SafetyBackupsList.tsx` exports `SAFETY_BACKUPS_KEY` as `["safety-backups"]`
    - `SafetyBackupsList.tsx` exports named function `SafetyBackupsList`
    - Component calls `invoke<SafetyBackupEntry[]>("list_safety_backups")` via useQuery
    - Component renders Shield icon, timestamp via `toLocaleString()`, and size via `formatBytes()`
    - Component has loading state with 3 Skeleton elements
    - Component has empty state with text "No safety backups yet"
    - Component has error state with text "Could not load safety backups"
    - `DataHealthPage.tsx` imports and renders `SafetyBackupsList` below `BackupCard`
    - Section heading "Safety Backups" uses `text-lg font-semibold` class
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>SafetyBackupsList renders in Data Health page with loading, empty, populated, and error states.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for SafetyBackupsList</name>
  <files>tests/data-health/SafetyBackupsList.test.tsx</files>
  <read_first>
    - src/features/data-health/SafetyBackupsList.tsx (the component created in Task 2)
    - tests/data-health/backupCard.test.tsx (analog -- mock patterns for invoke, QueryClient wrapper, RTL render + waitFor)
    - .planning/phases/82-restore-execution-safety-backups/82-PATTERNS.md (SafetyBackupsList test patterns)
  </read_first>
  <action>
    Create `tests/data-health/SafetyBackupsList.test.tsx`.

    Set up mocks: `const mockInvoke = vi.fn();` with `vi.mock("@tauri-apps/api/core", () => ({ invoke: (...args: unknown[]) => mockInvoke(...args) }));`.

    Create a QueryClient wrapper function for rendering with React Query (same pattern as backupCard.test.tsx): `new QueryClient({ defaultOptions: { queries: { retry: false } } })` wrapped in `QueryClientProvider`. Create a fresh QueryClient in each test (or in beforeEach) to avoid cache leakage.

    Reset `mockInvoke` in `beforeEach`.

    Import `SafetyBackupsList` from `@/features/data-health/SafetyBackupsList`.

    Write these test cases:

    1. "renders safety backup entries with timestamps and sizes": Mock `mockInvoke` to resolve with `[{ filename: "safety-2026-05-19-1430.zip", timestamp: "2026-05-19T14:30:00Z", size_bytes: 42000 }]`. Render the component, `waitFor` the timestamp text to appear (use `toLocaleString` of the timestamp date). Assert the formatted size appears (use the same `formatBytes(42000)` output -- likely "41 KB" or similar). Assert `mockInvoke` was called with `"list_safety_backups"`.

    2. "shows empty state when no safety backups exist": Mock `mockInvoke` to resolve with `[]`. Render, `waitFor` text "No safety backups yet" to appear. Assert the explanation text "Safety backups are created automatically" is present.

    3. "shows error state when invoke fails": Mock `mockInvoke` to reject. Render, `waitFor` text "Could not load safety backups" to appear.
  </action>
  <verify>
    <automated>cd "C:\Documents\Claude Apps\Warhammer App" && pnpm test -- tests/data-health/SafetyBackupsList.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at `tests/data-health/SafetyBackupsList.test.tsx`
    - Test "renders safety backup entries" passes -- asserts invoke called with "list_safety_backups" and entry data renders
    - Test "shows empty state" passes -- asserts "No safety backups yet" text visible
    - Test "shows error state" passes -- asserts "Could not load safety backups" text visible
    - All 3 tests pass: `pnpm test -- tests/data-health/SafetyBackupsList.test.tsx` exits 0
    - `pnpm test` (full suite) passes with 0 failures
  </acceptance_criteria>
  <done>SafetyBackupsList has 3 passing tests covering populated, empty, and error states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS invoke -> Rust | create_safety_backup called before sync; list_safety_backups for display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-06 | Denial of Service | Pre-sync safety backup adds latency | accept | VACUUM INTO is fast for typical hobby DB sizes (<10MB); sync already takes seconds for CSV fetch |
| T-82-07 | Information Disclosure | Safety backup list shows file sizes | accept | Local desktop app; file sizes of own backups are not sensitive |
| T-82-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
- `pnpm build` succeeds
- `pnpm test -- tests/data-health/SafetyBackupsList.test.tsx` passes (3 tests)
- `pnpm test` full suite passes
- useRulesSync.ts contains `invoke("create_safety_backup")` before the Promise.all line
- DataHealthPage renders SafetyBackupsList below BackupCard
</verification>

<success_criteria>
Rules sync creates a safety backup before fetching CSVs (aborting on failure). SafetyBackupsList renders in Data Health with loading, empty, populated, and error states. All tests pass.
</success_criteria>

<output>
Create `.planning/phases/82-restore-execution-safety-backups/82-03-SUMMARY.md` when done
</output>

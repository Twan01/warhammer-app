---
phase: 67-game-day-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/game-day/GameDayReadinessPanel.tsx
  - src/features/game-day/GameDayPage.tsx
  - tests/game-day/GameDayReadinessPanel.test.tsx
  - tests/game-day/GameDayPage.test.tsx
autonomous: true
requirements: [GD-01]

must_haves:
  truths:
    - "Game Day page shows points total and limit with red text when exceeded"
    - "Game Day page shows PointsFreshnessBadge for stale/unknown points warnings"
    - "Game Day page shows readiness gaps listing unpainted and unassembled units"
    - "Game Day page shows tactical role coverage pills with dashed border for gaps"
    - "Game Day page shows collapsible warning detail with per-unit hard/soft breakdown"
    - "Game Day page shows all-clear state when zero warnings and all units complete"
  artifacts:
    - path: "src/features/game-day/GameDayReadinessPanel.tsx"
      provides: "Pre-game readiness panel component"
      exports: ["GameDayReadinessPanel"]
    - path: "tests/game-day/GameDayReadinessPanel.test.tsx"
      provides: "Unit tests for readiness panel"
      min_lines: 80
  key_links:
    - from: "src/features/game-day/GameDayPage.tsx"
      to: "GameDayReadinessPanel"
      via: "JSX render between GameDayHeader and Tabs"
      pattern: "<GameDayReadinessPanel"
    - from: "src/features/game-day/GameDayReadinessPanel.tsx"
      to: "src/lib/computeUnitWarnings.ts"
      via: "useMemo calling computeListHealthStats and computeUnitWarnings"
      pattern: "computeListHealthStats"
    - from: "src/features/game-day/GameDayPage.tsx"
      to: "src/hooks/useDatasheet.ts"
      via: "useRulesSyncMeta hook for freshness"
      pattern: "useRulesSyncMeta"
---

<objective>
Surface army list validation warnings in the Game Day pre-game view so the user walks into a game fully informed about points, readiness, freshness, and tactical coverage.

Purpose: This is the capstone integration for the v0.2.10 milestone — connecting Phase 66's warning system (computeUnitWarnings, computeListHealthStats, tactical roles) into the Game Day page that was built in Phase 56.

Output: GameDayReadinessPanel component rendered between GameDayHeader and Tabs, showing points status, freshness badge, readiness gaps, role coverage pills, and collapsible per-unit warning detail.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-game-day-integration/67-CONTEXT.md
@.planning/phases/67-game-day-integration/67-RESEARCH.md
@.planning/phases/67-game-day-integration/67-PATTERNS.md
@.planning/phases/67-game-day-integration/67-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/computeUnitWarnings.ts:
```typescript
export interface UnitWarnings { hard: string[]; soft: string[]; }
export interface WarningContext { totalPoints: number; pointsLimit: number | null; freshness: SyncFreshness; }
export interface ListHealthStats {
  totalPoints: number; pointsLimit: number | null; ownershipPct: number;
  battleReadyPct: number; hardWarningCount: number; softWarningCount: number; pointsExceeded: boolean;
}
export function computeUnitWarnings(unit: Pick<ArmyListUnitRow, "effective_points" | "points_override" | "status_painting" | "status_assembly">, context: WarningContext): UnitWarnings;
export function computeListHealthStats(units: ArmyListUnitRow[], pointsLimit: number | null, freshness: SyncFreshness): ListHealthStats;
```

From src/types/armyList.ts:
```typescript
export interface ArmyListUnitRow {
  id: number; list_id: number; unit_id: number; points_override: number | null;
  notes: string | null; created_at: string; unit_name: string; unit_points: number;
  faction_id: number; status_assembly: number; status_painting: string;
  painting_percentage: number; effective_points: number; tactical_role: string | null;
}
export const TACTICAL_ROLES = ["anti_infantry", "anti_tank", "screening", "objective_holder", "fire_support", "melee", "utility"] as const;
export type TacticalRole = typeof TACTICAL_ROLES[number];
export const TACTICAL_ROLES_DISPLAY: Record<TacticalRole, string>;
```

From src/lib/syncFreshness.ts:
```typescript
export type SyncFreshness = "fresh" | "stale" | "never";
export function getSyncFreshness(lastSyncAt: string | null): SyncFreshness;
```

From src/features/game-day/GameDayPage.tsx (current JSX structure):
- Line 59-64: GameDayHeader render
- Line 66: Tabs render starts
- Insert point: between GameDayHeader close and Tabs open
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create GameDayReadinessPanel component with tests</name>
  <files>
    src/features/game-day/GameDayReadinessPanel.tsx,
    tests/game-day/GameDayReadinessPanel.test.tsx
  </files>
  <read_first>
    src/features/game-day/GameDayReadinessPanel.tsx (does not exist yet — confirms no conflicts),
    src/features/army-lists/ArmyListSummaryBar.tsx (canonical pattern analog — props, stats row, role coverage, readiness section, warning display),
    src/lib/computeUnitWarnings.ts (computeListHealthStats, computeUnitWarnings, WarningContext, ListHealthStats types),
    src/types/armyList.ts (ArmyListUnitRow, TACTICAL_ROLES, TACTICAL_ROLES_DISPLAY, TacticalRole),
    src/lib/syncFreshness.ts (SyncFreshness type),
    src/features/army-lists/PointsFreshnessBadge.tsx (self-contained component to embed — confirm import path),
    src/components/ui/collapsible.tsx (Collapsible, CollapsibleTrigger, CollapsibleContent exports),
    src/components/ui/status-badge.tsx (StatusBadge component for readiness gaps),
    tests/workshop-play/armyListReadinessPanel.test.tsx (test analog — makeUnit factory, TooltipProvider wrapping, render pattern),
    .planning/phases/67-game-day-integration/67-UI-SPEC.md (visual anatomy, interaction contract, states to implement),
    .planning/phases/67-game-day-integration/67-PATTERNS.md (exact code excerpts for every sub-pattern)
  </read_first>
  <behavior>
    - Test 1: renders points display as "X / Y pts" when pointsLimit is set
    - Test 2: renders points display as "X pts" when pointsLimit is null
    - Test 3: points value has text-destructive class when pointsExceeded is true
    - Test 4: renders PointsFreshnessBadge (mocked, assert data-testid="freshness-badge")
    - Test 5: renders warning count with hard+soft split tooltip text "N critical, N informational"
    - Test 6: warning count text has text-destructive class when hard warnings present
    - Test 7: warning count text has text-amber-500 class when only soft warnings present
    - Test 8: collapsible detail lists affected units with their warning messages
    - Test 9: readiness gaps section shows unpainted units with StatusBadge
    - Test 10: readiness gaps shows "Not assembled" text for units with status_assembly === 0
    - Test 11: role coverage pills render with count for each role when hasAnyRole is true
    - Test 12: uncovered roles show dashed border class (border-dashed)
    - Test 13: role coverage section hidden when no units have tactical_role
    - Test 14: "All units battle-ready" text shown when zero warnings and all units painting-complete
  </behavior>
  <action>
    Create GameDayReadinessPanel as a presentation component per D-02 (NOT reuse of ArmyListSummaryBar). The component receives props: units (ArmyListUnitRow[]), pointsLimit (number | null), freshness (SyncFreshness).

    Internal structure follows 67-PATTERNS.md exactly:
    1. useMemo calling computeListHealthStats(units, pointsLimit, freshness) for aggregate stats
    2. useMemo building unitsWithWarnings via computeUnitWarnings per unit, filtered to those with warnings
    3. useMemo computing notReadyUnits filtered by status_painting !== "Completed" OR status_assembly === 0 (per D-07)
    4. useMemo computing roleCounts via reduce with the "u.tactical_role in counts" guard from ArmyListSummaryBar
    5. hasAnyRole = units.some(u => u.tactical_role !== null) for progressive disclosure (per D-09)

    Visual layout per 67-UI-SPEC.md anatomy:
    - Container: flex flex-col gap-3 px-4 py-3 bg-muted/30 border-b
    - Stat row: Total (X / Y pts or X pts), Owned (N%), Ready (N%) using the Stat helper sub-component from 67-PATTERNS.md
    - Progress bar: battle-gold fill for battleReadyPct
    - Freshness + warnings row: embed PointsFreshnessBadge (import from @/features/army-lists/PointsFreshnessBadge), warning count with Tooltip showing "N critical, N informational"
    - Warning count span is CollapsibleTrigger (per D-03) — clicking toggles collapsible detail. ChevronDown icon h-3 w-3 with transition-transform rotate-180 when open.
    - CollapsibleContent: per-unit warning list, each unit showing name + hard warnings prefixed with AlertCircle (text-destructive h-3 w-3) + soft warnings prefixed with AlertTriangle (text-amber-500 h-3 w-3)
    - Role Coverage section (only when hasAnyRole per D-09): section label "Role Coverage" in text-xs font-semibold uppercase tracking-wide, then flex flex-wrap gap-2 of pills. Covered: bg-secondary rounded-full px-2 py-1 text-xs. Uncovered: border border-dashed border-muted-foreground/40 text-muted-foreground bg-transparent rounded-full px-2 py-1 text-xs.
    - Not Ready section: label "Not ready (N)" + list of notReadyUnits with unit_name and StatusBadge, plus "Not assembled" text-muted-foreground text-xs when status_assembly === 0
    - All-clear state: when notReadyUnits.length === 0 AND totalWarnings === 0, show bg-battle-gold/10 text-battle-gold pill with "All units battle-ready"

    For tests, mock PointsFreshnessBadge (it calls hooks internally) as a span with data-testid="freshness-badge". Use makeUnit factory with correct field names (list_id not army_list_id, include tactical_role, points_override). Wrap all renders in TooltipProvider (Pitfall 1 from RESEARCH.md).
  </action>
  <verify>
    <automated>pnpm test -- tests/game-day/GameDayReadinessPanel.test.tsx</automated>
  </verify>
  <done>
    - GameDayReadinessPanel.tsx exports GameDayReadinessPanel function component
    - Points display shows "X / Y pts" format with text-destructive when exceeded (per D-04)
    - PointsFreshnessBadge is rendered in the panel (per D-05)
    - Warning count displays with hard/soft split and collapsible detail (per D-03)
    - Readiness gaps show unpainted and unassembled units with StatusBadge (per D-06, D-07)
    - Role coverage pills show with dashed border for uncovered roles (per D-08), hidden when no roles assigned (per D-09)
    - All 14+ tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire GameDayReadinessPanel into GameDayPage and update page test</name>
  <files>
    src/features/game-day/GameDayPage.tsx,
    tests/game-day/GameDayPage.test.tsx
  </files>
  <read_first>
    src/features/game-day/GameDayPage.tsx (current page — see exact insertion point between line 64 GameDayHeader close and line 66 Tabs),
    src/features/game-day/GameDayReadinessPanel.tsx (created in Task 1 — confirm export name and props),
    src/hooks/useDatasheet.ts (useRulesSyncMeta export — confirm hook signature),
    src/lib/syncFreshness.ts (getSyncFreshness function signature),
    tests/game-day/GameDayPage.test.tsx (current test — see existing mocks at lines 23-76, note army_list_id field needs correction to list_id, missing tactical_role and points_override fields in useArmyListWithUnits mock)
  </read_first>
  <action>
    Modify GameDayPage.tsx per D-01 and D-10:
    1. Add imports: useRulesSyncMeta from @/hooks/useDatasheet, getSyncFreshness from @/lib/syncFreshness, GameDayReadinessPanel from ./GameDayReadinessPanel
    2. Inside GameDayPage body, after the existing useFactions/faction hooks, add: const { data: syncMeta } = useRulesSyncMeta(); and const freshness = getSyncFreshness(syncMeta?.last_sync_at ?? null); (per D-11, Pitfall 2 null-safety)
    3. In JSX, insert GameDayReadinessPanel between GameDayHeader closing tag (line 64) and Tabs opening tag (line 66): units={units ?? []}, pointsLimit={list.points_limit}, freshness={freshness}

    Modify GameDayPage.test.tsx:
    1. Fix useArmyListWithUnits mock: change army_list_id to list_id (Pitfall 5), add points_override: null, add tactical_role: null, add notes: null
    2. useRulesSyncMeta mock already exists at line 75 as { data: null } — no change needed
    3. Add TooltipProvider wrapper to createWrapper function (wrap children in TooltipProvider inside QueryClientProvider)
    4. Add new test: "renders GameDayReadinessPanel" — assert the readiness panel renders by checking for text content that the panel produces (e.g., "Total" stat label or "pts" text)
  </action>
  <verify>
    <automated>pnpm test -- tests/game-day/GameDayPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - GameDayPage.tsx imports GameDayReadinessPanel, useRulesSyncMeta, getSyncFreshness
    - GameDayPage.tsx contains const freshness = getSyncFreshness(syncMeta?.last_sync_at ?? null)
    - JSX has GameDayReadinessPanel rendered between GameDayHeader and Tabs
    - GameDayPage.test.tsx mock uses list_id (not army_list_id) and includes tactical_role: null, points_override: null
    - GameDayPage.test.tsx wraps renders in TooltipProvider
    - All existing + new GameDayPage tests pass
  </acceptance_criteria>
  <done>
    - GameDayReadinessPanel is visible on Game Day page between header and tabs (per D-01)
    - No new queries added — uses existing useArmyListWithUnits and adds only useRulesSyncMeta for freshness (per D-10, D-11)
    - All GameDayPage tests pass including the new readiness panel render assertion
    - Full test suite (pnpm test) passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No new trust boundaries — display-only component reading data already in memory from existing React Query hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Information Disclosure | GameDayReadinessPanel | accept | Component displays data the user already has access to via army list detail. No new data surfaces, no cross-user data. Single-user desktop app. |
</threat_model>

<verification>
1. `pnpm test -- tests/game-day/GameDayReadinessPanel.test.tsx` — all panel tests pass
2. `pnpm test -- tests/game-day/GameDayPage.test.tsx` — all page tests pass including new readiness panel assertion
3. `pnpm test` — full suite green
4. `pnpm build` — TypeScript compiles without errors
</verification>

<success_criteria>
- Game Day page shows pre-game readiness panel between header and tabs
- Panel displays points total/limit with red text when exceeded
- Panel shows PointsFreshnessBadge for freshness status
- Panel shows collapsible warning detail with per-unit breakdown
- Panel shows readiness gaps (unpainted + unassembled units)
- Panel shows role coverage pills with dashed border for uncovered roles
- Panel shows "All units battle-ready" when no warnings
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/67-game-day-integration/67-01-SUMMARY.md`
</output>

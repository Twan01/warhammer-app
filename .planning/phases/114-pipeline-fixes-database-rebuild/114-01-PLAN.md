---
phase: 114-pipeline-fixes-database-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-unit-db.ts
  - tests/build-pipeline/weaponParsing.test.ts
autonomous: true
requirements:
  - PFX-01
  - PFX-02

must_haves:
  truths:
    - "Weapon range field is populated for all weapons (was empty due to wrong CSV column name)"
    - "Weapon keywords field is populated for all weapons (was reading wrong CSV column)"
    - "Weapon group and line order correctly map to CSV line and line_in_wargear columns"
    - "weaponGroupTracker counter-based logic is removed entirely"
  artifacts:
    - path: "scripts/build-unit-db.ts"
      provides: "Fixed weapon parsing with correct CSV column mappings"
      contains: 'row["range"]'
    - path: "scripts/build-unit-db.ts"
      provides: "Fixed weapon keywords column"
      contains: 'row["description"]'
    - path: "tests/build-pipeline/weaponParsing.test.ts"
      provides: "Tests for weapon CSV column mapping and group tracking"
      min_lines: 40
  key_links:
    - from: "scripts/build-unit-db.ts"
      to: "scripts/data/Datasheets_wargear.csv"
      via: "CSV column accessors"
      pattern: 'row\["range"\]'
---

<objective>
Fix the three systematic parsing bugs in build-unit-db.ts discovered during the Phase 113 audit: (1) weapon range reads wrong CSV column case, (2) weapon keywords reads wrong CSV column name, (3) weapon_group uses a faulty counter instead of the CSV's own line/line_in_wargear columns. Add tests to prevent regressions.

Purpose: These three bugs affect ALL 2,472+ weapons across SM/NEC/DG (and thousands more across all factions). Fixing them is the highest-impact change in this phase.
Output: Corrected build-unit-db.ts, new weaponParsing.test.ts
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-pipeline-fixes-database-rebuild/114-RESEARCH.md

<interfaces>
<!-- CSV column headers from Datasheets_wargear.csv (pipe-delimited): -->
<!-- datasheet_id|line|line_in_wargear|dice|name|description|range|type|A|BS_WS|S|AP|D| -->
<!-- "line" = weapon profile group number per unit (1,2,3...) -->
<!-- "line_in_wargear" = sub-profile within group (1=base, 2=supercharge) -->
<!-- "description" = weapon special rules/keywords (e.g. "Anti-Infantry 4+", "Devastating Wounds") -->
<!-- "range" = weapon range string (e.g. "12\"", "24\"", "Melee") -->

From scripts/lib/types.ts (UdbUnitWeaponRow interface):
```typescript
export interface UdbUnitWeaponRow {
  unit_id: string;
  weapon_group: number;
  line_order: number;
  name: string;
  category: string;
  range: string;
  attacks: string;
  skill: string;
  strength: string;
  ap: string;
  damage: string;
  keywords: string;
  name_fr: string | null;
}
```

From src/features/units/LoadoutSection.tsx (weapon_group consumer):
```typescript
// Pattern 5: group weapons by weapon_group field, sorted ASC
const groups = new Map<number, UdbUnitWeapon[]>();
for (const w of allWeapons) {
  const bucket = groups.get(w.weapon_group) ?? [];
  bucket.push(w);
  groups.set(w.weapon_group, bucket);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create weapon parsing tests</name>
  <files>tests/build-pipeline/weaponParsing.test.ts</files>
  <read_first>
    - scripts/build-unit-db.ts (lines 252-288 -- current buggy weapon parsing logic)
    - scripts/data/Datasheets_wargear.csv (first 5 lines -- confirm header format)
    - tests/build-pipeline/parseCsv.test.ts (existing test patterns for CSV parsing)
    - scripts/lib/types.ts (UdbUnitWeaponRow interface)
  </read_first>
  <behavior>
    - Test 1: weapon range is extracted from row["range"] (lowercase), not row["Range"]
    - Test 2: weapon keywords is extracted from row["description"], not row["keywords"]
    - Test 3: weapon_group equals CSV "line" column value directly (not a counter)
    - Test 4: line_order equals CSV "line_in_wargear" column value directly (not CSV "line")
    - Test 5: multi-weapon unit (3 weapons across 2 groups) produces correct weapon_group and line_order assignments without any counter state
  </behavior>
  <action>
    Create tests/build-pipeline/weaponParsing.test.ts. Since the weapon parsing logic is inline in build-unit-db.ts (not an exported function), test it by extracting the parsing logic into a testable helper or by testing the CSV row mapping directly.

    Approach: Create a test that simulates the weapon row parsing by testing the column accessor mapping. Build mock CSV row objects with the exact header names from the CSV file (datasheet_id, line, line_in_wargear, name, description, range, type, A, BS_WS, S, AP, D) and verify the expected output fields.

    For the multi-weapon test, use the Imotekh example from RESEARCH: unit 000000522 with 3 weapons across groups 1, 2, 3 where line_in_wargear is always 1. Verify weapon_group=1,2,3 and line_order=1,1,1.

    Use the same vitest patterns as tests/build-pipeline/parseCsv.test.ts (import { describe, it, expect } from "vitest").

    The tests should initially FAIL because they test the correct behavior (lowercase "range", "description" column, direct line/line_in_wargear mapping) which the current code does not implement.
  </action>
  <verify>
    <automated>pnpm test -- tests/build-pipeline/weaponParsing.test.ts -x 2>&1 | findstr /C:"FAIL" /C:"pass" /C:"Tests"</automated>
  </verify>
  <acceptance_criteria>
    - tests/build-pipeline/weaponParsing.test.ts exists with at least 5 test cases
    - Tests cover: range column casing, keywords->description mapping, weapon_group=line, line_order=line_in_wargear, multi-weapon grouping
    - Tests define the CORRECT behavior (they will fail against current buggy code, pass after Task 2)
  </acceptance_criteria>
  <done>Weapon parsing test file created with 5+ test cases covering all three bug classes. Tests define correct CSV column mapping behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Fix three systematic CSV parsing bugs</name>
  <files>scripts/build-unit-db.ts</files>
  <read_first>
    - scripts/build-unit-db.ts (lines 252-290 -- weapon parsing section to fix)
    - scripts/data/Datasheets_wargear.csv (first 3 lines -- confirm exact header names)
    - tests/build-pipeline/weaponParsing.test.ts (tests from Task 1 that must pass)
  </read_first>
  <action>
    Apply three fixes to the weapon parsing section in scripts/build-unit-db.ts (lines 252-288):

    Fix 1 (D-01, PFX-01): Line 279 -- change row["Range"] to row["range"]. The CSV header is lowercase.

    Fix 2 (D-02, PFX-01): Line 285 -- change row["keywords"] to row["description"]. The CSV uses "description" for weapon special rules/keywords text.

    Fix 3 (D-04, PFX-01): Lines 257-275 -- replace the weaponGroupTracker counter logic with direct CSV column mapping:
    - Remove the weaponGroupTracker Map declaration (line 258)
    - Change line 265: const lineOrder = parseInt(row["line"]...) becomes const weaponGroup = parseInt(row["line"]...)
    - Add: const lineOrder = parseInt(row["line_in_wargear"]?.trim() ?? "1", 10) || 1
    - Remove the if (lineOrder === 1) block that increments the counter (lines 267-271)
    - Update the weapons.push call to use the new weaponGroup and lineOrder variables

    The weaponGroupTracker variable, its Map instantiation, and all references to it are deleted entirely.

    Per D-06/PFX-02: normalization already handles apostrophe variants (confirmed in RESEARCH Finding 3). No changes to scripts/lib/normalize.ts needed.
  </action>
  <verify>
    <automated>pnpm test -- tests/build-pipeline/weaponParsing.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - scripts/build-unit-db.ts line ~279 reads row["range"] (not row["Range"])
    - scripts/build-unit-db.ts line ~285 reads row["description"] (not row["keywords"])
    - No occurrence of "weaponGroupTracker" in scripts/build-unit-db.ts
    - weapon_group assignment uses parseInt(row["line"]) directly
    - line_order assignment uses parseInt(row["line_in_wargear"]) directly
    - All weaponParsing.test.ts tests pass
    - pnpm test passes (full suite -- no regressions)
  </acceptance_criteria>
  <done>Three systematic CSV parsing bugs fixed. Weapon range, keywords, and group mapping all use correct CSV column names. Tests pass. No weaponGroupTracker remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- this plan modifies dev-side build scripts only. No user input, no network, no auth.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Tampering | build-unit-db.ts CSV parsing | accept | Dev-only script; output (unit_database.json) is committed to git and reviewed before release |
</threat_model>

<verification>
pnpm test -- tests/build-pipeline/ -x
All weapon parsing tests pass with correct column mappings.
</verification>

<success_criteria>
- Three CSV column bugs fixed in build-unit-db.ts
- weaponGroupTracker removed, replaced with direct CSV line/line_in_wargear mapping
- weaponParsing.test.ts exists with 5+ tests covering all three bug classes
- Full test suite green (pnpm test)
</success_criteria>

<output>
Create `.planning/phases/114-pipeline-fixes-database-rebuild/114-01-SUMMARY.md` when done
</output>

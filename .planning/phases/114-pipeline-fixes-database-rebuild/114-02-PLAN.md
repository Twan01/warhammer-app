---
phase: 114-pipeline-fixes-database-rebuild
plan: 02
type: execute
wave: 2
depends_on:
  - 114-01
files_modified:
  - scripts/data/aliases.json
  - src-tauri/data/unit_database.json
  - scripts/data/coverage-report.json
  - scripts/build-unit-db.ts
autonomous: true
requirements:
  - PFX-03
  - PFX-04

must_haves:
  truths:
    - "aliases.json contains only entries for genuine BSData<->Wahapedia name mismatches, not Wahapedia-only units"
    - "A fresh database rebuild produces weapons with populated range and keywords fields"
    - "Coverage report shows SM/NEC/DG data quality improvements compared to pre-fix baseline"
    - "MIN_COVERAGE_PCT is updated if coverage justifies it"
  artifacts:
    - path: "src-tauri/data/unit_database.json"
      provides: "Rebuilt canonical unit database with pipeline fixes applied"
    - path: "scripts/data/coverage-report.json"
      provides: "Updated coverage percentages after rebuild"
    - path: "scripts/data/aliases.json"
      provides: "Targeted aliases for genuine edge cases only"
  key_links:
    - from: "scripts/build-unit-db.ts"
      to: "src-tauri/data/unit_database.json"
      via: "build script output"
      pattern: "writeFileSync.*unit_database"
    - from: "scripts/build-unit-db.ts"
      to: "scripts/data/coverage-report.json"
      via: "coverage report generation"
      pattern: "coverage-report"
---

<objective>
Investigate unmatched units to add targeted aliases for genuine name mismatches (not Wahapedia-only units), rebuild the database with all pipeline fixes from Plan 01, and verify improved data quality. Update MIN_COVERAGE_PCT if warranted.

Purpose: Completes the pipeline fix cycle -- bugs are fixed (Plan 01), now aliases fill remaining gaps and the rebuild proves everything works together.
Output: Updated aliases.json, rebuilt unit_database.json, updated coverage-report.json
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-pipeline-fixes-database-rebuild/114-RESEARCH.md
@.planning/phases/114-pipeline-fixes-database-rebuild/114-01-SUMMARY.md

<interfaces>
<!-- Current coverage baseline (from coverage-report.json): -->
<!-- SM: 58.1% (173/298), NEC: 79.7% (51/64), DG: 50.7% (36/71), Overall: 60.1% (1028/1711) -->

<!-- Unmatched unit categories from audit reports: -->
<!-- SM: 79 missing_alias + 43 forge_world -->
<!-- NEC: 6 missing_alias + 7 forge_world -->
<!-- DG: 9 missing_alias + 26 forge_world -->

<!-- aliases.json format: { "Wahapedia Name": "BSData Name" } -->
<!-- BPH-04 validates at build time: warns on unused aliases, unknown BSData names -->

<!-- Audit JSON files contain machine-readable error data with unit names and error types -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate unmatched units and add targeted aliases</name>
  <files>scripts/data/aliases.json</files>
  <read_first>
    - scripts/data/aliases.json (current alias entries -- 44 entries)
    - .planning/phases/113-priority-faction-data-audit/reports/sm-audit.json (SM unmatched units)
    - .planning/phases/113-priority-faction-data-audit/reports/nec-audit.json (NEC unmatched units)
    - .planning/phases/113-priority-faction-data-audit/reports/dg-audit.json (DG unmatched units)
    - scripts/lib/bsdata.ts (matchUnit 3-pass logic -- understand what is already matched)
  </read_first>
  <action>
    Per D-07/D-09/D-10/D-11: For each "missing_alias" unit from the three audit reports, determine which category it falls into:

    Category A -- Wahapedia-only (no BSData equivalent): Skip. These units have no .cat file entry to match against. Most SM chapter-specific characters and Kill Team units fall here.

    Category B -- Genuine name mismatch (BSData has unit under different name): Add alias to aliases.json.

    Category C -- Already matched via cross-faction: Skip.

    Investigation method: For each missing_alias unit name, search the BSData .cat XML files in scripts/data/bsdata/ for the unit name or a close variant. Use grep across all .cat files. Only add an alias when a clear BSData match exists under a different name.

    Expected aliases from RESEARCH Finding 4 (NEC examples): "Lokhust Heavy Destroyers" might need an alias if BSData uses singular form. Check each of the 6 NEC, 9 DG, and a representative sample of the 79 SM missing_alias units.

    Per D-09: Do NOT add aliases for Forge World units (already classified separately in audit). Do NOT add aliases for units that simply do not exist in BSData.

    After adding aliases, verify no BPH-04 validation warnings by checking that each BSData target name actually exists in the .cat files.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/build-unit-db.ts 2>&1 | findstr /C:"alias" /C:"warning" /C:"unused"</automated>
  </verify>
  <acceptance_criteria>
    - aliases.json contains only entries where BOTH the Wahapedia name AND the BSData target name are confirmed to exist in their respective data sources
    - No alias entries added for Forge World units or Wahapedia-only units (per D-07/D-09)
    - Build script runs without BPH-04 alias validation warnings for the new entries
    - Each new alias entry has been verified by grepping BSData .cat files for the target name
  </acceptance_criteria>
  <done>Targeted aliases added for genuine name mismatches only. Wahapedia-only units explicitly skipped. No BPH-04 warnings on new entries.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild database and verify improvements</name>
  <files>src-tauri/data/unit_database.json, scripts/data/coverage-report.json, scripts/build-unit-db.ts</files>
  <read_first>
    - scripts/build-unit-db.ts (top 30 lines -- MIN_COVERAGE_PCT constant location)
    - scripts/data/coverage-report.json (current baseline percentages)
    - src-tauri/data/unit_database.json (first 50 lines -- structure verification)
  </read_first>
  <action>
    Per D-12/D-13/D-14:

    Step 1: Run full database rebuild:
    node --experimental-strip-types scripts/build-unit-db.ts

    Step 2: Verify weapon data quality by spot-checking the rebuilt unit_database.json:
    - Find a weapon entry and confirm range field is populated (was empty before fix)
    - Find a weapon entry and confirm keywords field is populated (was empty before fix)
    - Find a multi-weapon unit (e.g., search for "Imotekh") and confirm weapon_group values are sequential (1,2,3) not all 1

    Step 3: Compare coverage report:
    - Read new scripts/data/coverage-report.json
    - Compare SM/NEC/DG percentages against baseline: SM 58.1%, NEC 79.7%, DG 50.7%, Overall 60.1%
    - Document the delta

    Step 4: Per D-13, update MIN_COVERAGE_PCT in build-unit-db.ts if coverage improves significantly:
    - Current threshold: 55% (MIN_COVERAGE_PCT constant near top of file)
    - If overall coverage exceeds 70%: raise to 65%
    - If overall coverage stays below 70%: leave at 55% or raise conservatively to just below the new overall percentage

    Step 5: Run build one more time to confirm it passes with the updated threshold.

    Step 6: Run full test suite to confirm no regressions: pnpm test
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/build-unit-db.ts 2>&1 | findstr /C:"coverage" /C:"Coverage" /C:"threshold" /C:"BUILD"</automated>
  </verify>
  <acceptance_criteria>
    - unit_database.json rebuilt successfully (build exits 0)
    - At least one spot-checked weapon has non-empty range field (was "" before)
    - At least one spot-checked weapon has non-empty keywords field (was "" before)
    - coverage-report.json shows updated percentages
    - MIN_COVERAGE_PCT updated per D-13 rules (raised if coverage > 70%, otherwise conservative)
    - Build passes with updated threshold (exit 0)
    - pnpm test passes (full suite green)
  </acceptance_criteria>
  <done>Database rebuilt with all pipeline fixes. Coverage improvement documented. MIN_COVERAGE_PCT adjusted appropriately. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- modifies dev-side build scripts and data files only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-02 | Tampering | aliases.json entries | accept | BPH-04 validates at build time; entries reviewed in git diff before commit |
| T-114-03 | Tampering | unit_database.json output | accept | Static JSON committed to git; changes visible in diff |
</threat_model>

<verification>
node --experimental-strip-types scripts/build-unit-db.ts exits 0.
pnpm test passes.
coverage-report.json shows improvements over baseline.
Weapon entries have populated range and keywords fields.
</verification>

<success_criteria>
- Targeted aliases added for genuine name mismatches (not Wahapedia-only units)
- Database rebuilt successfully with all Plan 01 + Plan 02 fixes
- Coverage percentages improved or stable for SM/NEC/DG
- Weapon range and keywords fields populated (was empty before)
- MIN_COVERAGE_PCT updated appropriately
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/114-pipeline-fixes-database-rebuild/114-02-SUMMARY.md` when done
</output>

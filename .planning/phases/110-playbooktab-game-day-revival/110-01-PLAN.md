---
phase: 110-playbooktab-game-day-revival
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/game-day/gameDayStore.ts
  - src/lib/computeUnitWarnings.ts
  - tests/game-day/gameDayStore.test.ts
  - tests/lib/computeUnitWarnings.test.ts
autonomous: true
requirements: [INT-03, INT-04]
must_haves:
  truths:
    - "OPG toggle state persists correctly across re-imports using stable unit_id:ability_name keys"
    - "Zustand persist migration drops old double-colon keys on version bump"
    - "Army list validation warns when DEDICATED TRANSPORT count exceeds non-transport non-character units"
    - "Army list validation warns when EPIC HERO units appear more than once (by udb_unit_id)"
    - "All new checks are soft warnings, not hard errors"
  artifacts:
    - path: "src/features/game-day/gameDayStore.ts"
      provides: "Zustand persist v1 migration dropping :: keys"
      contains: "version: 1"
    - path: "src/lib/computeUnitWarnings.ts"
      provides: "DEDICATED TRANSPORT and EPIC HERO soft warning checks"
      contains: "epic hero"
    - path: "tests/game-day/gameDayStore.test.ts"
      provides: "Migration and key format tests"
      contains: "migrate"
    - path: "tests/lib/computeUnitWarnings.test.ts"
      provides: "DEDICATED TRANSPORT and EPIC HERO test scenarios"
      contains: "EPIC HERO"
  key_links:
    - from: "src/lib/computeUnitWarnings.ts"
      to: "src/types/armyList.ts"
      via: "Pick<ArmyListUnitRow, ...> extended with udb_unit_id and udb_keywords"
      pattern: "udb_unit_id.*udb_keywords"
---

<objective>
Stabilize Game Day OPG key persistence and extend army list validation with canonical role/keyword checks.

Purpose: OPG keys currently use AUTOINCREMENT ability IDs that change on re-import, causing ghost toggles. Enhanced validation catches common list-building mistakes (too many transports, duplicate Epic Heroes) using canonical data already present on ArmyListUnitRow.

Output: Updated gameDayStore with persist migration, extended computeListWarnings with two new soft-warning checks, and comprehensive test coverage for both.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-playbooktab-game-day-revival/110-CONTEXT.md
@.planning/phases/110-playbooktab-game-day-revival/110-RESEARCH.md
@.planning/phases/110-playbooktab-game-day-revival/110-PATTERNS.md

<interfaces>
<!-- From src/features/game-day/gameDayStore.ts -->
export interface GameDayListState {
  cp: number;
  cpHistory: number[];
  startingCp: number;
  checklistItems: ChecklistItem[];
  usedAbilities: string[];
}

interface GameDayStore {
  listStates: Record<string, GameDayListState>;
  // ... actions
  toggleAbilityUsed: (listId: number, abilityKey: string) => void;
}

<!-- Current persist config at line 147: { name: "game-day-state" } — no version field -->

<!-- From src/lib/computeUnitWarnings.ts -->
export function computeListWarnings(
  context: WarningContext,
  units: Array<Pick<ArmyListUnitRow, "udb_role" | "unit_id">> = [],
): UnitWarnings

<!-- From src/types/armyList.ts lines 55-72 -->
export interface ArmyListUnitRow extends ArmyListUnit {
  udb_unit_id: string | null;
  udb_role: string | null;
  udb_keywords: string | null;
  // ... other fields
}

<!-- From tests/lib/computeUnitWarnings.test.ts — makeUnit factory already includes udb_unit_id, udb_keywords, udb_role fields -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zustand persist migration and validation test scaffolds</name>
  <files>src/features/game-day/gameDayStore.ts, tests/game-day/gameDayStore.test.ts, src/lib/computeUnitWarnings.ts, tests/lib/computeUnitWarnings.test.ts</files>
  <read_first>
    - src/features/game-day/gameDayStore.ts (current persist config, GameDayStore and GameDayListState types)
    - tests/game-day/gameDayStore.test.ts (existing test patterns, beforeEach reset)
    - src/lib/computeUnitWarnings.ts (current computeListWarnings signature and BATTLELINE check pattern)
    - tests/lib/computeUnitWarnings.test.ts (makeUnit factory, makeContext factory, existing test patterns)
    - src/types/armyList.ts (ArmyListUnitRow — confirm udb_unit_id, udb_keywords, udb_role fields exist)
  </read_first>
  <behavior>
    gameDayStore migration tests (extend existing describe):
    - Test: v0 state with usedAbilities containing "::" keys should have those keys removed after migration
    - Test: v0 state with usedAbilities containing single-colon keys should preserve them
    - Test: v0 state with empty usedAbilities should remain empty after migration
    - Test: v0 state with mixed old (::) and new (:) keys should keep only new keys

    computeListWarnings DEDICATED TRANSPORT tests (new describe block):
    - Test: returns soft warning when transport count (3) exceeds non-transport non-character count (2) at 2000pts
    - Test: no warning when transport count (1) does not exceed non-transport non-character count (3) at 2000pts
    - Test: no warning when transport count is 0
    - Test: skips check when pointsLimit is null
    - Test: ghost units (unit_id = null) are excluded from transport counting

    computeListWarnings EPIC HERO tests (new describe block):
    - Test: returns soft warning when two units share same udb_unit_id and both have "Epic Hero" in udb_keywords
    - Test: no warning when Epic Hero appears only once
    - Test: no warning when no units have "Epic Hero" keyword
    - Test: skips check when pointsLimit is null
    - Test: case-insensitive match on "epic hero" keyword
  </behavior>
  <action>
    **Part A — gameDayStore migration (per D-06, D-07, D-08):**

    In gameDayStore.ts, update the persist config object at line 147 from `{ name: "game-day-state" }` to include `version: 1` and a `migrate` function. The migrate function receives `(persistedState: unknown, fromVersion: number)` and returns `GameDayStore`. When `fromVersion === 0`, iterate `old.listStates` values and filter each `ls.usedAbilities` array to remove any string containing `"::"` (old double-colon format). Cast `persistedState as { listStates?: Record<string, GameDayListState> }` for the v0 shape. Return `persistedState as GameDayStore` for any other version.

    In gameDayStore.test.ts, add a new `describe("persist migration")` block. Use `useGameDayStore.setState(...)` to inject v0-shaped state with `usedAbilities` arrays containing `"::"` keys, then call the migrate function directly. The migrate function can be tested by importing the store and manually calling setState with pre-migration data, then checking the state after the store re-initializes. Alternative: export a standalone `migrateGameDayState` function from gameDayStore.ts for direct testing (Claude's discretion on approach).

    **Part B — computeListWarnings extension (per D-09, D-10, D-11):**

    In computeUnitWarnings.ts, extend the `computeListWarnings` signature to add `"udb_unit_id"` and `"udb_keywords"` to the Pick type (line 83). After the existing BATTLELINE check block (after line 109, before `return { hard, soft }`), add two new check blocks:

    1. DEDICATED TRANSPORT cap: guarded by `context.pointsLimit !== null`. Filter linked units (`unit_id !== null`). Count those with `udb_role?.toLowerCase() === "dedicated transport"` as transportCount. Count those where role is NOT "dedicated transport" AND NOT "character" as nonTransportNonCharacterCount. If `transportCount > 0 && transportCount > nonTransportNonCharacterCount`, push soft warning string matching UI-SPEC copy exactly.

    2. EPIC HERO uniqueness: guarded by `context.pointsLimit !== null`. Filter units where `unit_id !== null` AND `udb_keywords?.toLowerCase().includes("epic hero")`. Map to `udb_unit_id`, filter nulls. Check for duplicates via `some((id, idx) => arr.indexOf(id) !== idx)`. If duplicates found, push soft warning string matching UI-SPEC copy exactly.

    Warning strings must be exactly: "DEDICATED TRANSPORT count exceeds non-transport, non-character units" and "EPIC HERO must be unique (duplicate detected)".

    In computeUnitWarnings.test.ts, add two new describe blocks inside the existing `computeListWarnings` describe. Each test case uses the existing `makeUnit` and `makeContext` factories. The makeUnit factory already has `udb_unit_id`, `udb_keywords`, and `udb_role` fields — pass overrides for these. The new Pick type requires passing objects with `udb_unit_id` and `udb_keywords` fields to `computeListWarnings` — use makeUnit to ensure type compatibility.
  </action>
  <verify>
    <automated>pnpm test -- tests/game-day/gameDayStore.test.ts tests/lib/computeUnitWarnings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - gameDayStore.ts persist config has `version: 1` and a `migrate` property
    - migrate function filters out strings containing "::" from usedAbilities arrays in listStates
    - computeListWarnings signature Pick includes "udb_unit_id" and "udb_keywords" alongside existing "udb_role" and "unit_id"
    - DEDICATED TRANSPORT soft warning fires when transportCount > nonTransportNonCharacterCount, guarded by pointsLimit !== null
    - EPIC HERO soft warning fires when duplicate udb_unit_id found among units with "epic hero" in udb_keywords, guarded by pointsLimit !== null
    - Warning strings are exactly "DEDICATED TRANSPORT count exceeds non-transport, non-character units" and "EPIC HERO must be unique (duplicate detected)"
    - All existing tests in both files still pass (no regressions)
    - New migration tests cover: old keys removed, new keys preserved, empty state unchanged, mixed keys filtered
    - New validation tests cover: warning fires, no false positive, null pointsLimit skips, ghost units excluded, case-insensitive
  </acceptance_criteria>
  <done>gameDayStore migration passes all new tests; computeListWarnings extended with DEDICATED TRANSPORT and EPIC HERO checks passing all new tests; existing tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage -> Zustand store | Persisted state from prior app versions crosses into current store via migrate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Tampering | gameDayStore migrate | accept | State is client-local localStorage only (no PII, no server sync). Malformed JSON at worst resets OPG toggles — acceptable per D-07. |
| T-110-02 | Information Disclosure | computeListWarnings | accept | Warning strings contain no user data — only static text and counts derived from already-loaded DB data. |
</threat_model>

<verification>
pnpm test -- tests/game-day/gameDayStore.test.ts tests/lib/computeUnitWarnings.test.ts
</verification>

<success_criteria>
- gameDayStore persist migration works (old :: keys dropped, new : keys preserved)
- DEDICATED TRANSPORT soft warning fires correctly with no false positives
- EPIC HERO uniqueness soft warning fires correctly with no false positives
- All existing tests pass without modification
</success_criteria>

<output>
Create `.planning/phases/110-playbooktab-game-day-revival/110-01-SUMMARY.md` when done
</output>

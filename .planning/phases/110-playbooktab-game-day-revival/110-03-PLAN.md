---
phase: 110-playbooktab-game-day-revival
plan: 03
type: execute
wave: 2
depends_on: [110-01, 110-02]
files_modified: []
autonomous: false
requirements: [INT-01, INT-02, INT-03, INT-04]
must_haves:
  truths:
    - "All four INT requirements are verified end-to-end in the running app"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 110 integration work in the running application.

Purpose: Confirm that canonical data flows correctly through PlaybookTab, Game Day weapons are visible and collapsible, OPG toggles persist with stable keys, and validation warnings appear for transport/hero composition errors.

Output: Human verification of all four INT requirements.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/110-playbooktab-game-day-revival/110-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite validation</name>
  <files></files>
  <read_first>
    - tests/game-day/gameDayStore.test.ts (confirm migration tests exist)
    - tests/lib/computeUnitWarnings.test.ts (confirm DEDICATED TRANSPORT and EPIC HERO tests exist)
  </read_first>
  <action>
    Run the full test suite to confirm no regressions from both plans. This validates INT-03 migration logic and INT-04 validation logic at the unit test level.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass (zero failures)
    - gameDayStore.test.ts includes migration tests
    - computeUnitWarnings.test.ts includes DEDICATED TRANSPORT and EPIC HERO tests
  </acceptance_criteria>
  <done>Full test suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 110 integration work:
    - INT-01: PlaybookTab shows canonical stats/weapons/abilities for linked units
    - INT-02: Game Day UnitAbilityCard has a collapsible Weapons section (collapsed by default)
    - INT-03: OPG toggle keys use stable unit_id:ability_name format
    - INT-04: Army list validation shows soft warnings for too many Dedicated Transports and duplicate Epic Heroes
  </what-built>
  <how-to-verify>
    Run `pnpm tauri dev` and verify each requirement:

    **INT-01 — PlaybookTab canonical data:**
    1. Navigate to Collection, open any unit that is linked to a canonical datasheet (has a udb_unit_id)
    2. Click the Playbook tab
    3. Verify the stat block (M/T/Sv/W/Ld/OC) shows numeric values — not empty dashes
    4. Verify the Weapons collapsible shows ranged and/or melee weapon profiles
    5. Verify the Datasheet Abilities collapsible shows ability names and descriptions

    **INT-02 — Game Day weapon profiles:**
    1. Navigate to Army Lists, open a list with linked units
    2. Click "Game Day" to enter Game Day mode
    3. Expand a unit card (click the card header)
    4. Verify a "Weapons" section appears between "Once Per Game" and "Abilities" sections
    5. Verify the Weapons section is collapsed by default — click to expand
    6. Verify ranged/melee weapon tables show Name, Range, Attacks, BS/WS, Strength, AP, Damage columns

    **INT-03 — OPG key stability:**
    1. In Game Day, find a unit with a "Once Per Game" ability
    2. Click "Available" to mark it as "Used"
    3. Close and reopen Game Day — verify the toggle state persists
    4. (Key format verification is handled by automated tests)

    **INT-04 — Enhanced validation:**
    1. Create or edit an army list at 2000pts
    2. Add more Dedicated Transport units than non-transport, non-character units
    3. Verify a soft warning appears about Dedicated Transport count
    4. Add the same Epic Hero unit twice (if possible with collection)
    5. Verify a soft warning appears about Epic Hero uniqueness
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — verification only.

## STRIDE Threat Register

No new threats — verification only.
</threat_model>

<verification>
pnpm test (all tests pass)
Visual verification of all four INT requirements in running app
</verification>

<success_criteria>
- Full test suite green
- Human confirms all four INT requirements work correctly in the running app
</success_criteria>

<output>
Create `.planning/phases/110-playbooktab-game-day-revival/110-03-SUMMARY.md` when done
</output>

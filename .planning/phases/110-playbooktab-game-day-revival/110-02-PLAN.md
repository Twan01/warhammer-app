---
phase: 110-playbooktab-game-day-revival
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/units/WeaponTable.tsx
  - src/features/units/PlaybookDatasheet.tsx
  - src/features/units/PlaybookTab.tsx
  - src/features/game-day/UnitAbilityCard.tsx
autonomous: true
requirements: [INT-01, INT-02, INT-03]
must_haves:
  truths:
    - "PlaybookTab shows canonical stat block, weapon profiles, and ability text for linked units — no empty sections"
    - "Game Day UnitAbilityCard shows a collapsible Weapons section between OPG and regular abilities"
    - "Weapons section defaults to collapsed in Game Day"
    - "OPG toggle keys use unit_id:ability_name format (single colon, no ability.id)"
    - "WeaponTable is a shared component used by both PlaybookDatasheet and UnitAbilityCard"
  artifacts:
    - path: "src/features/units/WeaponTable.tsx"
      provides: "Shared WeaponTable component extracted from PlaybookDatasheet"
      exports: ["WeaponTable"]
      min_lines: 30
    - path: "src/features/units/PlaybookDatasheet.tsx"
      provides: "PlaybookDatasheet importing shared WeaponTable"
      contains: "import { WeaponTable }"
    - path: "src/features/game-day/UnitAbilityCard.tsx"
      provides: "Weapons collapsible section and stable OPG keys"
      contains: "defaultOpen={false}"
  key_links:
    - from: "src/features/units/PlaybookDatasheet.tsx"
      to: "src/features/units/WeaponTable.tsx"
      via: "named import"
      pattern: "import.*WeaponTable.*from.*WeaponTable"
    - from: "src/features/game-day/UnitAbilityCard.tsx"
      to: "src/features/units/WeaponTable.tsx"
      via: "named import"
      pattern: "import.*WeaponTable.*from.*WeaponTable"
    - from: "src/features/game-day/UnitAbilityCard.tsx"
      to: "src/features/game-day/gameDayStore.ts"
      via: "OPG key format unit_id:ability_name"
      pattern: "unit\\.unit_id.*:.*ability\\.name"
---

<objective>
Extract WeaponTable to a shared component, add weapon profiles to Game Day cards, fix OPG key format, and verify PlaybookTab canonical data rendering.

Purpose: Game Day users need tactical weapon data (range, attacks, strength, AP, damage) accessible during play. The WeaponTable already exists in PlaybookDatasheet — extracting it enables reuse. OPG keys must use stable ability names instead of reassignable AUTOINCREMENT IDs. PlaybookTab must reliably show canonical data for linked units.

Output: New shared WeaponTable.tsx, updated PlaybookDatasheet (import change), updated UnitAbilityCard (weapons section + OPG key fix), audited PlaybookTab (stat fallback for linked units).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-playbooktab-game-day-revival/110-CONTEXT.md
@.planning/phases/110-playbooktab-game-day-revival/110-RESEARCH.md
@.planning/phases/110-playbooktab-game-day-revival/110-PATTERNS.md
@.planning/phases/110-playbooktab-game-day-revival/110-UI-SPEC.md

<interfaces>
<!-- From src/db/queries/unitDatabase.ts -->
export interface UdbWeapon {
  unit_id: string;
  name: string;
  range: string | null;
  attacks: string | null;
  skill: string | null;
  strength: string | null;
  ap: string | null;
  damage: string | null;
  keywords: string | null;
  category: string;       // "Ranged" or "Melee"
  weapon_group: string;
  line_order: number;
}

export interface UdbUnitDetail {
  unit_id: string;
  name: string;
  faction_id: string;
  role: string | null;
  models: Array<{ M: string|null; T: string|null; Sv: string|null; W: string|null; Ld: string|null; OC: string|null; /* ... */ }>;
  weapons: UdbWeapon[];
  abilities: UdbAbility[];
  keywords: UdbKeyword[];
}

<!-- From src/features/units/PlaybookDatasheet.tsx — WeaponTable is local function at lines 108-143 -->
<!-- From src/features/game-day/UnitAbilityCard.tsx — OPG key at line 50: `${unit.unit_id}::${ability.id ?? ability.name}` -->
<!-- Collapsible, CollapsibleTrigger, CollapsibleContent already imported in UnitAbilityCard.tsx -->
<!-- ChevronDown already imported in UnitAbilityCard.tsx -->

<!-- From src/hooks/useDatasheet.ts -->
export function useDatasheet(unitId: number | undefined): UseQueryResult<UdbUnitDetail | null>
<!-- Already called in UnitAbilityCard.tsx line 38: const { data: datasheet } = useDatasheet(unitIdOrUndefined) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract WeaponTable and update PlaybookDatasheet import</name>
  <files>src/features/units/WeaponTable.tsx, src/features/units/PlaybookDatasheet.tsx</files>
  <read_first>
    - src/features/units/PlaybookDatasheet.tsx (full file — WeaponTable at lines 108-143, imports at top, SECTION_LABEL_CLASS at line 6-7, usage at lines 40 and 46)
    - src/db/queries/unitDatabase.ts (UdbWeapon type definition — confirm fields)
  </read_first>
  <action>
    Create src/features/units/WeaponTable.tsx as a new file. Import `UdbWeapon` type from `@/db/queries/unitDatabase`. Export a named function `WeaponTable` with props `{ weapons: UdbWeapon[]; statLabel: "BS" | "WS" }`. The function body is an exact copy of the local `WeaponTable` function from PlaybookDatasheet.tsx lines 108-143 (the 7-column grid with header row, data rows, and keywords sub-row). No changes to JSX, classNames, or logic.

    In PlaybookDatasheet.tsx:
    - Add import: `import { WeaponTable } from "@/features/units/WeaponTable";` after the existing imports (after line 4)
    - Remove the local `function WeaponTable` block at lines 107-143 (the comment line 107 and the function body through line 143)
    - Keep the `UdbWeapon` import in PlaybookDatasheet.tsx ONLY if it is used elsewhere — check if any other code references `UdbWeapon` directly. If not, remove it from the import list. The type is: `import type { UdbUnitDetail, UdbWeapon, UdbAbility }` at line 4. `UdbWeapon` is only used by WeaponTable (now moved out), so remove `UdbWeapon` from line 4, leaving `import type { UdbUnitDetail, UdbAbility }`.
    - The two existing `<WeaponTable>` JSX usages at lines 40 and 46 remain unchanged — they now resolve via the new import.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/features/units/WeaponTable.tsx exists and exports a named function WeaponTable
    - WeaponTable accepts props { weapons: UdbWeapon[]; statLabel: "BS" | "WS" }
    - PlaybookDatasheet.tsx imports WeaponTable from "@/features/units/WeaponTable"
    - PlaybookDatasheet.tsx no longer contains a local function WeaponTable definition
    - PlaybookDatasheet.tsx line 4 import no longer includes UdbWeapon (moved to WeaponTable.tsx)
    - pnpm build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>WeaponTable extracted as shared component; PlaybookDatasheet imports it; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Game Day weapons section, OPG key fix, and PlaybookTab audit</name>
  <files>src/features/game-day/UnitAbilityCard.tsx, src/features/units/PlaybookTab.tsx</files>
  <read_first>
    - src/features/game-day/UnitAbilityCard.tsx (full file — OPG key at line 50, Collapsible imports lines 6-10, existing sections layout lines 86-166)
    - src/features/units/PlaybookTab.tsx (full file — statValue function at line 123, importedStatValue at lines 135-141, hasDatasheetLink at line 83, useEffect init at lines 86-96)
    - src/features/units/WeaponTable.tsx (just-created file — confirm export name)
    - .planning/phases/110-playbooktab-game-day-revival/110-UI-SPEC.md (visual spec for weapons section positioning and copy)
    - .planning/phases/110-playbooktab-game-day-revival/110-PATTERNS.md (UnitAbilityCard section — weapons collapsible pattern, anti-pattern note about nested triggers)
  </read_first>
  <action>
    **UnitAbilityCard.tsx changes (INT-02 + INT-03):**

    1. Add import: `import { WeaponTable } from "@/features/units/WeaponTable";` after the existing imports.

    2. OPG key fix (per D-06, D-08): On line 50, change the key computation from `key: \`${unit.unit_id}::${ability.id ?? ability.name}\`` to `key: \`${unit.unit_id}:${ability.name}\``. Single colon separator, ability.name only (drop ability.id entirely).

    3. Add weapons section between the OPG abilities block (ends around line 118 closing div) and the regular abilities block (starts at line 121). The weapons section renders inside the existing `CollapsibleContent` (line 86), within the `!dsLoading && !noteLoading` conditional.

    Weapons section structure: Compute `rangedWeapons` and `meleeWeapons` from `datasheet?.weapons` using the same filter logic as PlaybookDatasheet.tsx (ranged: `w.category === "Ranged"`, melee: `w.category === "Melee" || (w.category !== "Ranged" && w.range === "Melee")`). These can be computed inside a useMemo or inline.

    Wrap in a conditional: only render if rangedWeapons.length > 0 or meleeWeapons.length > 0. Use a `<div className="flex flex-col gap-2 pt-2">` container with a nested `<Collapsible defaultOpen={false}>`. The trigger button uses `className="flex items-center justify-between w-full py-1 text-left"` with a span "Weapons" using `className="text-xs font-semibold uppercase tracking-wider text-muted-foreground"` and a `<ChevronDown className="h-3 w-3 text-muted-foreground transition-transform duration-200 data-[state=open]:rotate-180" />`.

    Inside `<CollapsibleContent>`, render ranged and melee sub-sections each with a label span ("Ranged"/"Melee") using `className="text-[10px] font-semibold text-muted-foreground uppercase"` and a `<WeaponTable>` component call. Use `statLabel="BS"` for ranged, `statLabel="WS"` for melee.

    IMPORTANT: The inner collapsible trigger must NOT use `asChild` with the same SVG last-child rotation pattern as the outer card trigger (line 75). Use a standalone `<CollapsibleTrigger asChild>` wrapping a `<button>` element. The inner ChevronDown uses `h-3 w-3` (smaller than outer's `h-4 w-4`) to differentiate visual hierarchy.

    4. Update the `hasAbilities` variable (line 70) to also account for weapons data. Add a `hasWeapons` computed value: `const hasWeapons = (datasheet?.weapons ?? []).length > 0;`. Update the empty-state condition at line 158 from `!hasAbilities && !hasNotes` to `!hasAbilities && !hasWeapons && !hasNotes`.

    **PlaybookTab.tsx audit (INT-01, per D-01, D-02):**

    The `statValue` function at line 123 returns local state values (from strategy_notes). For linked units where strategy_notes stats are null but canonical data exists, the stat block shows empty values. Fix: modify `statValue` to fall back to `importedStatValue(key)` when the local value is null and `hasDatasheetLink` is true.

    Change the `statValue` function body: after the existing switch statement that returns move/toughness/saveStat/wounds/leadership/objectiveControl, capture the result in a `const local` variable. If `local === null && hasDatasheetLink`, return `importedStatValue(key)` instead. This ensures linked units always show canonical stats even if the user never explicitly saved.

    Per D-02: this does NOT overwrite user-entered values. The fallback only fires when the local value is null (user has not entered anything). If user has a non-null value that differs from canonical, their value is preserved.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - UnitAbilityCard.tsx imports WeaponTable from "@/features/units/WeaponTable"
    - OPG key at former line 50 uses template literal with single colon and ability.name only: `${unit.unit_id}:${ability.name}`
    - No occurrence of "::" in UnitAbilityCard.tsx OPG key computation
    - Weapons section exists between OPG abilities and regular abilities inside CollapsibleContent
    - Weapons Collapsible has defaultOpen={false}
    - Inner ChevronDown uses h-3 w-3 (not h-4 w-4)
    - WeaponTable called with statLabel="BS" for ranged and statLabel="WS" for melee
    - Empty state condition accounts for hasWeapons (weapons data prevents empty message)
    - PlaybookTab.tsx statValue function falls back to importedStatValue when local is null and hasDatasheetLink is true
    - pnpm build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>Game Day shows collapsible weapons section; OPG keys use stable format; PlaybookTab shows canonical stats for linked units with null strategy notes; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite udb_* data -> React rendering | Canonical data from trusted import pipeline rendered in UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-03 | Spoofing | WeaponTable | accept | Data sourced from bundled SQLite database (not user input); XSS not possible in React text nodes |
| T-110-04 | Tampering | OPG key format | accept | Keys are client-local localStorage; ability.name sourced from canonical DB — no user-controlled input in key |
</threat_model>

<verification>
pnpm build && pnpm test
</verification>

<success_criteria>
- WeaponTable is a shared exported component in src/features/units/WeaponTable.tsx
- PlaybookDatasheet imports WeaponTable (no local definition)
- UnitAbilityCard shows collapsed weapons section with ranged/melee WeaponTables
- OPG keys use unit_id:ability_name (single colon, no ability.id)
- PlaybookTab statValue falls back to canonical data for linked units with null stats
- TypeScript build passes with no errors
</success_criteria>

<output>
Create `.planning/phases/110-playbooktab-game-day-revival/110-02-SUMMARY.md` when done
</output>

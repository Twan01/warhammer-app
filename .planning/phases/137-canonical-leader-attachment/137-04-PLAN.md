---
phase: 137-canonical-leader-attachment
plan: 04
type: execute
wave: 4
depends_on: ["137-03"]
files_modified:
  - src/db/queries/bsdataExtended.ts
autonomous: true
requirements: [PLAY-03]

must_haves:
  truths:
    - "The dead synced leader-target writer replaceSyncedLeaderTargets is removed (zero references after the canonical repoint)"
    - "No production code writes to synced_leader_targets after this plan"
    - "The rules-hub consumer (getLeaderTargetsByFaction / SyncedLeaderTargetRow via useLeaderTargetsByFaction) still compiles — it remains an out-of-scope separate consumer"
    - "The synced_leader_targets TABLE is left in place (no drop migration this phase)"
  artifacts:
    - path: "src/db/queries/bsdataExtended.ts"
      provides: "module with the dead replaceSyncedLeaderTargets writer removed"
      contains: "getLeaderTargetsByFaction"
  key_links:
    - from: "src/db/queries/bsdataExtended.ts"
      to: "synced_leader_targets table"
      via: "no remaining INSERT/DELETE writer (only the read-path getLeaderTargetsByFaction remains for rules-hub)"
      pattern: "getLeaderTargetsByFaction"
---

<objective>
Honest dead-code cleanup of the now-orphaned synced leader-target WRITER path (D-10).
After Plan 03 repointed the army-list leader UI onto canonical data, the name-keyed
`replaceSyncedLeaderTargets` writer has zero references — remove it. Leave the empty
`synced_leader_targets` TABLE in place (no drop migration — avoids a second parity-gate
bump in one phase; the table is inert).

Purpose: Remove dead code without speculative schema churn. CRITICAL SCOPE CORRECTION:
the research's D-10 also names `getLeaderTargetsByFaction` / `SyncedLeaderTargetRow` for
removal, but those are STILL referenced by an out-of-scope separate consumer — the
rules-hub `DatasheetPointsTab` (via `useLeaderTargetsByFaction` in `useBsdataFaction.ts`).
Per D-10's explicit "if unreferenced" condition, they must STAY this phase (removing them
would break the rules-hub compile). Only the genuinely-unreferenced
`replaceSyncedLeaderTargets` is removed here.

Output: a smaller `bsdataExtended.ts` with the dead writer gone; rules-hub read path intact;
table untouched; full suite + parity gate still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/137-canonical-leader-attachment/137-CONTEXT.md
@.planning/phases/137-canonical-leader-attachment/137-RESEARCH.md

<interfaces>
<!-- bsdataExtended.ts:
     - replaceSyncedLeaderTargets(rows: BsdataLeaderTarget[], syncedAt) — lines ~98-122.
       VERIFIED zero callers in src/ (only its own definition). Contains the only
       DELETE/INSERT into synced_leader_targets. -> REMOVE.
     - getLeaderTargetsByFaction(factionId) + interface SyncedLeaderTargetRow — lines ~185-202.
       STILL referenced by useBsdataFaction.ts (useLeaderTargetsByFaction) -> DatasheetPointsTab
       (rules-hub, out of scope). -> KEEP.
     - BsdataLeaderTarget type is owned by src/lib/parseBsdataExtended.ts and still used there;
       removing replaceSyncedLeaderTargets must NOT remove that type — only drop its import name
       here if no other use of the import remains in bsdataExtended.ts. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the dead replaceSyncedLeaderTargets writer (keep rules-hub read path + table)</name>
  <read_first>
    - src/db/queries/bsdataExtended.ts (lines ~98-122 replaceSyncedLeaderTargets; lines ~185-202 getLeaderTargetsByFaction + SyncedLeaderTargetRow to KEEP; line 6 the BsdataLeaderTarget import)
    - src/lib/parseBsdataExtended.ts (owner of BsdataLeaderTarget — do NOT touch)
    - src/hooks/useBsdataFaction.ts (line 17 getLeaderTargetsByFaction import; line 61 useLeaderTargetsByFaction — the rules-hub consumer keeping the read path alive)
    - .planning/phases/137-canonical-leader-attachment/137-CONTEXT.md (D-10 — "if unreferenced"; leave synced_leader_targets table in place)
  </read_first>
  <action>
    First CONFIRM zero callers with `grep -rn "replaceSyncedLeaderTargets" src/` — expect exactly one
    hit (its own definition in `bsdataExtended.ts`). If any OTHER caller exists, STOP and do not remove.

    Delete the `replaceSyncedLeaderTargets` function (lines ~98-122) from `src/db/queries/bsdataExtended.ts`.
    If, after removal, the `BsdataLeaderTarget` import (line 6) is no longer used elsewhere in this file,
    remove that import NAME from the import statement (but leave the type defined in
    `parseBsdataExtended.ts` untouched).

    KEEP `getLeaderTargetsByFaction` and the `SyncedLeaderTargetRow` interface (lines ~185-202) — still
    consumed by the rules-hub (`useLeaderTargetsByFaction` -> `DatasheetPointsTab`), which is out of this
    phase's scope. Do NOT add a drop migration for the `synced_leader_targets` table (D-10).

    Confirm no production code writes to `synced_leader_targets` anymore: after removal,
    `grep -rn "synced_leader_targets" src/` should show only the read query inside
    `getLeaderTargetsByFaction` (plus possibly a doc comment) — no INSERT/DELETE statements.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && if grep -rn "replaceSyncedLeaderTargets\|INSERT INTO synced_leader_targets\|DELETE FROM synced_leader_targets" src/; then echo "FAIL: writer remains"; exit 1; fi; pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "replaceSyncedLeaderTargets" src/` returns no matches.
    - `grep -rn "INSERT INTO synced_leader_targets\|DELETE FROM synced_leader_targets" src/` returns no matches (no writer remains).
    - `getLeaderTargetsByFaction` and `SyncedLeaderTargetRow` still exist in `bsdataExtended.ts` (rules-hub read path preserved).
    - No drop migration for `synced_leader_targets` was added (`ls src-tauri/migrations/` shows 050 as the highest).
    - `pnpm build` (tsc + vite) compiles with no errors.
  </acceptance_criteria>
  <done>Dead writer removed; rules-hub read path + table intact; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite + parity regression gate after cleanup</name>
  <read_first>
    - .planning/phases/137-canonical-leader-attachment/137-VALIDATION.md (Sampling: per-wave-merge `pnpm test`; pre-verify-work full suite + check:version green)
    - tests/data-layer/migration-parity.test.ts (parity gate — must stay green)
  </read_first>
  <action>
    Run the full TypeScript test suite and the parity gate to prove the cleanup introduced no regression
    across the phase's surfaces (data-layer migration 050, leader-targets query/test, LeaderAttachmentSheet
    component test, and the untouched rules-hub path). This is the phase-gate sampling run per 137-VALIDATION.md.
    No watch flag.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm check:version && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check:version` exits 0 (migration 050 parity intact; no CR bytes; version match).
    - `pnpm test` (full Vitest suite) exits green — including `tests/data-layer/leader-targets.test.ts`, `tests/data-layer/migration-parity.test.ts`, and `tests/features/army-lists/LeaderAttachmentSheet.test.tsx`.
  </acceptance_criteria>
  <done>Full suite + parity gate green after dead-code removal — phase ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure code deletion; no new data flow or input surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-09 | Tampering | accidental over-removal breaking rules-hub | mitigate | Explicit KEEP of `getLeaderTargetsByFaction`/`SyncedLeaderTargetRow`; `pnpm build` + full `pnpm test` confirm rules-hub still compiles/passes |
| T-137-10 | Repudiation | leaving a live writer to a "dead" table | mitigate | grep gate asserts zero remaining INSERT/DELETE into `synced_leader_targets` |
| T-137-SC | Tampering | npm installs | accept | No new packages this phase (RESEARCH §Package Legitimacy Audit: N/A) |

No high-severity threats: deletion-only change with no new input, auth, network, or session surface.
</threat_model>

<verification>
- `grep` confirms `replaceSyncedLeaderTargets` and all `synced_leader_targets` writers are gone.
- `pnpm build` compiles (rules-hub read path preserved).
- `pnpm check:version` exits 0 and `pnpm test` full suite green.
</verification>

<success_criteria>
- The dead synced leader-target writer is removed; no production code writes to synced_leader_targets.
- The rules-hub read path (getLeaderTargetsByFaction / SyncedLeaderTargetRow) still compiles — kept per D-10's "if unreferenced" condition.
- The synced_leader_targets table is left in place (no drop migration).
- Full suite + parity gate green.
</success_criteria>

<output>
Create `.planning/phases/137-canonical-leader-attachment/137-04-SUMMARY.md` when done.
</output>

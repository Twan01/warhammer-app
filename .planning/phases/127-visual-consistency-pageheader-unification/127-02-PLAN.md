---
phase: 127-visual-consistency-pageheader-unification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/spending/SpendingPage.tsx
  - src/features/data-health/DataHealthPage.tsx
  - src/app/settings/page.tsx
  - src/features/paints/PaintsPage.tsx
  - src/features/factions/FactionsEmptyState.tsx
autonomous: true
requirements: [VIS-03, VIS-04, VIS-05, VIS-08, VIS-09]

must_haves:
  truths:
    - "Spending page uses p-6 gap-6 padding on all three layout branches (loading, error, data)"
    - "Spending page section headings use text-sm font-semibold uppercase tracking-widest text-muted-foreground"
    - "Data Health page root container uses flex flex-col gap-6 p-6 (not space-y-6)"
    - "Data Health page section headings use text-sm font-semibold uppercase tracking-widest text-muted-foreground"
    - "Settings page root container uses flex flex-col gap-6 p-6 (not space-y-6)"
    - "Paints filtered empty state shows icon-pill pattern with icon, heading, description, and Clear filters button"
    - "FactionsEmptyState description paragraph has max-w-xs constraint"
  artifacts:
    - path: "src/features/spending/SpendingPage.tsx"
      provides: "Standard p-6 gap-6 layout and standard section headings"
    - path: "src/features/paints/PaintsPage.tsx"
      provides: "Icon-pill filtered empty state"
      contains: "rounded-xl bg-muted/40 p-4"
    - path: "src/features/factions/FactionsEmptyState.tsx"
      provides: "max-w-xs on description paragraph"
      contains: "max-w-xs"
  key_links: []
---

<objective>
Normalize page spacing to the standard p-6 gap-6 layout container on Spending, Data Health, and Settings pages. Standardize section headings in Spending and Data Health to the Dashboard pattern. Fix the two empty state patterns (Paints icon-pill, Factions max-w-xs).

Purpose: Every page container, section heading, and empty state follows the same visual rhythm.
Output: 5 files updated with consistent spacing, headings, and empty state patterns.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-visual-consistency-pageheader-unification/127-CONTEXT.md
@.planning/phases/127-visual-consistency-pageheader-unification/127-PATTERNS.md
@.planning/phases/127-visual-consistency-pageheader-unification/127-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spacing and heading standardization on Spending, Data Health, and Settings</name>
  <files>src/features/spending/SpendingPage.tsx, src/features/data-health/DataHealthPage.tsx, src/app/settings/page.tsx</files>
  <read_first>
    src/features/spending/SpendingPage.tsx
    src/features/data-health/DataHealthPage.tsx
    src/app/settings/page.tsx
    src/features/dashboard/DashboardPage.tsx
  </read_first>
  <action>
    Per D-04: In SpendingPage.tsx, fix ALL THREE layout branches (loading, error, data). Each has a root div with `max-w-3xl mx-auto p-8`. The changes:
    - Loading branch: change `className="max-w-3xl mx-auto p-8 flex flex-col gap-12"` to `className="flex flex-col gap-6 p-6"` (remove max-w-3xl mx-auto, change p-8 to p-6, change gap-12 to gap-6)
    - Error branch: change `className="max-w-3xl mx-auto p-8"` to `className="p-6"` (remove max-w-3xl mx-auto, change p-8 to p-6)
    - Data branch: change `className="max-w-3xl mx-auto p-8 flex flex-col gap-12"` to `className="flex flex-col gap-6 p-6"` (remove max-w-3xl mx-auto, change p-8 to p-6, change gap-12 to gap-6)

    Per D-03 (VIS-03 continuation): In SpendingPage.tsx, standardize section headings:
    - "Monthly Trend" h2 (className="text-base font-semibold") to `p` with className="text-sm font-semibold uppercase tracking-widest text-muted-foreground"
    - "Breakdown" h2 (className="text-base font-semibold") to `p` with className="text-sm font-semibold uppercase tracking-widest text-muted-foreground"

    Per D-05: In DataHealthPage.tsx, change root container div from `className="p-6 space-y-6"` to `className="flex flex-col gap-6 p-6"`. Do NOT change any inner space-y-4 wrappers.

    Per D-03 (VIS-03 continuation): In DataHealthPage.tsx, standardize section headings:
    - "Table Counts" h2 (className="text-lg font-semibold") to `p` with className="text-sm font-semibold uppercase tracking-widest text-muted-foreground"
    - "Safety Backups" h2 (className="text-lg font-semibold") to `p` with className="text-sm font-semibold uppercase tracking-widest text-muted-foreground"

    Per D-05: In settings/page.tsx, change root container div from `className="p-6 space-y-6"` to `className="flex flex-col gap-6 p-6"`.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - SpendingPage.tsx has zero occurrences of "max-w-3xl"
    - SpendingPage.tsx has zero occurrences of "p-8"
    - SpendingPage.tsx has zero occurrences of "gap-12"
    - SpendingPage.tsx has zero h2 elements with "text-base font-semibold"
    - SpendingPage.tsx contains 2 p elements with "text-sm font-semibold uppercase tracking-widest text-muted-foreground"
    - DataHealthPage.tsx root div className is "flex flex-col gap-6 p-6" (no space-y-6)
    - DataHealthPage.tsx still contains inner "space-y-4" wrappers (unchanged)
    - DataHealthPage.tsx has zero h2 elements with "text-lg font-semibold"
    - DataHealthPage.tsx contains 2 p elements with "text-sm font-semibold uppercase tracking-widest text-muted-foreground"
    - settings/page.tsx root div className is "flex flex-col gap-6 p-6" (no space-y-6)
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>Spending, Data Health, and Settings pages all use the standard p-6 gap-6 flex layout container. Section headings in Spending and Data Health match the Dashboard pattern. No max-w-3xl centering remains on Spending. Inner DataHealth wrappers are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Empty state pattern fixes for Paints and Factions</name>
  <files>src/features/paints/PaintsPage.tsx, src/features/factions/FactionsEmptyState.tsx</files>
  <read_first>
    src/features/paints/PaintsPage.tsx
    src/features/factions/FactionsEmptyState.tsx
  </read_first>
  <action>
    Per D-06: In PaintsPage.tsx, find the filtered empty state block (the div with className="flex flex-col items-start gap-2" containing "No paints match your filters" text and a Clear filters ghost button). Replace the entire block with the icon-pill pattern:
    - Outer div: className="flex flex-col items-center justify-center gap-3 py-16 text-center"
    - Icon pill: div with className="rounded-xl bg-muted/40 p-4" containing a Palette icon (from lucide-react) with className="h-8 w-8 text-muted-foreground"
    - Text wrapper: div with className="space-y-1"
    - Heading: p with className="text-base font-semibold" and text "No paints match your filters"
    - Description: p with className="text-sm text-muted-foreground max-w-xs" and text "Try adjusting or clearing your filters to see more results."
    - Button: existing Clear filters Button with variant="ghost" size="sm", add className="mt-2", keep the existing onClick handler (clearAll or equivalent)
    - Add Palette to the existing lucide-react import statement in PaintsPage.tsx

    Per D-07: In FactionsEmptyState.tsx, find the description p element (className="text-sm text-muted-foreground") and add max-w-xs to its className, making it "text-sm text-muted-foreground max-w-xs".
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - PaintsPage.tsx contains "rounded-xl bg-muted/40 p-4" (icon-pill wrapper)
    - PaintsPage.tsx contains `Palette` in the lucide-react import
    - PaintsPage.tsx contains "py-16" on the filtered empty state outer div
    - PaintsPage.tsx filtered empty state has "text-base font-semibold" heading
    - PaintsPage.tsx filtered empty state description p element has max-w-xs in its className
    - PaintsPage.tsx has zero occurrences of "flex flex-col items-start gap-2" for the filtered state
    - FactionsEmptyState.tsx description p element has "max-w-xs" in its className
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>Paints filtered empty state uses the icon-pill pattern matching FactionsEmptyState. FactionsEmptyState body text has max-w-xs width constraint. PaintsPage description has max-w-xs constraint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — CSS class and markup changes only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-02 | N/A | N/A | accept | No security surface — visual class changes only |
</threat_model>

<verification>
pnpm build succeeds with zero TypeScript errors.
grep for "max-w-3xl" in SpendingPage.tsx returns 0 matches.
grep for "space-y-6" in DataHealthPage.tsx and settings/page.tsx returns 0 matches.
grep for "text-base font-semibold" in SpendingPage.tsx returns 0 matches.
grep for "text-lg font-semibold" in DataHealthPage.tsx returns 0 matches.
grep for "rounded-xl bg-muted/40 p-4" in PaintsPage.tsx returns 1 match.
grep for "max-w-xs" in FactionsEmptyState.tsx returns 1 match.
</verification>

<success_criteria>
Spending page uses standard p-6 gap-6 layout on all branches with standardized headings. Data Health and Settings use flex flex-col gap-6 with standardized headings. Paints filtered empty state shows icon-pill pattern. Factions empty state description has max-w-xs.
</success_criteria>

<output>
Create `.planning/phases/127-visual-consistency-pageheader-unification/127-02-SUMMARY.md` when done
</output>

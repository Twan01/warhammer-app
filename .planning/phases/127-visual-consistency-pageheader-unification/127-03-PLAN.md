---
phase: 127-visual-consistency-pageheader-unification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/recipes/RecipeCard.tsx
  - src/features/recipes/SectionedTimeline.tsx
  - src/features/dashboard/DashboardPage.tsx
autonomous: true
requirements: [VIS-06, VIS-07]

must_haves:
  truths:
    - "Status dot colors render correctly in dark mode without hardcoded hex values"
    - "Dashboard action buttons have uniform icon size matching the project standard"
  artifacts:
    - path: "src/features/recipes/RecipeCard.tsx"
      provides: "Theme-token status dots"
    - path: "src/features/recipes/SectionedTimeline.tsx"
      provides: "Theme-token status dots"
    - path: "src/features/dashboard/DashboardPage.tsx"
      provides: "Standard button icon sizing"
  key_links: []
---

<objective>
Replace hardcoded hex status dot colors with Tailwind utility classes in RecipeCard and SectionedTimeline, and standardize Dashboard button icon sizing to the h-4 w-4 mr-2 pattern.

Purpose: Eliminate all hardcoded hex colors from status indicators and normalize icon sizing to the project standard.
Output: 3 files updated with consistent design tokens and icon sizing.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-visual-consistency-pageheader-unification/127-CONTEXT.md
@.planning/phases/127-visual-consistency-pageheader-unification/127-PATTERNS.md
@.planning/phases/127-visual-consistency-pageheader-unification/127-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded hex status dots in RecipeCard and SectionedTimeline</name>
  <files>src/features/recipes/RecipeCard.tsx, src/features/recipes/SectionedTimeline.tsx</files>
  <read_first>
    src/features/recipes/RecipeCard.tsx
    src/features/recipes/SectionedTimeline.tsx
  </read_first>
  <action>
    Per D-08: In RecipeCard.tsx, find all span elements that have `style={{ backgroundColor: "#..." }}` for status dots. Each span has className="inline-block h-2 w-2 rounded-full shrink-0" with an inline style. For each occurrence:
    - `style={{ backgroundColor: "#22c55e" }}` — remove the style prop entirely, add `bg-green-500` to the existing className
    - `style={{ backgroundColor: "#f59e0b" }}` — remove the style prop entirely, add `bg-amber-500` to the existing className
    - `style={{ backgroundColor: "#ef4444" }}` — remove the style prop entirely, add `bg-red-500` to the existing className

    There are approximately 6 occurrences across multiple rendering branches in RecipeCard.tsx. Replace ALL of them.

    Per D-08: In SectionedTimeline.tsx, apply the same replacement for the 2 occurrences:
    - `#22c55e` becomes `bg-green-500` added to className, style prop removed
    - `#ef4444` becomes `bg-red-500` added to className, style prop removed

    After changes, the resulting span elements should look like: `<span className="inline-block h-2 w-2 rounded-full shrink-0 bg-green-500" />`
  </action>
  <verify>
    <automated>pnpm build && grep -c "backgroundColor" src/features/recipes/RecipeCard.tsx | grep "^0$" && grep -c "backgroundColor" src/features/recipes/SectionedTimeline.tsx | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - RecipeCard.tsx has zero occurrences of "backgroundColor"
    - RecipeCard.tsx has zero occurrences of "#22c55e", "#f59e0b", or "#ef4444"
    - RecipeCard.tsx contains "bg-green-500" (at least 1 occurrence)
    - RecipeCard.tsx contains "bg-amber-500" (at least 1 occurrence)
    - RecipeCard.tsx contains "bg-red-500" (at least 1 occurrence)
    - SectionedTimeline.tsx has zero occurrences of "backgroundColor"
    - SectionedTimeline.tsx contains "bg-green-500" and "bg-red-500"
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>All status dots in RecipeCard and SectionedTimeline use Tailwind utility classes. Zero hardcoded hex color values remain in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard button icon sizing standardization</name>
  <files>src/features/dashboard/DashboardPage.tsx</files>
  <read_first>
    src/features/dashboard/DashboardPage.tsx
  </read_first>
  <action>
    Per D-09: In DashboardPage.tsx, find the 3 Lucide icon instances that use `size={14} className="mr-1.5"` and replace each with `className="h-4 w-4 mr-2"` (removing the size prop entirely). The 3 instances are:
    - A Plus icon (around line 256) — change from `size={14} className="mr-1.5"` to `className="h-4 w-4 mr-2"`, keep aria-hidden={true}
    - A Paintbrush icon (around line 324) — same change
    - A Plus icon (around line 332) — same change

    Per Claude's discretion: while editing DashboardPage.tsx, scan for any other Lucide icons using the `size` prop pattern and convert those too if found. The standard pattern is always `className="h-4 w-4 mr-2"` for button icons.
  </action>
  <verify>
    <automated>pnpm build && grep -c "size={14}" src/features/dashboard/DashboardPage.tsx | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage.tsx has zero occurrences of `size={14}`
    - DashboardPage.tsx has zero occurrences of `mr-1.5` on icon elements
    - DashboardPage.tsx contains at least 3 occurrences of "h-4 w-4 mr-2"
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>All Dashboard button icons use the standard h-4 w-4 mr-2 className pattern. No size prop deviations remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — CSS class and prop changes only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-03 | N/A | N/A | accept | No security surface — visual token replacement only |
</threat_model>

<verification>
pnpm build succeeds with zero TypeScript errors.
grep for "backgroundColor" in RecipeCard.tsx and SectionedTimeline.tsx returns 0 matches.
grep for "size={14}" in DashboardPage.tsx returns 0 matches.
pnpm test passes (existing RecipeCard tests still pass after class changes).
</verification>

<success_criteria>
RecipeCard and SectionedTimeline status dots use Tailwind bg-* classes with zero inline style hex values. Dashboard button icons use the h-4 w-4 mr-2 standard. Full build passes.
</success_criteria>

<output>
Create `.planning/phases/127-visual-consistency-pageheader-unification/127-03-SUMMARY.md` when done
</output>

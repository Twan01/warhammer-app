---
phase: 80-export-ui-backup-status
plan: 02
type: execute
wave: 2
depends_on:
  - 80-01
files_modified:
  - src/features/data-health/BackupCard.tsx
  - src/features/dashboard/DataHealthSummaryCard.tsx
  - src-tauri/src/lib.rs
  - tests/data-health/backupCard.test.tsx
  - tests/dashboard/DataHealthSummaryCard.test.tsx
autonomous: true
requirements:
  - STS-01
  - STS-02
  - STS-03
  - STS-04
must_haves:
  truths:
    - "BackupCard shows color-coded dot + tier label + age label (e.g. 'Healthy -- Backed up today')"
    - "BackupCard export invokes export_backup (not backup_database)"
    - "BackupCard save dialog defaults to .zip extension with HHMM timestamp filename"
    - "DataHealthSummaryCard backup row shows color dot instead of HardDrive icon"
    - "DataHealthSummaryCard backup row uses getBackupFreshness/getBackupAgeLabel from shared utility"
    - "backup_database command is removed from lib.rs invoke_handler"
    - "pnpm build succeeds (TypeScript + Vite)"
  artifacts:
    - path: "src/features/data-health/BackupCard.tsx"
      provides: "Migrated backup card with export_backup + health dot"
      contains: "export_backup"
    - path: "src/features/dashboard/DataHealthSummaryCard.tsx"
      provides: "Dashboard summary with backup freshness dot"
      contains: "BACKUP_FRESHNESS_DOT_CLASS"
    - path: "src-tauri/src/lib.rs"
      provides: "Rust backend without backup_database dead code"
    - path: "tests/data-health/backupCard.test.tsx"
      provides: "Updated test assertions for export_backup + zip + dot"
    - path: "tests/dashboard/DataHealthSummaryCard.test.tsx"
      provides: "New test for dashboard backup dot rendering"
      min_lines: 30
  key_links:
    - from: "src/features/data-health/BackupCard.tsx"
      to: "src/lib/backupFreshness.ts"
      via: "import getBackupFreshness, getBackupAgeLabel, BACKUP_FRESHNESS_DOT_CLASS"
      pattern: "from.*backupFreshness"
    - from: "src/features/dashboard/DataHealthSummaryCard.tsx"
      to: "src/lib/backupFreshness.ts"
      via: "import getBackupFreshness, getBackupAgeLabel, BACKUP_FRESHNESS_DOT_CLASS"
      pattern: "from.*backupFreshness"
    - from: "src/features/data-health/BackupCard.tsx"
      to: "Rust export_backup command"
      via: "invoke('export_backup', { destination })"
      pattern: 'invoke.*export_backup'
---

<objective>
Migrate BackupCard to the new structured backup system, add health status dots to both BackupCard and DataHealthSummaryCard, remove the dead backup_database Rust command, and update all tests.

Purpose: Delivers the user-facing backup status experience (STS-01 through STS-04) — color-coded health indicators on Data Health page and Dashboard, with the export flow wired to the Phase 79 Rust commands.
Output: Updated BackupCard.tsx, DataHealthSummaryCard.tsx, lib.rs, and test files
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-export-ui-backup-status/80-CONTEXT.md
@.planning/phases/80-export-ui-backup-status/80-RESEARCH.md
@.planning/phases/80-export-ui-backup-status/80-PATTERNS.md
@.planning/phases/80-export-ui-backup-status/80-UI-SPEC.md
@.planning/phases/80-export-ui-backup-status/80-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: src/lib/backupFreshness.ts -->
export type BackupFreshness = "healthy" | "recommended" | "overdue" | "never";
export function getBackupFreshness(date: string | null): BackupFreshness;
export function getBackupAgeLabel(date: string | null): string;
export const BACKUP_FRESHNESS_DOT_CLASS: Record<BackupFreshness, string>;

<!-- From src/hooks/useDiagnostics.ts — unchanged -->
export interface BackupStatus { date: string; path: string; success: boolean; }
export function useBackupStatus(): BackupStatus | null;
export const BACKUP_STORAGE_KEY: string;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate BackupCard + update DataHealthSummaryCard + remove dead Rust command</name>
  <files>src/features/data-health/BackupCard.tsx, src/features/dashboard/DataHealthSummaryCard.tsx, src-tauri/src/lib.rs</files>
  <read_first>
    - src/features/data-health/BackupCard.tsx (current state — all lines, note lines 21-32 formatRelativeDate/extractFilename to remove, line 42 filename, lines 44-48 save dialog, line 54 invoke, lines 77-81 subtitle)
    - src/features/dashboard/DataHealthSummaryCard.tsx (current state — note lines 1 HardDrive import, lines 20-27 backupLabel IIFE, lines 57-60 backup row JSX)
    - src-tauri/src/lib.rs (lines 575-616 backup_database function, line ~866 invoke_handler macro entry)
    - src/lib/backupFreshness.ts (Plan 01 output — verify exports available)
    - .planning/phases/80-export-ui-backup-status/80-PATTERNS.md (exact before/after patterns for all three files)
    - .planning/phases/80-export-ui-backup-status/80-UI-SPEC.md (interaction contract states, layout contract, copywriting contract)
  </read_first>
  <action>
    **BackupCard.tsx (per D-03, D-04, D-05):**

    1. Add import: getBackupFreshness, getBackupAgeLabel, BACKUP_FRESHNESS_DOT_CLASS from @/lib/backupFreshness.
    2. Delete the formatRelativeDate function (lines 21-27) and extractFilename function (lines 29-32) — replaced by shared utility.
    3. In handleBackup: replace the defaultFilename with UTC timestamp format hobbyforge-backup-YYYY-MM-DD-HHMM.zip using pad helper for zero-padding month/day/hours/minutes from new Date() with getUTC* methods (per D-04).
    4. Update save() call: title from "Save Database Backup" to "Save Backup", filter name from "SQLite Database" to "HobbyForge Backup", extensions from ["db"] to ["zip"] (per D-04).
    5. Change invoke("backup_database", { destination }) to invoke("export_backup", { destination }) (per D-05).
    6. Before the return statement, compute: tier = getBackupFreshness(backupStatus?.date ?? null), tierLabel = tier capitalized first letter, ageLabel = getBackupAgeLabel(backupStatus?.date ?? null).
    7. Replace the subtitle span (lines 77-81) with a flex div containing: color dot span (inline-block h-2 w-2 rounded-full + BACKUP_FRESHNESS_DOT_CLASS[tier]) + text span "tierLabel — ageLabel". For never state: "Never — No backup yet". For other states: e.g. "Healthy — Backed up today" (per UI-SPEC copywriting contract).

    **DataHealthSummaryCard.tsx (per D-06, D-07):**

    1. Change lucide-react import: remove HardDrive, keep Database only.
    2. Add import: getBackupFreshness, getBackupAgeLabel, BACKUP_FRESHNESS_DOT_CLASS from @/lib/backupFreshness.
    3. Replace the backupLabel IIFE (lines 20-27) with two lines: backupTier = getBackupFreshness(backup?.date ?? null), backupLabel = getBackupAgeLabel(backup?.date ?? null).
    4. Replace backup row JSX (lines 57-60): swap HardDrive icon for color dot span (inline-block h-2 w-2 rounded-full + BACKUP_FRESHNESS_DOT_CLASS[backupTier]). Keep the wrapper div flex items-center gap-1.5 and the text-muted-foreground span with backupLabel.

    **lib.rs (per D-08):**

    1. Delete the entire backup_database function (approximately lines 575-616 including the doc comment and #[tauri::command] attribute).
    2. Remove the backup_database entry from the invoke_handler! macro (approximately line 866). Both edits must happen together to avoid Rust compile errors.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - BackupCard.tsx contains invoke("export_backup" and does NOT contain invoke("backup_database"
    - BackupCard.tsx contains filters: [{ name: "HobbyForge Backup", extensions: ["zip"] }]
    - BackupCard.tsx contains BACKUP_FRESHNESS_DOT_CLASS import and inline-block h-2 w-2 rounded-full dot element
    - BackupCard.tsx does NOT contain formatRelativeDate or extractFilename functions
    - BackupCard.tsx default filename matches pattern hobbyforge-backup-YYYY-MM-DD-HHMM.zip
    - DataHealthSummaryCard.tsx contains BACKUP_FRESHNESS_DOT_CLASS import and does NOT contain HardDrive import
    - DataHealthSummaryCard.tsx does NOT contain the backupLabel IIFE (inline age calculation removed)
    - lib.rs does NOT contain fn backup_database and its invoke_handler macro does NOT list backup_database
    - pnpm build exits 0 (TypeScript compilation + Vite build succeed)
  </acceptance_criteria>
  <done>BackupCard uses export_backup with .zip dialog, shows tier dot + age label. DataHealthSummaryCard shows backup dot instead of HardDrive icon. backup_database removed from Rust backend. TypeScript build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update BackupCard tests + create DataHealthSummaryCard test</name>
  <files>tests/data-health/backupCard.test.tsx, tests/dashboard/DataHealthSummaryCard.test.tsx</files>
  <read_first>
    - tests/data-health/backupCard.test.tsx (current assertions to update: line 54 "No backups yet", lines 65-72 save dialog title+filter, lines 84-95 invoke command name+path extension)
    - src/features/data-health/BackupCard.tsx (updated state from Task 1 — verify new text patterns)
    - src/features/dashboard/DataHealthSummaryCard.tsx (updated state from Task 1 — verify imports and JSX)
    - tests/datasheet/syncFreshness.test.ts (test pattern reference for fake timers)
  </read_first>
  <action>
    **backupCard.test.tsx updates:**

    1. Update test "shows 'No backups yet'" (line 54): change assertion from screen.getByText("No backups yet") to screen.getByText(/Never.*No backup/i) since the new subtitle format is "Never — No backup yet".
    2. Update test "calls save dialog" (lines 65-72): change title assertion from "Save Database Backup" to "Save Backup", change extensions assertion from ["db"] to ["zip"].
    3. Update test "invokes backup_database" (lines 84-95): rename test description to reference export_backup, change mockSave return path from "C:\\backups\\test.db" to "C:\\backups\\test.zip", change assertion from invoke("backup_database", ...) to invoke("export_backup", ...) with destination "C:\\backups\\test.zip".
    4. Update test "does not invoke backup_database" (line 75): rename to "does not invoke export_backup if dialog is cancelled".
    5. Update test "shows success toast" (line 99): change mockSave return from .db to .zip path.
    6. Update test "shows error toast" (line 111): change mockSave return from .db to .zip path.
    7. Add new test "STS-02: renders muted dot when no backup exists": render BackupCard, query for an element with class bg-muted-foreground, assert it exists in the document.
    8. Add new test "STS-02: renders correct tier dot for recent backup": override useBackupStatus mock to return { date: new Date().toISOString(), path: "test.zip", success: true }, use vi.useFakeTimers + vi.setSystemTime, render BackupCard, query for bg-green-500 dot element.

    **DataHealthSummaryCard.test.tsx (new file):**

    Create tests/dashboard/DataHealthSummaryCard.test.tsx. Mock @/hooks/useDatasheet (useRulesSyncMeta returns { data: null, isLoading: false }), @/hooks/useDiagnostics (useDiagnosticFlags returns { data: [], isLoading: false }, useBackupStatus returns null), @/lib/syncFreshness (return real values), @tanstack/react-router (Link as a div passthrough). Import DataHealthSummaryCard. Tests:
    - "STS-04: renders backup dot with muted color when no backup": render component, assert presence of bg-muted-foreground dot element and text "No backup".
    - "STS-04: renders backup dot with green color for recent backup": override useBackupStatus mock to return recent date, use vi.useFakeTimers, render, assert bg-green-500 element and text matching /Backed up today/.
    - "STS-04: does not render HardDrive icon": render component, assert no element with data-lucide="hard-drive" or aria-label containing "HardDrive" (confirm icon was removed).
  </action>
  <verify>
    <automated>pnpm test -- tests/data-health/backupCard.test.tsx tests/dashboard/DataHealthSummaryCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - backupCard.test.tsx contains invoke("export_backup" and does NOT contain invoke("backup_database"
    - backupCard.test.tsx dialog assertion uses title "Save Backup" and extensions ["zip"]
    - backupCard.test.tsx "No backups" test asserts /Never.*No backup/i (not exact "No backups yet")
    - backupCard.test.tsx has at least one test asserting bg-muted-foreground dot presence
    - DataHealthSummaryCard.test.tsx exists with at least 3 tests covering STS-04
    - pnpm test -- tests/data-health/backupCard.test.tsx tests/dashboard/DataHealthSummaryCard.test.tsx exits 0
    - pnpm test exits 0 (full suite green)
  </acceptance_criteria>
  <done>All backup-related tests updated for export_backup + zip + dot rendering. New DataHealthSummaryCard test covers STS-04 backup dot display. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> OS save dialog -> Rust | File path from native dialog passed to export_backup Rust command |
| localStorage -> React component | BackupStatus JSON parsed by useBackupStatus hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-02 | Tampering | Save dialog path | accept | Path comes from OS native dialog (not user text input); Rust command validates via std::fs::File::create. No additional sanitization needed. |
| T-80-03 | Information Disclosure | localStorage backup metadata | accept | Only stores date, path, success boolean. No PII or sensitive data. Single-user desktop app. |
| T-80-SC | Tampering | npm installs | accept | No packages installed in this phase — zero supply chain surface |
</threat_model>

<verification>
pnpm build && pnpm test
</verification>

<success_criteria>
- BackupCard invokes export_backup with .zip save dialog and shows color-coded health dot + tier + age label
- DataHealthSummaryCard shows backup freshness dot (not HardDrive icon) with age label from shared utility
- backup_database function and handler entry removed from lib.rs
- All existing tests updated and passing
- New DataHealthSummaryCard test covers STS-04
- Full test suite and TypeScript build green
</success_criteria>

<output>
Create `.planning/phases/80-export-ui-backup-status/80-02-SUMMARY.md` when done
</output>

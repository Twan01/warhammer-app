---
phase: 62-applied-recipe-data-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 62-01
files_modified:
  - src/db/queries/recipeAssignments.ts
  - src/hooks/useRecipeAssignments.ts
  - tests/painting/recipeAssignments.test.ts
autonomous: true
requirements:
  - AR-01

must_haves:
  truths:
    - "Query functions for CRUD on assignments and step progress exist with parameterized $1,$2 queries"
    - "React Query hooks expose cache keys and mutations with symmetric invalidation per D-13"
    - "upsertStepProgress uses ON CONFLICT DO UPDATE SET to preserve row id"
    - "bulkCreateAssignments uses INSERT OR IGNORE to silently skip duplicate (unit_id, recipe_id) pairs"
    - "All query mock tests and hook invalidation tests pass"
  artifacts:
    - path: "src/db/queries/recipeAssignments.ts"
      provides: "CRUD for unit_recipe_assignments and unit_recipe_step_progress"
      exports: ["getAssignmentsByUnit", "getAssignmentsByRecipe", "getAssignment", "createAssignment", "deleteAssignment", "getStepProgress", "upsertStepProgress", "bulkCreateAssignments"]
    - path: "src/hooks/useRecipeAssignments.ts"
      provides: "React Query hooks with cache key exports"
      exports: ["ASSIGNMENTS_KEY", "UNIT_ASSIGNMENTS_KEY", "RECIPE_ASSIGNMENTS_KEY", "STEP_PROGRESS_KEY", "useAssignmentsByUnit", "useAssignmentsByRecipe", "useCreateAssignment", "useDeleteAssignment", "useStepProgress", "useToggleStepProgress", "useBulkCreateAssignments"]
    - path: "tests/painting/recipeAssignments.test.ts"
      provides: "Query mock tests and hook invalidation contract tests"
      min_lines: 100
  key_links:
    - from: "src/db/queries/recipeAssignments.ts"
      to: "src/db/client.ts"
      via: "getDb() import"
      pattern: "import.*getDb.*from.*@/db/client"
    - from: "src/db/queries/recipeAssignments.ts"
      to: "src/types/recipeAssignment.ts"
      via: "type imports"
      pattern: "import type.*from.*@/types/recipeAssignment"
    - from: "src/hooks/useRecipeAssignments.ts"
      to: "src/db/queries/recipeAssignments.ts"
      via: "query function imports"
      pattern: "import.*from.*@/db/queries/recipeAssignments"
    - from: "tests/painting/recipeAssignments.test.ts"
      to: "src/hooks/useRecipeAssignments.ts"
      via: "hook + key imports for invalidation tests"
      pattern: "import.*UNIT_ASSIGNMENTS_KEY.*useCreateAssignment"
---

<objective>
Create the query module, React Query hooks, and comprehensive tests for applied recipe assignments and step progress CRUD.

Purpose: Completes the data layer so Phase 63 can build UI directly against these hooks. Tests verify SQL correctness via mocks and cache invalidation symmetry per D-13.
Output: Query module with 8 functions, hook module with 7 hooks + 4 cache key exports, test file covering all query SQL assertions and hook invalidation contracts.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-applied-recipe-data-layer/62-CONTEXT.md
@.planning/phases/62-applied-recipe-data-layer/62-PATTERNS.md
@.planning/phases/62-applied-recipe-data-layer/62-01-SUMMARY.md

@src/db/queries/recipeSections.ts
@src/hooks/useRecipeSections.ts
@tests/painting/recipeSections.test.ts
@src/types/recipeAssignment.ts

<interfaces>
<!-- Types created by Plan 01 that this plan consumes -->

From src/types/recipeAssignment.ts:
  export interface RecipeAssignment { id: number; unit_id: number; recipe_id: number; created_at: string; }
  export type CreateRecipeAssignmentInput = Omit<RecipeAssignment, "id" | "created_at">;
  export interface StepProgress { id: number; assignment_id: number; order_index: number; completed: number; completed_at: string | null; }
  export interface AssignmentProgress { total: number; completed: number; percentage: number; bySectionId: Map<number | null, { total: number; completed: number }>; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query module — recipeAssignments.ts</name>
  <files>src/db/queries/recipeAssignments.ts</files>
  <read_first>
    - src/db/queries/recipeSections.ts (canonical query module — import pattern, getDb(), $1/$2 params, SELECT/INSERT/DELETE patterns, loop pattern for bulk ops)
    - src/db/queries/recipePaints.ts (verify order_index scope — check how addRecipePaint assigns order_index to confirm A2 assumption)
    - src/types/recipeAssignment.ts (RecipeAssignment, CreateRecipeAssignmentInput, StepProgress types)
  </read_first>
  <action>
    Create src/db/queries/recipeAssignments.ts per D-10. Import getDb from @/db/client. Import types RecipeAssignment, CreateRecipeAssignmentInput, StepProgress from @/types/recipeAssignment.

    8 exported async functions:

    1. getAssignmentsByUnit(unitId: number): Promise<RecipeAssignment[]> — SELECT * FROM unit_recipe_assignments WHERE unit_id = $1 ORDER BY created_at ASC

    2. getAssignmentsByRecipe(recipeId: number): Promise<RecipeAssignment[]> — SELECT * FROM unit_recipe_assignments WHERE recipe_id = $1 ORDER BY created_at ASC

    3. getAssignment(id: number): Promise<RecipeAssignment | null> — SELECT * FROM unit_recipe_assignments WHERE id = $1, return first element or null

    4. createAssignment(input: CreateRecipeAssignmentInput): Promise<number> — INSERT INTO unit_recipe_assignments (unit_id, recipe_id) VALUES ($1, $2), return result.lastInsertId ?? 0. Add JSDoc noting UNIQUE constraint throws on duplicate (unit_id, recipe_id).

    5. deleteAssignment(id: number): Promise<void> — DELETE FROM unit_recipe_assignments WHERE id = $1

    6. getStepProgress(assignmentId: number): Promise<StepProgress[]> — SELECT * FROM unit_recipe_step_progress WHERE assignment_id = $1 ORDER BY order_index ASC

    7. upsertStepProgress(assignmentId: number, orderIndex: number, completed: boolean): Promise<void> — INSERT INTO unit_recipe_step_progress (assignment_id, order_index, completed, completed_at) VALUES ($1, $2, $3, $4) ON CONFLICT(assignment_id, order_index) DO UPDATE SET completed = excluded.completed, completed_at = excluded.completed_at. Pass completed ? 1 : 0 for $3, completed ? new Date().toISOString() : null for $4. Per D-05 and anti-pattern note: use ON CONFLICT DO UPDATE SET, NOT INSERT OR REPLACE.

    8. bulkCreateAssignments(unitIds: number[], recipeId: number): Promise<void> — Loop over unitIds, each iteration: INSERT OR IGNORE INTO unit_recipe_assignments (unit_id, recipe_id) VALUES ($1, $2). INSERT OR IGNORE silently skips duplicates per Pitfall 2.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | findstr /C:"error" || echo "build passed"</automated>
  </verify>
  <acceptance_criteria>
    - File imports getDb from @/db/client
    - All 8 functions exported: getAssignmentsByUnit, getAssignmentsByRecipe, getAssignment, createAssignment, deleteAssignment, getStepProgress, upsertStepProgress, bulkCreateAssignments
    - All SQL uses $1, $2 positional params (no ? or named params)
    - upsertStepProgress SQL contains "ON CONFLICT(assignment_id, order_index) DO UPDATE SET" (NOT INSERT OR REPLACE)
    - bulkCreateAssignments SQL contains "INSERT OR IGNORE"
    - upsertStepProgress passes completed ? 1 : 0 (boolean to integer cast)
    - getAssignment returns first element or null (not array)
    - pnpm build succeeds
  </acceptance_criteria>
  <done>Query module with all 8 CRUD functions compiles and follows project parameterized query conventions</done>
</task>

<task type="auto">
  <name>Task 2: Hook module + comprehensive tests</name>
  <files>src/hooks/useRecipeAssignments.ts, tests/painting/recipeAssignments.test.ts</files>
  <read_first>
    - src/hooks/useRecipeSections.ts (canonical hook module — cache key exports, useQuery with enabled guard, useMutation + invalidateQueries, CASCADE INVALIDATION CONTRACT comment)
    - tests/painting/recipeSections.test.ts (canonical test pattern — vi.mock setup, executeMock/selectMock, makeWrapper helper, SQL assertion groups, hook invalidation spy pattern)
    - src/db/queries/recipeAssignments.ts (the query functions created in Task 1 — know their signatures for import and mutationFn wiring)
  </read_first>
  <action>
    Create src/hooks/useRecipeAssignments.ts per D-11, D-12, D-13.

    Import useQuery, useMutation, useQueryClient from @tanstack/react-query. Import all 8 query functions from @/db/queries/recipeAssignments. Import CreateRecipeAssignmentInput from @/types/recipeAssignment.

    4 cache key exports per D-12: ASSIGNMENTS_KEY = ["recipe-assignments"] as const, UNIT_ASSIGNMENTS_KEY = (unitId: number) => ["recipe-assignments", "by-unit", unitId] as const, RECIPE_ASSIGNMENTS_KEY = (recipeId: number) => ["recipe-assignments", "by-recipe", recipeId] as const, STEP_PROGRESS_KEY = (assignmentId: number) => ["recipe-assignments", "progress", assignmentId] as const.

    7 hooks:

    1. useAssignmentsByUnit(unitId: number | undefined) — useQuery with enabled guard (unitId !== undefined), queryKey: UNIT_ASSIGNMENTS_KEY(unitId!), queryFn: getAssignmentsByUnit(unitId!). Return empty array when disabled.

    2. useAssignmentsByRecipe(recipeId: number | undefined) — same pattern with RECIPE_ASSIGNMENTS_KEY.

    3. useCreateAssignment() — useMutation<number, Error, CreateRecipeAssignmentInput>, onSuccess invalidates UNIT_ASSIGNMENTS_KEY(input.unit_id) and RECIPE_ASSIGNMENTS_KEY(input.recipe_id). Add CASCADE INVALIDATION CONTRACT comment per D-13.

    4. useDeleteAssignment() — useMutation<void, Error, { id: number; unitId: number; recipeId: number }>, mutationFn: ({ id }) => deleteAssignment(id), onSuccess invalidates UNIT_ASSIGNMENTS_KEY(variables.unitId) and RECIPE_ASSIGNMENTS_KEY(variables.recipeId). MUST match useCreateAssignment keys exactly per D-13 symmetry.

    5. useStepProgress(assignmentId: number | undefined) — useQuery with enabled guard, queryKey: STEP_PROGRESS_KEY(assignmentId!), queryFn: getStepProgress(assignmentId!).

    6. useToggleStepProgress() — useMutation<void, Error, { assignmentId: number; orderIndex: number; completed: boolean }>, mutationFn forwards to upsertStepProgress, onSuccess invalidates STEP_PROGRESS_KEY(variables.assignmentId).

    7. useBulkCreateAssignments() — useMutation<void, Error, { unitIds: number[]; recipeId: number }>, mutationFn forwards to bulkCreateAssignments, onSuccess invalidates ASSIGNMENTS_KEY (broad prefix to cover all per-unit views) and RECIPE_ASSIGNMENTS_KEY(variables.recipeId). Per Pitfall 5.

    Create tests/painting/recipeAssignments.test.ts following the recipeSections.test.ts pattern exactly:

    Top-of-file: vi.mock("@/db/client", () => ({ getDb: async () => ({ select: selectMock, execute: executeMock }) })). Import query functions and hooks AFTER vi.mock.

    beforeEach: reset both mocks, set executeMock default to { lastInsertId: 1 }, selectMock default to [].

    makeWrapper helper: QueryClient with retry: false, spy on invalidateQueries.

    Test groups:
    A) Query SQL assertions — one describe per query function: verify SQL string contains correct table name, WHERE clause, ORDER BY, ON CONFLICT, params array matches input.
    B) Hook invalidation contract — one describe per mutation hook: verify exact cache keys invalidated on success. Critical test: useDeleteAssignment invalidates exactly the same keys as useCreateAssignment (D-13 symmetry). useBulkCreateAssignments invalidates ASSIGNMENTS_KEY broad prefix (Pitfall 5).

    Run full test suite at end to confirm no regressions.
  </action>
  <verify>
    <automated>pnpm test -- tests/painting/recipeAssignments.test.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - useRecipeAssignments.ts exports 4 cache key constants: ASSIGNMENTS_KEY, UNIT_ASSIGNMENTS_KEY, RECIPE_ASSIGNMENTS_KEY, STEP_PROGRESS_KEY
    - useRecipeAssignments.ts exports 7 hooks: useAssignmentsByUnit, useAssignmentsByRecipe, useCreateAssignment, useDeleteAssignment, useStepProgress, useToggleStepProgress, useBulkCreateAssignments
    - useCreateAssignment onSuccess invalidates exactly UNIT_ASSIGNMENTS_KEY(input.unit_id) and RECIPE_ASSIGNMENTS_KEY(input.recipe_id)
    - useDeleteAssignment onSuccess invalidates exactly the same two keys (symmetry D-13)
    - useBulkCreateAssignments onSuccess invalidates ASSIGNMENTS_KEY (broad prefix) and RECIPE_ASSIGNMENTS_KEY
    - Test file has query SQL assertion tests for all 8 query functions
    - Test file has hook invalidation tests for all 4 mutation hooks
    - Test file uses vi.mock("@/db/client") pattern with executeMock/selectMock
    - pnpm test -- tests/painting/recipeAssignments.test.ts passes
    - pnpm test (full suite) passes
  </acceptance_criteria>
  <done>Hook module with all 7 hooks and 4 cache keys, plus test file with query SQL assertions and hook invalidation contract coverage — full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Query functions → SQLite | TypeScript number params cross into SQL via $1/$2 parameterized queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-03 | Tampering | recipeAssignments.ts query functions | mitigate | All queries use $1, $2 parameterized syntax via tauri-plugin-sql — no string interpolation in SQL |
| T-62-04 | Denial of Service | bulkCreateAssignments loop | accept | Loop over unitIds array — bounded by collection size (personal app, ~hundreds max); no external input |
| T-62-05 | Information Disclosure | Cache keys expose entity IDs | accept | Local desktop app; no network; cache is in-memory React Query only |
</threat_model>

<verification>
pnpm build succeeds (all new files compile)
pnpm test -- tests/painting/recipeAssignments.test.ts passes
pnpm test -- tests/lib/computeAssignmentProgress.test.ts passes
pnpm test passes (full suite, no regressions)
</verification>

<success_criteria>
Query module exports 8 functions with correct parameterized SQL.
Hook module exports 4 cache keys and 7 hooks with symmetric invalidation.
Test file covers all query SQL assertions and hook invalidation contracts.
Full test suite passes with no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/62-applied-recipe-data-layer/62-02-SUMMARY.md`
</output>

---
phase: 62-applied-recipe-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/021_applied_recipe_assignments.sql
  - src-tauri/src/lib.rs
  - src/types/recipeAssignment.ts
  - src/lib/computeAssignmentProgress.ts
  - tests/lib/computeAssignmentProgress.test.ts
autonomous: true
requirements:
  - AR-01

must_haves:
  truths:
    - "D-01, D-02, D-03: Migration 021 creates both unit_recipe_assignments and unit_recipe_step_progress tables with correct FK CASCADE chains and UNIQUE constraints"
    - "D-04, D-05: No section-level progress storage; composite key (assignment_id, order_index) not recipe_step_id FK"
    - "D-06: Reorder misalignment accepted as edge case"
    - "D-07, D-08: computeCompletionPercentage returns correct rounded integer percentages including 0 for 0 total; optional sections count"
    - "D-09: computeAssignmentProgress returns total/completed/percentage plus per-section breakdown via bySectionId Map"
    - "D-14: Type definitions (RecipeAssignment, CreateRecipeAssignmentInput, StepProgress, AssignmentProgress) match SQLite table columns exactly"
  artifacts:
    - path: "src-tauri/migrations/021_applied_recipe_assignments.sql"
      provides: "DDL for unit_recipe_assignments and unit_recipe_step_progress"
      contains: "UNIQUE(unit_id, recipe_id)"
    - path: "src-tauri/src/lib.rs"
      provides: "Migration version 21 registration"
      contains: "version: 21"
    - path: "src/types/recipeAssignment.ts"
      provides: "RecipeAssignment, CreateRecipeAssignmentInput, StepProgress, AssignmentProgress"
      exports: ["RecipeAssignment", "CreateRecipeAssignmentInput", "StepProgress", "AssignmentProgress"]
    - path: "src/lib/computeAssignmentProgress.ts"
      provides: "Pure completion percentage and assignment progress computation"
      exports: ["computeCompletionPercentage", "computeAssignmentProgress"]
    - path: "tests/lib/computeAssignmentProgress.test.ts"
      provides: "Unit tests for pure functions"
      min_lines: 50
  key_links:
    - from: "src/lib/computeAssignmentProgress.ts"
      to: "src/types/recipeAssignment.ts"
      via: "AssignmentProgress return type"
      pattern: "AssignmentProgress"
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/migrations/021_applied_recipe_assignments.sql"
      via: "include_str! macro"
      pattern: "021_applied_recipe_assignments.sql"
---

<objective>
Create the applied recipe schema (migration 021), type definitions, and TDD-tested pure functions for computing assignment progress.

Purpose: Establishes the data foundation (tables + types) and completion logic that Plan 02's query/hook layer will build on. Pure functions are tested without mocks.
Output: Migration file, lib.rs registration, TypeScript types, two pure functions with comprehensive unit tests.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-applied-recipe-data-layer/62-CONTEXT.md
@.planning/phases/62-applied-recipe-data-layer/62-PATTERNS.md

@src/types/recipeSection.ts
@src/lib/computeWorkflowPosition.ts
@tests/lib/computeWorkflowPosition.test.ts
@src-tauri/src/lib.rs
@src-tauri/migrations/018_recipe_sections.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + lib.rs registration + type definitions</name>
  <files>src-tauri/migrations/021_applied_recipe_assignments.sql, src-tauri/src/lib.rs, src/types/recipeAssignment.ts</files>
  <read_first>
    - src-tauri/migrations/018_recipe_sections.sql (migration DDL pattern)
    - src-tauri/src/lib.rs (find get_migrations() — insert version 21 entry after version 20)
    - src/types/recipeSection.ts (Entity/CreateInput type pattern, boolean comment convention)
  </read_first>
  <action>
    Create migration file 021_applied_recipe_assignments.sql per D-01 and D-02. Two CREATE TABLE IF NOT EXISTS statements in one file:

    Table 1 — unit_recipe_assignments: id INTEGER PRIMARY KEY AUTOINCREMENT, unit_id INTEGER NOT NULL REFERENCES units(id) ON DELETE CASCADE, recipe_id INTEGER NOT NULL REFERENCES painting_recipes(id) ON DELETE CASCADE, created_at TEXT NOT NULL DEFAULT (datetime('now')), UNIQUE(unit_id, recipe_id). Per D-01 and D-03.

    Table 2 — unit_recipe_step_progress: id INTEGER PRIMARY KEY AUTOINCREMENT, assignment_id INTEGER NOT NULL REFERENCES unit_recipe_assignments(id) ON DELETE CASCADE, order_index INTEGER NOT NULL, completed INTEGER NOT NULL DEFAULT 0, completed_at TEXT, UNIQUE(assignment_id, order_index). Per D-02 and D-03.

    In lib.rs, add Migration block with version: 21, description: "applied_recipe_assignments", sql: include_str!("../migrations/021_applied_recipe_assignments.sql"), kind: MigrationKind::Up — insert before the closing ] of get_migrations() vec (after the version 20 entry around line 126).

    Create src/types/recipeAssignment.ts per D-14 with four exports:
    - RecipeAssignment interface mirroring unit_recipe_assignments columns (id: number, unit_id: number, recipe_id: number, created_at: string)
    - CreateRecipeAssignmentInput = Omit<RecipeAssignment, "id" | "created_at">
    - StepProgress interface mirroring unit_recipe_step_progress columns (id: number, assignment_id: number, order_index: number, completed: number with "0 | 1 SQLite boolean" comment, completed_at: string | null)
    - AssignmentProgress interface (total: number, completed: number, percentage: number, bySectionId: Map<number | null, { total: number; completed: number }>)
  </action>
  <verify>
    <automated>grep -c "AUTOINCREMENT" src-tauri/migrations/021_applied_recipe_assignments.sql | findstr "2" && grep -c "version: 21" src-tauri/src/lib.rs | findstr "1" && pnpm build 2>&1 | findstr /C:"error" || echo "build check passed"</automated>
  </verify>
  <acceptance_criteria>
    - 021_applied_recipe_assignments.sql contains exactly 2 CREATE TABLE IF NOT EXISTS statements
    - unit_recipe_assignments has UNIQUE(unit_id, recipe_id) constraint
    - unit_recipe_step_progress has UNIQUE(assignment_id, order_index) constraint
    - Both tables have ON DELETE CASCADE on their FK columns
    - lib.rs contains a Migration block with version: 21 and include_str!("../migrations/021_applied_recipe_assignments.sql")
    - recipeAssignment.ts exports RecipeAssignment, CreateRecipeAssignmentInput, StepProgress, AssignmentProgress
    - StepProgress.completed typed as number (not boolean)
    - pnpm build succeeds (TypeScript compilation passes)
  </acceptance_criteria>
  <done>Migration 021 file exists with both tables, lib.rs registers version 21, type file compiles with all four exports</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure functions with TDD</name>
  <files>src/lib/computeAssignmentProgress.ts, tests/lib/computeAssignmentProgress.test.ts</files>
  <read_first>
    - src/lib/computeWorkflowPosition.ts (pure function pattern — file header, no React/DB imports, export interface + export function)
    - tests/lib/computeWorkflowPosition.test.ts (pure function test pattern — no vi.mock, describe/it structure, fixture helpers)
    - src/types/recipeAssignment.ts (the AssignmentProgress interface created in Task 1)
  </read_first>
  <behavior>
    computeCompletionPercentage:
    - (0, 0) returns 0
    - (4, 2) returns 50
    - (3, 1) returns 33 (rounded)
    - (3, 3) returns 100
    - (7, 3) returns 43 (rounds 42.857)

    computeAssignmentProgress:
    - Empty steps array returns { total: 0, completed: 0, percentage: 0, bySectionId: empty Map }
    - 4 steps, 2 completed: returns total=4, completed=2, percentage=50
    - bySectionId groups steps by section_id correctly (2 sections with different completion)
    - null section_id (flat recipe without sections) handled as a valid Map key
    - Progress records for order_index values NOT in steps array are ignored (stale progress)
    - All steps completed returns percentage=100
  </behavior>
  <action>
    RED phase: Create tests/lib/computeAssignmentProgress.test.ts first. Import only from vitest and the function under test. No vi.mock needed (pure functions). Write describe blocks for computeCompletionPercentage and computeAssignmentProgress per the behavior list above. Use inline fixture objects — no makeFixture helper needed for these simple data shapes. Run pnpm test -- tests/lib/computeAssignmentProgress.test.ts — all tests MUST fail (module not found or function not defined).

    GREEN phase: Create src/lib/computeAssignmentProgress.ts per D-07, D-08, D-09. File header comment referencing Phase 62 and AR-01. Export AssignmentProgress interface (or import from @/types/recipeAssignment if already defined there — Claude's discretion on co-location). Export computeCompletionPercentage: guard totalSteps === 0 returns 0, otherwise Math.round((completedSteps / totalSteps) * 100). Export computeAssignmentProgress: build progressMap from progress array keyed by order_index, iterate steps to count total/completed and populate bySectionId Map, return AssignmentProgress with percentage from computeCompletionPercentage. Run tests — all MUST pass.

    REFACTOR phase: Review for clarity. Ensure no imports from @/db/* or React. Commit.
  </action>
  <verify>
    <automated>pnpm test -- tests/lib/computeAssignmentProgress.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - computeCompletionPercentage(0, 0) === 0
    - computeCompletionPercentage(4, 2) === 50
    - computeCompletionPercentage(3, 1) === 33
    - computeAssignmentProgress with empty steps returns percentage 0 and empty bySectionId
    - computeAssignmentProgress correctly groups steps by section_id in bySectionId Map
    - computeAssignmentProgress ignores progress records whose order_index is not in the steps array
    - No imports from @/db/* or react in computeAssignmentProgress.ts
    - All tests pass: pnpm test -- tests/lib/computeAssignmentProgress.test.ts
  </acceptance_criteria>
  <done>Both pure functions implemented and passing all unit tests with TDD commits (RED then GREEN)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL migration | DDL executed at app startup — no user input involved |
| Pure functions | Receive pre-typed arrays from React Query hooks — no raw user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | Migration DDL | accept | DDL runs at startup from bundled SQL; no user input; FK enforcement via PRAGMA foreign_keys = ON in client.ts |
| T-62-02 | Information Disclosure | Pure functions | accept | No PII; hobby progress data only; local desktop app |
</threat_model>

<verification>
pnpm build succeeds (TypeScript compilation)
pnpm test -- tests/lib/computeAssignmentProgress.test.ts passes all tests
Migration file contains correct DDL for both tables
lib.rs registers version 21
</verification>

<success_criteria>
Migration 021 creates both tables with correct FK CASCADE chains and UNIQUE constraints.
Type file exports all four interfaces/types.
Pure functions pass comprehensive unit tests covering edge cases.
No TypeScript compilation errors.
</success_criteria>

<output>
After completion, create `.planning/phases/62-applied-recipe-data-layer/62-01-SUMMARY.md`
</output>

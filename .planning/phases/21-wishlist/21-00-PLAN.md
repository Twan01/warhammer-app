---
phase: 21-wishlist
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/wishlist/wishlistQueries.test.ts
  - tests/wishlist/WishlistPage.test.tsx
autonomous: true
requirements:
  - WISH-01
  - WISH-02
  - WISH-03
  - WISH-04

must_haves:
  truths:
    - "Test stubs exist with it.skip for all WISH-01..04 behaviors"
    - "pnpm test passes (stubs do not import non-existent modules)"
  artifacts:
    - path: "tests/wishlist/wishlistQueries.test.ts"
      provides: "SQL contract test stubs for CRUD operations"
      contains: "it.skip"
    - path: "tests/wishlist/WishlistPage.test.tsx"
      provides: "Component integration test stubs for UI behaviors"
      contains: "it.skip"
  key_links: []
---

<objective>
Create Wave 0 test stubs for Phase 21 Wishlist — Nyquist compliance foundation.

Purpose: Establish the test scaffold that Wave 1 and Wave 2 will activate. Stubs use `it.skip` so they pass immediately but document all expected behaviors. Stubs MUST NOT import any not-yet-created modules (mirrors Phase 18/19/26/27/28/29 pattern).

Output: Two test files with skipped stubs covering all 4 requirements.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-wishlist/21-CONTEXT.md
@.planning/phases/21-wishlist/21-RESEARCH.md
@.planning/phases/21-wishlist/21-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL contract test stubs</name>
  <files>tests/wishlist/wishlistQueries.test.ts</files>
  <read_first>
    - tests/battle-log/battleLogQueries.test.ts (pattern to follow for SQL contract test structure)
    - .planning/phases/21-wishlist/21-RESEARCH.md (Pitfall 7: no updated_at column)
  </read_first>
  <action>
Create `tests/wishlist/wishlistQueries.test.ts` with the following structure:

1. File-level comment: `// Wave 0 stubs — Phase 21 (WISH-01..04) SQL contract tests`
2. TODO Wave 1 import comment block (DO NOT actually import — modules do not exist yet):
   ```
   // TODO Wave 1: import { getWishlistItems, createWishlistItem, updateWishlistItem, deleteWishlistItem } from "@/db/queries/wishlistItems";
   // TODO Wave 1: import type { WishlistItem, CreateWishlistItemInput, UpdateWishlistItemInput } from "@/types/wishlistItem";
   ```
3. Import only `describe` and `it` from vitest (no vi, no mocks — stubs only).
4. Describe blocks:
   - `describe("getWishlistItems")` — 2 stubs:
     - `it.skip("returns all items ordered by created_at DESC", ...)`
     - `it.skip("returns empty array when no items exist", ...)`
   - `describe("createWishlistItem")` — 3 stubs:
     - `it.skip("inserts name, faction_id, estimated_cost_pence, notes and returns lastInsertId", ...)`
     - `it.skip("stores null for estimated_cost_pence when not provided", ...)`
     - `it.skip("stores null for notes when not provided", ...)`
   - `describe("updateWishlistItem")` — 2 stubs:
     - `it.skip("updates all fields using full-replacement UPDATE (no COALESCE)", ...)`
     - `it.skip("can clear estimated_cost_pence and notes to null", ...)`
   - `describe("deleteWishlistItem")` — 1 stub:
     - `it.skip("deletes item by id", ...)`

Each `it.skip` callback body should be empty: `() => {}`
  </action>
  <verify>
    <automated>pnpm test -- tests/wishlist/wishlistQueries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/wishlist/wishlistQueries.test.ts exists
    - File contains exactly 8 `it.skip` calls
    - File does NOT contain any `import` statement without a `//` comment prefix (no real imports of non-existent modules)
    - File contains `// TODO Wave 1: import { getWishlistItems`
    - File contains `// TODO Wave 1: import type { WishlistItem`
    - pnpm test exits 0 for this file
  </acceptance_criteria>
  <done>8 SQL contract test stubs exist, all skipped, file passes vitest</done>
</task>

<task type="auto">
  <name>Task 2: Create component integration test stubs</name>
  <files>tests/wishlist/WishlistPage.test.tsx</files>
  <read_first>
    - tests/battle-log/BattleLogPage.test.tsx (if exists — pattern for component test stubs)
    - tests/workshop-play/ (recent Wave 0 stub examples)
    - .planning/phases/21-wishlist/21-RESEARCH.md (Pitfall 6: getAllByText on shadcn Select)
  </read_first>
  <action>
Create `tests/wishlist/WishlistPage.test.tsx` with the following structure:

1. File-level comment: `// Wave 0 stubs — Phase 21 (WISH-01..04) Component integration tests`
2. TODO Wave 1/2 import comment block (DO NOT actually import):
   ```
   // TODO Wave 2: import { render, screen } from "@testing-library/react";
   // TODO Wave 2: import { WishlistPage } from "@/features/wishlist/WishlistPage";
   // TODO Wave 2: import { vi } from "vitest";
   ```
3. Pitfall annotations as file-level JSDoc:
   ```
   /**
    * Pitfall 6: shadcn Select renders selected value in both visible span AND hidden
    * native option — use getAllByText or scope queries to specific container role.
    */
   ```
4. Import only `describe` and `it` from vitest.
5. Describe blocks:
   - `describe("WishlistPage — WISH-01 (add item)")` — 2 stubs:
     - `it.skip("renders Sheet form with name, faction, cost, notes fields on Add click", ...)`
     - `it.skip("submits form and new item appears in list", ...)`
   - `describe("WishlistPage — WISH-02 (view items)")` — 3 stubs:
     - `it.skip("renders rows with name, faction name, estimated cost, notes, date", ...)`
     - `it.skip("shows empty state when no items exist", ...)`
     - `it.skip("shows total estimated cost summary bar when items exist", ...)`
   - `describe("WishlistPage — WISH-03 (delete item)")` — 2 stubs:
     - `it.skip("opens delete dialog on Delete button click", ...)`
     - `it.skip("removes item from list after confirm", ...)`
   - `describe("WishlistPage — WISH-04 (notes)")` — 1 stub:
     - `it.skip("displays notes text (truncated) on row for items with notes", ...)`

Each `it.skip` callback body should be empty: `() => {}`
  </action>
  <verify>
    <automated>pnpm test -- tests/wishlist/WishlistPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tests/wishlist/WishlistPage.test.tsx exists
    - File contains exactly 8 `it.skip` calls
    - File does NOT contain any uncommented `import` from `@/features/` or `@/db/` or `@/hooks/` or `@/types/`
    - File contains `// TODO Wave 2: import { WishlistPage }`
    - File contains `Pitfall 6` annotation
    - pnpm test exits 0 for this file
  </acceptance_criteria>
  <done>8 component test stubs exist, all skipped, file passes vitest</done>
</task>

</tasks>

<verification>
pnpm test -- tests/wishlist/ — all 16 stubs skipped, exit 0
pnpm test — full suite still passes (no import breakage)
</verification>

<success_criteria>
- 16 total it.skip stubs across 2 files covering WISH-01..04
- No non-existent module imports (TODO comments only)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/21-wishlist/21-00-SUMMARY.md`
</output>

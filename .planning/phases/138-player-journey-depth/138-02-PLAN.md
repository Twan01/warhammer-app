---
phase: 138-player-journey-depth
plan: 02
type: execute
wave: 2
depends_on: [138-01]
files_modified:
  - src/features/unit-database/UnitComparePage.tsx
  - src/features/unit-database/UnitCompareColumn.tsx
  - src/features/unit-database/UnitCompareActionBar.tsx
  - src/features/unit-database/UdbUnitRow.tsx
  - src/features/unit-database/DatabaseBrowserPage.tsx
  - tests/unit-database/UnitCompare.test.tsx
autonomous: true
requirements: [PLAY-01]
must_haves:
  truths:
    - "User can add a unit to comparison from a UDB row via a GitCompare toggle (capped at 3, with a tooltip)"
    - "A sticky 'Compare (N)' action bar appears when >=1 unit is selected and navigates to /unit-database/compare (enabled at >=2)"
    - "The compare page renders 2-3 columns (stat block + shared WeaponTable + abilities + keywords + points) using ONE batched query"
    - "Cells/rows that differ across columns get a bg-faction-accent/15 highlight; identical ones are neutral"
    - "With <2 selected ids the compare page shows the 'Select units to compare' empty state"
  artifacts:
    - path: "src/features/unit-database/UnitComparePage.tsx"
      provides: "Full-page compare layout: empty/loading/populated branches + page-level diff map"
      contains: "useUdbUnitsByIds"
      min_lines: 40
    - path: "src/features/unit-database/UnitCompareColumn.tsx"
      provides: "Per-unit column: stat block + WeaponTable + abilities + keywords + points with diff cells"
      contains: "WeaponTable"
      min_lines: 40
    - path: "src/features/unit-database/UnitCompareActionBar.tsx"
      provides: "Sticky Compare (N) action bar"
      contains: "Compare ("
    - path: "src/features/unit-database/UdbUnitRow.tsx"
      provides: "Add-to-compare GitCompare toggle (cap-aware, tooltip)"
      contains: "GitCompare"
  key_links:
    - from: "src/features/unit-database/UnitComparePage.tsx"
      to: "src/hooks/useUnitDatabase.ts"
      via: "useUdbUnitsByIds(ids) single batched call"
      pattern: "useUdbUnitsByIds"
    - from: "src/features/unit-database/UnitCompareColumn.tsx"
      to: "src/features/units/WeaponTable.tsx"
      via: "shared WeaponTable per column"
      pattern: "WeaponTable"
    - from: "src/features/unit-database/UnitCompareActionBar.tsx"
      to: "/unit-database/compare"
      via: "navigate on Compare click"
      pattern: "/unit-database/compare"
---

<objective>
Build the PLAY-01 comparison UI on top of 138-01's batch query, selection store, and
route: the full-page `UnitComparePage` (2-3 columns), the per-column `UnitCompareColumn`
(stat block + shared WeaponTable + abilities + keywords + points with binary diff
highlighting), the per-row "add to compare" affordance on `UdbUnitRow`, and the sticky
`UnitCompareActionBar` wired into `DatabaseBrowserPage`.

Purpose: Deliver the visible side-by-side comparison with differences highlighted — the
core PLAY-01 capability — reusing the shared WeaponTable (HON-08) and a single batched
fetch (no hooks-in-loop).
Output: 3 new components + 2 modified files + a component test for column rendering,
diff highlighting, and the empty state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-player-journey-depth/138-CONTEXT.md
@.planning/phases/138-player-journey-depth/138-PATTERNS.md
@.planning/phases/138-player-journey-depth/138-UI-SPEC.md
@.planning/phases/138-player-journey-depth/138-VALIDATION.md
@.planning/phases/138-player-journey-depth/138-01-SUMMARY.md

<interfaces>
<!-- From 138-01 (this plan's dependency) + existing codebase. Use directly. -->

From 138-01 (src/hooks/useUnitDatabase.ts):
  export function useUdbUnitsByIds(ids: string[]) -> useQuery returning UdbUnitDetail[] (staleTime Infinity, enabled ids.length>0)

From 138-01 (src/features/unit-database/databaseBrowserFilters.ts):
  compareIds: Set<string>; addToCompare(id); removeFromCompare(id); clearCompare()  (cap 3)

UdbUnitDetail shape (src/db/queries/unitDatabase.ts return of getUdbUnitDetail):
  { id, faction_id, name, role, base_points, damaged_w, damaged_desc,
    models: UdbModel[]  (models[0] carries M, T, Sv, W, Ld, OC stat fields),
    weapons: UdbWeapon[]  (each has weapon_group, name, category, range, attacks, skill, strength, ap, damage, keywords),
    abilities, keywords, points, composition }

Shared component (src/features/units/WeaponTable.tsx — deduped Phase 136):
  <WeaponTable weapons={...} statLabel="BS" />  (one per column; ranged uses BS, melee uses WS — mirror UdbDatasheetSheet usage)

Column-content analog (src/features/unit-database/UdbDatasheetSheet.tsx):
  - SECTION_LABEL = "text-xs font-semibold text-muted-foreground uppercase tracking-wide"
  - imports WeaponTable; renders stat block + ranged/melee WeaponTable + abilities + keywords + points sections.

Row analog (src/features/unit-database/UdbUnitRow.tsx):
  export function UdbUnitRow({ unit, onOpen, ownershipData }) — parent div has onClick={() => onOpen(unit.id)};
  any nested interactive element MUST call e.stopPropagation().
  Badge variant="outline" for owned; readiness dot via resolveReadinessDotClass(ownershipData.all_statuses).

DatabaseBrowserPage (src/features/unit-database/DatabaseBrowserPage.tsx):
  Page container div wraps the unit list/search; ownershipMap useMemo at ~88-97.

Tooltip primitive: src/components/ui/tooltip.tsx (shadcn) — Tooltip/TooltipTrigger/TooltipContent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test — compare page render, diff highlight, empty state</name>
  <files>tests/unit-database/UnitCompare.test.tsx</files>
  <read_first>
    - tests/unit-database/UdbDatasheetSheet.test.tsx (component-test idiom: mocking hooks with vi.mock, RTL render, Tauri-free)
    - tests/unit-database/UdbUnitRow.test.tsx (row-render + interaction test idiom)
    - .planning/phases/138-player-journey-depth/138-UI-SPEC.md (Surface 1 + copywriting contract — exact empty-state strings)
    - .planning/phases/138-player-journey-depth/138-VALIDATION.md (Wave 0: UnitCompare.test.tsx)
  </read_first>
  <behavior>
    - With 2 mocked units (mock useUdbUnitsByIds to return a 2-element array and mock the store to have 2 compareIds): UnitComparePage renders 2 columns, each showing both unit names and a WeaponTable region.
    - A stat that differs across the two units renders inside an element carrying the `bg-faction-accent/15` class; a stat identical across both does NOT.
    - With compareIds.size < 2 (mock store to 1 id): the page shows the empty-state heading "Select units to compare" and the "Browse Unit Database" CTA.
    - UdbUnitRow renders a GitCompare toggle button with the aria-label "Add {name} to comparison" (or "Remove ..." when selected).
  </behavior>
  <action>
    Create `tests/unit-database/UnitCompare.test.tsx`. Mock `@/hooks/useUnitDatabase` (so `useUdbUnitsByIds` returns a controlled `UdbUnitDetail[]`) and `./databaseBrowserFilters` / `@/features/unit-database/databaseBrowserFilters` (controlled `compareIds`), following the vi.mock pattern in `UdbDatasheetSheet.test.tsx`. Assert: (1) 2 columns rendered for 2 units (query by unit names); (2) the differing-stat cell has class containing `bg-faction-accent/15` (use `container.querySelector('.bg-faction-accent\\/15')` or `toHaveClass`); (3) empty state shows exact copy "Select units to compare"; (4) the UdbUnitRow GitCompare toggle exists by its aria-label. RED until Tasks 2-4 land — commit as `test(138-02): add failing compare UI tests`.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit-database/UnitCompare.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit-database/UnitCompare.test.tsx` exists with assertions for: 2-column render, diff-cell `bg-faction-accent/15` presence, empty-state copy "Select units to compare", and the row toggle aria-label.
    - Test is RED before Tasks 2-4, GREEN after.
  </acceptance_criteria>
  <done>Test file exists, fails against not-yet-built components.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UnitCompareColumn — per-unit column with diff cells (PLAY-01, D-04)</name>
  <files>src/features/unit-database/UnitCompareColumn.tsx</files>
  <read_first>
    - src/features/unit-database/UdbDatasheetSheet.tsx (stat block + ranged/melee WeaponTable + abilities + keywords + points sections; SECTION_LABEL :23; WeaponTable import :21)
    - src/features/units/WeaponTable.tsx (shared component props — statLabel union)
    - .planning/phases/138-player-journey-depth/138-PATTERNS.md (section "UnitCompareColumn.tsx (new)" lines 321-358 — diff cell wrapping)
    - .planning/phases/138-player-journey-depth/138-UI-SPEC.md (Surface 1: column card spec, stat 3x2 grid, section headers, diff treatment)
  </read_first>
  <action>
    Create `export function UnitCompareColumn({ unit, statDiffMap, allUnits }: { unit: UdbUnitDetail; statDiffMap: Map<string, boolean>; allUnits: UdbUnitDetail[] })`. Column wrapper: `bg-card border border-border/60 shadow-sm rounded-lg p-4 flex flex-col gap-4`. Header: unit name `text-base font-semibold` + faction `text-xs text-muted-foreground`. Sections "Stats" / "Weapons" / "Abilities" / "Keywords" / "Points", each header `text-xs font-semibold uppercase tracking-widest text-muted-foreground` with a `border-t border-border/40 pt-3 mt-1` separator. STATS: 3x2 grid of M, T, Sv, W, Ld, OC from `unit.models[0]`; each value `text-xl font-semibold text-center` wrapped so that when `statDiffMap.get(field)` is true the value span gets `bg-faction-accent/15 rounded px-1` (D-04 binary highlight); label `text-xs text-muted-foreground text-center`. WEAPONS: split `unit.weapons` into ranged/melee by category and render `<WeaponTable weapons={ranged} statLabel="BS" />` and `<WeaponTable weapons={melee} statLabel="WS" />` (mirror UdbDatasheetSheet); a weapon whose name is absent in at least one other unit in `allUnits` gets its row wrapper highlighted with `bg-faction-accent/15 rounded px-1` (compute the per-unit weapon-name Sets from allUnits — presence diff, not value diff). ABILITIES + KEYWORDS: flat lists; an item absent in at least one other column's set gets `bg-faction-accent/15 rounded px-1`. POINTS: each tier `text-sm tabular-nums`; tiers differing across columns get `bg-faction-accent/15 rounded`. Do NOT call any hook here — all comparison data arrives via props (page-level diff map). Reuse the `UdbUnitDetail` type imported from the queries module.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'WeaponTable' src/features/unit-database/UnitCompareColumn.tsx` >= 1 (shared component, NOT a new table variant).
    - `grep -c 'bg-faction-accent/15' src/features/unit-database/UnitCompareColumn.tsx` >= 1.
    - No React hook calls in the file (`grep -E 'use[A-Z]' ...` returns no useQuery/useState/Zustand hook — diff data is props).
    - `pnpm build` (tsc) clean.
  </acceptance_criteria>
  <done>Column renders all five sections with shared WeaponTable and binary diff highlights; receives diff data via props.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UnitComparePage + UnitCompareActionBar (PLAY-01, D-01/D-02/D-03)</name>
  <files>src/features/unit-database/UnitComparePage.tsx, src/features/unit-database/UnitCompareActionBar.tsx</files>
  <read_first>
    - src/features/dashboard/DashboardPage.tsx (full-page route component; useMemo derived maps; loading/empty/populated branches; PageHeader usage)
    - src/features/unit-database/UdbDatasheetSheet.tsx (column content reference)
    - src/features/unit-database/UnitCompareColumn.tsx (Task 2 — the column this page renders)
    - .planning/phases/138-player-journey-depth/138-PATTERNS.md (sections "UnitComparePage.tsx" lines 263-317 + "UnitCompareActionBar.tsx" lines 361-398 — statDiffMap, grid template, action bar structure)
    - .planning/phases/138-player-journey-depth/138-UI-SPEC.md (Surface 1 empty/under-2 state copy + Surface 2 action bar spec)
  </read_first>
  <action>
    (a) `UnitComparePage`: read `{ compareIds, clearCompare }` from `useDatabaseBrowserFilters`; `const ids = useMemo(() => [...compareIds].sort(), [compareIds])`; `const { data: units = [], isLoading } = useUdbUnitsByIds(ids)` — ONE batched call, never a hook in a loop (PITFALL #8). Compute `statDiffMap` at page level: `useMemo<Map<string,boolean>>` over fields ["M","T","Sv","W","Ld","OC"], `map.set(field, new Set(units.map(u => String(u.models[0]?.[field] ?? null))).size > 1)`. Three branches: (1) `isLoading` -> Skeleton; (2) `ids.length < 2` -> centered empty state heading "Select units to compare", body "Choose 2 or 3 units from the Unit Database to compare them side by side.", CTA Button "Browse Unit Database" navigating to "/unit-database"; (3) populated -> PageHeader title "Compare Units" with a ghost "Clear" Button (`onClick={clearCompare}`) + a CSS grid `style={{ gridTemplateColumns: \`repeat(${units.length}, minmax(280px, 1fr))\` }}` rendering one `<UnitCompareColumn key={unit.id} unit={unit} statDiffMap={statDiffMap} allUnits={units} />` per unit (horizontal scroll on overflow). Add an error/failed-load fallback message "Failed to load unit data. Try navigating back and reselecting." when the query errors. Export `UnitComparePage` (this replaces any stub left by 138-01).
    (b) `UnitCompareActionBar`: read `{ compareIds, clearCompare }` + `useNavigate()`; return null when `compareIds.size === 0`; container `sticky bottom-0 z-10 bg-card border-t border-border/60 shadow-lg px-4 py-3 flex items-center justify-between` (treat `py-3`/`px-4` as intended per UI-SPEC FLAG); left label `"{count} unit selected" / "{count} units selected"` in `text-sm font-medium`; right group: outline "Clear" Button (`onClick={clearCompare}`) + default "Compare ({count})" Button `disabled={compareIds.size < 2}` navigating `to: "/unit-database/compare"`.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit-database/UnitCompare.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useUdbUnitsByIds' src/features/unit-database/UnitComparePage.tsx` === 1 (single batched call, not in a `.map`).
    - UnitComparePage contains the exact strings "Select units to compare" and "Compare Units".
    - UnitCompareActionBar contains "Compare (" and `disabled` tied to `< 2`.
    - UnitCompare.test.tsx GREEN for the 2-column render + diff-cell + empty-state assertions.
  </acceptance_criteria>
  <done>Compare page renders 2-3 columns from one batched query with a page-level diff map; empty state and action bar work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: UdbUnitRow add-to-compare toggle + wire action bar in DatabaseBrowserPage (PLAY-01, D-03)</name>
  <files>src/features/unit-database/UdbUnitRow.tsx, src/features/unit-database/DatabaseBrowserPage.tsx</files>
  <read_first>
    - src/features/unit-database/UdbUnitRow.tsx (existing row :70-108 — onOpen parent click; Badge; points span)
    - src/features/unit-database/DatabaseBrowserPage.tsx (page container + ownershipMap :88-97; where to slot the sticky bar)
    - src/components/ui/tooltip.tsx (Tooltip primitive — for the icon-only button, UI-SPEC FLAG)
    - .planning/phases/138-player-journey-depth/138-PATTERNS.md (section "UdbUnitRow.tsx" compare toggle lines 463-492 + DatabaseBrowserPage wiring lines 539-575)
    - .planning/phases/138-player-journey-depth/138-UI-SPEC.md (Surface 2 toggle spec + copywriting aria-labels)
  </read_first>
  <action>
    (a) `UdbUnitRow`: import `{ GitCompare }` from "lucide-react", `{ useDatabaseBrowserFilters }` from "./databaseBrowserFilters", Button, and the Tooltip primitives. Read `{ compareIds, addToCompare, removeFromCompare }`; `const isInCompare = compareIds.has(unit.id)`; `const compareDisabled = compareIds.size >= 3 && !isInCompare`. Add a `Button variant="ghost" size="icon" className="h-6 w-6 shrink-0"` BEFORE the points span, with `text-faction-accent bg-faction-accent/10` when `isInCompare`, `opacity-50 cursor-not-allowed` when `compareDisabled`, `disabled={compareDisabled}`, a `GitCompare size={16}` icon, and `onClick={(e) => { e.stopPropagation(); isInCompare ? removeFromCompare(unit.id) : addToCompare(unit.id); }}` (e.stopPropagation REQUIRED — parent div has onOpen). aria-label "Add {unit.name} to comparison" / "Remove {unit.name} from comparison". Wrap the button in a Tooltip (TooltipTrigger asChild) with TooltipContent matching the aria-label (UI-SPEC FLAG: icon-only button needs a tooltip).
    (b) `DatabaseBrowserPage`: import and render `<UnitCompareActionBar />` as a sibling inside the page container, after the main list/search content, so it sticks to the bottom of the scroll region (`sticky bottom-0` handled inside the bar component). No other changes in this task.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit-database/UnitCompare.test.tsx tests/unit-database/UdbUnitRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GitCompare' src/features/unit-database/UdbUnitRow.tsx` >= 1 and the toggle onClick calls `e.stopPropagation()`.
    - `grep -c 'compareDisabled' src/features/unit-database/UdbUnitRow.tsx` >= 1 (cap enforced at the affordance, not just render).
    - The toggle is wrapped in a Tooltip (`grep -c 'Tooltip' src/features/unit-database/UdbUnitRow.tsx` >= 1).
    - `grep -c 'UnitCompareActionBar' src/features/unit-database/DatabaseBrowserPage.tsx` >= 1.
    - UnitCompare.test.tsx + UdbUnitRow.test.tsx GREEN.
  </acceptance_criteria>
  <done>Each UDB row has a cap-aware, tooltip'd add-to-compare toggle; the sticky action bar shows on the browser page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI selection -> route/state | compareIds drive the batched query (boundary handled/parameterized in 138-01). This plan is pure client render of already-fetched, read-only canonical data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-03 | Tampering | compare render path | accept | No DB writes, no user-supplied SQL; ids parameterized in 138-01. Pure read-only render. |
| T-138-04 | Denial of service | unbounded compare set | mitigate | Hard cap of 3 enforced at the affordance (compareDisabled) AND in the store (138-01) — bounds the batched query size. |
| T-138-SC | Tampering | npm installs | accept | No package installs this phase; audit table N/A. |
</threat_model>

<verification>
- `pnpm test -- tests/unit-database/` green (compare UI + row toggle).
- `pnpm build` (tsc) clean.
- Manual (per VALIDATION.md): open Unit Database -> select 2-3 -> Compare -> columns align, diffs highlighted, WeaponTable per column (EN+FR).
</verification>

<success_criteria>
- The user can select up to 3 units and view them side by side with differences highlighted.
- The compare page uses exactly one batched query (no hooks-in-loop).
- The shared WeaponTable (HON-08) is reused per column (no new table variant).
</success_criteria>

<output>
Create `.planning/phases/138-player-journey-depth/138-02-SUMMARY.md` when done.
</output>

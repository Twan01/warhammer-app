---
phase: 138-player-journey-depth
plan: 03
type: execute
wave: 3
depends_on: [138-01, 138-02]
files_modified:
  - src/db/queries/unitDatabase.ts
  - src/hooks/useUnitDatabase.ts
  - src/hooks/useUnits.ts
  - src/features/unit-database/UdbUnitRow.tsx
  - src/features/unit-database/UdbDatasheetSheet.tsx
  - src/features/unit-database/UdbSearchResults.tsx
  - src/features/unit-database/DatabaseBrowserPage.tsx
  - src/features/units/collectionFilters.ts
  - src/features/units/applyUnitFilters.ts
  - tests/unit-database/udbOwnership.test.ts
  - tests/unit-database/UdbSearchResults.test.tsx
  - tests/collection/applyUnitFilters.test.ts
autonomous: true
requirements: [PLAY-04]
must_haves:
  truths:
    - "The already-shipped Collection<->UDB loop (owned badge, View Datasheet, Add to Collection) is verified working end-to-end before any change"
    - "The 'Owned xN' badge on a UDB row and in the datasheet header deep-links into the Collection pre-filtered to that udb unit"
    - "Cross-faction search results (UdbSearchResults) show owned badges sourced from a faction-agnostic ownership Map"
    - "getOwnedCountsByUdbUnitId returns owned counts grouped by udb_unit_id with no faction scope"
    - "units create/update/delete mutations invalidate the new ['udb-ownership-all'] key (invalidation symmetry, D-08)"
  artifacts:
    - path: "src/db/queries/unitDatabase.ts"
      provides: "getOwnedCountsByUdbUnitId faction-agnostic GROUP BY"
      contains: "getOwnedCountsByUdbUnitId"
    - path: "src/hooks/useUnitDatabase.ts"
      provides: "useUdbOwnershipAll hook (staleTime 0)"
      contains: "useUdbOwnershipAll"
    - path: "src/features/units/collectionFilters.ts"
      provides: "udbUnitIdFilter state + setter + clearAll reset"
      contains: "udbUnitIdFilter"
    - path: "src/features/units/applyUnitFilters.ts"
      provides: "udbUnitIdFilter clause"
      contains: "udbUnitIdFilter"
  key_links:
    - from: "src/features/unit-database/UdbUnitRow.tsx"
      to: "/collection"
      via: "owned badge Link + setUdbUnitIdFilter"
      pattern: "setUdbUnitIdFilter"
    - from: "src/features/unit-database/UdbSearchResults.tsx"
      to: "src/features/unit-database/DatabaseBrowserPage.tsx"
      via: "ownershipAllMap prop"
      pattern: "ownershipAllMap"
    - from: "src/hooks/useUnits.ts"
      to: "src/hooks/useUnitDatabase.ts"
      via: "invalidate ['udb-ownership-all'] on unit mutations"
      pattern: "udb-ownership-all"
---

<objective>
Close the two confirmed PLAY-04 gaps after a verification pass that the existing loop
works: (D-06) make the "Owned xN" badge a deep link into the filtered Collection, and
(D-07) surface owned badges in cross-faction search via a faction-agnostic ownership
query + Map. Maintain invalidation symmetry (D-08) so counts never go stale.

Purpose: Complete the bidirectional Collection<->Unit Database loop everywhere the user
can see a unit, WITHOUT rebuilding the already-shipped badge / View Datasheet / Add to
Collection flows (D-05 — "Bulletproof & Honest" milestone: audit, do not rebuild).
Output: verification notes in the SUMMARY, the deep-link, the search ownership coverage,
the new query/hook, and the invalidation wiring + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-player-journey-depth/138-CONTEXT.md
@.planning/phases/138-player-journey-depth/138-RESEARCH.md
@.planning/phases/138-player-journey-depth/138-PATTERNS.md
@.planning/phases/138-player-journey-depth/138-UI-SPEC.md
@.planning/phases/138-player-journey-depth/138-VALIDATION.md

<interfaces>
<!-- Existing contracts (verified by researcher). Use directly. -->

Faction-scoped ownership analog (src/db/queries/unitDatabase.ts:292):
  getUdbOwnershipByFaction(factionId): SELECT u.udb_unit_id, COUNT(*) AS owned_count,
    GROUP_CONCAT(u.status_painting,'|') AS all_statuses FROM units u JOIN udb_units uu ON uu.id=u.udb_unit_id
    WHERE uu.faction_id=$1 AND u.udb_unit_id IS NOT NULL GROUP BY u.udb_unit_id
  -> returns UdbOwnershipEntry[]  (REUSE this same type for the new query — no new type)

Hook analog (src/hooks/useUnitDatabase.ts:135):
  UDB_OWNERSHIP_KEY = (factionId) => ["udb-ownership", factionId]
  useUdbOwnership(factionId): useQuery({ ..., staleTime: 0 })  // dynamic, NOT Infinity

units mutation invalidation today (src/hooks/useUnits.ts:49,74,94):
  create/update/delete each call qc.invalidateQueries({ queryKey: ["udb-ownership"] })  // prefix-matches faction-scoped key
  NOTE: ["udb-ownership-all"] is a DIFFERENT key — prefix invalidation of ["udb-ownership"] does NOT cover it. Must add explicitly.

UdbUnitRow owned badge (src/features/unit-database/UdbUnitRow.tsx:92-101):
  Badge variant="outline" "Owned x{owned_count}" — currently NO Link/navigate (the D-06 gap).
  Parent div has onClick onOpen — nested Link MUST e.stopPropagation().

UdbDatasheetSheet owned badge (header, ~lines 109-113): same Badge — wrap in Link (D-06).

UdbSearchResults (src/features/unit-database/UdbSearchResults.tsx):
  Props today: { query: string; onSelectResult: (unitId: string) => void }. result.unit_id is the udb id.
  NO ownership data today (the D-07 gap, confirmed real).

collectionFilters (src/features/units/collectionFilters.ts):
  Zustand store with subFactionFilter/setSubFactionFilter/clearAll — the exact pattern to mirror for udbUnitIdFilter.

applyUnitFilters (src/features/units/applyUnitFilters.ts):
  UnitFiltersInput + filter body of `if (...) return false` clauses. Unit type HAS udb_unit_id (src/types/unit.ts:54).

DatabaseBrowserPage ownershipMap useMemo (src/features/unit-database/DatabaseBrowserPage.tsx:88-97) — the model for ownershipAllMap.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verification pass — confirm the shipped loop works (D-05, no rebuild)</name>
  <files>(verification only — record findings in SUMMARY; no production edits in this task)</files>
  <read_first>
    - src/features/units/UnitDetailSheet.tsx (:108-119 — Collection -> View Datasheet navigate to /unit-database with udbUnitId search)
    - src/features/unit-database/UdbDatasheetSheet.tsx (:100-114 — onAddToCollection)
    - src/features/unit-database/DatabaseBrowserPage.tsx (:157 handleAddToCollection, :178 handleFactionLinkConfirm, :88-97 ownershipMap)
    - src/features/unit-database/UdbUnitRow.tsx (:92-101 owned badge)
    - src/hooks/useUnits.ts (:39-94 — confirm units mutations invalidate ["udb-ownership"])
    - tests/unit-database/UdbDatasheetSheet.test.tsx, tests/unit-database/UdbUnitRow.test.tsx (existing coverage)
  </read_first>
  <action>
    Run the existing unit-database test suite and read the four shipped flows to confirm they work end-to-end BEFORE changing anything (D-05): (1) owned-count badge populates from DatabaseBrowserPage.ownershipMap, (2) Collection -> "View Datasheet" navigates to /unit-database?udbUnitId=..., (3) UDB -> "Add to Collection" creates a unit pre-linked via udb_unit_id, (4) units mutations invalidate ["udb-ownership"]. Record a short verification note in the plan SUMMARY (one line per flow: working / regression-found). Do NOT re-implement or refactor any of these flows. If a flow is actually broken, STOP and flag it in the SUMMARY rather than silently fixing scope creep.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit-database/ tests/units/</automated>
  </verify>
  <acceptance_criteria>
    - Existing unit-database + units suites are GREEN (no regression baseline).
    - SUMMARY records a verification line for each of the four shipped flows.
    - No production source file modified in this task.
  </acceptance_criteria>
  <done>The four shipped loop flows are confirmed working (or a real break is flagged); no rebuild performed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 tests — faction-agnostic ownership query, search badges, collection filter clause</name>
  <files>tests/unit-database/udbOwnership.test.ts, tests/unit-database/UdbSearchResults.test.tsx, tests/collection/applyUnitFilters.test.ts</files>
  <read_first>
    - tests/unit-database/unitDatabase.queries.test.ts (DB-mock idiom for query tests)
    - tests/unit-database/UdbUnitRow.test.tsx (component render/interaction idiom; navigate/Link mocking)
    - tests/collection/ (existing collection tests — confirm location of applyUnitFilters coverage)
    - .planning/phases/138-player-journey-depth/138-VALIDATION.md (Wave 0 requirements)
    - .planning/phases/138-player-journey-depth/138-PATTERNS.md (applyUnitFilters clause lines 631-672; UdbSearchResults badges lines 496-535)
  </read_first>
  <behavior>
    - getOwnedCountsByUdbUnitId() returns rows grouped by udb_unit_id with owned_count + all_statuses, across ALL factions, excluding NULL udb_unit_id.
    - UdbSearchResults, given an ownershipAllMap containing an entry for a result's unit_id, renders an "Owned x{n}" badge for that row; given no entry, renders no badge.
    - applyUnitFilters with udbUnitIdFilter="X" keeps only units whose udb_unit_id === "X"; with udbUnitIdFilter null/undefined it is a no-op.
  </behavior>
  <action>
    Create `tests/unit-database/udbOwnership.test.ts` (mock getDb().select; assert the SELECT has no faction JOIN/WHERE and groups by udb_unit_id). Create `tests/unit-database/UdbSearchResults.test.tsx` (mock useUdbSearch to return 1-2 results; render with an ownershipAllMap prop; assert badge present/absent). Create or extend `tests/collection/applyUnitFilters.test.ts` (pure function; assert the udbUnitIdFilter clause). RED until Tasks 3-5 land — commit as `test(138-03): add failing tests for ownership-all + search badges + collection filter`.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit-database/udbOwnership.test.ts tests/unit-database/UdbSearchResults.test.tsx tests/collection/applyUnitFilters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Three test files exist with the behaviors above.
    - applyUnitFilters test asserts both the match-only case and the null no-op case.
    - Tests RED before Tasks 3-5, GREEN after.
  </acceptance_criteria>
  <done>Three Wave 0 test files exist and fail against not-yet-built code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: getOwnedCountsByUdbUnitId + useUdbOwnershipAll + invalidation symmetry (PLAY-04, D-07/D-08)</name>
  <files>src/db/queries/unitDatabase.ts, src/hooks/useUnitDatabase.ts, src/hooks/useUnits.ts</files>
  <read_first>
    - src/db/queries/unitDatabase.ts (getUdbOwnershipByFaction :292-307 — the GROUP BY to make faction-agnostic; getUdbOwnershipForUnit :267)
    - src/hooks/useUnitDatabase.ts (useUdbOwnership :125-148 — staleTime 0, key factory)
    - src/hooks/useUnits.ts (:39-94 — the three mutation onSuccess blocks invalidating ["udb-ownership"])
    - .planning/phases/138-player-journey-depth/138-PATTERNS.md (getOwnedCountsByUdbUnitId lines 83-118; useUdbOwnershipAll lines 167-197)
  </read_first>
  <action>
    (a) QUERY: Add `export async function getOwnedCountsByUdbUnitId(): Promise<UdbOwnershipEntry[]>` to `unitDatabase.ts` — the faction-agnostic superset of `getUdbOwnershipByFaction`: `SELECT u.udb_unit_id, COUNT(*) AS owned_count, GROUP_CONCAT(u.status_painting,'|') AS all_statuses FROM units u WHERE u.udb_unit_id IS NOT NULL GROUP BY u.udb_unit_id` — no JOIN, no params (no injection surface; T-138-05). Reuse the existing `UdbOwnershipEntry` type.
    (b) HOOK: Add `export const UDB_OWNERSHIP_ALL_KEY = ["udb-ownership-all"] as const` and `export function useUdbOwnershipAll()` to `useUnitDatabase.ts` — `useQuery({ queryKey: UDB_OWNERSHIP_ALL_KEY, queryFn: getOwnedCountsByUdbUnitId, staleTime: 0 })` (dynamic, like useUdbOwnership; no enabled guard, no gcTime Infinity). Add `getOwnedCountsByUdbUnitId` to the import from the queries module.
    (c) INVALIDATION (D-08): In `useUnits.ts`, add `qc.invalidateQueries({ queryKey: ["udb-ownership-all"] })` to EACH of the three mutation onSuccess blocks (create/update/delete) right next to the existing `["udb-ownership"]` invalidation. The new key is distinct and is NOT covered by the existing prefix invalidation — this is the PITFALL #9 symmetry fix.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit-database/udbOwnership.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getOwnedCountsByUdbUnitId' src/db/queries/unitDatabase.ts` >= 1; the query has `GROUP BY u.udb_unit_id`, `WHERE u.udb_unit_id IS NOT NULL`, and NO faction JOIN.
    - `grep -c 'useUdbOwnershipAll' src/hooks/useUnitDatabase.ts` >= 1 with `staleTime: 0`.
    - `grep -c 'udb-ownership-all' src/hooks/useUnits.ts` === 3 (one per mutation).
    - udbOwnership.test.ts GREEN; `pnpm build` clean.
  </acceptance_criteria>
  <done>Faction-agnostic ownership query + hook exist; all three units mutations invalidate ["udb-ownership-all"].</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Collection deep-link filter — collectionFilters + applyUnitFilters (PLAY-04, D-06)</name>
  <files>src/features/units/collectionFilters.ts, src/features/units/applyUnitFilters.ts</files>
  <read_first>
    - src/features/units/collectionFilters.ts (subFactionFilter/setSubFactionFilter/clearAll — exact pattern to mirror)
    - src/features/units/applyUnitFilters.ts (UnitFiltersInput + filter clause body)
    - src/types/unit.ts (:54 udb_unit_id on Unit — confirm the field exists on the generic)
    - .planning/phases/138-player-journey-depth/138-PATTERNS.md (collectionFilters lines 579-627; applyUnitFilters lines 631-672)
  </read_first>
  <action>
    (a) `collectionFilters.ts`: add to the interface `udbUnitIdFilter: string | null; setUdbUnitIdFilter: (id: string | null) => void;`; in create() initial state `udbUnitIdFilter: null`; action `setUdbUnitIdFilter: (id) => set({ udbUnitIdFilter: id })`; add `udbUnitIdFilter: null` to the `clearAll` reset object (so the deep-link filter clears with the rest).
    (b) `applyUnitFilters.ts`: add `udbUnitIdFilter?: string | null;` to `UnitFiltersInput`; add the clause `if (filters.udbUnitIdFilter && unit.udb_unit_id !== filters.udbUnitIdFilter) return false;` as the first clause in the filter body. `unit.udb_unit_id` is on the `Unit` type (confirmed) — no narrowing needed.
    Wire the consumer: ensure the Collection page that calls `applyUnitFilters` passes `udbUnitIdFilter` from `useCollectionFilters` into the filters input (find the existing call site that already spreads the other collectionFilters fields and add `udbUnitIdFilter` to it).
  </action>
  <verify>
    <automated>pnpm test -- tests/collection/applyUnitFilters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'udbUnitIdFilter' src/features/units/collectionFilters.ts` >= 3 (field + setter + clearAll).
    - `grep -c 'udbUnitIdFilter' src/features/units/applyUnitFilters.ts` >= 2 (interface + clause).
    - The Collection page call site passes udbUnitIdFilter into applyUnitFilters.
    - applyUnitFilters.test.ts GREEN; `pnpm build` clean.
  </acceptance_criteria>
  <done>collectionFilters holds udbUnitIdFilter; applyUnitFilters honors it; the Collection page consumes it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Owned-badge deep links + search ownership wiring (PLAY-04, D-06/D-07)</name>
  <files>src/features/unit-database/UdbUnitRow.tsx, src/features/unit-database/UdbDatasheetSheet.tsx, src/features/unit-database/UdbSearchResults.tsx, src/features/unit-database/DatabaseBrowserPage.tsx</files>
  <read_first>
    - src/features/unit-database/UdbUnitRow.tsx (owned badge :92-101 — wrap in Link, e.stopPropagation since parent has onOpen; the compare toggle from 138-02 is already here)
    - src/features/unit-database/UdbDatasheetSheet.tsx (header owned badge ~:109-113)
    - src/features/unit-database/UdbSearchResults.tsx (props + result row rendering; result.unit_id is the udb id)
    - src/features/unit-database/DatabaseBrowserPage.tsx (ownershipMap useMemo :88-97; where UdbSearchResults is rendered; the UnitCompareActionBar wired by 138-02 is already here)
    - .planning/phases/138-player-journey-depth/138-PATTERNS.md (UdbUnitRow owned-badge Link lines 437-461; UdbSearchResults badges lines 496-535; DatabaseBrowserPage ownershipAllMap lines 539-575)
    - .planning/phases/138-player-journey-depth/138-UI-SPEC.md (Surface 3 — badge stays variant="outline", gains hover:bg-secondary cursor-pointer, NO underline/icon)
  </read_first>
  <action>
    (a) `UdbUnitRow`: import `{ Link }` from "@tanstack/react-router" and `{ useCollectionFilters }` from "@/features/units/collectionFilters"; `const setUdbUnitIdFilter = useCollectionFilters((s) => s.setUdbUnitIdFilter)`. Wrap the existing owned `Badge variant="outline"` in `<Link to="/collection" onClick={(e) => { e.stopPropagation(); setUdbUnitIdFilter(unit.id); }} aria-label={\`View ${ownershipData.owned_count} owned ${unit.name} in Collection\`}>` and add `hover:bg-secondary cursor-pointer` to the badge className (keep variant="outline", NO underline, NO external-link icon — UI-SPEC Surface 3).
    (b) `UdbDatasheetSheet`: same Link treatment on the header owned badge, calling `setUdbUnitIdFilter(unit.id)` then navigating to /collection (the sheet unmounts on route change).
    (c) `UdbSearchResults`: add optional prop `ownershipAllMap?: Map<string, { owned_count: number; all_statuses: string }>`; for each result, look up `ownershipAllMap?.get(result.unit_id)` and, when owned_count > 0, render the same owned `Badge` wrapped in the same `/collection` Link (calling setUdbUnitIdFilter(result.unit_id), e.stopPropagation). Import Link + useCollectionFilters.
    (d) `DatabaseBrowserPage`: call `const { data: ownershipAllEntries = [] } = useUdbOwnershipAll()`; build `ownershipAllMap` via a useMemo mirroring the existing ownershipMap (lines 88-97); pass `ownershipAllMap={ownershipAllMap}` to `<UdbSearchResults ... />`. Leave the faction-scoped `ownershipMap`/`useUdbOwnership` flow untouched (coexist — RESEARCH OQ#3).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit-database/UdbSearchResults.test.tsx tests/unit-database/UdbUnitRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'setUdbUnitIdFilter' src/features/unit-database/UdbUnitRow.tsx` >= 1 and the owned badge is wrapped in a `Link to="/collection"` with `e.stopPropagation()`.
    - `grep -c 'Link' src/features/unit-database/UdbDatasheetSheet.tsx` >= 1 on the owned badge.
    - `grep -c 'ownershipAllMap' src/features/unit-database/UdbSearchResults.tsx` >= 1 and badges render from it.
    - `grep -c 'useUdbOwnershipAll' src/features/unit-database/DatabaseBrowserPage.tsx` >= 1 and ownershipAllMap is passed to UdbSearchResults.
    - UdbSearchResults.test.tsx + UdbUnitRow.test.tsx GREEN.
  </acceptance_criteria>
  <done>Owned badges deep-link to the filtered Collection from the row, datasheet header, and search results; search ownership is sourced from the faction-agnostic Map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> SQLite query | getOwnedCountsByUdbUnitId is a parameterless SELECT aggregate over the local units table — no user-supplied input. |
| UDB -> Collection navigation | The owned-badge deep link sets a Zustand filter (udbUnitIdFilter) then navigates; the value is a canonical udb id from a rendered row, not free-form text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-05 | Tampering (SQL injection) | getOwnedCountsByUdbUnitId | accept | No parameters and no string interpolation — static SELECT aggregate; no injection surface. |
| T-138-06 | Spoofing/Tampering (stale state) | owned-count cache | mitigate | useUdbOwnershipAll uses staleTime:0 AND units mutations explicitly invalidate ["udb-ownership-all"] (D-08) — counts cannot silently desync after add/remove. |
| T-138-07 | Information disclosure | deep-link filter | accept | Navigates within the local app to a filtered Collection of the user's own units; no new data exposure. |
| T-138-SC | Tampering | npm installs | accept | No package installs this phase; audit table N/A. |
</threat_model>

<verification>
- `pnpm test -- tests/unit-database/ tests/collection/ tests/units/` green.
- `pnpm build` (tsc) clean.
- Manual (per VALIDATION.md): click "Owned xN" on a UDB row -> lands on Collection filtered to that unit; Collection->View Datasheet resolves; UDB->Add to Collection increments owned count; search results show owned badges.
</verification>

<success_criteria>
- The bidirectional loop is closed everywhere a unit is visible (row, datasheet header, cross-faction search).
- No shipped flow was rebuilt (D-05) — only the two confirmed gaps closed + invalidation symmetry added.
- Owned counts never go stale after a units mutation.
</success_criteria>

<output>
Create `.planning/phases/138-player-journey-depth/138-03-SUMMARY.md` when done (include the Task 1 verification notes).
</output>

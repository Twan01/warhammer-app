---
phase: 138-player-journey-depth
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/dashboard/GoalProgressCard.tsx
  - src/features/dashboard/DashboardPage.tsx
  - tests/dashboard/GoalProgressCard.test.tsx
  - tests/dashboard/goalProgressInvalidation.test.ts
autonomous: true
requirements: [PLAY-05]
must_haves:
  truths:
    - "The dashboard shows a 'Hobby Goals' section (left column, after Hobby Health) with one progress bar per active goal"
    - "Each goal row shows name, period label, 'count / target', and a progress bar fill keyed to status (accent active / battle-gold completed)"
    - "When no active goals exist, the widget shows 'No active goals.' with a 'Set a hobby goal ->' link to /goals"
    - "Logging a painting session invalidates GOAL_PROGRESS_KEY (regression-locked so the widget never goes stale)"
  artifacts:
    - path: "src/features/dashboard/GoalProgressCard.tsx"
      provides: "Compact active-goals widget consuming useGoals + useGoalProgress"
      contains: "useGoalProgress"
      min_lines: 30
    - path: "src/features/dashboard/DashboardPage.tsx"
      provides: "'Hobby Goals' section rendering GoalProgressCard"
      contains: "GoalProgressCard"
  key_links:
    - from: "src/features/dashboard/GoalProgressCard.tsx"
      to: "src/hooks/useGoals.ts"
      via: "useGoals() + useGoalProgress()"
      pattern: "useGoalProgress"
    - from: "src/features/dashboard/DashboardPage.tsx"
      to: "src/features/dashboard/GoalProgressCard.tsx"
      via: "import + render in left column"
      pattern: "GoalProgressCard"
    - from: "src/features/dashboard/GoalProgressCard.tsx"
      to: "/goals"
      via: "empty-state Link"
      pattern: "/goals"
---

<objective>
Surface hobby goal progress on the dashboard (PLAY-05): a compact `GoalProgressCard`
that consumes the EXISTING `useGoals()` + `useGoalProgress()` hooks (no new query, no
derivation rewrite — D-09) and renders one progress bar per active goal, with an empty
state linking to GoalsPage. Lock the painting-session -> goal-progress invalidation with
a regression test.

Purpose: Passive visibility of goal progress on the dashboard, reusing the verified
rules.db-free derivation and the GoalCard progress-bar idiom.
Output: 1 new component, the dashboard section wiring, a render+empty-state test, and an
invalidation regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-player-journey-depth/138-CONTEXT.md
@.planning/phases/138-player-journey-depth/138-PATTERNS.md
@.planning/phases/138-player-journey-depth/138-UI-SPEC.md
@.planning/phases/138-player-journey-depth/138-VALIDATION.md

<interfaces>
<!-- Existing contracts (verified by researcher). Use directly — no rebuild. -->

Goal hooks (src/hooks/useGoals.ts:16-27):
  export function useGoals()        -> useQuery<HobbyGoal[]>
  export function useGoalProgress() -> useQuery<Map<goalId, count>>  (GOAL_PROGRESS_KEY = ["goal-progress"])
  Goal create/update/delete mutations already invalidate ["goal-progress"].

Goal derivation (src/db/queries/goals.ts): getGoalProgress queries only painting_sessions + hobby_goals (rules.db-free; reuse as-is).

Period helper (src/lib/computeGoalPeriod.ts):
  computeGoalPeriod(timeframe, period) -> { label, isExpired, ... }
  deriveGoalStatus(progressCount, target, isExpired) -> "active" | "completed" | "missed"

GoalCard progress-bar idiom to ADAPT (NOT import) — src/features/goals/GoalCard.tsx:
  Track:  <div className="h-1.5 w-full rounded-full bg-border/40">
  Fill:   <div className={`h-1.5 rounded-full transition-all duration-500 ${fillColor}`} style={{ width: `${pct}%` }} />
  pct:    Math.min(100, Math.round((count / Math.max(1, target_count)) * 100))
  fillColor: completed -> bg-battle-gold ; active -> bg-faction-accent

CONFIRMED (RESEARCH OQ#1 resolved): src/hooks/useJournalSessions.ts useCreatePaintingSession ALREADY
  invalidates ["goal-progress"] (and useDeletePaintingSession too). No new invalidation needed —
  only a regression test that locks this behavior.

Dashboard left column (src/features/dashboard/DashboardPage.tsx):
  Left column is a `<div className="flex flex-col gap-6">` containing "Command Center", "Hobby Health", "By Faction" sections.
  Section header pattern: <p className="text-sm font-semibold uppercase tracking-widest text-muted-foreground">{NAME}</p>
  Insert the new "Hobby Goals" <section> AFTER "Hobby Health" (per UI-SPEC Surface 4 + RESEARCH OQ#2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 tests — widget render/empty-state + session invalidation regression</name>
  <files>tests/dashboard/GoalProgressCard.test.tsx, tests/dashboard/goalProgressInvalidation.test.ts</files>
  <read_first>
    - tests/dashboard/ (existing dashboard tests — vi.mock idiom for useGoals/useGoalProgress; RTL render)
    - tests/goals/ (existing goal tests — GoalCard render patterns, HobbyGoal mock shape)
    - src/hooks/useJournalSessions.ts (useCreatePaintingSession onSuccess :56-69 — the ["goal-progress"] invalidation to lock)
    - .planning/phases/138-player-journey-depth/138-UI-SPEC.md (Surface 4 + copywriting: exact empty-state strings)
    - .planning/phases/138-player-journey-depth/138-VALIDATION.md (Wave 0 requirements incl. the invalidation assertion)
  </read_first>
  <behavior>
    - GoalProgressCard with 2 mocked active goals + a progress Map: renders both goal names, a "{count} / {target}" label per goal, and a progress-bar fill element per goal.
    - GoalProgressCard with empty goals: renders "No active goals." and a Link with text "Set a hobby goal ->" pointing to /goals.
    - Invalidation regression: useCreatePaintingSession's onSuccess invalidates the ["goal-progress"] query key (assert via a spied/mocked QueryClient.invalidateQueries, OR assert the source contains the invalidation — prefer a behavioral spy if the existing session-hook tests already mock the QueryClient).
  </behavior>
  <action>
    Create `tests/dashboard/GoalProgressCard.test.tsx` mocking `@/hooks/useGoals` (controlled goals + progress Map) and `@/lib/computeGoalPeriod` if needed for deterministic isExpired/label, following the dashboard test mock idiom. Assert populated render (2 goals, labels, fills) and empty state (exact copy "No active goals." + "Set a hobby goal ->" Link to /goals). Create `tests/dashboard/goalProgressInvalidation.test.ts` asserting useCreatePaintingSession invalidates ["goal-progress"] (mirror how existing session-hook tests assert invalidation; if none exist, mock @tanstack/react-query useQueryClient and spy invalidateQueries). RED until Task 2 (widget) lands; the invalidation test should be GREEN immediately (behavior already shipped) — if it is RED, that means the invalidation regressed and Task 2 must restore it. Commit as `test(138-04): add failing goal widget tests + invalidation regression lock`.
  </action>
  <verify>
    <automated>pnpm test -- tests/dashboard/GoalProgressCard.test.tsx tests/dashboard/goalProgressInvalidation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/dashboard/GoalProgressCard.test.tsx` exists with populated + empty-state assertions (exact copy "No active goals." and "Set a hobby goal ->").
    - `tests/dashboard/goalProgressInvalidation.test.ts` exists and asserts the ["goal-progress"] invalidation on session create.
    - Widget render tests RED before Task 2; invalidation test GREEN (already-shipped behavior).
  </acceptance_criteria>
  <done>Both test files exist; widget tests fail against the not-yet-built component, invalidation test passes (locking shipped behavior).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GoalProgressCard widget + dashboard "Hobby Goals" section (PLAY-05, D-09/D-10)</name>
  <files>src/features/dashboard/GoalProgressCard.tsx, src/features/dashboard/DashboardPage.tsx</files>
  <read_first>
    - src/features/goals/GoalCard.tsx (progress-bar idiom :72-77, fillColor :21-26, pct :17-18 — ADAPT, do not import)
    - src/features/dashboard/DashboardPage.tsx ("Hobby Health" section ~:386-411 — section header pattern + left-column container; insertion point after "By Faction" ~:435)
    - src/hooks/useGoals.ts (useGoals, useGoalProgress, GOAL_PROGRESS_KEY)
    - src/lib/computeGoalPeriod.ts (computeGoalPeriod, deriveGoalStatus)
    - src/features/goals/GoalsPage.tsx (the /goals empty-state link target)
    - .planning/phases/138-player-journey-depth/138-PATTERNS.md (GoalProgressCard full shape lines 676-765; DashboardPage section lines 769-798)
    - .planning/phases/138-player-journey-depth/138-UI-SPEC.md (Surface 4 — placement, goal-row layout, empty-state card, progress label "{count} / {target}")
  </read_first>
  <action>
    (a) Create `export function GoalProgressCard()` in `src/features/dashboard/GoalProgressCard.tsx` consuming `const { data: goals = [] } = useGoals()` and `const { data: progressMap } = useGoalProgress()` (D-09 — NO new query/derivation). Filter to active goals: `goals.filter(g => !computeGoalPeriod(g.timeframe, g.period).isExpired)` (show active + completed; hide expired/missed to keep the card optimistic). EMPTY STATE (no active goals): a single `Card` (`bg-card border border-border/60 shadow-sm`) with `CardContent p-4 flex flex-col gap-1` containing `"No active goals."` (`text-sm text-muted-foreground`) and a TanStack Router `<Link to="/goals" className="text-sm text-faction-accent">Set a hobby goal →</Link>`. POPULATED: a `flex flex-col gap-3`; per goal compute `count = progressMap?.get(goal.id) ?? 0`, `period = computeGoalPeriod(...)`, `pct = Math.min(100, Math.round((count / Math.max(1, goal.target_count)) * 100))`, `status = deriveGoalStatus(count, goal.target_count, period.isExpired)`, `fillColor = status === "completed" ? "bg-battle-gold" : "bg-faction-accent"`. Each row: name (`text-sm font-medium truncate flex-1`) + `"{count} / {goal.target_count}"` (`text-xs text-muted-foreground tabular-nums shrink-0`) on one baseline-aligned row; period.label (`text-xs text-muted-foreground`); the GoalCard progress-bar idiom (track `h-1.5 w-full rounded-full bg-border/40`, fill `h-1.5 rounded-full transition-all duration-500 ${fillColor}` width `${pct}%`). Do NOT render edit/delete buttons or a status badge (compact dashboard variant).
    (b) `DashboardPage`: add `import { GoalProgressCard } from "./GoalProgressCard"` and insert, AFTER the "Hobby Health" section (and after "By Faction", per UI-SPEC) inside the left-column `flex flex-col gap-6` div, a new `<section className="flex flex-col gap-4"><p className="text-sm font-semibold uppercase tracking-widest text-muted-foreground">Hobby Goals</p><GoalProgressCard /></section>`. Do not touch the right column.
  </action>
  <verify>
    <automated>pnpm test -- tests/dashboard/GoalProgressCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useGoalProgress' src/features/dashboard/GoalProgressCard.tsx` >= 1 and `grep -c 'useGoals' ...` >= 1 (reuses existing hooks; no new query module imported).
    - GoalProgressCard contains exact strings "No active goals." and "Set a hobby goal" and a `to="/goals"` Link.
    - Progress fill uses `bg-faction-accent` (active) and `bg-battle-gold` (completed).
    - `grep -c 'GoalProgressCard' src/features/dashboard/DashboardPage.tsx` >= 1 and a "Hobby Goals" section header is present.
    - GoalProgressCard.test.tsx GREEN; `pnpm build` clean.
  </acceptance_criteria>
  <done>The dashboard renders a "Hobby Goals" section with per-goal progress bars (reusing existing hooks) and an empty state linking to /goals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard render -> existing hooks | GoalProgressCard only reads from already-existing, verified rules.db-free goal hooks; no new data path, no writes, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-08 | Spoofing/Tampering (stale progress) | dashboard goal widget | mitigate | Reuses useGoalProgress (GOAL_PROGRESS_KEY); a regression test locks that session mutations invalidate it so the widget cannot silently go stale (RESEARCH OQ#1). |
| T-138-09 | Information disclosure | goal widget | accept | Displays the user's own local goal data already shown on /goals; no new exposure. |
| T-138-SC | Tampering | npm installs | accept | No package installs this phase; audit table N/A. |
</threat_model>

<verification>
- `pnpm test -- tests/dashboard/` green (widget render + empty state + invalidation lock).
- `pnpm build` (tsc) clean.
- Manual (per VALIDATION.md): open dashboard -> active goals show progress bars; with zero goals, empty state links to GoalsPage.
</verification>

<success_criteria>
- Goal progress is visible on the dashboard with a per-goal progress visualization.
- The widget reuses the existing (verified) derivation + hooks — no rewrite.
- Logging a painting session keeps the widget fresh (invalidation regression-locked).
</success_criteria>

<output>
Create `.planning/phases/138-player-journey-depth/138-04-SUMMARY.md` when done.
</output>

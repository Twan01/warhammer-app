---
phase: 138-player-journey-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/queries/unitDatabase.ts
  - src/hooks/useUnitDatabase.ts
  - src/features/unit-database/databaseBrowserFilters.ts
  - src/app/router.tsx
  - tests/unit-database/unitDatabase.queries.test.ts
  - tests/unit-database/compareFilters.test.ts
autonomous: true
requirements: [PLAY-01]
must_haves:
  truths:
    - "getUdbUnitsByIds(['a','b']) returns a UdbUnitDetail for each id in one batched query (no per-unit hook loop)"
    - "useUdbUnitsByIds is disabled when the id list is empty and uses a stable sorted cache key"
    - "databaseBrowserFilters can hold up to 3 compare ids; the 4th add is a no-op (hard cap)"
    - "The /unit-database/compare route is registered and resolves to UnitComparePage"
  artifacts:
    - path: "src/db/queries/unitDatabase.ts"
      provides: "getUdbUnitsByIds batch query (WHERE id IN, positional params)"
      contains: "getUdbUnitsByIds"
    - path: "src/hooks/useUnitDatabase.ts"
      provides: "useUdbUnitsByIds hook (staleTime Infinity, sorted-array key)"
      contains: "useUdbUnitsByIds"
    - path: "src/features/unit-database/databaseBrowserFilters.ts"
      provides: "compareIds Set + addToCompare/removeFromCompare/clearCompare actions"
      contains: "compareIds"
    - path: "src/app/router.tsx"
      provides: "/unit-database/compare route registration"
      contains: "/unit-database/compare"
  key_links:
    - from: "src/hooks/useUnitDatabase.ts"
      to: "src/db/queries/unitDatabase.ts"
      via: "useUdbUnitsByIds -> getUdbUnitsByIds"
      pattern: "getUdbUnitsByIds"
    - from: "src/app/router.tsx"
      to: "src/features/unit-database/UnitComparePage.tsx"
      via: "lazy import of UnitComparePage"
      pattern: "UnitComparePage"
---

<objective>
Build the PLAY-01 data and routing foundation: a batched multi-id canonical-unit
query, its React Query hook, the compare-selection Zustand state (cap 3), and the
`/unit-database/compare` route. This is the contract layer that the compare UI
(138-02) consumes.

Purpose: Avoid the N+1 / hooks-in-loop anti-pattern by fetching all selected units
in a single `WHERE id IN (...)` query, and give the UI plan a ready route + selection
store to build against.
Output: `getUdbUnitsByIds`, `useUdbUnitsByIds`, `compareIds` store actions, the
compare route registration, and Wave 0 tests covering the batch query + cap-3 store.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-player-journey-depth/138-CONTEXT.md
@.planning/phases/138-player-journey-depth/138-RESEARCH.md
@.planning/phases/138-player-journey-depth/138-PATTERNS.md
@.planning/phases/138-player-journey-depth/138-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase / PATTERNS.md. Use directly — no exploration needed. -->

Existing single-unit query to generalize (src/db/queries/unitDatabase.ts:198):
  export async function getUdbUnitDetail(unitId: string, locale?: "en" | "fr"): Promise<UdbUnitDetail | null>
  - SELECTs the base udb_units row by `WHERE id = $1`, then Promise.all of 6 sub-queries
    (udb_unit_models, udb_unit_weapons, abilities, keywords, points, composition) each `WHERE unit_id = $1`.
  - Returns { ...unit, models, weapons, abilities, keywords, points, composition }.

Existing single-unit hook to model (src/hooks/useUnitDatabase.ts:73):
  export const UDB_UNIT_DETAIL_KEY = (unitId: string, locale: Locale) => ["udb-unit-detail", unitId, locale] as const;
  export function useUdbUnitDetail(unitId: string | null) {
    const locale = useLocale();
    return useQuery({ queryKey: ..., queryFn: ..., enabled: !!unitId, staleTime: Infinity, gcTime: Infinity });
  }
  - Imports: useLocale + Locale from "@/stores/localeStore".

Existing Zustand store to extend (src/features/unit-database/databaseBrowserFilters.ts):
  useDatabaseBrowserFilters = create<DatabaseBrowserFiltersState>((set) => ({ ...selectedFactionId, searchText, ...setters, clearFilters }))

Existing route precedents (src/app/router.tsx):
  - unitDatabaseRoute (lines ~200-208): createRoute({ getParentRoute: () => layoutRoute, path: "/unit-database", validateSearch: z.object({ udbUnitId: z.string().optional() }), component: UnitDatabasePageShell })
  - paintingModeRoute (~214-219): flat-sibling route under bareLayoutRoute — the model for a flat sibling compare route.
  - Lazy import adapter pattern: const X = lazy(() => import("...").then(m => ({ default: m.X })))
  - routeTree: layoutRoute.addChildren([...]) — add the new route to this array.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 tests — batch query + compare-store cap-3</name>
  <files>tests/unit-database/unitDatabase.queries.test.ts, tests/unit-database/compareFilters.test.ts</files>
  <read_first>
    - tests/unit-database/unitDatabase.queries.test.ts (existing query test — mirror its DB-mock setup for getUdbUnitsByIds)
    - tests/unit-database/UdbUnitList.test.tsx (existing unit-database test for the project's mocking idiom)
    - src/db/queries/unitDatabase.ts (getUdbUnitDetail at :198 — the shape under test)
    - src/features/unit-database/databaseBrowserFilters.ts (the store being extended)
    - .planning/phases/138-player-journey-depth/138-VALIDATION.md (Wave 0 requirements)
  </read_first>
  <behavior>
    - getUdbUnitsByIds(["a","b"]) returns an array with one UdbUnitDetail per id (length 2), each carrying models/weapons/abilities/keywords/points.
    - getUdbUnitsByIds([]) returns [] without hitting the DB.
    - getUdbUnitsByIds tolerates a missing id (returns only the rows that exist; no throw).
    - databaseBrowserFilters: addToCompare adds an id; size never exceeds 3; the 4th distinct add is a no-op (size stays 3). removeFromCompare deletes; clearCompare empties the Set.
  </behavior>
  <action>
    Extend `tests/unit-database/unitDatabase.queries.test.ts` with a `getUdbUnitsByIds` describe block covering the four behaviors above, mocking `getDb().select` the same way the existing tests in that file mock it (mirror the `getUdbUnitDetail` test setup). Create `tests/unit-database/compareFilters.test.ts` testing the Zustand store actions `addToCompare`/`removeFromCompare`/`clearCompare` directly via `useDatabaseBrowserFilters.getState()`/`setState()` — assert the hard cap at 3 (add 4 distinct ids -> `compareIds.size === 3`). Reset store state between tests. These tests are RED until Task 2/Task 3 land; this is intentional (TDD RED step) — commit them as `test(138-01): add failing tests for batch query + compare store`.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit-database/compareFilters.test.ts tests/unit-database/unitDatabase.queries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit-database/compareFilters.test.ts` exists and asserts `compareIds.size === 3` after four distinct `addToCompare` calls.
    - `tests/unit-database/unitDatabase.queries.test.ts` contains a `getUdbUnitsByIds` describe block with an empty-array short-circuit assertion.
    - Tests are RED before Task 2/3 (reference missing exports), GREEN after.
  </acceptance_criteria>
  <done>Both test files exist with the behaviors above; they fail referencing not-yet-created exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getUdbUnitsByIds batch query (PLAY-01, D-02)</name>
  <files>src/db/queries/unitDatabase.ts</files>
  <read_first>
    - src/db/queries/unitDatabase.ts (getUdbUnitDetail :198-265 — the exact shape to generalize; getUdbOwnershipByFaction :292 for the IN/params idiom)
    - .planning/phases/138-player-journey-depth/138-PATTERNS.md (section "add getUdbUnitsByIds" — placeholder + Promise.all generalization, lines 33-79)
    - tests/unit-database/unitDatabase.queries.test.ts (the test from Task 1)
  </read_first>
  <action>
    Add `export async function getUdbUnitsByIds(ids: string[], locale?: "en" | "fr"): Promise<UdbUnitDetail[]>` to `src/db/queries/unitDatabase.ts`, generalizing `getUdbUnitDetail`. Guard `if (ids.length === 0) return []` at the top. Build positional placeholders `const placeholders = ids.map((_, i) => \`$${i + 1}\`).join(", ")` and run the base query `SELECT ... FROM udb_units WHERE id IN (${placeholders})` binding the `ids` array (NEVER string-interpolate ids — threat T-138-01). Reuse the same `fr`/`COALESCE(name_fr, name)` column selection as `getUdbUnitDetail`. For each returned unit row, run the identical 6 sub-queries (`udb_unit_models`, `udb_unit_weapons`, abilities, keywords, points, composition) via `WHERE unit_id = $1` with `[unit.id]`, wrapped in `Promise.all(unitRows.map(async (unit) => ({ ...unit, models, weapons, ... })))`. Return type is `UdbUnitDetail[]` — no new type needed (reuse the existing `UdbUnitDetail` interface that `getUdbUnitDetail` returns).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit-database/unitDatabase.queries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/db/queries/unitDatabase.ts | grep -c 'getUdbUnitsByIds'` >= 1.
    - Source contains `WHERE id IN (` and binds the `ids` array (no template-string interpolation of ids into the SQL).
    - The `getUdbUnitsByIds` describe block in `unitDatabase.queries.test.ts` is GREEN.
    - `pnpm build` (tsc) clean for this file (no type errors on the `UdbUnitDetail[]` return).
  </acceptance_criteria>
  <done>getUdbUnitsByIds returns one UdbUnitDetail per existing id from a single batched IN query with positional params; query tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useUdbUnitsByIds hook + compareIds store + compare route (PLAY-01, D-02/D-03/D-01)</name>
  <files>src/hooks/useUnitDatabase.ts, src/features/unit-database/databaseBrowserFilters.ts, src/app/router.tsx</files>
  <read_first>
    - src/hooks/useUnitDatabase.ts (useUdbUnitDetail :73-86 — key factory + enabled + staleTime; imports of useLocale/Locale)
    - src/features/unit-database/databaseBrowserFilters.ts (full store — interface + create body + clearFilters)
    - src/app/router.tsx (unitDatabaseRoute :200-208, paintingModeRoute :214-219, lazy-import adapter, routeTree addChildren)
    - .planning/phases/138-player-journey-depth/138-PATTERNS.md (sections "add useUdbUnitsByIds" lines 122-163, "add compareIds Set" lines 201-259, "add unitDatabaseCompareRoute" lines 802-852)
    - tests/unit-database/compareFilters.test.ts (the store test from Task 1)
  </read_first>
  <action>
    (a) HOOK: Add `export const UDB_UNITS_BY_IDS_KEY = (ids: string[], locale: Locale) => ["udb-units-by-ids", [...ids].sort(), locale] as const` and `export function useUdbUnitsByIds(ids: string[])` to `src/hooks/useUnitDatabase.ts`. Sort the ids in the key for stable cache identity (RESEARCH finding #5 — a Set is not JSON-serializable; pass a SORTED array). Set `enabled: ids.length > 0`, `staleTime: Infinity`, `gcTime: Infinity`, `queryFn: () => getUdbUnitsByIds(ids, locale)` with `const locale = useLocale()`. Add `getUdbUnitsByIds` to the existing import from `@/db/queries/unitDatabase`.
    (b) STORE: Add to `databaseBrowserFilters.ts` the interface fields `compareIds: Set<string>; addToCompare: (id: string) => void; removeFromCompare: (id: string) => void; clearCompare: () => void;`. In the create() body add `compareIds: new Set<string>()` and the three actions per PATTERNS lines 245-258 — `addToCompare` returns early (`return s`) when `s.compareIds.size >= 3` (hard cap, D-03), and always creates a NEW Set before mutating so React/Zustand sees a fresh reference. Leave the existing `clearFilters` as-is (do NOT clear compareIds in clearFilters — compare selection is independent of search/faction filters; PITFALL #5 clearing happens via the compare page's Clear button).
    (c) ROUTE: In `src/app/router.tsx` add a lazy import `const UnitComparePage = lazy(() => import("../features/unit-database/UnitComparePage").then(m => ({ default: m.UnitComparePage })))` (path relative to router.tsx — confirm the existing lazy imports' relative style and match it). Register a FLAT SIBLING route (NOT a nested child — RESEARCH finding #1): `const unitDatabaseCompareRoute = createRoute({ getParentRoute: () => layoutRoute, path: "/unit-database/compare", component: UnitComparePage })` with NO validateSearch (reads from Zustand). Add `unitDatabaseCompareRoute` to the `layoutRoute.addChildren([...])` array. NOTE: `UnitComparePage` does not exist yet (138-02 creates it) — the lazy import is a deferred dynamic import, so tsc will not fail on the missing module at this point; if tsc DOES complain, leave a minimal stub `export function UnitComparePage() { return null }` in the target file to be replaced by 138-02 (and note it in the SUMMARY).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit-database/compareFilters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/hooks/useUnitDatabase.ts | grep -c 'useUdbUnitsByIds'` >= 1 and the key factory sorts ids (`[...ids].sort()` present).
    - `grep -c 'compareIds' src/features/unit-database/databaseBrowserFilters.ts` >= 4 (field + 3 actions reference it).
    - `addToCompare` contains a `>= 3` guard.
    - `grep -c '/unit-database/compare' src/app/router.tsx` >= 1 and `unitDatabaseCompareRoute` appears in the routeTree addChildren array.
    - compareFilters.test.ts GREEN.
  </acceptance_criteria>
  <done>useUdbUnitsByIds calls the batch query with a stable sorted key; compareIds store enforces cap-3; /unit-database/compare route registered. Compare-store tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI selection -> SQLite query | compareIds (Set<string> of canonical UDB ids) flows from in-app row selection into `getUdbUnitsByIds`. Ids originate from rendered UDB rows, not free-form user text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-01 | Tampering (SQL injection) | `getUdbUnitsByIds` IN-clause | mitigate | Build `$1,$2,...` positional placeholders and bind the `ids` array as parameters; NEVER string-interpolate ids into the SQL string (matches the project's `$1,$2` param rule). |
| T-138-02 | Information disclosure | batch query result | accept | Read-only canonical (non-PII) data already exposed via the single-unit query; no new exposure. |
| T-138-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH: no new packages); audit table N/A. |
</threat_model>

<verification>
- `pnpm test -- tests/unit-database/` green (compare store + batch query).
- `pnpm build` (tsc) clean.
- `getUdbUnitsByIds` uses positional params (no id interpolation) — manual grep confirms T-138-01 mitigation.
</verification>

<success_criteria>
- A single batched query returns all selected units' detail.
- The compare-selection store enforces a hard cap of 3.
- `/unit-database/compare` resolves (to a stub or the real page) so 138-02 can build the UI.
</success_criteria>

<output>
Create `.planning/phases/138-player-journey-depth/138-01-SUMMARY.md` when done.
</output>

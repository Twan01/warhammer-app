---
phase: 35-v2.2-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/battle-log/BattleLogSheet.tsx
  - src/hooks/useJournalSessions.ts
  - src/hooks/useUnits.ts
  - src/features/paints/PaintSheet.tsx
  - src/features/paints/paintSchema.ts
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "BattleLogSheet defaults today's date using local timezone (not UTC)"
    - "Deleting a painting session invalidates goal-progress cache"
    - "Updating a unit invalidates army-lists cache"
    - "PaintSheet purchase_date form field saves user-entered dates to the database"
  artifacts:
    - path: "src/features/battle-log/BattleLogSheet.tsx"
      provides: "todayISO import from @/lib/dates replacing local UTC helper"
      contains: "import { todayISO } from"
    - path: "src/hooks/useJournalSessions.ts"
      provides: "goal-progress cache invalidation on delete"
      contains: "goal-progress"
    - path: "src/hooks/useUnits.ts"
      provides: "army-lists cache invalidation on unit update"
      contains: "army-lists"
    - path: "src/features/paints/PaintSheet.tsx"
      provides: "purchase_date wired from form to mutation payload"
      contains: "purchase_date"
    - path: "src/features/paints/paintSchema.ts"
      provides: "purchase_date field in Zod schema"
      contains: "purchase_date"
  key_links:
    - from: "src/features/battle-log/BattleLogSheet.tsx"
      to: "src/lib/dates.ts"
      via: "import { todayISO }"
      pattern: "import.*todayISO.*from.*@/lib/dates"
    - from: "src/hooks/useJournalSessions.ts"
      to: "src/hooks/useGoals.ts"
      via: "goal-progress query key invalidation"
      pattern: 'queryKey.*goal-progress'
    - from: "src/hooks/useUnits.ts"
      to: "src/hooks/useArmyLists.ts"
      via: "army-lists query key invalidation"
      pattern: 'queryKey.*army-lists'
    - from: "src/features/paints/PaintSheet.tsx"
      to: "src/features/paints/paintSchema.ts"
      via: "purchase_date field in schema and form"
      pattern: "purchase_date.*values"
---

<objective>
Close 4 low-severity tech debt items identified in the v2.2 milestone audit. All are 1-5 line surgical fixes: a timezone-safe import swap, two missing cache invalidations, and one unwired form field.

Purpose: Eliminate known tech debt before v2.4 work begins so these issues don't compound.
Output: 4 files patched, existing 644 tests still green.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v2.2-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BattleLogSheet timezone fix + cache invalidation patches</name>
  <files>src/features/battle-log/BattleLogSheet.tsx, src/hooks/useJournalSessions.ts, src/hooks/useUnits.ts</files>
  <read_first>
    - src/features/battle-log/BattleLogSheet.tsx (see local todayIso() on line 47-49 and its two call sites on lines 52 and 87)
    - src/lib/dates.ts (source of truth — todayISO() uses local timezone getFullYear/getMonth/getDate)
    - src/hooks/useJournalSessions.ts (see useDeletePaintingSession onSettled block, lines 76-79 — missing goal-progress)
    - src/hooks/useUnits.ts (see useUpdateUnit onSuccess block, lines 44-56 — missing army-lists)
    - src/hooks/useGoals.ts (confirms GOAL_PROGRESS_KEY = ["goal-progress"])
  </read_first>
  <action>
Three independent fixes in three files:

**Fix 1 — BattleLogSheet.tsx (timezone safety):**
1. Add `import { todayISO } from "@/lib/dates";` to the imports (after the existing import block, around line 5-6 area).
2. Delete the local `todayIso()` function entirely (lines 47-49: `function todayIso(): string { return new Date().toISOString().slice(0, 10); }`).
3. Replace both call sites:
   - Line 52 `battle_date: todayIso(),` becomes `battle_date: todayISO(),` (capital I, capital S, capital O — matches the export name in dates.ts).
   - Line 87 `return { ...DEFAULT_VALUES, battle_date: todayIso() };` becomes `return { ...DEFAULT_VALUES, battle_date: todayISO() };`.

**Fix 2 — useJournalSessions.ts (goal-progress cache invalidation on delete):**
In `useDeletePaintingSession`, inside the `onSettled` callback (after line 78 `qc.invalidateQueries({ queryKey: ["hobby-analytics"] });`), add:
```typescript
      qc.invalidateQueries({ queryKey: ["goal-progress"] });
```
This matches the pattern already used in `useCreatePaintingSession` on line 43.

**Fix 3 — useUnits.ts (army-lists cache invalidation on unit update):**
In `useUpdateUnit`, inside the `onSuccess` callback (after line 54 `qc.invalidateQueries({ queryKey: ["army-list-readiness"] });`), add:
```typescript
      // Phase 35: tier confirm writes to units.points — army list COALESCE chain needs refresh
      qc.invalidateQueries({ queryKey: ["army-lists"] });
```
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "import { todayISO } from" src/features/battle-log/BattleLogSheet.tsx returns 1
    - grep -c "function todayIso" src/features/battle-log/BattleLogSheet.tsx returns 0 (local function deleted)
    - grep -c "todayISO()" src/features/battle-log/BattleLogSheet.tsx returns 2 (DEFAULT_VALUES + buildDefaultValues)
    - grep "goal-progress" src/hooks/useJournalSessions.ts returns matches in BOTH useCreatePaintingSession AND useDeletePaintingSession
    - grep -c '"army-lists"' src/hooks/useUnits.ts returns 1
    - pnpm build exits 0
  </acceptance_criteria>
  <done>BattleLogSheet imports todayISO from @/lib/dates (no local UTC helper remains), useDeletePaintingSession invalidates goal-progress, useUpdateUnit invalidates army-lists. TypeScript build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PaintSheet purchase_date form field to mutation</name>
  <files>src/features/paints/paintSchema.ts, src/features/paints/PaintSheet.tsx</files>
  <read_first>
    - src/features/paints/paintSchema.ts (current schema has no purchase_date field)
    - src/features/paints/PaintSheet.tsx (line 105: purchase_date hardcoded to null in payload; no form field exists)
    - src/types/paint.ts (Paint interface has purchase_date: string | null on line 25; CreatePaintInput includes it via Omit)
  </read_first>
  <action>
**Step 1 — paintSchema.ts: Add purchase_date field to the Zod schema.**

After the `purchase_price_pence` field (line 29), add:
```typescript
  purchase_date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, "Date must be YYYY-MM-DD").optional().nullable().or(z.literal("")),
```

This makes it an optional nullable string matching YYYY-MM-DD format (same pattern as hex_color with its .or(z.literal("")) to allow empty string from cleared date input).

**Step 2 — PaintSheet.tsx: Add purchase_date to DEFAULT_VALUES.**

In the `DEFAULT_VALUES` object (around line 41-53), add after `purchase_price_pence: null,`:
```typescript
  purchase_date: null,
```

**Step 3 — PaintSheet.tsx: Add purchase_date to buildDefaultValues.**

In the `buildDefaultValues` function's edit branch (around line 55-73), add after `purchase_price_pence: paint.purchase_price_pence ?? null,`:
```typescript
      purchase_date: paint.purchase_date ?? null,
```

**Step 4 — PaintSheet.tsx: Wire purchase_date in the onSubmit payload.**

Replace line 105:
```typescript
        purchase_date: null, // purchase_date added in migration 008; not yet surfaced in this form
```
with:
```typescript
        purchase_date: values.purchase_date || null,
```

**Step 5 — PaintSheet.tsx: Add purchase_date form field in the JSX.**

Insert a new FormField AFTER the `purchase_price_pence` field block (after line 366, before SheetFooter) and before the SheetFooter:

```tsx
            <FormField
              name="purchase_date"
              control={form.control}
              render={({ field }) => (
                <FormItem>
                  <FormLabel>Purchase Date</FormLabel>
                  <FormControl>
                    <Input
                      type="date"
                      {...field}
                      value={field.value ?? ""}
                      onChange={(e) => field.onChange(e.target.value || null)}
                    />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
```

This uses `type="date"` (same pattern as BattleLogSheet's battle_date field). Empty string from a cleared input coerces to null via `|| null`.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "purchase_date" src/features/paints/paintSchema.ts returns 1 (the new field)
    - grep "purchase_date" src/features/paints/PaintSheet.tsx does NOT contain "null, // purchase_date added in migration" (hardcoded null comment removed)
    - grep -c 'purchase_date' src/features/paints/PaintSheet.tsx returns at least 4 (DEFAULT_VALUES + buildDefaultValues + payload + FormField name)
    - grep -c 'type="date"' src/features/paints/PaintSheet.tsx returns 1 (the new date input)
    - pnpm build exits 0
  </acceptance_criteria>
  <done>PaintSheet has a visible purchase_date date input. User-entered dates flow through Zod validation to the mutation payload. Existing paints with purchase_date show the saved value when editing. New paints default to null (no date). TypeScript build passes.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full test suite to confirm no regressions:

```bash
cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test
```

Expected: 644 passed (or more), 0 failed. The 2 pre-existing skips are acceptable.

Also verify the build:

```bash
pnpm build
```

Expected: exit 0 with no TypeScript errors.
</verification>

<success_criteria>
1. BattleLogSheet uses todayISO() from @/lib/dates — no local UTC helper function exists in the file
2. useDeletePaintingSession invalidates ["goal-progress"] cache in its onSettled callback
3. useUpdateUnit invalidates ["army-lists"] cache in its onSuccess callback
4. PaintSheet has a visible purchase_date date input field wired through Zod schema to the mutation payload
5. pnpm build exits 0
6. pnpm test shows 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/35-v2.2-gap-closure/35-01-SUMMARY.md`
</output>

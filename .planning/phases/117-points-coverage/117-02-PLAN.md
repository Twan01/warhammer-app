---
phase: 117-points-coverage
plan: 02
type: execute
wave: 2
depends_on: [117-01]
files_modified:
  - scripts/build-unit-db.ts
  - scripts/update-unit-database.ts
  - scripts/audit-faction.ts
  - scripts/lib/types.ts
  - scripts/lib/bsdata.ts
  - scripts/lib/parseXml.ts
  - scripts/lib/normalize.ts
  - scripts/data/aliases.json
  - package.json
autonomous: true
requirements: [PTS-02, PTS-03, PTS-04]

must_haves:
  truths:
    - "No reference to @xmldom/xmldom exists in any script or package.json"
    - "No reference to parseXml, loadAliases, readBsdataCatFiles, parseBsdataModelCounts, or matchUnit exists in any script"
    - "Coverage threshold is 90% and the build passes"
    - "BSData-specific type definitions are removed from types.ts"
    - "Coverage report shows simplified format without BSData match method columns"
  artifacts:
    - path: "scripts/build-unit-db.ts"
      provides: "Clean build script with no BSData imports or dead code"
    - path: "scripts/update-unit-database.ts"
      provides: "Clean update script with no BSData imports or dead code"
    - path: "scripts/audit-faction.ts"
      provides: "Clean audit script importing readCsvFile from parseCsv.ts"
    - path: "scripts/lib/types.ts"
      provides: "Types without BSData-specific interfaces"
    - path: "package.json"
      provides: "No @xmldom/xmldom dependency"
  key_links:
    - from: "scripts/build-unit-db.ts"
      to: "scripts/lib/parseCsv.ts"
      via: "import readCsvFile (no more bsdata.ts import)"
      pattern: "from.*parseCsv"
    - from: "scripts/audit-faction.ts"
      to: "scripts/lib/parseCsv.ts"
      via: "import readCsvFile (relocated)"
      pattern: "from.*parseCsv"
---

<objective>
Remove all BSData dependencies from the build pipeline: delete dead files, clean imports, remove @xmldom/xmldom, simplify types and coverage report, raise coverage threshold to 90%.

Purpose: Complete the BSData-to-Wahapedia migration. After this plan, the pipeline has zero BSData references and is simpler, faster, and more reliable.
Output: Clean pipeline with no BSData code. @xmldom/xmldom removed. 90% coverage gate enforced.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-points-coverage/117-CONTEXT.md
@.planning/phases/117-points-coverage/117-RESEARCH.md
@.planning/phases/117-points-coverage/117-PATTERNS.md
@.planning/phases/117-points-coverage/117-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove BSData files, clean libraries, remove dependency</name>
  <files>
    scripts/lib/bsdata.ts (DELETE)
    scripts/lib/parseXml.ts (DELETE)
    scripts/lib/normalize.ts (DELETE)
    scripts/data/aliases.json (DELETE)
    scripts/lib/types.ts
    package.json
  </files>
  <read_first>
    - scripts/lib/bsdata.ts (confirm readCsvFile has been relocated to parseCsv.ts by Plan 01 -- if so, entire file is deletable)
    - scripts/lib/parseXml.ts (confirm entire file is BSData XML parser -- delete entirely per D-06)
    - scripts/lib/normalize.ts (confirm normalizeName only used in bsdata.ts, loadAliases only used for BSData -- delete entirely per D-06)
    - scripts/lib/types.ts (identify BSData types to remove: PointsTier, BsdataUnitPoints, BsdataModelCount at lines ~10-28; FactionCoverage optional fields matched_exact, matched_normalized, matched_alias, unmatched_names at lines ~116-130)
    - package.json (find @xmldom/xmldom in dependencies -- remove per D-05)
  </read_first>
  <action>
    Per D-04, D-05, D-06:

    1. DELETE files entirely: `scripts/lib/bsdata.ts`, `scripts/lib/parseXml.ts`, `scripts/lib/normalize.ts`, `scripts/data/aliases.json`.

    2. EDIT `scripts/lib/types.ts`:
       - Remove `PointsTier` interface (modelCount: number, points: number)
       - Remove `BsdataUnitPoints` interface (datasheet_name, faction_id, points, tiers)
       - Remove `BsdataModelCount` interface (unit_name, faction_id, min_models, max_models)
       - From `FactionCoverage` interface: remove optional fields `matched_exact`, `matched_normalized`, `matched_alias`, `unmatched_names`
       - Keep all other types (UdbFactionRow, UdbUnitRow, UdbUnitPointsRow, etc.)

    3. EDIT `package.json`: Remove `"@xmldom/xmldom": "^0.9.10"` from dependencies per D-05.

    4. Run `pnpm install` to update lockfile after removing the dependency.
  </action>
  <verify>
    <automated>grep -r "xmldom\|parseXml\|loadAliases\|readBsdataCatFiles\|parseBsdataModelCounts\|matchUnit\|BsdataUnitPoints\|BsdataModelCount\|PointsTier" scripts/lib/ package.json 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/lib/bsdata.ts does not exist
    - scripts/lib/parseXml.ts does not exist
    - scripts/lib/normalize.ts does not exist
    - scripts/data/aliases.json does not exist
    - types.ts has no PointsTier, BsdataUnitPoints, or BsdataModelCount interfaces
    - FactionCoverage has no matched_exact, matched_normalized, matched_alias fields
    - package.json has no @xmldom/xmldom entry
    - grep for "xmldom" across scripts/ and package.json returns no matches
  </acceptance_criteria>
  <done>All BSData library code, types, data files, and the @xmldom/xmldom dependency are removed.</done>
</task>

<task type="auto">
  <name>Task 2: Clean BSData imports and dead code from all three scripts, raise threshold, simplify coverage report</name>
  <files>
    scripts/build-unit-db.ts
    scripts/update-unit-database.ts
    scripts/audit-faction.ts
  </files>
  <read_first>
    - scripts/build-unit-db.ts (identify: BSData imports at lines ~19-25 including DOMParser, parseCatXml, loadAliases, bsdata imports; BSDATA_DIR and ALIASES_PATH constants at lines ~68-71; any remaining commented-out BSData code from Plan 01 Task 2; validateAliases function at lines ~120-145; coverage report columns at lines ~539-657; MIN_COVERAGE_PCT constant at line ~62)
    - scripts/update-unit-database.ts (identify: same BSData imports at lines ~18-25; BSDATA_DIR constant at line ~82; remaining BSData dead code from Plan 01 Task 3; MIN_COVERAGE_PCT if present)
    - scripts/audit-faction.ts (identify: DOMParser import and polyfill at lines ~15-17; readCsvFile import from bsdata.ts at line ~24 -- this must change to import from parseCsv.ts)
  </read_first>
  <action>
    Per D-04, D-07, D-08, D-12, D-13:

    **build-unit-db.ts:**
    1. REMOVE imports: `DOMParser` from "@xmldom/xmldom", `globalThis.DOMParser` polyfill line, `parseCatXml` from "./lib/parseXml.ts", `loadAliases` from "./lib/normalize.ts", BSData functions from "./lib/bsdata.ts" (readBsdataCatFiles, parseBsdataModelCounts, matchUnit, MatchMethod). Keep the `readCsvFile, extractModelCount` import from `./lib/parseCsv.ts` (added by Plan 01).
    2. REMOVE constants: `BSDATA_DIR`, `ALIASES_PATH`.
    3. REMOVE the `validateAliases()` function entirely (dead code after BSData removal).
    4. REMOVE any commented-out / dead BSData Step 8 code left by Plan 01.
    5. RAISE `MIN_COVERAGE_PCT` from 58 to 90 per D-12.
    6. SIMPLIFY coverage report per D-13: change column headers from "Faction / Units / Matched / Exact / Norm / Alias / Coverage" to "Faction / Units / WithPoints / Coverage". Remove the per-faction `factionMatchStats` map and the BSData match method tracking. Each faction row should show: faction name, total units, units with points (base_points not null OR has points tier entries), coverage percentage. Remove the badge/method breakdown that tracked exact/normalized/alias match counts.

    **update-unit-database.ts:**
    1. Same import cleanup as build-unit-db.ts: remove DOMParser, parseCatXml, loadAliases, BSData bsdata.ts imports.
    2. REMOVE `BSDATA_DIR` constant.
    3. REMOVE any commented-out / dead BSData code from Plan 01.
    4. RAISE `MIN_COVERAGE_PCT` to 90 if this script has its own threshold constant.
    5. SIMPLIFY coverage report same as build-unit-db.ts.

    **audit-faction.ts per D-08:**
    1. REMOVE the `DOMParser` import from "@xmldom/xmldom" and the `globalThis.DOMParser` polyfill line.
    2. CHANGE `import { readCsvFile } from "./lib/bsdata.ts"` to `import { readCsvFile } from "./lib/parseCsv.ts"`.
    3. Verify no other BSData references remain.
  </action>
  <verify>
    <automated>grep -r "xmldom\|parseCatXml\|loadAliases\|readBsdataCatFiles\|parseBsdataModelCounts\|matchUnit\|BSDATA_DIR\|ALIASES_PATH\|validateAliases\|bsdata" scripts/build-unit-db.ts scripts/update-unit-database.ts scripts/audit-faction.ts 2>&1; echo "---"; npx tsx scripts/build-unit-db.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Zero grep hits for xmldom, parseCatXml, loadAliases, readBsdataCatFiles, parseBsdataModelCounts, matchUnit, BSDATA_DIR, ALIASES_PATH, validateAliases, or "bsdata" across all three scripts
    - build-unit-db.ts has MIN_COVERAGE_PCT = 90
    - build-unit-db.ts coverage report outputs columns: Faction, Units, WithPoints, Coverage (no Exact/Norm/Alias columns)
    - `npx tsx scripts/build-unit-db.ts` exits 0 with 90%+ coverage
    - `npx tsx scripts/update-unit-database.ts` exits 0
    - `npx tsx scripts/audit-faction.ts SM` exits 0 (or with expected output, no import errors)
    - No TypeScript compilation errors: `pnpm build` passes
  </acceptance_criteria>
  <done>All three scripts are clean of BSData references. Coverage threshold raised to 90%. Coverage report simplified. Build passes with the new Wahapedia-only pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply. Dev-side build script cleanup only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-02 | Tampering | dependency removal | accept | Removing a dependency reduces attack surface; no new packages added |
</threat_model>

<verification>
1. `grep -r "xmldom" scripts/ package.json` returns no results
2. `npx tsx scripts/build-unit-db.ts` exits 0 with coverage >= 90%
3. `npx tsx scripts/update-unit-database.ts` exits 0
4. `pnpm build` passes (no TypeScript errors from removed types/imports)
5. Build output shows "WithPoints" column, no "Exact/Norm/Alias" columns
6. Sub-faction count > 0 in build output
</verification>

<success_criteria>
- Zero references to @xmldom/xmldom, BSData functions, or BSData types in the codebase
- scripts/lib/bsdata.ts, parseXml.ts, normalize.ts, aliases.json deleted
- MIN_COVERAGE_PCT = 90 and build passes
- Coverage report uses simplified Wahapedia-only format
- pnpm build succeeds (TypeScript clean)
- audit-faction.ts imports readCsvFile from parseCsv.ts
</success_criteria>

<output>
Create `.planning/phases/117-points-coverage/117-02-SUMMARY.md` when done
</output>

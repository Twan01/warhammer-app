---
phase: 78-dashboard-command-center-game-day-after-action
plan: 03
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - src/features/battle-log/BattleLogSheet.tsx
  - src/features/game-day/GameDayHeader.tsx
  - src/features/game-day/GameDayPage.tsx
  - src/features/game-day/ChecklistTab.tsx
autonomous: true
requirements: [GD-01, GD-02, GD-03, GD-04]

must_haves:
  truths:
    - "End Game button in GameDayHeader opens BattleLogSheet pre-filled with army_list_id, battle_date, and opponent_faction"
    - "BattleLogSheet has a collapsible After-Action section containing forgotten rules, MVP, underperformer, lessons learned, and changes next time"
    - "Forgotten rules from recent battle logs appear as amber reminder items above the checklist in ChecklistTab"
    - "changes_next_time field is inside the collapsible section serving as list-level notes"
    - "MVP and underperformer selects are moved into the collapsible section, not duplicated"
  artifacts:
    - path: "src/features/battle-log/BattleLogSheet.tsx"
      provides: "Extended form with after-action collapsible + prefill prop"
      contains: "After-Action"
    - path: "src/features/game-day/GameDayHeader.tsx"
      provides: "End Game button"
      contains: "End Game"
    - path: "src/features/game-day/ChecklistTab.tsx"
      provides: "Forgotten rules reminders rendering"
      contains: "Reminders from last games"
  key_links:
    - from: "src/features/game-day/GameDayHeader.tsx"
      to: "src/features/battle-log/BattleLogSheet.tsx"
      via: "onEndGame callback → BattleLogSheet prefill prop"
      pattern: "onEndGame"
    - from: "src/features/game-day/GameDayPage.tsx"
      to: "src/features/battle-log/BattleLogSheet.tsx"
      via: "BattleLogSheet sibling portal with endGameOpen state"
      pattern: "endGameOpen"
    - from: "src/features/game-day/ChecklistTab.tsx"
      to: "src/hooks/useBattleLogs.ts"
      via: "useForgottenRules hook import"
      pattern: "useForgottenRules"
---

<objective>
Implement the Game Day after-action flow: End Game button, BattleLogSheet extension
with collapsible after-action section and pre-fill support, and forgotten rules
reminders in the pre-game checklist.

Purpose: GD-01 through GD-04 — close the game-to-log loop and surface learnings for future games.
Output: Extended BattleLogSheet, End Game button in GameDayHeader, forgotten rules in ChecklistTab.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/78-dashboard-command-center-game-day-after-action/78-CONTEXT.md
@.planning/phases/78-dashboard-command-center-game-day-after-action/78-UI-SPEC.md
@.planning/phases/78-dashboard-command-center-game-day-after-action/78-PATTERNS.md
@.planning/phases/78-dashboard-command-center-game-day-after-action/78-01-SUMMARY.md

<interfaces>
<!-- BattleLogSheet current structure -->
From src/features/battle-log/BattleLogSheet.tsx:
- Props: { open, log, onClose } — needs prefill?: Partial<BattleLogFormValues> added
- DEFAULT_VALUES at line 48: BattleLogFormValues object
- buildDefaultValues pattern: if (log) return edit mode, else return DEFAULT_VALUES
- TEXTAREA_CLASS constant at line 45
- NO_LIST = "__none__" and NO_UNIT = "__none__" sentinel values
- Form Groups 1-4 with Separator between groups
- MVP (mvp_unit_id) and underperformer (underperforming_unit_id) selects are in Group 3
- lessons_learned and changes_next_time textareas are in Group 4

<!-- BattleLogFormValues (extended in Plan 01) -->
From src/features/battle-log/battleLogSchema.ts:
- Now includes forgotten_rules: string | null, mvp_notes: string | null,
  underperformer_notes: string | null

<!-- GameDayHeader current props -->
From src/features/game-day/GameDayHeader.tsx:
interface GameDayHeaderProps {
  listName: string;
  factionName: string | null;
  detachmentName: string | null;
  listId: number;
}

<!-- GameDayPage current structure -->
From src/features/game-day/GameDayPage.tsx:
- Renders GameDayHeader, GameDayReadinessPanel, Tabs (stratagems, units, checklist)
- No existing sheet portals — needs BattleLogSheet added as sibling

<!-- ChecklistTab current props -->
From src/features/game-day/ChecklistTab.tsx:
interface ChecklistTabProps { listId: number; }

<!-- useForgottenRules hook (created in Plan 01) -->
From src/hooks/useBattleLogs.ts:
export function useForgottenRules(armyListId: number | undefined): UseQueryResult<string[]>;
export const FORGOTTEN_RULES_KEY: (id: number) => readonly string[];

<!-- Collapsible from shadcn (already installed) -->
From @/components/ui/collapsible:
Collapsible, CollapsibleTrigger, CollapsibleContent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BattleLogSheet with prefill prop and collapsible After-Action section</name>
  <files>
    src/features/battle-log/BattleLogSheet.tsx
  </files>
  <read_first>
    src/features/battle-log/BattleLogSheet.tsx,
    src/features/battle-log/battleLogSchema.ts,
    src/types/battleLog.ts,
    src/hooks/useBattleLogs.ts,
    .planning/phases/78-dashboard-command-center-game-day-after-action/78-UI-SPEC.md
  </read_first>
  <action>
    Per D-10, D-11, D-12, D-13, D-14, D-17, GD-01, GD-02, GD-04, and UI-SPEC:

    1. Add optional prefill prop to BattleLogSheet: prefill?: Partial<BattleLogFormValues>.
       Derive isPrefilled = prefill !== undefined && log === null.
       Update SheetTitle: isEdit ? "Edit Game" : isPrefilled ? "End Game" : "Log Game".
       Update SheetDescription when isPrefilled: "Record your game result and after-action notes."

    2. Extend buildDefaultValues to accept a second param prefill?: Partial<BattleLogFormValues>.
       When not editing (log === null), merge: { ...DEFAULT_VALUES, battle_date: todayISO(), ...prefill }.
       Update the useEffect reset call to pass prefill as well.

    3. Add forgotten_rules, mvp_notes, underperformer_notes to DEFAULT_VALUES (all null).

    4. MOVE (not duplicate) the mvp_unit_id Select and underperforming_unit_id Select fields
       OUT of their current Group 3 position. Also MOVE lessons_learned and changes_next_time
       textareas OUT of their current Group 4 position.

    5. After the last existing form group (notes field), add a Separator, then a Collapsible
       component (defaultOpen={false}). Import Collapsible, CollapsibleTrigger, CollapsibleContent
       from @/components/ui/collapsible and ChevronDown from lucide-react.

       CollapsibleTrigger: flex items-center gap-2 w-full, label "After-Action"
       (text-xs font-semibold text-muted-foreground uppercase tracking-wide),
       ChevronDown (size={12}) with ml-auto.

       CollapsibleContent: flex flex-col gap-4 pt-2, containing in order:
       (a) forgotten_rules FormField — textarea with TEXTAREA_CLASS, rows={4},
           placeholder "Rules you forgot this game -- one per line".
           On read (when log exists), convert the JSON string to newline-separated text:
           log.forgotten_rules ? (JSON.parse(log.forgotten_rules) as string[]).join("\n") : null.
       (b) mvp_unit_id Select (moved from Group 3)
       (c) mvp_notes FormField — textarea with TEXTAREA_CLASS, placeholder "Notes on MVP unit"
       (d) underperforming_unit_id Select (moved from Group 3)
       (e) underperformer_notes FormField — textarea with TEXTAREA_CLASS, placeholder "Notes on underperformer"
       (f) lessons_learned textarea (moved from Group 4)
       (g) changes_next_time textarea (moved from Group 4)

    6. In the onSubmit handler, when building the create/update input, serialize forgotten_rules:
       if form value is a string, split by newline, filter empty, JSON.stringify the array.
       If null/empty, pass null. Also pass mvp_notes, underperformer_notes values.

    7. In buildDefaultValues edit mode (when log exists), map the new fields:
       forgotten_rules: log.forgotten_rules ? (JSON.parse(log.forgotten_rules) as string[]).join("\n") : null,
       mvp_notes: log.mvp_notes ?? null,
       underperformer_notes: log.underperformer_notes ?? null.
       Wrap JSON.parse in try/catch, fallback to null on error.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - BattleLogSheet accepts optional prefill prop of type Partial<BattleLogFormValues>
    - SheetTitle shows "End Game" when isPrefilled is true
    - SheetDescription shows "Record your game result and after-action notes." when isPrefilled
    - buildDefaultValues merges prefill over DEFAULT_VALUES when log is null
    - DEFAULT_VALUES includes forgotten_rules: null, mvp_notes: null, underperformer_notes: null
    - Collapsible component wraps the after-action section with trigger label "After-Action"
    - forgotten_rules textarea has placeholder "Rules you forgot this game -- one per line"
    - mvp_unit_id and underperforming_unit_id selects appear INSIDE the collapsible, NOT in their old positions
    - lessons_learned and changes_next_time textareas appear INSIDE the collapsible, NOT in their old positions
    - onSubmit serializes forgotten_rules string to JSON array string via split/filter/stringify
    - Edit mode deserializes forgotten_rules JSON array to newline-separated string
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>BattleLogSheet supports pre-fill for End Game flow and has collapsible After-Action section with all after-action fields</done>
</task>

<task type="auto">
  <name>Task 2: Add End Game button to GameDayHeader and wire BattleLogSheet in GameDayPage</name>
  <files>
    src/features/game-day/GameDayHeader.tsx,
    src/features/game-day/GameDayPage.tsx
  </files>
  <read_first>
    src/features/game-day/GameDayHeader.tsx,
    src/features/game-day/GameDayPage.tsx,
    src/features/game-day/gameDayStore.ts,
    src/features/battle-log/BattleLogSheet.tsx,
    src/lib/dates.ts,
    .planning/phases/78-dashboard-command-center-game-day-after-action/78-UI-SPEC.md
  </read_first>
  <action>
    Per D-10, D-11, GD-01, and UI-SPEC:

    1. In GameDayHeader.tsx — add onEndGame callback to GameDayHeaderProps:
       onEndGame: () => void.
       Import Flag from lucide-react.
       Add "End Game" button in the top row, after the title/faction area (inside the
       flex items-center gap-3 div, as the last child before it closes).
       Button: variant="outline", size="sm",
       className="border-battle-gold text-battle-gold hover:bg-battle-gold/10 shrink-0",
       onClick={onEndGame}. Content: Flag icon (size={14}, className="mr-1.5", aria-hidden)
       + "End Game" text.

    2. In GameDayPage.tsx — add state: const [endGameOpen, setEndGameOpen] = useState(false).
       Import useState (already imported), BattleLogSheet from @/features/battle-log/BattleLogSheet,
       todayISO from @/lib/dates, and type BattleLogFormValues from @/features/battle-log/battleLogSchema.

       Pass onEndGame={() => setEndGameOpen(true)} to GameDayHeader.

       Add BattleLogSheet as a SIBLING at the end of the return JSX (after the closing div
       of the main content, before the fragment closes — wrap return in fragment if needed):
       BattleLogSheet open={endGameOpen} log={null}
       prefill={{ army_list_id: listId, battle_date: todayISO(), opponent_faction: "" }}
       onClose={() => setEndGameOpen(false)}.

       Note: opponent_faction is set to empty string because gameDayStore does not store
       opponent faction (verified: gameDayStore.ts has no opponent field). The user fills
       it in the sheet. If faction name is available from the page props, optionally pass
       it — but opponent_faction means the OPPONENT's faction, not the player's, so leave
       it as "" for the user to fill.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - GameDayHeaderProps includes onEndGame: () => void
    - GameDayHeader renders a Button with text "End Game" and Flag icon
    - Button has className containing border-battle-gold and text-battle-gold
    - Button onClick calls onEndGame prop
    - GameDayPage has endGameOpen state variable
    - GameDayPage passes onEndGame={() => setEndGameOpen(true)} to GameDayHeader
    - GameDayPage renders BattleLogSheet as sibling with open={endGameOpen}, log={null}, and prefill containing army_list_id and battle_date
    - BattleLogSheet onClose sets endGameOpen to false
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>End Game button opens pre-filled BattleLogSheet from Game Day; sibling portal contract respected</done>
</task>

<task type="auto">
  <name>Task 3: Render forgotten rules reminders in ChecklistTab</name>
  <files>
    src/features/game-day/ChecklistTab.tsx
  </files>
  <read_first>
    src/features/game-day/ChecklistTab.tsx,
    src/hooks/useBattleLogs.ts,
    .planning/phases/78-dashboard-command-center-game-day-after-action/78-UI-SPEC.md
  </read_first>
  <action>
    Per D-15, D-16, GD-03, and UI-SPEC:

    1. Import useForgottenRules from @/hooks/useBattleLogs, useQuery from @tanstack/react-query
       (if not already), and BookOpen from lucide-react.

    2. Inside ChecklistTab, call useForgottenRules(listId). This queries the last 3 battle logs
       for the same army_list_id and returns a deduplicated string[] of forgotten rules.

    3. Render the reminders section ABOVE the existing checklist items div (the div.flex.flex-col.gap-1.p-4).
       Only render when forgottenRules.length > 0.

       Structure per UI-SPEC:
       div.flex.flex-col.gap-1.px-4.pt-4 containing:
       - Group label: p with "Reminders from last games"
         (text-xs font-semibold text-muted-foreground uppercase tracking-wide)
       - For each rule: div.flex.items-center.gap-3.rounded-md.border.px-3.py-2.bg-amber-500/10
         containing:
         BookOpen icon (size={14}, className="text-amber-500 shrink-0"),
         span.text-sm with rule text,
         span.text-xs.text-amber-600.font-semibold.ml-auto with "Reminder".

    4. These items are non-interactive (no checkbox, no click handler). They are purely
       informational reminders extracted from past battle logs.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ChecklistTab calls useForgottenRules(listId)
    - When forgottenRules array is non-empty, a "Reminders from last games" section renders above checklist items
    - Each reminder item has bg-amber-500/10 background and BookOpen icon
    - Each reminder item displays the rule text and a "Reminder" tag
    - When forgottenRules is empty, no reminder section renders (no empty state for this)
    - Reminder items are non-interactive (no checkbox, no onClick)
    - npx tsc --noEmit exits 0
    - pnpm build exits 0
  </acceptance_criteria>
  <done>ChecklistTab surfaces forgotten rules from recent battle logs as amber reminder items above the user's checklist</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB TEXT → JSON.parse | forgotten_rules column parsed in buildDefaultValues and getRecentForgottenRules |
| Zustand persist → component | gameDayStore listStates read by ChecklistTab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-06 | T (Tampering) | BattleLogSheet forgotten_rules deserialization | mitigate | Wrap JSON.parse in try/catch in buildDefaultValues; fallback to null |
| T-78-07 | S (Spoofing) | BattleLogSheet prefill prop | accept | Prefill comes from app state (listId, todayISO), not user input; local app only |
| T-78-08 | I (Info Disclosure) | ChecklistTab forgotten rules | accept | Local-only data display; no network transmission |
</threat_model>

<verification>
1. npx tsc --noEmit exits 0
2. pnpm build exits 0
3. End Game button visible in GameDayHeader with gold border styling
4. Clicking End Game opens BattleLogSheet with pre-filled army_list_id and battle_date
5. BattleLogSheet shows "End Game" title when pre-filled
6. After-Action collapsible section contains all moved fields + new forgotten_rules textarea
7. ChecklistTab shows amber reminder items when forgotten rules exist in recent battle logs
</verification>

<success_criteria>
- "End Game" button in Game Day opens pre-filled BattleLogSheet (GD-01)
- After-action section captures forgotten rules, MVP/underperformer, lessons, changes (GD-02)
- Forgotten rules from past games surface as reminders in ChecklistTab (GD-03)
- changes_next_time serves as list-level notes inside the after-action collapsible (GD-04)
- MVP and underperformer fields moved into collapsible (not duplicated)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/78-dashboard-command-center-game-day-after-action/78-03-SUMMARY.md`
</output>

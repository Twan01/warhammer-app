---
phase: 78-dashboard-command-center-game-day-after-action
plan: 02
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - src/features/dashboard/NextPaintingActionCard.tsx
  - src/features/dashboard/ReadyToPlayCard.tsx
  - src/features/dashboard/DataHealthSummaryCard.tsx
  - src/features/dashboard/DashboardPage.tsx
autonomous: true
requirements: [DB-01, DB-02, DB-03]

must_haves:
  truths:
    - "D-01, D-02, D-03: Dashboard displays a Next Painting Action card showing step description, section name, time estimate, paint availability dots, and empty state when no assignments"
    - "D-05, D-06, D-07: Dashboard displays a Ready to Play card showing most-recently-edited army list name, total points, unpainted count, sync freshness, and empty state when no lists"
    - "D-08, D-09: Dashboard displays a Data Health summary card showing sync age dot, warning count, backup status, and link to /data-health page"
  artifacts:
    - path: "src/features/dashboard/NextPaintingActionCard.tsx"
      provides: "Next Painting Action dashboard card"
      exports: ["NextPaintingActionCard"]
    - path: "src/features/dashboard/ReadyToPlayCard.tsx"
      provides: "Ready to Play dashboard card"
      exports: ["ReadyToPlayCard"]
    - path: "src/features/dashboard/DataHealthSummaryCard.tsx"
      provides: "Data Health summary dashboard card"
      exports: ["DataHealthSummaryCard"]
  key_links:
    - from: "src/features/dashboard/NextPaintingActionCard.tsx"
      to: "src/hooks/useNextPaintingAction.ts"
      via: "useNextPaintingAction import (includes paint availability per D-02)"
      pattern: "useNextPaintingAction"
    - from: "src/features/dashboard/DashboardPage.tsx"
      to: "src/features/dashboard/NextPaintingActionCard.tsx"
      via: "component import"
      pattern: "NextPaintingActionCard"
    - from: "src/features/dashboard/DataHealthSummaryCard.tsx"
      to: "src/hooks/useDiagnostics.ts"
      via: "useDiagnosticFlags + useBackupStatus imports"
      pattern: "useDiagnosticFlags"
---

<objective>
Create the three dashboard command center cards (Next Painting Action, Ready to Play,
Data Health Summary) and wire them into the DashboardPage CSS grid.

Purpose: DB-01, DB-02, DB-03 — transform the dashboard from passive summary to actionable command center.
Output: Three new card components rendered in the dashboard left column.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/78-dashboard-command-center-game-day-after-action/78-CONTEXT.md
@.planning/phases/78-dashboard-command-center-game-day-after-action/78-UI-SPEC.md
@.planning/phases/78-dashboard-command-center-game-day-after-action/78-PATTERNS.md
@.planning/phases/78-dashboard-command-center-game-day-after-action/78-01-SUMMARY.md

<interfaces>
<!-- useNextPaintingAction hook (created in Plan 01) -->
From src/hooks/useNextPaintingAction.ts:
export const NEXT_PAINTING_ACTION_KEY = ["next-painting-action"] as const;
export function useNextPaintingAction(): UseQueryResult<FirstIncompleteStep | null>;

From src/db/queries/recipeAssignments.ts:
export interface FirstIncompleteStep {
  assignment_id: number;
  unit_id: number;
  unit_name: string;
  recipe_id: number;
  recipe_name: string;
  recipe_step_id: number;
  description: string;
  section_name: string | null;
  section_id: number | null;
  order_index: number;
  time_estimate_minutes: number | null;
  created_at: string;
}

<!-- Diagnostics hooks (from Phase 77) -->
From src/hooks/useDiagnostics.ts:
export function useDiagnosticFlags(): UseQueryResult<DiagnosticFlag[]>;
export function useBackupStatus(): BackupStatus | null;
export const BACKUP_STORAGE_KEY = "lastBackup";
export interface BackupStatus { date: string; path: string; success: boolean; }

<!-- Sync freshness -->
From src/lib/syncFreshness.ts:
export function getSyncFreshness(lastSyncAt: string | null): "fresh" | "stale" | "expired";
export function getSyncAgeLabel(lastSyncAt: string | null): string;
export const FRESHNESS_DOT_CLASS: Record<string, string>;

<!-- Army list hooks -->
From src/hooks/useArmyLists.ts:
export function useArmyLists(): UseQueryResult<ArmyList[]>;
export function useArmyListWithUnits(id: number): UseQueryResult<ArmyListUnit[]>;

<!-- computeListHealthStats -->
From src/lib/computeUnitWarnings.ts:
export function computeListHealthStats(units: ArmyListUnit[]): ListHealthStats;

<!-- Dashboard card pattern -->
From src/features/dashboard/ArmyReadinessCard.tsx:
- section label: text-sm font-semibold uppercase tracking-widest text-muted-foreground
- card wrapper: rounded-lg border border-border/60 bg-card p-4 shadow-sm transition-shadow duration-150 hover:shadow-md
- empty state: flex flex-col items-center gap-2 py-6 text-muted-foreground

<!-- DashboardPage grid -->
From src/features/dashboard/DashboardPage.tsx:
- Layout: grid grid-cols-1 gap-6 p-6 lg:grid-cols-[3fr_2fr]
- Left column starts at line 372: div.flex.flex-col.gap-6 contains Hobby Health + By Faction
- Right column at line 426: div.flex.flex-col.gap-6 contains ActiveProjectsPanel + RecentActivityFeed + ArmyReadinessCard
- Sibling portal contract: sheets are top-level siblings, never nested in cards
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NextPaintingActionCard, ReadyToPlayCard, and DataHealthSummaryCard</name>
  <files>
    src/features/dashboard/NextPaintingActionCard.tsx,
    src/features/dashboard/ReadyToPlayCard.tsx,
    src/features/dashboard/DataHealthSummaryCard.tsx
  </files>
  <read_first>
    src/features/dashboard/CurrentFocusCard.tsx,
    src/features/dashboard/ArmyReadinessCard.tsx,
    src/hooks/useNextPaintingAction.ts,
    src/hooks/useDiagnostics.ts,
    src/hooks/useArmyLists.ts,
    src/lib/syncFreshness.ts,
    src/lib/computeUnitWarnings.ts,
    src/hooks/useDatasheet.ts,
    .planning/phases/78-dashboard-command-center-game-day-after-action/78-UI-SPEC.md
  </read_first>
  <action>
    Create three new dashboard card components following the exact card patterns from
    ArmyReadinessCard (section label + card wrapper + empty state + loading skeleton).

    1. NextPaintingActionCard.tsx — per D-01, D-02, D-03, DB-01, and UI-SPEC:
       - Call useNextPaintingAction() hook. Loading state: Skeleton h-24 w-full.
       - When data is null: empty state with Palette icon (20px, opacity-40),
         heading "No painting action found", body "Assign a recipe to a unit to see your next step here." per UI-SPEC copywriting.
       - When data exists: section label "NEXT PAINTING ACTION", then card showing:
         Palette icon (14px) + section_name (text-xs text-muted-foreground) + " . " + description (text-sm, line-clamp-2),
         time estimate row with Clock icon (12px) + "Est. {time_estimate_minutes} min" (text-xs text-muted-foreground tabular-nums) — show only when time_estimate_minutes is not null,
         paint availability row: for each paint in data.paints, render a colored dot
           (12×12px rounded-full inline) colored by ownership — emerald-500 if owned, amber-500 if
           running low, zinc-400 if missing — followed by paint name (text-xs text-muted-foreground).
           Per D-02 and UI-SPEC: owned/missing/running-low indicator per paint.
         link row: text-xs text-muted-foreground underline-offset-2 hover:underline "Go to recipe" — use Link from @tanstack/react-router to="/painting-projects".
       - Card class: rounded-lg border border-border/60 bg-card p-4 shadow-sm transition-shadow duration-150 hover:shadow-md.

    2. ReadyToPlayCard.tsx — per D-05, D-06, D-07, DB-02:
       - Call useArmyLists() hook. Sort data by updated_at DESC, take first item. If no lists: empty state
         with Shield icon (20px, opacity-40), heading "No army lists yet",
         body "Create an army list to track battle readiness." per UI-SPEC.
       - When a list exists: call useArmyListWithUnits(list.id) and useRulesSyncMeta().
         Compute health stats via computeListHealthStats(units). Get sync freshness via
         getSyncFreshness(syncMeta?.last_sync_at ?? null) and getSyncAgeLabel.
       - Section label "READY TO PLAY", card showing: list name + formatted points
         (text-sm font-semibold), metadata row with Shield icon (12px) + unpainted count + Clock icon (12px) + sync age label.
         Use FRESHNESS_DOT_CLASS for sync dot color. Use text-battle-gold on list name when
         battleReadyPct is 100 and no warnings. Warning badge (bg-amber-500/15 text-amber-600
         text-xs px-1.5 py-0.5 rounded) shown when unpainted > 0 or sync stale.

    3. DataHealthSummaryCard.tsx — per D-08, D-09, DB-03:
       - Call useRulesSyncMeta(), useDiagnosticFlags(), and useBackupStatus() (the last is not
         a React Query hook — it reads localStorage synchronously).
       - Section label "DATA HEALTH".
       - Three metrics in horizontal flex row (flex items-center gap-4):
         (a) Sync dot (rounded-full w-2 h-2 colored per getSyncFreshness) + "Synced X days ago" or "Never synced" via getSyncAgeLabel,
         (b) Database icon (12px) + warning count from flags.length (or reduce by count) — "No warnings" (text-muted-foreground) or "{N} warning(s)" (text-amber-600 dark:text-amber-400),
         (c) HardDrive icon (12px) + backup age — compute days since backup.date, show "Backed up X days ago" or "No backup".
       - Link at bottom: Link to="/data-health" text "View full report" (text-xs text-muted-foreground underline-offset-2 hover:underline).
       - Loading: render individual Skeletons for each metric.
       - Import Link from @tanstack/react-router.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/features/dashboard/NextPaintingActionCard.tsx exports named NextPaintingActionCard component
    - NextPaintingActionCard calls useNextPaintingAction() and renders step description, section_name, time_estimate_minutes, paint availability dots (owned/missing/running-low per D-02), and "Go to recipe" link
    - NextPaintingActionCard renders empty state with "No painting action found" text when data is null
    - src/features/dashboard/ReadyToPlayCard.tsx exports named ReadyToPlayCard component
    - ReadyToPlayCard calls useArmyLists() and sorts by updated_at DESC
    - ReadyToPlayCard renders list name, points, unpainted count, sync freshness dot
    - ReadyToPlayCard renders empty state with "No army lists yet" text
    - src/features/dashboard/DataHealthSummaryCard.tsx exports named DataHealthSummaryCard component
    - DataHealthSummaryCard calls useRulesSyncMeta(), useDiagnosticFlags(), useBackupStatus()
    - DataHealthSummaryCard renders sync dot, warning count, backup age in horizontal row
    - DataHealthSummaryCard contains Link to="/data-health" with text "View full report"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Three dashboard cards render with data, loading, and empty states; all follow ArmyReadinessCard pattern</done>
</task>

<task type="auto">
  <name>Task 2: Wire three new cards into DashboardPage grid</name>
  <files>
    src/features/dashboard/DashboardPage.tsx
  </files>
  <read_first>
    src/features/dashboard/DashboardPage.tsx,
    src/features/dashboard/NextPaintingActionCard.tsx,
    src/features/dashboard/ReadyToPlayCard.tsx,
    src/features/dashboard/DataHealthSummaryCard.tsx,
    .planning/phases/78-dashboard-command-center-game-day-after-action/78-UI-SPEC.md
  </read_first>
  <action>
    Per UI-SPEC Dashboard Grid Placement: the three new cards go in the left column below
    the HobbyPipeline full-width section, grouped under a "Command Center" section label.

    1. Add three imports at the top of DashboardPage.tsx:
       import { NextPaintingActionCard } from "./NextPaintingActionCard";
       import { ReadyToPlayCard } from "./ReadyToPlayCard";
       import { DataHealthSummaryCard } from "./DataHealthSummaryCard";

    2. In the left column div (the div.flex.flex-col.gap-6 that currently contains
       Hobby Health + By Faction sections, around line 372), insert a new section
       ABOVE the Hobby Health section. The new section structure:

       section.flex.flex-col.gap-4 with section label p "Command Center"
       (text-sm font-semibold uppercase tracking-widest text-muted-foreground),
       then a div.flex.flex-col.gap-4 containing:
       NextPaintingActionCard, ReadyToPlayCard, DataHealthSummaryCard — in that order.

    3. No new Sheet portals needed — these cards link/navigate, they do not open sheets.
       Respect the sibling portal contract comment at line 445.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage.tsx imports NextPaintingActionCard, ReadyToPlayCard, DataHealthSummaryCard
    - DashboardPage.tsx renders all three cards inside a "Command Center" section in the left column
    - Section label reads "Command Center" with uppercase tracking-widest styling
    - Cards appear in order: NextPaintingActionCard, ReadyToPlayCard, DataHealthSummaryCard
    - No new Sheet siblings added to DashboardPage (cards link out, not open sheets)
    - npx tsc --noEmit exits 0
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Dashboard renders three new command center cards in left column; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → component | BackupStatus read from localStorage (user-controlled storage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-04 | T (Tampering) | DataHealthSummaryCard useBackupStatus | accept | useBackupStatus already wraps JSON.parse in try/catch (Phase 77); local-only, no security impact |
| T-78-05 | I (Info Disclosure) | Dashboard cards | accept | All data local to user's machine, no network exposure |
</threat_model>

<verification>
1. npx tsc --noEmit exits 0
2. pnpm build exits 0
3. Dashboard renders three new cards with correct data
4. Empty states display when no assignments/lists/sync data
5. Data Health card links to /data-health
</verification>

<success_criteria>
- Dashboard shows "Next Painting Action" card with step description, time estimate, section name
- Dashboard shows "Ready to Play" card with list name, points, unpainted count, sync freshness
- Dashboard shows "Data Health" card with sync dot, warning count, backup age, and link to full report
- All three cards have loading skeletons and empty states
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/78-dashboard-command-center-game-day-after-action/78-02-SUMMARY.md`
</output>

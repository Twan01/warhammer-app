---
phase: 78-dashboard-command-center-game-day-after-action
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/battleLog.ts
  - src/features/battle-log/battleLogSchema.ts
  - src/db/queries/battleLogs.ts
  - src/db/queries/recipeAssignments.ts
  - src/hooks/useNextPaintingAction.ts
  - src/hooks/useBattleLogs.ts
autonomous: true
requirements: [DB-01, GD-02, GD-03]

must_haves:
  truths:
    - "BattleLog type includes forgotten_rules, mvp_notes, underperformer_notes, promoted_to_reminder fields"
    - "battleLogSchema validates forgotten_rules as nullable string"
    - "createBattleLog and updateBattleLog persist forgotten_rules, mvp_notes, underperformer_notes columns"
    - "getRecentForgottenRules returns deduplicated string array from last 3 battle logs for an army list"
    - "getMostRecentAssignmentWithIncompleteStep returns the first incomplete step across all assignments in a single SQL query"
    - "useNextPaintingAction hook returns typed NextPaintingAction result via React Query"
  artifacts:
    - path: "src/hooks/useNextPaintingAction.ts"
      provides: "Composite hook for dashboard Next Painting Action card"
      exports: ["useNextPaintingAction", "NEXT_PAINTING_ACTION_KEY"]
    - path: "src/db/queries/recipeAssignments.ts"
      provides: "getMostRecentAssignmentWithIncompleteStep query"
      contains: "getMostRecentAssignmentWithIncompleteStep"
    - path: "src/db/queries/battleLogs.ts"
      provides: "getRecentForgottenRules query + extended create/update"
      contains: "getRecentForgottenRules"
  key_links:
    - from: "src/hooks/useNextPaintingAction.ts"
      to: "src/db/queries/recipeAssignments.ts"
      via: "getMostRecentAssignmentWithIncompleteStep import"
      pattern: "getMostRecentAssignmentWithIncompleteStep"
    - from: "src/db/queries/battleLogs.ts"
      to: "src/types/battleLog.ts"
      via: "BattleLog type import"
      pattern: "forgotten_rules"
---

<objective>
Extend the battle log data layer with after-action columns and create the data-fetching
infrastructure for both the dashboard Next Painting Action card and the Game Day
forgotten rules reminders pipeline.

Purpose: Wave 1 foundation — all UI plans in Wave 2 depend on these types, queries, and hooks.
Output: Extended BattleLog type, extended schema/queries, new useNextPaintingAction hook,
new getRecentForgottenRules query.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-dashboard-command-center-game-day-after-action/78-CONTEXT.md
@.planning/phases/78-dashboard-command-center-game-day-after-action/78-RESEARCH.md
@.planning/phases/78-dashboard-command-center-game-day-after-action/78-PATTERNS.md

<interfaces>
<!-- BattleLog type (current state — missing 4 columns from migration 027) -->
From src/types/battleLog.ts:
export interface BattleLog {
  id: number;
  army_list_id: number | null;
  battle_date: string;
  opponent: string | null;
  opponent_faction: string;
  mission: string;
  points_played: number | null;
  result: BattleLogResult;
  my_score: number | null;
  opponent_score: number | null;
  mvp_unit_id: number | null;
  underperforming_unit_id: number | null;
  lessons_learned: string | null;
  changes_next_time: string | null;
  notes: string | null;
  created_at: string;
}
export type CreateBattleLogInput = Omit<BattleLog, "id" | "created_at">;
export type UpdateBattleLogInput = CreateBattleLogInput & { id: number };

<!-- RecipeAssignment query pattern -->
From src/db/queries/recipeAssignments.ts:
export async function getAssignmentsByUnit(unitId: number): Promise<RecipeAssignment[]>
export async function getStepProgress(assignmentId: number): Promise<StepProgress[]>

<!-- RecipePaint type has time_estimate_minutes -->
From src/types/recipePaint.ts line 17:
  time_estimate_minutes: number | null;

<!-- Existing battleLogSchema (needs forgotten_rules + mvp_notes + underperformer_notes) -->
From src/features/battle-log/battleLogSchema.ts:
export const battleLogSchema = z.object({ ... });
export type BattleLogFormValues = z.infer<typeof battleLogSchema>;

<!-- Hook pattern -->
From src/hooks/useRecipeAssignments.ts:
export const UNIT_ASSIGNMENTS_KEY = (unitId: number) => ["recipe-assignments", "unit", unitId] as const;
export function useAssignmentsByUnit(unitId: number | undefined) { ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BattleLog types, schema, and queries with after-action columns</name>
  <files>
    src/types/battleLog.ts,
    src/features/battle-log/battleLogSchema.ts,
    src/db/queries/battleLogs.ts,
    src/hooks/useBattleLogs.ts
  </files>
  <read_first>
    src/types/battleLog.ts,
    src/features/battle-log/battleLogSchema.ts,
    src/db/queries/battleLogs.ts,
    src/hooks/useBattleLogs.ts,
    src-tauri/migrations/027_battle_log_after_action.sql
  </read_first>
  <action>
    Per D-13, D-14, and RESEARCH Critical Finding: migration 027 already added forgotten_rules TEXT,
    mvp_notes TEXT, underperformer_notes TEXT, and promoted_to_reminder INTEGER NOT NULL DEFAULT 0
    to the battle_logs table, but the TypeScript type was never updated.

    1. In src/types/battleLog.ts — add four fields to the BattleLog interface before created_at:
       forgotten_rules: string | null (JSON TEXT: serialized string[]),
       mvp_notes: string | null,
       underperformer_notes: string | null,
       promoted_to_reminder: number (0 | 1 boolean).
       CreateBattleLogInput and UpdateBattleLogInput derive via Omit so they auto-update.

    2. In src/features/battle-log/battleLogSchema.ts — add three new fields to the battleLogSchema
       z.object: forgotten_rules as z.string().max(5000).nullable(), mvp_notes as z.string().max(2000).nullable(),
       underperformer_notes as z.string().max(2000).nullable(). Do NOT add promoted_to_reminder
       to the schema (it is not a form field — it is a derived flag set programmatically).

    3. In src/db/queries/battleLogs.ts — extend createBattleLog INSERT to include
       forgotten_rules, mvp_notes, underperformer_notes columns (positions $15, $16, $17).
       Extend updateBattleLog SET clause to include forgotten_rules = $16, mvp_notes = $17,
       underperformer_notes = $18 (adjust all positional params accordingly).
       Full-replacement pattern — no COALESCE for any of these nullable columns.

    4. In src/db/queries/battleLogs.ts — add getRecentForgottenRules(armyListId: number): Promise<string[]>.
       Query: SELECT forgotten_rules FROM battle_logs WHERE army_list_id = $1 AND forgotten_rules IS NOT NULL
       ORDER BY battle_date DESC, created_at DESC LIMIT 3.
       Parse each row's forgotten_rules with JSON.parse inside try/catch (return [] on parse error,
       log console.warn). Deduplicate via Set, return [...seen].

    5. In src/hooks/useBattleLogs.ts — add and export FORGOTTEN_RULES_KEY function:
       (armyListId: number) => ["forgotten-rules", armyListId] as const.
       Add and export useForgottenRules(armyListId: number | undefined) hook using useQuery
       with enabled: armyListId !== undefined, queryFn calling getRecentForgottenRules.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/types/battleLog.ts BattleLog interface contains forgotten_rules: string | null,
      mvp_notes: string | null, underperformer_notes: string | null, promoted_to_reminder: number
    - src/features/battle-log/battleLogSchema.ts contains forgotten_rules, mvp_notes,
      underperformer_notes in the z.object call
    - src/db/queries/battleLogs.ts createBattleLog INSERT lists forgotten_rules, mvp_notes,
      underperformer_notes in column list with matching positional params
    - src/db/queries/battleLogs.ts updateBattleLog SET clause includes forgotten_rules, mvp_notes,
      underperformer_notes with positional params — no COALESCE
    - src/db/queries/battleLogs.ts exports getRecentForgottenRules function
    - getRecentForgottenRules uses try/catch around JSON.parse
    - src/hooks/useBattleLogs.ts exports useForgottenRules and FORGOTTEN_RULES_KEY
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>BattleLog type, schema, queries, and hook all include after-action fields; TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create useNextPaintingAction hook with single-query DB function</name>
  <files>
    src/db/queries/recipeAssignments.ts,
    src/hooks/useNextPaintingAction.ts
  </files>
  <read_first>
    src/db/queries/recipeAssignments.ts,
    src/hooks/useRecipeAssignments.ts,
    src/types/recipeAssignment.ts,
    src/types/recipePaint.ts,
    src-tauri/migrations/012_recipe_steps.sql,
    src-tauri/migrations/021_applied_recipe_assignments.sql
  </read_first>
  <action>
    Per D-01, D-04, and RESEARCH Pitfall 6: create a single SQL query that returns the first
    incomplete step across all recipe assignments, avoiding N+1.

    1. In src/db/queries/recipeAssignments.ts — define and export a new interface FirstIncompleteStep
       with fields: assignment_id (number), unit_id (number), unit_name (string), recipe_id (number),
       recipe_name (string), recipe_step_id (number), description (string), section_name (string | null),
       section_id (number | null), order_index (number), time_estimate_minutes (number | null),
       created_at (string — assignment created_at for sort).

    2. Add and export getMostRecentAssignmentWithIncompleteStep(): Promise<FirstIncompleteStep | null>.
       SQL joins: unit_recipe_assignments a JOIN units u ON u.id = a.unit_id
       JOIN recipes r ON r.id = a.recipe_id JOIN recipe_steps rs ON rs.recipe_id = a.recipe_id
       LEFT JOIN recipe_sections sec ON sec.id = rs.section_id
       LEFT JOIN unit_recipe_step_progress p ON p.assignment_id = a.id AND p.recipe_step_id = rs.id.
       WHERE p.id IS NULL (step has no progress row = incomplete).
       ORDER BY a.created_at DESC, rs.order_index ASC LIMIT 1.
       Return rows[0] ?? null.

       CRITICAL: sort by a.created_at DESC (NOT updated_at — column does not exist on
       unit_recipe_assignments per migration 021). The column name is time_estimate_minutes
       (not time_estimate) per migration 012.

    3. Create src/hooks/useNextPaintingAction.ts — export NEXT_PAINTING_ACTION_KEY =
       ["next-painting-action"] as const. Export useNextPaintingAction() returning
       useQuery with queryKey NEXT_PAINTING_ACTION_KEY, queryFn getMostRecentAssignmentWithIncompleteStep.
       Also re-export FirstIncompleteStep type for consumer components.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/db/queries/recipeAssignments.ts exports FirstIncompleteStep interface with all 12 fields
    - src/db/queries/recipeAssignments.ts exports getMostRecentAssignmentWithIncompleteStep function
    - SQL query uses a.created_at DESC (not updated_at)
    - SQL query uses rs.time_estimate_minutes (not time_estimate)
    - SQL query uses LEFT JOIN unit_recipe_step_progress with WHERE p.id IS NULL
    - SQL query uses $1-style positional params (though this query has no params)
    - src/hooks/useNextPaintingAction.ts exports useNextPaintingAction and NEXT_PAINTING_ACTION_KEY
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>useNextPaintingAction hook returns first incomplete recipe step via single SQL round-trip; compiles cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite TEXT → JSON.parse | forgotten_rules column contains free-form TEXT that is parsed as JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | T (Tampering) | battleLogs.ts getRecentForgottenRules | mitigate | Wrap JSON.parse in try/catch; return [] on malformed data; log console.warn |
| T-78-02 | I (Info Disclosure) | recipeAssignments.ts | accept | Local-only SQLite, no network exposure; query returns only assignment metadata |
| T-78-03 | D (DoS) | getMostRecentAssignmentWithIncompleteStep | accept | LIMIT 1 prevents unbounded result sets; local DB only |
</threat_model>

<verification>
1. npx tsc --noEmit exits 0
2. pnpm test passes (no existing tests broken by type extension)
3. BattleLog type has all 4 new fields
4. battleLogSchema has forgotten_rules, mvp_notes, underperformer_notes
5. createBattleLog and updateBattleLog handle all new columns
6. getRecentForgottenRules handles malformed JSON gracefully
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- BattleLog data layer fully supports after-action fields
- useNextPaintingAction hook ready for dashboard card consumption
- getRecentForgottenRules query ready for ChecklistTab consumption
- No migration needed (027 already covers the schema)
</success_criteria>

<output>
After completion, create `.planning/phases/78-dashboard-command-center-game-day-after-action/78-01-SUMMARY.md`
</output>

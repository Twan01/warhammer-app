---
phase: 15-warhammer-40k-datasheet-and-rules-integration
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/datasheet/csvParse.test.ts
  - tests/datasheet/stripHtml.test.ts
  - tests/datasheet/migration.test.ts
  - tests/datasheet/datasheetQueries.test.ts
  - tests/datasheet/useDatasheet.test.tsx
  - tests/datasheet/DatasheetPicker.test.tsx
  - tests/datasheet/DatasheetImportDialog.test.tsx
autonomous: true
requirements:
  - DS-01
  - DS-04
  - DS-07
  - DS-08
  - DS-09
user_setup: []
must_haves:
  truths:
    - "tests/datasheet/csvParse.test.ts exists with at least 3 it.skip blocks (header parse, trailing pipe, empty body)"
    - "tests/datasheet/stripHtml.test.ts exists with at least 3 it.skip blocks (tag strip, &amp;/&lt; entities, &nbsp;/numeric entities)"
    - "tests/datasheet/migration.test.ts exists with at least 1 it.skip block (rules_001_schema.sql content + lib.rs version: 1 registration for sqlite:rules.db)"
    - "tests/datasheet/datasheetQueries.test.ts exists with at least 4 it.skip blocks (getDatasheetsByFaction, getFullDatasheet, getRulesSyncMeta, upsertDatasheetLink in hobbyforge.db)"
    - "tests/datasheet/useDatasheet.test.tsx exists with at least 2 it.skip blocks (returns null when unitId undefined, returns FullDatasheet when linked)"
    - "tests/datasheet/DatasheetPicker.test.tsx exists with at least 2 it.skip blocks (renders faction-filtered list, search filter substring match)"
    - "tests/datasheet/DatasheetImportDialog.test.tsx exists with at least 2 it.skip blocks (renders one row per conflict, default choice 'use')"
    - "All 7 stub files exit cleanly: pnpm test exits 0 (skipped tests count as passing for vitest exit code)"
    - "No file imports source-under-test files yet — Plans 15-01..05 add imports while replacing .skip with real bodies"
  artifacts:
    - path: "tests/datasheet/csvParse.test.ts"
      provides: "Wave 0 stub for parseWahapediaCsv pure function"
      contains: "it.skip("
    - path: "tests/datasheet/stripHtml.test.ts"
      provides: "Wave 0 stub for stripHtml pure function"
      contains: "it.skip("
    - path: "tests/datasheet/migration.test.ts"
      provides: "Wave 0 stub for rules_001_schema.sql + lib.rs registration"
      contains: "it.skip("
    - path: "tests/datasheet/datasheetQueries.test.ts"
      provides: "Wave 0 stub for src/db/queries/datasheets.ts query functions"
      contains: "it.skip("
    - path: "tests/datasheet/useDatasheet.test.tsx"
      provides: "Wave 0 stub for useDatasheet hook"
      contains: "it.skip("
    - path: "tests/datasheet/DatasheetPicker.test.tsx"
      provides: "Wave 0 stub for DatasheetPicker render + search"
      contains: "it.skip("
    - path: "tests/datasheet/DatasheetImportDialog.test.tsx"
      provides: "Wave 0 stub for DatasheetImportDialog conflict render"
      contains: "it.skip("
  key_links:
    - from: "tests/datasheet/*.test.ts(x)"
      to: "Vitest globals (vitest.config.ts globals:true)"
      via: "explicit `import { describe, it } from 'vitest'` for tsc strict-mode compatibility (mirrors tests/hobby-journal/* and tests/spending/*)"
      pattern: "it\\.skip\\("
---

<objective>
Create the seven Wave 0 stub test files required by `15-VALIDATION.md` §"Wave 0 Requirements" so that downstream plans (15-01 through 15-05) have concrete `<automated>` test targets to flip from `.skip` to real assertions. Each file declares the exact `it.skip` blocks named in `15-VALIDATION.md` §"Per-Task Verification Map" and contains commented stubs documenting what later plans must assert.

Purpose: Closes the Nyquist gap listed in `15-VALIDATION.md` §"Wave 0 Requirements". Without these stub files the executors for plans 15-01..05 have `<automated>MISSING</automated>` verify targets. With them, every later task has a concrete failing-or-skipped vitest target to flip green.

Output: Seven new files under `tests/datasheet/` (a brand-new directory). Each contains `it.skip()` placeholders and TODO markers documenting what later plans must implement.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-warhammer-40k-datasheet-and-rules-integration/15-CONTEXT.md
@.planning/phases/15-warhammer-40k-datasheet-and-rules-integration/15-RESEARCH.md
@.planning/phases/15-warhammer-40k-datasheet-and-rules-integration/15-VALIDATION.md
@.planning/phases/15-warhammer-40k-datasheet-and-rules-integration/15-UI-SPEC.md

@tests/setup.ts
@tests/foundation/strategyNoteQueries.test.ts
@tests/hobby-journal/migration005.test.ts
@tests/spending/computeSpendingStats.test.ts
@tests/theming/FactionSummaryCard.test.tsx

<interfaces>
<!-- Vitest config — globals:true means describe/it/expect are global, but explicit
     vitest imports are still required for tsc strict-mode (project rule) -->

From vitest.config.ts (already configured):
```typescript
test: {
  globals: true,
  environment: "jsdom",
  setupFiles: ["./tests/setup.ts"],
  include: ["tests/**/*.test.ts", "tests/**/*.test.tsx"],
}
```

<!-- Wave 0 stub pattern confirmed from Plans 13-00 and 14-00:
     - it.skip blocks named per VALIDATION.md test-name strings
     - it.skip() with TODO markers naming the later plan that fills it in
     - no imports of source-under-test (those files don't exist yet)
     - explicit `import { describe, it } from "vitest"` for tsc strict-mode
     - .tsx extension when test renders JSX (component or hook with provider wrapper)
     - .ts extension for pure-function and content-shape tests
     - pnpm test exits 0 because skipped tests count as passing for the suite -->

<!-- Existing pattern to mirror:
     - SQL test pattern: tests/foundation/strategyNoteQueries.test.ts (vi.mock("@/db/client", ...))
     - Migration content test pattern: tests/hobby-journal/migration005.test.ts (readFileSync + regex assertions)
     - Component render stub: tests/theming/FactionSummaryCard.test.tsx
     - Pure function test pattern: tests/spending/computeSpendingStats.test.ts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/datasheet/csvParse.test.ts + tests/datasheet/stripHtml.test.ts (pure-function stubs)</name>
  <files>tests/datasheet/csvParse.test.ts, tests/datasheet/stripHtml.test.ts</files>
  <read_first>
    - .planning/phases/15-warhammer-40k-datasheet-and-rules-integration/15-VALIDATION.md (§Per-Task Verification Map rows 15-01-01 csvParse and 15-01-02 stripHtml)
    - .planning/phases/15-warhammer-40k-datasheet-and-rules-integration/15-RESEARCH.md (§Pattern 3 Pipe-Delimited CSV Parse; §Stripping HTML from Ability Descriptions)
    - tests/spending/computeSpendingStats.test.ts (template for pure-function stubs — describe/it.skip pattern, no SUT imports)
    - tests/setup.ts (confirms vitest globals enabled)
  </read_first>
  <action>
Create TWO files in this task. Both `.ts` (no JSX). The directory `tests/datasheet/` does not exist yet — the Write tool creates parent dirs.

**Part A — Create `tests/datasheet/csvParse.test.ts`** with EXACTLY this content.

```typescript
/**
 * Phase 15 — parseWahapediaCsv pure function tests (Wave 0 stubs).
 *
 * STATUS: skipped. Plan 15-02 will:
 *   1. Create src/lib/parseWahapediaCsv.ts exporting parseWahapediaCsv(raw: string).
 *   2. Replace each `it.skip` below with `it`.
 *   3. Add real assertions matching 15-VALIDATION.md row 15-01-01.
 *
 * The stub exists in Wave 0 so Plan 15-02 has a concrete failing-or-skipped vitest target.
 *
 * Pipe-delimited CSV format details from 15-RESEARCH.md §Pattern 3:
 *   - first line is headers split by "|"
 *   - each subsequent line maps to an object keyed by header
 *   - Wahapedia CSVs have a trailing "|" on each row (last empty column tolerated)
 *   - empty body returns []
 */
import { describe, it } from "vitest";

describe("parseWahapediaCsv — Wave 0 stubs", () => {
  it.skip("DS-01: parses simple pipe-delimited CSV with 3 columns and 2 rows", () => {
    // Plan 15-02 will:
    //   - import { parseWahapediaCsv } from "@/lib/parseWahapediaCsv"
    //   - const raw = "id|name|role\n001|Intercessors|Battleline\n002|Terminators|Elite"
    //   - const rows = parseWahapediaCsv(raw)
    //   - assert rows.length === 2
    //   - assert rows[0] equals { id: "001", name: "Intercessors", role: "Battleline" }
    //   - assert rows[1] equals { id: "002", name: "Terminators", role: "Elite" }
  });

  it.skip("DS-01: handles Wahapedia trailing-pipe rows (header and rows both end with |)", () => {
    // Plan 15-02 will:
    //   - const raw = "id|name|\n001|Intercessors|\n002|Terminators|"
    //   - const rows = parseWahapediaCsv(raw)
    //   - assert rows.length === 2
    //   - assert rows[0].id === "001" && rows[0].name === "Intercessors"
    //   - the trailing empty header maps to "" — values["" key] are tolerated
  });

  it.skip("DS-01: returns [] when the input is empty or contains only the header line", () => {
    // Plan 15-02 will:
    //   - assert parseWahapediaCsv("") deep-equals []
    //   - assert parseWahapediaCsv("id|name|role") deep-equals []  // single line = no data rows
  });
});
```

**Part B — Create `tests/datasheet/stripHtml.test.ts`** with EXACTLY this content.

```typescript
/**
 * Phase 15 — stripHtml pure function tests (Wave 0 stubs).
 *
 * STATUS: skipped. Plan 15-02 will:
 *   1. Create src/lib/stripHtml.ts exporting stripHtml(html: string).
 *   2. Replace each `it.skip` below with `it`.
 *   3. Add real assertions matching 15-VALIDATION.md row 15-01-02.
 *
 * The stub exists in Wave 0 so Plan 15-02 has a concrete failing-or-skipped vitest target.
 *
 * Behavior contract from 15-RESEARCH.md §Stripping HTML from Ability Descriptions:
 *   - replace HTML tags with empty string: /<[^>]*>/g → ""
 *   - decode &amp; &lt; &gt; &nbsp; entities literally
 *   - decode &#NNN; numeric entities by char code
 *   - .trim() at end
 */
import { describe, it } from "vitest";

describe("stripHtml — Wave 0 stubs", () => {
  it.skip("DS-09: strips <b>, <i>, <br> HTML tags from ability description text", () => {
    // Plan 15-02 will:
    //   - import { stripHtml } from "@/lib/stripHtml"
    //   - assert stripHtml("<b>Sustained Hits 1</b>") === "Sustained Hits 1"
    //   - assert stripHtml("Line one<br>Line two") === "Line oneLine two"
    //   - assert stripHtml("<i>Lethal Hits</i>") === "Lethal Hits"
  });

  it.skip("DS-09: decodes named HTML entities &amp; &lt; &gt; &nbsp;", () => {
    // Plan 15-02 will:
    //   - assert stripHtml("&amp;") === "&"
    //   - assert stripHtml("&lt;Twin&gt;") === "<Twin>"
    //   - assert stripHtml("Hits&nbsp;1") === "Hits 1"
  });

  it.skip("DS-09: decodes numeric HTML entities &#NNN; (e.g. &#8211; for en-dash)", () => {
    // Plan 15-02 will:
    //   - assert stripHtml("&#8211;") === "–"   // en-dash U+2013
    //   - assert stripHtml("range &#8212; 12&quot;") contains the em-dash and trims trailing whitespace
    //   - assert stripHtml("  spaces  ") === "spaces" (.trim() applied)
  });
});
```

Both files use only `import { describe, it } from "vitest"`. Do NOT import `parseWahapediaCsv` or `stripHtml` — those files do not exist yet (Plan 15-02 creates them and replaces these stubs in-place).
  </action>
  <verify>
    <automated>pnpm test --run tests/datasheet/csvParse.test.ts tests/datasheet/stripHtml.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/datasheet/csvParse.test.ts` exists
    - csvParse test file contains literal `describe("parseWahapediaCsv — Wave 0 stubs"`
    - csvParse test file contains literal `it.skip("DS-01: parses simple pipe-delimited CSV`
    - csvParse test file contains literal `it.skip("DS-01: handles Wahapedia trailing-pipe rows`
    - csvParse test file contains literal `it.skip("DS-01: returns [] when the input is empty`
    - csvParse test file contains at least 3 `it.skip(` occurrences
    - File `tests/datasheet/stripHtml.test.ts` exists
    - stripHtml test file contains literal `describe("stripHtml — Wave 0 stubs"`
    - stripHtml test file contains literal `it.skip("DS-09: strips <b>, <i>, <br>`
    - stripHtml test file contains literal `it.skip("DS-09: decodes named HTML entities`
    - stripHtml test file contains literal `it.skip("DS-09: decodes numeric HTML entities`
    - stripHtml test file contains at least 3 `it.skip(` occurrences
    - Both files contain literal `import { describe, it } from "vitest"`
    - Neither file contains literal `from "@/lib/parseWahapediaCsv"` or `from "@/lib/stripHtml"` (SUT does not exist yet)
    - Running `pnpm test --run tests/datasheet/csvParse.test.ts tests/datasheet/stripHtml.test.ts` exits with code 0
    - Combined test output reports 0 failed (skipped count >= 6)
  </acceptance_criteria>
  <done>Two stub files created — csvParse has 3 it.skip stubs (simple parse, trailing-pipe, empty body); stripHtml has 3 it.skip stubs (tags, named entities, numeric entities). Both files exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/datasheet/migration.test.ts + tests/datasheet/datasheetQueries.test.ts (schema + query stubs)</name>
  <files>tests/datasheet/migration.test.ts, tests/datasheet/datasheetQueries.test.ts</files>
  <read_first>
    - .planning/phases/15-warhammer-40k-datasheet-and-rules-integration/15-VALIDATION.md (§Per-Task Verification Map rows 15-01-03 migration and 15-02-01 datasheetQueries)
    - .planning/phases/15-warhammer-40k-datasheet-and-rules-integration/15-RESEARCH.md (§rules.db Schema Design; §Querying Datasheets for Faction Picker; §Querying Full Datasheet for Import; §Checking Rules DB Population Status)
    - tests/hobby-journal/migration005.test.ts (template — readFileSync + regex content assertions)
    - tests/foundation/strategyNoteQueries.test.ts (template for vi.mock("@/db/client") query mocking pattern)
    - src-tauri/src/lib.rs (current state — get_migrations() function structure)
  </read_first>
  <action>
Create TWO files in this task.

**Part A — Create `tests/datasheet/migration.test.ts`** with EXACTLY this content.

```typescript
/**
 * Phase 15 — Migration content tests (Wave 0 stubs).
 *
 * STATUS: skipped. Plan 15-01 will:
 *   1. Create src-tauri/migrations/rules_001_schema.sql with the rw_factions, rw_datasheets,
 *      rw_datasheet_models, rw_datasheet_abilities, rw_datasheet_keywords, rw_sources, rw_sync_meta tables.
 *   2. Create src-tauri/migrations/007_datasheet_link.sql adding `datasheet_id TEXT` to unit_strategy_notes
 *      (no FK because cross-database FKs are not supported — see Pitfall 1).
 *   3. Add get_rules_migrations() function to src-tauri/src/lib.rs and chain
 *      .add_migrations("sqlite:rules.db", get_rules_migrations()) onto the SQL plugin.
 *   4. Add version: 7, description: "datasheet_link", include_str!("../migrations/007_datasheet_link.sql")
 *      to get_migrations().
 *   5. Replace each `it.skip` below with `it`.
 *   6. Add real readFileSync assertions matching 15-VALIDATION.md row 15-01-03.
 *
 * Mirrors tests/hobby-journal/migration005.test.ts pattern. Content-shape test only —
 * tauri-plugin-sql IPC cannot run in jsdom.
 */
import { describe, it } from "vitest";

describe("rules_001_schema.sql + migration 007 — Wave 0 stubs", () => {
  it.skip("DS-01: rules_001_schema.sql contains all 7 rw_* tables (rw_factions, rw_datasheets, rw_datasheet_models, rw_datasheet_abilities, rw_datasheet_keywords, rw_sources, rw_sync_meta)", () => {
    // Plan 15-01 will:
    //   - readFileSync("src-tauri/migrations/rules_001_schema.sql", "utf-8")
    //   - assert sql matches /CREATE TABLE IF NOT EXISTS rw_factions/
    //   - assert sql matches /CREATE TABLE IF NOT EXISTS rw_datasheets/
    //   - assert sql matches /CREATE TABLE IF NOT EXISTS rw_datasheet_models/
    //   - assert sql matches /CREATE TABLE IF NOT EXISTS rw_datasheet_abilities/
    //   - assert sql matches /CREATE TABLE IF NOT EXISTS rw_datasheet_keywords/
    //   - assert sql matches /CREATE TABLE IF NOT EXISTS rw_sources/
    //   - assert sql matches /CREATE TABLE IF NOT EXISTS rw_sync_meta/
    //   - assert sql matches /id\s+INTEGER PRIMARY KEY CHECK \(id = 1\)/   // rw_sync_meta single row
    //   - assert sql matches /datasheet_id\s+TEXT.+REFERENCES rw_datasheets\(id\)\s+ON DELETE CASCADE/
    //   - assert sql does NOT match /\bDROP\b/i  (additive only)
    //   - assert sql does NOT match /\bDELETE FROM\b/i
  });

  it.skip("DS-06: 007_datasheet_link.sql adds datasheet_id TEXT column to unit_strategy_notes (NO REFERENCES — cross-DB FK not supported)", () => {
    // Plan 15-01 will:
    //   - readFileSync("src-tauri/migrations/007_datasheet_link.sql", "utf-8")
    //   - assert sql matches /ALTER TABLE unit_strategy_notes ADD COLUMN datasheet_id TEXT/
    //   - assert sql does NOT match /REFERENCES rw_datasheets/  (Pitfall 1: SQLite cannot FK across DB files)
    //   - assert sql does NOT match /\bDROP\b/i
    //   - assert sql does NOT match /\bDELETE FROM\b/i
  });

  it.skip("DS-01 + DS-06: src-tauri/src/lib.rs registers BOTH databases — version: 7 entry for hobbyforge.db AND get_rules_migrations() chained for sqlite:rules.db", () => {
    // Plan 15-01 will:
    //   - readFileSync("src-tauri/src/lib.rs", "utf-8")
    //   - assert libRs matches /version:\s*7\s*,/
    //   - assert libRs matches /description:\s*"datasheet_link"/
    //   - assert libRs matches /007_datasheet_link\.sql/
    //   - assert libRs matches /fn get_rules_migrations\(\)/
    //   - assert libRs matches /version:\s*1\s*,[\s\S]*description:\s*"rules_schema"/   // first rules.db migration
    //   - assert libRs matches /rules_001_schema\.sql/
    //   - assert libRs matches /\.add_migrations\("sqlite:rules\.db",\s*get_rules_migrations\(\)\)/
    //   - readFileSync("src-tauri/tauri.conf.json", "utf-8")
    //   - parsed JSON: assert plugins.sql.preload includes "sqlite:rules.db"
  });
});
```

**Part B — Create `tests/datasheet/datasheetQueries.test.ts`** with EXACTLY this content.

```typescript
/**
 * Phase 15 — datasheets query module tests (Wave 0 stubs).
 *
 * STATUS: skipped. Plan 15-03 will:
 *   1. Create src/db/queries/datasheets.ts exporting getDatasheetsByFaction, getFullDatasheet,
 *      getRulesSyncMeta, upsertDatasheetLink (writes to hobbyforge.db unit_strategy_notes).
 *   2. Create src/db/rules-client.ts exporting getRulesDb singleton.
 *   3. Create src/types/datasheet.ts exporting RwFaction, RwDatasheet, RwDatasheetModel,
 *      RwDatasheetAbility, RwDatasheetKeyword, RwSource, RulesSyncMeta, FullDatasheet,
 *      DatasheetSummary types.
 *   4. Replace each `it.skip` below with `it`.
 *   5. Add real assertions matching 15-VALIDATION.md row 15-02-01.
 *
 * Mirrors tests/foundation/strategyNoteQueries.test.ts vi.mock("@/db/client") pattern,
 * plus a parallel vi.mock("@/db/rules-client") for the second DB connection.
 */
import { describe, it } from "vitest";

describe("datasheets queries — Wave 0 stubs", () => {
  it.skip("DS-04: getDatasheetsByFaction issues SELECT id, name, role FROM rw_datasheets WHERE faction_id = $1 ORDER BY name ASC against rules.db", () => {
    // Plan 15-03 will:
    //   - vi.mock("@/db/rules-client") with selectMock/executeMock
    //   - selectMock.mockResolvedValueOnce([{ id: "001", name: "Intercessors", role: "Battleline" }])
    //   - import { getDatasheetsByFaction } from "@/db/queries/datasheets"
    //   - const rows = await getDatasheetsByFaction("SM")
    //   - assert selectMock called with /SELECT id, name, role FROM rw_datasheets WHERE faction_id = \$1 ORDER BY name ASC/
    //   - assert second arg equals ["SM"]
    //   - assert rows.length === 1 && rows[0].name === "Intercessors"
  });

  it.skip("DS-07: getFullDatasheet returns null when datasheet ID does not exist; otherwise returns ds + models + abilities + keywords + source", () => {
    // Plan 15-03 will:
    //   - selectMock.mockResolvedValueOnce([])  // ds query returns no rows
    //   - import { getFullDatasheet } from "@/db/queries/datasheets"
    //   - assert (await getFullDatasheet("missing")) === null
    //   - reset; selectMock returns rows for ds, models, abilities, keywords, source in 5 sequential calls
    //   - assert returned object has shape { ds, models, abilities, keywords, source }
    //   - assert abilities query SQL excludes Wargear types: /type NOT IN \('Wargear', 'Wargear profile', 'Fortification \(.+\)'\)/
  });

  it.skip("DS-02 + DS-03: getRulesSyncMeta returns null when rules.db is empty/uninitialized; otherwise returns the single rw_sync_meta row", () => {
    // Plan 15-03 will:
    //   - selectMock.mockRejectedValueOnce(new Error("no such table: rw_sync_meta"))
    //   - import { getRulesSyncMeta } from "@/db/queries/datasheets"
    //   - assert (await getRulesSyncMeta()) === null  (try/catch returns null on schema-missing error)
    //   - reset; selectMock.mockResolvedValueOnce([{ id: 1, last_sync_at: "2026-05-04T12:00:00Z", wahapedia_version: "2026-04-27 20:55:42" }])
    //   - const meta = await getRulesSyncMeta()
    //   - assert meta?.last_sync_at === "2026-05-04T12:00:00Z"
  });

  it.skip("DS-06: upsertDatasheetLink writes datasheet_id to unit_strategy_notes in hobbyforge.db (NOT rules.db); creates row if no strategy note exists yet", () => {
    // Plan 15-03 will:
    //   - vi.mock("@/db/client") (hobbyforge.db, NOT rules-client)
    //   - selectMock.mockResolvedValueOnce([])  // no existing strategy_notes row for unit
    //   - import { upsertDatasheetLink } from "@/db/queries/datasheets"
    //   - await upsertDatasheetLink({ unit_id: 7, datasheet_id: "000000882" })
    //   - assert executeMock called with INSERT INTO unit_strategy_notes (unit_id, datasheet_id) VALUES ($1, $2)
    //   - reset; selectMock.mockResolvedValueOnce([{ id: 1 }])  // existing row
    //   - assert executeMock called with UPDATE unit_strategy_notes SET datasheet_id=$2 WHERE unit_id=$1
  });
});
```

Both files use only `import { describe, it } from "vitest"`. Do NOT import `@/db/queries/datasheets`, `@/db/rules-client`, or `@/types/datasheet` — those files do not exist yet.
  </action>
  <verify>
    <automated>pnpm test --run tests/datasheet/migration.test.ts tests/datasheet/datasheetQueries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/datasheet/migration.test.ts` exists
    - migration test file contains literal `describe("rules_001_schema.sql + migration 007 — Wave 0 stubs"`
    - migration test file contains literal `it.skip("DS-01: rules_001_schema.sql contains all 7 rw_* tables`
    - migration test file contains literal `it.skip("DS-06: 007_datasheet_link.sql adds datasheet_id TEXT column`
    - migration test file contains literal `it.skip("DS-01 + DS-06: src-tauri/src/lib.rs registers BOTH databases`
    - migration test file contains at least 3 `it.skip(` occurrences
    - File `tests/datasheet/datasheetQueries.test.ts` exists
    - datasheetQueries test file contains literal `describe("datasheets queries — Wave 0 stubs"`
    - datasheetQueries test file contains literal `it.skip("DS-04: getDatasheetsByFaction`
    - datasheetQueries test file contains literal `it.skip("DS-07: getFullDatasheet returns null`
    - datasheetQueries test file contains literal `it.skip("DS-02 + DS-03: getRulesSyncMeta`
    - datasheetQueries test file contains literal `it.skip("DS-06: upsertDatasheetLink`
    - datasheetQueries test file contains at least 4 `it.skip(` occurrences
    - Both files contain literal `import { describe, it } from "vitest"`
    - Neither file contains literal `from "@/db/queries/datasheets"` or `from "@/db/rules-client"`
    - Running `pnpm test --run tests/datasheet/migration.test.ts tests/datasheet/datasheetQueries.test.ts` exits with code 0
    - Combined test output reports 0 failed (skipped count >= 7)
  </acceptance_criteria>
  <done>Two stub files created — migration has 3 it.skip stubs (rules schema, hobbyforge migration 007, lib.rs registration); datasheetQueries has 4 it.skip stubs (faction-filtered list, full datasheet, sync meta, link upsert). Both files exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/datasheet/useDatasheet.test.tsx + DatasheetPicker.test.tsx + DatasheetImportDialog.test.tsx (hook + 2 component stubs)</name>
  <files>tests/datasheet/useDatasheet.test.tsx, tests/datasheet/DatasheetPicker.test.tsx, tests/datasheet/DatasheetImportDialog.test.tsx</files>
  <read_first>
    - .planning/phases/15-warhammer-40k-datasheet-and-rules-integration/15-VALIDATION.md (§Per-Task Verification Map rows 15-02-02, 15-03-01, 15-03-02)
    - .planning/phases/15-warhammer-40k-datasheet-and-rules-integration/15-UI-SPEC.md (§DatasheetPicker layout; §DatasheetImportDialog layout; §Conflict Resolution Flow default-choice rule)
    - .planning/phases/15-warhammer-40k-datasheet-and-rules-integration/15-RESEARCH.md (§Pattern 6 staleTime: Infinity for Datasheet Hooks)
    - tests/hobby-journal/useJournalSessions.test.ts (template for hook test stub style)
    - tests/theming/FactionSummaryCard.test.tsx (template for component render stub style)
    - src/hooks/useStrategyNote.ts (hook pattern reference)
  </read_first>
  <action>
Create THREE files in this task. All `.tsx` because each renders JSX (component test or hook with QueryClientProvider wrapper).

**Part A — Create `tests/datasheet/useDatasheet.test.tsx`** with EXACTLY this content.

```typescript
/**
 * Phase 15 — useDatasheet hook tests (Wave 0 stubs).
 *
 * STATUS: skipped. Plan 15-03 will:
 *   1. Create src/hooks/useDatasheet.ts exporting useDatasheet (calls getFullDatasheet via the
 *      datasheet_id stored on unit_strategy_notes), DATASHEET_KEY factory, and useRulesSyncMeta.
 *   2. Replace each `it.skip` below with `it`.
 *   3. Add real assertions matching 15-VALIDATION.md row 15-02-02.
 *
 * Mirrors src/hooks/useStrategyNote.ts pattern (per-unit query key factory, staleTime: Infinity).
 */
import { describe, it } from "vitest";

describe("useDatasheet — Wave 0 stubs", () => {
  it.skip("DS-04 + DS-06: useDatasheet returns { data: null } when unitId is undefined (sheet not yet open)", () => {
    // Plan 15-03 will:
    //   - vi.mock("@/db/queries/datasheets") with getFullDatasheet returning null
    //   - render <QueryClientProvider><Consumer unitId={undefined} /></QueryClientProvider>
    //   - assert hook result.current.data === null && enabled === false (no fetch fired)
    //   - assert getFullDatasheet was NOT called
  });

  it.skip("DS-07: useDatasheet returns FullDatasheet (ds + models + abilities + keywords + source) when the unit has a datasheet_id link", () => {
    // Plan 15-03 will:
    //   - vi.mock("@/db/queries/datasheets") returning full payload from getFullDatasheet
    //   - prime QueryClient with no cached data
    //   - render <QueryClientProvider><Consumer unitId={7} /></QueryClientProvider>
    //   - waitFor(() => expect(result.current.data).not.toBeNull())
    //   - assert result.current.data.ds.name === "Intercessors"
    //   - assert result.current.data.abilities.length > 0
    //   - assert getFullDatasheet was called once with the unit's stored datasheet_id
  });
});
```

**Part B — Create `tests/datasheet/DatasheetPicker.test.tsx`** with EXACTLY this content.

```typescript
/**
 * Phase 15 — DatasheetPicker component tests (Wave 0 stubs).
 *
 * STATUS: skipped. Plan 15-04 will:
 *   1. Create src/features/units/DatasheetPicker.tsx — Dialog with autoFocus search input,
 *      faction-pre-filtered list (~200 datasheets), client-side substring filter.
 *   2. Replace each `it.skip` below with `it`.
 *   3. Add real assertions matching 15-VALIDATION.md row 15-03-01.
 */
import { describe, it } from "vitest";

describe("DatasheetPicker — Wave 0 stubs", () => {
  it.skip("DS-04: renders Dialog with the unit's faction name in DialogDescription and one button per pre-filtered datasheet row", () => {
    // Plan 15-04 will:
    //   - vi.mock("@/hooks/useDatasheet") — useDatasheetsByFaction returns
    //     [{ id: "001", name: "Intercessors", role: "Battleline" }, { id: "002", name: "Hellblasters", role: "Battleline" }]
    //   - render <DatasheetPicker open={true} factionId="SM" factionName="Space Marines" onSelect={vi.fn()} onSkip={vi.fn()} />
    //   - assert screen.getByText(/Searching Space Marines datasheets/) is in document
    //   - assert screen.getByText("Intercessors") is in document
    //   - assert screen.getByText("Hellblasters") is in document
    //   - assert screen.getByRole("button", { name: "Skip" }) is in document
  });

  it.skip("DS-04: search input filters list by case-insensitive substring match on the name field", () => {
    // Plan 15-04 will:
    //   - render with 3 datasheets (Intercessors, Terminators, Hellblasters)
    //   - fireEvent.change(searchInput, { target: { value: "term" } })
    //   - assert screen.queryByText("Intercessors") is null
    //   - assert screen.getByText("Terminators") is in document
    //   - clear input
    //   - assert screen.queryByText("Intercessors") is in document again
    //   - empty-state branch: type "zzz" → assert screen.getByText(/No datasheets found/) appears
  });
});
```

**Part C — Create `tests/datasheet/DatasheetImportDialog.test.tsx`** with EXACTLY this content.

```typescript
/**
 * Phase 15 — DatasheetImportDialog component tests (Wave 0 stubs).
 *
 * STATUS: skipped. Plan 15-04 will:
 *   1. Create src/features/units/DatasheetImportDialog.tsx — Dialog showing a per-field
 *      "Keep yours / Use datasheet" toggle for every conflicting field; default choice "use".
 *   2. Replace each `it.skip` below with `it`.
 *   3. Add real assertions matching 15-VALIDATION.md row 15-03-02.
 */
import { describe, it } from "vitest";

describe("DatasheetImportDialog — Wave 0 stubs", () => {
  it.skip("DS-08: renders one field row per conflict; each row shows current value, incoming value, and Keep/Use buttons", () => {
    // Plan 15-04 will:
    //   - const conflicts = [
    //       { key: "M", label: "M", currentValue: "5", incomingValue: "6", choice: "use" },
    //       { key: "T", label: "T", currentValue: "4", incomingValue: "4", choice: "use" }, // would be filtered before reaching dialog
    //       { key: "abilities", label: "Personal Ability Notes", currentValue: "Old text", incomingValue: "Imported text", choice: "use" },
    //     ]
    //   - render <DatasheetImportDialog open={true} conflicts={conflicts} onConfirm={vi.fn()} onClose={vi.fn()} />
    //   - assert screen.getAllByRole("button", { name: "Keep" }).length === 3 (one per row)
    //   - assert screen.getAllByRole("button", { name: "Use datasheet" }).length === 3
    //   - assert screen.getByText("Yours: 5") is in document (M current value display)
    //   - assert screen.getByText("Datasheet: 6") is in document
    //   - assert screen.getByRole("button", { name: "Apply import" }) is in document
    //   - assert screen.getByRole("button", { name: "Discard changes" }) is in document
  });

  it.skip("DS-08: every row defaults to choice = 'use' (Use datasheet button shows variant='default'); clicking 'Keep' on a row flips that row's choice and visual state", () => {
    // Plan 15-04 will:
    //   - render dialog with 1 conflict row, choice: "use"
    //   - assert the "Use datasheet" button has data-variant="default" or className containing "bg-primary"
    //   - assert the "Keep" button has variant="outline" (lighter visual)
    //   - fireEvent.click(screen.getByRole("button", { name: "Keep" }))
    //   - re-query and assert the "Keep" button now shows the variant="default" style
    //   - click "Apply import" — assert onConfirm called with the resolved selections (e.g. { M: "keep", ... })
  });
});
```

All three files use only `import { describe, it } from "vitest"`. Do NOT import any of `@/hooks/useDatasheet`, `@/features/units/DatasheetPicker`, or `@/features/units/DatasheetImportDialog` — those files do not exist yet.
  </action>
  <verify>
    <automated>pnpm test --run tests/datasheet/useDatasheet.test.tsx tests/datasheet/DatasheetPicker.test.tsx tests/datasheet/DatasheetImportDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/datasheet/useDatasheet.test.tsx` exists
    - useDatasheet test file contains literal `describe("useDatasheet — Wave 0 stubs"`
    - useDatasheet test file contains literal `it.skip("DS-04 + DS-06: useDatasheet returns { data: null }`
    - useDatasheet test file contains literal `it.skip("DS-07: useDatasheet returns FullDatasheet`
    - useDatasheet test file contains at least 2 `it.skip(` occurrences
    - File `tests/datasheet/DatasheetPicker.test.tsx` exists
    - DatasheetPicker test file contains literal `describe("DatasheetPicker — Wave 0 stubs"`
    - DatasheetPicker test file contains literal `it.skip("DS-04: renders Dialog with the unit's faction name`
    - DatasheetPicker test file contains literal `it.skip("DS-04: search input filters list`
    - DatasheetPicker test file contains at least 2 `it.skip(` occurrences
    - File `tests/datasheet/DatasheetImportDialog.test.tsx` exists
    - DatasheetImportDialog test file contains literal `describe("DatasheetImportDialog — Wave 0 stubs"`
    - DatasheetImportDialog test file contains literal `it.skip("DS-08: renders one field row per conflict`
    - DatasheetImportDialog test file contains literal `it.skip("DS-08: every row defaults to choice = 'use'`
    - DatasheetImportDialog test file contains at least 2 `it.skip(` occurrences
    - All three files contain literal `import { describe, it } from "vitest"`
    - None of the three files contains literal `from "@/hooks/useDatasheet"`, `from "@/features/units/DatasheetPicker"`, or `from "@/features/units/DatasheetImportDialog"`
    - Running `pnpm test --run tests/datasheet/useDatasheet.test.tsx tests/datasheet/DatasheetPicker.test.tsx tests/datasheet/DatasheetImportDialog.test.tsx` exits with code 0
    - Combined test output reports 0 failed (skipped count >= 6)
    - `pnpm test` (full suite) exits 0 — confirming no regression on the existing 279-test baseline
  </acceptance_criteria>
  <done>Three stub files created — useDatasheet has 2 it.skip stubs (no unit, full payload); DatasheetPicker has 2 it.skip stubs (faction-filtered render, search filter); DatasheetImportDialog has 2 it.skip stubs (per-field row layout, default 'use' choice). Full suite exits 0.</done>
</task>

</tasks>

<verification>
- `pnpm test --run tests/datasheet/` exits 0 (all 7 stub files run; no failures, only skipped tests)
- `pnpm test` (full suite) exits 0 (no regression — the new files only add skipped tests on top of the existing 279-test baseline from Phase 14)
- `pnpm tsc --noEmit` exits 0 (`describe`, `it` imports resolve from "vitest"; no SUT imports anywhere)
- Wave 0 gaps in `15-VALIDATION.md` §"Wave 0 Requirements" are now resolved (all 7 files exist)
- Plans 15-01..05 will replace each `.skip` with a real body alongside the source-under-test it tests
</verification>

<success_criteria>
- All 7 stub test files exist under `tests/datasheet/`
- Each stub names exactly the behaviors plans 15-01, 15-02, 15-03, 15-04, 15-05 must implement
- Plans 15-01..05 have defined `<automated>` verify targets (no MISSING references)
- No production code touched in this plan
- Total skipped it() count: csvParse 3 + stripHtml 3 + migration 3 + datasheetQueries 4 + useDatasheet 2 + DatasheetPicker 2 + DatasheetImportDialog 2 = 19 stubs
</success_criteria>

<output>
After completion, create `.planning/phases/15-warhammer-40k-datasheet-and-rules-integration/15-00-SUMMARY.md` listing:
- 7 new files (tests/datasheet/{csvParse,stripHtml,migration,datasheetQueries}.test.ts + {useDatasheet,DatasheetPicker,DatasheetImportDialog}.test.tsx)
- it.skip blocks declared per VALIDATION map
- Total stub it.skip counts: csvParse 3, stripHtml 3, migration 3, datasheetQueries 4, useDatasheet 2, DatasheetPicker 2, DatasheetImportDialog 2 → 19 stubs
- Plans 15-01..05 will replace each .skip with a real body alongside the source-under-test
- Wave 0 gaps from 15-VALIDATION.md are now closed
</output>

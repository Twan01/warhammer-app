---
phase: 140-close-play-02-03-tail-repoint-rules-hub-datasheetpointstab-f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/data-layer/leaderTargetsByFaction.test.ts
  - src/db/queries/leaderTargets.ts
  - src/hooks/useLeaderTargets.ts
  - src/features/rules-hub/DatasheetPointsTab.tsx
  - src/db/queries/bsdataExtended.ts
  - src/hooks/useBsdataFaction.ts
  - tests/army-list/ArmyListsPage.test.tsx
autonomous: true
requirements: [PLAY-02, PLAY-03]

must_haves:
  truths:
    - "The Rules Hub DatasheetPointsTab 'Leader — Can attach to' section reads from the canonical udb_leader_targets table (faction-scoped join through udb_units), not the dead synced_leader_targets table."
    - "No UI surface or hook depends on the unwritten synced_leader_targets table; useLeaderTargetsByFaction / getLeaderTargetsByFaction / SyncedLeaderTargetRow no longer exist in the codebase."
    - "For a faction with canonical leader pairs, the faction-scoped query returns real leader→target rows ordered by leader_name then target_name; an unknown faction returns an empty array."
  artifacts:
    - path: "src/db/queries/leaderTargets.ts"
      provides: "getLeaderTargetsByFactionCanonical query + CanonicalLeaderTargetRow type"
      contains: "getLeaderTargetsByFactionCanonical"
    - path: "src/hooks/useLeaderTargets.ts"
      provides: "useLeaderTargetsByFactionCanonical hook + faction key factory"
      contains: "useLeaderTargetsByFactionCanonical"
    - path: "tests/data-layer/leaderTargetsByFaction.test.ts"
      provides: "Faction-scoped query correctness + cross-faction exclusion + unknown-faction empty"
      min_lines: 60
  key_links:
    - from: "src/features/rules-hub/DatasheetPointsTab.tsx"
      to: "src/hooks/useLeaderTargets.ts"
      via: "useLeaderTargetsByFactionCanonical(factionId) call"
      pattern: "useLeaderTargetsByFactionCanonical"
    - from: "src/hooks/useLeaderTargets.ts"
      to: "src/db/queries/leaderTargets.ts"
      via: "getLeaderTargetsByFactionCanonical queryFn"
      pattern: "getLeaderTargetsByFactionCanonical"
    - from: "src/db/queries/leaderTargets.ts"
      to: "udb_leader_targets JOIN udb_units"
      via: "double-join on leader/target unit ids filtered by leader faction"
      pattern: "udb_leader_targets"
---

<objective>
Close the v0.6.0 milestone-audit integration gap on the Rules Hub leader display seam. The
`DatasheetPointsTab` "Leader — Can attach to" section currently reads the dead
`synced_leader_targets` table (zero writers in the Wahapedia-only pipeline) and therefore
silently renders empty. Repoint it to the canonical `udb_leader_targets` table via a new
faction-scoped query + hook, then retire the now-zero-caller dead readers — finishing the
cleanup STATE.md D-10 deliberately deferred.

Purpose: Completes the PLAY-02/PLAY-03 tail on the read-only Rules Hub display surface
(Phase 137 already repointed the army-list builder); makes the schema honest by removing the
last consumer of the dead synced table.
Output: One canonical faction-scoped query, one canonical hook, a repointed component, three
removed dead symbols, a cleaned test mock, and a new data-layer test proving query correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-close-play-02-03-tail-repoint-rules-hub-datasheetpointstab-f/140-CONTEXT.md
@.planning/phases/140-close-play-02-03-tail-repoint-rules-hub-datasheetpointstab-f/140-RESEARCH.md
@.planning/phases/140-close-play-02-03-tail-repoint-rules-hub-datasheetpointstab-f/140-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from live source — no exploration required. -->

New type to add to src/db/queries/leaderTargets.ts (identical shape to the old SyncedLeaderTargetRow, per D-04):
```typescript
export interface CanonicalLeaderTargetRow {
  leader_name: string;
  faction_id: string | null;
  target_name: string;
}
```

New query to add to src/db/queries/leaderTargets.ts (co-located beneath getLeaderTargetsForList). Signature:
```typescript
export async function getLeaderTargetsByFactionCanonical(
  factionId: string,
): Promise<CanonicalLeaderTargetRow[]>;
```
Exact SQL (verified by RESEARCH — both faction_id and id are TEXT, no CAST; composite PK + 1:1 name joins mean NO DISTINCT):
```sql
SELECT leader_u.name AS leader_name, leader_u.faction_id, target_u.name AS target_name
FROM udb_leader_targets lt
JOIN udb_units leader_u ON leader_u.id = lt.leader_unit_id
JOIN udb_units target_u ON target_u.id = lt.target_unit_id
WHERE leader_u.faction_id = $1
ORDER BY leader_name, target_name
```
Bind factionId via the [$1] positional param array (matches getLeaderTargetsForList / getUdbPointsByFaction idiom).

New hook to add to src/hooks/useLeaderTargets.ts (mirrors useLeaderTargetsByFaction ergonomics in useBsdataFaction.ts so the call site swaps cleanly):
```typescript
export const LEADER_TARGETS_BY_FACTION_KEY = (factionId: string) =>
  ["leader-targets-by-faction-canonical", factionId] as const;

export function useLeaderTargetsByFactionCanonical(factionId: string | undefined): ...
// staleTime: Infinity, gcTime: Infinity, enabled: factionId !== undefined
// disabled-state queryKey: ["leader-targets-by-faction-canonical", "disabled"]
```

DatasheetPointsTab.tsx current bindings to swap (verified line numbers):
- line 7: `useLeaderTargetsByFaction` imported from "@/hooks/useBsdataFaction"
- line 14: `type SyncedLeaderTargetRow` imported from "@/db/queries/bsdataExtended"
- line ~79, ~120: `SyncedLeaderTargetRow[]` prop type annotations on DatasheetDetail / DatasheetContent
- line ~362: `const { data: leaderTargets = [] } = useLeaderTargetsByFaction(factionId);`
factionId is a required `string` prop on DatasheetPointsTab; the new hook accepts `string | undefined` but is always called with the defined value.

useBsdataFaction.ts symbols to PRESERVE (still live, consumed by DatasheetPointsTab): useModelCountsByFaction, useLoadoutOptionsByFaction, MODEL_COUNTS_KEY, LOADOUT_OPTIONS_KEY. Only remove the leader-targets trio.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 data-layer test for the faction-scoped canonical query</name>
  <files>tests/data-layer/leaderTargetsByFaction.test.ts</files>
  <read_first>
    - tests/data-layer/leader-targets.test.ts — mirror this harness exactly: `// @vitest-environment node` header, `createFullDb()` (in-memory DB, reads every `src-tauri/migrations/*.sql` sorted by numeric prefix, `PRAGMA foreign_keys = ON`), and the seed-with-FK-OFF-then-ON pattern (insert udb_factions / udb_units / udb_leader_targets with `foreign_keys = OFF`, re-enable before querying).
    - src/db/queries/leaderTargets.ts — the SQL string this test exercises (see <interfaces>); replicate the SELECT inline via better-sqlite3 `db.prepare(...).all(factionId)` because the Tauri `getDb()` async bridge is unavailable in node env (same approach as leader-targets.test.ts).
  </read_first>
  <action>
    Create tests/data-layer/leaderTargetsByFaction.test.ts mirroring tests/data-layer/leader-targets.test.ts. Reuse the `createFullDb()` helper verbatim (in-memory DB + full migration chain + PRAGMA foreign_keys = ON). Seed (with foreign_keys = OFF, then re-enable): 2 udb_factions rows (id 'FA' name 'Faction A'; id 'FB' name 'Faction B'); 5 udb_units rows — LA (faction 'FA','Leader A'), TA1 (faction 'FA','Target A1'), TA2 (faction 'FA','Target A2'), LB (faction 'FB','Leader B'), TB1 (faction 'FB','Target B1') — supplying the `updated_at` column with `datetime('now')` as the existing test does; 3 udb_leader_targets rows (LA→TA1, LA→TA2, LB→TB1). Define the faction-scoped SELECT inline as a constant string identical to the D-02 SQL but with `?` for better-sqlite3 positional binding (better-sqlite3 uses `?`, not `$1`; the production query uses `$1` for Tauri plugin-sql — note this divergence in a comment). Write three `it` cases: (1) querying 'FA' returns exactly [{leader_name:'Leader A',faction_id:'FA',target_name:'Target A1'},{leader_name:'Leader A',faction_id:'FA',target_name:'Target A2'}] in that order; (2) querying 'FA' returns zero rows where leader_name==='Leader B' or target_name==='Target B1' (cross-faction exclusion); (3) querying 'UNKNOWN' returns an empty array. Close the db in each case as the existing test does.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/data-layer/leaderTargetsByFaction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/data-layer/leaderTargetsByFaction.test.ts exists and starts with `// @vitest-environment node`.
    - The file seeds 2 factions, 5 udb_units, 3 udb_leader_targets rows.
    - Three `it` cases present: correct-pairs-for-FA (ordered), cross-faction-exclusion, unknown-faction-empty.
    - `pnpm test -- tests/data-layer/leaderTargetsByFaction.test.ts` exits 0 (all three green).
  </acceptance_criteria>
  <done>The data-layer test exists, exercises the faction-scoped JOIN over the full migration chain, and all three cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical query + hook, repoint DatasheetPointsTab</name>
  <files>src/db/queries/leaderTargets.ts, src/hooks/useLeaderTargets.ts, src/features/rules-hub/DatasheetPointsTab.tsx</files>
  <read_first>
    - src/db/queries/leaderTargets.ts — the canonical module; copy the getLeaderTargetsForList idiom (getDb(), db.select<T[]>(sql, [param])) for the new fn. Per D-01 the new query lives HERE, not in bsdataExtended.ts.
    - src/features/rules-hub/DatasheetPointsTab.tsx — lines 1-20 (imports), the getUdbPointsByFaction faction-filter idiom, the DatasheetDetail/DatasheetContent prop types (~line 79, ~120) and the hook call (~line 362). The component body that filters `leader_name === unitName` and renders `target_name` badges stays UNCHANGED.
    - src/hooks/useBsdataFaction.ts — the useLeaderTargetsByFaction shape (staleTime/gcTime Infinity, enabled guard, KEY factory, disabled queryKey) to mirror for the new canonical hook.
    - src/hooks/useLeaderTargets.ts — the existing useLeaderTargets(listId) module the new hook is appended to.
  </read_first>
  <action>
    (a) In src/db/queries/leaderTargets.ts add `export interface CanonicalLeaderTargetRow` (shape per D-04: leader_name string, faction_id string|null, target_name string) and `export async function getLeaderTargetsByFactionCanonical(factionId: string): Promise<CanonicalLeaderTargetRow[]>` executing the D-02 SQL (see <interfaces>) via `const db = await getDb(); return db.select<CanonicalLeaderTargetRow[]>(sql, [factionId]);`. No DISTINCT (composite PK + 1:1 name joins). Add a brief doc comment noting Phase 140, PLAY-02/03 secondary surface, and that $1 is a positional bind (no interpolation).
    (b) In src/hooks/useLeaderTargets.ts add `LEADER_TARGETS_BY_FACTION_KEY` factory + `useLeaderTargetsByFactionCanonical(factionId: string | undefined)` (see <interfaces>): import getLeaderTargetsByFactionCanonical + CanonicalLeaderTargetRow from the query module, staleTime: Infinity, gcTime: Infinity, enabled: factionId !== undefined, disabled-state queryKey ["leader-targets-by-faction-canonical","disabled"]. Use a distinct key namespace from the old hook to avoid cache collision (Pitfall 3). Do NOT touch the existing useLeaderTargets(listId) or its LEADER_TARGETS_KEY.
    (c) In src/features/rules-hub/DatasheetPointsTab.tsx: swap the line-7 import `useLeaderTargetsByFaction` → `useLeaderTargetsByFactionCanonical` from "@/hooks/useLeaderTargets" (remove it from the useBsdataFaction import group; KEEP useModelCountsByFaction + useLoadoutOptionsByFaction there); swap the line-14 `type SyncedLeaderTargetRow` import → `type CanonicalLeaderTargetRow` from "@/db/queries/leaderTargets" (remove it from the bsdataExtended import group; KEEP SyncedLoadoutOptionRow there); replace all `SyncedLeaderTargetRow` prop-type annotations (~line 79, ~120) with `CanonicalLeaderTargetRow`; swap the ~line-362 call `useLeaderTargetsByFaction(factionId)` → `useLeaderTargetsByFactionCanonical(factionId)`. Leave the filter/badge JSX untouched. Do NOT remove the dead symbols from bsdataExtended.ts / useBsdataFaction.ts in this task (that is Task 3) — they remain valid module exports so the build stays green here.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'export async function getLeaderTargetsByFactionCanonical' src/db/queries/leaderTargets.ts` matches.
    - `grep -n 'CanonicalLeaderTargetRow' src/db/queries/leaderTargets.ts` matches the interface definition.
    - `grep -n 'useLeaderTargetsByFactionCanonical' src/hooks/useLeaderTargets.ts` matches.
    - `grep -c 'SyncedLeaderTargetRow' src/features/rules-hub/DatasheetPointsTab.tsx` returns 0 (all swapped).
    - `grep -c 'useLeaderTargetsByFaction\b' src/features/rules-hub/DatasheetPointsTab.tsx` returns 0 (only the *Canonical name remains).
    - `pnpm build` exits 0 (strict TS clean — no stale/unused imports in the component).
  </acceptance_criteria>
  <done>The canonical query + hook exist, DatasheetPointsTab consumes them, the filter/badge body is unchanged, and `pnpm build` is green.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the dead synced-table readers + clean the stale test mock</name>
  <files>src/db/queries/bsdataExtended.ts, src/hooks/useBsdataFaction.ts, tests/army-list/ArmyListsPage.test.tsx</files>
  <read_first>
    - src/db/queries/bsdataExtended.ts (lines ~159-176) — `SyncedLeaderTargetRow` interface + `getLeaderTargetsByFaction` fn (reads dead `synced_leader_targets`); both removed here. Leave getModelCountsByFaction / SyncedModelCountRow / loadout helpers intact.
    - src/hooks/useBsdataFaction.ts — remove the `getLeaderTargetsByFaction` import (line ~17), the faction `LEADER_TARGETS_KEY` factory (lines ~30-31), and `useLeaderTargetsByFaction` (lines ~61-71). PRESERVE useModelCountsByFaction, useLoadoutOptionsByFaction, MODEL_COUNTS_KEY, LOADOUT_OPTIONS_KEY.
    - tests/army-list/ArmyListsPage.test.tsx (lines ~64-67) — the `vi.mock("@/db/queries/bsdataExtended", ...)` factory contains a stale `getLeaderTargetsByFaction: vi.fn().mockResolvedValue([])` key (RESEARCH landmine). Remove that one key; KEEP getEnhancementsByFaction.
  </read_first>
  <action>
    (a) In src/db/queries/bsdataExtended.ts delete the `SyncedLeaderTargetRow` interface and the `getLeaderTargetsByFaction` function (the only reader of `synced_leader_targets`). Touch nothing else.
    (b) In src/hooks/useBsdataFaction.ts delete: the `getLeaderTargetsByFaction` name from the bsdataExtended import block; the faction `LEADER_TARGETS_KEY` factory; and the `useLeaderTargetsByFaction` hook. Keep the two live hooks and their two KEY factories. Update the file's top doc comment if it still claims three hooks.
    (c) In tests/army-list/ArmyListsPage.test.tsx remove the `getLeaderTargetsByFaction: vi.fn().mockResolvedValue([])` entry from the `@/db/queries/bsdataExtended` mock factory so the mock stays honest. Leave getEnhancementsByFaction.
    After this task the symbols useLeaderTargetsByFaction, getLeaderTargetsByFaction, and SyncedLeaderTargetRow must have ZERO occurrences anywhere in src/ and tests/.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'getLeaderTargetsByFaction\b' src tests` returns no matches.
    - `grep -rn 'useLeaderTargetsByFaction\b' src tests` returns no matches.
    - `grep -rn 'SyncedLeaderTargetRow' src tests` returns no matches.
    - `grep -rn 'synced_leader_targets' src` returns no matches (only the migration DDL retains it; src is clean).
    - `grep -c 'useModelCountsByFaction' src/hooks/useBsdataFaction.ts` and `grep -c 'useLoadoutOptionsByFaction' src/hooks/useBsdataFaction.ts` both ≥ 1 (live hooks preserved).
    - `pnpm build` exits 0 (strict noUnusedLocals catches any orphaned import).
    - `pnpm test` exits 0 (full suite green, including ArmyListsPage and the new data-layer test).
  </acceptance_criteria>
  <done>The three dead symbols are gone, the stale test-mock key is removed, the two live BSData hooks are preserved, and both `pnpm build` and `pnpm test` are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component → DB read | DatasheetPointsTab supplies a `factionId` (its own required prop, derived from the canonical faction list — not free user text) to a read-only SELECT over `udb_leader_targets`/`udb_units`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-01 | Tampering | getLeaderTargetsByFactionCanonical SQL | mitigate | factionId bound via `$1` positional param (no string interpolation) — ASVS V5 satisfied; verified by the data-layer test seeding/querying the parameterized path. |
| T-140-02 | Information Disclosure | faction-scoped read | accept | Read-only canonical game data (public Wahapedia-derived points/attachment pairs); no PII, no auth/authz surface, local single-user desktop app. |
| T-140-03 | Denial of Service | query result size | accept | Bounded by composite-PK leader-target rows per faction (tens of rows); fetched once at tab level (D-07, no per-row hooks), staleTime Infinity. |

No new write path, no user-controlled interpolation, no auth/authz change. No high/medium threats requiring a dedicated mitigation task — the parameterized bind in Task 2 + the test in Task 1 fully cover T-140-01. No npm/pip/cargo installs in this phase, so the supply-chain (T-*-SC) checkpoint is not applicable.
</threat_model>

<verification>
- `pnpm test -- tests/data-layer/leaderTargetsByFaction.test.ts` — the new faction-scoped query test passes (correct pairs, cross-faction exclusion, unknown-faction empty).
- `pnpm build` — strict TypeScript clean (no stale `SyncedLeaderTargetRow` / `useLeaderTargetsByFaction` imports).
- `pnpm test` — full suite green (ArmyListsPage mock cleaned, no regressions).
- `grep -rn 'getLeaderTargetsByFaction\b\|useLeaderTargetsByFaction\b\|SyncedLeaderTargetRow' src tests` — zero matches.
- Manual (per VALIDATION.md, post-execution): `pnpm tauri dev` → Rules Hub for a faction with leader pairs (e.g. Space Marines) → expand a Character datasheet → the "Leader — Can attach to" section lists real target badges; a non-leader unit shows no section.
</verification>

<success_criteria>
1. DatasheetPointsTab's "Leader — Can attach to" section reads canonical `udb_leader_targets` (faction-scoped join through `udb_units`), not `synced_leader_targets`. (ROADMAP SC-1)
2. `useLeaderTargetsByFaction` / the `bsdataExtended` `synced_leader_targets` reader are removed; no UI surface depends on the unwritten bsdata table. (ROADMAP SC-2)
3. For a faction with canonical leader pairs the query returns real leader→target rows (data-layer test), and the section's empty state appears only when a faction genuinely has none. (ROADMAP SC-3)
</success_criteria>

<output>
Create `.planning/phases/140-close-play-02-03-tail-repoint-rules-hub-datasheetpointstab-f/140-01-SUMMARY.md` when done.
</output>

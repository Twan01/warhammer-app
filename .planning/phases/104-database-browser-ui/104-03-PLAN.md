---
phase: 104-database-browser-ui
plan: 03
type: execute
wave: 2
depends_on:
  - 104-01
files_modified:
  - src/features/unit-database/UdbDatasheetSheet.tsx
  - src/features/unit-database/UdbStatBlock.tsx
  - src/features/unit-database/UdbWeaponsTable.tsx
autonomous: false
requirements:
  - BUI-03

must_haves:
  truths:
    - "Selecting a unit shows a right-side Sheet with the full datasheet"
    - "Stat block shows M, T, SV, W, LD, OC values for each model profile"
    - "Ranged and melee weapons are separated into distinct collapsible tables"
    - "Abilities are grouped by type: Core, Faction, Unit"
    - "Keywords are displayed as inline badges"
    - "Points tiers show all model-count brackets"
    - "Damaged profile shows threshold and description when present"
  artifacts:
    - path: "src/features/unit-database/UdbDatasheetSheet.tsx"
      provides: "Full datasheet Sheet overlay component"
      min_lines: 80
    - path: "src/features/unit-database/UdbStatBlock.tsx"
      provides: "Stat block table for model profiles"
      min_lines: 30
    - path: "src/features/unit-database/UdbWeaponsTable.tsx"
      provides: "Weapon table (ranged or melee) with stat columns"
      min_lines: 40
  key_links:
    - from: "src/features/unit-database/UdbDatasheetSheet.tsx"
      to: "src/hooks/useUnitDatabase.ts"
      via: "useUdbUnitDetail"
      pattern: "import.*useUdbUnitDetail"
    - from: "src/features/unit-database/UdbDatasheetSheet.tsx"
      to: "src/features/unit-database/UdbStatBlock.tsx"
      via: "component import"
      pattern: "import.*UdbStatBlock"
    - from: "src/features/unit-database/UdbDatasheetSheet.tsx"
      to: "src/features/unit-database/UdbWeaponsTable.tsx"
      via: "component import"
      pattern: "import.*UdbWeaponsTable"
---

<objective>
Build the full datasheet detail Sheet: stat block, weapon tables, abilities, keywords, points tiers, and damaged profile.

Purpose: Delivers the rich unit detail view (BUI-03) that opens when a user clicks a unit row. This is the most information-dense component in the phase, rendering the complete datasheet from 6 joined udb_* tables.

Output: 3 component files -- the Sheet wrapper, stat block table, and weapons table. After this plan, the Sheet placeholder in DatabaseBrowserPage is replaced with the real component.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-database-browser-ui/104-CONTEXT.md
@.planning/phases/104-database-browser-ui/104-RESEARCH.md
@.planning/phases/104-database-browser-ui/104-PATTERNS.md
@.planning/phases/104-database-browser-ui/104-UI-SPEC.md
@.planning/phases/104-database-browser-ui/104-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: src/db/queries/unitDatabase.ts -->
export interface UdbUnitDetail {
  unit: { id: string; name: string; role: string | null; base_points: number | null; damaged_w: string | null; damaged_desc: string | null; };
  models: UdbModel[];
  weapons: UdbWeapon[];
  abilities: UdbAbility[];
  keywords: UdbKeyword[];
  points: UdbPointsTier[];
  composition: UdbComposition[];
}
export interface UdbModel { id: number; unit_id: string; line_order: number; name: string | null; M: string | null; T: number | null; Sv: string | null; inv_sv: string | null; W: number | null; Ld: string | null; OC: number | null; }
export interface UdbWeapon { id: number; unit_id: string; weapon_group: number; line_order: number; name: string; category: string | null; range: string | null; attacks: string | null; skill: string | null; strength: string | null; ap: string | null; damage: string | null; keywords: string | null; }
export interface UdbAbility { id: number; unit_id: string; line_order: number; name: string; description: string | null; ability_type: string | null; }
export interface UdbKeyword { unit_id: string; keyword: string; is_faction: number; }
export interface UdbPointsTier { id: number; unit_id: string; model_count: number; points: number; }
export interface UdbComposition { id: number; unit_id: string; min_models: number; max_models: number; notes: string | null; }

<!-- From Plan 01: src/hooks/useUnitDatabase.ts -->
export function useUdbUnitDetail(unitId: string | null): UseQueryResult<UdbUnitDetail | null>

<!-- From PlaybookDatasheet.tsx: Section label class constant -->
const SECTION_LABEL_CLASS = "text-xs font-semibold text-muted-foreground uppercase tracking-wide";

<!-- From PlaybookDatasheet.tsx: WargearTable grid layout -->
grid grid-cols-[1fr_36px_32px_36px_28px_32px_28px] gap-x-1 px-2 py-1 border-b border-border

<!-- UI Spec: Sheet dimensions -->
Sheet side="right", className="w-[480px] sm:w-[600px]"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UdbStatBlock, UdbWeaponsTable, and UdbDatasheetSheet components</name>
  <files>
    src/features/unit-database/UdbStatBlock.tsx,
    src/features/unit-database/UdbWeaponsTable.tsx,
    src/features/unit-database/UdbDatasheetSheet.tsx
  </files>
  <read_first>
    src/features/units/PlaybookDatasheet.tsx,
    src/db/queries/unitDatabase.ts,
    src/hooks/useUnitDatabase.ts,
    src/features/unit-database/DatabaseBrowserPage.tsx
  </read_first>
  <action>
    Create UdbStatBlock component:
    - Props: `models: UdbModel[]`
    - Grid table layout for model profile stats: columns M, T, SV, W, LD, OC (+ Name column for multi-model units)
    - Column headers: `text-[10px] font-semibold text-muted-foreground uppercase tracking-wide text-center`
    - Stat values: `text-xl font-semibold text-center` (Display scale from UI Spec)
    - If inv_sv is present on a model, show it next to SV as "SV++" format (e.g., "3+/4++")
    - If multiple model profiles, show Name column and render one row per profile
    - If single profile, omit the Name column for cleaner display
    - Grid layout: use CSS grid with appropriate column widths similar to WargearTable pattern

    Create UdbWeaponsTable component:
    - Props: `weapons: UdbWeapon[]`, `statLabel: "BS" | "WS"` (determines whether skill column header shows BS or WS)
    - Reusable for both ranged and melee weapons
    - Grid layout following WargearTable pattern from PlaybookDatasheet.tsx: `grid grid-cols-[1fr_36px_32px_36px_28px_32px_28px] gap-x-1`
    - Column headers: Name, Range, A, BS/WS, S, AP, D — using `text-[10px] font-semibold text-muted-foreground uppercase tracking-wide text-center first:text-left`
    - Weapon rows: `px-2 py-1 border-b border-border` for each weapon
    - If a weapon has `keywords` (TEXT field with comma-separated values), show them below the weapon row in `text-xs text-muted-foreground italic`
    - Use case-insensitive comparison for weapon category splitting: `w.category?.toLowerCase() === "ranged"` vs `"melee"` (per Pitfall 5 in RESEARCH.md)

    Create UdbDatasheetSheet component (per D-07, D-08):
    - Props: `unitId: string | null`, `open: boolean`, `onOpenChange: (open: boolean) => void`
    - Uses shadcn `Sheet` with `side="right"` and className `w-[480px] sm:w-[600px]`
    - Calls `useUdbUnitDetail(unitId)` inside the Sheet (enabled only when unitId is non-null)
    - Loading state inside SheetContent: Skeleton blocks (1x h-16 for stat block, 4x h-6 for sections)
    - Content sections in order per UI Spec:
      1. Unit name: `text-xl font-semibold`
      2. Faction name line + role Badge: `text-sm text-muted-foreground` (faction name from detail.unit, role as Badge variant="secondary")
      3. `Separator`
      4. UdbStatBlock with detail.models
      5. Composition: text line showing model count range and all points tiers inline. Format each tier as "{points} pts ({model_count} models)". Show as comma-separated list.
      6. `Separator`
      7. Ranged Weapons Collapsible (default open): filter `detail.weapons` by `w.category?.toLowerCase() === "ranged"`. Render `UdbWeaponsTable` with `statLabel="BS"`. Only show section if ranged weapons exist.
      8. Melee Weapons Collapsible (default open): filter by `"melee"`. `statLabel="WS"`. Only show if melee weapons exist.
      9. `Separator`
      10. Abilities Collapsible (default open): three-way split on `ability_type` — "Core" abilities, "Faction" abilities, everything else as "Unit" abilities (following PlaybookDatasheet.tsx pattern). Each ability: name in `text-sm font-semibold` + description in `text-sm text-muted-foreground leading-relaxed`, wrapped in `flex flex-col gap-1 pl-2 border-l border-border` (AbilityEntry pattern).
      11. Keywords: inline Badge list with `text-xs`. Split into Faction Keywords (where `is_faction === 1`) shown first with a subtle label, then regular keywords.
      12. Damaged Profile: only if `detail.unit.damaged_w` is non-null. Show "Damaged: {damaged_w}" as threshold label, then `damaged_desc` as ability description text.
    - All Collapsibles use `ChevronDown` icon with `data-[state=open]:rotate-180` transition on the trigger
    - Use the SECTION_LABEL_CLASS constant: `text-xs font-semibold text-muted-foreground uppercase tracking-wide`
    - SheetContent should have `overflow-y: auto` for long datasheets

    After creating the Sheet, wire it into DatabaseBrowserPage.tsx: replace the placeholder `{selectedUnitId && <div>Sheet placeholder</div>}` with `<UdbDatasheetSheet unitId={selectedUnitId} open={!!selectedUnitId} onOpenChange={(open) => { if (!open) setSelectedUnitId(null); }} />`. Import UdbDatasheetSheet.
  </action>
  <acceptance_criteria>
    - UdbStatBlock renders M, T, SV, W, LD, OC columns for each model profile
    - UdbStatBlock shows inv_sv next to SV when present
    - UdbWeaponsTable accepts statLabel prop and renders BS or WS column header accordingly
    - UdbWeaponsTable grid uses grid-cols matching the WargearTable pattern from PlaybookDatasheet.tsx
    - UdbWeaponsTable shows weapon keywords below each weapon row when present
    - UdbDatasheetSheet uses Sheet side="right" with w-[480px] sm:w-[600px]
    - useUdbUnitDetail is called with enabled: !!unitId inside the Sheet
    - Weapon split uses case-insensitive comparison (toLowerCase)
    - Abilities are split into Core/Faction/Unit groups with individual ability entries using border-l border-border
    - Keywords section renders Faction Keywords separately from regular keywords
    - Damaged profile section renders only when damaged_w is non-null
    - All Collapsibles default open and use ChevronDown with rotate-180 transition
    - DatabaseBrowserPage.tsx imports and renders UdbDatasheetSheet with open/onOpenChange props
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build</automated>
  </verify>
  <done>Full datasheet Sheet renders stat block, ranged + melee weapon tables, grouped abilities, keyword badges, points tiers, and damaged profile. Sheet is wired into DatabaseBrowserPage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Unit Database browser with faction picker (alignment grouped), virtualized unit list with role sections, FTS5 search, role/keyword/point filters, and full datasheet detail Sheet.
  </what-built>
  <how-to-verify>
    1. Run `pnpm tauri dev`
    2. Click "Unit Database" in the sidebar (Play group, after Rules Hub) -- page loads with faction picker on left
    3. Verify factions are grouped: Space Marines, Imperium, Chaos, Xenos
    4. Click "Space Marines" -- unit list appears grouped by role (CHARACTER, BATTLELINE, etc.) with collapsible headers showing counts
    5. Verify unit rows show name, role badge, and "from N pts"
    6. Scroll through a large faction (e.g., Space Marines) -- confirm smooth virtual scrolling without jank
    7. Click any unit row -- Sheet opens from right showing stat block, weapons tables, abilities, keywords
    8. Verify the stat block shows M/T/SV/W/LD/OC values
    9. Verify weapons are split into Ranged and Melee collapsible sections
    10. Verify abilities show Core/Faction/Unit grouping with descriptions
    11. Type "intercessor" in the search bar -- FTS5 results appear replacing the faction browser
    12. Click a search result -- Sheet opens with that unit's datasheet
    13. Clear the search -- faction browser returns
    14. With a faction selected, use the Role filter to filter to CHARACTER only -- list updates
    15. Set a point range (e.g., min 100) -- filtered results update
    16. Click "Clear" to reset filters
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the browsing experience</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB data -> Sheet rendering | Untrusted text from DB rendered in React (auto-escaped by JSX) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-05 | Information Disclosure | UdbDatasheetSheet renders all DB text | accept | All data is user-imported unit database (no secrets); React JSX auto-escapes HTML |
| T-104-06 | Tampering | Weapon category comparison | mitigate | Use case-insensitive toLowerCase comparison for "ranged"/"melee" split per RESEARCH.md Pitfall 5 |
| T-104-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
1. `pnpm build` passes
2. Datasheet Sheet opens and shows all 6 data sections
3. Weapon tables correctly split ranged vs melee
4. Abilities correctly group by type
5. Damaged profile appears only for units that have it
6. Human verification confirms browsing experience works end-to-end
</verification>

<success_criteria>
- All 3 component files exist with correct rendering
- Sheet wired into DatabaseBrowserPage
- Full datasheet renders: stat block, weapons, abilities, keywords, points, damaged profile
- Human approves the browsing experience
</success_criteria>

<output>
Create `.planning/phases/104-database-browser-ui/104-03-SUMMARY.md` when done
</output>

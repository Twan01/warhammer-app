---
phase: 104-database-browser-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 104-01
files_modified:
  - src/features/unit-database/DatabaseBrowserPage.tsx
  - src/features/unit-database/FactionPicker.tsx
  - src/features/unit-database/UdbUnitList.tsx
  - src/features/unit-database/UdbUnitRow.tsx
  - src/features/unit-database/UdbSearchResults.tsx
  - src/features/unit-database/DatabaseBrowserFilters.tsx
autonomous: true
requirements:
  - BUI-01
  - BUI-02
  - BUI-04
  - BUI-05
  - BUI-06

must_haves:
  truths:
    - "User sees factions grouped under Imperium / Space Marines / Chaos / Xenos alignment headers"
    - "Clicking a faction shows its units grouped by role categories with collapsible sections"
    - "Each unit row shows name, 'from N pts', and model count range"
    - "Typing in the search bar shows FTS5 results as a flat list replacing the faction browser"
    - "Clearing search restores the faction browser view"
    - "User can filter by role, keyword, and point range independently or combined"
    - "Unit lists with 100+ entries scroll smoothly via virtual scrolling"
  artifacts:
    - path: "src/features/unit-database/DatabaseBrowserPage.tsx"
      provides: "Top-level page orchestrating search, faction picker, unit list, and sheet"
      min_lines: 50
    - path: "src/features/unit-database/FactionPicker.tsx"
      provides: "Alignment-grouped faction list with active selection"
      min_lines: 40
    - path: "src/features/unit-database/UdbUnitList.tsx"
      provides: "Virtualized role-grouped unit list"
      min_lines: 60
    - path: "src/features/unit-database/UdbUnitRow.tsx"
      provides: "Single unit row (name, pts, model count)"
      min_lines: 15
    - path: "src/features/unit-database/UdbSearchResults.tsx"
      provides: "Flat FTS5 search results list"
      min_lines: 30
    - path: "src/features/unit-database/DatabaseBrowserFilters.tsx"
      provides: "Role/keyword/point range filter bar"
      min_lines: 40
  key_links:
    - from: "src/features/unit-database/DatabaseBrowserPage.tsx"
      to: "src/hooks/useUnitDatabase.ts"
      via: "useUdbFactions, useUdbUnits, useUdbSearch"
      pattern: "import.*from.*hooks/useUnitDatabase"
    - from: "src/features/unit-database/DatabaseBrowserPage.tsx"
      to: "src/features/unit-database/databaseBrowserFilters.ts"
      via: "useDatabaseBrowserFilters"
      pattern: "import.*useDatabaseBrowserFilters"
    - from: "src/features/unit-database/UdbUnitList.tsx"
      to: "@tanstack/react-virtual"
      via: "useVirtualizer"
      pattern: "import.*useVirtualizer.*react-virtual"
---

<objective>
Build the complete browsing UI: faction picker with alignment groups, virtualized unit list with role sections, search results, and filter bar.

Purpose: Delivers the core browsing experience where users can select a faction, see units by role, search across all factions, and filter by role/keyword/point range. This is the interactive front-end for all udb_* data hooks built in Plan 01.

Output: 6 component files comprising the full database browser interaction layer (excluding the datasheet detail Sheet, which is Plan 03).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-database-browser-ui/104-CONTEXT.md
@.planning/phases/104-database-browser-ui/104-RESEARCH.md
@.planning/phases/104-database-browser-ui/104-PATTERNS.md
@.planning/phases/104-database-browser-ui/104-UI-SPEC.md
@.planning/phases/104-database-browser-ui/104-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: src/db/queries/unitDatabase.ts -->
export interface UdbFaction { id: string; name: string; short_name: string | null; }
export interface UdbUnitSummary { id: string; name: string; role: string | null; base_points: number | null; min_models: number | null; max_models: number | null; }
export interface UdbSearchResult { unit_id: string; name: string; faction_name: string; keywords: string; }

<!-- From Plan 01: src/hooks/useUnitDatabase.ts -->
export function useUdbFactions(): UseQueryResult<UdbFaction[]>
export function useUdbUnits(factionId: string | null): UseQueryResult<UdbUnitSummary[]>
export function useUdbSearch(query: string): UseQueryResult<UdbSearchResult[]>

<!-- From Plan 01: src/features/unit-database/databaseBrowserFilters.ts -->
export const useDatabaseBrowserFilters: UseBoundStore<...>
// selectedFactionId, searchText, roleFilter, keywordFilter, pointMin, pointMax + setters + clearFilters

<!-- From Plan 01: src/features/unit-database/applyUdbFilters.ts -->
export function applyUdbFilters(units: UdbUnitSummary[], filters: UdbFiltersInput): UdbUnitSummary[]

<!-- From Plan 01: src/features/unit-database/factionAlignmentMap.ts -->
export const FACTION_ALIGNMENT: Record<string, Alignment>
export const ALIGNMENT_ORDER: readonly ["Space Marines", "Imperium", "Chaos", "Xenos"]
export type Alignment = "Space Marines" | "Imperium" | "Chaos" | "Xenos"

<!-- UI Spec: Layout dimensions -->
Faction picker: w-60 (240px), border-r border-border, overflow-y-auto
Unit row: h-10 (40px fixed for virtual scroll), flex items-center gap-3 px-4
Role section header: text-xs font-semibold uppercase tracking-wide text-muted-foreground
Alignment group header: text-xs font-semibold uppercase tracking-wide text-muted-foreground px-3 py-2
Search bar: full-width Input, debounced 300ms
Virtual scroll: useVirtualizer with estimateSize: () => 40, overscan: 5
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DatabaseBrowserPage, FactionPicker, and DatabaseBrowserFilters components</name>
  <files>
    src/features/unit-database/DatabaseBrowserPage.tsx,
    src/features/unit-database/FactionPicker.tsx,
    src/features/unit-database/DatabaseBrowserFilters.tsx
  </files>
  <read_first>
    src/features/rules-hub/RulesHubPage.tsx,
    src/features/unit-database/databaseBrowserFilters.ts,
    src/features/unit-database/factionAlignmentMap.ts,
    src/hooks/useUnitDatabase.ts,
    src/features/unit-database/applyUdbFilters.ts,
    src/features/unit-database/DatabaseBrowserPage.tsx
  </read_first>
  <action>
    Replace the placeholder `DatabaseBrowserPage.tsx` with the full page component (per D-03, D-09, D-10).

    DatabaseBrowserPage layout:
    - Outer container: `flex flex-col gap-6 p-6`
    - Page heading: `text-3xl font-semibold tracking-tight` with text "Unit Database"
    - Global search bar: full-width `Input` with `Search` icon (Lucide), placeholder "Search units across all factions...", value bound to `searchText` from Zustand store via `useDatabaseBrowserFilters`. Debounce 300ms before passing to `useUdbSearch`. Use a local `debouncedSearch` state with `useEffect` + `setTimeout`/`clearTimeout` pattern.
    - Conditional rendering per D-10: when `searchText` is non-empty (after debounce), hide the two-panel layout and show `UdbSearchResults`. When empty, show the two-panel layout.
    - Two-panel layout: `flex gap-0` container. Left panel: `FactionPicker` (w-60, border-r). Right panel: `flex-1 overflow-hidden flex flex-col` containing `DatabaseBrowserFilters` then `UdbUnitList` (or placeholder empty state "Select a faction to browse units." when no faction selected).
    - Wire `useMemo` to apply `applyUdbFilters` on the units data from `useUdbUnits(selectedFactionId)`.
    - Track `selectedUnitId: string | null` via local useState for opening the datasheet Sheet. Pass `selectedUnitId` and `setSelectedUnitId` down. The actual Sheet component comes from Plan 03 -- for now, render a placeholder `{selectedUnitId && <div>Sheet placeholder for {selectedUnitId}</div>}` that Plan 03 will replace.

    FactionPicker component (per D-01, D-04):
    - Props: `factions: UdbFaction[]`, `selectedFactionId: string | null`, `onSelectFaction: (id: string) => void`, `isLoading: boolean`
    - Container: `w-60 border-r border-border overflow-y-auto flex flex-col` with height `calc(100vh - offset)` or a suitable CSS approach that fills available vertical space
    - Group factions by alignment using `FACTION_ALIGNMENT` and `ALIGNMENT_ORDER`. Use `useMemo` to compute `Record<Alignment, UdbFaction[]>` grouped map. Factions not in the alignment map go under a trailing "Other" group (defensive).
    - For each alignment group: render a header `text-xs font-semibold uppercase tracking-wide text-muted-foreground px-3 py-2`, then faction buttons.
    - Each faction button: `px-3 py-2 text-sm cursor-pointer hover:bg-accent rounded-sm flex justify-between items-center w-full text-left`. Active state: add `border-l-2 border-primary bg-secondary` when `faction.id === selectedFactionId`. Show faction name on the left.
    - Loading state: 6 `Skeleton` rows at `h-8 w-full rounded` each.

    DatabaseBrowserFilters component (per D-05, D-11, D-12):
    - Reads roleFilter, keywordFilter, pointMin, pointMax and their setters from `useDatabaseBrowserFilters`.
    - Horizontal bar: `flex flex-wrap items-center gap-3 px-4 py-3`
    - Role filter: shadcn `Select` with placeholder "Role", options from the 9 GW role categories as a const array. Clear option to reset. Values: Character, Battleline, Dedicated Transport, Fortification, etc.
    - Keyword filter: `Input` with `Tag` icon, placeholder "Keyword...", bound to `keywordFilter`
    - Point range: two small `Input` type="number" with `Coins` icon, placeholders "Min pts" and "Max pts", bound to `pointMin`/`pointMax`
    - Clear all button: only shown when any filter is active, calls `clearFilters()`
  </action>
  <acceptance_criteria>
    - DatabaseBrowserPage renders page heading "Unit Database" with class text-3xl font-semibold tracking-tight
    - Search Input has placeholder "Search units across all factions..."
    - When searchText is non-empty, UdbSearchResults is rendered and two-panel layout is hidden
    - When searchText is empty, two-panel layout is rendered
    - FactionPicker groups factions under 4 alignment headers in order: Space Marines, Imperium, Chaos, Xenos
    - Selected faction shows border-l-2 and bg-secondary
    - FactionPicker shows Skeleton loading state when isLoading is true
    - DatabaseBrowserFilters renders role Select, keyword Input, point min/max Inputs
    - Clear filters button only visible when at least one filter is active
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build</automated>
  </verify>
  <done>Page shell orchestrates search/browse modes, faction picker groups by alignment, filter bar provides role/keyword/point controls</done>
</task>

<task type="auto">
  <name>Task 2: UdbUnitList with virtual scrolling, UdbUnitRow, and UdbSearchResults</name>
  <files>
    src/features/unit-database/UdbUnitList.tsx,
    src/features/unit-database/UdbUnitRow.tsx,
    src/features/unit-database/UdbSearchResults.tsx
  </files>
  <read_first>
    src/features/unit-database/DatabaseBrowserPage.tsx,
    src/features/units/PlaybookDatasheet.tsx,
    src/features/unit-database/applyUdbFilters.ts,
    src/db/queries/unitDatabase.ts
  </read_first>
  <action>
    Create UdbUnitList component (per D-05, D-06, BUI-02, BUI-06):
    - Props: `units: UdbUnitSummary[]`, `isLoading: boolean`, `onOpenUnit: (id: string) => void`
    - Implement the flattened virtual list pattern (RESEARCH.md anti-pattern avoidance): compute a flat array with discriminated union items: `{ kind: "header"; role: string; count: number }` and `{ kind: "unit"; unit: UdbUnitSummary }`. Use `useMemo` to group units by `unit.role` (null role goes to "Uncategorized"), then interleave header items before each group.
    - One `useVirtualizer` from `@tanstack/react-virtual` over the entire flattened array. Config: `count: flatItems.length`, `getScrollElement: () => parentRef.current`, `estimateSize: (index) => flatItems[index].kind === "header" ? 36 : 40` (36px for headers, 40px for unit rows), `overscan: 5`.
    - Parent scroll container: ref-based div with `overflow-y: auto` and height computed via CSS `h-[calc(100vh-220px)]` (adjust for header + filter bar height). Position relative wrapper with height from `virtualizer.getTotalSize()`.
    - For "header" items: render role section header with `text-xs font-semibold uppercase tracking-wide text-muted-foreground px-4 py-2 bg-background sticky` showing "ROLE_NAME (count)".
    - For "unit" items: render `UdbUnitRow`.
    - Loading state: 8 `Skeleton` rows at h-10.
    - Empty state (filtered to 0 results): "No units match your filters." in `text-sm text-muted-foreground italic`.

    Create UdbUnitRow component:
    - Props: `unit: UdbUnitSummary`, `onOpen: (id: string) => void`
    - Layout: `flex items-center gap-3 px-4 h-10 hover:bg-secondary cursor-pointer transition-colors` with onClick calling `onOpen(unit.id)`
    - Content: unit name (`text-sm font-medium flex-1 truncate`), then role `Badge` (variant="secondary", `text-xs`), then points display (`text-xs text-muted-foreground tabular-nums w-20 text-right`) showing "from {base_points} pts" or dash if null.

    Create UdbSearchResults component (per D-09, D-10):
    - Props: `query: string`, `onSelectResult: (unitId: string, factionId: string) => void`
    - Uses `useUdbSearch(query)` hook internally.
    - Loading state: 5 `Skeleton` rows at h-10.
    - Empty state: centered text "No units found" heading + "No units match \"{query}\". Try a different name, keyword, or ability." body.
    - Results list: each result row shows faction name (`text-xs text-muted-foreground`), unit name (`text-sm font-medium`), keywords snippet. Clicking calls `onSelectResult(result.unit_id, ...)` -- note: UdbSearchResult does not contain faction_id directly, so the onSelectResult callback receives unit_id and the parent DatabaseBrowserPage can use it to set selectedUnitId and open the Sheet directly (without needing to navigate to a faction first).
  </action>
  <acceptance_criteria>
    - UdbUnitList uses exactly ONE useVirtualizer call (not per-section)
    - The flattened array contains interleaved header and unit items using a kind discriminant
    - estimateSize returns 36 for header items and 40 for unit items
    - overscan is set to 5
    - Scroll container has overflow-y: auto with a calculated height
    - UdbUnitRow renders unit name, role Badge, and points display "from N pts"
    - UdbUnitRow has h-10 fixed height (40px for virtual scroll accuracy)
    - UdbSearchResults shows loading skeletons while useUdbSearch is fetching
    - UdbSearchResults shows "No units found" empty state when results are empty and query is non-empty
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build</automated>
  </verify>
  <done>Virtual-scrolled unit list renders role-grouped units with smooth scrolling, unit rows show name/pts/role, search results display FTS5 matches as a flat list</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> search debounce | Search text flows through debounce to FTS5 (sanitized in Plan 01 query layer) |
| User input -> filter state | Filter values are client-side only, applied via pure function |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-03 | Denial of Service | Virtual scroll with large dataset | mitigate | useVirtualizer limits rendered DOM nodes to viewport + overscan (5); estimateSize provides accurate heights |
| T-104-04 | Tampering | FTS5 query via search bar | accept | Sanitization handled in Plan 01 query layer (searchUdbUnits strips operator chars) |
| T-104-SC | Tampering | npm installs | accept | No new packages in this plan; @tanstack/react-virtual installed in Plan 01 |
</threat_model>

<verification>
1. `pnpm build` passes
2. In dev mode: select a faction, see units grouped by role with virtual scrolling
3. Type in search bar, see FTS5 results replace the faction browser
4. Clear search, faction browser returns
5. Apply role/keyword/point filters, list updates accordingly
</verification>

<success_criteria>
- All 6 component files exist and render correctly
- Virtual scroll works on faction with 100+ units
- Search debounce fires FTS5 query after 300ms
- Filters apply AND logic
- TypeScript compilation passes
</success_criteria>

<output>
Create `.planning/phases/104-database-browser-ui/104-02-SUMMARY.md` when done
</output>

---
phase: 142-technique-authoring-library-browse
plan: 04
type: execute
wave: 4
depends_on: ["142-03"]
files_modified:
  - src/features/techniques/applyTechniqueFilters.ts
  - src/features/techniques/TechniqueCard.tsx
  - src/features/techniques/TechniqueCardGrid.tsx
  - src/features/techniques/TechniqueEmptyState.tsx
  - src/features/techniques/TechniqueDeleteDialog.tsx
  - src/features/techniques/TechniqueDetailSheet.tsx
  - src/features/techniques/TechniqueLibraryTab.tsx
  - src/features/recipes/RecipesPage.tsx
  - tests/techniques/TechniqueCard.test.tsx
  - tests/techniques/TechniqueDetailSheet.test.tsx
  - tests/techniques/TechniqueDeleteDialog.test.tsx
  - tests/techniques/TechniqueLibraryTab.test.tsx
autonomous: true
requirements: [TECH-03, TECH-05, LIB-01, LIB-02, LIB-03, LIB-04]
must_haves:
  truths:
    - "A Techniques tab appears on the /recipes page (no new route, no new sidebar entry)"
    - "The tab lists techniques as cards with name, effect, difficulty, usage count, slot count, step count"
    - "Name search + effect filter narrow the card list"
    - "Card click opens a detail Sheet showing the section/step tree, colour slots, and a used-by-recipes list (0 → 'Not used by any recipes yet.')"
    - "Delete shows a usage-count warning dialog; edit/duplicate are reachable from card and detail"
  artifacts:
    - path: "src/features/techniques/applyTechniqueFilters.ts"
      provides: "Pure name + effect filter"
      contains: "applyTechniqueFilters"
    - path: "src/features/techniques/TechniqueCard.tsx"
      provides: "Library card with counts + edit/duplicate/delete actions"
      min_lines: 40
    - path: "src/features/techniques/TechniqueCardGrid.tsx"
      provides: "Grid + skeleton + empty states"
    - path: "src/features/techniques/TechniqueDetailSheet.tsx"
      provides: "Detail Sheet: slots, step tree, used-by list"
      contains: "Not used by any recipes yet"
    - path: "src/features/techniques/TechniqueDeleteDialog.tsx"
      provides: "Delete confirm with usage count"
      contains: "usageCount"
    - path: "src/features/techniques/TechniqueLibraryTab.tsx"
      provides: "Filter bar + grid + sheets/dialogs wiring"
    - path: "src/features/recipes/RecipesPage.tsx"
      provides: "Tabs: Recipes | Techniques"
      contains: "TechniqueLibraryTab"
  key_links:
    - from: "src/features/recipes/RecipesPage.tsx"
      to: "src/features/techniques/TechniqueLibraryTab.tsx"
      via: "TabsContent value=techniques"
      pattern: "TechniqueLibraryTab"
    - from: "src/features/techniques/TechniqueLibraryTab.tsx"
      to: "src/hooks/useTechniques.ts"
      via: "useTechniquesWithCounts"
      pattern: "useTechniquesWithCounts"
    - from: "src/features/techniques/TechniqueDetailSheet.tsx"
      to: "getTechniqueUsedByRecipes"
      via: "useTechniqueUsedByRecipes"
      pattern: "useTechniqueUsedByRecipes|UsedByRecipes"
---

<objective>
Build the technique library browse surface and wire it as a second tab on the existing /recipes page. Deliver the card grid, detail Sheet, delete dialog, filters, and empty state, then add the shadcn Tabs control to `RecipesPage`. Write the four RTL test files that close the remaining Nyquist gaps.

Purpose: This completes the user-facing phase outcome (LIB-01..04, TECH-03, TECH-05). The library lives under Workshop/Recipes with zero new sidebar entry or route — a hard milestone non-negotiable. Usage counts read 0 this phase (no instances until Phase 143) but the plumbing must be correct now.

Output: seven new components + RecipesPage modification + four RTL tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/142-technique-authoring-library-browse/142-PATTERNS.md
@.planning/phases/142-technique-authoring-library-browse/142-UI-SPEC.md
@.planning/phases/142-technique-authoring-library-browse/142-03-SUMMARY.md

<interfaces>
<!-- From plans 01-03 — import directly -->
@/types/technique: Technique
@/db/queries/techniques: TechniqueWithCounts (returned by getTechniquesWithCounts)
@/hooks/useTechniques: useTechniquesWithCounts, useTechnique, useTechniqueUsedByRecipes, useDeleteTechnique, useDuplicateTechnique
@/hooks/useTechniqueSections: useTechniqueSections, useTechniqueSteps
@/hooks/useTechniqueColourSlots: useTechniqueColourSlots
@/features/techniques/TechniqueFormSheet: TechniqueFormSheet
@/features/techniques/techniqueSchema: RECIPE_EFFECTS

Analog source files (adapt):
- src/features/recipes/RecipeCard.tsx (difficultyColors lines 10-15, card lines 104-213)
- src/features/recipes/RecipeCardGrid.tsx (skeleton lines 47-67, grid lines 73-94)
- src/features/recipes/RecipeDetailSheet.tsx (Sheet lines 188-236, Field helper lines 387-396, footer lines 351-371)
- src/features/recipes/RecipeDeleteDialog.tsx (full file lines 1-61)
- src/features/recipes/RecipesPage.tsx (filter state lines 44-76, toolbar) — MODIFY this file
- src/features/recipes/applyRecipeFilters.ts (lines 17-52)
- src/components/ui/tabs.tsx (CONFIRMED present)
</interfaces>

<ui_contract>
From 142-UI-SPEC.md:
- Tabs default "recipes"; wrap existing RecipesPage body in TabsContent value="recipes"; add TabsContent value="techniques" → TechniqueLibraryTab. PageHeader title/subtitle unchanged. Tab state local useState; each tab keeps independent filter state.
- TechniqueCard: Card `cursor-pointer hover:shadow-md transition-shadow gap-3`; name text-sm font-semibold leading-tight; slot count + step count rows (Layers icon, "{N} slot(s)"/"{N} step(s)"); effect + difficulty badges (difficultyColors map); usage count line always shown ("Not used yet" / "Used by {N} recipe(s)"); action row Edit/Duplicate/Delete ghost icon buttons (h-7 w-7) with the exact aria-labels and e.stopPropagation on the row. NO swatch strip, NO availability badge, NO faction badge.
- TechniqueCardGrid: grid `repeat(auto-fill, minmax(280px,1fr))`; 6 skeleton cards `rounded-xl border bg-card p-6 flex flex-col gap-3`; TechniqueEmptyState when no filter active; inline "No techniques match your filters." when filtered + empty.
- TechniqueDetailSheet: SheetContent side right `overflow-y-auto sm:max-w-md`; title = name; effect/difficulty badges; Field helper sections — Colour Slots (placeholder swatch circle + name + italic role hint), Technique Steps (section/step tree), Used by (recipe names or "Not used by any recipes yet."); footer Delete (ghost text-destructive) + Duplicate Technique (outline + Copy) + Edit Technique. OMIT faction/unit/sessions/assignments/wishlist/apply/start-painting.
- TechniqueDeleteDialog: title "Delete technique?"; description varies by usageCount (0 vs N — exact copy in contract); footer "Keep Technique" (outline) + Delete (destructive, "Deleting…" while pending).
- Copywriting + empty-state copy: 142-UI-SPEC Copywriting Contract is authoritative.
</ui_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: applyTechniqueFilters + TechniqueCard + TechniqueCardGrid + EmptyState</name>
  <files>src/features/techniques/applyTechniqueFilters.ts, src/features/techniques/TechniqueCard.tsx, src/features/techniques/TechniqueCardGrid.tsx, src/features/techniques/TechniqueEmptyState.tsx, tests/techniques/TechniqueCard.test.tsx</files>
  <read_first>
    - src/features/recipes/applyRecipeFilters.ts (lines 17-52)
    - src/features/recipes/RecipeCard.tsx (difficultyColors, card structure)
    - src/features/recipes/RecipeCardGrid.tsx (skeleton + grid)
    - src/db/queries/techniques.ts (TechniqueWithCounts shape)
    - .planning/phases/142-technique-authoring-library-browse/142-PATTERNS.md (TechniqueCard lines 410-479, TechniqueCardGrid lines 483-525, applyTechniqueFilters lines 706-727)
    - .planning/phases/142-technique-authoring-library-browse/142-UI-SPEC.md (TechniqueCard + grid specs)
  </read_first>
  <behavior>
    - Test (plan 01 applyTechniqueFilters.test): name filter narrows by case-insensitive substring; effect filter narrows by exact match; empty filters return all
    - Test (TechniqueCard.test): renders name, "Used by N recipes"/"Not used yet", slot count, step count, difficulty badge
  </behavior>
  <action>
    `applyTechniqueFilters.ts`: export `TechniqueFilterState { nameFilter: string; effectFilter: string | null }` and `applyTechniqueFilters(techniques: TechniqueWithCounts[], filters): TechniqueWithCounts[]` filtering on technique.name (case-insensitive contains when nameFilter non-empty) and technique.effect (exact match when effectFilter !== null). This closes the plan-01 RED filter test.
    `TechniqueCard.tsx`: props technique (TechniqueWithCounts), onClick, onEdit, onDelete, onDuplicate. Copy difficultyColors map verbatim. Card `cursor-pointer hover:shadow-md transition-shadow gap-3` with aria-label `View ${technique.name}`; header name; effect badge (outline) + difficulty badge (with difficultyColors tint); slot count + step count rows (Layers icon); usage count line ("Not used yet" when 0, else "Used by {N} recipe(s)"); action row `flex items-center gap-1 pt-1 border-t border-border/50` with onClick stopPropagation, three ghost h-7 w-7 icon buttons Edit (Pencil, aria-label="Edit technique"), Duplicate (Copy, aria-label="Duplicate technique"), Delete (Trash2 text-destructive, aria-label="Delete technique"). NO swatch/availability/faction.
    `TechniqueCardGrid.tsx`: props data, isLoading, isFiltered, onCardClick, onEdit, onDelete, onDuplicate. Loading → 6 skeleton cards (`rounded-xl border bg-card p-6 flex flex-col gap-3`, 4 Skeleton lines). Empty + not filtered → TechniqueEmptyState (with onAdd CTA). Empty + filtered → inline `<p className="text-sm text-muted-foreground col-span-full">No techniques match your filters.</p>`. Else grid `repeat(auto-fill, minmax(280px,1fr))` of TechniqueCard.
    `TechniqueEmptyState.tsx`: heading "No techniques yet", body per copy contract, CTA Button "Add Technique" calling onAdd.
    `tests/techniques/TechniqueCard.test.tsx` (jsdom): render TechniqueCard with a stub TechniqueWithCounts (usage_count 0 and a non-zero case), assert name, usage copy, slot/step counts, and difficulty badge text appear (LIB-02). Mock nothing beyond what the card imports.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/techniques/TechniqueCard.test.tsx tests/features/techniques/applyTechniqueFilters.test.ts 2>&1 | grep -v '^#' | grep -E "passed|failed|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - applyTechniqueFilters filters by name (case-insensitive contains) and effect (exact); the plan-01 applyTechniqueFilters test is GREEN
    - TechniqueCard renders counts + usage line + difficulty badge; no swatch/availability/faction
    - TechniqueCard.test.tsx is GREEN with assertions for name, usage copy, slot count, step count
    - Grid shows skeleton/empty/filtered-empty/grid states correctly
  </acceptance_criteria>
  <done>Filter function, card, grid, and empty state are built and tested (LIB-02, LIB-03 filter half).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TechniqueDetailSheet + TechniqueDeleteDialog + their tests</name>
  <files>src/features/techniques/TechniqueDetailSheet.tsx, src/features/techniques/TechniqueDeleteDialog.tsx, tests/techniques/TechniqueDetailSheet.test.tsx, tests/techniques/TechniqueDeleteDialog.test.tsx</files>
  <read_first>
    - src/features/recipes/RecipeDetailSheet.tsx (Sheet lines 188-236, Field helper lines 387-396, footer lines 351-371, step tree logic ~280-290)
    - src/features/recipes/RecipeDeleteDialog.tsx (full file lines 1-61)
    - src/hooks/useTechniques.ts (useTechniqueUsedByRecipes, useDeleteTechnique, useDuplicateTechnique)
    - src/hooks/useTechniqueSections.ts, useTechniqueColourSlots.ts
    - .planning/phases/142-technique-authoring-library-browse/142-PATTERNS.md (TechniqueDetailSheet lines 527-597, TechniqueDeleteDialog lines 600-652)
    - .planning/phases/142-technique-authoring-library-browse/142-UI-SPEC.md (detail Sheet + delete dialog specs + Copywriting Contract)
  </read_first>
  <behavior>
    - Test (TechniqueDetailSheet.test): with usage count 0, renders "Not used by any recipes yet." (LIB-04); renders the section/step tree (TECH-05)
    - Test (TechniqueDeleteDialog.test): description includes the usage count when N > 0 and the 0-recipe copy when 0 (TECH-03)
  </behavior>
  <action>
    `TechniqueDetailSheet.tsx`: props open, technique (Technique | null), onClose, onEdit, onDelete. SheetContent side right `overflow-y-auto sm:max-w-md`, `key={technique?.id ?? "none"}`. Header title = name; effect/difficulty badges row. Load sections/steps/slots via the hooks and used-by via useTechniqueUsedByRecipes(technique?.id). Body uses the Field helper (copy verbatim): Colour Slots field (each slot: placeholder swatch circle `inline-block h-3 w-3 rounded-full border border-border bg-muted` + name + italic role_hint), Technique Steps field (render the section/step tree — reuse the same SectionedTimeline/flat logic as RecipeDetailSheet; if SectionedTimeline needs recipe-specific props, render a simple ordered section→step list instead, showing step_name + painting_phase/tool/dilution/time), Used by field (recipe names list; when empty "Not used by any recipes yet."). Footer: Delete Technique (ghost text-destructive) + Duplicate Technique (outline + Copy, calls useDuplicateTechnique with newName "Copy of {name}", toasts) + Edit Technique. OMIT faction/unit/sessions/assignments/wishlist/apply/start-painting.
    `TechniqueDeleteDialog.tsx`: adapt RecipeDeleteDialog. Props open, technique, usageCount, onClose. Title "Delete technique?"; DialogDescription branches on usageCount (0 → permanent-remove copy; N → "{name}" is used by {N} recipe(s)... copy). Footer "Keep Technique" (outline) + Delete (destructive, "Deleting…" while useDeleteTechnique.isPending); on confirm mutateAsync(technique.id), toast success/error, onClose.
    `tests/techniques/TechniqueDetailSheet.test.tsx` (jsdom): vi.mock the technique hooks to return empty slots/sections/steps and zero used-by; render with a stub technique; assert "Not used by any recipes yet." appears (LIB-04) and a seeded step name appears when sections/steps are mocked (TECH-05).
    `tests/techniques/TechniqueDeleteDialog.test.tsx` (jsdom): render with usageCount=2 and assert the description mentions "2 recipes"; render with usageCount=0 and assert the permanent-remove copy (TECH-03). vi.mock useDeleteTechnique.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/techniques/TechniqueDetailSheet.test.tsx tests/techniques/TechniqueDeleteDialog.test.tsx 2>&1 | grep -v '^#' | grep -E "passed|failed|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - TechniqueDetailSheet renders Colour Slots, Technique Steps tree, and Used by sections; shows "Not used by any recipes yet." when count is 0
    - TechniqueDeleteDialog description varies correctly by usageCount
    - Both RTL tests GREEN (TECH-05/LIB-04 and TECH-03)
  </acceptance_criteria>
  <done>Detail Sheet and delete dialog are built and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TechniqueLibraryTab + RecipesPage Tabs integration + tab test</name>
  <files>src/features/techniques/TechniqueLibraryTab.tsx, src/features/recipes/RecipesPage.tsx, tests/techniques/TechniqueLibraryTab.test.tsx</files>
  <read_first>
    - src/features/recipes/RecipesPage.tsx (full file — filter state lines 44-76, toolbar, body to wrap in TabsContent)
    - src/components/ui/tabs.tsx (Tabs/TabsList/TabsTrigger/TabsContent API)
    - src/features/techniques/TechniqueCardGrid.tsx, TechniqueDetailSheet.tsx, TechniqueDeleteDialog.tsx, TechniqueFormSheet.tsx (Tasks 1-2 + plan 03)
    - src/features/techniques/applyTechniqueFilters.ts (Task 1)
    - src/hooks/useTechniques.ts (useTechniquesWithCounts)
    - .planning/phases/142-technique-authoring-library-browse/142-PATTERNS.md (TechniqueLibraryTab lines 655-702, RecipesPage modify lines 1005-1034)
    - .planning/phases/142-technique-authoring-library-browse/142-UI-SPEC.md (Tabs + filter bar specs + Interaction Contracts)
  </read_first>
  <behavior>
    - Test (TechniqueLibraryTab.test): both "Recipes" and "Techniques" tab triggers render on the page; the techniques tab shows technique cards; name filter narrows the visible cards; effect filter narrows them (LIB-01, LIB-03)
  </behavior>
  <action>
    `TechniqueLibraryTab.tsx`: self-contained component, no props. Local state: nameFilter (string), effectFilter (string | null), and Sheet/dialog state (selected technique + detailOpen, editing + formOpen, deleting + deleteOpen). Data via useTechniquesWithCounts(). `filtered = useMemo(() => applyTechniqueFilters(data, { nameFilter, effectFilter }), [...])`. Toolbar mirroring RecipesPage: Input "Search techniques…" w-48; effect Select (w-40) with "__all__" → "All effects" plus RECIPE_EFFECTS; "Add Technique" Button (Plus icon) opening the form in create mode; "Clear filters" ghost button when a filter is active. Render TechniqueCardGrid (passing isFiltered = nameFilter !== "" || effectFilter !== null) with handlers: onCardClick → open detail; onEdit → open form in edit mode (close detail); onDelete → open delete dialog (pre-pass the card's usage_count); onDuplicate → useDuplicateTechnique mutate with "Copy of {name}". Mount TechniqueDetailSheet, TechniqueFormSheet (key technique?.id ?? "new"), TechniqueDeleteDialog.
    Modify `RecipesPage.tsx`: import Tabs/TabsList/TabsTrigger/TabsContent from "@/components/ui/tabs" and TechniqueLibraryTab. Add `const [activeTab, setActiveTab] = useState<"recipes" | "techniques">("recipes")`. Keep PageHeader title "Recipes"/subtitle unchanged. Below PageHeader, wrap the existing recipes filter bar + grid + sheets in `<Tabs value={activeTab} onValueChange=...>` with TabsList (TabsTrigger "recipes" / "techniques") and TabsContent value="recipes" containing the existing body verbatim, plus TabsContent value="techniques" rendering <TechniqueLibraryTab />. Do NOT alter any existing recipe filter logic — only wrap it. No route change.
    `tests/techniques/TechniqueLibraryTab.test.tsx` (jsdom): vi.mock useTechniquesWithCounts to return 2-3 stub techniques with distinct names/effects; render TechniqueLibraryTab inside a QueryClientProvider; assert technique cards render; type into the search Input and assert the list narrows; select an effect and assert it narrows (LIB-01 partial via tab triggers asserted in the RecipesPage render if practical, LIB-03 via filtering). If rendering the full RecipesPage is heavier, assert the two TabsTrigger labels render in a focused RecipesPage test or assert TechniqueLibraryTab's own filter behavior — the load-bearing assertion is that name + effect filters narrow the cards.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/techniques/TechniqueLibraryTab.test.tsx 2>&1 | grep -v '^#' | grep -E "passed|failed|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - RecipesPage renders a Tabs control with "Recipes" and "Techniques" triggers; existing recipes body is wrapped in TabsContent value="recipes" unchanged
    - No new route added and no sidebar change (RecipesPage is the only modified app-shell file)
    - TechniqueLibraryTab filters cards by name and effect
    - TechniqueLibraryTab.test.tsx is GREEN (LIB-01, LIB-03)
  </acceptance_criteria>
  <done>The Techniques tab is live on /recipes with full browse/filter/detail/delete/edit/duplicate wiring; all RTL tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite regression gate</name>
  <files>(no source changes — verification task)</files>
  <read_first>
    - .planning/phases/142-technique-authoring-library-browse/142-VALIDATION.md (full suite gate)
  </read_first>
  <action>
    Run the full Vitest suite and the version/schema gate to confirm no regressions across the 2916+ existing tests and that all six technique-related test files are GREEN. If any failure traces to a technique file, fix it; if a failure is a pre-existing unrelated flake, note it in the SUMMARY rather than fixing out of scope.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test 2>&1 | grep -v '^#' | grep -E "Test Files|Tests +[0-9]|passed|failed" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Full `pnpm test` suite is green (no new failures introduced by this phase)
    - All six technique test files (3 data-layer from plan 01/02 + techniqueSchema + applyTechniqueFilters + 4 RTL) pass
  </acceptance_criteria>
  <done>The full test suite passes; phase is ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filter input → in-memory filter | user search text; no SQL (pure JS filter) |
| delete action → cascade | deleting a technique cascades to sections/steps/slots/instances |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-08 | Tampering | technique delete cascade | mitigate | usage-count warning dialog surfaces N-recipe impact before delete; cascade is the intended migration-051 ON DELETE behavior |
| T-142-09 | Information Disclosure | used-by recipe names query | accept | local single-user data; parameterized $1 query; no cross-user exposure |
</threat_model>

<verification>
- `pnpm test` full suite green
- All six technique test files GREEN
- RecipesPage is the only app-shell file modified (no router.tsx or AppSidebar change) — confirms "no new sidebar entry / no new route"
- Manual-only (non-blocking UAT per 142-VALIDATION): open app → /recipes → Techniques tab → create/edit/duplicate/delete a technique end-to-end
</verification>

<success_criteria>
- Technique library browse is live as a second tab on /recipes
- Cards show name/effect/difficulty/usage/slot/step counts; name + effect filters work
- Detail Sheet shows slots, step tree, and used-by list; delete shows usage warning
- Full test suite green; all 11 phase requirements covered across plans 01-04
</success_criteria>

<output>
Create `.planning/phases/142-technique-authoring-library-browse/142-04-SUMMARY.md` when done
</output>

---
phase: 142-technique-authoring-library-browse
plan: 03
type: execute
wave: 3
depends_on: ["142-02"]
files_modified:
  - src/features/techniques/TechniqueSlotRow.tsx
  - src/features/techniques/TechniqueStepRow.tsx
  - src/features/techniques/TechniqueStepList.tsx
  - src/features/techniques/TechniqueSectionCard.tsx
  - src/features/techniques/TechniqueSectionList.tsx
  - src/features/techniques/TechniqueFormSheet.tsx
autonomous: true
requirements: [TECH-01, TECH-02, SLOT-01, SLOT-02]
must_haves:
  truths:
    - "User can author a named technique with colour slots (name + role hint) in a Sheet form"
    - "Slots are drag-reorderable; each step row has a slot-picker Select replacing the recipe paint combobox"
    - "Removing a slot from the slot list nulls colour_slot_id on every step that referenced it"
    - "Sections and steps are drag-reorderable; saving routes through saveTechniqueGraph via useCreate/useUpdateTechnique"
  artifacts:
    - path: "src/features/techniques/TechniqueSlotRow.tsx"
      provides: "Sortable slot editor row (name + role_hint inputs + remove)"
      min_lines: 30
    - path: "src/features/techniques/TechniqueStepRow.tsx"
      provides: "Sortable step row with slot-picker Select (no paint combobox, no photo, no alt paint)"
      contains: "__none__"
    - path: "src/features/techniques/TechniqueStepList.tsx"
      provides: "Step list within a section (dnd-kit), passes slots down"
    - path: "src/features/techniques/TechniqueSectionCard.tsx"
      provides: "Collapsible section card (name/surface/optional/notes — no workflow metadata)"
    - path: "src/features/techniques/TechniqueSectionList.tsx"
      provides: "DndContext over technique sections, passes slots down"
    - path: "src/features/techniques/TechniqueFormSheet.tsx"
      provides: "Create/edit Sheet with metadata fields + colour slots section + sections/steps editor + save"
      contains: "saveTechniqueGraph"
  key_links:
    - from: "src/features/techniques/TechniqueFormSheet.tsx"
      to: "src/hooks/useTechniques.ts"
      via: "useCreateTechnique / useUpdateTechnique"
      pattern: "useCreateTechnique|useUpdateTechnique"
    - from: "src/features/techniques/TechniqueStepRow.tsx"
      to: "DraftTechniqueSlot list"
      via: "slot-picker Select options keyed by slot.localId"
      pattern: "colour_slot_id"
---

<objective>
Build the technique authoring form: a Sheet mirroring `RecipeFormSheet` with metadata fields, a drag-reorderable colour-slots list (the one net-new UI concept), a slot-picker Select on each step row replacing the recipe paint combobox, and the sections/steps editor adapted from the recipe components. Save routes through the plan-02 hooks.

Purpose: This is the authoring surface for TECH-01, TECH-02, SLOT-01, SLOT-02. The critical correctness concern is the slot-removal handler: when a slot is removed, every step referencing its localId must reset colour_slot_id to null (Pitfall 2 / interaction contract), otherwise the slot-localId-to-PK resolution at save time silently nulls the wrong references.

Output: six new components under `src/features/techniques/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/142-technique-authoring-library-browse/142-PATTERNS.md
@.planning/phases/142-technique-authoring-library-browse/142-UI-SPEC.md
@.planning/phases/142-technique-authoring-library-browse/142-02-SUMMARY.md

<interfaces>
<!-- From plan 01/02 — import directly -->
@/types/technique: DraftTechniqueSlot, DraftTechniqueStep, DraftTechniqueSection, TechniqueFormValues, Technique
@/features/techniques/techniqueSchema: techniqueSchema, RECIPE_EFFECTS, RECIPE_DIFFICULTIES
@/features/techniques/techniqueSection: makeDraftTechniqueSection, buildDraftTechniqueSections, buildDraftTechniqueSlots
@/hooks/useTechniques: useCreateTechnique, useUpdateTechnique
@/hooks/useTechniqueSections: useTechniqueSections, useTechniqueSteps
@/hooks/useTechniqueColourSlots: useTechniqueColourSlots

Analog source files (adapt, do not import):
- src/features/recipes/RecipeFormSheet.tsx (form init lines 122-165, Sheet JSX, footer) — sm:max-w-xl
- src/features/recipes/RecipeStepRow.tsx (dnd-kit sortable lines 33-40, row layout lines 64-185)
- src/features/recipes/RecipeStepList.tsx
- src/features/recipes/RecipeSectionCard.tsx (Collapsible + sortable lines 52-80)
- src/features/recipes/RecipeSectionList.tsx (DndContext lines 1-66)
</interfaces>

<ui_contract>
From 142-UI-SPEC.md (authoritative visual/interaction spec):
- TechniqueSlotRow: `flex gap-2 rounded-md border p-2`; GripVertical drag handle; two Inputs (name flex-1, role_hint flex-1 text-xs) in `flex flex-1 flex-col gap-1.5`; Trash2 remove button (text-destructive, aria-label="Remove slot"). gap-1.5 is a DECLARED spacing exception.
- Slot picker on step row: `<Select value={step.colour_slot_id ?? "__none__"}>` in a w-40 container; SelectItem "__none__" = "-- no slot --"; map slots by s.localId; placeholder "Colour slot".
- Step row: 3-line layout from RecipeStepRow; line 1 replaces PaintCombobox (w-40) with the slot picker AND omits the photo button; line 2 `grid-cols-5 gap-1.5` keeps tool/technique/dilution/min, 5th cell (alt paint) omitted/empty; line 3 notes Input verbatim.
- Section card: keep name/surface/optional/notes; REMOVE section_type/technique/execution_mode/applies_to workflow block.
- FormSheet: sm:max-w-xl; title "New Technique"/"Edit Technique"; metadata fields (name autoFocus, description textarea, effect Select RECIPE_EFFECTS, difficulty Select RECIPE_DIFFICULTIES, estimated_minutes number, result_photo upload, notes textarea); Colour Slots section between metadata and the sections editor with an "Add Slot" button; footer Close + "Add Technique"/"Save Technique".
- Copywriting + validation toasts: see 142-UI-SPEC Copywriting Contract (name required, ≥1 step, all steps named, created/saved/error toasts).
</ui_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TechniqueSlotRow + TechniqueStepRow (slot picker)</name>
  <files>src/features/techniques/TechniqueSlotRow.tsx, src/features/techniques/TechniqueStepRow.tsx</files>
  <read_first>
    - src/features/recipes/RecipeStepRow.tsx (full file — dnd-kit sortable, 3-line layout, the PaintCombobox + photo button to REMOVE)
    - src/types/technique.ts (DraftTechniqueSlot, DraftTechniqueStep)
    - .planning/phases/142-technique-authoring-library-browse/142-PATTERNS.md (TechniqueSlotRow lines 240-279, TechniqueStepRow lines 362-407)
    - .planning/phases/142-technique-authoring-library-browse/142-UI-SPEC.md (slot row + slot picker + step row specs)
  </read_first>
  <action>
    `TechniqueSlotRow.tsx`: a sortable row editing one DraftTechniqueSlot. Props: slot, onChange(updated slot), onRemove. Use `useSortable({ id: slot.localId })` with `transform: CSS.Transform.toString(transform)`, `opacity: isDragging ? 0.4 : 1`. Render container `flex gap-2 rounded-md border p-2`, GripVertical drag handle (mt-1 self-start cursor-grab, aria-label="Drag to reorder slot"), two Inputs (name flex-1 placeholder "Slot name, e.g. Base Coat" maxLength 80; role_hint flex-1 text-xs placeholder "Role hint, e.g. brightest core" maxLength 120) inside `flex flex-1 flex-col gap-1.5`, and a ghost icon Trash2 remove button (text-destructive, self-start, aria-label="Remove slot"). Key on slot.localId at the list level (parent).
    `TechniqueStepRow.tsx`: adapt RecipeStepRow. Props: step, slots (DraftTechniqueSlot[]), onChange, onRemove. Keep the dnd-kit sortable wiring and 3-line structure. LINE 1: replace PaintCombobox with a w-40 Select bound to `step.colour_slot_id ?? "__none__"`, onValueChange sets colour_slot_id to null when "__none__" else the slot localId; SelectContent first item value="__none__" label "-- no slot --", then map slots by s.localId showing s.name (italic "Unnamed slot" when blank); OMIT the photo upload Button entirely. LINE 2: `grid-cols-5 gap-1.5` with tool/technique/dilution/time Inputs; the 5th cell (recipe alt-paint combobox) is omitted or an empty div. LINE 3: notes Input verbatim. Do NOT import openDialog/readFile/writeFile/BaseDirectory/PaintCombobox/ImageIcon. No step_photo_path or alt_paint_id references (those columns do not exist).
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm exec tsc --noEmit 2>&1 | grep -v '^#' | grep -iE "TechniqueSlotRow|TechniqueStepRow" || echo "no row component type errors"</automated>
  </verify>
  <acceptance_criteria>
    - TechniqueSlotRow uses useSortable keyed on slot.localId and renders name + role_hint inputs + remove button
    - TechniqueStepRow has NO PaintCombobox, NO photo button, NO alt_paint_id/step_photo_path references
    - The step slot picker uses value `step.colour_slot_id ?? "__none__"` and maps slots by localId
    - tsc clean for both files
  </acceptance_criteria>
  <done>Slot row and step row render with the slot picker; no recipe-only paint/photo concerns leak in.</done>
</task>

<task type="auto">
  <name>Task 2: TechniqueStepList + TechniqueSectionCard + TechniqueSectionList</name>
  <files>src/features/techniques/TechniqueStepList.tsx, src/features/techniques/TechniqueSectionCard.tsx, src/features/techniques/TechniqueSectionList.tsx</files>
  <read_first>
    - src/features/recipes/RecipeStepList.tsx, src/features/recipes/RecipeSectionCard.tsx (lines 52-80 sortable+Collapsible), src/features/recipes/RecipeSectionList.tsx (lines 1-66 DndContext)
    - src/features/techniques/TechniqueStepRow.tsx (Task 1)
    - src/types/technique.ts (DraftTechniqueSection, DraftTechniqueStep, DraftTechniqueSlot)
    - .planning/phases/142-technique-authoring-library-browse/142-PATTERNS.md (TechniqueSectionList lines 283-324, TechniqueSectionCard lines 328-358)
  </read_first>
  <action>
    `TechniqueStepList.tsx`: adapt RecipeStepList — DndContext + SortableContext over a section's steps, renders TechniqueStepRow per step, "Add Step" button, handleDragEnd via arrayMove keyed on step.localId. Accept a `slots: DraftTechniqueSlot[]` prop and pass it to each TechniqueStepRow. Remove any onCreateNewPaint prop.
    `TechniqueSectionCard.tsx`: adapt RecipeSectionCard — sortable (id section.localId) + Collapsible; header row with GripVertical handle, collapse chevron, name Input, delete Button; CollapsibleContent with surface Select + optional checkbox + notes Input + TechniqueStepList. REMOVE the workflow metadata block (section_type/technique/execution_mode/applies_to and any workflowOpen state). Accept and pass down `slots`.
    `TechniqueSectionList.tsx`: adapt RecipeSectionList lines 1-66 — DndContext + SortableContext over sections, sensors (PointerSensor distance 5 + KeyboardSensor), handleDragEnd via arrayMove keyed on section.localId, render TechniqueSectionCard per section in a `flex flex-col gap-3`. Props: sections, onChange, slots. Remove onCreateNewPaint.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm exec tsc --noEmit 2>&1 | grep -v '^#' | grep -iE "TechniqueStepList|TechniqueSectionCard|TechniqueSectionList" || echo "no section component type errors"</automated>
  </verify>
  <acceptance_criteria>
    - TechniqueSectionCard has NO section_type/technique/execution_mode/applies_to fields or workflow block
    - slots prop is threaded SectionList → SectionCard → StepList → StepRow
    - All three components use localId for dnd ids and React keys
    - tsc clean for all three files
  </acceptance_criteria>
  <done>The technique section/step editor renders and reorders, with slots threaded to every step row.</done>
</task>

<task type="auto">
  <name>Task 3: TechniqueFormSheet (metadata + slots + save)</name>
  <files>src/features/techniques/TechniqueFormSheet.tsx</files>
  <read_first>
    - src/features/recipes/RecipeFormSheet.tsx (full file — form init lines 122-165, defaults, useEffect re-init, Sheet JSX, footer, save handler)
    - src/features/techniques/TechniqueSectionList.tsx, TechniqueSlotRow.tsx (Tasks 1-2)
    - src/hooks/useTechniques.ts, src/hooks/useTechniqueSections.ts, src/hooks/useTechniqueColourSlots.ts (plan 02)
    - src/features/techniques/techniqueSection.ts (makeDraftTechniqueSection, buildDraftTechniqueSections, buildDraftTechniqueSlots)
    - .planning/phases/142-technique-authoring-library-browse/142-PATTERNS.md (TechniqueFormSheet lines 161-237)
    - .planning/phases/142-technique-authoring-library-browse/142-UI-SPEC.md (FormSheet spec + Colour Slots section + Copywriting Contract)
  </read_first>
  <action>
    Create `TechniqueFormSheet.tsx` mirroring RecipeFormSheet. Props: open, technique (Technique | null), onClose. `isEdit = technique !== null`. useForm with zodResolver(techniqueSchema) + buildDefaults(technique). State: `const [sections, setSections] = useState<DraftTechniqueSection[]>([makeDraftTechniqueSection("Steps")])` and `const [slots, setSlots] = useState<DraftTechniqueSlot[]>([])`. Load existing data in edit mode via useTechniqueSections(technique?.id), useTechniqueSteps(technique?.id), useTechniqueColourSlots(technique?.id); in a useEffect keyed on technique?.id (+ loaded lengths), reset the form and rebuild slots via buildDraftTechniqueSlots and sections via buildDraftTechniqueSections (passing the loaded slots so each step's DB colour_slot_id maps back to a slot localId), or reset to a single empty section + empty slots when creating. Put `key={technique?.id ?? "new"}` on SheetContent to force re-mount.
    JSX: SheetContent `overflow-y-auto sm:max-w-xl`; header title "New Technique"/"Edit Technique" + description per copy contract; Form with metadata FormFields (name autoFocus, description textarea, effect Select over RECIPE_EFFECTS, difficulty Select over RECIPE_DIFFICULTIES, estimated_minutes number min 1, result_photo upload button reusing the recipe upload pattern, notes textarea). Colour Slots section between metadata and the sections editor: header "Colour Slots" (text-xs font-semibold uppercase tracking-wide text-muted-foreground), a DndContext + SortableContext over slots rendering TechniqueSlotRow (its own dnd context, separate from the section list), and an "Add Slot" outline button appending a new draft slot (crypto.randomUUID localId, dbId null). Then TechniqueSectionList passing sections + slots + onChange, with an "Add Section" button. Footer: Close (outline) + submit "Add Technique"/"Save Technique" disabled while submitting.
    CRITICAL slot-removal handler: `removeSlot(localId)` must (1) drop the slot from slots, AND (2) iterate every section's steps and set colour_slot_id to null for any step whose colour_slot_id === the removed slot's localId, then setSections with the cleaned copy. This prevents the save-time mis-resolution (Pitfall 2).
    onSubmit: validate name non-empty, ≥1 step total across sections, all steps have a name — show the exact toast warnings from the copy contract and abort on failure. Recompute order_index from array positions for slots/sections/steps. Call useCreateTechnique or useUpdateTechnique passing formValues, slots, sections (and existing slots/sections/steps for edit so the diff works). On success toast "Technique created."/"Technique saved." and onClose; on error toast "Failed to save technique. Changes were not saved."
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm exec tsc --noEmit 2>&1 | grep -v '^#' | grep -iE "TechniqueFormSheet" || echo "no form sheet type errors"</automated>
  </verify>
  <acceptance_criteria>
    - removeSlot nulls colour_slot_id on every step referencing the removed slot's localId before saving
    - Colour Slots list uses a separate DndContext from the section list
    - onSubmit enforces name + ≥1 step + all-steps-named with the exact copy-contract toasts
    - Save routes through useCreateTechnique (create) or useUpdateTechnique (edit) with existing slots/sections/steps passed for the diff
    - tsc clean
  </acceptance_criteria>
  <done>The technique authoring form creates and edits techniques non-destructively with the colour-slots model fully wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form inputs → draft state → save | user text in name/slot/role-hint/step fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-06 | Tampering | slot name / role-hint length | mitigate | maxLength 80 (name) / 120 (role hint) on Inputs + Zod name max 120 on form submit |
| T-142-07 | Tampering | stale step→slot reference on slot removal | mitigate | removeSlot handler nulls referencing steps' colour_slot_id (Pitfall 2 guard) |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` clean for all six components
- `pnpm test` full suite still green (no regressions to recipe components which are untouched)
- Manual-only (non-blocking UAT per 142-VALIDATION): create a technique with 2 sections, steps, 2 slots; reorder; remove a slot and confirm referencing steps clear
</verification>

<success_criteria>
- Technique authoring Sheet is fully functional: metadata, drag-reorderable slots, slot-picker steps, sections editor
- Slot removal correctly clears step references
- Save is non-destructive via the plan-02 graph save
</success_criteria>

<output>
Create `.planning/phases/142-technique-authoring-library-browse/142-03-SUMMARY.md` when done
</output>

---
phase: 102-smart-context-pre-filling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/recipes/ApplyRecipeDialog.tsx
  - src/features/units/UnitDetailSheet.tsx
  - tests/recipes/ApplyRecipeDialogGrouping.test.tsx
autonomous: true
requirements:
  - SCP-02

must_haves:
  truths:
    - "ApplyRecipeDialog shows 'Suggested' group with faction-matching recipes when factionId prop is provided"
    - "ApplyRecipeDialog shows 'Other' group with remaining recipes below 'Suggested'"
    - "Both Suggested and Other groups are selectable — user can pick any recipe"
    - "When factionId is null or absent, all recipes show in a single flat list (current behavior)"
    - "Search filters across both groups simultaneously"
    - "UnitDetailSheet passes unit.faction_id to ApplyRecipeDialog"
  artifacts:
    - path: "src/features/recipes/ApplyRecipeDialog.tsx"
      provides: "Recipe picker with faction-based Suggested/Other grouping"
      contains: "CommandGroup"
    - path: "src/features/units/UnitDetailSheet.tsx"
      provides: "Prop threading from unit to ApplyRecipeDialog"
      contains: "factionId={unit.faction_id}"
    - path: "tests/recipes/ApplyRecipeDialogGrouping.test.tsx"
      provides: "Tests for grouping behavior"
      min_lines: 50
  key_links:
    - from: "src/features/units/UnitDetailSheet.tsx"
      to: "src/features/recipes/ApplyRecipeDialog.tsx"
      via: "factionId prop"
      pattern: "factionId.*unit\\.faction_id"
    - from: "src/features/recipes/ApplyRecipeDialog.tsx"
      to: "CommandGroup"
      via: "heading prop"
      pattern: 'heading=.*Suggested'
---

<objective>
Add faction-based recipe grouping to ApplyRecipeDialog and wire the factionId prop from UnitDetailSheet. When a unit has a faction, recipes matching that faction appear in a "Suggested" group above all others in an "Other" group. When the unit has no faction, current flat-list behavior is preserved.

Purpose: Implements SCP-02 (recipe picker pre-filters by target unit's faction). The user sees their faction's recipes first when applying a recipe from a unit context, reducing selection time.
Output: Modified ApplyRecipeDialog.tsx with grouping, modified UnitDetailSheet.tsx with prop threading, new test file.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-smart-context-pre-filling/102-CONTEXT.md
@.planning/phases/102-smart-context-pre-filling/102-RESEARCH.md
@.planning/phases/102-smart-context-pre-filling/102-PATTERNS.md
@.planning/phases/102-smart-context-pre-filling/102-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/features/recipes/ApplyRecipeDialog.tsx (CURRENT - before changes):
```typescript
interface ApplyRecipeDialogProps {
  open: boolean;
  unitId: number;
  onClose: () => void;
}

export function ApplyRecipeDialog({ open, unitId, onClose }: ApplyRecipeDialogProps) {
  // Line 57: const { data: recipes = [] } = useRecipes();
  // Line 65: const factionMap = useMemo(() => { ... }, [factions]);
  // Lines 112-128: Single <CommandGroup> rendering all recipes
}
```

From src/features/units/UnitDetailSheet.tsx (CURRENT - before changes):
```typescript
// Line 49: export function UnitDetailSheet({ open, unit, onClose, ... }) {
// Line 52: faction derived from unit.faction_id via factions lookup
// Lines 272-278:
<ApplyRecipeDialog
  open={applyDialogOpen}
  unitId={unit.id}
  onClose={() => setApplyDialogOpen(false)}
/>
```

From src/components/ui/command.tsx:
```typescript
// CommandGroup accepts heading prop (passed through to cmdk CommandPrimitive.Group)
// Search works across all groups by CommandItem value
```

From src/types/recipe.ts:
```typescript
export interface PaintingRecipe {
  id: number;
  name: string;
  faction_id: number | null;
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add faction grouping to ApplyRecipeDialog and wire from UnitDetailSheet</name>
  <files>src/features/recipes/ApplyRecipeDialog.tsx, src/features/units/UnitDetailSheet.tsx</files>
  <read_first>
    src/features/recipes/ApplyRecipeDialog.tsx
    src/features/units/UnitDetailSheet.tsx
    src/components/ui/command.tsx
  </read_first>
  <action>
    In ApplyRecipeDialog.tsx:

    1. Add factionId to ApplyRecipeDialogProps interface (line 30-34): factionId?: number | null. Per D-04.

    2. Destructure factionId in the component function signature (line 45-48).

    3. Add a useMemo after the factionMap memo (after line 69) that splits recipes into suggested and other arrays. Per D-06: if factionId is null or undefined, return { suggested: recipes, other: [] } (flat list, no grouping). Otherwise filter: suggested = recipes where r.faction_id === factionId, other = recipes where r.faction_id !== factionId.

    4. Replace the single CommandGroup (lines 112-128) with two conditional CommandGroup components per the UI-SPEC:
       - First group: render when suggested.length > 0. Use heading={factionId != null && other.length > 0 ? `Suggested (${suggested.length})` : undefined}. Map over suggested array with the same CommandItem rendering as current.
       - Second group: render when other.length > 0. Use heading={`Other (${other.length})`}. Map over other array with the same CommandItem rendering.
       - Per D-05: search/filter behavior unchanged — cmdk searches across all groups by CommandItem value natively.
       - Per UI-SPEC: include count in parentheses in group headers.

    In UnitDetailSheet.tsx:

    5. Add factionId={unit.faction_id} prop to the ApplyRecipeDialog render call (line 273-278). Per D-09, this is the only call site that carries unit context. The unit variable is already available and typed as Unit with faction_id: number | null.
  </action>
  <acceptance_criteria>
    - ApplyRecipeDialogProps has factionId?: number | null
    - useMemo splits recipes into suggested/other based on factionId match
    - When factionId is null, suggested equals full recipes array and other is empty (flat list)
    - Two CommandGroup elements render conditionally: "Suggested (N)" and "Other (N)"
    - When factionId is null, single CommandGroup renders without heading (current behavior)
    - UnitDetailSheet passes factionId={unit.faction_id} to ApplyRecipeDialog
    - pnpm build completes without TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>ApplyRecipeDialog shows Suggested/Other groups when factionId provided, flat list when null. UnitDetailSheet threads unit.faction_id to the dialog. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Test ApplyRecipeDialog faction grouping</name>
  <files>tests/recipes/ApplyRecipeDialogGrouping.test.tsx</files>
  <read_first>
    tests/recipes/RecipeDetailSheetPaint.test.tsx
    src/features/recipes/ApplyRecipeDialog.tsx
  </read_first>
  <action>
    Create tests/recipes/ApplyRecipeDialogGrouping.test.tsx following the mock pattern from the analog test file RecipeDetailSheetPaint.test.tsx.

    Mock all hooks used by ApplyRecipeDialog: useRecipes (return array with 4 recipes: 2 with faction_id=10, 1 with faction_id=20, 1 with faction_id=null), useFactions (return [{id:10,name:"Ultramarines"},{id:20,name:"Tau Empire"}]), useRecipeSections (return {data:[]}), useRecipePaints (return {data:[]}), usePaints (return {data:[]}), useRecipeAssignments (return {useCreateAssignment: () => ({mutate:vi.fn(), isPending:false})}). Create QueryClient wrapper with retry:false.

    Test cases for SCP-02:

    1. "shows Suggested and Other groups when factionId matches some recipes" — render ApplyRecipeDialog with open=true, unitId=1, factionId=10. Assert text "Suggested" appears in the document. Assert text "Other" appears in the document. Assert the two faction-10 recipes appear in the rendered output.

    2. "shows flat list when factionId is null" — render with factionId=null. Assert text "Suggested" does NOT appear. Assert text "Other" does NOT appear. Assert all 4 recipes are visible.

    3. "shows flat list when factionId is undefined (prop absent)" — render without factionId prop. Same assertions as test 2.

    4. "both groups are selectable" — render with factionId=10. Use userEvent or fireEvent to click a recipe from the Other group. Assert setSelectedRecipeId is called (or the preview view appears with the recipe name).

    Use describe block labeled "SCP-02: ApplyRecipeDialog faction grouping".
  </action>
  <acceptance_criteria>
    - Test file exists at tests/recipes/ApplyRecipeDialogGrouping.test.tsx
    - At least 3 test cases: grouped with factionId, flat with null, flat with undefined
    - All hooks used by ApplyRecipeDialog are mocked
    - pnpm test -- tests/recipes/ApplyRecipeDialogGrouping.test.tsx passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/recipes/ApplyRecipeDialogGrouping.test.tsx -x</automated>
  </verify>
  <done>All grouping tests pass: Suggested/Other groups appear when factionId provided, flat list when null or absent, recipes from both groups are selectable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. factionId is an internal prop from the same React tree. Recipe data is already loaded from local SQLite.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-02 | Information Disclosure | Recipe grouping | accept | Grouping uses faction_id comparison on already-loaded recipe data. No new data exposed — all recipes were already visible in the flat list. |
</threat_model>

<verification>
pnpm build passes (TypeScript check)
pnpm test -- tests/recipes/ApplyRecipeDialogGrouping.test.tsx -x passes
</verification>

<success_criteria>
ApplyRecipeDialog groups recipes into Suggested/Other when a factionId prop is provided, preserving flat list behavior when absent. UnitDetailSheet threads unit.faction_id to the dialog. Tests validate grouping, flat list fallback, and cross-group selectability.
</success_criteria>

<output>
Create `.planning/phases/102-smart-context-pre-filling/102-02-SUMMARY.md` when done
</output>

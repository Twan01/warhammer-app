---
phase: 76-points-resolver-unit-rules-mapping-split-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/resolveUnitPoints.ts
  - src/types/unitRulesMapping.ts
  - src/types/armyList.ts
  - src/db/queries/armyLists.ts
  - src/db/queries/dashboard.ts
  - src/db/queries/unitRulesMapping.ts
  - src/hooks/useUnitRulesMapping.ts
  - src/lib/computeUnitWarnings.ts
  - tests/lib/resolveUnitPoints.test.ts
  - tests/lib/computeUnitWarnings.test.ts
autonomous: true
requirements:
  - PV-01
  - PV-02
  - PV-03
  - PV-04
  - PV-05
  - PV-06
  - PV-07

must_haves:
  truths:
    - "resolveUnitPoints() returns correct source label for each COALESCE fallback level"
    - "SQL queries expose synced_points and override_points as separate columns"
    - "Dashboard COALESCE chain matches the 4-level standard with LEFT JOINs"
    - "computeUnitWarnings no longer contains list-level warnings"
    - "computeListWarnings returns list-level warnings only"
    - "computeListHealthStats uses computeListWarnings for list counts"
    - "Unit rules mapping CRUD layer works with upsert semantics"
  artifacts:
    - path: "src/lib/resolveUnitPoints.ts"
      provides: "Pure points resolver function with source labeling"
      exports: ["resolveUnitPoints", "ResolvedPoints", "PointsSource"]
    - path: "src/types/unitRulesMapping.ts"
      provides: "Entity/UpsertInput types for unit_rules_mapping table"
      exports: ["UnitRulesMapping", "UpsertUnitRulesMappingInput", "MatchStatus", "MATCH_STATUSES"]
    - path: "src/db/queries/unitRulesMapping.ts"
      provides: "CRUD queries for unit_rules_mapping"
      exports: ["getUnitRulesMapping", "getUnitRulesMappings", "upsertUnitRulesMapping", "deleteUnitRulesMapping", "findMatchingDatasheets", "findRulesDatasheets"]
    - path: "src/hooks/useUnitRulesMapping.ts"
      provides: "React Query hooks for unit rules mapping"
      exports: ["UNIT_RULES_MAPPING_KEY", "useUnitRulesMapping", "useUpsertUnitRulesMapping", "useDeleteUnitRulesMapping"]
    - path: "tests/lib/resolveUnitPoints.test.ts"
      provides: "Tests for resolver function"
      min_lines: 40
  key_links:
    - from: "src/lib/resolveUnitPoints.ts"
      to: "src/types/armyList.ts"
      via: "ArmyListUnitRow extended with synced_points, override_points"
      pattern: "synced_points.*override_points"
    - from: "src/db/queries/armyLists.ts"
      to: "src/types/armyList.ts"
      via: "SQL SELECT exposes sup.points AS synced_points, uo.points AS override_points"
      pattern: "AS synced_points"
    - from: "src/db/queries/dashboard.ts"
      to: "synced_unit_points + unit_overrides"
      via: "LEFT JOIN + 4-level COALESCE"
      pattern: "COALESCE.*sup\\.points.*uo\\.points.*u\\.points"
---

<objective>
Build the complete data and logic layer for Phase 76: the resolveUnitPoints() pure function, unit_rules_mapping CRUD stack (types/queries/hooks), SQL column exposure for source labeling, dashboard COALESCE fix, and warning split refactor.

Purpose: Establishes all non-UI foundations that Plan 02 (UI integration) depends on. Every surface that shows points will read from a single resolver; the warning system cleanly separates list-level from unit-level concerns; the mapping CRUD layer is ready for the Sheet UI.

Output: 8 new/modified source files + 2 test files, all passing.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-points-resolver-unit-rules-mapping-split-warnings/76-CONTEXT.md
@.planning/phases/76-points-resolver-unit-rules-mapping-split-warnings/76-RESEARCH.md
@.planning/phases/76-points-resolver-unit-rules-mapping-split-warnings/76-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/armyList.ts (current ArmyListUnitRow — must be extended):
```typescript
export interface ArmyListUnitRow extends ArmyListUnit {
  unit_name: string;
  unit_points: number | null;
  effective_points: number;
  faction_id: number;
  status_assembly: number;
  status_painting: string;
  painting_percentage: number;
  tactical_role: string | null;
}
```

From src/types/unitOverride.ts (analog for UnitRulesMapping type pattern):
```typescript
export interface UnitOverride {
  id: number;
  unit_id: number;
  points: number | null;
  // ... stat fields ...
  created_at: string;
  updated_at: string;
}
export type UpsertUnitOverrideInput = {
  unit_id: number;
  points: number | null;
  // ... stat fields ...
};
```

From src/lib/computeUnitWarnings.ts (current exports to refactor):
```typescript
export interface UnitWarnings { hard: string[]; soft: string[]; }
export interface WarningContext { totalPoints: number; pointsLimit: number | null; freshness: SyncFreshness; }
export interface ListHealthStats { totalPoints: number; pointsLimit: number | null; ownershipPct: number; battleReadyPct: number; hardWarningCount: number; softWarningCount: number; pointsExceeded: boolean; }
export function computeUnitWarnings(unit, context): UnitWarnings;
export function computeListHealthStats(units, pointsLimit, freshness): ListHealthStats;
```

From src/db/queries/armyLists.ts line 63 (current COALESCE in getArmyListWithUnits):
```sql
COALESCE(alu.points_override, sup.points, uo.points, u.points, 0) AS effective_points
```

From src/db/queries/dashboard.ts line 93 (divergent 2-level COALESCE):
```sql
SUM(COALESCE(u.points, 0)) AS points_owned
```

From migration 026 (unit_rules_mapping schema):
```sql
CREATE TABLE IF NOT EXISTS unit_rules_mapping (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    unit_id             INTEGER NOT NULL UNIQUE REFERENCES units(id) ON DELETE CASCADE,
    rules_datasheet_id  TEXT,
    match_status        TEXT    NOT NULL DEFAULT 'auto',
    source              TEXT,
    created_at          TEXT    NOT NULL DEFAULT (datetime('now')),
    updated_at          TEXT    NOT NULL DEFAULT (datetime('now'))
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolver function, types, SQL exposure, and tests</name>
  <files>
    src/lib/resolveUnitPoints.ts,
    src/types/armyList.ts,
    src/types/unitRulesMapping.ts,
    src/db/queries/armyLists.ts,
    src/db/queries/dashboard.ts,
    tests/lib/resolveUnitPoints.test.ts
  </files>
  <read_first>
    src/types/armyList.ts,
    src/types/unitOverride.ts,
    src/lib/computeUnitWarnings.ts,
    src/db/queries/armyLists.ts,
    src/db/queries/dashboard.ts,
    src-tauri/migrations/026_unit_rules_mapping.sql,
    tests/lib/computeUnitWarnings.test.ts
  </read_first>
  <behavior>
    - resolveUnitPoints({ points_override: 100, synced_points: 95, override_points: 80, unit_points: 75 }) returns { points: 100, source: "override" }
    - resolveUnitPoints({ points_override: null, synced_points: 95, override_points: 80, unit_points: 75 }) returns { points: 95, source: "synced" }
    - resolveUnitPoints({ points_override: null, synced_points: null, override_points: 80, unit_points: 75 }) returns { points: 80, source: "user-override" }
    - resolveUnitPoints({ points_override: null, synced_points: null, override_points: null, unit_points: 75 }) returns { points: 75, source: "base" }
    - resolveUnitPoints({ points_override: null, synced_points: null, override_points: null, unit_points: null }) returns { points: 0, source: "unknown" }
    - resolveUnitPoints with points_override = 0 (falsy but not null) returns { points: 0, source: "override" }
  </behavior>
  <action>
    Per D-01/D-02/D-03: Create src/lib/resolveUnitPoints.ts exporting PointsSource union type ("override" | "synced" | "user-override" | "base" | "unknown"), ResolvedPoints interface ({ points: number; source: PointsSource }), and resolveUnitPoints() pure function. The function takes an object with four nullable number fields (points_override, synced_points, override_points, unit_points) and returns ResolvedPoints. The if-chain order MUST match the SQL COALESCE argument order exactly: points_override first, then synced_points, then override_points, then unit_points, else unknown. Use strict null check (!=  null) not truthiness to handle 0 correctly.

    Per D-06: Create src/types/unitRulesMapping.ts exporting MatchStatus type ("auto" | "confirmed" | "manual"), MATCH_STATUSES const array, UnitRulesMapping interface (id, unit_id, rules_datasheet_id: string | null, match_status: MatchStatus, source: string | null, created_at, updated_at), and UpsertUnitRulesMappingInput type (unit_id, rules_datasheet_id, match_status, source optional).

    Per D-02: Extend ArmyListUnitRow in src/types/armyList.ts with two new fields: synced_points: number | null, override_points: number | null.

    Per D-02: In src/db/queries/armyLists.ts getArmyListWithUnits, add two SELECT columns after painting_percentage: sup.points AS synced_points, uo.points AS override_points. The LEFT JOINs for sup and uo already exist.

    Per D-04: In src/db/queries/dashboard.ts getArmyReadinessByFaction, upgrade the 2-level COALESCE to 4-level: COALESCE(sup.points, uo.points, u.points, 0). Add two LEFT JOINs after the existing JOIN units: LEFT JOIN unit_overrides uo ON uo.unit_id = u.id, LEFT JOIN synced_unit_points sup ON sup.unit_name = u.name AND (sup.faction_id IS NULL OR sup.faction_id = CAST(u.faction_id AS TEXT)). Update both SUM expressions to use the new COALESCE. Per D-05, no source labeling needed here (aggregation only).

    Write tests in tests/lib/resolveUnitPoints.test.ts covering all 5 source levels plus the 0-value edge case.
  </action>
  <verify>
    <automated>pnpm test -- tests/lib/resolveUnitPoints.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/resolveUnitPoints.ts exports resolveUnitPoints, ResolvedPoints, PointsSource
    - resolveUnitPoints({ points_override: 100, synced_points: null, override_points: null, unit_points: null }).source === "override"
    - resolveUnitPoints({ points_override: null, synced_points: 95, override_points: null, unit_points: null }).source === "synced"
    - resolveUnitPoints({ points_override: null, synced_points: null, override_points: null, unit_points: null }) returns { points: 0, source: "unknown" }
    - resolveUnitPoints({ points_override: 0, synced_points: 95, override_points: null, unit_points: null }).source === "override" (0 is not null)
    - ArmyListUnitRow in armyList.ts includes synced_points: number | null and override_points: number | null
    - getArmyListWithUnits SQL contains "sup.points AS synced_points" and "uo.points AS override_points"
    - getArmyReadinessByFaction SQL contains "LEFT JOIN unit_overrides" and "LEFT JOIN synced_unit_points" and "COALESCE(sup.points, uo.points, u.points, 0)"
    - src/types/unitRulesMapping.ts exports UnitRulesMapping, UpsertUnitRulesMappingInput, MatchStatus, MATCH_STATUSES
    - pnpm test -- tests/lib/resolveUnitPoints.test.ts passes all tests
    - pnpm build completes without TypeScript errors
  </acceptance_criteria>
  <done>Resolver function returns correct source for all 5 COALESCE levels plus edge cases; SQL queries expose intermediate columns; dashboard COALESCE upgraded to 4-level; unit rules mapping types defined; all tests pass; build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Warning split + mapping queries/hooks + test updates</name>
  <files>
    src/lib/computeUnitWarnings.ts,
    src/db/queries/unitRulesMapping.ts,
    src/hooks/useUnitRulesMapping.ts,
    tests/lib/computeUnitWarnings.test.ts
  </files>
  <read_first>
    src/lib/computeUnitWarnings.ts,
    src/db/queries/unitOverrides.ts,
    src/hooks/useUnitOverride.ts,
    src/db/queries/syncedUnitPoints.ts,
    src/db/rules-client.ts,
    tests/lib/computeUnitWarnings.test.ts,
    src/types/unitRulesMapping.ts
  </read_first>
  <behavior>
    - computeListWarnings({ totalPoints: 2100, pointsLimit: 2000, freshness: "fresh" }).hard contains "Points exceeded"
    - computeListWarnings({ totalPoints: 1500, pointsLimit: 2000, freshness: "stale" }).soft contains "Stale points data"
    - computeListWarnings({ totalPoints: 1500, pointsLimit: 2000, freshness: "fresh" }) returns empty hard and soft
    - computeUnitWarnings for a unit with totalPoints > pointsLimit does NOT contain "Points exceeded"
    - computeUnitWarnings for freshness "stale" does NOT contain "Stale points"
    - computeListHealthStats uses computeListWarnings for list counts, no deduplication filter
  </behavior>
  <action>
    Per D-11: Add a new computeListWarnings(context: WarningContext): UnitWarnings function to src/lib/computeUnitWarnings.ts. Move the "Points exceeded" check (context.pointsLimit !== null and context.totalPoints > context.pointsLimit -> hard) and the "Stale points" check (context.freshness === "stale" || "never" -> soft with label "Stale points data") into this new function. Export it.

    Per D-12: Remove both checks from computeUnitWarnings. After this change, computeUnitWarnings only contains: Not painted, Not assembled, Manual override, Unknown points. No list-level conditions.

    Per Pitfall 3: Update computeListHealthStats to call computeListWarnings(context) once for list-level counts. Remove the hardWarningCount deduplication filter (warnings.hard.filter(w => w !== "Points exceeded")). New logic: listWarnings = computeListWarnings(context); hardWarningCount = listWarnings.hard.length; softWarningCount = listWarnings.soft.length; then loop units for unit-level counts adding to both counters.

    Per D-07: Create src/db/queries/unitRulesMapping.ts importing getDb from @/db/client and types from @/types/unitRulesMapping. Implement: getUnitRulesMapping(unitId) returning UnitRulesMapping | null (SELECT * WHERE unit_id=$1 LIMIT 1), getUnitRulesMappings() returning all rows, upsertUnitRulesMapping(input) using select-then-insert/update pattern on unit_id (same as unitOverrides.ts), deleteUnitRulesMapping(unitId). Add findMatchingDatasheets(unitName, factionId) querying synced_unit_points table for name matches (SELECT unit_name, faction_id, points WHERE unit_name = $1 OR unit_name LIKE $2, with %unitName% for LIKE). Add findRulesDatasheets(searchTerm) querying rules.db via getRulesDb() (SELECT id, name, faction_id FROM rw_datasheets WHERE name LIKE $1 ORDER BY name LIMIT 20).

    Per D-08: Create src/hooks/useUnitRulesMapping.ts following the useUnitOverride.ts pattern exactly. Export UNIT_RULES_MAPPING_KEY(unitId) returning ["unit-rules-mapping", unitId], useUnitRulesMapping(unitId: number | undefined) with enabled guard, useUpsertUnitRulesMapping() with onSuccess invalidating the mapping key + ["army-lists"] cascade, useDeleteUnitRulesMapping() with same invalidation.

    Update tests/lib/computeUnitWarnings.test.ts: Add describe block for computeListWarnings testing "Points exceeded" and "Stale points data" in list context. Update existing computeUnitWarnings tests: the "Points exceeded" test should now assert it is NOT present in computeUnitWarnings output. The "Stale points" tests should assert "Stale points" is NOT in computeUnitWarnings output. Update the accumulate-multiple-soft test to expect 4 warnings (remove "Stale points"). Update computeListHealthStats tests to verify correct counts after the split.
  </action>
  <verify>
    <automated>pnpm test -- tests/lib/computeUnitWarnings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/computeUnitWarnings.ts exports computeListWarnings function
    - computeListWarnings({ totalPoints: 2100, pointsLimit: 2000, freshness: "fresh" }).hard includes "Points exceeded"
    - computeListWarnings({ totalPoints: 1500, pointsLimit: 2000, freshness: "stale" }).soft includes "Stale points data"
    - computeUnitWarnings with totalPoints > pointsLimit does NOT include "Points exceeded" in hard array
    - computeUnitWarnings with freshness "stale" does NOT include "Stale points" in soft array
    - computeListHealthStats has no .filter(w => w !== "Points exceeded") call
    - src/db/queries/unitRulesMapping.ts exports getUnitRulesMapping, getUnitRulesMappings, upsertUnitRulesMapping, deleteUnitRulesMapping, findMatchingDatasheets, findRulesDatasheets
    - src/hooks/useUnitRulesMapping.ts exports UNIT_RULES_MAPPING_KEY, useUnitRulesMapping, useUpsertUnitRulesMapping, useDeleteUnitRulesMapping
    - upsertUnitRulesMapping uses select-then-insert/update pattern (not INSERT OR REPLACE)
    - findRulesDatasheets imports getRulesDb from @/db/rules-client
    - pnpm test -- tests/lib/computeUnitWarnings.test.ts passes all tests
    - pnpm build completes without TypeScript errors
  </acceptance_criteria>
  <done>Warning system cleanly split into list-level and unit-level; mapping CRUD queries and hooks complete; all updated tests pass; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> SQL | Search terms in findRulesDatasheets and findMatchingDatasheets |
| rules.db -> hobbyforge.db | Cross-DB text references (rules_datasheet_id) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Tampering | findRulesDatasheets | mitigate | Parameterized query with $1 positional syntax, LIMIT 20 on results |
| T-76-02 | Tampering | findMatchingDatasheets | mitigate | Parameterized query with $1/$2 positional syntax |
| T-76-03 | Information Disclosure | resolveUnitPoints | accept | Pure function with no DB access, operates on already-fetched data |
</threat_model>

<verification>
1. `pnpm test -- tests/lib/resolveUnitPoints.test.ts` -- all resolver tests pass
2. `pnpm test -- tests/lib/computeUnitWarnings.test.ts` -- all warning split tests pass
3. `pnpm build` -- no TypeScript errors across the project
</verification>

<success_criteria>
- resolveUnitPoints() correctly labels source for all 5 COALESCE levels
- ArmyListUnitRow type includes synced_points and override_points
- getArmyListWithUnits SQL exposes intermediate columns
- getArmyReadinessByFaction uses 4-level COALESCE with LEFT JOINs
- computeUnitWarnings contains only unit-level warnings
- computeListWarnings contains only list-level warnings
- Unit rules mapping CRUD layer (types, queries, hooks) is complete
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/76-points-resolver-unit-rules-mapping-split-warnings/76-01-SUMMARY.md`
</output>

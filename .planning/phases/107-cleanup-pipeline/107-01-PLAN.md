---
phase: 107-cleanup-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # DELETE - query modules
  - src/db/queries/datasheets.ts
  - src/db/queries/rulesExtended.ts
  - src/db/queries/syncErrors.ts
  - src/db/queries/rulesSnapshot.ts
  - src/db/queries/pointsImportHistory.ts
  - src/db/queries/unitRulesMapping.ts
  # DELETE - db client
  - src/db/rules-client.ts
  # DELETE - hooks
  - src/hooks/useRulesSync.ts
  - src/hooks/useRulesExtended.ts
  - src/hooks/useSyncErrors.ts
  - src/hooks/useUnitRulesMapping.ts
  # DELETE - sync utilities
  - src/lib/parseWahapediaCsv.ts
  - src/lib/validateCsvHeaders.ts
  - src/lib/computeSyncDiff.ts
  - src/lib/syncFreshness.ts
  - src/lib/normalizePointsNames.ts
  # DELETE - UI components
  - src/features/rules-hub/SyncStatusCard.tsx
  - src/features/rules-hub/PointsDeltaSection.tsx
  - src/features/units/PlaybookSyncDetails.tsx
  # DELETE - Rust migrations
  - src-tauri/migrations/rules_001_schema.sql
  - src-tauri/migrations/rules_002_wargear_abilities.sql
  - src-tauri/migrations/rules_003_sync_meta_counts.sql
  - src-tauri/migrations/rules_004_datasheet_points.sql
  # DELETE - types
  - src/types/pointsDelta.ts
  - src/types/unitRulesMapping.ts
  # DELETE - tests
  - tests/datasheet/computePointsDelta.test.ts
  - tests/datasheet/DatasheetImportDialog.test.tsx
  - tests/datasheet/datasheetQueries.test.ts
  - tests/datasheet/fetchBsdataPoints.test.ts
  - tests/datasheet/migration.test.ts
  - tests/datasheet/rulesExtendedDetachment.test.ts
  - tests/datasheet/rulesExtendedQueries.test.ts
  - tests/datasheet/rulesSnapshot.test.ts
  - tests/datasheet/syncErrorQueries.test.ts
  - tests/datasheet/syncFreshness.test.ts
  - tests/datasheet/syncMetaQueries.test.ts
  - tests/datasheet/useRulesExtended.test.tsx
  - tests/datasheet/validateCsvHeaders.test.ts
  - tests/datasheet/pointsSchema.test.ts
  - tests/datasheet/computeSyncDiff.test.ts
  - tests/datasheet/csvParse.test.ts
  - tests/datasheet/useRulesSync.test.ts
  - tests/datasheet/DatasheetPicker.test.tsx
  - tests/datasheet/useDatasheet.test.tsx
  - tests/rules-sync/useRulesSync.test.ts
  # MODIFY - hooks
  - src/hooks/useDatasheet.ts
  - src/hooks/useUnitKeywords.ts
  # NEW - hook
  - src/hooks/useUdbMeta.ts
  # MODIFY - queries
  - src/db/queries/diagnostics.ts
  # MODIFY - UI components (redirect consumers)
  - src/features/rules-hub/RulesHubPage.tsx
  - src/features/units/PlaybookTab.tsx
  - src/features/units/PlaybookStats.tsx
  - src/features/units/PlaybookRules.tsx
  - src/features/units/DatasheetPicker.tsx
  - src/features/units/UnitFormRequired.tsx
  - src/features/army-lists/DatasheetBrowserDialog.tsx
  - src/features/army-lists/DetachmentPicker.tsx
  - src/features/army-lists/ArmyListDetailPage.tsx
  - src/features/army-lists/ArmyListDetailSheet.tsx
  - src/features/army-lists/ArmyListUnitRow.tsx
  - src/features/army-lists/RulesMappingSheet.tsx
  - src/features/army-lists/PointsFreshnessBadge.tsx
  - src/features/army-lists/DetachmentRulesSection.tsx
  - src/features/data-health/VersionInfoCard.tsx
  - src/features/data-health/DiagnosticsCard.tsx
  - src/features/dashboard/DataHealthSummaryCard.tsx
  - src/features/dashboard/ReadyToPlayCard.tsx
  - src/features/game-day/GameDayPage.tsx
  - src/features/game-day/StrategemsTab.tsx
  - src/features/game-day/UnitAbilityCard.tsx
  - src/features/rules-hub/DatasheetPointsTab.tsx
  - src/features/rules-hub/DetachmentCard.tsx
  - src/features/rules-hub/StratagemCard.tsx
  # MODIFY - Rust backend
  - src-tauri/src/lib.rs
  # MODIFY - config
  - src-tauri/tauri.conf.json
autonomous: true
requirements:
  - CLN-02
  - CLN-03

must_haves:
  truths:
    - "App compiles with zero references to getRulesDb or rules-client"
    - "All features that read unit data use udb_* tables via getDb()"
    - "TypeScript build passes with no errors after all deletions"
    - "Existing tests pass (minus deleted test files)"
    - "Rust compiles with no rules.db migration registration or bulk_sync_rules"
  artifacts:
    - path: "src/hooks/useUdbMeta.ts"
      provides: "Replacement for useRulesSyncMeta across 11 consumers"
      exports: ["useUdbMeta", "UDB_META_KEY"]
    - path: "src/hooks/useDatasheet.ts"
      provides: "Redirected hooks using unitDatabase.ts queries"
    - path: "src/hooks/useUnitKeywords.ts"
      provides: "Redirected keyword lookup using udb_unit_keywords"
    - path: "src/db/queries/diagnostics.ts"
      provides: "Single-DB diagnostic queries"
  key_links:
    - from: "src/hooks/useDatasheet.ts"
      to: "src/db/queries/unitDatabase.ts"
      via: "import redirection"
      pattern: "import.*unitDatabase"
    - from: "src/hooks/useUdbMeta.ts"
      to: "src/db/client.ts"
      via: "getDb() query on udb_meta"
      pattern: "getDb.*udb_meta"
---

<objective>
Eliminate rules.db entirely: delete all dead sync infrastructure (~30 files), redirect surviving hooks to udb_* queries in hobbyforge.db, clean Rust backend, and ensure single-database architecture compiles and passes tests.

Purpose: Complete the architectural migration from dual-database (hobbyforge.db + rules.db) to single-database (hobbyforge.db with canonical udb_* tables). This removes the entire CSV sync pipeline, the rules.db connection singleton, all rw_* table queries, and ~20 dead test files.

Output: Single-database app that compiles cleanly with all features working via udb_* tables. No file, import, or runtime reference to rules.db remains.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-cleanup-pipeline/107-CONTEXT.md
@.planning/phases/107-cleanup-pipeline/107-RESEARCH.md
@.planning/phases/107-cleanup-pipeline/107-PATTERNS.md

<interfaces>
<!-- Redirect targets: the canonical udb_* query layer and hooks -->

From src/db/queries/unitDatabase.ts (canonical replacement for datasheets.ts):
  export async function getUdbFactions(): Promise<UdbFaction[]>
  export async function getUdbUnitsByFaction(factionId: string): Promise<UdbUnitSummary[]>
  export async function getUdbUnitDetail(unitId: string): Promise<UdbUnitDetail | null>
  export async function searchUdbUnits(query: string): Promise<UdbSearchResult[]>
  export async function getUdbOwnershipByFaction(factionId: string): Promise<UdbOwnershipEntry[]>

From src/hooks/useUnitDatabase.ts (canonical hooks):
  export const UDB_FACTIONS_KEY = ["udb-factions"] as const
  export const UDB_UNITS_KEY = (factionId: string) => ["udb-units", factionId] as const
  export const UDB_UNIT_DETAIL_KEY = (unitId: string) => ["udb-unit-detail", unitId] as const
  export function useUdbFactions()
  export function useUdbUnits(factionId: string | null)
  export function useUdbUnitDetail(unitId: string | null)
  export function useUdbSearch(query: string)
  export function useUdbOwnership(factionId: string | null)

From src/db/client.ts:
  export async function getDb(): Promise<Database>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useUdbMeta hook, redirect useDatasheet and useUnitKeywords, modify diagnostics</name>
  <files>
    src/hooks/useUdbMeta.ts (NEW)
    src/hooks/useDatasheet.ts (REWRITE)
    src/hooks/useUnitKeywords.ts (REWRITE)
    src/db/queries/diagnostics.ts (MODIFY)
  </files>
  <read_first>
    src/hooks/useUnitDatabase.ts (analog for hook pattern, cache keys, staleTime: Infinity)
    src/hooks/useDatasheet.ts (current state — 7 exports, all importing from datasheets.ts)
    src/hooks/useUnitKeywords.ts (current state — imports getUnitKeywords from datasheets.ts)
    src/db/queries/diagnostics.ts (current state — imports getRulesDb, has getSchemaVersions, getUnmatchedPointsCount)
    src/db/queries/unitDatabase.ts (replacement query functions)
    src/db/client.ts (getDb singleton)
  </read_first>
  <action>
    1. CREATE src/hooks/useUdbMeta.ts: Export UDB_META_KEY = ["udb-meta"] and useUdbMeta() hook. The hook queries udb_meta table via getDb() for columns: version, built_at, game_system, unit_count, faction_count. Use staleTime: Infinity. Return rows[0] ?? null. This replaces useRulesSyncMeta across 11 consumer components.

    2. REWRITE src/hooks/useDatasheet.ts: Remove ALL imports from @/db/queries/datasheets. Redirect each export:
       - useDatasheet(unitId: number | undefined): Query the unit's udb_unit_id from units table via getDb(), then call getUdbUnitDetail(udbUnitId) from unitDatabase.ts. Return null when unit has no udb_unit_id.
       - useDatasheetsByFaction(factionId: string | undefined): Redirect to getUdbUnitsByFaction(factionId) from unitDatabase.ts.
       - useDatasheetsByFactionWithPoints(factionId: string | undefined): Same redirect as above (udb units already include points).
       - useWahapediaFactions(): Redirect to getUdbFactions() from unitDatabase.ts.
       - useWahapediaFactionId(localFactionName: string | undefined): Query getUdbFactions() and find by name match. The udb_factions table uses the same Wahapedia IDs (Phase 103 decision).
       - REMOVE useRulesSyncMeta entirely — consumers will import useUdbMeta instead.
       - Keep all existing cache key exports (DATASHEET_KEY, DATASHEETS_BY_FACTION_KEY, WAHAPEDIA_FACTIONS_KEY, WAHAPEDIA_FACTION_KEY) for backward compatibility.

    3. REWRITE src/hooks/useUnitKeywords.ts: Replace import from @/db/queries/datasheets with a direct getDb() query against udb_unit_keywords table. The SQL joins udb_units u JOIN udb_unit_keywords k ON k.unit_id = u.id WHERE LOWER(u.name) = LOWER($1) AND LOWER(k.keyword) IN ('character', 'epic hero'). Keep the UnitKeywordStatus interface and SAFE_DEFAULT export.

    4. MODIFY src/db/queries/diagnostics.ts:
       - Remove the import of getRulesDb from @/db/rules-client.
       - Change SchemaVersions interface: remove the rules property, keep only hobbyforge: number.
       - Rewrite getSchemaVersions(): query only getDb() for PRAGMA user_version. Remove the getRulesDb() call.
       - DELETE the getUnmatchedPointsCount() function entirely (queries rw_datasheet_points in rules.db).
       - MODIFY getDiagnosticFlags(): remove getUnmatchedPointsCount from the Promise.all array.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useUdbMeta.ts exports useUdbMeta and UDB_META_KEY
    - src/hooks/useDatasheet.ts has zero imports from @/db/queries/datasheets
    - src/hooks/useDatasheet.ts does NOT export useRulesSyncMeta
    - src/hooks/useUnitKeywords.ts has zero imports from @/db/queries/datasheets
    - src/hooks/useUnitKeywords.ts queries udb_unit_keywords table
    - src/db/queries/diagnostics.ts has zero imports from @/db/rules-client
    - src/db/queries/diagnostics.ts does not contain getUnmatchedPointsCount function
    - SchemaVersions interface has no rules property
  </acceptance_criteria>
  <done>All 4 hook/query files compile cleanly with zero references to rules-client or datasheets queries. useUdbMeta is the canonical replacement for useRulesSyncMeta.</done>
</task>

<task type="auto">
  <name>Task 2: Delete all dead files, redirect all consumer components, clean Rust backend</name>
  <files>
    # ~30 files DELETED (see files_modified list above)
    # ~20 component files MODIFIED to swap imports
    # src-tauri/src/lib.rs MODIFIED
    # src-tauri/tauri.conf.json MODIFIED
  </files>
  <read_first>
    src-tauri/src/lib.rs (Rust backend — find get_rules_migrations, bulk_sync_rules, BulkSyncPayload, SyncResult, .add_migrations("sqlite:rules.db", ...), preflight_migration_repair rules.db block, generate_handler! macro, BackupManifest rules_schema_version)
    src-tauri/tauri.conf.json (line 52: "sqlite:rules.db")
    src/features/rules-hub/RulesHubPage.tsx (imports SyncStatusCard, useRulesSyncMeta, useWahapediaFactions, useRulesExtended hooks)
    src/features/units/PlaybookTab.tsx (imports useDatasheet, useRulesSyncMeta, useRulesSync, useSyncErrors, useRulesExtended)
    src/features/army-lists/ArmyListDetailPage.tsx (imports useWahapediaFactionId, useRulesSyncMeta)
    src/features/army-lists/ArmyListDetailSheet.tsx (imports useWahapediaFactionId, useRulesSyncMeta)
    src/features/army-lists/ArmyListUnitRow.tsx (imports useUnitRulesMapping)
    src/features/army-lists/RulesMappingSheet.tsx (imports useUnitRulesMapping)
    src/features/army-lists/DetachmentPicker.tsx (imports useDetachmentsByFaction)
    src/features/army-lists/DetachmentRulesSection.tsx (imports useRulesExtended)
    src/features/units/UnitFormRequired.tsx (imports useDatasheetRole from useUnitRulesMapping)
    src/features/data-health/VersionInfoCard.tsx (imports useRulesSyncMeta, useRulesSyncErrors)
    src/features/data-health/DiagnosticsCard.tsx (imports useRulesSyncMeta)
    src/features/dashboard/DataHealthSummaryCard.tsx (imports useRulesSyncMeta)
    src/features/dashboard/ReadyToPlayCard.tsx (imports useRulesSyncMeta)
    src/features/game-day/GameDayPage.tsx (imports useRulesSyncMeta)
    src/features/game-day/StrategemsTab.tsx (imports useStratagemsByDetachment)
  </read_first>
  <action>
    Execute the deletion and redirection in dependency order (leaf-first):

    **A. Delete all leaf files (no other source file depends on these):**
    Delete these files in one batch:
    - 5 sync utilities: parseWahapediaCsv.ts, validateCsvHeaders.ts, computeSyncDiff.ts, syncFreshness.ts, normalizePointsNames.ts (per D-06)
    - 5 dead query modules: syncErrors.ts, rulesSnapshot.ts, pointsImportHistory.ts, unitRulesMapping.ts, datasheets.ts (per D-07, D-08, D-09, D-15, D-11)
    - 1 dead query module: rulesExtended.ts (per D-12)
    - 3 dead UI components: SyncStatusCard.tsx, PointsDeltaSection.tsx, PlaybookSyncDetails.tsx (per D-17)
    - 4 dead hooks: useRulesSync.ts, useRulesExtended.ts, useSyncErrors.ts, useUnitRulesMapping.ts (per D-05, D-14)
    - 1 dead db client: rules-client.ts (per D-01)
    - 2 dead type files: pointsDelta.ts, unitRulesMapping.ts (per D-10)
    - 4 Rust migration files: rules_001 through rules_004 (per D-03)
    - ~20 dead test files under tests/datasheet/ and tests/rules-sync/ (keep rulesFavorites.test.ts, rulesNotes.test.ts, useRulesFavorites.test.tsx, useRulesNotes.test.tsx, stripHtml.test.ts)

    **B. Redirect all consumer components (swap imports):**

    For the 11 components using useRulesSyncMeta: Replace import { useRulesSyncMeta } from "@/hooks/useDatasheet" with import { useUdbMeta } from "@/hooks/useUdbMeta". Change const { data: syncMeta } to const { data: udbMeta }. Update property access from syncMeta?.last_synced to udbMeta?.built_at, from syncMeta?.datasheet_count to udbMeta?.unit_count. Affected files:
    - ArmyListDetailPage.tsx, ArmyListDetailSheet.tsx, PointsFreshnessBadge.tsx, PlaybookTab.tsx, RulesHubPage.tsx, DataHealthSummaryCard.tsx, ReadyToPlayCard.tsx, DiagnosticsCard.tsx, VersionInfoCard.tsx, GameDayPage.tsx

    For useWahapediaFactionId consumers (ArmyListDetailPage.tsx, ArmyListDetailSheet.tsx, DatasheetBrowserDialog.tsx): useWahapediaFactionId is still exported from useDatasheet.ts (redirected in Task 1). No import path change needed, just verify it works.

    For useUnitRulesMapping consumers (ArmyListUnitRow.tsx, RulesMappingSheet.tsx): The unit_rules_mapping table and queries are being deleted. ArmyListUnitRow.tsx should remove the useUnitRulesMapping call and its dependent logic (rules mapping indicators). RulesMappingSheet.tsx should either be deleted or stubbed to show "Feature removed" since there is no rules.db to map to. The udb_unit_id FK on units is the canonical link now.

    For useDatasheetRole consumer (UnitFormRequired.tsx): Replace import { useDatasheetRole } from "@/hooks/useUnitRulesMapping" with a direct query of the unit's udb_unit_id joined to udb_units.role. Write a small inline hook or add a getUdbRoleForUnit function to unitDatabase.ts.

    For useRulesExtended consumers: DetachmentPicker.tsx, DetachmentRulesSection.tsx, PlaybookRules.tsx, StrategemsTab.tsx, DetachmentCard.tsx, StratagemCard.tsx, PlaybookTab.tsx — remove all useRulesExtended imports. For detachment/stratagem features, degrade gracefully: return empty arrays, hide sections, or show "Detachment rules coming in a future update" placeholder text. These features lose their data source (EXT-03 deferred).

    For useRulesSync consumers (PlaybookTab.tsx, SyncStatusCard.tsx — SyncStatusCard already deleted): Remove import and all sync trigger UI (sync button, sync progress, sync error display).

    For useSyncErrors consumers (PlaybookTab.tsx, VersionInfoCard.tsx): Remove import and sync error display sections.

    For RulesHubPage.tsx (per D-16): Remove all sync controls (SyncStatusCard import, sync state, PointsDeltaSection). Simplify to render a redirect to DatabaseBrowserPage or inline the DatabaseBrowserPage content. Keep the route and sidebar entry.

    For DatasheetPointsTab.tsx: Redirect from datasheets.ts queries to unitDatabase.ts queries.

    **C. Clean Rust backend (per D-02):**
    In src-tauri/src/lib.rs:
    - DELETE the get_rules_migrations() function (defines rules_001 through rules_004 Migration structs)
    - DELETE the BulkSyncPayload and SyncResult structs
    - DELETE the entire bulk_sync_rules async function
    - REMOVE bulk_sync_rules from the generate_handler![] macro
    - REMOVE .add_migrations("sqlite:rules.db", get_rules_migrations()) from the plugin builder
    - In preflight_migration_repair(): remove the rules_db path, rules_migrations variable, and the repair_migration_checksums call for rules.db
    - In create_backup_manifest(): change rules_schema_version from get_rules_migrations().len() as u32 to 0. Keep the field with #[serde(default)] for backward compat of old backup deserialization.
    - Keep includes_rules_db field in BackupManifest with #[serde(default)] — set to false in new manifests.

    **D. Clean tauri.conf.json (per D-04):**
    Remove "sqlite:rules.db" from the preload array at line 52. Result: preload array contains only "sqlite:hobbyforge.db".
  </action>
  <verify>
    <automated>pnpm build && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "getRulesDb" src/ returns 0 results
    - grep -r "rules-client" src/ returns 0 results
    - grep -r "useRulesSync\b" src/ returns 0 results (not matching useRulesSyncMeta or similar)
    - grep -r "useRulesExtended" src/ returns 0 results
    - grep -r "useSyncErrors" src/ returns 0 results
    - grep -r "useUnitRulesMapping" src/ returns 0 results
    - grep -r "bulk_sync_rules" src-tauri/ returns 0 results
    - grep -r "get_rules_migrations" src-tauri/ returns 0 results
    - grep "sqlite:rules.db" src-tauri/tauri.conf.json returns 0 results
    - rules_001_schema.sql through rules_004_datasheet_points.sql do not exist in src-tauri/migrations/
    - src/db/rules-client.ts does not exist
    - pnpm build exits 0
    - pnpm test exits 0
  </acceptance_criteria>
  <done>Zero references to rules.db anywhere in source. TypeScript and Rust compile cleanly. All tests pass. Single-database architecture achieved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This phase only deletes code and redirects queries to existing trusted paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Tampering | BackupManifest backward compat | mitigate | Keep rules_schema_version and includes_rules_db fields with #[serde(default)] so old backups deserialize correctly |
| T-107-02 | Denial of Service | Startup crash if rules.db reference remains | mitigate | Remove both tauri.conf.json preload entry AND Rust .add_migrations() call in same commit |
| T-107-03 | Information Disclosure | Dead sync error logs exposing data | accept | Sync error table stays as empty table (deferred DROP); no runtime writes |
</threat_model>

<verification>
1. `pnpm build` passes (TypeScript + Vite)
2. `pnpm test` passes (remaining tests)
3. `grep -r "getRulesDb" src/` returns empty
4. `grep -r "rules-client" src/` returns empty
5. `grep -r "bulk_sync_rules" src-tauri/` returns empty
6. `grep "sqlite:rules.db" src-tauri/tauri.conf.json` returns empty
7. Files rules-client.ts, datasheets.ts, rulesExtended.ts, useRulesSync.ts, useRulesExtended.ts, useSyncErrors.ts, useUnitRulesMapping.ts do not exist
</verification>

<success_criteria>
- App architecture uses single hobbyforge.db with udb_* tables for all unit data
- Zero source files reference getRulesDb, rules-client, or rw_* tables
- Rust backend has no bulk_sync_rules command, no rules.db migration registration
- TypeScript and Rust compilation both pass cleanly
- All surviving tests pass
- Features that consumed rules.db data now use udb_* equivalents or degrade gracefully
</success_criteria>

<output>
Create `.planning/phases/107-cleanup-pipeline/107-01-SUMMARY.md` when done
</output>

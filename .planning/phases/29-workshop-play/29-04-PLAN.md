---
phase: 29-workshop-play
plan: 04
type: execute
wave: 3
depends_on:
  - 29-02
  - 29-03
files_modified: []
autonomous: false
requirements:
  - WKSP-01
  - WKSP-02
  - PLAY-01
  - PLAY-02
must_haves:
  truths:
    - "All 4 Phase 29 requirements verified in the live Tauri app"
    - "Paint swatches render correctly for hex and non-hex paints"
    - "Recipe swatch strip shows overlapping circles matching paint colors"
    - "Army List readiness panel shows progress bar and not-ready list"
    - "Battle Log shows live readiness points that update when painting status changes"
  artifacts: []
  key_links: []
---

<objective>
Manual smoke test verifying all four Phase 29 requirements (WKSP-01, WKSP-02, PLAY-01, PLAY-02) work correctly in the live Tauri app.

Purpose: Automated tests cover logic and rendering. This checkpoint confirms visual rendering, data integration, and end-to-end flows in the real desktop app.
Output: Human confirmation that all 4 requirements are met.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-workshop-play/29-CONTEXT.md
@.planning/phases/29-workshop-play/29-02-SUMMARY.md
@.planning/phases/29-workshop-play/29-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build check</name>
  <files></files>
  <read_first>
    - tests/workshop-play/paintRowSwatch.test.tsx (verify all stubs are flipped)
    - tests/workshop-play/recipeSwatchData.test.ts (verify all stubs are flipped)
    - tests/workshop-play/armyListReadinessPanel.test.tsx (verify all stubs are flipped)
    - tests/workshop-play/armyListReadiness.test.ts (verify all stubs are flipped)
  </read_first>
  <action>
Run the complete test suite and TypeScript build to confirm no regressions:

1. Run `pnpm test` — full suite must pass with zero failures
2. Run `pnpm build` — TypeScript type-check + Vite build must succeed
3. Verify zero `it.skip` remain in tests/workshop-play/ — grep for "it.skip" and assert count is 0
4. Start the Tauri dev server with `pnpm tauri dev` for the human verification step
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test exits 0 with zero failures
    - pnpm build exits 0 with zero TypeScript errors
    - grep for "it.skip" in tests/workshop-play/ returns zero matches
  </acceptance_criteria>
  <done>Full suite green, build passes, zero skipped stubs, Tauri dev server running</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual smoke test of all Phase 29 features</name>
  <files></files>
  <action>
Present the 21-step smoke test checklist to the user and wait for human verification of all four Phase 29 requirements in the live Tauri app. The app should already be running from Task 1.

Smoke test steps cover:
- WKSP-01: Paint swatch rendering on Paints page (steps 1-5)
- WKSP-02: Recipe swatch strip on Recipes page (steps 6-10)
- PLAY-01: Army List readiness panel (steps 11-16)
- PLAY-02: Battle Log live readiness points (steps 17-21)
  </action>
  <verify>
    <automated>echo "Human verification checkpoint — no automated command"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms WKSP-01: paint swatches render correctly for hex and non-hex paints
    - User confirms WKSP-02: recipe swatch strip shows overlapping circles
    - User confirms PLAY-01: readiness panel shows progress bar and not-ready list
    - User confirms PLAY-02: live readiness points update when painting status changes
  </acceptance_criteria>
  <done>User types "approved" confirming all 4 requirements verified in live app</done>
  <what-built>
Phase 29 Workshop + Play enrichments:
- WKSP-01: Paint color swatches in Paints page table rows
- WKSP-02: Recipe swatch strip (overlapping paint circles) in Recipes page table
- PLAY-01: Army List readiness panel (progress bar + not-ready list + 100% gold message)
- PLAY-02: Battle Log live readiness points inline with army name
  </what-built>
  <how-to-verify>
**WKSP-01 — Paint Swatches (Paints page):**
1. Navigate to Workshop > Paints
2. Verify each paint row shows a small colored circle next to the paint name
3. Confirm paints with hex colors show the correct fill color
4. Confirm paints without hex colors show a gray/muted placeholder circle
5. Verify all swatches are the same size (16px circles, rounded)

**WKSP-02 — Recipe Swatch Strip (Recipes page):**
6. Navigate to Workshop > Recipes
7. Verify each recipe row shows a "Palette" column with small overlapping circles
8. Confirm the circles match the colors of the paints in the recipe
9. If a recipe has 8+ paints, verify a "+N" indicator appears
10. Confirm recipes with 0 paints show "--" in the Palette column

**PLAY-01 — Army List Readiness Panel:**
11. Navigate to Play > Army Lists
12. Click on an army list that has some unpainted units
13. Verify the summary bar shows Total pts, Painted pts, Battle-ready % stats
14. Verify a thin gold progress bar appears below the stats
15. Verify a "Not ready" section lists units that are not "Completed" with their current status badge
16. If all units are "Completed", verify a gold "All units battle-ready" message appears instead

**PLAY-02 — Battle Log Readiness Points:**
17. Navigate to Play > Battle Log
18. Find a battle log entry linked to an army list
19. Verify it shows "armyListName (X/Y pts ready) . date" on line 2
20. Go back to the Collection page, change a unit's painting status (e.g. mark as Completed)
21. Navigate back to Battle Log and verify the readiness points updated to reflect the change
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 29, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Full test suite green (pnpm test)
- Build passes (pnpm build)
- Human confirms all 4 requirements work visually in the live app
</verification>

<success_criteria>
- Zero test failures, zero build errors
- Human approves WKSP-01: consistent swatches on Paints page
- Human approves WKSP-02: swatch strip on Recipes page
- Human approves PLAY-01: readiness panel in Army List detail
- Human approves PLAY-02: live readiness points in Battle Log
</success_criteria>

<output>
After completion, create `.planning/phases/29-workshop-play/29-04-SUMMARY.md`
</output>

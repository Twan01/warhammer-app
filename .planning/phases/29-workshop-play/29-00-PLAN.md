---
phase: 29-workshop-play
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/workshop-play/paintRowSwatch.test.tsx
  - tests/workshop-play/recipeSwatchData.test.ts
  - tests/workshop-play/armyListReadinessPanel.test.tsx
  - tests/workshop-play/armyListReadiness.test.ts
autonomous: true
requirements:
  - WKSP-01
  - WKSP-02
  - PLAY-01
  - PLAY-02
must_haves:
  truths:
    - "4 test stub files exist under tests/workshop-play/ with it.skip stubs covering all 4 requirements"
    - "Each stub file documents the exact assertions Wave 1/2 plans will write"
    - "pnpm test passes with all stubs skipped (zero failures)"
  artifacts:
    - path: "tests/workshop-play/paintRowSwatch.test.tsx"
      provides: "WKSP-01 swatch rendering test stubs"
      contains: "it.skip"
    - path: "tests/workshop-play/recipeSwatchData.test.ts"
      provides: "WKSP-02 batch query + hook + swatch strip stubs"
      contains: "it.skip"
    - path: "tests/workshop-play/armyListReadinessPanel.test.tsx"
      provides: "PLAY-01 readiness panel test stubs"
      contains: "it.skip"
    - path: "tests/workshop-play/armyListReadiness.test.ts"
      provides: "PLAY-02 batch query + hook + row integration stubs"
      contains: "it.skip"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all four Phase 29 requirements (WKSP-01, WKSP-02, PLAY-01, PLAY-02).

Purpose: Establish the Nyquist contract — every it.skip stub names the exact assertion Wave 1/2 plans will fill in, serving as executable specifications. Wave 0 stubs use `it.skip` (not `xit`/`xtest`) per Phase 26 decision.
Output: 4 test files under tests/workshop-play/ with all stubs skipped and pnpm test green.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-workshop-play/29-CONTEXT.md
@.planning/phases/29-workshop-play/29-RESEARCH.md
@.planning/phases/29-workshop-play/29-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WKSP-01 + WKSP-02 test stub files</name>
  <files>tests/workshop-play/paintRowSwatch.test.tsx, tests/workshop-play/recipeSwatchData.test.ts</files>
  <read_first>
    - src/features/paints/PaintRow.tsx (swatch rendering lines 29-38 — verify current pattern)
    - src/db/queries/recipePaints.ts (existing query signatures)
    - src/hooks/useRecipePaints.ts (existing hook exports)
    - tests/battle-log/battleLogQueries.test.ts (Wave 0 stub pattern reference — it.skip + TODO comments)
  </read_first>
  <action>
Create directory tests/workshop-play/ if it does not exist.

**File 1: tests/workshop-play/paintRowSwatch.test.tsx**

Create a test stub file with:
- Top-level `import { describe, it } from "vitest";`
- A file-level JSDoc comment: `/** Wave 0 stubs for WKSP-01 — PaintRow swatch consistency. Plan 29-02 fills these. */`
- `describe.skip("PaintRow swatch rendering (WKSP-01)", () => { ... })` with these it.skip stubs:
  1. `it.skip("renders a colored swatch span with backgroundColor when paint has hex_color")` — TODO Plan 29-02: render PaintRow with paint.hex_color = "#FF0000", assert span has style backgroundColor "#FF0000" and classes "h-4 w-4 rounded-full"
  2. `it.skip("renders a muted fallback swatch when paint has no hex_color")` — TODO Plan 29-02: render PaintRow with paint.hex_color = null, assert span has class "bg-muted" and no inline backgroundColor
  3. `it.skip("swatch span is 16x16px (h-4 w-4) with rounded-full and border")` — TODO Plan 29-02: assert swatch span has classes including "h-4", "w-4", "rounded-full", "border", "border-border"

**File 2: tests/workshop-play/recipeSwatchData.test.ts**

Create a test stub file with:
- Top-level `import { describe, it } from "vitest";`
- A file-level JSDoc comment: `/** Wave 0 stubs for WKSP-02 — recipe swatch data + strip UI. Plan 29-01 fills query/hook stubs. Plan 29-02 fills UI stubs. */`
- `describe.skip("getRecipeSwatchColors (WKSP-02)", () => { ... })` with:
  1. `it.skip("returns flat array with recipe_id, paint_id, hex_color ordered by recipe_id, order_index")` — TODO Plan 29-01: mock getDb, call getRecipeSwatchColors(), assert db.select called with correct JOIN SQL
  2. `it.skip("returns empty array when no recipe_paints exist")` — TODO Plan 29-01: mock getDb returning [], assert empty result
- `describe.skip("useRecipeSwatchData hook (WKSP-02)", () => { ... })` with:
  3. `it.skip("maps flat rows into Map<recipe_id, SwatchEntry[]>")` — TODO Plan 29-01: mock getRecipeSwatchColors, call hook, assert Map has correct grouping
  4. `it.skip("returns empty Map when no data")` — TODO Plan 29-01: mock empty return, assert Map.size === 0
- `describe.skip("Recipe swatch strip rendering (WKSP-02)", () => { ... })` with:
  5. `it.skip("renders up to 8 swatch circles for a recipe's paints")` — TODO Plan 29-02: render RecipeTable with swatchColorsByRecipe Map, assert 8 swatch spans
  6. `it.skip("renders +N overflow indicator when recipe has more than 8 paints")` — TODO Plan 29-02: pass 10 paints, assert "+2" text visible
  7. `it.skip("renders bg-muted fallback circle for paints without hex_color")` — TODO Plan 29-02: pass paint with null hex_color, assert bg-muted class
  8. `it.skip("applies negative margin (-ml-1) on second and subsequent swatches")` — TODO Plan 29-02: render strip with 3 paints, assert second span has "-ml-1" class
  </action>
  <verify>
    <automated>pnpm test -- tests/workshop-play/paintRowSwatch.test.tsx tests/workshop-play/recipeSwatchData.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/workshop-play/paintRowSwatch.test.tsx exists and contains `describe.skip("PaintRow swatch rendering (WKSP-01)"`
    - tests/workshop-play/paintRowSwatch.test.tsx contains exactly 3 `it.skip` calls
    - tests/workshop-play/recipeSwatchData.test.ts exists and contains 3 `describe.skip` blocks
    - tests/workshop-play/recipeSwatchData.test.ts contains exactly 8 `it.skip` calls
    - Both files contain `TODO Plan 29-` comments naming which plan fills each stub
    - pnpm test exits 0 (all stubs skipped, no failures)
  </acceptance_criteria>
  <done>2 test stub files with 11 it.skip stubs covering WKSP-01 and WKSP-02, pnpm test green</done>
</task>

<task type="auto">
  <name>Task 2: Create PLAY-01 + PLAY-02 test stub files</name>
  <files>tests/workshop-play/armyListReadinessPanel.test.tsx, tests/workshop-play/armyListReadiness.test.ts</files>
  <read_first>
    - src/features/army-lists/ArmyListSummaryBar.tsx (current component structure — lines 21-56)
    - src/db/queries/armyLists.ts (existing query functions)
    - src/hooks/useArmyLists.ts (existing hook exports and key patterns)
    - src/features/battle-log/BattleLogRow.tsx (current line 2 rendering — lines 82-92)
    - src/components/ui/status-badge.tsx (StatusBadge API — import path and props)
  </read_first>
  <action>
**File 3: tests/workshop-play/armyListReadinessPanel.test.tsx**

Create a test stub file with:
- Top-level `import { describe, it } from "vitest";`
- File-level JSDoc: `/** Wave 0 stubs for PLAY-01 — ArmyListSummaryBar readiness panel upgrade. Plan 29-03 fills these. */`
- `describe.skip("ArmyListSummaryBar readiness panel (PLAY-01)", () => { ... })` with:
  1. `it.skip("renders a progress bar with width matching battleReadyPct")` — TODO Plan 29-03: render with 2 units (1 Completed, 1 not), assert progress bar div style.width = "50%"
  2. `it.skip("progress bar uses bg-battle-gold class for the filled portion")` — TODO Plan 29-03: assert inner progress div has class "bg-battle-gold"
  3. `it.skip("renders not-ready unit list with StatusBadge per non-Completed unit")` — TODO Plan 29-03: render with 2 units where 1 is "Primed", assert unit_name and StatusBadge visible
  4. `it.skip("hides not-ready list when all units are Completed")` — TODO Plan 29-03: render with all Completed units, assert "All units battle-ready" text visible
  5. `it.skip("shows gold-tinted 'All units battle-ready' message at 100%")` — TODO Plan 29-03: assert element with "All units battle-ready" text has class "text-battle-gold"
  6. `it.skip("still renders existing stat row (Total, Painted, Battle-ready)")` — TODO Plan 29-03: render with units, assert "Total" and "Painted" and "Battle-ready" labels present

**File 4: tests/workshop-play/armyListReadiness.test.ts**

Create a test stub file with:
- Top-level `import { describe, it } from "vitest";`
- File-level JSDoc: `/** Wave 0 stubs for PLAY-02 — army list readiness batch query + hook + BattleLogRow display. Plan 29-01 fills query/hook stubs. Plan 29-03 fills UI stubs. */`
- `describe.skip("getArmyListReadiness query (PLAY-02)", () => { ... })` with:
  1. `it.skip("returns total_points and battle_ready_points per army list id")` — TODO Plan 29-01: mock getDb, call with [1,2], assert db.select called with GROUP BY SQL and IN clause
  2. `it.skip("returns empty array when called with empty ids array")` — TODO Plan 29-01: call with [], assert returns [] without hitting db
  3. `it.skip("uses COALESCE(points_override, points, 0) for effective points in SQL")` — TODO Plan 29-01: assert SQL string contains COALESCE
  4. `it.skip("uses status_painting = 'Completed' for battle-ready check in SQL")` — TODO Plan 29-01: assert SQL string contains "'Completed'" (not 'Complete')
- `describe.skip("useArmyListReadiness hook (PLAY-02)", () => { ... })` with:
  5. `it.skip("maps query rows into Map<id, { total, battleReady }>")` — TODO Plan 29-01: mock getArmyListReadiness, call hook, assert Map structure
  6. `it.skip("is disabled when ids array is empty (Pitfall 2)")` — TODO Plan 29-01: call with [], assert enabled: false
  7. `it.skip("sorts ids in query key for stable caching (Pitfall 7)")` — TODO Plan 29-01: call with [3,1,2], assert query key contains [1,2,3]
- `describe.skip("BattleLogRow readiness display (PLAY-02)", () => { ... })` with:
  8. `it.skip("renders '(battleReady/total pts ready)' when armyListReadiness is provided")` — TODO Plan 29-03: render BattleLogRow with armyListReadiness={total:200, battleReady:120}, assert text "(120/200 pts ready)"
  9. `it.skip("renders tabular-nums class on readiness point values")` — TODO Plan 29-03: assert span containing pts has "tabular-nums" class
  10. `it.skip("renders no readiness info when armyListReadiness is null")` — TODO Plan 29-03: render with armyListReadiness=null, assert no "pts ready" text
  11. `it.skip("still renders '(Army list deleted)' when army_list_id is set but armyListName is null")` — TODO Plan 29-03: render with army_list_id=5, armyListName=null, assert italic "(Army list deleted)"
  </action>
  <verify>
    <automated>pnpm test -- tests/workshop-play/armyListReadinessPanel.test.tsx tests/workshop-play/armyListReadiness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/workshop-play/armyListReadinessPanel.test.tsx exists and contains `describe.skip("ArmyListSummaryBar readiness panel (PLAY-01)"`
    - tests/workshop-play/armyListReadinessPanel.test.tsx contains exactly 6 `it.skip` calls
    - tests/workshop-play/armyListReadiness.test.ts exists and contains 3 `describe.skip` blocks
    - tests/workshop-play/armyListReadiness.test.ts contains exactly 11 `it.skip` calls
    - Both files contain `TODO Plan 29-` comments naming which plan fills each stub
    - pnpm test exits 0 (all stubs skipped, no failures)
  </acceptance_criteria>
  <done>2 test stub files with 17 it.skip stubs covering PLAY-01 and PLAY-02, pnpm test green</done>
</task>

</tasks>

<verification>
- All 4 test files exist under tests/workshop-play/
- Total stub count: 28 it.skip stubs across 4 files (11 WKSP + 17 PLAY)
- pnpm test passes with all stubs skipped
- Every stub has a TODO comment naming the plan that fills it (29-01, 29-02, or 29-03)
</verification>

<success_criteria>
- 4 test stub files created with 28 it.skip stubs
- pnpm test exits 0
- Every Phase 29 requirement (WKSP-01, WKSP-02, PLAY-01, PLAY-02) has at least 3 stubs
</success_criteria>

<output>
After completion, create `.planning/phases/29-workshop-play/29-00-SUMMARY.md`
</output>

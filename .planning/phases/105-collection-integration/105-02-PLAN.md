---
phase: 105-collection-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 105-01
files_modified:
  - src/features/unit-database/UdbUnitRow.tsx
  - src/features/unit-database/UdbUnitList.tsx
  - src/features/unit-database/UdbDatasheetSheet.tsx
  - src/features/unit-database/DatabaseBrowserPage.tsx
  - src/features/units/UnitSheet.tsx
autonomous: true
requirements:
  - COL-01
  - COL-04
  - COL-05
  - COL-07

must_haves:
  truths:
    - "Database browser rows show 'Owned xN' badge for units in the collection"
    - "Database browser rows show a readiness dot colored by worst painting status across owned copies"
    - "User can click 'Add to Collection' on a datasheet sheet and get a pre-filled UnitSheet"
    - "Pre-filled fields (name, faction_id, category, points, model_count) are editable before saving"
    - "After saving, the ownership badge updates on the database browser row without page refresh"
    - "Custom/kitbash units can still be created manually without a database link"
  artifacts:
    - path: "src/features/unit-database/UdbUnitRow.tsx"
      provides: "Ownership badge and readiness dot rendering"
      contains: "ownershipData"
    - path: "src/features/unit-database/UdbUnitList.tsx"
      provides: "ownershipMap prop threading to each row"
      contains: "ownershipMap"
    - path: "src/features/unit-database/UdbDatasheetSheet.tsx"
      provides: "Add to Collection button with onAddToCollection callback"
      contains: "onAddToCollection"
    - path: "src/features/unit-database/DatabaseBrowserPage.tsx"
      provides: "Orchestration of ownership data, UnitSheet state, and add-to-collection flow"
      contains: "useUdbOwnership"
    - path: "src/features/units/UnitSheet.tsx"
      provides: "prefill prop support and Database Link display field"
      contains: "prefill"
  key_links:
    - from: "DatabaseBrowserPage.tsx"
      to: "useUdbOwnership hook"
      via: "hook call with selectedFactionId"
      pattern: "useUdbOwnership"
    - from: "DatabaseBrowserPage.tsx"
      to: "UdbUnitList"
      via: "ownershipMap prop"
      pattern: "ownershipMap"
    - from: "UdbDatasheetSheet.tsx"
      to: "DatabaseBrowserPage.tsx"
      via: "onAddToCollection callback"
      pattern: "onAddToCollection"
    - from: "DatabaseBrowserPage.tsx"
      to: "UnitSheet"
      via: "prefill + prefillUdbUnitId props"
      pattern: "prefill"
---

<objective>
Wire ownership badges, readiness dots, and the "Add from Database" flow into the database browser UI.

Purpose: Users see which units they own, how painted they are, and can add database units to their collection in one action with pre-filled fields.
Output: Extended UdbUnitRow with badges, UdbDatasheetSheet with "Add to Collection" button, DatabaseBrowserPage orchestration, UnitSheet prefill support.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-collection-integration/105-CONTEXT.md
@.planning/phases/105-collection-integration/105-RESEARCH.md
@.planning/phases/105-collection-integration/105-PATTERNS.md
@.planning/phases/105-collection-integration/105-UI-SPEC.md
@.planning/phases/105-collection-integration/105-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor needs these contracts -->

From src/types/unit.ts (extended by Plan 01):
- Unit.udb_unit_id: string | null
- CreateUnitInput includes udb_unit_id via Omit pattern
- PAINTING_STATUS_ORDER: ["Not Started","Built","Primed","Basecoated","Shaded","Layered","Highlighted","Details Done","Based","Varnished","Completed"]

From src/types/faction.ts (extended by Plan 01):
- Faction.wahapedia_faction_id: string | null

From src/db/queries/unitDatabase.ts (extended by Plan 01):
- interface UdbOwnershipEntry { udb_unit_id: string; owned_count: number; all_statuses: string; }
- function getUdbOwnershipByFaction(factionId: string): Promise<UdbOwnershipEntry[]>

From src/hooks/useUnitDatabase.ts (extended by Plan 01):
- UDB_OWNERSHIP_KEY = (factionId: string) => ["udb-ownership", factionId]
- function useUdbOwnership(factionId: string | null): UseQueryResult<UdbOwnershipEntry[]>

From src/features/unit-database/UdbUnitRow.tsx (current):
- interface UdbUnitRowProps { unit: UdbUnitSummary; onOpen: (id: string) => void; }
- Layout: [name flex-1] [role badge] [points w-20 text-right]

From src/features/unit-database/UdbDatasheetSheet.tsx (current):
- interface UdbDatasheetSheetProps { unitId: string | null; open: boolean; onOpenChange: (open: boolean) => void; }
- useUdbUnitDetail(unitId) returns UdbUnitDetail with: id, name, faction_id, role, points[], composition[], weapons[], abilities[], keywords[]

From src/features/unit-database/DatabaseBrowserPage.tsx (current):
- State: selectedFactionId (string | null), selectedUnitId (string | null)
- Renders UdbUnitList and UdbDatasheetSheet

From src/features/units/UnitSheet.tsx (current):
- interface UnitSheetProps { open: boolean; unit: Unit | EnrichedUnit | null; defaultFactionId?: number; onClose: () => void; }
- Uses form.reset(buildDefaultValues(unit, defaultFactionId)) in useEffect
- onSubmit calls createUnit (when unit is null) or updateUnit

From src/hooks/useFactions.ts:
- useFactions() returns UseQueryResult<Faction[]>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ownership and readiness badges on UdbUnitRow + UdbUnitList threading</name>
  <files>src/features/unit-database/UdbUnitRow.tsx, src/features/unit-database/UdbUnitList.tsx</files>
  <read_first>
    src/features/unit-database/UdbUnitRow.tsx
    src/features/unit-database/UdbUnitList.tsx
    src/types/unit.ts
    .planning/phases/105-collection-integration/105-UI-SPEC.md
  </read_first>
  <action>
    **UdbUnitRow.tsx (per D-09, D-11):**

    Extend UdbUnitRowProps interface: add ownershipData optional prop typed as { owned_count: number; all_statuses: string } or null.

    Add an exported pure function resolveWorstStatus(allStatuses: string): string that splits the pipe-delimited string, iterates statuses, and returns the one with the lowest PAINTING_STATUS_ORDER.indexOf() value. Import PAINTING_STATUS_ORDER from "@/types/unit". For statuses not found in PAINTING_STATUS_ORDER (e.g. future additions), treat them as index -1 (worst/unknown).

    Add an exported pure function resolveReadinessDotClass(allStatuses: string): string. Define a Set DONE_STATUSES containing "Varnished", "Completed", "Display Ready", and "Battle Ready" (per D-11 — green condition includes "Display Ready" and "Battle Ready" explicitly). Get worstStatus via resolveWorstStatus. Split allStatuses by "|" and check if EVERY status is in DONE_STATUSES — if yes, return "bg-emerald-400". If worstStatus is "Not Started" AND all statuses are "Not Started", return "bg-muted-foreground/50". Otherwise return "bg-amber-500" (in progress). These colors match the UI-SPEC.md tier system.

    IMPORTANT: The green condition checks that ALL copies are done, not just the worst status. A unit with statuses "Completed|Not Started" should be amber (in progress), not green. Split and check every status against DONE_STATUSES.

    In the JSX, insert the following elements between the role Badge and the points span:
    1. When ownershipData exists and owned_count > 0: render Badge with variant="outline" className="text-xs shrink-0" showing "Owned x{owned_count}" (use multiplication sign character x).
    2. Immediately after: render a span with className combining "inline-block h-2 w-2 rounded-full shrink-0" and the result of resolveReadinessDotClass(ownershipData.all_statuses). Add a title attribute from a resolveReadinessLabel function that returns "All copies painted" for done, "Not started" for not-started, and "In progress ({worstStatus})" otherwise.

    **UdbUnitList.tsx (per D-10):**

    Extend UdbUnitListProps (or equivalent interface) to include optional ownershipMap prop typed as Map of string to { owned_count: number; all_statuses: string }.

    In the virtualizer row render, pass ownershipData={ownershipMap?.get(item.unit.id) ?? null} to each UdbUnitRow component.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test -- tests/collection/udbCollectionLink.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - UdbUnitRow accepts ownershipData prop of type { owned_count: number; all_statuses: string } | null
    - When ownershipData.owned_count > 0, a Badge with text "Owned xN" renders
    - When ownershipData.owned_count > 0, a readiness dot span renders with class derived from PAINTING_STATUS_ORDER resolution
    - resolveWorstStatus uses PAINTING_STATUS_ORDER.indexOf for semantic ordering (NOT alphabetical)
    - resolveReadinessDotClass checks ALL statuses against DONE_STATUSES for the green condition, not just worstStatus
    - DONE_STATUSES includes "Varnished", "Completed", "Display Ready", and "Battle Ready" per D-11
    - Readiness dot classes: bg-emerald-400 (all done), bg-amber-500 (in progress), bg-muted-foreground/50 (all not started)
    - UdbUnitList accepts ownershipMap prop and passes lookup result to each UdbUnitRow
    - resolveWorstStatus and resolveReadinessDotClass are exported (for testing)
    - pnpm build passes
    - Pure function tests from Plan 01 Task 0 pass
  </acceptance_criteria>
  <done>UdbUnitRow shows ownership count badge and readiness dot. UdbUnitList threads ownershipMap to each row. Pure functions exported and tested.</done>
</task>

<task type="auto">
  <name>Task 2: "Add to Collection" flow — UdbDatasheetSheet button + DatabaseBrowserPage orchestration + ownership data</name>
  <files>src/features/unit-database/UdbDatasheetSheet.tsx, src/features/unit-database/DatabaseBrowserPage.tsx</files>
  <read_first>
    src/features/unit-database/UdbDatasheetSheet.tsx
    src/features/unit-database/DatabaseBrowserPage.tsx
    src/hooks/useUnitDatabase.ts
    src/hooks/useFactions.ts
    src/features/units/unitSchema.ts
  </read_first>
  <action>
    **UdbDatasheetSheet.tsx (per D-04):**

    Extend UdbDatasheetSheetProps: add onAddToCollection optional callback typed as (unit: UdbUnitDetail) => void. UdbUnitDetail is the return type of useUdbUnitDetail — import it from the query layer or inline the type reference.

    Add a "Add to Collection" Button in the sheet. Place it after the SheetHeader content, inside the loaded-unit block. Use Button variant="default" size="sm" className="w-full mt-2". Icon: Plus from lucide-react, h-4 w-4 mr-2. Label: "Add to Collection". On click: call onAddToCollection(unit) where unit is the loaded UdbUnitDetail.

    If the parent also passes ownershipData (optional prop), change button label to "Add Another to Collection" when ownershipData.owned_count > 0. If not passing ownershipData, always show "Add to Collection" (simpler).

    **DatabaseBrowserPage.tsx (per D-04, D-05, D-07, D-10):**

    Import useUdbOwnership and UDB_OWNERSHIP_KEY from "@/hooks/useUnitDatabase". Import useFactions from "@/hooks/useFactions". Import UnitSheet from "@/features/units/UnitSheet". Import the UnitFormValues type or Partial thereof from "@/features/units/unitSchema".

    Add state variables: unitSheetOpen (boolean, default false), unitSheetPrefill (Partial of UnitFormValues or null, default null), unitSheetUdbId (string or null, default null).

    Call useUdbOwnership(selectedFactionId) — selectedFactionId is the currently viewed faction's Wahapedia text ID (e.g. "SM"). Build ownershipMap as a useMemo: iterate ownershipEntries, create Map keyed by udb_unit_id with value { owned_count, all_statuses }.

    Call useFactions() to get the collection factions list for faction ID mapping.

    Create handleAddToCollection function that receives a UdbUnitDetail. Map the udb faction_id (text) to collection faction_id (integer) by finding the faction in collectionFactions where wahapedia_faction_id matches. Extract base_points from the first points tier (unit.points[0]?.points ?? null). Extract min_models from the first composition entry (unit.composition[0]?.min_models ?? 1). Build prefill object with: name (unit.name), faction_id (mapped integer or 0 if no match), category (unit.role or ""), points (base_points), model_count (min_models). Set unitSheetPrefill, unitSheetUdbId (unit.id), close datasheet sheet (setSelectedUnitId(null)), open UnitSheet (setUnitSheetOpen(true)).

    Pass ownershipMap to UdbUnitList component.

    Pass onAddToCollection={handleAddToCollection} to UdbDatasheetSheet component.

    Render UnitSheet at the end of the JSX with: open={unitSheetOpen}, unit={null} (always create mode), prefill={unitSheetPrefill ?? undefined}, prefillUdbUnitId={unitSheetUdbId}, onClose handler that resets all three state variables to defaults.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - UdbDatasheetSheet renders "Add to Collection" button with Plus icon when onAddToCollection prop is provided
    - Button calls onAddToCollection(unit) with the loaded UdbUnitDetail
    - DatabaseBrowserPage calls useUdbOwnership(selectedFactionId) and builds ownershipMap via useMemo
    - DatabaseBrowserPage passes ownershipMap to UdbUnitList
    - DatabaseBrowserPage passes onAddToCollection to UdbDatasheetSheet
    - handleAddToCollection maps udb faction_id to collection faction_id via wahapedia_faction_id match
    - handleAddToCollection builds prefill with name, faction_id, category, points (lowest tier), model_count (min_models or 1)
    - UnitSheet rendered with open/prefill/prefillUdbUnitId props, unit=null (create mode)
    - pnpm build passes
  </acceptance_criteria>
  <done>"Add to Collection" button on datasheet sheet. Page orchestrates ownership data, faction mapping, and pre-filled UnitSheet creation flow.</done>
</task>

<task type="auto">
  <name>Task 3: UnitSheet prefill support and Database Link display</name>
  <files>src/features/units/UnitSheet.tsx</files>
  <read_first>
    src/features/units/UnitSheet.tsx
    src/features/units/unitSchema.ts
    src/types/unit.ts
  </read_first>
  <action>
    **UnitSheet.tsx (per D-05, D-06, D-15, D-16):**

    Extend UnitSheetProps: add prefill optional prop typed as Partial of UnitFormValues, and prefillUdbUnitId optional prop typed as string or null.

    Modify buildDefaultValues function (or its equivalent): add a third parameter `prefill?: Partial<UnitFormValues>`. When unit is null (create mode) and prefill is provided, spread prefill over the empty defaults: { ...emptyDefaults, ...prefill }. When unit is provided (edit mode), ignore prefill — existing logic unchanged. Per D-06, all pre-filled fields are editable.

    Update the useEffect that calls form.reset: add prefill to the dependency array. Call buildDefaultValues(unit, defaultFactionId, prefill).

    In the onSubmit handler: when creating a new unit (unit is null), include udb_unit_id in the CreateUnitInput payload. Set it to prefillUdbUnitId ?? null. When editing (unit is not null), include udb_unit_id as (unit as Unit).udb_unit_id ?? null in the UpdateUnitInput payload. This ensures all call sites pass udb_unit_id explicitly — matching the no-COALESCE pattern in updateUnit SQL (see Plan 01 Task 2). Do NOT add udb_unit_id to the Zod unitSchema — it is system-managed, not user-editable.

    Add a "Database Link" display field (per D-15): render a read-only text element above or below the form fields. In edit mode (unit is not null): if (unit as Unit).udb_unit_id exists, show text "Linked to unit database". If udb_unit_id is null, show "Custom unit (no database link)". In create mode with prefillUdbUnitId: show "Linked to unit database". Use text-xs text-muted-foreground styling, inside the sheet content area (px-4 pb-0 above the form).

    Style: p element with className="text-xs text-muted-foreground px-4 pb-0".
  </action>
  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - UnitSheetProps includes prefill?: Partial<UnitFormValues> and prefillUdbUnitId?: string | null
    - buildDefaultValues applies prefill overlay when unit is null
    - useEffect dependency array includes prefill
    - onSubmit passes udb_unit_id in CreateUnitInput payload (from prefillUdbUnitId)
    - onSubmit passes udb_unit_id in UpdateUnitInput payload (from unit.udb_unit_id) — ensuring no-COALESCE SQL is safe
    - udb_unit_id is NOT added to unitSchema (Zod)
    - "Database Link" display shows "Linked to unit database" when linked, "Custom unit (no database link)" when not
    - UnitSheet still works in manual create mode (no prefill, no prefillUdbUnitId) — COL-07 preserved
    - pnpm build passes
    - Full test suite passes
  </acceptance_criteria>
  <done>UnitSheet supports pre-filled create mode from database entries. Database Link field shows link status. Manual creation still works without database link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UDB data -> UnitSheet prefill | Database unit data populates form fields — user can edit before saving |
| Form submission -> SQLite | All values pass through Zod validation and parameterized queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-04 | Tampering | UnitSheet prefill | mitigate | Pre-filled values pass through existing Zod schema validation before createUnit call |
| T-105-05 | Spoofing | ownershipMap | accept | Data is local-only, user owns both source and destination — no spoofing vector |
</threat_model>

<verification>
- `pnpm build` passes after all UI extensions
- `pnpm test` full suite passes (including Plan 01 test files for pure functions)
- Database browser rows show ownership badges for owned units
- "Add to Collection" button on datasheet sheet opens pre-filled UnitSheet
</verification>

<success_criteria>
- UdbUnitRow displays "Owned xN" badge and readiness dot for owned units, nothing for unowned
- Readiness dot uses DONE_STATUSES including "Varnished", "Completed", "Display Ready", "Battle Ready" per D-11
- UdbDatasheetSheet has "Add to Collection" button that triggers pre-fill flow
- DatabaseBrowserPage orchestrates ownership data and UnitSheet creation
- UnitSheet accepts prefill prop and includes udb_unit_id in creation payload
- UnitSheet onSubmit always passes udb_unit_id explicitly for both create and edit paths
- Manual unit creation without database link still works unchanged
- TypeScript build and full test suite pass
</success_criteria>

<output>
Create `.planning/phases/105-collection-integration/105-02-SUMMARY.md` when done
</output>

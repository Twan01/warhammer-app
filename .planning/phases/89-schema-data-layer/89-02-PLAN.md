---
phase: 89-schema-data-layer
plan: 02
type: execute
wave: 2
depends_on: [89-01]
files_modified:
  - src/db/queries/armyLists.ts
  - src/hooks/useArmyLists.ts
  - tests/lib/resolveUnitPoints.test.ts
  - tests/army-list/armyListQueries.test.ts
  - tests/army-list/armyListEnhancements.test.ts
autonomous: true
requirements: [DL-03, DL-04]

must_haves:
  truths:
    - "getArmyListWithUnits returns ghost units alongside real units in a single SELECT"
    - "getArmyListWithUnits uses ORDER BY alu.created_at ASC, alu.id ASC for stable insertion order"
    - "COALESCE chain is identical across getArmyListWithUnits, getArmyListReadiness, and resolveUnitPoints"
    - "setWarlord enforces one-per-list via list_id-scoped two-step UPDATE"
    - "Ghost unit points resolve via COALESCE(u.name, alu.ghost_unit_name) join key"
    - "Enhancement CRUD functions exist for army_list_enhancements table"
    - "All new mutation hooks invalidate the correct cache keys"
    - "Existing resolveUnitPoints tests pass with tier_points added"
  artifacts:
    - path: "src/db/queries/armyLists.ts"
      provides: "Extended queries + new CRUD functions"
      contains: "setWarlord"
    - path: "src/hooks/useArmyLists.ts"
      provides: "New mutation hooks for v3 features"
      contains: "useSetWarlord"
    - path: "tests/army-list/armyListEnhancements.test.ts"
      provides: "Enhancement CRUD test coverage"
      contains: "addEnhancement"
  key_links:
    - from: "src/hooks/useArmyLists.ts"
      to: "src/db/queries/armyLists.ts"
      via: "import of new query functions"
      pattern: "import.*setWarlord.*addGhostUnitToList"
    - from: "src/db/queries/armyLists.ts"
      to: "src/types/armyList.ts"
      via: "type imports for new interfaces"
      pattern: "AddGhostUnitToListInput.*ArmyListEnhancement"
---

<objective>
Implement all query functions, mutation hooks, and tests for the Army Lists 3.0 data layer.

Purpose: Wire the schema contracts from Plan 01 into working query functions, React Query hooks, and test coverage. After this plan, the data layer fully supports ghost units, warlord designation, tier-based points, enhancement CRUD, leader attachment, and stable ordering -- all ready for UI phases.

Output: Extended query module, new mutation hooks, and comprehensive test coverage.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-schema-data-layer/89-CONTEXT.md
@.planning/phases/89-schema-data-layer/89-RESEARCH.md
@.planning/phases/89-schema-data-layer/89-PATTERNS.md
@.planning/phases/89-schema-data-layer/89-01-SUMMARY.md

<interfaces>
<!-- Types created by Plan 01 that this plan depends on -->

From src/types/armyList.ts (after Plan 01):
- ArmyListUnit: { unit_id: number | null; ghost_unit_name: string | null; is_warlord: number; selected_model_count: number | null; leader_attached_to_id: number | null; ... }
- ArmyListUnitRow extends ArmyListUnit: { tier_points: number | null; faction_id: number | null; status_painting: string | null; ... }
- AddGhostUnitToListInput: { list_id: number; ghost_unit_name: string; points_override?: number | null; notes?: string | null }
- ArmyListEnhancement: { id: number; list_id: number; army_list_unit_id: number; enhancement_name: string; enhancement_points: number; created_at: string }
- AddEnhancementInput: { list_id: number; army_list_unit_id: number; enhancement_name: string; enhancement_points: number }

From src/lib/resolveUnitPoints.ts (after Plan 01):
- PointsSource: "override" | "tier" | "synced" | "user-override" | "base" | "unknown"
- resolveUnitPoints(row: { points_override, tier_points, synced_points, override_points, unit_points }): ResolvedPoints
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query functions in armyLists.ts</name>
  <files>src/db/queries/armyLists.ts</files>
  <read_first>
    src/db/queries/armyLists.ts (current full file — getArmyListWithUnits lines 52-76, getArmyListReadiness lines 223-245, getArmyListUnitNames lines 30-41, addUnitToList lines 153-161, clearArmyListDetachment lines 119-129)
    src/db/queries/unitLoadouts.ts (activateLoadout two-step pattern for setWarlord reference — lines 68-76)
    src/types/armyList.ts (Plan 01 output — new type imports needed)
    .planning/phases/89-schema-data-layer/89-PATTERNS.md (Patterns 3-8: query patterns with exact SQL)
    .planning/phases/89-schema-data-layer/89-RESEARCH.md (Pitfalls 2-4, 7: COALESCE divergence, ghost unit JOIN, warlord scoping, updateArmyListUnit)
  </read_first>
  <action>
    Update imports at top of armyLists.ts to add: AddGhostUnitToListInput, ArmyListEnhancement, AddEnhancementInput from @/types/armyList.

    1. EXTEND getArmyListWithUnits (per D-06, D-08, D-09, D-11):
       - Change JOIN units u ON u.id = alu.unit_id to LEFT JOIN units u ON u.id = alu.unit_id
       - Add to SELECT: alu.ghost_unit_name, alu.is_warlord, alu.selected_model_count, alu.leader_attached_to_id
       - Change u.name AS unit_name to COALESCE(u.name, alu.ghost_unit_name) AS unit_name
       - Add LEFT JOIN synced_unit_point_tiers tier ON tier.unit_name = COALESCE(u.name, alu.ghost_unit_name) AND tier.model_count = alu.selected_model_count AND (tier.faction_id IS NULL OR tier.faction_id = CAST(u.faction_id AS TEXT))
       - Add tier.points AS tier_points to SELECT
       - Change COALESCE from 5-level to 6-level: COALESCE(alu.points_override, tier.points, sup.points, uo.points, u.points, 0)
       - Change synced_unit_points JOIN key from sup.unit_name = u.name to sup.unit_name = COALESCE(u.name, alu.ghost_unit_name)
       - Change ORDER BY from alu.created_at ASC to alu.created_at ASC, alu.id ASC (per D-11, DL-03 tiebreaker)

    2. EXTEND getArmyListReadiness (per D-09 — MUST match getArmyListWithUnits atomically):
       - Change JOIN units u to LEFT JOIN units u (ghost units included in totals)
       - Add LEFT JOIN synced_unit_point_tiers tier (same ON clause as getArmyListWithUnits)
       - Change synced_unit_points JOIN key to use COALESCE(u.name, alu.ghost_unit_name)
       - Change both COALESCE expressions to 6-level: COALESCE(alu.points_override, tier.points, sup.points, uo.points, u.points, 0)
       - Ghost units have u.status_painting = NULL so CASE WHEN evaluates to ELSE 0 (correct behavior)

    3. EXTEND getArmyListUnitNames (per Research Open Question 1):
       - Change JOIN units u to LEFT JOIN units u
       - Change u.name AS unit_name to COALESCE(u.name, alu.ghost_unit_name) AS unit_name

    4. ADD setWarlord(armyListUnitId: number, listId: number): Promise<void> (per D-10):
       - Single UPDATE with CASE WHEN id = $1 THEN 1 ELSE 0 END WHERE list_id = $2
       - Parameters: [armyListUnitId, listId]
       - MUST scope by list_id in WHERE clause (Pitfall 4)

    5. ADD clearWarlord(listId: number): Promise<void>:
       - UPDATE army_list_units SET is_warlord = 0 WHERE list_id = $1

    6. ADD addGhostUnitToList(input: AddGhostUnitToListInput): Promise<number> (per D-04):
       - INSERT INTO army_list_units (list_id, unit_id, ghost_unit_name, points_override, notes) VALUES ($1, NULL, $2, $3, $4)
       - Return result.lastInsertId ?? 0

    7. ADD setLeaderAttachment(armyListUnitId: number, targetId: number): Promise<void> (per D-03):
       - UPDATE army_list_units SET leader_attached_to_id = $2 WHERE id = $1

    8. ADD clearLeaderAttachment(armyListUnitId: number): Promise<void> (per D-13):
       - UPDATE army_list_units SET leader_attached_to_id = NULL WHERE id = $1

    9. ADD setSelectedModelCount(armyListUnitId: number, count: number): Promise<void> (per D-08):
       - UPDATE army_list_units SET selected_model_count = $2 WHERE id = $1

    10. ADD clearSelectedModelCount(armyListUnitId: number): Promise<void> (per D-13):
        - UPDATE army_list_units SET selected_model_count = NULL WHERE id = $1

    11. ADD addEnhancement(input: AddEnhancementInput): Promise<number> (per D-01):
        - INSERT INTO army_list_enhancements (list_id, army_list_unit_id, enhancement_name, enhancement_points) VALUES ($1, $2, $3, $4)

    12. ADD removeEnhancement(enhancementId: number): Promise<void>:
        - DELETE FROM army_list_enhancements WHERE id = $1

    13. ADD getEnhancementsByList(listId: number): Promise<ArmyListEnhancement[]>:
        - SELECT * FROM army_list_enhancements WHERE list_id = $1 ORDER BY created_at ASC

    Do NOT modify updateArmyListUnit — per Pitfall 7, new fields have dedicated functions.
    Update the module-level JSDoc to note the 6-level COALESCE chain.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - getArmyListWithUnits uses LEFT JOIN units (not INNER JOIN)
    - getArmyListWithUnits SELECT includes alu.ghost_unit_name, alu.is_warlord, alu.selected_model_count, alu.leader_attached_to_id, tier.points AS tier_points
    - getArmyListWithUnits COALESCE is 6-level: COALESCE(alu.points_override, tier.points, sup.points, uo.points, u.points, 0)
    - getArmyListWithUnits ORDER BY is alu.created_at ASC, alu.id ASC
    - getArmyListReadiness COALESCE matches getArmyListWithUnits exactly (6-level)
    - getArmyListReadiness uses LEFT JOIN units (not INNER JOIN)
    - getArmyListUnitNames uses LEFT JOIN units and COALESCE for unit_name
    - setWarlord scopes its UPDATE by list_id ($2) in the WHERE clause
    - addGhostUnitToList passes NULL as unit_id ($1 position is list_id, unit_id is hardcoded NULL)
    - clearLeaderAttachment and clearSelectedModelCount follow the SET col = NULL WHERE id = $1 pattern
    - addEnhancement/removeEnhancement/getEnhancementsByList target army_list_enhancements table
    - pnpm build exits 0
  </acceptance_criteria>
  <done>All query functions implemented: 3 queries extended, 10 new functions added. COALESCE chain is identical across all 3 sites.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation hooks + tests</name>
  <files>src/hooks/useArmyLists.ts, tests/lib/resolveUnitPoints.test.ts, tests/army-list/armyListQueries.test.ts, tests/army-list/armyListEnhancements.test.ts</files>
  <read_first>
    src/hooks/useArmyLists.ts (current hooks — useAddUnitToList lines 135-147 for invalidation pattern, useRemoveUnitFromList lines 159-171 for RemoveUnitFromListInput pattern, useClearArmyListDetachment lines 93-104)
    src/db/queries/armyLists.ts (Plan 02 Task 1 output — new function exports to import)
    tests/lib/resolveUnitPoints.test.ts (current 9 test cases — must add tier_points: null to all existing fixtures)
    tests/army-list/armyListQueries.test.ts (mock setup pattern lines 1-24, SQL assertion pattern)
    tests/army-list/clearArmyListDetachment.test.ts (SQL assertion pattern for reference)
    .planning/phases/89-schema-data-layer/89-PATTERNS.md (hooks section: useSetWarlord, useAddGhostUnitToList, useAddEnhancement, useRemoveEnhancement, useClearLeaderAttachment patterns)
  </read_first>
  <action>
    PART A — src/hooks/useArmyLists.ts:

    1. Add imports from @/db/queries/armyLists for: setWarlord, clearWarlord, addGhostUnitToList, setLeaderAttachment, clearLeaderAttachment, setSelectedModelCount, clearSelectedModelCount, addEnhancement, removeEnhancement, getEnhancementsByList.
    2. Add type imports from @/types/armyList for: AddGhostUnitToListInput, AddEnhancementInput, ArmyListEnhancement.

    3. ADD useSetWarlord hook: variables { army_list_unit_id: number, list_id: number }. mutationFn calls setWarlord(army_list_unit_id, list_id). onSuccess invalidates ARMY_LIST_UNITS_KEY(list_id), ARMY_LIST_KEY(list_id), ARMY_LISTS_KEY, ["dashboard-stats"], ["army-list-readiness"].

    4. ADD useAddGhostUnitToList hook: variables AddGhostUnitToListInput. mutationFn calls addGhostUnitToList. onSuccess invalidates ARMY_LIST_KEY(list_id), ARMY_LIST_UNITS_KEY(list_id), ARMY_LISTS_KEY, ["dashboard-stats"], ["army-list-readiness"].

    5. ADD useAddEnhancement hook: variables AddEnhancementInput. mutationFn calls addEnhancement. onSuccess invalidates ARMY_LIST_KEY(list_id), ARMY_LIST_UNITS_KEY(list_id), ARMY_LISTS_KEY, ["dashboard-stats"], ["army-list-readiness"].

    6. ADD useRemoveEnhancement hook: variables { enhancement_id: number, list_id: number }. mutationFn calls removeEnhancement(enhancement_id). onSuccess invalidates same set as useAddEnhancement using list_id.

    7. ADD useSetLeaderAttachment hook: variables { army_list_unit_id: number, target_id: number, list_id: number }. mutationFn calls setLeaderAttachment(army_list_unit_id, target_id). onSuccess invalidates ARMY_LIST_UNITS_KEY(list_id), ARMY_LIST_KEY(list_id), ["army-list-readiness"].

    8. ADD useClearLeaderAttachment hook: variables { army_list_unit_id: number, list_id: number }. mutationFn calls clearLeaderAttachment(army_list_unit_id). onSuccess invalidates ARMY_LIST_UNITS_KEY(list_id), ARMY_LIST_KEY(list_id), ["army-list-readiness"].

    9. ADD useSetSelectedModelCount hook: variables { army_list_unit_id: number, count: number, list_id: number }. mutationFn calls setSelectedModelCount(army_list_unit_id, count). onSuccess invalidates ARMY_LIST_UNITS_KEY(list_id), ARMY_LIST_KEY(list_id), ARMY_LISTS_KEY, ["dashboard-stats"], ["army-list-readiness"].

    10. ADD useClearSelectedModelCount hook: variables { army_list_unit_id: number, list_id: number }. mutationFn calls clearSelectedModelCount(army_list_unit_id). onSuccess invalidates same set as useSetSelectedModelCount.

    11. ADD useEnhancementsByList query hook: queryKey ["army-list-enhancements", listId]. queryFn calls getEnhancementsByList(listId). enabled when listId is defined.

    PART B — tests/lib/resolveUnitPoints.test.ts:

    1. Add tier_points: null to ALL 9 existing test fixture objects (non-breaking — maintains existing behavior since tier_points null falls through).
    2. Add 3 new test cases:
       - "returns 'tier' source when tier_points is set and points_override is null": { points_override: null, tier_points: 150, synced_points: 95, override_points: 80, unit_points: 75 } => { points: 150, source: "tier" }
       - "handles 0 as a valid value for tier_points": { points_override: null, tier_points: 0, synced_points: 95, ... } => { points: 0, source: "tier" }
       - "tier_points takes priority over synced_points": { points_override: null, tier_points: 200, synced_points: 180, ... } => { points: 200, source: "tier" }
    3. Update the type contract test to include "tier" in the expected source values array.

    PART C — tests/army-list/armyListQueries.test.ts:

    1. Add new describe block for setWarlord: verify SQL contains CASE WHEN id = $1 THEN 1 ELSE 0 END and WHERE list_id = $2; verify params are [armyListUnitId, listId].
    2. Add test for addGhostUnitToList: verify INSERT contains unit_id with NULL, ghost_unit_name at $2 position.
    3. Add test for clearLeaderAttachment: verify SET leader_attached_to_id = NULL WHERE id = $1.
    4. Add test for clearSelectedModelCount: verify SET selected_model_count = NULL WHERE id = $1.

    PART D — tests/army-list/armyListEnhancements.test.ts (NEW FILE):

    1. Use same mock setup as armyListQueries.test.ts (vi.mock @/db/client with selectMock/executeMock).
    2. Test addEnhancement: verify INSERT INTO army_list_enhancements with $1-$4 params.
    3. Test removeEnhancement: verify DELETE FROM army_list_enhancements WHERE id = $1.
    4. Test getEnhancementsByList: verify SELECT * FROM army_list_enhancements WHERE list_id = $1.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - useArmyLists.ts exports useSetWarlord, useAddGhostUnitToList, useAddEnhancement, useRemoveEnhancement, useSetLeaderAttachment, useClearLeaderAttachment, useSetSelectedModelCount, useClearSelectedModelCount, useEnhancementsByList
    - All new mutation hooks include list_id in their variables type for cache invalidation
    - All new mutation hooks invalidate at minimum ARMY_LIST_UNITS_KEY(list_id) and ["army-list-readiness"]
    - resolveUnitPoints.test.ts has 12+ test cases (9 existing updated + 3 new tier_points tests)
    - All existing resolveUnitPoints tests pass with tier_points: null added to fixtures
    - armyListQueries.test.ts has tests for setWarlord, addGhostUnitToList, clearLeaderAttachment, clearSelectedModelCount
    - armyListEnhancements.test.ts exists with tests for addEnhancement, removeEnhancement, getEnhancementsByList
    - pnpm test exits 0 (all tests pass)
    - pnpm build exits 0
  </acceptance_criteria>
  <done>All mutation hooks wired with correct cache invalidation; 12+ resolveUnitPoints tests pass; query function tests validate SQL patterns; enhancement CRUD tests pass. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Query params | All parameters come from app state (local SQLite), not external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-04 | Tampering | setWarlord | mitigate | list_id scoping in WHERE clause prevents cross-list mutation (Pitfall 4) |
| T-89-05 | Tampering | addGhostUnitToList | accept | ghost_unit_name is user-provided text but only used in local SQLite; parameterized query prevents injection |
| T-89-06 | Info Disclosure | getEnhancementsByList | accept | Local-only data; no network exposure |
</threat_model>

<verification>
1. `pnpm build` exits 0
2. `pnpm test` exits 0 — all tests pass
3. `pnpm test -- tests/lib/resolveUnitPoints.test.ts` — 12+ tests pass including 3 new tier_points tests
4. `pnpm test -- tests/army-list/` — all army list tests pass including new enhancement tests
5. Grep verification: `grep -c "setWarlord" src/hooks/useArmyLists.ts` returns at least 2 (import + function)
6. Grep verification: `grep -c "LEFT JOIN units" src/db/queries/armyLists.ts` returns at least 3 (getArmyListWithUnits, getArmyListReadiness, getArmyListUnitNames)
</verification>

<success_criteria>
All query functions extended/added, all mutation hooks wired with correct invalidation, resolveUnitPoints tests updated for tier_points, new query tests cover setWarlord/ghost/enhancement/clear functions, full test suite passes.
</success_criteria>

<output>
Create `.planning/phases/89-schema-data-layer/89-02-SUMMARY.md` when done
</output>

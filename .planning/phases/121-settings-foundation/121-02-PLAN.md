---
phase: 121-settings-foundation
plan: 02
type: execute
wave: 2
depends_on: [121-01]
files_modified:
  - src/app/settings/page.tsx
  - tests/settings/SettingsPage.test.tsx
autonomous: true
requirements: [INF-03]

must_haves:
  truths:
    - "Settings page loads at /settings with h1 heading 'Settings'"
    - "Three tabs are visible: Preferences, Data, About"
    - "Preferences tab is active by default on page mount"
    - "Each tab shows placeholder content with heading and description"
    - "Loading state shows a Skeleton while useAppSettings resolves"
    - "Error state shows destructive text when useAppSettings rejects"
  artifacts:
    - path: "src/app/settings/page.tsx"
      provides: "SettingsPage component with tabbed layout"
      exports: ["SettingsPage"]
      min_lines: 30
    - path: "tests/settings/SettingsPage.test.tsx"
      provides: "Component render tests for tab structure"
      min_lines: 30
  key_links:
    - from: "src/app/settings/page.tsx"
      to: "src/hooks/useAppSettings.ts"
      via: "import useAppSettings"
      pattern: "import.*useAppSettings.*from.*@/hooks/useAppSettings"
    - from: "src/app/settings/page.tsx"
      to: "src/components/ui/tabs.tsx"
      via: "import Tabs, TabsList, TabsTrigger, TabsContent"
      pattern: "import.*Tabs.*from.*@/components/ui/tabs"
---

<objective>
Replace the Settings placeholder page with a real tabbed layout using shadcn Tabs, wired to useAppSettings for loading/error states.

Purpose: Delivers the visible Settings UI shell that Phases 122-125 will populate with actual controls.
Output: Working Settings page at /settings with 3 tabs and placeholder content.
</objective>

<execution_context>
@C:/Documents/Claude Apps/Warhammer App/.claude/get-shit-done/workflows/execute-plan.md
@C:/Documents/Claude Apps/Warhammer App/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-settings-foundation/121-CONTEXT.md
@.planning/phases/121-settings-foundation/121-UI-SPEC.md
@.planning/phases/121-settings-foundation/121-PATTERNS.md
@.planning/phases/121-settings-foundation/121-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/hooks/useAppSettings.ts (created in Plan 01):
- `export const APP_SETTINGS_KEY = ["app-settings"] as const;`
- `export function useAppSettings(): UseQueryResult<AppSettingsMap>`
- `export function useUpdateSetting(): UseMutationResult<void, Error, { key: string; value: string }>`

From src/components/ui/tabs.tsx (existing shadcn component):
- `export { Tabs, TabsList, TabsTrigger, TabsContent }`
- Usage: `<Tabs defaultValue="..."><TabsList><TabsTrigger value="...">Label</TabsTrigger></TabsList><TabsContent value="...">...</TabsContent></Tabs>`

From src/components/ui/skeleton.tsx (existing shadcn component):
- `export { Skeleton }`
- Usage: `<Skeleton className="h-4 w-48" />`

From src/app/router.tsx (existing route, lines 169-171):
- Route at `/settings` with lazy import expecting `SettingsPage` named export
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Settings placeholder with tabbed page</name>
  <files>src/app/settings/page.tsx</files>
  <read_first>
    - src/app/settings/page.tsx (current placeholder — 5 lines, exports SettingsPage)
    - src/features/rules-hub/RulesHubPage.tsx (analog for Tabs usage: import pattern lines 1-4, Tabs structure lines 148-155, Skeleton loading pattern lines 211-217)
    - .planning/phases/121-settings-foundation/121-UI-SPEC.md (approved visual contract: layout, copy, states)
    - src/components/ui/tabs.tsx (verify Tabs/TabsList/TabsTrigger/TabsContent exports)
  </read_first>
  <action>
    Replace entire contents of `src/app/settings/page.tsx` per D-06, D-07, D-08 and the UI-SPEC:

    Imports: `Tabs, TabsList, TabsTrigger, TabsContent` from `@/components/ui/tabs`, `Skeleton` from `@/components/ui/skeleton`, `useAppSettings` from `@/hooks/useAppSettings`.

    Export `function SettingsPage()` (must keep same export name — router depends on it per D-08).

    Component body:
    - Call `useAppSettings()` destructuring `isLoading` and `isError`
    - Outer div: `className="p-6 space-y-6"` (per UI-SPEC spacing)
    - h1: `className="text-xl font-semibold"` with text "Settings" (per UI-SPEC typography)
    - `Tabs` with `defaultValue="preferences"` per D-06/D-07
    - TabsList with three TabsTrigger elements: value `"preferences"` label "Preferences", value `"data"` label "Data", value `"about"` label "About"
    - TabsContent for "preferences" with `className="mt-4"`: conditionally render Skeleton (`className="h-4 w-48"`) when isLoading, error text "Could not load settings. Restart the app to try again." with `className="text-destructive text-sm"` when isError, otherwise render h2 "Preferences" (`text-lg font-semibold`) and p "Language, currency, default faction, and points target — coming in the next update." (`text-muted-foreground text-sm`)
    - TabsContent for "data" with `className="mt-4"`: h2 "Data Management" (`text-lg font-semibold`) and p "Data health link, factory reset, and preference backup — coming in the next update." (`text-muted-foreground text-sm`)
    - TabsContent for "about" with `className="mt-4"`: h2 "About HobbyForge" (`text-lg font-semibold`) and p "App version, data statistics, and attribution — coming in the next update." (`text-muted-foreground text-sm`)

    Data and About tabs do not need loading/error guards — they are static placeholder text in Phase 121.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx exports `function SettingsPage` (not default export, not renamed)
    - page.tsx imports from `@/components/ui/tabs` and `@/hooks/useAppSettings`
    - Outer div has className `p-6 space-y-6`
    - h1 text is "Settings" with className containing `text-xl font-semibold`
    - Tabs has `defaultValue="preferences"`
    - Three TabsTrigger elements with values "preferences", "data", "about"
    - Preferences TabsContent handles isLoading (Skeleton) and isError (destructive text) states
    - All three placeholder bodies contain the exact copy from UI-SPEC copywriting contract
    - `pnpm build` passes
  </acceptance_criteria>
  <done>Settings page renders with 3 tabs, default Preferences, loading/error states, and placeholder content matching UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Create SettingsPage component tests</name>
  <files>tests/settings/SettingsPage.test.tsx</files>
  <read_first>
    - src/app/settings/page.tsx (the component just created — verify exact text strings and structure)
    - tests/foundation/useRecipes.test.ts (analog: vi.mock pattern for hooks, makeWrapper with QueryClient)
    - tests/app-shell/ (check for any existing component test patterns with screen.getByRole/getByText)
  </read_first>
  <action>
    Create `tests/settings/SettingsPage.test.tsx`:

    Mock `@/hooks/useAppSettings` with vi.mock returning `useAppSettings` as a vi.fn(). Import `render, screen` from `@testing-library/react`, `describe, it, expect, vi` from vitest, and the `SettingsPage` component.

    Helper: `mockUseAppSettings` function that casts the mocked import to set return value — pattern: `vi.mocked(useAppSettings).mockReturnValue(...)` with `{ data: {}, isLoading: false, isError: false }` as default (cast to any for partial return).

    Test suite "SettingsPage":
    - Test: `renders Settings heading` — mockUseAppSettings with loaded state, render SettingsPage, assert `screen.getByRole("heading", { level: 1 })` has text "Settings"
    - Test: `renders three tab triggers` — render, assert screen.getByRole("tab", { name: "Preferences" }), screen.getByRole("tab", { name: "Data" }), screen.getByRole("tab", { name: "About" }) all exist
    - Test: `Preferences tab is active by default` — render, assert the Preferences tab trigger has `aria-selected="true"` (via `getByRole("tab", { name: "Preferences", selected: true })`)
    - Test: `shows Skeleton when loading` — mockUseAppSettings with `isLoading: true`, render, assert no "Preferences" heading (h2) visible, but a skeleton element exists (query by test class or role)
    - Test: `shows error message when query fails` — mockUseAppSettings with `isError: true`, render, assert screen.getByText matching "Could not load settings" exists

    Wrap SettingsPage renders in a QueryClientProvider with a fresh QueryClient (retry: false) since Tabs might need the React context. Or simpler: since useAppSettings is fully mocked, a plain render may suffice — try without wrapper first.
  </action>
  <verify>
    <automated>pnpm test -- tests/settings/SettingsPage.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test file mocks `@/hooks/useAppSettings` (no Tauri IPC dependency)
    - Test verifies h1 "Settings" heading exists
    - Test verifies 3 tab triggers with correct labels (Preferences, Data, About)
    - Test verifies Preferences is the default active tab (aria-selected)
    - Test verifies loading state renders Skeleton (no heading visible)
    - Test verifies error state renders "Could not load settings" text
    - `pnpm test -- tests/settings/SettingsPage.test.tsx` passes all tests
  </acceptance_criteria>
  <done>SettingsPage tests pass, covering heading, tabs, default tab, loading, and error states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Read-only UI shell with no user input in Phase 121 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-03 | Spoofing | SettingsPage | accept | No auth in app; local single-user desktop tool |
| T-121-SC | Tampering | npm installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
- `pnpm build` passes (TypeScript compilation)
- `pnpm test -- tests/settings/SettingsPage.test.tsx` passes (component render tests)
- `pnpm test -- tests/settings/` passes (all settings tests including Plan 01 tests)
- `pnpm test` full suite still green (no regressions)
</verification>

<success_criteria>
- Settings page at /settings shows h1 "Settings" and 3 tabs
- Preferences tab is default active
- Each tab has placeholder heading and body text matching UI-SPEC copy
- Loading state shows Skeleton, error state shows destructive message
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/121-settings-foundation/121-02-SUMMARY.md` when done
</output>

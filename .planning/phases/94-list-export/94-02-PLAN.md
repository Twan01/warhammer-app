---
phase: 94-list-export
plan: 02
type: execute
wave: 2
depends_on: [94-01]
files_modified:
  - src/features/army-lists/ExportDropdown.tsx
  - src/features/army-lists/PrintPreviewDialog.tsx
  - src/features/army-lists/ArmyListDetailSheet.tsx
  - src/features/army-lists/ArmyListsPage.tsx
  - src/styles/globals.css
  - tests/army-lists/PrintPreviewDialog.test.tsx
autonomous: false
requirements: [EXP-01, EXP-02, EXP-03, EXP-04]

must_haves:
  truths:
    - "User can copy army list as formatted text to clipboard via Export dropdown"
    - "User can open a print preview dialog and print the army list"
    - "User can save army list as JSON file via native save dialog"
    - "User can export army list as PDF file via jsPDF lazy-loaded"
    - "Export dropdown is always visible in ArmyListDetailSheet header"
  artifacts:
    - path: "src/features/army-lists/ExportDropdown.tsx"
      provides: "DropdownMenu with 4 export format options"
      exports: ["ExportDropdown"]
    - path: "src/features/army-lists/PrintPreviewDialog.tsx"
      provides: "Print preview dialog with @media print support"
      exports: ["PrintPreviewDialog"]
    - path: "src/styles/globals.css"
      provides: "@media print rules hiding app shell"
      contains: "@media print"
  key_links:
    - from: "src/features/army-lists/ExportDropdown.tsx"
      to: "src/lib/exportArmyList.ts"
      via: "handler functions use formatArmyListForExport + buildClipboardText + buildJsonFormat"
      pattern: "import.*exportArmyList"
    - from: "src/features/army-lists/ArmyListDetailSheet.tsx"
      to: "src/features/army-lists/ExportDropdown.tsx"
      via: "renders ExportDropdown in header area"
      pattern: "ExportDropdown"
    - from: "src/features/army-lists/ArmyListsPage.tsx"
      to: "src/features/army-lists/PrintPreviewDialog.tsx"
      via: "sibling portal pattern with printPreviewOpen state"
      pattern: "PrintPreviewDialog"
---

<objective>
Build the Export UI: ExportDropdown component (4 format options), PrintPreviewDialog (sibling portal), @media print CSS, and wire all export handlers into ArmyListDetailSheet and ArmyListsPage. This delivers all 4 export formats end-to-end.

Purpose: Completes EXP-01 through EXP-04 by connecting the formatting utility (Plan 01) to UI triggers, Tauri plugin APIs, and file I/O. After this plan, users can copy, print, save JSON, and export PDF from any army list.

Output: ExportDropdown.tsx, PrintPreviewDialog.tsx, modified ArmyListDetailSheet.tsx and ArmyListsPage.tsx, @media print CSS, PrintPreviewDialog test.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-list-export/94-CONTEXT.md
@.planning/phases/94-list-export/94-RESEARCH.md
@.planning/phases/94-list-export/94-PATTERNS.md
@.planning/phases/94-list-export/94-UI-SPEC.md
@.planning/phases/94-list-export/94-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — the export utility functions -->

From src/lib/exportArmyList.ts (created in Plan 01):
```typescript
export interface ExportUnit {
  displayName: string;
  points: number;
  isWarlord: boolean;
  isGhost: boolean;
  leaderLabel: string | null;
  enhancementName: string | null;
}

export interface ExportData {
  list: ArmyList;
  factionName: string | null;
  sortedUnits: ExportUnit[];
  enhancements: ArmyListEnhancement[];
  totalPoints: number;
  enhancementTotal: number;
}

export function formatArmyListForExport(
  list: ArmyList,
  units: ArmyListUnitRow[],
  enhancements: ArmyListEnhancement[],
  factionName: string | null,
): ExportData;

export function buildClipboardText(data: ExportData): string;
export function buildJsonFormat(data: ExportData): string;
export function slugify(name: string): string;
export function dateStamp(): string;
```

From src/features/army-lists/ArmyListDetailSheet.tsx (current props):
```typescript
interface ArmyListDetailSheetProps {
  open: boolean;
  list: ArmyList | null;
  onClose: () => void;
  onEdit: (list: ArmyList) => void;
  onDelete: (list: ArmyList) => void;
  onAddUnit: () => void;
  onConfigureUnit: (armyListUnitId: number) => void;
  onEnhanceUnit: (armyListUnitId: number) => void;
  onBrowseDatasheets: () => void;
}
```

From src/features/army-lists/ArmyListsPage.tsx (sibling portal state, lines 38-83):
```typescript
const [selectedListId, setSelectedListId] = useState<number | null>(null);
const [datasheetBrowserOpen, setDatasheetBrowserOpen] = useState(false);
// ... existing portal states
const closeDetail = () => {
  setSelectedListId(null);
  setUnitPickerOpen(false);
  setLoadoutUnitId(null);
  setEnhancementUnitId(null);
  setDatasheetBrowserOpen(false);
};
```

From src/features/army-lists/DatasheetBrowserDialog.tsx (Dialog sibling portal analog):
```typescript
interface DatasheetBrowserDialogProps {
  open: boolean;
  listId: number | null;
  factionId: number | null;
  onClose: () => void;
}
export function DatasheetBrowserDialog({ open, listId, factionId, onClose }) {
  return (
    <Dialog open={open} onOpenChange={(o) => { if (!o) onClose(); }}>
      <DialogContent className="sm:max-w-[700px]">...</DialogContent>
    </Dialog>
  );
}
```

From src/components/common/AppSidebar.tsx (DropdownMenu analog, lines 80-145):
```tsx
<DropdownMenu>
  <DropdownMenuTrigger asChild>
    <Button variant="outline" ...>...</Button>
  </DropdownMenuTrigger>
  <DropdownMenuContent side="right" align="start">
    <DropdownMenuItem onClick={...}>...</DropdownMenuItem>
  </DropdownMenuContent>
</DropdownMenu>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExportDropdown, PrintPreviewDialog, and @media print CSS</name>
  <read_first>
    - src/features/army-lists/DatasheetBrowserDialog.tsx (Dialog sibling portal analog)
    - src/components/common/AppSidebar.tsx (DropdownMenu analog, lines 80-145)
    - src/lib/exportArmyList.ts (Plan 01 output — ExportData, ExportUnit types)
    - src/types/armyList.ts (ArmyList, ArmyListUnitRow, ArmyListEnhancement)
    - src/styles/globals.css (current state — add @media print block)
    - .planning/phases/94-list-export/94-UI-SPEC.md (component inventory, interaction contract, print layout, copywriting)
    - .planning/phases/94-list-export/94-PATTERNS.md (ExportDropdown and PrintPreviewDialog sections)
  </read_first>
  <files>
    src/features/army-lists/ExportDropdown.tsx,
    src/features/army-lists/PrintPreviewDialog.tsx,
    src/styles/globals.css,
    tests/army-lists/PrintPreviewDialog.test.tsx
  </files>
  <action>
    Create src/features/army-lists/ExportDropdown.tsx:
    - Named export `ExportDropdown` with props: onCopyToClipboard, onPrint, onSaveJson, onSavePdf (all `() => void`), disabled (optional boolean).
    - Uses shadcn DropdownMenu/DropdownMenuTrigger/DropdownMenuContent/DropdownMenuItem from `@/components/ui/dropdown-menu`.
    - Trigger: Button variant="outline" size="sm" with Download icon from lucide-react + "Export" label (per UI-SPEC copywriting contract).
    - Four DropdownMenuItems in order per D-01: Clipboard icon + "Copy to Clipboard", Printer icon + "Print", FileJson icon + "Save as JSON", FileText icon + "Save as PDF".
    - Each item fires the corresponding onClick prop.
    - Icons from lucide-react: Download, Clipboard, Printer, FileJson, FileText.
    - Component is presentational only — no async logic, no state.

    Create src/features/army-lists/PrintPreviewDialog.tsx:
    - Named export `PrintPreviewDialog` with props: open (boolean), list (ArmyList | null), units (ArmyListUnitRow[]), enhancements (ArmyListEnhancement[]), factionName (string | null), onClose (() => void).
    - Uses Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription/DialogFooter from `@/components/ui/dialog`.
    - Dialog shell follows DatasheetBrowserDialog pattern: `open={open} onOpenChange={(o) => { if (!o) onClose(); }}`.
    - DialogContent className includes `sm:max-w-3xl print:hidden` — the `print:hidden` hides the dialog chrome during printing (per D-08, Pitfall 5).
    - Inside DialogContent, render a div with className `print-content` and id `print-content`. This div contains the full army list layout per UI-SPEC Print Layout Structure: army name + points header, faction/detachment/date line, separator, UNITS section with leader pairs indented (target first, leader with indented prefix), ghost units shown italic with "(Planned)", warlord units with "(Warlord)" bold suffix, ENHANCEMENTS section, total line.
    - Uses formatArmyListForExport from `@/lib/exportArmyList` to transform data for display.
    - Uses shadcn Table/TableHeader/TableRow/TableHead/TableBody/TableCell for the units table.
    - DialogFooter has "Print" Button (default variant, calls `window.print()`) and "Close" Button (outline variant, calls onClose).
    - Print layout uses screen-friendly styles; @media print in globals.css handles the print-specific overrides.

    In src/styles/globals.css, add an `@media print` block at the end of the file:
    - Hide everything in body except #print-content: `body > *:not(#print-content) { display: none !important; }` and `.print-content { display: block !important; position: absolute; top: 0; left: 0; width: 100%; }`.
    - Also hide all elements with `data-radix-portal` attribute (Dialog overlay/backdrop): `[data-radix-portal] { display: none !important; }`.
    - Then make #print-content visible: `#print-content { display: block !important; position: fixed; top: 0; left: 0; width: 100%; background: white; color: black; }`.
    - Print typography per UI-SPEC: body 11pt, headings 13pt bold, display 16pt bold.
    - Section separators: 1px solid #cccccc.
    - Ghost unit names: italic style (per UI-SPEC color contract).

    Create tests/army-lists/PrintPreviewDialog.test.tsx:
    - Uses render from @testing-library/react and screen for queries.
    - Mock `@/lib/exportArmyList` only if needed; prefer passing pre-formatted data via props.
    - Test: renders army name when open=true and list provided.
    - Test: renders unit names from the units array.
    - Test: Print button is present with text "Print".
    - Test: does not render content when open=false.
    - Wrap render in a div to avoid Radix Dialog portal issues in jsdom — use `container` option if needed.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/army-lists/PrintPreviewDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ExportDropdown.tsx exports ExportDropdown component with 4 DropdownMenuItems
    - ExportDropdown trigger button has text "Export" and uses Download icon
    - PrintPreviewDialog.tsx exports PrintPreviewDialog with Dialog shell matching DatasheetBrowserDialog pattern
    - PrintPreviewDialog DialogContent has `print:hidden` in className
    - PrintPreviewDialog has a div with id="print-content" containing the list layout
    - PrintPreviewDialog has "Print" button calling window.print() and "Close" button
    - globals.css has @media print block with rules hiding body children except #print-content
    - globals.css @media print hides [data-radix-portal] elements
    - tests/army-lists/PrintPreviewDialog.test.tsx has at least 4 tests passing (renders army name, renders unit names, Print button present, does not render when closed)
  </acceptance_criteria>
  <done>ExportDropdown renders 4 format options with correct icons and labels. PrintPreviewDialog renders army list in print-friendly layout with @media print CSS. Print button triggers window.print(). Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire export handlers into ArmyListDetailSheet and ArmyListsPage</name>
  <read_first>
    - src/features/army-lists/ArmyListDetailSheet.tsx (full file — understand current props and header layout)
    - src/features/army-lists/ArmyListsPage.tsx (full file — understand sibling portal state machine)
    - src/features/army-lists/ExportDropdown.tsx (Task 1 output)
    - src/features/army-lists/PrintPreviewDialog.tsx (Task 1 output)
    - src/lib/exportArmyList.ts (Plan 01 output — all exported functions)
    - src/features/data-health/BackupCard.tsx (save dialog + invoke pattern analog, lines 56-93)
    - .planning/phases/94-list-export/94-RESEARCH.md (Pattern 1-4, anti-patterns)
    - .planning/phases/94-list-export/94-UI-SPEC.md (interaction contract, state catalogue, copywriting)
  </read_first>
  <files>
    src/features/army-lists/ArmyListDetailSheet.tsx,
    src/features/army-lists/ArmyListsPage.tsx
  </files>
  <action>
    Modify ArmyListDetailSheet.tsx:
    - Add `onPrintPreview: () => void` to ArmyListDetailSheetProps (sibling portal trigger, same pattern as onBrowseDatasheets).
    - Import ExportDropdown from `./ExportDropdown`.
    - Import formatArmyListForExport, buildClipboardText, buildJsonFormat, slugify, dateStamp from `@/lib/exportArmyList`.
    - Import writeText from `@tauri-apps/plugin-clipboard-manager`.
    - Import save from `@tauri-apps/plugin-dialog`.
    - Import writeTextFile from `@tauri-apps/plugin-fs`.
    - Import invoke from `@tauri-apps/api/core`.
    - Import toast from `sonner` (already imported).

    Create 4 async handler functions inside the component (they access `list`, `units`, `listEnhancements`, `faction` from component scope):

    handleCopyToClipboard: Call formatArmyListForExport with list, units, listEnhancements, faction?.name. Call buildClipboardText on result. Call writeText(text). Toast success "List copied to clipboard". Catch -> toast error "Failed to copy -- check clipboard permissions".

    handleSaveJson: Build export data same way. Call buildJsonFormat. Call save() from plugin-dialog with title "Save Army List as JSON", defaultPath `${slugify(list.name)}-${dateStamp()}.json`, filters [{name:"JSON", extensions:["json"]}]. Guard: if (!destination) return. Call writeTextFile(destination, jsonString). Toast success "List saved as JSON". Catch -> toast error "Failed to save JSON -- check file permissions".

    handleSavePdf: Build export data. Call save() dialog first (title "Save Army List as PDF", defaultPath with .pdf, filters [{name:"PDF", extensions:["pdf"]}]). Guard null. Then lazy-load: `const { jsPDF } = await import("jspdf")` and `const autoTable = (await import("jspdf-autotable")).default`. Create doc with `new jsPDF({ orientation: "p", unit: "mm", format: "a4" })`. Build PDF content per UI-SPEC PDF Layout: army name (fontSize 18 bold), metadata line (fontSize 11), units table via autoTable with columns [Unit, Points, Notes], enhancements section, totals line (fontSize 12 bold), footer "Generated by HobbyForge" (fontSize 9, color #999999, bottom-right). Get buffer via `doc.output("arraybuffer")`. Write via `invoke("write_bytes_to_path", { destination, bytes: Array.from(new Uint8Array(buffer)) })`. Toast success "List saved as PDF". Catch -> toast error "Failed to generate PDF". CRITICAL: never call doc.save() (Pitfall 4). CRITICAL: import both jspdf and jspdf-autotable in same async scope (Pitfall 6).

    Render ExportDropdown in the header area, after ArmyListSummaryBar and before the Detachment section (around line 187, after the summary bar). Pass handlers: onCopyToClipboard={handleCopyToClipboard}, onPrint={onPrintPreview}, onSaveJson={handleSaveJson}, onSavePdf={handleSavePdf}. The export dropdown should always be visible per D-02. Place it in a `<div className="px-4 py-1">` wrapper for consistent spacing.

    Modify ArmyListsPage.tsx:
    - Import PrintPreviewDialog from `./PrintPreviewDialog`.
    - Add `const [printPreviewOpen, setPrintPreviewOpen] = useState(false)` to the portal state block.
    - Add `const openPrintPreview = () => setPrintPreviewOpen(true)` and `const closePrintPreview = () => setPrintPreviewOpen(false)` handlers.
    - Update closeDetail to also call `setPrintPreviewOpen(false)` (add to existing cleanup chain).
    - Pass `onPrintPreview={openPrintPreview}` as new prop to ArmyListDetailSheet.
    - Add `const { data: selectedListEnhancements } = useEnhancementsByList(selectedListId ?? undefined)` at ArmyListsPage level (this is a NEW hook call at page scope — do not assume it exists elsewhere) to pass enhancements to PrintPreviewDialog.
    - Derive factionName from `selectedList?.faction_id` + `factions` array (same pattern used in ArmyListCardWrapper).
    - Render PrintPreviewDialog as sibling portal at the bottom of the portals block (after DatasheetBrowserDialog), with props: open={printPreviewOpen}, list={selectedList}, units={selectedListUnits ?? []}, enhancements from the hook, factionName derived above, onClose={closePrintPreview}.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - ArmyListDetailSheetProps includes `onPrintPreview: () => void`
    - ArmyListDetailSheet renders ExportDropdown between ArmyListSummaryBar and Detachment section
    - handleCopyToClipboard calls writeText from @tauri-apps/plugin-clipboard-manager (NOT navigator.clipboard)
    - handleSaveJson calls save() with .json filter then writeTextFile to returned path
    - handleSavePdf lazy-imports jspdf and jspdf-autotable with `await import()`, never uses doc.save()
    - handleSavePdf calls invoke("write_bytes_to_path") with Uint8Array from doc.output("arraybuffer")
    - All save handlers guard against null destination (user cancels dialog)
    - ArmyListsPage has printPreviewOpen state and renders PrintPreviewDialog as sibling portal
    - ArmyListsPage closeDetail resets printPreviewOpen to false
    - pnpm build completes without TypeScript errors
  </acceptance_criteria>
  <done>Export dropdown visible in every army list detail sheet. Clipboard copy writes tournament-style text via Tauri plugin. Print preview opens as sibling portal dialog with window.print(). JSON saves via native dialog + writeTextFile. PDF generated via lazy-loaded jsPDF + written via Rust command. All toasts show per copywriting contract. pnpm build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete list export feature with 4 formats: clipboard text, print preview, JSON file, PDF file. Export dropdown added to army list detail sheet header.</what-built>
  <how-to-verify>
    1. Run `pnpm tauri dev` and open an army list with at least 2-3 units (include a warlord-designated unit if available)
    2. In the army list detail sheet, verify the "Export" dropdown button is visible near the summary bar
    3. Click "Export" -> "Copy to Clipboard":
       - Paste into a text editor — verify tournament-style format with army name, faction, detachment, units with points, total line
       - Verify toast says "List copied to clipboard"
    4. Click "Export" -> "Print":
       - Verify PrintPreviewDialog opens with army list content (name, units, points, totals)
       - Click "Print" button — verify browser print dialog opens
       - Verify print preview shows only the list content (no app shell, no dialog chrome)
       - Close print dialog and close the PrintPreviewDialog
    5. Click "Export" -> "Save as JSON":
       - Verify native save dialog opens with .json filter and a sensible default filename
       - Save the file, open it in a text editor — verify JSON has "format": "hobbyforge-army-list", "version": "1.0", units array, enhancements array
       - Verify toast says "List saved as JSON"
    6. Click "Export" -> "Save as PDF":
       - Verify native save dialog opens with .pdf filter
       - Save the file, open the PDF — verify it has army name header, unit table, totals, "Generated by HobbyForge" footer
       - Verify toast says "List saved as PDF"
    7. Test with an empty army list — verify all 4 formats produce valid (minimal) output without errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> filename | Army list name flows through slugify() before reaching save dialog defaultPath |
| App data -> clipboard | Army list text written to system clipboard via Tauri plugin |
| App data -> filesystem | JSON text and PDF binary written to user-chosen paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering | slugify() in export handlers | mitigate | slugify strips all non-alphanumeric chars; save dialog validates final path |
| T-94-02 | Information Disclosure | clipboard write | accept | Content is user's own army list data; no PII |
| T-94-03 | Tampering | write_bytes_to_path Rust cmd | mitigate | Path from save() dialog only; user confirms destination |
| T-94-04 | Denial of Service | jsPDF lazy import | accept | Async import may take 500ms on slow disk; loading state communicated via toast/UI |
| T-94-SC | Tampering | npm/cargo installs | mitigate | All packages Approved in RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
pnpm build (TypeScript compilation passes)
pnpm test -- tests/army-lists/PrintPreviewDialog.test.tsx
pnpm test -- tests/lib/exportArmyList.test.ts
Manual verification: all 4 export formats produce correct output in running app
</verification>

<success_criteria>
- Export dropdown visible in army list detail sheet header with 4 format options
- Clipboard copy produces tournament-style text with leader pairs, ghost markers, warlord tags
- Print preview opens in Dialog, window.print() shows only list content via @media print
- JSON file saved via native dialog with correct versioned schema
- PDF file generated via lazy-loaded jsPDF with unit table and HobbyForge footer
- All save handlers guard null destinations (user cancel)
- All success/error toasts match UI-SPEC copywriting contract
- pnpm build passes without errors
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/94-list-export/94-02-SUMMARY.md` when done
</output>

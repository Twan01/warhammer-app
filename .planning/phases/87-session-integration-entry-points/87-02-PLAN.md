---
phase: 87-session-integration-entry-points
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/dashboard/NextPaintingActionCard.tsx
  - src/features/dashboard/CurrentFocusCard.tsx
  - src/features/dashboard/DashboardPage.tsx
  - src/hooks/useKanbanEnrichment.ts
  - src/features/painting-projects/KanbanCard.tsx
  - src/features/painting-projects/KanbanColumn.tsx
  - src/features/painting-projects/KanbanBoard.tsx
  - src/features/recipes/RecipeDetailSheet.tsx
  - src/features/units/AppliedRecipesTab.tsx
autonomous: true
requirements: [EP-01, EP-02, EP-03, EP-04, EP-05, EP-06]

must_haves:
  truths:
    - "User can reach Painting Mode from Dashboard NextPaintingActionCard"
    - "User can reach Painting Mode from CurrentFocusCard"
    - "User can reach Painting Mode from Unit Detail applied recipe panel"
    - "User can reach Painting Mode from Kanban card"
    - "User can reach Painting Mode from RecipeDetailSheet"
    - "Entry point surfaces do not show a Paint link when there is no applied recipe"
  artifacts:
    - path: "src/features/dashboard/NextPaintingActionCard.tsx"
      provides: "Link to painting mode using data.assignment_id"
      contains: "bare-layout/painting-mode"
    - path: "src/features/dashboard/CurrentFocusCard.tsx"
      provides: "Optional onPaint prop and Paint ghost button"
      contains: "onPaint"
    - path: "src/hooks/useKanbanEnrichment.ts"
      provides: "assignmentIds map in KanbanEnrichment interface"
      contains: "assignmentIds"
    - path: "src/features/painting-projects/KanbanCard.tsx"
      provides: "Paint button when assignmentId defined"
      contains: "onPaint"
    - path: "src/features/recipes/RecipeDetailSheet.tsx"
      provides: "Per-unit Paint button from useAssignmentsByRecipe query"
      contains: "useAssignmentsByRecipe"
    - path: "src/features/units/AppliedRecipesTab.tsx"
      provides: "Start Painting / Continue button per applied recipe row"
      contains: "bare-layout/painting-mode"
  key_links:
    - from: "src/features/dashboard/NextPaintingActionCard.tsx"
      to: "/bare-layout/painting-mode/$assignmentId"
      via: "Link component with params"
      pattern: "bare-layout/painting-mode"
    - from: "src/features/dashboard/DashboardPage.tsx"
      to: "src/features/dashboard/CurrentFocusCard.tsx"
      via: "onPaint prop wired when primaryAssignment exists"
      pattern: "onPaint.*primaryAssignment"
    - from: "src/hooks/useKanbanEnrichment.ts"
      to: "src/features/painting-projects/KanbanCard.tsx"
      via: "assignmentIds map threaded through KanbanColumn"
      pattern: "assignmentIds"
---

<objective>
Wire all five entry point surfaces to navigate to Painting Mode, and guard each CTA so it only appears when an applied recipe assignment exists.

Purpose: Users can reach Painting Mode from any natural surface in the app where they see their painting units, rather than having to navigate to a specific route manually.

Output: Modified NextPaintingActionCard, CurrentFocusCard, AppliedRecipesTab, KanbanCard (+ KanbanColumn, KanbanBoard, useKanbanEnrichment), and RecipeDetailSheet — all with navigation to `/bare-layout/painting-mode/$assignmentId`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-session-integration-entry-points/87-CONTEXT.md
@.planning/phases/87-session-integration-entry-points/87-RESEARCH.md
@.planning/phases/87-session-integration-entry-points/87-PATTERNS.md
@.planning/phases/87-session-integration-entry-points/87-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/features/dashboard/NextPaintingActionCard.tsx:
// data comes from useNextPaintingAction() which returns FirstIncompleteStep
// FirstIncompleteStep has: assignment_id: number, description: string, etc.
// Current Link target: to="/painting-projects"

From src/features/dashboard/CurrentFocusCard.tsx:
export interface CurrentFocusCardProps {
  unit: Unit | null;
  faction: Faction | undefined;
  photo: UnitPhotoWithUrl | undefined;
  onOpen: () => void;
  onLog: () => void;
  recipeName?: string | null;
  extraRecipeCount?: number;
  workflowPosition?: WorkflowPosition | null;
  appliedProgress?: AppliedRecipeProgress | null;
}

From src/hooks/useKanbanEnrichment.ts:
export interface KanbanEnrichment {
  recipeNames: Map<number, string>;
  photoCounts: Map<number, number>;
  appliedProgress: Map<number, AppliedRecipeProgress>;
}

From src/features/painting-projects/KanbanCard.tsx:
export interface KanbanCardProps {
  unit: Unit;
  faction: Faction | undefined;
  onRemoveFromBoard: (unit: Unit) => void;
  onEditUnit: (unit: Unit) => void;
  onLogSession: (unitId: number) => void;
  recipeName?: string;
  photoCount?: number;
  workflowPosition?: WorkflowPosition | null;
  appliedProgress?: AppliedRecipeProgress | null;
}

From src/features/painting-projects/KanbanColumn.tsx:
export interface KanbanColumnProps {
  status: PaintingStatus;
  units: Unit[];
  factionMap: Map<number, Faction>;
  onRemoveFromBoard: (unit: Unit) => void;
  onEditUnit: (unit: Unit) => void;
  onLogSession: (unitId: number) => void;
  enrichment?: KanbanEnrichment;
  workflowPositions?: Map<number, WorkflowPosition>;
}

From src/hooks/useRecipeAssignments.ts:
export function useAssignmentsByRecipe(recipeId: number | undefined);
// Returns { data: RecipeAssignment[] } — hook has enabled: recipeId !== undefined guard

From src/features/dashboard/DashboardPage.tsx:
// primaryAssignment is computed as: focusAssignments[focusAssignments.length - 1]
// primaryAssignment has .id (assignment ID) and .recipe_id, .unit_id
// DashboardPage does NOT currently import useNavigate — must be added
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire dashboard entry points (EP-01, EP-02) and unit detail (EP-03)</name>
  <files>
    src/features/dashboard/NextPaintingActionCard.tsx,
    src/features/dashboard/CurrentFocusCard.tsx,
    src/features/dashboard/DashboardPage.tsx,
    src/features/units/AppliedRecipesTab.tsx
  </files>
  <read_first>
    src/features/dashboard/NextPaintingActionCard.tsx,
    src/features/dashboard/CurrentFocusCard.tsx,
    src/features/dashboard/DashboardPage.tsx,
    src/features/units/AppliedRecipesTab.tsx,
    .planning/phases/87-session-integration-entry-points/87-PATTERNS.md,
    .planning/phases/87-session-integration-entry-points/87-UI-SPEC.md
  </read_first>
  <action>
    **NextPaintingActionCard.tsx** per D-08:
    - Replace the existing Link (lines 64-69) that navigates to `/painting-projects` with a Link to `/bare-layout/painting-mode/$assignmentId` using `params={{ assignmentId: String(data.assignment_id) }}`.
    - Change link text from "Go to recipe" to "Start Painting".
    - Keep existing className and styling unchanged.
    - The `data` object from `useNextPaintingAction()` already has `assignment_id` on `FirstIncompleteStep` type.

    **CurrentFocusCard.tsx** per D-09, D-14:
    - Add `onPaint?: () => void` to `CurrentFocusCardProps` interface.
    - In the button column (after the "Log" button), add a conditional Paint button:
      `{onPaint && (<Button variant="ghost" size="sm" onClick={onPaint} data-testid="paint-btn"><Palette size={14} className="mr-1.5" aria-hidden={true} /> Paint</Button>)}`
    - Import `Palette` from `lucide-react` (add to existing icon imports).

    **DashboardPage.tsx** per D-09, D-14:
    - Add `import { useNavigate } from "@tanstack/react-router"` to imports.
    - Add `const navigate = useNavigate()` in the component body.
    - Where `CurrentFocusCard` is rendered, add `onPaint` prop:
      `onPaint={primaryAssignment !== undefined ? () => navigate({ to: "/bare-layout/painting-mode/$assignmentId", params: { assignmentId: String(primaryAssignment.id) } }) : undefined}`
    - `primaryAssignment` is already computed at line ~104. Only pass `onPaint` when it exists (D-14 guard).

    **AppliedRecipesTab.tsx** per D-10:
    - Add `import { useNavigate } from "@tanstack/react-router"` and `import { Palette } from "lucide-react"`.
    - Add `const navigate = useNavigate()` in the component body.
    - In each assignment row header (the flex div with recipe name and delete button), add a "Paint" button between the recipe name and delete button:
      `<Button variant="outline" size="sm" onClick={() => navigate({ to: "/bare-layout/painting-mode/$assignmentId", params: { assignmentId: String(assignment.id) } })} data-testid={"paint-btn-" + assignment.id}>` with icon `<Palette size={14} className="mr-1.5" />` and text.
    - Per UI-SPEC: Show "Start Painting" when 0 steps are done, "Continue" when >0 steps are done. If step progress data is not readily available in this component, use "Paint" as a safe default label. Check if `appliedProgress` or similar data is passed to this component. If not available without additional queries, use static "Paint" label (acceptable per Claude's discretion on micro-copy).
    - Ensure the row flex container has `gap-2` for spacing between elements.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - NextPaintingActionCard Link `to` is `/bare-layout/painting-mode/$assignmentId` with `params={{ assignmentId: String(data.assignment_id) }}`
    - NextPaintingActionCard Link text is "Start Painting"
    - CurrentFocusCardProps has `onPaint?: () => void`
    - CurrentFocusCard renders Paint button only when `onPaint` is defined
    - Paint button has `data-testid="paint-btn"` and uses Palette icon at size={14}
    - DashboardPage imports and calls `useNavigate`
    - DashboardPage passes `onPaint` to CurrentFocusCard only when `primaryAssignment !== undefined`
    - AppliedRecipesTab has a Paint/Start Painting button per assignment row with `data-testid` pattern `paint-btn-{id}`
    - AppliedRecipesTab button navigates to `/bare-layout/painting-mode/$assignmentId`
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Dashboard NextPaintingActionCard links directly to painting mode. CurrentFocusCard shows a "Paint" button when an assignment exists. AppliedRecipesTab has a paint action per applied recipe row. All guarded by D-14.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Kanban entry point (EP-04) + RecipeDetailSheet (EP-05)</name>
  <files>
    src/hooks/useKanbanEnrichment.ts,
    src/features/painting-projects/KanbanCard.tsx,
    src/features/painting-projects/KanbanColumn.tsx,
    src/features/painting-projects/KanbanBoard.tsx,
    src/features/recipes/RecipeDetailSheet.tsx
  </files>
  <read_first>
    src/hooks/useKanbanEnrichment.ts,
    src/features/painting-projects/KanbanCard.tsx,
    src/features/painting-projects/KanbanColumn.tsx,
    src/features/painting-projects/KanbanBoard.tsx,
    src/features/recipes/RecipeDetailSheet.tsx,
    src/hooks/useRecipeAssignments.ts,
    .planning/phases/87-session-integration-entry-points/87-PATTERNS.md,
    .planning/phases/87-session-integration-entry-points/87-UI-SPEC.md
  </read_first>
  <action>
    **useKanbanEnrichment.ts** per EP-04:
    - Add `assignmentIds: Map<number, number>` to `KanbanEnrichment` interface (unit_id to primary assignment.id).
    - In `queryFn`, declare `const assignmentIdsMap = new Map<number, number>()` before the `Promise.all(sortedIds.map(...))` block.
    - Inside the map callback, after `appliedProgressMap.set(unitId, ...)`, add `assignmentIdsMap.set(unitId, primary.id)`. The `primary` variable is already in scope (line ~38: `const primary = assignments[assignments.length - 1]`).
    - Add `assignmentIds: assignmentIdsMap` to the return object.

    **KanbanCard.tsx** per D-11, D-14:
    - Add `assignmentId?: number` and `onPaint?: (assignmentId: number) => void` to `KanbanCardProps`.
    - Import `Palette` from `lucide-react`.
    - In the card header (after the existing Log Session button and before KanbanCardActions), add a paint button conditionally:
      ```
      {onPaint && assignmentId !== undefined && (
        <button type="button" onClick={(e) => { e.stopPropagation(); onPaint(assignmentId); }}
          aria-label={`Open painting mode for ${unit.name}`} title="Paint"
          className="h-6 w-6 flex items-center justify-center rounded text-muted-foreground hover:text-foreground transition-colors duration-150 focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring">
          <Palette size={14} />
        </button>
      )}
      ```
    - Do NOT use `useNavigate` inside KanbanCard (DnD sortable context anti-pattern per RESEARCH.md). Navigation is handled via the `onPaint` callback.

    **KanbanColumn.tsx**:
    - Add `onPaint?: (assignmentId: number) => void` to `KanbanColumnProps`.
    - Pass `assignmentId={enrichment?.assignmentIds?.get(u.id)}` and `onPaint={onPaint}` to each `KanbanCard` in the render loop.

    **KanbanBoard.tsx**:
    - Add `import { useNavigate } from "@tanstack/react-router"`.
    - Add `const navigate = useNavigate()` in the component body.
    - Add `function handlePaint(assignmentId: number) { navigate({ to: "/bare-layout/painting-mode/$assignmentId", params: { assignmentId: String(assignmentId) } }); }`.
    - Pass `onPaint={handlePaint}` to each `KanbanColumn` in the render loop.
    - In the `DragOverlay` KanbanCard instance, pass `onPaint={undefined}` (drag overlay should not have action buttons).

    **RecipeDetailSheet.tsx** per D-12, D-14:
    - Add `import { useAssignmentsByRecipe } from "@/hooks/useRecipeAssignments"`.
    - Add `const { data: assignments = [] } = useAssignmentsByRecipe(recipe?.id)` after existing hooks. The hook has `enabled: recipeId !== undefined` guard so passing `recipe?.id` is safe.
    - In the Sheet body, after the existing sessions section, add an "Applied to Units" section when `assignments.length > 0`:
      Render each assignment as a row with the unit name from `unitMap.get(a.unit_id)` and a "Paint {unitName}" outline button that calls `navigate({ to: "/bare-layout/painting-mode/$assignmentId", params: { assignmentId: String(a.id) } })`.
    - `useNavigate` is already imported (line 3) and instantiated (line 182) in RecipeDetailSheet.
    - `unitMap` is already computed (lines 84-88).
    - Use `data-testid={"paint-unit-btn-" + a.id}` on each button.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `KanbanEnrichment` interface has `assignmentIds: Map<number, number>`
    - `useKanbanEnrichment` queryFn populates `assignmentIdsMap` from `primary.id` and returns it
    - `KanbanCardProps` has `assignmentId?: number` and `onPaint?: (assignmentId: number) => void`
    - KanbanCard renders Palette icon button only when both `onPaint` and `assignmentId` are defined (D-14)
    - KanbanCard paint button calls `e.stopPropagation()` before `onPaint(assignmentId)` (prevents DnD interference)
    - KanbanCard does NOT import `useNavigate` (anti-pattern guard)
    - `KanbanColumnProps` has `onPaint?: (assignmentId: number) => void` and threads it to KanbanCard
    - KanbanColumn passes `enrichment?.assignmentIds?.get(u.id)` as `assignmentId` to each card
    - KanbanBoard adds `useNavigate`, creates `handlePaint`, and passes `onPaint={handlePaint}` to each KanbanColumn
    - RecipeDetailSheet calls `useAssignmentsByRecipe(recipe?.id)` and renders per-assignment Paint buttons with `data-testid` pattern `paint-unit-btn-{id}`
    - RecipeDetailSheet Paint buttons navigate to `/bare-layout/painting-mode/$assignmentId`
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Kanban cards show a paint button when an assignment exists, threaded from KanbanBoard through KanbanColumn. RecipeDetailSheet shows per-unit paint buttons for applied recipes. All five entry points are wired. EP-06 D-14 guards suppress CTAs when no assignment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params -> component | assignmentId from route params is already validated as number in PaintingModePage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-03 | Tampering | Entry point navigation params | accept | assignmentId is cast to number and validated in PaintingModePage; invalid IDs show "Assignment not found" |
| T-87-04 | Information Disclosure | KanbanEnrichment assignmentIds | accept | Assignment IDs are low-sensitivity internal identifiers; no PII exposure |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no errors
- `pnpm test` full suite passes (no existing tests broken)
- Manual: Dashboard NextPaintingActionCard "Start Painting" navigates to painting mode
- Manual: CurrentFocusCard "Paint" button appears when unit has assignment, navigates correctly
- Manual: AppliedRecipesTab "Paint" button per row navigates to painting mode
- Manual: Kanban card paint icon appears when unit has assignment, navigates correctly
- Manual: RecipeDetailSheet shows "Paint {unit}" buttons for applied units
- Manual: All entry points suppress paint CTA when no assignment exists
</verification>

<success_criteria>
- All five entry point surfaces navigate to `/bare-layout/painting-mode/$assignmentId` (EP-01 through EP-05)
- No paint CTA shown when assignment does not exist (EP-06, D-14)
- KanbanEnrichment extended with assignmentIds at zero extra query cost
- TypeScript compiles cleanly
- No existing tests broken
</success_criteria>

<output>
Create `.planning/phases/87-session-integration-entry-points/87-02-SUMMARY.md` when done
</output>

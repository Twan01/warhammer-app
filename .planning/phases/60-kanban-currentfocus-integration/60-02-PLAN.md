---
phase: 60-kanban-currentfocus-integration
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - src/features/painting-projects/KanbanCard.tsx
  - src/features/painting-projects/KanbanColumn.tsx
  - src/features/painting-projects/KanbanBoard.tsx
  - src/features/dashboard/CurrentFocusCard.tsx
  - src/features/dashboard/DashboardPage.tsx
  - tests/painting/KanbanCard.test.tsx
  - tests/dashboard/CurrentFocusCard.test.tsx
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02

must_haves:
  truths:
    - "KanbanCard shows workflow section and next step name below recipe name when position available"
    - "KanbanCard shows existing getNextActionHint when no workflow position available"
    - "CurrentFocusCard shows section-aware guidance with technique and step count"
    - "Cards degrade gracefully for all five scenarios: no recipe, no sessions, flat recipe, sectioned recipe, complete"
    - "Workflow display is additive -- never removes existing card information"
  artifacts:
    - path: "src/features/painting-projects/KanbanCard.tsx"
      provides: "Workflow context line on kanban cards"
      contains: "workflowPosition"
    - path: "src/features/dashboard/CurrentFocusCard.tsx"
      provides: "Section-aware workflow guidance on focus card"
      contains: "workflowPosition"
    - path: "tests/painting/KanbanCard.test.tsx"
      provides: "Extended tests for workflow display on KanbanCard"
    - path: "tests/dashboard/CurrentFocusCard.test.tsx"
      provides: "New tests for workflow display on CurrentFocusCard"
  key_links:
    - from: "src/features/painting-projects/KanbanBoard.tsx"
      to: "src/hooks/useWorkflowPositions.ts"
      via: "useWorkflowPositions(activeUnitIds)"
      pattern: "useWorkflowPositions"
    - from: "src/features/dashboard/DashboardPage.tsx"
      to: "src/hooks/useWorkflowPositions.ts"
      via: "useWorkflowPositions for focus unit"
      pattern: "useWorkflowPositions"
    - from: "src/features/painting-projects/KanbanColumn.tsx"
      to: "src/features/painting-projects/KanbanCard.tsx"
      via: "workflowPosition prop passthrough via Map.get"
      pattern: "workflowPositions"
---

<objective>
Wire workflow position data into KanbanCard and CurrentFocusCard so users see where they are in a recipe.

Purpose: PROJ-01 requires KanbanCard to show section/step context; PROJ-02 requires CurrentFocusCard to show section-aware guidance. Both consume the hook and pure function built in Plan 01.

Output: Modified KanbanBoard -> KanbanColumn -> KanbanCard prop chain, modified DashboardPage -> CurrentFocusCard, and component tests for both cards.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-kanban-currentfocus-integration/60-CONTEXT.md
@.planning/phases/60-kanban-currentfocus-integration/60-UI-SPEC.md
@.planning/phases/60-kanban-currentfocus-integration/60-PATTERNS.md
@.planning/phases/60-kanban-currentfocus-integration/60-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/lib/computeWorkflowPosition.ts (created in Plan 01):
  export interface WorkflowPosition {
    sectionName: string | null;
    sectionIndex: number | null;
    totalSections: number;
    stepName: string | null;
    stepIndex: number | null;
    totalSteps: number;
    technique: string | null;
    isComplete: boolean;
    nextStepName: string | null;
  }

From src/hooks/useWorkflowPositions.ts (created in Plan 01):
  export function useWorkflowPositions(unitIds: number[]): UseQueryResult<Map<number, WorkflowPosition>>
  export const WORKFLOW_POSITIONS_KEY: (unitIds: number[]) => readonly string[]

From src/features/painting-projects/KanbanCard.tsx (current):
  interface KanbanCardProps { unit, faction, onRemoveFromBoard, onEditUnit, onLogSession, recipeName?, photoCount? }
  Line 109-113: existing next-action hint block: {unit.status_painting !== "Completed" && <p>-> {getNextActionHint(...)}</p>}

From src/features/painting-projects/KanbanColumn.tsx (current):
  interface KanbanColumnProps { status, units, factionMap, onRemoveFromBoard, onEditUnit, onLogSession, enrichment? }
  Line 52-61: KanbanCard render with enrichment?.recipeNames.get(u.id), enrichment?.photoCounts.get(u.id)

From src/features/painting-projects/KanbanBoard.tsx (current):
  Line 53: const { data: enrichment } = useKanbanEnrichment(activeUnitIds);
  Line 153-162: KanbanColumn render passing enrichment

From src/features/dashboard/CurrentFocusCard.tsx (current):
  interface CurrentFocusCardProps { unit, faction, photo, onOpen, onLog, recipeName?, extraRecipeCount? }
  Line 66-70: recipe name row with Palette icon

From src/features/dashboard/DashboardPage.tsx (current):
  Line 82-87: focusRecipes query for focus unit
  Line 314-328: CurrentFocusCard render
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire workflow positions into Kanban prop chain</name>
  <files>src/features/painting-projects/KanbanCard.tsx, src/features/painting-projects/KanbanColumn.tsx, src/features/painting-projects/KanbanBoard.tsx, tests/painting/KanbanCard.test.tsx</files>
  <read_first>
    src/features/painting-projects/KanbanCard.tsx
    src/features/painting-projects/KanbanColumn.tsx
    src/features/painting-projects/KanbanBoard.tsx
    src/hooks/useWorkflowPositions.ts
    src/lib/computeWorkflowPosition.ts
    src/features/dashboard/getNextActionHint.ts
    tests/painting/KanbanCard.test.tsx
    .planning/phases/60-kanban-currentfocus-integration/60-UI-SPEC.md
  </read_first>
  <action>
    KanbanBoard.tsx modifications:
    - Import useWorkflowPositions from @/hooks/useWorkflowPositions
    - After the existing useKanbanEnrichment call (line 53), add: const { data: workflowPositions } = useWorkflowPositions(activeUnitIds);
    - Pass workflowPositions to each KanbanColumn (line 153-162) as a new prop
    - Also pass workflowPositions to the DragOverlay KanbanCard if desired (optional, not required)

    KanbanColumn.tsx modifications:
    - Import WorkflowPosition type from @/lib/computeWorkflowPosition
    - Add workflowPositions?: Map<number, WorkflowPosition> to KanbanColumnProps interface
    - Destructure workflowPositions from props
    - Pass workflowPosition={workflowPositions?.get(u.id)} to each KanbanCard (line 52-61)

    KanbanCard.tsx modifications:
    - Import WorkflowPosition type from @/lib/computeWorkflowPosition
    - Add workflowPosition?: WorkflowPosition | null to KanbanCardProps interface
    - Destructure workflowPosition from props
    - Replace the existing next-action hint block (lines 109-113) with workflow-aware conditional rendering per UI-SPEC:
      a. When workflowPosition exists and is not complete: show workflow context line using the arrow prefix pattern matching existing hint style (text-xs italic text-muted-foreground/70)
         - Full section+step: "{arrow} {sectionName}: {nextStepName}"
         - Section only (stepIndex null): "{arrow} {sectionName}"
         - Flat (sectionName null): "{arrow} step {stepIndex+1}/{totalSteps}"
      b. When workflowPosition.isComplete: show "{arrow} Complete"
      c. When workflowPosition is undefined/null: fall through to existing getNextActionHint(unit.status_painting) (D-15)
    - Use the existing arrow character from line 111 (the literal string used in the codebase)
    - Apply truncate class to workflow line for card width constraint

    tests/painting/KanbanCard.test.tsx extensions:
    - Import WorkflowPosition type
    - Add test: "PROJ-01: renders workflow section and next step when workflowPosition provided" -- pass workflowPosition prop with sectionName="Armour" and nextStepName="Layer Highlight", assert both appear in rendered output
    - Add test: "PROJ-01: renders Complete when isComplete is true" -- pass workflowPosition with isComplete=true, assert "Complete" text appears
    - Add test: "PROJ-05: falls back to getNextActionHint when no workflowPosition" -- render without workflowPosition prop, assert existing hint text appears (e.g., "Apply primer" for Built status)
    - Add test: "PROJ-01: renders step N/M for flat recipe" -- pass workflowPosition with sectionName=null, stepIndex=3, totalSteps=12, assert "step 4/12" appears
    - Keep all existing tests unchanged
  </action>
  <verify>
    <automated>pnpm test -- tests/painting/KanbanCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - KanbanCardProps interface includes workflowPosition?: WorkflowPosition | null
    - KanbanColumnProps interface includes workflowPositions?: Map<number, WorkflowPosition>
    - KanbanBoard.tsx calls useWorkflowPositions(activeUnitIds) and passes result to KanbanColumn
    - KanbanCard renders "{arrow} {sectionName}: {nextStepName}" when full workflow position available
    - KanbanCard renders "{arrow} Complete" when isComplete is true
    - KanbanCard renders existing getNextActionHint when workflowPosition is null/undefined
    - All existing KanbanCard tests still pass
    - New PROJ-01/PROJ-05 tests pass
    - pnpm test -- tests/painting/KanbanCard.test.tsx exits 0
  </acceptance_criteria>
  <done>KanbanCard shows workflow context derived from the shared pure function. Existing cards without recipe data continue showing status-based hints. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire workflow position into CurrentFocusCard and DashboardPage</name>
  <files>src/features/dashboard/CurrentFocusCard.tsx, src/features/dashboard/DashboardPage.tsx, tests/dashboard/CurrentFocusCard.test.tsx</files>
  <read_first>
    src/features/dashboard/CurrentFocusCard.tsx
    src/features/dashboard/DashboardPage.tsx
    src/hooks/useWorkflowPositions.ts
    src/lib/computeWorkflowPosition.ts
    tests/painting/KanbanCard.test.tsx
    .planning/phases/60-kanban-currentfocus-integration/60-UI-SPEC.md
  </read_first>
  <action>
    DashboardPage.tsx modifications:
    - Import useWorkflowPositions from @/hooks/useWorkflowPositions
    - After the focusRecipes query block (line 82-87), add a useWorkflowPositions call for the focus unit: useWorkflowPositions(focusUnitId !== null ? [focusUnitId] : [])
    - Destructure the data as workflowPositions
    - Pass workflowPosition={workflowPositions?.get(focusUnit?.id ?? -1)} to CurrentFocusCard (around line 314-328)

    CurrentFocusCard.tsx modifications:
    - Import WorkflowPosition type from @/lib/computeWorkflowPosition
    - Import Layers icon from lucide-react (add to existing import destructure on line 17)
    - Add workflowPosition?: WorkflowPosition | null to CurrentFocusCardProps interface
    - Destructure workflowPosition from props
    - After the recipe name row (line 66-70), insert workflow guidance line per UI-SPEC, D-07 format:
      a. Conditionally render only when workflowPosition is non-null
      b. When not complete and section+step+technique: "{sectionName}: {technique} -- step {stepIndex+1}/{totalSteps}"
      c. When not complete and section+step no technique: "{sectionName} -- step {stepIndex+1}/{totalSteps}"
      d. When not complete and section only: "{sectionName}"
      e. When not complete and flat (sectionName null): "Step {stepIndex+1}/{totalSteps}"
      f. When isComplete: "Recipe complete"
    - Use text-xs text-muted-foreground styling matching the recipe name row above
    - Prefix with Layers icon at size={12} with gap-1, matching the Palette icon pattern on recipe name row
    - Apply tabular-nums class on the step count portion via a span
    - Apply truncate class to the entire line

    tests/dashboard/CurrentFocusCard.test.tsx (new file):
    - Follow tests/painting/KanbanCard.test.tsx conventions: vitest imports, makeUnit/makeFaction factory functions, renderCard helper
    - No DnD wrapper needed (CurrentFocusCard is not sortable)
    - Import CurrentFocusCard from @/features/dashboard/CurrentFocusCard
    - Import WorkflowPosition type from @/lib/computeWorkflowPosition
    - Test 1: "PROJ-02: renders workflow guidance with section, technique, and step count" -- pass workflowPosition with sectionName="Armour", technique="Drybrush", stepIndex=3, totalSteps=12. Assert "Armour: Drybrush" and "step 4/12" appear.
    - Test 2: "PROJ-02: renders workflow guidance without technique" -- pass workflowPosition with sectionName="Armour", technique=null, stepIndex=3, totalSteps=12. Assert "Armour" and "step 4/12" appear. Assert ":" does not appear between section name and step.
    - Test 3: "PROJ-02: renders Recipe complete when isComplete" -- pass workflowPosition with isComplete=true. Assert "Recipe complete" text appears.
    - Test 4: "PROJ-05: does not render workflow line when workflowPosition is null" -- render without workflowPosition. Assert no Layers icon or step count in output.
    - Test 5: "PROJ-02: renders flat recipe step count" -- pass workflowPosition with sectionName=null, stepIndex=5, totalSteps=20. Assert "Step 6/20" appears.
    - Test 6: "renders unit name and faction" -- basic render test, assert unit name and faction name appear.
    - Render helper: render(<CurrentFocusCard unit={unit} faction={faction} photo={undefined} onOpen={vi.fn()} onLog={vi.fn()} workflowPosition={pos} />)
  </action>
  <verify>
    <automated>pnpm test -- tests/dashboard/CurrentFocusCard.test.tsx tests/painting/KanbanCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CurrentFocusCardProps interface includes workflowPosition?: WorkflowPosition | null
    - DashboardPage.tsx calls useWorkflowPositions for the focus unit and passes result to CurrentFocusCard
    - CurrentFocusCard renders "SectionName: Technique -- step N/M" format per D-07
    - CurrentFocusCard renders "Recipe complete" when isComplete is true
    - CurrentFocusCard renders nothing extra when workflowPosition is null (existing behavior preserved)
    - Layers icon (lucide-react) prefixes the workflow guidance line at 12px
    - tests/dashboard/CurrentFocusCard.test.tsx exists with 6+ test cases
    - pnpm test -- tests/dashboard/CurrentFocusCard.test.tsx exits 0
    - pnpm test exits 0 (full suite green)
  </acceptance_criteria>
  <done>CurrentFocusCard shows section-aware workflow guidance. Dashboard fetches workflow for focus unit. Full test suite passes. Phase 60 all requirements delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook -> Components | Workflow data flows as typed props; components only render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03 | I (Info Disclosure) | CurrentFocusCard | accept | Displays recipe section/step names from local DB; no sensitive data |
| T-60-04 | T (Tampering) | DashboardPage | accept | Read-only display; no mutations in this plan |
</threat_model>

<verification>
pnpm test -- tests/painting/KanbanCard.test.tsx tests/dashboard/CurrentFocusCard.test.tsx
pnpm test
pnpm build
</verification>

<success_criteria>
- KanbanCard shows workflow context when available, falls back to status hints when not
- CurrentFocusCard shows section-aware guidance in D-07 format
- Both cards handle all 5 degradation scenarios (D-15 through D-19)
- All existing tests remain green
- Full test suite passes
- TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/60-kanban-currentfocus-integration/60-02-SUMMARY.md`
</output>

---
phase: 60-kanban-currentfocus-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/computeWorkflowPosition.ts
  - src/hooks/useWorkflowPositions.ts
  - src/db/queries/recipes.ts
  - tests/lib/computeWorkflowPosition.test.ts
autonomous: true
requirements:
  - PROJ-03
  - PROJ-04
  - PROJ-05

must_haves:
  truths:
    - "computeWorkflowPosition returns full section+step position for sectioned recipes"
    - "computeWorkflowPosition returns step-only position for flat recipes with no sections"
    - "computeWorkflowPosition returns section-only position when session has section_name but no valid step ID"
    - "computeWorkflowPosition returns null when session has neither step nor section info"
    - "computeWorkflowPosition detects recipe completion (last step of last section)"
    - "computeWorkflowPosition handles orphaned step IDs gracefully (falls back to section_name)"
    - "useWorkflowPositions returns Map<unitId, WorkflowPosition> using batch enrichment pattern"
  artifacts:
    - path: "src/lib/computeWorkflowPosition.ts"
      provides: "WorkflowPosition interface + computeWorkflowPosition pure function"
      exports: ["WorkflowPosition", "computeWorkflowPosition"]
    - path: "src/hooks/useWorkflowPositions.ts"
      provides: "Batch React Query hook returning Map<number, WorkflowPosition>"
      exports: ["WORKFLOW_POSITIONS_KEY", "useWorkflowPositions"]
    - path: "tests/lib/computeWorkflowPosition.test.ts"
      provides: "Pure function tests covering all edge cases"
      min_lines: 80
  key_links:
    - from: "src/hooks/useWorkflowPositions.ts"
      to: "src/lib/computeWorkflowPosition.ts"
      via: "import computeWorkflowPosition"
      pattern: "computeWorkflowPosition"
    - from: "src/hooks/useWorkflowPositions.ts"
      to: "src/db/queries/recipes.ts"
      via: "getRecipeNamesByUnitIds for recipe IDs"
      pattern: "getRecipeNamesByUnitIds"
---

<objective>
Create the workflow position derivation logic and batch data-fetching hook for Phase 60.

Purpose: PROJ-03/04/05 require a shared pure function that derives a user's current position in a multi-section recipe from their last logged painting session. This function is consumed by both KanbanCard and CurrentFocusCard (Plan 02), so it must be built and tested first.

Output: `computeWorkflowPosition.ts` pure function, `useWorkflowPositions.ts` batch hook, comprehensive unit tests, and an extended `getRecipeNamesByUnitIds` query that returns recipe IDs alongside names.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-kanban-currentfocus-integration/60-CONTEXT.md
@.planning/phases/60-kanban-currentfocus-integration/60-RESEARCH.md
@.planning/phases/60-kanban-currentfocus-integration/60-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/recipeSection.ts:
  interface RecipeSection { id, recipe_id, name, surface, optional, order_index, notes, section_type, technique, execution_mode, applies_to, created_at, updated_at }

From src/types/recipePaint.ts:
  interface RecipeStep { id, recipe_id, paint_id, step_name, order_index, notes, painting_phase, tool, technique, dilution, time_estimate_minutes, step_photo_path, alt_paint_id, section_id, created_at }

From src/types/paintingSession.ts:
  interface PaintingSession { id, unit_id, session_date, duration_minutes, notes, created_at, recipe_id, recipe_step_id, section_name }

From src/db/queries/recipes.ts:
  getRecipeNamesByUnitIds(unitIds: number[]): Promise<{ unit_id: number; name: string }[]>
  SQL: SELECT unit_id, name FROM painting_recipes WHERE unit_id IN (...)

From src/db/queries/paintingSessions.ts:
  getSessionsByUnit(unitId: number): Promise<PaintingSession[]>
  SQL: SELECT * FROM painting_sessions WHERE unit_id = $1 ORDER BY session_date DESC, id DESC

From src/db/queries/recipeSections.ts:
  getRecipeSections(recipeId: number): Promise<RecipeSection[]>
  SQL: SELECT * FROM recipe_sections WHERE recipe_id = $1 ORDER BY order_index ASC, id ASC

From src/db/queries/recipePaints.ts:
  getRecipePaintsByRecipe(recipeId: number): Promise<RecipeStep[]>
  SQL: SELECT * FROM recipe_steps WHERE recipe_id = $1 ORDER BY order_index ASC

From src/hooks/useKanbanEnrichment.ts:
  Batch pattern: sort IDs, useQuery with sorted key, Promise.all sub-queries, build Map, enabled: sortedIds.length > 0, staleTime: 5 * 60 * 1000
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create computeWorkflowPosition pure function with tests</name>
  <files>src/lib/computeWorkflowPosition.ts, tests/lib/computeWorkflowPosition.test.ts</files>
  <read_first>
    src/types/recipeSection.ts
    src/types/recipePaint.ts
    src/types/paintingSession.ts
    src/lib/computeDelta.ts
    tests/lib/dates.test.ts
  </read_first>
  <behavior>
    - Test 1 (D-05): returns null when both lastSessionStepId and lastSessionSectionName are null
    - Test 2 (D-05): returns null when steps array is empty
    - Test 3 (D-17): flat recipe (sections=[]) with step ID returns stepIndex, totalSteps, sectionName=null, sectionIndex=null, totalSections=0
    - Test 4 (D-17): flat recipe last step returns isComplete=true, nextStepName=null
    - Test 5 (D-18): sectioned recipe returns sectionName, sectionIndex, totalSections, stepName, stepIndex, totalSteps, technique
    - Test 6 (D-03): next step within same section returns nextStepName from sectionSteps[stepIndex+1]
    - Test 7 (D-03): last step of non-last section returns first step of next section as nextStepName
    - Test 8 (D-09): last step of last section returns isComplete=true, nextStepName=null
    - Test 9 (D-04): section_name only (no step ID) returns section-level position with stepName=null, stepIndex=null
    - Test 10 (Pitfall 5): orphaned step ID (not found in steps) falls back to section_name match
    - Test 11: orphaned step ID with no section_name returns null
    - Test 12: section_name that does not match any section returns null
  </behavior>
  <action>
    Create src/lib/computeWorkflowPosition.ts exporting the WorkflowPosition interface and computeWorkflowPosition function per D-01 through D-09.

    WorkflowPosition interface fields: sectionName (string|null), sectionIndex (number|null), totalSections (number), stepName (string|null), stepIndex (number|null), totalSteps (number), technique (string|null), isComplete (boolean), nextStepName (string|null).

    Function signature: computeWorkflowPosition(lastSessionStepId: number|null, lastSessionSectionName: string|null, sections: RecipeSection[], steps: RecipeStep[]): WorkflowPosition | null.

    Algorithm flow:
    1. Per D-05: if both inputs null or steps empty, return null
    2. Sort sections by order_index
    3. If lastSessionStepId is not null, find step in steps array
       - If not found (Pitfall 5), fall through to section_name path
       - If found and sections empty (D-17 flat recipe): compute flat position
       - If found and sections exist (D-18): find owning section via step.section_id, compute section-relative indices, apply D-03 next-step logic
    4. Per D-04: if lastSessionSectionName is not null and sections exist, find matching section by name, return section-level position (stepName=null, stepIndex=null)
    5. Return null if nothing matched

    Follow src/lib/computeDelta.ts conventions: JSDoc with Phase 60 reference, type-only imports from @/types/, early-return null pattern, no side effects.

    Create tests/lib/computeWorkflowPosition.test.ts following tests/lib/dates.test.ts conventions: vitest imports, @/ path alias, descriptive describe blocks. Build test fixtures inline (RecipeSection and RecipeStep partial objects with only fields needed by the function: id, name, order_index, section_id, step_name, technique for sections).
  </action>
  <verify>
    <automated>pnpm test -- tests/lib/computeWorkflowPosition.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/computeWorkflowPosition.ts exports WorkflowPosition interface and computeWorkflowPosition function
    - Function accepts (lastSessionStepId, lastSessionSectionName, sections, steps) and returns WorkflowPosition | null
    - tests/lib/computeWorkflowPosition.test.ts contains at least 12 test cases covering all behavior items above
    - All tests pass: pnpm test -- tests/lib/computeWorkflowPosition.test.ts exits 0
    - No React or DB imports in computeWorkflowPosition.ts (pure function, type-only imports)
  </acceptance_criteria>
  <done>Pure function computes workflow position for all cases: sectioned, flat, section-only, complete, orphaned step, null fallback. All 12+ tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Create useWorkflowPositions hook and extend recipe batch query</name>
  <files>src/hooks/useWorkflowPositions.ts, src/db/queries/recipes.ts</files>
  <read_first>
    src/hooks/useKanbanEnrichment.ts
    src/db/queries/recipes.ts
    src/db/queries/paintingSessions.ts
    src/db/queries/recipeSections.ts
    src/db/queries/recipePaints.ts
    src/lib/computeWorkflowPosition.ts
  </read_first>
  <action>
    Step 1: Extend getRecipeNamesByUnitIds in src/db/queries/recipes.ts to also return recipe id. Change SQL from "SELECT unit_id, name" to "SELECT id, unit_id, name". Update return type from { unit_id: number; name: string }[] to { id: number; unit_id: number; name: string }[]. This is additive and won't break existing consumers that destructure only unit_id and name.

    Step 2: Create src/hooks/useWorkflowPositions.ts following useKanbanEnrichment.ts exact pattern per D-10/D-11/D-12.

    Export WORKFLOW_POSITIONS_KEY factory: (unitIds: number[]) => ["workflow-positions", ...unitIds] as const.

    Export useWorkflowPositions(unitIds: number[]) hook:
    - Sort IDs for cache stability (Pitfall 2)
    - useQuery with WORKFLOW_POSITIONS_KEY(sortedIds)
    - enabled: sortedIds.length > 0
    - staleTime: 5 * 60 * 1000
    - queryFn:
      a. Call getRecipeNamesByUnitIds(sortedIds) to get recipe ID + unit_id mapping
      b. Build recipeMap: Map of unitId to { id, name }
      c. For each unit with a recipe, Promise.all fetch: getSessionsByUnit(unitId), getRecipeSections(recipeId), getRecipePaintsByRecipe(recipeId)
      d. Find latest session with recipe_step_id or section_name (first in DESC-sorted results, per Pitfall 3)
      e. Call computeWorkflowPosition with session data + sections + steps
      f. If position is non-null, set in result Map
    - Return Map<number, WorkflowPosition>

    Import paths: computeWorkflowPosition from @/lib/computeWorkflowPosition, getSessionsByUnit from @/db/queries/paintingSessions, getRecipeSections from @/db/queries/recipeSections, getRecipePaintsByRecipe from @/db/queries/recipePaints, getRecipeNamesByUnitIds from @/db/queries/recipes.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useWorkflowPositions.ts exports WORKFLOW_POSITIONS_KEY and useWorkflowPositions
    - useWorkflowPositions accepts unitIds: number[] and returns UseQueryResult with Map<number, WorkflowPosition>
    - getRecipeNamesByUnitIds return type includes id field alongside unit_id and name
    - pnpm build exits 0 with no TypeScript errors
    - Hook follows useKanbanEnrichment pattern: sorted IDs in query key, enabled guard, 5-minute staleTime, Promise.all for parallel sub-queries
  </acceptance_criteria>
  <done>Batch hook fetches workflow positions for all active units using existing queries. Recipe query extended to return id. TypeScript compilation passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite -> Frontend | Query results consumed as trusted (local DB, no untrusted input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | T (Tampering) | computeWorkflowPosition | accept | Pure function operates on in-memory data from local SQLite; no external input |
| T-60-02 | D (DoS) | useWorkflowPositions | accept | N+1 queries bounded by active unit count (typically 5-15); React Query caches 5 min |
</threat_model>

<verification>
pnpm test -- tests/lib/computeWorkflowPosition.test.ts
pnpm build
</verification>

<success_criteria>
- computeWorkflowPosition handles all 5 degradation scenarios (D-15 through D-19)
- Pure function has no React or DB dependencies (type-only imports)
- Hook follows established batch enrichment pattern exactly
- All tests pass, TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/60-kanban-currentfocus-integration/60-01-SUMMARY.md`
</output>

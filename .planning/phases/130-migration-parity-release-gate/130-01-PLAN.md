---
phase: 130-migration-parity-release-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/data-layer/db-helpers.ts
  - tests/data-layer/schema-shape.test.ts
autonomous: true
requirements: [REL-03]
nyquist_compliant: true

must_haves:
  truths:
    - "D-01: The data-layer migration list is derived from disk (readdirSync, numeric-prefix sort), not hand-maintained, so it can never drift from the .sql files on disk"
    - "D-06: The migration-parity test passes: lib.rs Migration{} count (47) equals HOBBYFORGE_MIGRATION_COUNT (47)"
    - "D-03: Migration 047 (army_list_unit_wargear) is applied AND its schema shape is asserted, not merely executed"
    - "D-02: PRAGMA foreign_keys is still ON after the full migration chain (migration 022 toggles it) — the post-chain guard is preserved verbatim"
  artifacts:
    - path: "tests/data-layer/db-helpers.ts"
      provides: "Disk-derived HOBBYFORGE_MIGRATIONS list + HOBBYFORGE_MIGRATION_COUNT"
      contains: "readdirSync("
    - path: "tests/data-layer/schema-shape.test.ts"
      provides: "army_list_unit_wargear (047) column-shape assertion"
      contains: "army_list_unit_wargear"
  key_links:
    - from: "tests/data-layer/db-helpers.ts"
      to: "src-tauri/migrations/*.sql"
      via: "readdirSync(migrationsDir) filtered to .sql, numeric-prefix sorted"
      pattern: "readdirSync\\(migrationsDir\\)"
    - from: "tests/data-layer/migration-parity.test.ts"
      to: "HOBBYFORGE_MIGRATION_COUNT"
      via: "lib.rs /Migration\\s*\\{/g count compared to helper count (no edit needed; goes green)"
      pattern: "Migration\\\\s\\*\\\\\\{"
---

<objective>
Make the data-layer migration list self-maintaining by deriving it from disk, which turns the currently-RED migration-parity test (`migration-parity.test.ts` D-06: 47 vs 46) green and removes the hand-maintained drift vector forever. Then genuinely exercise migration 047 by asserting the `army_list_unit_wargear` table's column shape.

Purpose: REL-03. The hardcoded `HOBBYFORGE_MIGRATIONS` array in `db-helpers.ts` stops at `046_*` (length 46) while disk and `lib.rs` both have 47 migrations — this is the root of the RED parity test and exactly the "schema-version representations silently disagree" class of bug this milestone closes.
Output: Edited `db-helpers.ts` (disk-derived list) and a new wargear assertion in `schema-shape.test.ts`. Zero net-new source files; zero migration `.sql` edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-migration-parity-release-gate/130-CONTEXT.md
@.planning/phases/130-migration-parity-release-gate/130-RESEARCH.md
@.planning/phases/130-migration-parity-release-gate/130-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase. Use directly — no exploration needed. -->

Current tests/data-layer/db-helpers.ts (the part being replaced — lines 8–65):
- `const migrationsDir = resolve(repoRoot, "src-tauri/migrations");` ALREADY EXISTS at line 9 — reuse it, do NOT recompute.
- `export const HOBBYFORGE_MIGRATIONS = [ "001_...", ..., "046_..." ] as const;` (lines 12–59) — the 46-element hardcoded tuple to REPLACE.
- `export const RULES_MIGRATIONS = [] as const;` (line 62) — Phase 107 comment; KEEP verbatim.
- `export const HOBBYFORGE_MIGRATION_COUNT = HOBBYFORGE_MIGRATIONS.length;` (line 64) — KEEP this shape; it becomes 47 automatically.
- Import line 4 is `import { readFileSync } from "node:fs";` — extend to also import `readdirSync`.
- `createHobbyforgeDb()` (lines 72–88) iterates `HOBBYFORGE_MIGRATIONS` as strings and re-checks the FK pragma — NO change needed; the FK-ON guard (lines 81–85) is preserved automatically (D-02).

ColumnInfo interface (schema-shape.test.ts:7–14) — REUSE, do not redefine:
  interface ColumnInfo { cid: number; name: string; type: string; notnull: number; dflt_value: string | null; pk: number; }

Shared db lifecycle in schema-shape.test.ts (lines ~17–25) — REUSE, no per-test createHobbyforgeDb()/close() needed:
  let db: Database.Database; beforeEach(() => { db = createHobbyforgeDb(); }); afterEach(() => { db.close(); });

Sibling assertion idiom to mirror (schema-shape.test.ts:58–73): table_info pragma → columns.find(c => c.name === colName) → labeled expect().toBeDefined(). Test title convention: it("<table> ... (NNN)") with migration number.

047 column list (from src-tauri/migrations/047_army_list_unit_wargear.sql, verified RESEARCH.md Finding 5):
  id, army_list_unit_id, weapon_name, quantity, created_at
  (047 FK target army_list_units is created in migration 031 — applies cleanly in-memory; no rules.db / cross-DB dependency.)

migration-parity.test.ts:23–31 (D-06 test) — NO edit. It imports HOBBYFORGE_MIGRATION_COUNT (a number) and counts lib.rs via /Migration\s*\{/g. After this plan it asserts 47 === 47 and goes GREEN.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive HOBBYFORGE_MIGRATIONS from disk in db-helpers.ts</name>
  <files>tests/data-layer/db-helpers.ts</files>
  <read_first>
    - tests/data-layer/db-helpers.ts (the file being modified — current hardcoded array lines 12–59, count line 64, import line 4, migrationsDir line 9, FK guard lines 81–85)
    - scripts/check-migrations.mjs (lines 9–14: existing readdirSync + .filter(.endsWith('.sql')) + numeric-prefix /^(\d+)_/ parse — the house-style idiom to copy)
    - .planning/phases/130-migration-parity-release-gate/130-RESEARCH.md (Finding 1: exact replacement code shape + numeric-vs-lexical-sort rationale + tuple→readonly string[] type-change caveat)
    - .planning/phases/130-migration-parity-release-gate/130-PATTERNS.md (db-helpers.ts section: which lines to touch and which to leave verbatim)
  </read_first>
  <action>
    Add `readdirSync` to the existing `import { readFileSync } from "node:fs";` line. Replace the hardcoded `HOBBYFORGE_MIGRATIONS` tuple (lines 12–59) with a disk-derived list: `readdirSync(migrationsDir)` (reuse the EXISTING `migrationsDir` const at line 9 — do not recompute), `.filter` to names ending in `.sql`, then `.sort` by the 3-digit NUMERIC prefix using `Number.parseInt(a.slice(0, 3), 10)` minus `Number.parseInt(b.slice(0, 3), 10)` (per D-01: numeric, NOT lexical/default sort — future-proofs against un-padded prefixes). Type the export as `readonly string[]` (it changes from an `as const` tuple — RESEARCH.md Finding 1 confirms the only importers, a number count and a string iterator, are unaffected). Keep the `RULES_MIGRATIONS = [] as const;` line and its Phase-107 comment verbatim. Keep `HOBBYFORGE_MIGRATION_COUNT = HOBBYFORGE_MIGRATIONS.length;` unchanged — it auto-becomes 47. Add a short comment that the list is disk-derived so it can never drift from lib.rs / the .sql files. Do NOT touch `createHobbyforgeDb()` or the post-chain `PRAGMA foreign_keys` ON guard (lines 81–85) — D-02 is satisfied by leaving that block untouched. Do NOT edit any `src-tauri/migrations/*.sql` file.
  </action>
  <verify>
    <automated>pnpm test -- tests/data-layer/migration-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- tests/data-layer/migration-parity.test.ts` exits 0 (the D-06 parity test 47 === 47 is GREEN; previously RED at 47 vs 46).
    - `tests/data-layer/db-helpers.ts` contains the literal `readdirSync(` and no longer contains the hardcoded string `"046_backfill_faction_udb_normalized.sql"` inside an array literal.
    - `tests/data-layer/db-helpers.ts` still contains the `PRAGMA foreign_keys not ON after migration chain` guard string (D-02 preserved, byte-for-byte unchanged).
    - The `Number.parseInt(` numeric-prefix sort is present (D-01: numeric sort, not bare `.sort()`).
    - No file under `src-tauri/migrations/` is modified (no migration bytes changed → `_sqlx_migrations` SHA-384 untouched).
  </acceptance_criteria>
  <done>The migration list is read from disk and numeric-sorted; `HOBBYFORGE_MIGRATION_COUNT` is 47; the D-06 parity test is green; the FK-ON guard and `createHobbyforgeDb()` body are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Assert army_list_unit_wargear (047) column shape in schema-shape.test.ts</name>
  <files>tests/data-layer/schema-shape.test.ts</files>
  <read_first>
    - tests/data-layer/schema-shape.test.ts (the file being modified — ColumnInfo interface lines 7–14, shared beforeEach/afterEach db, sibling assertion idiom lines 58–73, test-title convention)
    - src-tauri/migrations/047_army_list_unit_wargear.sql (source of truth for the column list — read only, do not edit)
    - .planning/phases/130-migration-parity-release-gate/130-RESEARCH.md (Finding 5: exact 047 column list + drop-in assertion; Finding 6: 047 applies hazard-free in the in-memory chain)
    - .planning/phases/130-migration-parity-release-gate/130-PATTERNS.md (schema-shape.test.ts section: reuse ColumnInfo + shared db, mirror the .find + labeled expect loop)
  </read_first>
  <action>
    Add one `it(...)` test in `schema-shape.test.ts` titled with the table and migration number (convention: `it("army_list_unit_wargear ... (047)")`). Use the suite's shared `db` from `beforeEach` — do NOT call `createHobbyforgeDb()`/`close()` inside the test. Query `db.pragma("table_info(army_list_unit_wargear)")` cast to the existing `ColumnInfo[]` interface (reuse it; do not redefine). Assert each expected column exists by mirroring the sibling idiom at lines 58–73: loop over the expected column names `["id", "army_list_unit_id", "weapon_name", "quantity", "created_at"]`, `columns.find((c) => c.name === colName)`, and a labeled `expect(col, \`column ${colName} should exist\`).toBeDefined()`. This is what makes 047 "genuinely covered, not just executed" (D-03). Place the assertion in `schema-shape.test.ts` (executor discretion per D-03 allowed `migration-parity.test.ts`, but `schema-shape.test.ts` has the shared db + ColumnInfo and is the semantic home — RESEARCH.md Open Q3).
  </action>
  <verify>
    <automated>pnpm test -- tests/data-layer/schema-shape.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- tests/data-layer/schema-shape.test.ts` exits 0.
    - `tests/data-layer/schema-shape.test.ts` contains `table_info(army_list_unit_wargear)` and asserts all five columns `id, army_list_unit_id, weapon_name, quantity, created_at`.
    - The new test reuses the existing `ColumnInfo` interface (no duplicate interface declaration added) and the shared `beforeEach` db (no in-test `createHobbyforgeDb()`).
    - `pnpm test` (full suite) exits 0 — the disk-derived chain now applies 047 across every data-layer suite without error.
  </acceptance_criteria>
  <done>A data-layer test asserts the `army_list_unit_wargear` schema shape against the live in-memory migration chain; the full Vitest suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk (`src-tauri/migrations/*.sql`) → test harness | Repo-internal, trusted local paths. No user/network input crosses here; `migrationsDir` is a fixed `resolve(...)` path, no traversal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01 | Tampering | Migration schema-version representations (disk vs lib.rs vs data-layer list) silently drifting | mitigate | Disk derivation removes the hand-maintained list as a drift vector; the D-06 parity test fails loudly on any lib.rs/disk disagreement. This plan is the source-of-truth leg the Plan 02 gate's transitive chain relies on. |
| T-130-PT | Tampering | Path traversal in the migrations glob | accept | `migrationsDir` is a fixed `resolve(repoRoot, "src-tauri/migrations")` — no user-supplied path component; not exploitable in a single-user desktop repo. |

Note (honest minimal surface): this is a single-user desktop app; the only inputs read are repo-internal trusted files. No auth/session/access-control/network surface. No npm/pip/cargo installs in this plan → no Package Legitimacy Gate. The plan *protects* the existing SHA-384 migration-checksum integrity by keeping the schema-version representations provably in agreement; it implements no cryptography and edits no applied `.sql` bytes.
</threat_model>

<verification>
- `pnpm test -- tests/data-layer/migration-parity.test.ts` exits 0 (D-06 green, 47 === 47).
- `pnpm test -- tests/data-layer/schema-shape.test.ts` exits 0 (047 wargear shape asserted).
- `pnpm test` (full data-layer + component suite) exits 0 — disk-derived chain applies 047 everywhere.
- `db-helpers.ts` contains `readdirSync(` and a numeric-prefix `Number.parseInt(` sort; no longer contains the `046_...` literal in an array; FK-ON guard intact.
- No `src-tauri/migrations/*.sql` file modified.
</verification>

<success_criteria>
- HOBBYFORGE_MIGRATIONS is disk-derived and numeric-sorted; HOBBYFORGE_MIGRATION_COUNT === 47 (REL-03, D-01).
- PRAGMA foreign_keys ON guard preserved verbatim (D-02).
- army_list_unit_wargear (047) column shape asserted in the data-layer suite (D-03).
- migration-parity.test.ts D-06 is green with no edit to that file.
- Full Vitest suite green.
</success_criteria>

<output>
Create `.planning/phases/130-migration-parity-release-gate/130-01-SUMMARY.md` when done.
</output>

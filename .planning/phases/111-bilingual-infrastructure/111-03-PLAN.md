---
phase: 111-bilingual-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["111-02"]
files_modified:
  - src/components/common/LocaleToggle.tsx
  - src/components/common/AppSidebar.tsx
  - src-tauri/src/lib.rs
autonomous: true
requirements: [FR-04, FR-05]

must_haves:
  truths:
    - "An EN/FR toggle is visible in the sidebar footer; clicking FR switches all canonical data labels to French"
    - "Switching locale invalidates all udb_* React Query keys so data refetches with the new locale"
    - "Searching in FTS5 with a French unit name returns the correct unit"
    - "The locale toggle persists across app restarts via localStorage"
    - "In collapsed sidebar, the toggle shows current locale code with tooltip"
  artifacts:
    - path: "src/components/common/LocaleToggle.tsx"
      provides: "EN/FR pill toggle component"
      exports: ["LocaleToggle"]
    - path: "src/components/common/AppSidebar.tsx"
      provides: "Sidebar with LocaleToggle integrated"
      contains: "LocaleToggle"
    - path: "src-tauri/src/lib.rs"
      provides: "FTS5 rebuild with French names in keywords column"
      contains: "name_fr"
  key_links:
    - from: "src/components/common/LocaleToggle.tsx"
      to: "src/stores/localeStore.ts"
      via: "useLocaleStore import"
      pattern: "useLocaleStore"
    - from: "src/components/common/LocaleToggle.tsx"
      to: "@tanstack/react-query"
      via: "useQueryClient().invalidateQueries"
      pattern: "invalidateQueries"
    - from: "src/components/common/AppSidebar.tsx"
      to: "src/components/common/LocaleToggle.tsx"
      via: "component import and render"
      pattern: "LocaleToggle"
---

<objective>
Locale toggle UI + FTS5 bilingual search (FR-04 UI, FR-05)

Purpose: Complete the user-facing locale experience by adding the EN/FR toggle to the sidebar and extending FTS5 to index French names for bilingual search.

Output: LocaleToggle.tsx component, AppSidebar integration, FTS5 rebuild SQL extension in lib.rs
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-bilingual-infrastructure/111-CONTEXT.md
@.planning/phases/111-bilingual-infrastructure/111-RESEARCH.md
@.planning/phases/111-bilingual-infrastructure/111-PATTERNS.md
@.planning/phases/111-bilingual-infrastructure/111-UI-SPEC.md
@.planning/phases/111-bilingual-infrastructure/111-02-SUMMARY.md

<interfaces>
<!-- From src/stores/localeStore.ts (created in Plan 02) -->
export type Locale = 'en' | 'fr';
export const useLocaleStore: { locale: Locale; setLocale: (l: Locale) => void }

<!-- From src/components/common/AppSidebar.tsx — insertion point (lines 220-238) -->
{/* Collapse toggle (above Settings, per UI-SPEC paragraph 4) */}
&lt;div className="px-2 pb-1"&gt;
  &lt;Button variant="ghost" size="icon" ... /&gt;
&lt;/div&gt;
{/* Settings (pinned bottom) */}
&lt;div className="border-t border-border px-2 py-2"&gt;...&lt;/div&gt;

<!-- From src-tauri/src/lib.rs — FTS5 rebuild (lines 730-741) -->
INSERT INTO udb_search(unit_id, name, faction_name, keywords)
SELECT u.id, u.name, f.name,
       COALESCE(u.sub_faction || ' ', '') || COALESCE(GROUP_CONCAT(k.keyword, ' '), '')
FROM udb_units u
JOIN udb_factions f ON f.id = u.faction_id
LEFT JOIN udb_unit_keywords k ON k.unit_id = u.id
GROUP BY u.id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LocaleToggle component and integrate into AppSidebar</name>
  <files>src/components/common/LocaleToggle.tsx, src/components/common/AppSidebar.tsx</files>
  <read_first>
    - src/components/common/AppSidebar.tsx (full file — understand DOM structure, collapse state, imports)
    - src/stores/localeStore.ts (verify export names from Plan 02)
    - .planning/phases/111-bilingual-infrastructure/111-UI-SPEC.md (interaction contract, visual specs)
    - .planning/phases/111-bilingual-infrastructure/111-PATTERNS.md (Pattern: LocaleToggle component)
    - .planning/phases/111-bilingual-infrastructure/111-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    Create src/components/common/LocaleToggle.tsx:
    - Props: { collapsed: boolean }
    - Import useLocaleStore and Locale from "@/stores/localeStore"
    - Import useQueryClient from "@tanstack/react-query"
    - Import Button from "@/components/ui/button"
    - Import Tooltip, TooltipContent, TooltipTrigger from "@/components/ui/tooltip"

    handleLocaleSwitch(next: Locale) function per D-06:
    - Calls setLocale(next)
    - Calls queryClient.invalidateQueries with prefix keys: ["udb-factions"], ["udb-units"], ["udb-unit-detail"]. Use prefix matching (no locale in the invalidation key) to clear ALL locale variants at once.

    Expanded sidebar layout: pill toggle with two Button segments inside a flex container with border. Active segment: variant="secondary". Inactive segment: variant="ghost". Both: size="sm", className="flex-1 rounded-none h-7 text-xs". Labels: "EN" and "FR" uppercase.

    Collapsed sidebar layout: single ghost icon-size Button showing current locale code (locale.toUpperCase()), with Tooltip showing "Switch to French" or "Switch to English". On click: toggle to opposite locale.

    Wrap in div className="px-2 pb-1" for consistent sidebar spacing.

    Integrate into AppSidebar.tsx:
    - Import LocaleToggle from "./LocaleToggle"
    - Render LocaleToggle collapsed={collapsed} as a sibling div ABOVE the collapse toggle div (insert before the "Collapse toggle" comment at line ~221). The LocaleToggle component renders its own wrapper div.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | findstr /C:"error" /C:"Successfully"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/common/LocaleToggle.tsx exists and exports LocaleToggle function component
    - LocaleToggle accepts { collapsed: boolean } prop
    - handleLocaleSwitch calls setLocale and invalidateQueries with 3 prefix keys
    - Expanded state renders two Button elements with text "EN" and "FR"
    - Collapsed state renders one Button with Tooltip
    - AppSidebar.tsx imports and renders LocaleToggle above the collapse toggle div
    - collapsed prop is passed from AppSidebar's collapsed state to LocaleToggle
    - pnpm build passes with zero TypeScript errors
  </acceptance_criteria>
  <done>LocaleToggle renders EN/FR pill in sidebar footer; clicking switches locale and invalidates UDB query cache</done>
</task>

<task type="auto">
  <name>Task 2: Extend FTS5 rebuild in lib.rs to index French names</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/lib.rs (lines 720-745 — FTS5 rebuild block in import_unit_database_inner)
    - src-tauri/migrations/038_udb_schema.sql (FTS5 table DDL to confirm column names)
    - .planning/phases/111-bilingual-infrastructure/111-RESEARCH.md (Pattern 5 FTS5, Pitfall 2)
    - .planning/phases/111-bilingual-infrastructure/111-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    Modify the FTS5 INSERT INTO udb_search SQL in the import_unit_database_inner function (around line 730-737).

    Per D-08 and RESEARCH Pitfall 2 recommendation: put French names in the keywords column (not the name column) to keep search result display clean. The name column stays as English-only u.name. The faction_name column stays as English-only f.name.

    Change the keywords column expression from:
      COALESCE(u.sub_faction || ' ', '') || COALESCE(GROUP_CONCAT(k.keyword, ' '), '')
    To:
      COALESCE(u.name_fr || ' ', '') || COALESCE(f.name_fr || ' ', '') || COALESCE(u.sub_faction || ' ', '') || COALESCE(GROUP_CONCAT(k.keyword, ' '), '')

    This prepends French unit name and French faction name to the keywords bag-of-words. FTS5 matches across all columns, so French names in keywords are fully searchable. The name and faction_name columns remain English for clean result display.

    No other Rust code changes needed. The _fr columns are already imported by the serde(default) bindings added in Phase 108.
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | findstr /C:"error" /C:"Finished"</automated>
  </verify>
  <acceptance_criteria>
    - lib.rs FTS5 INSERT SQL keywords column expression starts with COALESCE(u.name_fr || ' ', '') || COALESCE(f.name_fr || ' ', '')
    - The name column in the INSERT still uses u.name (not concatenated with name_fr)
    - The faction_name column in the INSERT still uses f.name (not concatenated with name_fr)
    - cargo check passes for the Tauri crate
    - The INSERT statement is syntactically valid SQL (balanced parentheses, correct concatenation operators)
  </acceptance_criteria>
  <done>FTS5 rebuild indexes French unit and faction names in keywords column for bilingual search</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand store -> React Query invalidation | locale switch triggers cache invalidation; locale is typed union |
| Rust SQL string -> SQLite FTS5 | Hardcoded SQL template; no user input interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-03 | Tampering | FTS5 keywords content | accept | Content comes from trusted udb_* tables populated by dev-side import; no user input path |
| T-111-04 | Denial of Service | invalidateQueries on locale switch | accept | Invalidates ~3 query prefixes; staleTime Infinity means no thundering herd; desktop single-user app |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- pnpm build passes (TypeScript)
- cargo check passes (Rust)
- LocaleToggle renders in sidebar between collapse button and settings
- Clicking FR triggers invalidateQueries for udb-factions, udb-units, udb-unit-detail
- FTS5 INSERT SQL includes name_fr and f.name_fr in keywords column
</verification>

<success_criteria>
FR-04 complete: EN/FR locale toggle visible in sidebar, persisted to localStorage, switches all canonical data display.
FR-05 complete: FTS5 search index includes French names for bilingual search.
</success_criteria>

<output>
Create `.planning/phases/111-bilingual-infrastructure/111-03-SUMMARY.md` when done
</output>

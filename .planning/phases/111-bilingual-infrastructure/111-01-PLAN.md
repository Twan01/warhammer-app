---
phase: 111-bilingual-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/data/translations_fr.json
  - scripts/build-unit-db.ts
  - scripts/lib/types.ts
autonomous: true
requirements: [FR-02]

must_haves:
  truths:
    - "Running the build script with a populated translations_fr.json writes French names into the _fr columns of unit_database.json"
    - "Running the build script without translations_fr.json produces a warning but completes successfully with _fr fields as null"
    - "The overlay is applied as the last mutation step before writing output JSON"
  artifacts:
    - path: "scripts/data/translations_fr.json"
      provides: "French translation overlay stub with sample entries"
      contains: "factions"
    - path: "scripts/build-unit-db.ts"
      provides: "Step 10.5 overlay loading and application"
      contains: "loadTranslationsFr"
  key_links:
    - from: "scripts/build-unit-db.ts"
      to: "scripts/data/translations_fr.json"
      via: "loadTranslationsFr function"
      pattern: "TRANSLATIONS_FR_PATH"
---

<objective>
Build script French overlay loading (FR-02)

Purpose: Enable the build pipeline to populate _fr columns from a manually curated translations_fr.json file, so that the bundled unit_database.json carries French names through to the Rust import and into SQLite.

Output: translations_fr.json stub file + loadTranslationsFr() function + overlay application step in build-unit-db.ts
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-bilingual-infrastructure/111-CONTEXT.md
@.planning/phases/111-bilingual-infrastructure/111-RESEARCH.md
@.planning/phases/111-bilingual-infrastructure/111-PATTERNS.md

<interfaces>
<!-- From scripts/lib/types.ts — _fr fields already present on all entity types -->
UdbFactionRow.name_fr: string | null  (line 38)
UdbUnitRow.name_fr: string | null  (line 50)
UdbUnitWeaponRow.name_fr: string | null  (line 79)
UdbUnitAbilityRow.name_fr: string | null, .description_fr: string | null  (lines 88-89)
UdbUnitKeywordRow.keyword_fr: string | null  (line 96)

<!-- From scripts/lib/normalize.ts — loadAliases graceful degrade pattern -->
export function loadAliases(filePath: string): Record&lt;string, string&gt;

<!-- From scripts/build-unit-db.ts — path constants -->
const DATA_DIR = join(REPO_ROOT, "scripts", "data");
const ALIASES_PATH = join(DATA_DIR, "aliases.json");
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create translations_fr.json stub and TranslationsFrOverlay type</name>
  <files>scripts/data/translations_fr.json, scripts/lib/types.ts</files>
  <read_first>
    - scripts/data/aliases.json (analog for JSON overlay structure)
    - scripts/lib/types.ts (existing types, _fr field definitions)
    - .planning/phases/111-bilingual-infrastructure/111-CONTEXT.md (D-01 overlay format)
  </read_first>
  <action>
    Create scripts/data/translations_fr.json with the D-01 structure: top-level keys "factions", "units", "abilities", "weapons", "keywords". Include 2-3 sample entries per section for verification:
    - factions: "SM" -> "Space Marines" (same in FR), "NEC" -> "Necrons" (same)
    - units: pick 2 real unit IDs from existing unit_database.json and provide French names
    - abilities: one entry keyed as "${unit_id}:${abilityName}" with name_fr and description_fr
    - weapons: one entry keyed as "${unit_id}:${weaponName}" with French name
    - keywords: "INFANTRY" -> "INFANTERIE", "CHARACTER" -> "PERSONNAGE"

    In scripts/lib/types.ts, add the TranslationsFrOverlay interface after existing type exports:
    - factions?: Record&lt;string, string&gt;
    - units?: Record&lt;string, string&gt;
    - abilities?: Record&lt;string, { name_fr?: string | null; description_fr?: string | null }&gt;
    - weapons?: Record&lt;string, string&gt;
    - keywords?: Record&lt;string, string&gt;

    Ensure .gitignore allows scripts/data/translations_fr.json (check that scripts/data/ has an exception like aliases.json does).
  </action>
  <verify>
    <automated>node -e "const t = JSON.parse(require('fs').readFileSync('scripts/data/translations_fr.json','utf-8')); console.log(Object.keys(t)); if(!t.factions||!t.units||!t.abilities||!t.weapons||!t.keywords) process.exit(1); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/data/translations_fr.json exists and parses as valid JSON
    - JSON has exactly 5 top-level keys: factions, units, abilities, weapons, keywords
    - factions values are strings, abilities values are objects with name_fr/description_fr
    - scripts/lib/types.ts exports TranslationsFrOverlay interface
    - pnpm build succeeds (TypeScript compilation passes)
  </acceptance_criteria>
  <done>translations_fr.json stub created with sample entries; TranslationsFrOverlay type exported from scripts/lib/types.ts</done>
</task>

<task type="auto">
  <name>Task 2: Add loadTranslationsFr and overlay application to build script</name>
  <files>scripts/build-unit-db.ts</files>
  <read_first>
    - scripts/build-unit-db.ts (full file — understand current step ordering)
    - scripts/lib/normalize.ts (loadAliases pattern for graceful degrade)
    - scripts/lib/types.ts (TranslationsFrOverlay type from Task 1)
    - .planning/phases/111-bilingual-infrastructure/111-PATTERNS.md (Pattern 1: Overlay Loading)
  </read_first>
  <action>
    Add TRANSLATIONS_FR_PATH constant after ALIASES_PATH (line ~59): join(DATA_DIR, "translations_fr.json").

    Add loadTranslationsFr() function near the helpers section (~line 75). Follow loadAliases pattern: existsSync check, try/catch JSON.parse, return TranslationsFrOverlay | null, console.warn on missing/malformed file.

    Import TranslationsFrOverlay from "./lib/types.ts" in the existing type import block (line ~33).

    In the main() function, add "Step 10.5 — Apply French overlay" AFTER the coverage report section and sub-faction stats (~line 594), but BEFORE the "Build Summary" console.log block (~line 598) and BEFORE the output JSON assembly (~line 619). This ensures all entity arrays (factions, units, abilities, weapons, keywords) are fully finalized before overlay application — per RESEARCH Pitfall 5.

    The overlay application loop iterates each entity array:
    - factions: if overlay.factions[f.id] exists, set f.name_fr = overlay.factions[f.id]
    - units: if overlay.units[u.id] exists, set u.name_fr = overlay.units[u.id]
    - abilities: key is "${a.unit_id}:${a.name}", if match found set a.name_fr and a.description_fr
    - weapons: key is "${w.unit_id}:${w.name}", if match found set w.name_fr
    - keywords: if overlay.keywords[k.keyword] exists, set k.keyword_fr

    Log a summary line: "French overlay: X factions, Y units, Z abilities, W weapons, V keywords translated" showing actual applied counts.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/build-unit-db.ts 2>&1 | findstr /C:"French overlay" /C:"Build complete"</automated>
  </verify>
  <acceptance_criteria>
    - Build script runs without errors: "Build complete!" appears in output
    - "French overlay:" summary line appears showing counts of applied translations
    - Output unit_database.json contains _fr fields that are non-null for entries present in translations_fr.json
    - When translations_fr.json is renamed/missing, build still completes with WARNING log and all _fr fields as null
    - pnpm build succeeds (TypeScript compilation passes)
  </acceptance_criteria>
  <done>Build script loads translations_fr.json overlay and populates _fr fields; graceful degrade when file is missing; summary log shows applied count</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev filesystem -> build script | translations_fr.json is a developer-curated file, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Tampering | translations_fr.json parse | accept | Dev-side file only; try/catch + graceful null degrade already specified |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- Build script runs to completion with translations_fr.json present
- Build script runs to completion with translations_fr.json absent (graceful degrade)
- Output unit_database.json has non-null _fr fields for entries in the overlay
- pnpm build passes TypeScript compilation
</verification>

<success_criteria>
FR-02 is satisfied: build script loads French translations from scripts/data/translations_fr.json overlay and populates _fr columns in the output JSON.
</success_criteria>

<output>
Create `.planning/phases/111-bilingual-infrastructure/111-01-SUMMARY.md` when done
</output>

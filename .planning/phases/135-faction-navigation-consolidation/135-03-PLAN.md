---
phase: 135-faction-navigation-consolidation
plan: 03
type: execute
wave: 3
depends_on: ["135-02"]
files_modified:
  - src/components/common/AppSidebar.tsx
autonomous: true
requirements: [HON-07]
must_haves:
  truths:
    - "Data Health no longer appears in the main sidebar MANAGEMENT_NAV"
    - "The /data-health route still resolves (kept) and the Settings -> Data 'Open Data Health' card still links to it"
    - "MANAGEMENT_NAV contains only Spending and Wishlist after this plan"
    - "pnpm build is clean (HeartPulse import removed, Shield retained)"
    - "Context decisions covered — D-09: minimal demotion — remove the Data Health sidebar entry only, keep the /data-health route and the Settings -> Data 'Open Data Health' card"
  artifacts:
    - path: "src/components/common/AppSidebar.tsx"
      provides: "MANAGEMENT_NAV with Data Health removed and HeartPulse import dropped"
  key_links:
    - from: "src/features/settings/DataManagementTab.tsx"
      to: "/data-health"
      via: "existing Open Data Health card (kept; satisfies HON-07 entry point)"
      pattern: "data-health"
---

<objective>
Demote Data Health out of the main sidebar; reach it from Settings -> Data.

Purpose: HON-07. The Data Health diagnostics page does not belong as a top-level sidebar destination. Per D-09 this is a minimal demotion: remove the `Data Health` entry from `MANAGEMENT_NAV` in `AppSidebar.tsx` and drop the now-unused `HeartPulse` import. The `/data-health` route stays, and the existing Settings -> Data "Open Data Health" card already provides the new entry point.

This plan is sequenced AFTER Plan 02 (Wave 3) because both edit `src/components/common/AppSidebar.tsx`; sequencing prevents a same-file conflict between parallel plans.

Output: edited `AppSidebar.tsx`. `pnpm build` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-faction-navigation-consolidation/135-CONTEXT.md
@.planning/phases/135-faction-navigation-consolidation/135-UI-SPEC.md

<interfaces>
<!-- After Plan 02, MANAGEMENT_NAV has 3 entries: Spending, Wishlist, Data Health -->
<!-- This plan removes the Data Health entry, leaving Spending + Wishlist -->
const MANAGEMENT_NAV = [
  { to: "/spending", label: "Spending", icon: Wallet },
  { to: "/wishlist", label: "Wishlist", icon: Heart },
] as const;

<!-- Import cleanup: remove HeartPulse (only used by the removed Data Health entry). -->
<!-- KEEP Shield (Quick Add Add Faction + FactionsEmptyState). KEEP Heart, Wallet (still used). -->
<!-- D-09: KEEP /data-health route and the Settings -> Data 'Open Data Health' card (DataManagementTab.tsx ~L162-183). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Data Health from the sidebar and drop the HeartPulse import</name>
  <files>src/components/common/AppSidebar.tsx</files>
  <read_first>
    - src/components/common/AppSidebar.tsx (the file being modified — MANAGEMENT_NAV array and the lucide-react import block L1-23)
    - src/features/settings/DataManagementTab.tsx (the kept "Open Data Health" card ~L162-183 — the entry point that satisfies HON-07)
    - .planning/phases/135-faction-navigation-consolidation/135-UI-SPEC.md (Sidebar Contract: target MANAGEMENT_NAV, import cleanup rules)
    - .planning/phases/135-faction-navigation-consolidation/135-RESEARCH.md (Pitfall 5: keep Shield; HeartPulse safe to remove)
  </read_first>
  <action>Edit `src/components/common/AppSidebar.tsx`. Remove the `{ to: "/data-health", label: "Data Health", icon: HeartPulse }` entry from `MANAGEMENT_NAV` (after Plan 02 this leaves exactly `Spending` and `Wishlist`). Remove `HeartPulse` from the lucide-react import block (it is only used by the deleted entry; `noUnusedLocals` would otherwise fail the build). Do NOT remove `Shield` (used by Quick Add "Add Faction" + FactionsEmptyState), `Wallet`, or `Heart`. Do NOT touch the `/data-health` route in router.tsx or the Settings -> Data "Open Data Health" card — both are kept per D-09. Keep the "Management" group label as-is with its 2 remaining items.</action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Data Health" src/components/common/AppSidebar.tsx == 0
    - grep -c "HeartPulse" src/components/common/AppSidebar.tsx == 0
    - src/components/common/AppSidebar.tsx still imports/uses Shield (grep -c "Shield" >= 1)
    - src/app/router.tsx still contains "dataHealthRoute" (route kept)
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Data Health is gone from the sidebar, HeartPulse import is dropped, Shield is retained, the /data-health route and Settings card are untouched, and the build is clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Data Health is reachable in <= 2 clicks via Settings</name>
  <what-built>Data Health removed from the sidebar Management group; /data-health route and the Settings -> Data "Open Data Health" card kept.</what-built>
  <how-to-verify>
    1. Run `pnpm tauri dev`.
    2. Confirm the sidebar Management group no longer shows "Data Health" (only Spending + Wishlist remain).
    3. Open Settings -> Data tab -> click "Open Data Health"; confirm it navigates to the full Data Health page (<= 2 clicks).
    4. Optional: navigate to `/data-health` directly (URL/deep link) and confirm the page still resolves.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI nav layer | Sidebar entry removal is internal navigation only; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-04 | Information Disclosure / capability loss | Data Health sidebar removal | mitigate | /data-health route kept; Settings -> Data "Open Data Health" card retained as the entry point (D-09). Verified by the blocking human-verify checkpoint and a router grep for dataHealthRoute. |
| T-135-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Legitimacy gate not applicable. |
</threat_model>

<verification>
- `pnpm build` clean (HeartPulse removed, Shield retained)
- Data Health absent from sidebar MANAGEMENT_NAV
- /data-health route resolves; Settings -> Data "Open Data Health" navigates there
</verification>

<success_criteria>
- Data Health is removed from the main sidebar.
- It remains reachable in <= 2 clicks via Settings -> Data -> "Open Data Health".
- Build is clean.
</success_criteria>

<output>
Create `.planning/phases/135-faction-navigation-consolidation/135-03-SUMMARY.md` when done
</output>

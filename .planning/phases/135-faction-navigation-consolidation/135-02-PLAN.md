---
phase: 135-faction-navigation-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["135-01"]
files_modified:
  - src/app/settings/page.tsx
  - src/app/router.tsx
  - src/components/common/AppSidebar.tsx
  - src/app/factions/page.tsx
autonomous: false
requirements: [HON-06]
must_haves:
  truths:
    - "Faction list/edit/delete/theme management is reachable from Settings -> Factions tab (2nd tab)"
    - "The standalone /factions route and its sidebar entry no longer exist"
    - "Quick Add -> Add Faction still opens the FactionSheet (create flow preserved)"
    - "pnpm build is clean (no dangling FactionsPage import, no unused Shield)"
  artifacts:
    - path: "src/app/settings/page.tsx"
      provides: "Factions tab (TabsTrigger value=factions + TabsContent rendering <FactionsPage/>)"
      contains: "value=\"factions\""
    - path: "src/app/router.tsx"
      provides: "router with factionsRoute removed, dataHealthRoute kept"
    - path: "src/components/common/AppSidebar.tsx"
      provides: "MANAGEMENT_NAV with the Factions entry removed; Quick Add Add Faction intact"
  key_links:
    - from: "src/app/settings/page.tsx"
      to: "src/features/factions/FactionsPage.tsx"
      via: "import + <FactionsPage/> in TabsContent"
      pattern: "FactionsPage"
    - from: "src/components/common/AppSidebar.tsx"
      to: "QuickAdd Add Faction"
      via: "Shield icon retained in DropdownMenuItem"
      pattern: "Shield"
---

<objective>
Retire the standalone `/factions` page and rehome faction management into Settings, with zero loss of capability.

Purpose: HON-06. Faction CRUD + theming currently lives on a standalone `/factions` sidebar page. This plan adds a "Factions" tab (2nd position) to Settings rendering the existing `FactionsPage` verbatim, removes the `/factions` route + sidebar entry, and confirms the Quick Add create path is untouched. Depends on Plan 01 so the consolidation migration lands and is verified before the UI that surfaces factions changes.

Output: edited `settings/page.tsx`, `router.tsx`, `AppSidebar.tsx`, and a deleted `src/app/factions/page.tsx`. `pnpm build` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-faction-navigation-consolidation/135-CONTEXT.md
@.planning/phases/135-faction-navigation-consolidation/135-RESEARCH.md
@.planning/phases/135-faction-navigation-consolidation/135-PATTERNS.md
@.planning/phases/135-faction-navigation-consolidation/135-UI-SPEC.md

<interfaces>
<!-- settings/page.tsx current 3-tab structure (VERIFIED) — insert factions as 2nd tab -->
<Tabs defaultValue="preferences"> with TabsTriggers: preferences | data | about
Each TabsContent uses className="mt-4".

<!-- FactionsPage (rehomed verbatim, D-08) -->
import { FactionsPage } from "@/features/factions/FactionsPage";
FactionsPage renders its own <PageHeader title="Factions" subtitle="Manage your army factions"> + flex flex-col gap-6 p-6 — kept as-is (UI-SPEC: acts as section heading, not a double-header).

<!-- router.tsx edit points (VERIFIED RESEARCH.md) -->
Remove: line ~26 lazy import FactionsPage; lines ~101-105 factionsRoute createRoute; the `factionsRoute,` entry in the route tree.
KEEP: DataHealthPage lazy import, dataHealthRoute definition, dataHealthRoute in route tree.

<!-- AppSidebar.tsx MANAGEMENT_NAV (VERIFIED) -->
Remove the { to: "/factions", label: "Factions", icon: Shield } entry only (this plan).
KEEP Shield import — used by Quick Add Add Faction DropdownMenuItem (~line 118) and FactionsEmptyState.
Do NOT remove the Data Health entry here — Plan 03 owns that edit to avoid a same-wave file conflict.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Factions tab to Settings</name>
  <files>src/app/settings/page.tsx</files>
  <read_first>
    - src/app/settings/page.tsx (the file being modified — existing 3-tab Tabs block)
    - src/features/factions/FactionsPage.tsx (the component being rendered verbatim, D-08)
    - .planning/phases/135-faction-navigation-consolidation/135-UI-SPEC.md (Tab Contract: 2nd position, mt-4, verbatim FactionsPage)
    - .planning/phases/135-faction-navigation-consolidation/135-PATTERNS.md (settings/page.tsx extend-existing target shape, lines 337-392)
  </read_first>
  <action>Edit `src/app/settings/page.tsx`. Add `import { FactionsPage } from "@/features/factions/FactionsPage";` to the import block. Insert a `<TabsTrigger value="factions">Factions</TabsTrigger>` as the SECOND trigger (between Preferences and Data) so the order reads Preferences | Factions | Data | About. Add a matching `<TabsContent value="factions" className="mt-4"><FactionsPage /></TabsContent>` after the preferences TabsContent. Keep `defaultValue="preferences"` unchanged. Do NOT wrap FactionsPage in an extra div or the useAppSettings loading guard — FactionsPage manages its own loading/error/empty via useFactions (per UI-SPEC).</action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/settings/page.tsx contains `value="factions"` and `<FactionsPage />`
    - The Factions trigger is the 2nd TabsTrigger (Preferences | Factions | Data | About): grep order check passes
    - pnpm build exits 0 (no dangling import)
  </acceptance_criteria>
  <done>Settings has a Factions tab in 2nd position rendering FactionsPage verbatim; build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the /factions route, its sidebar entry, and delete the page wrapper</name>
  <files>src/app/router.tsx, src/components/common/AppSidebar.tsx, src/app/factions/page.tsx</files>
  <read_first>
    - src/app/router.tsx (the file being modified — FactionsPage lazy import ~L26, factionsRoute ~L101-105, route-tree entry; KEEP dataHealthRoute)
    - src/components/common/AppSidebar.tsx (MANAGEMENT_NAV ~L57-62; Quick Add Add Faction ~L117-120; Shield/HeartPulse imports L9-18)
    - src/app/factions/page.tsx (thin wrapper to delete)
    - .planning/phases/135-faction-navigation-consolidation/135-PATTERNS.md (router.tsx + AppSidebar exact delete points, lines 259-333)
    - .planning/phases/135-faction-navigation-consolidation/135-RESEARCH.md (Pitfall 5: keep Shield, do not break the build)
  </read_first>
  <action>In `src/app/router.tsx`: remove the `FactionsPage` lazy import (~line 26), the `factionsRoute` `createRoute({ ... path: "/factions" ... })` definition (~lines 101-105), and the `factionsRoute,` entry in the route tree. KEEP `DataHealthPage`, `dataHealthRoute`, and the `dataHealthRoute,` route-tree entry untouched. In `src/components/common/AppSidebar.tsx`: remove ONLY the `{ to: "/factions", label: "Factions", icon: Shield }` entry from `MANAGEMENT_NAV` (leave the Data Health entry for Plan 03 to avoid a file conflict; leave Spending/Wishlist). KEEP the `Shield` import — it is still used by the Quick Add "Add Faction" DropdownMenuItem and FactionsEmptyState. Do NOT remove `HeartPulse` here (Plan 03 removes it with the Data Health entry). Delete the file `src/app/factions/page.tsx` (the thin route wrapper); the `src/features/factions/` components are kept and reused by the Settings tab.</action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/router.tsx no longer contains "factionsRoute" (grep -c "factionsRoute" == 0)
    - src/app/router.tsx still contains "dataHealthRoute" (grep -c "dataHealthRoute" >= 1)
    - src/components/common/AppSidebar.tsx MANAGEMENT_NAV no longer contains the "/factions" entry (grep -c '"/factions"' == 0)
    - src/components/common/AppSidebar.tsx still imports and uses Shield (grep -c "Shield" >= 1)
    - src/app/factions/page.tsx no longer exists
    - pnpm build exits 0
  </acceptance_criteria>
  <done>The /factions route and sidebar entry are gone, the page wrapper is deleted, dataHealthRoute is intact, Shield is retained, and the build is clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify faction capability fully preserved in its new home</name>
  <what-built>Factions tab added to Settings (2nd position) rendering FactionsPage verbatim; /factions route + sidebar entry removed; Quick Add "Add Faction" untouched.</what-built>
  <how-to-verify>
    1. Run `pnpm tauri dev`.
    2. Confirm the sidebar Management group no longer shows "Factions" (only Spending + Wishlist; Data Health still present until Plan 03).
    3. Open Settings -> click the "Factions" tab (2nd tab). Confirm the faction list renders with its "Factions" / "Manage your army factions" header.
    4. Edit a faction's color; confirm it saves and the `--faction-accent` theming updates.
    5. Create a new faction from the tab's "Add Faction" button, then delete an empty faction — confirm both flows work.
    6. Click the sidebar Quick Add (+) -> "Add Faction"; confirm the FactionSheet opens (create path still reachable independent of the retired route).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any lost capability / visual issue</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI route layer | Removing a route is internal navigation only; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-03 | Information Disclosure / capability loss | route + sidebar removal | mitigate | Faction CRUD rehomed to Settings -> Factions verbatim; Quick Add create path retained. Verified by the blocking human-verify checkpoint and pnpm build. |
| T-135-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Legitimacy gate not applicable. |
</threat_model>

<verification>
- `pnpm build` clean (no dangling FactionsPage import, no unused Shield)
- Settings -> Factions tab renders FactionsPage; CRUD + theming work
- Quick Add "Add Faction" opens the sheet
- /factions route removed; dataHealthRoute kept
</verification>

<success_criteria>
- Faction list/edit/delete/theme management is reachable from Settings -> Factions with no loss of capability.
- The standalone /factions route, sidebar entry, and page wrapper are removed.
- Quick Add "Add Faction" still works; build is clean.
</success_criteria>

<output>
Create `.planning/phases/135-faction-navigation-consolidation/135-02-SUMMARY.md` when done
</output>

---
phase: 135-faction-navigation-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/048_consolidate_factions.sql
  - src-tauri/src/lib.rs
  - tests/data-layer/migration048.test.ts
autonomous: true
requirements: [HON-05]
must_haves:
  truths:
    - "Duplicate collection factions sharing a wahapedia_faction_id are merged into one survivor row"
    - "Every unit still resolves a live faction after consolidation (units.faction_id RESTRICT preserved)"
    - "painting_recipes and army_lists faction links are preserved — no NULL introduced where a value existed"
    - "wishlist_items row count is unchanged — no CASCADE deletion"
    - "default_faction_id resolves to a live faction after consolidation (cold-boot theming intact)"
    - "The migration-parity gate passes with 48 migrations and pnpm check:version is green"
  artifacts:
    - path: "src-tauri/migrations/048_consolidate_factions.sql"
      provides: "Map-not-delete faction consolidation (re-point 4 FK surfaces + default_faction_id, then delete duplicates, then backfill NULLs)"
      contains: "wahapedia_faction_id"
    - path: "src-tauri/src/lib.rs"
      provides: "Migration{} entry version 48 (count 47 -> 48)"
      contains: "version: 48"
    - path: "tests/data-layer/migration048.test.ts"
      provides: "Zero-data-loss proof across all 4 FK surfaces + default_faction_id + cold-boot resolution + duplicate elimination"
      min_lines: 60
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/migrations/048_consolidate_factions.sql"
      via: "include_str! in Migration{} entry"
      pattern: "include_str!\\(\"../migrations/048_consolidate_factions.sql\"\\)"
    - from: "tests/data-layer/migration048.test.ts"
      to: "src-tauri/migrations/048_consolidate_factions.sql"
      via: "readFileSync + db.exec applies the migration under test"
      pattern: "048_consolidate_factions.sql"
---

<objective>
Ship migration 048 — the map-not-delete faction consolidation that is the data-loss trap of this milestone — and prove zero data loss with a data-layer test before any UI work depends on it.

Purpose: HON-05. User collection factions must be aligned/deduplicated toward the canonical `udb_factions` model with every FK reference preserved. The migration re-points dependents from duplicate faction rows to a single survivor row, then deletes only the now-orphaned duplicate — never deleting a faction that still has dependents.

Output: `048_consolidate_factions.sql`, its matching `lib.rs` `Migration{}` entry (count 47 -> 48), and `tests/data-layer/migration048.test.ts` asserting row-count invariants, FK resolution, cold-boot `default_faction_id` resolution, and duplicate elimination. All gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-faction-navigation-consolidation/135-CONTEXT.md
@.planning/phases/135-faction-navigation-consolidation/135-RESEARCH.md
@.planning/phases/135-faction-navigation-consolidation/135-PATTERNS.md
@.planning/phases/135-faction-navigation-consolidation/135-VALIDATION.md

<interfaces>
<!-- FK surfaces on factions.id (VERIFIED in RESEARCH.md FK Surface Enumeration) -->
- units.faction_id            ON DELETE RESTRICT   (migrations/001_core_schema.sql ~L22) — DELETE blocked until re-pointed
- painting_recipes.faction_id ON DELETE SET NULL   (migrations/001_core_schema.sql ~L69) — silent link loss if deleted first
- army_lists.faction_id       ON DELETE SET NULL   (migrations/001_core_schema.sql ~L105) — silent link loss if deleted first
- wishlist_items.faction_id   ON DELETE CASCADE     (migrations/009_wishlist.sql L5) — silent row loss if deleted first
- app_settings key 'default_faction_id' = TEXT value, NO FK (migrations/044_app_settings.sql) — manual UPDATE, CAST required
- painting_sessions has NO faction_id column (confirmed migrations 005/014/023) — NOT in scope

<!-- lib.rs current last entry (VERIFIED src-tauri/src/lib.rs L285-289) -->
Migration { version: 47, description: "army_list_unit_wargear", sql: include_str!("../migrations/047_army_list_unit_wargear.sql"), kind: MigrationKind::Up }

<!-- db-helpers harness (VERIFIED PATTERNS.md) -->
createHobbyforgeDb() runs ALL migrations including 048 (no-op on clean DB).
To test 048's transform, use an inline createDbUpToMigration(47) helper, seed duplicates, then db.exec the 048 file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing zero-data-loss test (Wave 0 scaffold)</name>
  <files>tests/data-layer/migration048.test.ts</files>
  <read_first>
    - tests/data-layer/migration033.test.ts (analog: node-env header, better-sqlite3, repoRoot resolution)
    - tests/data-layer/db-helpers.ts (createHobbyforgeDb pattern, HOBBYFORGE_MIGRATIONS, migrationsDir)
    - .planning/phases/135-faction-navigation-consolidation/135-PATTERNS.md (inline createDbUpToMigration helper + assertion shape, lines 198-255)
    - src-tauri/migrations/001_core_schema.sql (factions/units/painting_recipes/army_lists column shapes for seeding)
    - src-tauri/migrations/009_wishlist.sql (wishlist_items columns for seeding)
    - src-tauri/migrations/044_app_settings.sql (app_settings key/value shape for default_faction_id seeding)
  </read_first>
  <behavior>
    - Build a DB with only migrations 001-047 via an inline createDbUpToMigration(47) helper (readdirSync + numeric sort + filter <= 47).
    - Seed: one udb_factions row ('SM'); a survivor faction (lowest id, wahapedia_faction_id='SM'); a duplicate faction (higher id, wahapedia_faction_id='SM').
    - Seed one dependent on EACH of the 4 FK surfaces under the duplicate: a unit, a painting_recipe, an army_list, a wishlist_item; plus app_settings default_faction_id = CAST(duplicateId AS TEXT).
    - Record pre-migration counts for units/painting_recipes/army_lists/wishlist_items.
    - Apply migration 048 via db.exec(readFileSync(.../048_consolidate_factions.sql)).
    - Assert: all 4 row counts unchanged; duplicate faction row is gone; every units.faction_id === survivorId; no NULL introduced on the seeded recipe/army_list faction_id; default_faction_id value === String(survivorId) and resolves to a live factions.id; GROUP BY wahapedia_faction_id HAVING COUNT(*)>1 returns empty.
    - This test MUST fail initially (file 048 does not yet exist) — that is the RED state.
  </behavior>
  <action>Create `tests/data-layer/migration048.test.ts` with the `// @vitest-environment node` directive. Define an inline `createDbUpToMigration(upToNum)` helper mirroring the PATTERNS.md shape (do NOT use createHobbyforgeDb for the transform test — it would run 048 as a no-op on a clean DB). Seed the duplicate scenario across all four FK surfaces plus the TEXT `default_faction_id`. Use the survivor-selection rule "lowest id wins" to match the migration. Assert row-count invariants, duplicate elimination, FK re-point to survivor, no-NULL-introduced, and `default_faction_id` cold-boot resolution. Reference the seeded recipe/army_list by a known `name` so the no-NULL assertion is precise. Do NOT write the migration in this task — the test fails by design until Task 2.</action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/data-layer/migration048.test.ts 2>&1 | grep -qiE "fail|error|no such file|cannot" && echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - tests/data-layer/migration048.test.ts exists and contains the inline createDbUpToMigration helper (grep "createDbUpToMigration" returns a match)
    - The test seeds all four FK surfaces (grep finds INSERT INTO units, painting_recipes, army_lists, wishlist_items in the file)
    - Running the test currently FAILS (RED) because 048_consolidate_factions.sql does not yet exist
  </acceptance_criteria>
  <done>The test file exists, seeds the duplicate scenario across all 4 FK surfaces + default_faction_id, and fails because migration 048 is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 048 + register it in lib.rs (GREEN)</name>
  <files>src-tauri/migrations/048_consolidate_factions.sql, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/migrations/046_backfill_faction_udb_normalized.sql (correlated-UPDATE shape + normalized name-match backfill to copy verbatim for the NULL-backfill step)
    - src-tauri/migrations/039_collection_udb_link.sql (wahapedia_faction_id mapping origin)
    - src-tauri/migrations/033_database_hardening.sql (comment documenting: PRAGMA foreign_keys cannot be toggled inside a plugin-sql migration transaction)
    - src-tauri/src/lib.rs (get_migrations() array — append after the version:47 entry at ~L285-289)
    - .planning/phases/135-faction-navigation-consolidation/135-PATTERNS.md (the full Step 1-7 correlated-subquery SQL shape for 048, lines 58-127)
    - .planning/phases/135-faction-navigation-consolidation/135-RESEARCH.md (Pitfalls 1-3: no BEGIN/COMMIT, no PRAGMA toggle, CAST for app_settings)
  </read_first>
  <action>Create `src-tauri/migrations/048_consolidate_factions.sql` as a data-only consolidation. Survivor-selection rule: lowest `id` among rows sharing a non-NULL `wahapedia_faction_id` is the survivor. Use correlated-subquery re-point UPDATEs (NOT a temp table, NOT explicit BEGIN/COMMIT, NOT PRAGMA foreign_keys toggling — the plugin-sql runner wraps the file in its own transaction per the migration 033 note). Order the statements strictly: (1) re-point `units.faction_id` duplicate->survivor; (2) re-point `painting_recipes.faction_id`; (3) re-point `army_lists.faction_id`; (4) re-point `wishlist_items.faction_id`; (5) re-point `app_settings` value for `key = 'default_faction_id'` ONLY, using `CAST(... AS TEXT)` on both sides of the comparison and writing `CAST(survivor_id AS TEXT)`, also setting `updated_at = datetime('now')`; (6) `DELETE FROM factions` where id is a duplicate (now zero-dependency); (7) backfill still-NULL `wahapedia_faction_id` via the normalized name-match copied verbatim from migration 046. The app_settings UPDATE MUST be scoped to `key = 'default_faction_id'` and must not touch locale/currency/readiness keys (V5 / T-135-01). Save the file with LF line endings only — no CR bytes. Then append a `Migration{}` entry to `get_migrations()` in `lib.rs`: `version: 48, description: "consolidate_factions", sql: include_str!("../migrations/048_consolidate_factions.sql"), kind: MigrationKind::Up` immediately after the version:47 entry, bringing the count 47 -> 48.</action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/data-layer/migration048.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/data-layer/migration048.test.ts exits 0 (GREEN)
    - src-tauri/migrations/048_consolidate_factions.sql exists and contains no CR bytes: `node -e "process.exit(require('fs').readFileSync('src-tauri/migrations/048_consolidate_factions.sql').includes(13)?1:0)"` exits 0
    - The migration contains no BEGIN/COMMIT and no PRAGMA foreign_keys statement (grep -ciE "begin;|commit;|pragma foreign_keys" returns 0 ignoring comments)
    - The app_settings UPDATE is scoped to default_faction_id: grep "key = 'default_faction_id'" src-tauri/migrations/048_consolidate_factions.sql returns a match
    - src-tauri/src/lib.rs contains `version: 48` and `include_str!("../migrations/048_consolidate_factions.sql")`
  </acceptance_criteria>
  <done>Migration 048 exists with LF endings, re-points all 4 FK surfaces + the scoped default_faction_id, deletes orphaned duplicates, backfills NULLs; lib.rs has the version 48 entry; the data-loss test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the parity / version gates pass at 48 migrations</name>
  <files>tests/data-layer/migration048.test.ts</files>
  <read_first>
    - tests/data-layer/migration-parity.test.ts (Leg D-04/D-06 — auto-derives migration list from disk; compares lib.rs count)
    - tests/data-layer/db-helpers.ts (HOBBYFORGE_MIGRATION_COUNT is disk-derived; auto-increments to 48)
    - scripts/check-version.mjs (three-leg gate: version parity + file count == lib.rs count + CR-byte scan)
  </read_first>
  <action>Run the parity gates to prove migration 048 satisfies the Phase-130 release gate (D-10). The data-layer migration list and `HOBBYFORGE_MIGRATION_COUNT` are disk-derived and auto-pick-up 048; only the lib.rs `Migration{}` count needed the manual bump (done in Task 2). If `node scripts/check-version.mjs` reports a count mismatch, the lib.rs entry from Task 2 is missing or miscounted — fix it. If it reports a CR byte, re-save `048_consolidate_factions.sql` with LF endings. No `package.json`/`tauri.conf.json` version bump is required by this phase.</action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && pnpm test -- tests/data-layer/migration-parity.test.ts && node scripts/check-version.mjs</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test -- tests/data-layer/migration-parity.test.ts exits 0
    - node scripts/check-version.mjs exits 0
    - The migration count reported is 48 (file count == lib.rs Migration{} count == data-layer migration-list length)
  </acceptance_criteria>
  <done>Both parity gates are green at 48 migrations; the release gate accepts migration 048.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration 048 -> app_settings table | The consolidation UPDATE writes to the shared key/value settings store that also holds locale, currency, and army_readiness_target keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Tampering | app_settings UPDATE in migration 048 (V5 Input Validation) | mitigate | The UPDATE is scoped with `WHERE key = 'default_faction_id'`; it must not match locale/currency/army_readiness_target rows. Verified by the data-loss test asserting only default_faction_id changed and by the acceptance-criteria grep for the scoped WHERE clause. |
| T-135-02 | Denial of Service | premature DELETE before re-point | mitigate | Strict statement ordering (re-point all 4 FK surfaces + default_faction_id BEFORE the DELETE) prevents RESTRICT-block (units), SET NULL link loss (recipes/army_lists), and CASCADE row loss (wishlist). Verified by row-count invariants in migration048.test.ts. |
| T-135-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH.md: no new packages). Legitimacy gate not applicable. |
</threat_model>

<verification>
- `pnpm test -- tests/data-layer/migration048.test.ts` green (zero data loss across all 4 FK surfaces + default_faction_id)
- `pnpm test -- tests/data-layer/migration-parity.test.ts` green at 48 migrations
- `node scripts/check-version.mjs` green (version parity + count + CR-byte)
- `048_consolidate_factions.sql` has LF endings only
</verification>

<success_criteria>
- Migration 048 merges duplicate factions, re-points all 4 FK surfaces + default_faction_id, deletes only orphaned duplicates, and backfills still-NULL wahapedia_faction_id.
- Zero data loss is proven: row counts for units/painting_recipes/army_lists/wishlist_items are identical before/after; no FK is NULLed; default_faction_id resolves to a live faction.
- The Phase-130 parity gate is green at 48 migrations and `pnpm check:version` passes.
</success_criteria>

<output>
Create `.planning/phases/135-faction-navigation-consolidation/135-01-SUMMARY.md` when done
</output>

---
phase: 66-army-list-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/computeUnitWarnings.ts
  - src/types/armyList.ts
  - tests/lib/computeUnitWarnings.test.ts
autonomous: true
requirements: [LV-01, LV-03, LV-04]

must_haves:
  truths:
    - "computeUnitWarnings returns hard warnings for points-exceeded when totalPoints > pointsLimit"
    - "computeUnitWarnings returns soft warnings for unpainted, unassembled, manual override, unknown points, stale/never freshness"
    - "Points-exceeded warning is suppressed when pointsLimit is null"
    - "TACTICAL_ROLES const array contains exactly 7 roles with display labels"
    - "computeListHealthStats returns ownership %, total points, warning counts"
  artifacts:
    - path: "src/lib/computeUnitWarnings.ts"
      provides: "Pure warning classification function + list health aggregation"
      exports: ["computeUnitWarnings", "computeListHealthStats", "UnitWarnings", "WarningContext", "ListHealthStats"]
    - path: "src/types/armyList.ts"
      provides: "TACTICAL_ROLES const array, TacticalRole type, TACTICAL_ROLES_DISPLAY map, extended ArmyListUnitRow"
      contains: "TACTICAL_ROLES"
    - path: "tests/lib/computeUnitWarnings.test.ts"
      provides: "Unit tests for warning classification and health stats"
  key_links:
    - from: "src/lib/computeUnitWarnings.ts"
      to: "src/types/armyList.ts"
      via: "imports ArmyListUnitRow type"
      pattern: "import.*ArmyListUnitRow.*armyList"
---

<objective>
Create the pure warning classification function, tactical role type definitions, and comprehensive unit tests. This establishes the core logic that all downstream UI components depend on.

Purpose: The warning system is the heart of Phase 66 -- every UI surface (inline icons, summary bar, health panel) consumes the output of computeUnitWarnings. Building and testing it first ensures correctness before any UI integration.

Output: computeUnitWarnings.ts pure function, extended armyList.ts types, full test suite.
</objective>

<execution_context>
@.planning/phases/66-army-list-validation/66-RESEARCH.md
@.planning/phases/66-army-list-validation/66-CONTEXT.md
@.planning/phases/66-army-list-validation/66-UI-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/armyList.ts (current):
```typescript
export interface ArmyListUnitRow extends ArmyListUnit {
  unit_name: string;
  unit_points: number | null;
  effective_points: number;
  faction_id: number;
  status_assembly: number;       // 0 = not assembled, 1 = assembled
  status_painting: string;       // "Completed" = fully painted (canonical)
  painting_percentage: number;
}
```

From src/lib/syncFreshness.ts:
```typescript
export type SyncFreshness = "fresh" | "aging" | "stale" | "never";
```

From src/lib/computeWorkflowPosition.ts (pattern to follow):
```typescript
// Pure function: takes data in, returns typed result, no side effects
export function computeWorkflowPosition(
  lastSessionStepId: number | null,
  lastSessionSectionName: string | null,
  sections: RecipeSection[],
  steps: RecipeStep[],
): WorkflowPosition | null { ... }
```

From src/types/unit.ts (const array pattern to follow):
```typescript
export const PAINTING_STATUS_ORDER = ["Not Started", "Built", "Primed", ...] as const;
export type PaintingStatus = typeof PAINTING_STATUS_ORDER[number];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define TACTICAL_ROLES and extend ArmyListUnitRow type</name>
  <files>src/types/armyList.ts</files>
  <read_first>
    - src/types/armyList.ts
    - src/types/unit.ts (for PAINTING_STATUS_ORDER const array pattern)
  </read_first>
  <behavior>
    - TACTICAL_ROLES array has exactly 7 entries: anti_tank, screening, objective_holder, fire_support, melee_threat, utility, transport
    - TacticalRole is a union type inferred from the const array
    - TACTICAL_ROLES_DISPLAY maps each role to a human-readable label
    - ArmyListUnitRow extended with tactical_role: string | null
    - UpdateArmyListUnitInput extended with tactical_role: string | null
  </behavior>
  <action>
    Add to src/types/armyList.ts per D-07 and D-08:
    1. Export a TACTICAL_ROLES const array with the 7 values: anti_tank, screening, objective_holder, fire_support, melee_threat, utility, transport
    2. Export TacticalRole type inferred from the const array (typeof TACTICAL_ROLES[number])
    3. Export TACTICAL_ROLES_DISPLAY Record mapping each role to display label: Anti-Tank, Screening, Obj. Holder, Fire Support, Melee Threat, Utility, Transport
    4. Add tactical_role: string | null to ArmyListUnitRow interface (per D-05, lives on the join table)
    5. Add tactical_role: string | null to UpdateArmyListUnitInput interface (full-replacement, Pitfall 2)
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - TACTICAL_ROLES.length === 7
    - TACTICAL_ROLES includes "anti_tank", "screening", "objective_holder", "fire_support", "melee_threat", "utility", "transport"
    - ArmyListUnitRow has tactical_role property typed as string | null
    - UpdateArmyListUnitInput has tactical_role property typed as string | null
    - TACTICAL_ROLES_DISPLAY["anti_tank"] === "Anti-Tank"
    - TACTICAL_ROLES_DISPLAY["objective_holder"] === "Obj. Holder"
    - pnpm build succeeds with no type errors
  </acceptance_criteria>
  <done>Type definitions exported, build passes, downstream tasks can import TACTICAL_ROLES and extended types</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create computeUnitWarnings pure function with tests</name>
  <files>src/lib/computeUnitWarnings.ts, tests/lib/computeUnitWarnings.test.ts</files>
  <read_first>
    - src/lib/computeWorkflowPosition.ts (pure function pattern)
    - src/lib/syncFreshness.ts (SyncFreshness type)
    - src/types/armyList.ts (ArmyListUnitRow after Task 1)
    - tests/lib/computeWorkflowPosition.test.ts (test pattern if exists)
  </read_first>
  <behavior>
    - computeUnitWarnings with points exceeded: totalPoints=2100, pointsLimit=2000 returns hard: ["Points exceeded"]
    - computeUnitWarnings with pointsLimit=null: never returns "Points exceeded" regardless of totalPoints
    - computeUnitWarnings with status_painting !== "Completed": returns soft: includes "Not painted"
    - computeUnitWarnings with status_assembly === 0: returns soft: includes "Not assembled"
    - computeUnitWarnings with points_override !== null: returns soft: includes "Manual override"
    - computeUnitWarnings with effective_points === 0: returns soft: includes "Unknown points"
    - computeUnitWarnings with freshness "stale": returns soft: includes "Stale points"
    - computeUnitWarnings with freshness "never": returns soft: includes "Stale points"
    - computeUnitWarnings with freshness "fresh": does NOT include "Stale points"
    - computeUnitWarnings with fully healthy unit (Completed, assembled, known points, fresh, no override, under limit): returns { hard: [], soft: [] }
    - computeListHealthStats computes totalPoints as sum of effective_points
    - computeListHealthStats computes ownershipPct as 100 (all units owned per D-15)
    - computeListHealthStats computes battleReadyPct correctly
    - computeListHealthStats counts hardWarnings and softWarnings across all units
    - computeListHealthStats returns pointsExceeded=true when totalPoints > pointsLimit
  </behavior>
  <action>
    Create src/lib/computeUnitWarnings.ts following the computeWorkflowPosition pattern:

    1. Export UnitWarnings interface with hard: string[] and soft: string[]
    2. Export WarningContext interface with totalPoints: number, pointsLimit: number | null, freshness: SyncFreshness (import from syncFreshness.ts)
    3. Export computeUnitWarnings function taking Pick of ArmyListUnitRow (effective_points, points_override, status_painting, status_assembly) plus WarningContext, returning UnitWarnings
    4. Hard warning: "Points exceeded" when context.pointsLimit !== null AND context.totalPoints > context.pointsLimit (per D-04, list-level condition, Pitfall 4)
    5. Soft warnings: "Not painted" when status_painting !== "Completed" (canonical value, not "Complete"), "Not assembled" when status_assembly === 0, "Manual override" when points_override !== null, "Unknown points" when effective_points === 0, "Stale points" when freshness is "stale" or "never"
    6. Export ListHealthStats interface with totalPoints, pointsLimit, ownershipPct, battleReadyPct, hardWarningCount, softWarningCount, pointsExceeded
    7. Export computeListHealthStats function taking units array (ArmyListUnitRow[]), pointsLimit (number | null), freshness (SyncFreshness), returning ListHealthStats. Computes: totalPoints = sum of effective_points, paintedPoints = sum of effective_points where status_painting === "Completed", battleReadyPct = round((paintedPoints/totalPoints)*100) or 0, ownershipPct = 100 (per D-15), pointsExceeded = pointsLimit !== null AND totalPoints > pointsLimit, warning counts by iterating computeUnitWarnings per unit

    Create tests/lib/computeUnitWarnings.test.ts with tests for every behavior listed above. Use a helper factory function to create test ArmyListUnitRow objects with sane defaults (effective_points=100, status_painting="Completed", status_assembly=1, points_override=null).
  </action>
  <verify>
    <automated>pnpm test -- tests/lib/computeUnitWarnings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All test cases pass in tests/lib/computeUnitWarnings.test.ts
    - computeUnitWarnings is a pure function (no imports from db/, hooks/, or React)
    - SyncFreshness imported from @/lib/syncFreshness (not redefined)
    - Hard warning "Points exceeded" only appears when pointsLimit !== null AND totalPoints > pointsLimit
    - Soft warning "Stale points" appears for freshness "stale" AND "never" but not "fresh" or "aging"
    - computeListHealthStats.ownershipPct always returns 100
    - pnpm build succeeds
  </acceptance_criteria>
  <done>Pure warning functions with full test coverage; all tests green; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All inputs are from the local SQLite database (already trusted). Tactical role values are constrained by the TACTICAL_ROLES const array in TypeScript.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Tampering | tactical_role TEXT column | accept | Local SQLite only; no network input. TypeScript const array constrains UI. |
</threat_model>

<verification>
- pnpm test -- tests/lib/computeUnitWarnings.test.ts (all tests green)
- pnpm build (no type errors)
</verification>

<success_criteria>
- computeUnitWarnings classifies all hard/soft warning types per D-02
- computeListHealthStats aggregates correctly for the health panel per D-13
- TACTICAL_ROLES and TacticalRole type exported per D-07
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/66-army-list-validation/66-01-SUMMARY.md`
</output>

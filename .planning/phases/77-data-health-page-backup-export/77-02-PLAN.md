---
phase: 77-data-health-page-backup-export
plan: 02
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - src/features/data-health/DataHealthPage.tsx
  - src/features/data-health/VersionInfoCard.tsx
  - src/features/data-health/TableCountsGrid.tsx
  - src/features/data-health/DiagnosticsCard.tsx
  - src/features/data-health/BackupCard.tsx
  - src/app/data-health/page.tsx
autonomous: false
requirements:
  - DX-01
  - DX-02
  - DX-03
  - DX-04
  - BK-01
  - BK-02
  - BK-03

must_haves:
  truths:
    - "User can see app version, schema versions, last sync date, and sync error count on the Data Health page"
    - "User can see row counts for units, recipes, assignments, progress, and synced points"
    - "User can see diagnostic flags for orphaned progress, ambiguous matches, and stale sync"
    - "Diagnostics load asynchronously with skeleton placeholders -- page never blocks"
    - "User can click Create Backup, pick a file location, and receive a safe VACUUM INTO copy"
    - "After backup, last backup date and filename are displayed on the page"
  artifacts:
    - path: "src/features/data-health/DataHealthPage.tsx"
      provides: "Page root assembling all sections"
      contains: "DataHealthPage"
    - path: "src/features/data-health/VersionInfoCard.tsx"
      provides: "Version and schema info card"
      contains: "VersionInfoCard"
    - path: "src/features/data-health/TableCountsGrid.tsx"
      provides: "5 metric cards for table row counts"
      contains: "TableCountsGrid"
    - path: "src/features/data-health/DiagnosticsCard.tsx"
      provides: "Diagnostic flags list"
      contains: "DiagnosticsCard"
    - path: "src/features/data-health/BackupCard.tsx"
      provides: "Backup action and status card"
      contains: "BackupCard"
  key_links:
    - from: "src/features/data-health/VersionInfoCard.tsx"
      to: "src/hooks/useDiagnostics.ts"
      via: "useSchemaVersions hook"
      pattern: "useSchemaVersions"
    - from: "src/features/data-health/TableCountsGrid.tsx"
      to: "src/hooks/useDiagnostics.ts"
      via: "useTableCounts hook"
      pattern: "useTableCounts"
    - from: "src/features/data-health/BackupCard.tsx"
      to: "src-tauri/src/lib.rs"
      via: "invoke('backup_database')"
      pattern: "invoke.*backup_database"
    - from: "src/app/data-health/page.tsx"
      to: "src/features/data-health/DataHealthPage.tsx"
      via: "re-export"
      pattern: "DataHealthPage"
---

<objective>
Build the complete Data Health page UI with five sections: page header, version/schema info card, table counts grid, diagnostics flags card, and backup action card.

Purpose: Give the user a single page to inspect database health and create safe backups. Each section loads independently via its own React Query hook, so the page is immediately interactive.
Output: Five feature components in src/features/data-health/ wired through src/app/data-health/page.tsx.
</objective>

<execution_context>
@C:\Users\antoi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\antoi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-data-health-page-backup-export/77-CONTEXT.md
@.planning/phases/77-data-health-page-backup-export/77-RESEARCH.md
@.planning/phases/77-data-health-page-backup-export/77-UI-SPEC.md
@.planning/phases/77-data-health-page-backup-export/77-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/db/queries/diagnostics.ts:
```typescript
export interface TableCounts {
  units: number;
  painting_recipes: number;
  unit_recipe_assignments: number;
  unit_recipe_step_progress: number;
  synced_unit_points: number;
}
export interface DiagnosticFlag {
  type: string;
  count: number;
  description: string;
  severity: "warning" | "info";
}
export interface SchemaVersions {
  hobbyforge: number;
  rules: number;
}
```

From src/hooks/useDiagnostics.ts:
```typescript
export const TABLE_COUNTS_KEY = ["diagnostics", "table-counts"] as const;
export const DIAGNOSTIC_FLAGS_KEY = ["diagnostics", "flags"] as const;
export const SCHEMA_VERSIONS_KEY = ["diagnostics", "schema-versions"] as const;
export const BACKUP_STORAGE_KEY = "lastBackup";
export function useTableCounts(): UseQueryResult<TableCounts>;
export function useDiagnosticFlags(): UseQueryResult<DiagnosticFlag[]>;
export function useSchemaVersions(): UseQueryResult<SchemaVersions>;
export function useBackupStatus(): { date: string; path: string; success: boolean } | null;
```

From src/hooks/useDatasheet.ts:
```typescript
export function useRulesSyncMeta(): UseQueryResult<RulesSyncMeta | null>;
```

From src/hooks/useSyncErrors.ts:
```typescript
export function useRulesSyncErrors(): UseQueryResult<SyncError[]>;
```

From src/lib/syncFreshness.ts:
```typescript
export function getSyncFreshness(lastSyncAt: string | null): SyncFreshness;
export function getSyncAgeLabel(lastSyncAt: string | null): string;
export const FRESHNESS_DOT_CLASS: Record<SyncFreshness, string>;
```

From src/features/dashboard/StatCard.tsx:
```typescript
export function StatCard(props: StatCardProps);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature components -- VersionInfoCard, TableCountsGrid, DiagnosticsCard, BackupCard</name>
  <files>src/features/data-health/VersionInfoCard.tsx, src/features/data-health/TableCountsGrid.tsx, src/features/data-health/DiagnosticsCard.tsx, src/features/data-health/BackupCard.tsx</files>
  <read_first>
    - src/features/dashboard/StatCard.tsx (StatCardProps interface, usage pattern for metric display)
    - src/features/rules-hub/SyncStatusCard.tsx (sync status display patterns -- freshness dot, Badge usage)
    - src/hooks/useDiagnostics.ts (Plan 01 output -- hook signatures, BACKUP_STORAGE_KEY)
    - src/hooks/useDatasheet.ts (useRulesSyncMeta hook signature)
    - src/hooks/useSyncErrors.ts (useRulesSyncErrors hook)
    - src/lib/syncFreshness.ts (getSyncFreshness, getSyncAgeLabel, FRESHNESS_DOT_CLASS)
    - .planning/phases/77-data-health-page-backup-export/77-UI-SPEC.md (full visual contract -- sections 2-5)
  </read_first>
  <action>
    Create four components following the UI-SPEC precisely. All components are named function exports.

    **src/features/data-health/VersionInfoCard.tsx (UI-SPEC Section 2, per D-13/D-14/D-15):**
    - Import `useSchemaVersions` from `@/hooks/useDiagnostics`, `useRulesSyncMeta` from `@/hooks/useDatasheet`, `useRulesSyncErrors` from `@/hooks/useSyncErrors`, `getVersion` from `@tauri-apps/api/app`, `getSyncFreshness`, `getSyncAgeLabel`, `FRESHNESS_DOT_CLASS` from `@/lib/syncFreshness`
    - Use a local `useState` + `useEffect` to call `getVersion()` once and store the app version string
    - Render a single `Card` containing a horizontal flex row (`flex flex-wrap gap-8 p-6`) of 5 key-value pairs:
      1. "App Version" -> `v{appVersion}` (Skeleton w-16 h-4 while loading)
      2. "DB Schema" -> `v{schemaVersions.hobbyforge}` (Skeleton while loading)
      3. "Rules Schema" -> `v{schemaVersions.rules}` (Skeleton while loading)
      4. "Last Sync" -> `getSyncAgeLabel(syncMeta?.last_sync_at)` with freshness dot (8px rounded-full inline span using `FRESHNESS_DOT_CLASS[getSyncFreshness(syncMeta?.last_sync_at)]`)
      5. "Sync Errors" -> Badge with count. `variant="destructive"` if count > 0, `variant="secondary"` if 0
    - Each item is a vertical stack: label (`text-xs text-muted-foreground uppercase tracking-wider`) over value (`text-sm font-semibold`)
    - Each value independently shows Skeleton when its hook is loading

    **src/features/data-health/TableCountsGrid.tsx (UI-SPEC Section 3, per D-11):**
    - Import `useTableCounts` from `@/hooks/useDiagnostics`, `StatCard` from `@/features/dashboard/StatCard`
    - Render a grid (`grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4`)
    - 5 StatCard instances: value from `tableCountsData.{field}`, label from UI-SPEC table. No `animate`, no `to` prop.
    - Cards: "Units" (units), "Recipes" (painting_recipes), "Assignments" (unit_recipe_assignments), "Step Progress" (unit_recipe_step_progress), "Synced Points" (synced_unit_points)
    - Loading state: when `useTableCounts` isLoading, render 5 Card placeholders each with Skeleton w-12 h-8 and Skeleton w-20 h-4

    **src/features/data-health/DiagnosticsCard.tsx (UI-SPEC Section 4, per D-08/D-09/D-10):**
    - Import `useDiagnosticFlags` from `@/hooks/useDiagnostics`, `useRulesSyncMeta` from `@/hooks/useDatasheet`, `getSyncFreshness`, `getSyncAgeLabel` from `@/lib/syncFreshness`
    - Compute stale sync flag in this component (per D-10): if `getSyncFreshness(syncMeta?.last_sync_at) === "stale"`, add an info-severity flag with description `"Rules data is {getSyncAgeLabel()} -- consider re-syncing"` and type `"stale_sync"`
    - Combine stale sync flag with `useDiagnosticFlags()` data into a single array
    - Render a `Card` with `CardHeader` title "Diagnostics" and `CardContent` containing:
      - If all flags empty AND not loading: single row with green dot (8px `bg-green-500 rounded-full inline-block`) + "All diagnostics passed" in `text-sm text-muted-foreground`
      - Otherwise: vertical list of flag rows, each with: `Badge` (variant `"destructive"` for severity "warning", variant `"secondary"` for severity "info"), description text `text-sm text-foreground`, count right-aligned `text-sm font-semibold tabular-nums`. Rows separated by `border-b border-border/40` (last row no border, padding `py-3`)
    - Loading state: 3 rows of Skeleton full-width h-10 with gap-2

    **src/features/data-health/BackupCard.tsx (UI-SPEC Section 5, per D-04/D-06, BK-01/BK-03):**
    - Import `useBackupStatus`, `BACKUP_STORAGE_KEY` from `@/hooks/useDiagnostics`, `save` from `@tauri-apps/plugin-dialog`, `invoke` from `@tauri-apps/api/core`, `Download`, `Loader2` from `lucide-react`, `toast` from `sonner`
    - Local state: `isBackingUp: boolean` (default false), `backupStatus` from `useBackupStatus()`
    - Use `useState` for `backupStatus` so it re-renders after backup completes (call `useBackupStatus()` to init, update state after localStorage write)
    - `handleBackup` async function:
      1. Build default filename: `hobbyforge-backup-${new Date().toISOString().slice(0, 10)}.db`
      2. Call `save({ title: "Save Database Backup", defaultPath: filename, filters: [{ name: "SQLite Database", extensions: ["db"] }] })`
      3. If null (cancelled), return
      4. Set `isBackingUp = true`
      5. Try: `await invoke("backup_database", { destination })`, store `{ date: new Date().toISOString(), path: destination, success: true }` in localStorage under `BACKUP_STORAGE_KEY`, update local state, `toast.success("Backup created successfully")`
      6. Catch: `toast.error("Backup failed: {error.message}")`, set isBackingUp false
      7. Finally: set isBackingUp false
    - Render a `Card` with padding. Top row flex justify-between:
      - Left: "Database Backup" heading (`text-sm font-semibold`) + last backup status below (`text-sm text-muted-foreground`). If backupStatus exists: "Last backup: {relative date} -- {filename from path}". If null: "No backups yet"
      - Right: Button primary variant. When not backing up: text "Create Backup", icon `Download`. When backing up: text "Creating backup...", icon `Loader2` with `animate-spin`, disabled
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/features/data-health/VersionInfoCard.tsx exports VersionInfoCard, calls useSchemaVersions(), useRulesSyncMeta(), useRulesSyncErrors(), getVersion()
    - VersionInfoCard renders 5 key-value pairs: App Version, DB Schema, Rules Schema, Last Sync (with freshness dot), Sync Errors (with Badge)
    - src/features/data-health/TableCountsGrid.tsx exports TableCountsGrid, calls useTableCounts(), renders 5 StatCard instances in a responsive grid
    - StatCard labels match: "Units", "Recipes", "Assignments", "Step Progress", "Synced Points"
    - src/features/data-health/DiagnosticsCard.tsx exports DiagnosticsCard, calls useDiagnosticFlags() and useRulesSyncMeta(), computes stale sync flag
    - When no flags: renders green dot + "All diagnostics passed"
    - When flags present: renders Badge (destructive for warning, secondary for info) + description + count per row
    - src/features/data-health/BackupCard.tsx exports BackupCard, calls save() from plugin-dialog, invoke("backup_database"), writes to localStorage under BACKUP_STORAGE_KEY
    - BackupCard shows "No backups yet" when no prior backup, shows "Last backup: ..." when backup exists
    - Button shows Download icon normally, Loader2 animate-spin + disabled when backing up
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>All four section components render correctly with their hooks, loading states, and backup interaction</done>
</task>

<task type="auto">
  <name>Task 2: DataHealthPage assembly + route page re-export</name>
  <files>src/features/data-health/DataHealthPage.tsx, src/app/data-health/page.tsx</files>
  <read_first>
    - src/features/data-health/VersionInfoCard.tsx (Task 1 output)
    - src/features/data-health/TableCountsGrid.tsx (Task 1 output)
    - src/features/data-health/DiagnosticsCard.tsx (Task 1 output)
    - src/features/data-health/BackupCard.tsx (Task 1 output)
    - src/app/dashboard/page.tsx (page assembly pattern -- section ordering, heading style)
    - .planning/phases/77-data-health-page-backup-export/77-UI-SPEC.md (Section 1: Page Header + overall layout)
  </read_first>
  <action>
    **src/features/data-health/DataHealthPage.tsx (new file):**
    - Import all four section components: VersionInfoCard, TableCountsGrid, DiagnosticsCard, BackupCard
    - Export named function `DataHealthPage`
    - Render single-column scrollable layout with consistent page padding (match other pages -- typically `p-6 space-y-6`)
    - Section order per UI-SPEC:
      1. Page header: `h1` at `text-xl font-semibold` -- "Data Health"
      2. VersionInfoCard
      3. Section title "Table Counts" (`text-lg font-semibold`) + TableCountsGrid
      4. DiagnosticsCard (has its own CardHeader title)
      5. BackupCard

    **src/app/data-health/page.tsx (update Plan 01 stub):**
    - Replace the placeholder content with a re-export: `export { DataHealthPage } from "@/features/data-health/DataHealthPage"`
  </action>
  <verify>
    <automated>cd "C:/Documents/Claude Apps/Warhammer App" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/features/data-health/DataHealthPage.tsx exports DataHealthPage, imports and renders VersionInfoCard, TableCountsGrid, DiagnosticsCard, BackupCard in that order
    - Page heading is "Data Health" at text-xl font-semibold
    - Layout uses space-y-6 for section spacing
    - src/app/data-health/page.tsx re-exports DataHealthPage from the features module
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Data Health page is fully assembled, navigable via /data-health route, and shows all five sections with async loading</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Data Health page</name>
  <what-built>Complete Data Health page with version info, table counts, diagnostics flags, and backup functionality</what-built>
  <how-to-verify>
    1. Run `pnpm tauri dev` to launch the app
    2. Click "Data Health" in the sidebar (Management group, last item) -- verify HeartPulse icon
    3. Verify page loads with skeleton placeholders that resolve independently
    4. Check Version Info section shows: App Version (v0.2.13), DB Schema version, Rules Schema version, Last Sync date with colored dot, Sync Errors count
    5. Check Table Counts grid shows 5 metric cards with actual numbers: Units, Recipes, Assignments, Step Progress, Synced Points
    6. Check Diagnostics section -- if no issues, shows green dot + "All diagnostics passed"
    7. Click "Create Backup" button -- verify save dialog opens with default filename hobbyforge-backup-YYYY-MM-DD.db
    8. Pick a location and confirm -- verify toast "Backup created successfully" appears
    9. Verify "Last backup: ..." status updates on the BackupCard
    10. Verify the backup .db file exists at the chosen location and can be opened with a SQLite browser
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-selected file path -> Rust invoke | File path from save dialog sent to backup_database command |
| localStorage -> UI | Backup metadata read from localStorage rendered in page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04 | Spoofing | localStorage backup status | accept | Single-user desktop app; localStorage is user-writable by design |
| T-77-05 | Information Disclosure | Backup file on disk | accept | User chose the location; same data they already have access to |
| T-77-06 | Denial of Service | Large DB backup on slow disk | accept | VACUUM INTO is SQLite-managed; user can cancel via OS dialog |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. Navigate to /data-health -- page renders with all 5 sections
3. Version info shows real values (not placeholders) after loading
4. Table counts show actual row numbers
5. Backup flow: click -> dialog -> file created -> toast -> status updated
6. Diagnostics section shows appropriate flags or "All diagnostics passed"
</verification>

<success_criteria>
- Data Health page is navigable from sidebar
- All 5 sections render with real data from hooks
- Each section loads independently with skeleton placeholders
- Backup creates a valid .db file via VACUUM INTO
- Backup status persists in localStorage and displays on page
- All diagnostic flags show actionable descriptions
</success_criteria>

<output>
After completion, create `.planning/phases/77-data-health-page-backup-export/77-02-SUMMARY.md`
</output>

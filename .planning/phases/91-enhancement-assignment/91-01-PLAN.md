---
phase: 91-enhancement-assignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/queries/datasheets.ts
  - src/hooks/useUnitKeywords.ts
  - src/lib/computeUnitWarnings.ts
  - tests/army-list/computeListHealthStats.test.ts
autonomous: true
requirements:
  - ENH-02
  - ENH-03

must_haves:
  truths:
    - "getUnitKeywords('Space Marine Captain') returns { isCharacter: true, isEpicHero: false } when rules.db is synced"
    - "getUnitKeywords returns { isCharacter: false, isEpicHero: false } safely when rules.db is not synced (try/catch)"
    - "useUnitKeywords hook caches results with staleTime: Infinity"
    - "computeListHealthStats(units, limit, freshness, 60) includes the 60 enhancement points in totalPoints"
    - "pointsExceeded reflects unit points + enhancement points combined"
    - "computeListHealthStats called with 3 args still works (enhancementTotal defaults to 0)"
    - "computeListHealthStats tests all pass: pnpm test -- tests/army-list/computeListHealthStats.test.ts"
  artifacts:
    - path: "src/db/queries/datasheets.ts"
      provides: "getUnitKeywords function + UnitKeywordStatus interface"
      contains: "getUnitKeywords"
    - path: "src/hooks/useUnitKeywords.ts"
      provides: "useUnitKeywords React Query hook"
      exports: ["useUnitKeywords", "UNIT_KEYWORDS_KEY"]
    - path: "src/lib/computeUnitWarnings.ts"
      provides: "computeListHealthStats with optional enhancementTotal param"
      contains: "enhancementTotal = 0"
    - path: "tests/army-list/computeListHealthStats.test.ts"
      provides: "Tests for ENH-03 points math"
      contains: "computeListHealthStats"
  key_links:
    - from: "src/hooks/useUnitKeywords.ts"
      to: "src/db/queries/datasheets.ts"
      via: "import { getUnitKeywords }"
      pattern: "getUnitKeywords"
    - from: "src/features/army-lists/ArmyListSummaryBar.tsx"
      to: "src/lib/computeUnitWarnings.ts"
      via: "computeListHealthStats(units, pointsLimit, freshness, enhancementTotal)"
      pattern: "computeListHealthStats"
---

<objective>
Deliver the two foundational utilities that the EnhancementPickerSheet (Plan 02) depends on:
1. `getUnitKeywords` query function + `useUnitKeywords` hook — cross-DB lookup from rules.db to determine Character / Epic Hero status by unit name
2. `computeListHealthStats` extension — optional `enhancementTotal` param so enhancement points count toward the list total and `pointsExceeded` check

Also lays the test scaffold for the pure function so the full suite stays green.

Purpose: Plan 02 cannot be written without these contracts existing first. Both utilities are zero-UI data/logic work.
Output: Three modified/new source files + one new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-enhancement-assignment/91-CONTEXT.md
@.planning/phases/91-enhancement-assignment/91-RESEARCH.md
@.planning/phases/91-enhancement-assignment/91-PATTERNS.md

Prior phase summaries needed for patterns:
@.planning/phases/90-loadout-builder/90-02-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor must replicate — extracted from codebase. -->

From src/db/queries/datasheets.ts (existing patterns to match):
- All functions use: `const db = await getRulesDb();`
- All wrapped in try/catch returning null or safe default
- Existing keyword usage: `SELECT * FROM rw_datasheet_keywords WHERE datasheet_id = $1`
- Import pattern: `import { getRulesDb } from "@/db/rules-client";`

From src/hooks/useLoadoutOptions.ts (direct template for useUnitKeywords):
```
export const UNIT_KEYWORDS_KEY = (unitName: string) => ["unit-keywords", unitName] as const;
export function useUnitKeywords(unitName: string | undefined) {
  return useQuery({
    queryKey: unitName !== undefined ? UNIT_KEYWORDS_KEY(unitName) : (["unit-keywords"] as const),
    queryFn: () => unitName !== undefined ? getUnitKeywords(unitName) : Promise.resolve({ isCharacter: false, isEpicHero: false }),
    enabled: unitName !== undefined,
    staleTime: Infinity,
  });
}
```

From src/lib/computeUnitWarnings.ts (current signature to extend):
```typescript
export function computeListHealthStats(
  units: ArmyListUnitRow[],
  pointsLimit: number | null,
  freshness: SyncFreshness,
): ListHealthStats {
  const totalPoints = units.reduce((sum, u) => sum + u.effective_points, 0);
  ...
  const pointsExceeded = pointsLimit !== null && totalPoints > pointsLimit;
```

New `UnitKeywordStatus` interface to export:
```typescript
export interface UnitKeywordStatus {
  isCharacter: boolean;
  isEpicHero: boolean;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add getUnitKeywords to datasheets.ts + create useUnitKeywords hook</name>
  <files>src/db/queries/datasheets.ts, src/hooks/useUnitKeywords.ts</files>
  <read_first>
    - src/db/queries/datasheets.ts — read the full file to understand existing function structure, try/catch pattern, getRulesDb import, and placement point (after getRulesSyncMeta)
    - src/db/rules-client.ts — confirm getRulesDb export name
    - src/hooks/useLoadoutOptions.ts — direct template for the hook file structure
    - src-tauri/migrations/rules_001_schema.sql — confirm rw_datasheets and rw_datasheet_keywords table/column names
    - .planning/phases/91-enhancement-assignment/91-PATTERNS.md — Pattern Map sections for datasheets.ts and useUnitKeywords.ts
  </read_first>
  <behavior>
    - getUnitKeywords("Space Marine Captain") → { isCharacter: true, isEpicHero: false }
    - getUnitKeywords("Marneus Calgar") → { isCharacter: true, isEpicHero: true }
    - getUnitKeywords("Intercessors") → { isCharacter: false, isEpicHero: false }
    - getUnitKeywords("anything") when rules.db not synced → { isCharacter: false, isEpicHero: false } (no throw)
    - useUnitKeywords(undefined) → disabled query (does not run)
    - useUnitKeywords("Captain") → runs query, caches with staleTime: Infinity
  </behavior>
  <action>
    In src/db/queries/datasheets.ts — add after the existing getRulesSyncMeta function:

    Export a new interface UnitKeywordStatus with fields isCharacter: boolean and isEpicHero: boolean.

    Export a new async function getUnitKeywords(unitName: string): Promise<UnitKeywordStatus> that:
    - Wraps the entire body in try/catch; on catch returns { isCharacter: false, isEpicHero: false }
    - Gets db via getRulesDb()
    - Runs: SELECT k.keyword FROM rw_datasheets d JOIN rw_datasheet_keywords k ON k.datasheet_id = d.id WHERE LOWER(d.name) = LOWER($1) AND LOWER(k.keyword) IN ('character', 'epic hero'), passing [unitName]
    - Returns { isCharacter: rows.some(r => r.keyword.toLowerCase() === 'character'), isEpicHero: rows.some(r => r.keyword.toLowerCase() === 'epic hero') }

    Create src/hooks/useUnitKeywords.ts as a new file following the useLoadoutOptions.ts structure:
    - Export const UNIT_KEYWORDS_KEY = (unitName: string) => ["unit-keywords", unitName] as const
    - Export function useUnitKeywords(unitName: string | undefined) returning useQuery with:
      - queryKey: unitName !== undefined ? UNIT_KEYWORDS_KEY(unitName) : (["unit-keywords"] as const)
      - queryFn: returns getUnitKeywords(unitName) when defined, else Promise.resolve({ isCharacter: false, isEpicHero: false })
      - enabled: unitName !== undefined
      - staleTime: Infinity (keyword data only changes on rules.db re-sync)
    - Import getUnitKeywords and UnitKeywordStatus from "@/db/queries/datasheets"
    - Import useQuery from "@tanstack/react-query"
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript build passes with no errors. Both files exist and export the documented identifiers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend computeListHealthStats with enhancementTotal + write tests</name>
  <files>src/lib/computeUnitWarnings.ts, tests/army-list/computeListHealthStats.test.ts</files>
  <read_first>
    - src/lib/computeUnitWarnings.ts — read the full file, especially computeListHealthStats signature and the totalPoints / pointsExceeded lines (lines 106-146)
    - tests/army-list/armyListEnhancements.test.ts — test file structure, import style, fixture pattern to replicate
    - src/types/armyList.ts — ArmyListUnitRow interface (need effective_points field shape for test fixtures)
    - .planning/phases/91-enhancement-assignment/91-PATTERNS.md — Pattern Map section for computeUnitWarnings.ts and test patterns
  </read_first>
  <behavior>
    - computeListHealthStats(units850pts, 1000, "synced", 60) → totalPoints === 910
    - computeListHealthStats(units950pts, 1000, "synced", 60) → pointsExceeded === true (950+60=1010 > 1000)
    - computeListHealthStats(units500pts, 1000, "synced") → totalPoints === 500, pointsExceeded === false (backward compat: no 4th arg)
    - computeListHealthStats(units500pts, 1000, "synced", 0) → totalPoints === 500 (explicit zero same as omit)
    - battleReadyPct uses unitPoints only for the painted fraction denominator (enhancement points don't affect painted calculation)
  </behavior>
  <action>
    In src/lib/computeUnitWarnings.ts — modify computeListHealthStats:
    - Add optional 4th parameter: enhancementTotal = 0 (number, default 0 — makes all existing callers backward-compatible)
    - Rename the existing reduce to unitPoints: const unitPoints = units.reduce((sum, u) => sum + u.effective_points, 0)
    - Set const totalPoints = unitPoints + enhancementTotal
    - The battleReadyPct calculation must continue to use unitPoints as the denominator base: paintedPoints / totalPoints where totalPoints now includes enhancementTotal — this is correct per D-07 (full combined total is the denominator)
    - pointsExceeded already uses totalPoints which now includes enhancementTotal — no other change needed
    - WarningContext passed to computeListWarnings / computeUnitWarnings uses the new combined totalPoints — no change needed

    Create tests/army-list/computeListHealthStats.test.ts:
    - Import computeListHealthStats from "@/lib/computeUnitWarnings"
    - Import ArmyListUnitRow from "@/types/armyList"
    - Build a minimal unit fixture factory: makeUnit(pts: number): ArmyListUnitRow with effective_points = pts, status_painting = "Not Started", all other fields as empty/zero stubs to satisfy TypeScript
    - Test group "computeListHealthStats — enhancement total (ENH-03)":
      - "includes enhancementTotal in totalPoints": units [850pts], limit 1000, freshness "synced", enhancementTotal 60 → stats.totalPoints === 910
      - "triggers pointsExceeded when combined total exceeds limit": units [950pts], limit 1000, "synced", 60 → stats.pointsExceeded === true
      - "does NOT trigger pointsExceeded when combined is at limit": units [940pts], limit 1000, "synced", 60 → stats.pointsExceeded === false
      - "backward compat: 3-arg call returns unit points only": units [500pts], limit 1000, "synced" → stats.totalPoints === 500
      - "enhancementTotal = 0 is same as omit": units [500pts], limit 1000, "synced", 0 → stats.totalPoints === 500
  </action>
  <verify>
    <automated>pnpm test -- tests/army-list/computeListHealthStats.test.ts</automated>
  </verify>
  <done>All 5 tests pass. pnpm build produces no TypeScript errors. Existing callers of computeListHealthStats (ArmyListSummaryBar, any test files) continue to work with the 3-arg form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rules.db read | Read-only cross-DB query via getRulesDb(); parameterized with $1 — no user-controlled SQL construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Information Disclosure | getUnitKeywords try/catch | accept | Safe defaults returned on error; no sensitive data exposed — rules.db is read-only public game data |
| T-91-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- pnpm test -- tests/army-list/computeListHealthStats.test.ts passes (5 tests green)
- pnpm build produces zero TypeScript errors
- src/hooks/useUnitKeywords.ts exports useUnitKeywords and UNIT_KEYWORDS_KEY
- src/db/queries/datasheets.ts exports getUnitKeywords and UnitKeywordStatus
- computeListHealthStats signature is now: (units, pointsLimit, freshness, enhancementTotal = 0)
</verification>

<success_criteria>
- getUnitKeywords is callable with a unit name and returns { isCharacter, isEpicHero }
- getUnitKeywords swallows all errors and returns { false, false } when rules.db is unavailable
- useUnitKeywords wraps it with staleTime: Infinity and enabled guard
- computeListHealthStats 4th param defaults to 0; existing 3-arg callers compile and run identically
- 5 new tests all pass; full test suite remains green
</success_criteria>

<output>
Create `.planning/phases/91-enhancement-assignment/91-01-SUMMARY.md` when done
</output>

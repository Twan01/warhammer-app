---
phase: 136-code-honesty-decomposition
plan: 04
type: execute
wave: 2
depends_on: ["136-01"]
files_modified:
  - src/hooks/useEnhancements.ts
  - src/hooks/useBsdataFaction.ts
  - src/hooks/useArmyListSnapshots.ts
  - src/hooks/useRecipes.ts
  - src/hooks/useUnits.ts
  - src/hooks/useArmyLists.ts
  - src/features/rules-hub/EnhancementsList.tsx
  - src/features/rules-hub/DatasheetPointsTab.tsx
  - src/features/dashboard/DashboardPage.tsx
  - src/features/army-lists/SnapshotCompareDialog.tsx
  - src/features/units/UnitDeleteDialog.tsx
  - tests/army-list/UnitDeleteDialog.test.tsx
autonomous: true
requirements: [HON-10]

must_haves:
  truths:
    - "The 5 genuine render-path bypasses (EnhancementsList, DatasheetPointsTab x3, DashboardPage x2, SnapshotCompareDialog x2, UnitDeleteDialog x1) call named React Query hooks, not query functions inline"
    - "Removing or adding a unit to an army list invalidates the unit-army-lists cache so UnitDeleteDialog membership is never stale"
    - "No React Query hook is called inside a .map() or loop"
    - "The 5 justified exclusions (imperative event-handler one-shots / mutations) remain direct calls and are documented"
  artifacts:
    - path: "src/hooks/useEnhancements.ts"
      provides: "useEnhancementsByFaction + ENHANCEMENTS_BY_FACTION_KEY"
      contains: "export function useEnhancementsByFaction"
    - path: "src/hooks/useBsdataFaction.ts"
      provides: "useModelCountsByFaction / useLoadoutOptionsByFaction / useLeaderTargetsByFaction"
      contains: "export function useModelCountsByFaction"
  key_links:
    - from: "src/features/units/UnitDeleteDialog.tsx"
      to: "src/hooks/useUnits.ts"
      via: "useUnitArmyLists hook"
      pattern: "useUnitArmyLists\\("
    - from: "src/hooks/useArmyLists.ts"
      to: "unit-army-lists cache key"
      via: "invalidateQueries on add/remove onSuccess"
      pattern: "invalidateQueries\\([\\s\\S]*unit-army-lists"
---

<objective>
Route the 5 genuine render-path query bypasses through named React Query hooks (each with a `*_KEY` factory), restoring cache and invalidation guarantees, and close the `useUnitArmyLists` invalidation symmetry gap — with no hook-in-loop or N+1 regression (HON-10, per D-07/D-08/D-09).

Purpose: Restore the PROJECT.md rule "components only call hooks, never query functions directly" for the genuine render-path reads, while honestly leaving imperative event-handler one-shots as direct calls.
Output: 2 new hook files + 3 extended hook files + 5 re-pointed consumers + a symmetry fix + a stale-cache regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-code-honesty-decomposition/136-CONTEXT.md
@.planning/phases/136-code-honesty-decomposition/136-RESEARCH.md
@.planning/phases/136-code-honesty-decomposition/136-PATTERNS.md

<interfaces>
<!-- New/extended hooks (shapes from PATTERNS.md HON-10 sections): -->
useEnhancements.ts (NEW): ENHANCEMENTS_BY_FACTION_KEY(factionId), useEnhancementsByFaction(factionId) — staleTime/gcTime Infinity (BSData read-only).
useBsdataFaction.ts (NEW): MODEL_COUNTS_KEY/LOADOUT_OPTIONS_KEY/LEADER_TARGETS_KEY + useModelCountsByFaction/useLoadoutOptionsByFaction/useLeaderTargetsByFaction — staleTime/gcTime Infinity, enabled on factionId.
useArmyListSnapshots.ts (EXTEND): SNAPSHOT_DATA_KEY(id), useSnapshotData(id, enabled). Add getSnapshotData to imports.
useRecipes.ts (EXTEND): RECIPE_NAMES_BY_UNIT_KEY(ids), useRecipeNamesByUnitIds(ids). useRecipe already exists — reuse for getRecipeById. Existing recipe mutations already invalidate ["recipes","by-unit"] prefix.
useUnits.ts (EXTEND): UNIT_ARMY_LISTS_KEY(unitId), useUnitArmyLists(unitId, enabled). Add getArmyListsByUnitId to imports.
useArmyLists.ts (SYMMETRY FIX): add qc.invalidateQueries({ queryKey: ["unit-army-lists"] }) to useAddUnitToList.onSuccess AND useRemoveUnitFromList.onSuccess.

<!-- The 5 JUSTIFIED EXCLUSIONS (leave as direct calls — do NOT wrap): -->
- SnapshotHistorySheet.handleExportJson (on-demand event handler)
- RecipeFormSheet.saveRecipeGraph (a mutation/write, not a read)
- DataManagementTab getAppSettings/upsertAppSetting (imperative settings I/O in handlers)
- PlaybookTab getUdbUnitDetail/linkUdbUnit (imperative one-shots in handlePickerSelect)
- UnitDeleteDialog photo cleanup getPhotoFilenamesByUnit/getPhotosByUnit/deleteUnitPhoto (imperative cleanup in handleConfirm)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the BSData read-only hooks and re-point EnhancementsList + DatasheetPointsTab (3 queries)</name>
  <files>src/hooks/useEnhancements.ts, src/hooks/useBsdataFaction.ts, src/features/rules-hub/EnhancementsList.tsx, src/features/rules-hub/DatasheetPointsTab.tsx</files>
  <read_first>
    - src/features/rules-hub/EnhancementsList.tsx (inline useQuery wrapping getEnhancementsByFaction, lines ~8–13)
    - src/features/rules-hub/DatasheetPointsTab.tsx (3 inline useQuery calls: getModelCountsByFaction, getLoadoutOptionsByFaction, getLeaderTargetsByFaction — post Plan-01 state)
    - src/hooks/useArmyListSnapshots.ts (the KEY-factory + useQuery + enabled-guard pattern to copy)
    - .planning/phases/136-code-honesty-decomposition/136-PATTERNS.md §"HON-10: useEnhancements.ts", §"HON-10: DatasheetPointsTab.tsx (3 hook bypasses)", §"Shared Patterns: BSData read-only pattern"
    - CLAUDE.md §"React Query hooks" (one hook file per entity, ENTITY_KEY + useEntity)
  </read_first>
  <action>NOTE: this plan depends on Plan 01 (HON-08) because that plan already removed the shadow WeaponTable from `DatasheetPointsTab.tsx`; do this work on top of those edits. (1) Create `src/hooks/useEnhancements.ts` with `ENHANCEMENTS_BY_FACTION_KEY = (factionId) => ["enhancements-by-faction", factionId] as const` and `useEnhancementsByFaction(factionId)` wrapping `getEnhancementsByFaction` from `@/db/queries/bsdataExtended` with `staleTime: Infinity, gcTime: Infinity`. (2) Create `src/hooks/useBsdataFaction.ts` with three KEY factories (`MODEL_COUNTS_KEY`, `LOADOUT_OPTIONS_KEY`, `LEADER_TARGETS_KEY`) and three hooks (`useModelCountsByFaction`, `useLoadoutOptionsByFaction`, `useLeaderTargetsByFaction`) following the same BSData read-only pattern with an `enabled` guard on `factionId`. (3) In `EnhancementsList.tsx`, replace the inline `useQuery` with `useEnhancementsByFaction(factionId)` and drop the now-unused query/useQuery imports. (4) In `DatasheetPointsTab.tsx`, replace the three inline `useQuery` calls with the three new hooks, preserving the same destructured data/isLoading shape. These are canonical read-only tables — no mutation/invalidation symmetry concern.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `src/hooks/useEnhancements.ts` (`export function useEnhancementsByFaction`), `src/hooks/useBsdataFaction.ts` (`export function useModelCountsByFaction` + the other two).
    - `grep -c "useQuery" src/features/rules-hub/EnhancementsList.tsx` == 0 (now uses the named hook).
    - `grep -c "useQuery" src/features/rules-hub/DatasheetPointsTab.tsx` == 0 (3 inline calls replaced).
    - `grep -c "from \"@tanstack/react-query\"" src/features/rules-hub/EnhancementsList.tsx` == 0 (unused import removed).
    - `pnpm build` passes.
  </acceptance_criteria>
  <done>EnhancementsList and DatasheetPointsTab read BSData through named hooks; two new hook files follow the project convention.</done>
</task>

<task type="auto">
  <name>Task 2: Extend useRecipes + useArmyListSnapshots hooks and re-point DashboardPage + SnapshotCompareDialog</name>
  <files>src/hooks/useRecipes.ts, src/hooks/useArmyListSnapshots.ts, src/features/dashboard/DashboardPage.tsx, src/features/army-lists/SnapshotCompareDialog.tsx</files>
  <read_first>
    - src/features/dashboard/DashboardPage.tsx (2 inline useQuery calls: getRecipeNamesByUnitIds + getRecipeById, lines ~93–115)
    - src/features/army-lists/SnapshotCompareDialog.tsx (2 inline useQuery calls keyed ["snapshot-data", id])
    - src/hooks/useRecipes.ts (existing useRecipe at ~25–31; mutations already invalidate ["recipes","by-unit"] prefix at lines ~40/53/63/83)
    - src/hooks/useArmyListSnapshots.ts (existing useSnapshotsByList ~42–48 enabled-guard pattern; import block ~line 16)
    - .planning/phases/136-code-honesty-decomposition/136-PATTERNS.md §"HON-10: useRecipes.ts", §"HON-10: useArmyListSnapshots.ts"
  </read_first>
  <action>(1) In `useRecipes.ts`, add `RECIPE_NAMES_BY_UNIT_KEY = (ids) => ["recipes", "by-unit", ...ids] as const` and `useRecipeNamesByUnitIds(ids)` (enabled on `ids.length > 0`, disabled-key fallback), importing `getRecipeNamesByUnitIds` from `@/db/queries/recipes`. Confirm (do not add) that the existing recipe mutations invalidate the `["recipes","by-unit"]` prefix — they do (symmetry already satisfied). (2) In `useArmyListSnapshots.ts`, add `SNAPSHOT_DATA_KEY = (id) => ["snapshot-data", id] as const` and `useSnapshotData(id, enabled)` (enabled on `id !== null && enabled`), importing `getSnapshotData`. (3) In `DashboardPage.tsx`, replace the inline `getRecipeNamesByUnitIds` query with `useRecipeNamesByUnitIds(focusUnitId ? [focusUnitId] : [])` and replace the inline `getRecipeById` query with the EXISTING `useRecipe(primaryAssignment?.recipe_id)` (do not create a new hook — D-07 reuse rule). (4) In `SnapshotCompareDialog.tsx`, replace the two inline `useQuery({ queryKey: ["snapshot-data", id] ... })` calls with `useSnapshotData(idA, open)` and `useSnapshotData(idB, open)`. No hook inside a loop (D-08).</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function useRecipeNamesByUnitIds" src/hooks/useRecipes.ts` == 1; `grep -c "export function useSnapshotData" src/hooks/useArmyListSnapshots.ts` == 1.
    - `grep -c "useQuery" src/features/dashboard/DashboardPage.tsx` == 0 (both inline calls replaced; getRecipeById uses existing useRecipe).
    - `grep -c "useQuery" src/features/army-lists/SnapshotCompareDialog.tsx` == 0 (both replaced with useSnapshotData).
    - `grep -c "useRecipe(" src/features/dashboard/DashboardPage.tsx` >= 1 (reuses existing hook).
    - `pnpm build` passes; `pnpm test -- tests/dashboard/ tests/army-list/` green.
  </acceptance_criteria>
  <done>DashboardPage and SnapshotCompareDialog read through named hooks; getRecipeById reuses the existing useRecipe; recipe invalidation symmetry confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add useUnitArmyLists hook, fix the add/remove invalidation symmetry, and re-point UnitDeleteDialog with a stale-cache regression test</name>
  <files>src/hooks/useUnits.ts, src/hooks/useArmyLists.ts, src/features/units/UnitDeleteDialog.tsx, tests/army-list/UnitDeleteDialog.test.tsx</files>
  <read_first>
    - src/features/units/UnitDeleteDialog.tsx (inline useQuery keyed ["unit-army-lists", unit?.id] wrapping getArmyListsByUnitId; note the SEPARATE photo-cleanup direct calls in handleConfirm — those stay)
    - src/hooks/useUnits.ts (useUnit nullable-id enabled-guard pattern ~25–31; import block ~line 3)
    - src/hooks/useArmyLists.ts (useAddUnitToList.onSuccess ~line 161, useRemoveUnitFromList.onSuccess ~line 179, invalidateListWithReadiness helper, ["army-list-readiness"] prefix invalidation ~150)
    - tests/army-list/UnitDeleteDialog.test.tsx (existing test to extend)
    - tests/army-list/armyListHookInvalidations.test.ts (the invalidation-test precedent in this codebase)
    - .planning/phases/136-code-honesty-decomposition/136-PATTERNS.md §"HON-10: useUnits.ts", §"HON-10: useArmyLists.ts (symmetry fix)"
    - .planning/phases/136-code-honesty-decomposition/136-RESEARCH.md §"Pitfall C: useUnitArmyLists invalidation gap"
  </read_first>
  <behavior>
    - Test (symmetry): after `useRemoveUnitFromList` mutation succeeds, the `["unit-army-lists"]` query key is invalidated (membership cache cleared).
    - Test (symmetry): after `useAddUnitToList` mutation succeeds, the `["unit-army-lists"]` query key is invalidated.
    - Test (consumer): `UnitDeleteDialog` rendered with `useUnitArmyLists` mocked to return one or more lists shows the in-use warning state; mocked to return an empty list shows the safe-delete state.
  </behavior>
  <action>(1) In `useUnits.ts`, add `UNIT_ARMY_LISTS_KEY = (unitId) => ["unit-army-lists", unitId] as const` and `useUnitArmyLists(unitId, enabled)` (nullable-id enabled-guard pattern, disabled-key fallback), importing `getArmyListsByUnitId` from `@/db/queries/units`. (2) In `useArmyLists.ts`, add `qc.invalidateQueries({ queryKey: ["unit-army-lists"] })` to BOTH `useAddUnitToList.onSuccess` and `useRemoveUnitFromList.onSuccess` (string-prefix invalidation, same idiom as the existing `["army-list-readiness"]` prefix call — no key import needed). This closes the Pitfall-C stale-membership gap. (3) In `UnitDeleteDialog.tsx`, replace the inline `useQuery({ queryKey: ["unit-army-lists", ...] })` with `useUnitArmyLists(unit?.id ?? null, open)`. Leave the photo-cleanup `getPhotoFilenamesByUnit`/`getPhotosByUnit`/`deleteUnitPhoto` calls in `handleConfirm` as direct imperative calls (D-09 justified exclusion — imperative cleanup, no caching value). (4) Add the symmetry + consumer tests above to `tests/army-list/UnitDeleteDialog.test.tsx` (or co-locate the invalidation assertion in `armyListHookInvalidations.test.ts` following its precedent).</action>
  <verify>
    <automated>pnpm test -- tests/army-list/UnitDeleteDialog.test.tsx tests/army-list/armyListHookInvalidations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function useUnitArmyLists" src/hooks/useUnits.ts` == 1.
    - `grep -c "unit-army-lists" src/hooks/useArmyLists.ts` >= 2 (invalidation added to both add and remove onSuccess).
    - `grep -c "useQuery" src/features/units/UnitDeleteDialog.tsx` == 0 (membership read now via hook).
    - `grep -c "deleteUnitPhoto\|getPhotosByUnit" src/features/units/UnitDeleteDialog.tsx` >= 1 (justified-exclusion direct calls remain in handleConfirm).
    - The symmetry tests assert `["unit-army-lists"]` invalidation fires on add and remove; consumer test covers warning vs safe-delete states.
    - `pnpm build` passes; the targeted tests pass.
  </acceptance_criteria>
  <done>UnitDeleteDialog reads membership through useUnitArmyLists; add/remove mutations invalidate the membership cache (no stale count); photo cleanup correctly stays imperative; symmetry locked by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Hooks-layer hygiene refactor on an existing local single-user surface. No new input handling, no network, no new DB writes — only React Query cache routing of existing reads + one extra cache invalidation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-10-01 | Tampering (stale cache → wrong UI state) | UnitDeleteDialog membership | mitigate | The symmetry fix invalidates `["unit-army-lists"]` on add/remove (Pitfall C); a regression test (Task 3) asserts the invalidation fires, preventing a stale in-use warning that could mislead a delete decision. |
| T-136-10-02 | Tampering (N+1 / hook-in-loop regression) | all re-pointed consumers | mitigate | D-08 forbids hooks in `.map()`/loops; per-row data uses page-level memo Maps; `grep` gates confirm inline `useQuery` is removed without introducing per-item hooks; `pnpm build` enforces rules-of-hooks via TS. |

No high-severity threats introduced — cache-correctness refactor only; the 5 justified exclusions are imperative one-shots/mutations with no caching value and are explicitly left as direct calls.
</threat_model>

<verification>
- `grep -rn "useQuery" src/features/rules-hub/EnhancementsList.tsx src/features/rules-hub/DatasheetPointsTab.tsx src/features/dashboard/DashboardPage.tsx src/features/army-lists/SnapshotCompareDialog.tsx src/features/units/UnitDeleteDialog.tsx` returns 0 (all 5 genuine bypasses routed through hooks).
- `grep -c "unit-army-lists" src/hooks/useArmyLists.ts` >= 2 (symmetry fix present).
- `pnpm build` green; `pnpm test` green.
- Manual: remove a unit from an army list, open UnitDeleteDialog for that unit, confirm membership count is current (not stale).
</verification>

<success_criteria>
HON-10 met: the 5 genuine render-path bypasses are routed through named React Query hooks with `*_KEY` factories, cache + invalidation guarantees are restored (incl. the add/remove symmetry fix), no hook runs in a loop, and the 5 justified exclusions remain documented direct calls.
</success_criteria>

<output>
Create `.planning/phases/136-code-honesty-decomposition/136-04-SUMMARY.md` when done.
</output>

---
phase: 136-code-honesty-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/army-lists/ArmyListDetailPage.tsx
  - src/features/army-lists/ArmyListDetailHeader.tsx
  - src/features/army-lists/ArmyListQuickAdd.tsx
  - src/features/army-lists/useArmyListExport.ts
  - src/features/army-lists/ArmyListUnitTable.tsx
  - src/features/army-lists/ArmyListPortals.tsx
  - tests/army-list/ArmyListDetailPage.decomposition.test.tsx
autonomous: true
requirements: [HON-09]

must_haves:
  truths:
    - "ArmyListDetailPage.tsx orchestrator is under 250 lines"
    - "Each extracted child (Header, QuickAdd, UnitTable, Portals) and the export hook is under 200 lines"
    - "The full army-list detail page renders the same Header, QuickAdd, UnitTable, export actions, and all 9 portals as before extraction — no behavior change"
    - "The 9 portals remain sibling-rendered (never nested) and still driven by the reducer dispatch contract"
  artifacts:
    - path: "src/features/army-lists/ArmyListDetailHeader.tsx"
      provides: "Back-link + PageHeader + Edit/GameDay/Delete actions"
      contains: "export function ArmyListDetailHeader"
    - path: "src/features/army-lists/ArmyListQuickAdd.tsx"
      provides: "Search input + dropdown results list"
      contains: "export function ArmyListQuickAdd"
    - path: "src/features/army-lists/useArmyListExport.ts"
      provides: "Copy/JSON/PDF export handlers"
      contains: "export function useArmyListExport"
    - path: "src/features/army-lists/ArmyListUnitTable.tsx"
      provides: "DndContext + categorized unit rows + SortableUnitRow"
      contains: "export function ArmyListUnitTable"
    - path: "src/features/army-lists/ArmyListPortals.tsx"
      provides: "All 9 sibling Sheet/Dialog portals"
      contains: "export function ArmyListPortals"
  key_links:
    - from: "src/features/army-lists/ArmyListDetailPage.tsx"
      to: "src/features/army-lists/ArmyListPortals.tsx"
      via: "state + dispatch props bundle"
      pattern: "ArmyListPortals[\\s\\S]*dispatch"
    - from: "src/features/army-lists/ArmyListDetailPage.tsx"
      to: "src/features/army-lists/useArmyListExport.ts"
      via: "hook call returning export handlers"
      pattern: "useArmyListExport\\("
---

<objective>
Decompose the 786-line `ArmyListDetailPage.tsx` into the five research-specified seams — `ArmyListDetailHeader`, `ArmyListQuickAdd`, `useArmyListExport` hook, `ArmyListUnitTable`, `ArmyListPortals` — keeping the orchestrator under ~250 lines and each child under ~200, with ZERO behavior change (HON-09, per D-04/D-05/D-06).

Purpose: Bring the army-list code within the project's file-size conventions (PlaybookTab/UnitSheet precedent) and produce the clean `ArmyListUnitTable` that Phase 137 will later repoint — without building any new behavior here.
Output: Five new focused files + a slimmed orchestrator + a behavior-preservation integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-code-honesty-decomposition/136-CONTEXT.md
@.planning/phases/136-code-honesty-decomposition/136-RESEARCH.md
@.planning/phases/136-code-honesty-decomposition/136-PATTERNS.md
@.planning/phases/136-code-honesty-decomposition/136-UI-SPEC.md

<interfaces>
<!-- Props contracts for each extracted child — sourced from PATTERNS.md HON-09 sections. -->
<!-- Reducer contract (preserve verbatim dispatch calls in Portals): -->
From src/features/army-lists/armyListDetailReducer.ts:
```typescript
export type DetailPortalState = { /* portal open flags + selected ids */ };
export type DetailPortalAction = { type: string; /* ... */ };
// dispatch is React.Dispatch<DetailPortalAction>
```

ArmyListDetailHeaderProps: { list, faction, onEdit: () => void, onGameDay: () => void, onDelete: () => void }
ArmyListQuickAddProps: { quickAddSearch, setQuickAddSearch, quickAddResults, onAdd: (unitId: number) => void, onOpenUnitPicker: () => void, onOpenDatasheetBrowser: () => void }
useArmyListExport({ list, units, listEnhancements, faction, listWargear, locale }) => { handleCopyToClipboard, handleSaveJson, handleSavePdf }
ArmyListUnitTableProps: { unitsByCategory, collapsedCategories, onToggleCategory, leaderNameMap, leaderTargets, listEnhancements, listId, sensors, onDragEnd, onRemove, onConfigure, onEnhance, onAttachLeader, onToggleWarlord } (SortableUnitRow moves into this file)
ArmyListPortalsProps: { state, dispatch, list, listId, totalPoints, units, listEnhancements, factionName, loadoutUnit, enhancementUnit, leaderUnit, handleDeleteClose, handleDeleted }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 behavior-preservation integration test for ArmyListDetailPage</name>
  <files>tests/army-list/ArmyListDetailPage.decomposition.test.tsx</files>
  <read_first>
    - src/features/army-lists/ArmyListDetailPage.tsx (the 786-line file being decomposed — full read)
    - tests/army-list/UnitDeleteDialog.test.tsx (existing army-list test patterns: hook mocking, Tauri mocks)
    - tests/units/UnitSheet.decomposition.test.tsx (the decomposition-test precedent for this codebase)
    - .planning/phases/136-code-honesty-decomposition/136-VALIDATION.md §"Wave 0 Requirements" + §"Per-Task Verification Map" (HON-09 row)
    - .planning/phases/136-code-honesty-decomposition/136-UI-SPEC.md §"Render-identical checklist (HON-09)"
  </read_first>
  <action>Create `tests/army-list/ArmyListDetailPage.decomposition.test.tsx` BEFORE extracting anything — this is the behavior-preservation guard (Wave 0 per VALIDATION.md). Mock the data hooks (`useArmyListWithUnits`, `useArmyList`, faction/leader-target hooks) with `vi.mock` and mock Tauri plugin APIs (clipboard, dialog, fs, core invoke) per the existing army-list test setup. Assert that rendering `ArmyListDetailPage` with a populated mocked list shows: the header (list name + the 3 action buttons), the quick-add search input, the unit table with at least one category group and a unit row, the export toolbar buttons (Copy/JSON/PDF), and that toggling each portal-open reducer flag renders the corresponding Sheet/Dialog. This test must pass against the CURRENT undecomposed file first, then continue to pass unchanged after every extraction task. Do not assert on internal file structure — assert on rendered output only.</action>
  <verify>
    <automated>pnpm test -- tests/army-list/ArmyListDetailPage.decomposition.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `tests/army-list/ArmyListDetailPage.decomposition.test.tsx` exists and passes against the current (pre-extraction) `ArmyListDetailPage.tsx`.
    - The test asserts presence of: header + 3 action buttons, quick-add input, unit table with a category group + unit row, export buttons, and at least the Edit/Delete/SnapshotCompare portals when their reducer flags are set.
    - `pnpm build` passes.
  </acceptance_criteria>
  <done>A render-level behavior guard exists and is green against the undecomposed page; it becomes the invariant for all subsequent extraction commits.</done>
</task>

<task type="auto">
  <name>Task 2: Extract ArmyListDetailHeader, ArmyListQuickAdd, and useArmyListExport (mechanical block-moves)</name>
  <files>src/features/army-lists/ArmyListDetailHeader.tsx, src/features/army-lists/ArmyListQuickAdd.tsx, src/features/army-lists/useArmyListExport.ts, src/features/army-lists/ArmyListDetailPage.tsx</files>
  <read_first>
    - src/features/army-lists/ArmyListDetailPage.tsx (source blocks: header ~471–516, quick-add ~520–558, export handlers ~355–424)
    - .planning/phases/136-code-honesty-decomposition/136-PATTERNS.md §"HON-09: ArmyListDetailHeader.tsx", §"ArmyListQuickAdd.tsx", §"useArmyListExport.ts" (verbatim props contracts + import lists + line ranges)
    - src/features/units/PlaybookTab.tsx (the sub-component props-interface precedent)
    - .planning/phases/136-code-honesty-decomposition/136-UI-SPEC.md §"Child component visual contracts" (classes verbatim, no spacing/typography changes)
  </read_first>
  <action>Mechanical block-moves only — extract, do not rewrite (D-06). (1) Create `ArmyListDetailHeader.tsx` from lines ~471–516: move the back-link + `<PageHeader>` (faction badge + Edit/GameDay/Delete actions) verbatim; replace the inline `dispatch({type:"OPEN_EDIT"})` / `dispatch({type:"OPEN_DELETE"})` / navigate calls with the `onEdit`/`onDelete`/`onGameDay` callback props. (2) Create `ArmyListQuickAdd.tsx` from lines ~520–558: move the search input + dropdown results list; accept `quickAddSearch`, `setQuickAddSearch`, `quickAddResults`, `onAdd`, `onOpenUnitPicker`, `onOpenDatasheetBrowser` as props. (3) Create `useArmyListExport.ts` from lines ~355–424: move the three `useCallback` handlers (`handleCopyToClipboard`, `handleSaveJson`, `handleSavePdf`) and their Tauri/exportArmyList imports; the hook takes `{ list, units, listEnhancements, faction, listWargear, locale }` and returns the three handlers. In the orchestrator, replace the moved blocks with `<ArmyListDetailHeader .../>`, `<ArmyListQuickAdd .../>`, and `const { handleCopyToClipboard, handleSaveJson, handleSavePdf } = useArmyListExport({...})`. Copy all Tailwind classes verbatim (UI-SPEC). Land as separate revertable commits (Header, QuickAdd, export hook). Re-run the Task-1 test after each.</action>
  <verify>
    <automated>pnpm test -- tests/army-list/ArmyListDetailPage.decomposition.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `ArmyListDetailHeader.tsx` (`export function ArmyListDetailHeader`), `ArmyListQuickAdd.tsx` (`export function ArmyListQuickAdd`), `useArmyListExport.ts` (`export function useArmyListExport`).
    - `wc -l` each new file < 200.
    - `grep -n "useArmyListExport(" src/features/army-lists/ArmyListDetailPage.tsx` matches (orchestrator calls the hook).
    - Task-1 test still passes (no rendered-output change).
    - `pnpm build` passes.
  </acceptance_criteria>
  <done>Header, QuickAdd, and the export hook are extracted as verbatim block-moves; the page renders identically; orchestrator consumes them via props/hook.</done>
</task>

<task type="auto">
  <name>Task 3: Extract ArmyListUnitTable and ArmyListPortals, then trim the orchestrator under 250 lines</name>
  <files>src/features/army-lists/ArmyListUnitTable.tsx, src/features/army-lists/ArmyListPortals.tsx, src/features/army-lists/ArmyListDetailPage.tsx</files>
  <read_first>
    - src/features/army-lists/ArmyListDetailPage.tsx (post-Task-2 state; source blocks: SortableUnitRow ~91–133, DndContext table ~575–653, portals ~712–784, orchestrator remainder)
    - src/features/army-lists/armyListDetailReducer.ts (DetailPortalState/DetailPortalAction — the dispatch contract to preserve)
    - .planning/phases/136-code-honesty-decomposition/136-PATTERNS.md §"HON-09: ArmyListUnitTable.tsx", §"ArmyListPortals.tsx", §"ArmyListDetailPage.tsx (orchestrator)" (props contracts, the state+dispatch bundle decision, orchestrator-remainder list)
    - .planning/phases/136-code-honesty-decomposition/136-RESEARCH.md §"Pitfall B: ArmyListPortals dispatch coupling"
    - .planning/phases/136-code-honesty-decomposition/136-UI-SPEC.md §"Never-nested portal rule (preserved)"
  </read_first>
  <action>Mechanical block-moves only (D-06). (1) Create `ArmyListUnitTable.tsx`: move `SortableUnitRow` (~91–133) AND the DndContext + Table + categorized-row iteration block (~575–653) into it; accept the props bundle from the interfaces block (`unitsByCategory`, `collapsedCategories`, `onToggleCategory`, `leaderNameMap`, `leaderTargets`, `listEnhancements`, `listId`, `sensors`, `onDragEnd`, `onRemove`, `onConfigure`, `onEnhance`, `onAttachLeader`, `onToggleWarlord`). (2) Create `ArmyListPortals.tsx`: move all 9 sibling Sheet/Dialog components (~712–784) verbatim; pass `state` (the destructured `DetailPortalState`) + `dispatch` as a props bundle so the `dispatch({type:"..."})` calls stay byte-identical (Pitfall B / D-04 reducer contract); also pass `list`, `listId`, `totalPoints`, `units`, `listEnhancements`, `factionName`, `loadoutUnit`, `enhancementUnit`, `leaderUnit`, `handleDeleteClose`, `handleDeleted`. Keep the portals as siblings at the orchestrator JSX root (never-nested rule). (3) Trim the orchestrator: it retains imports, all data-hook calls, `useReducer` + state destructuring, the derived memos (`groupedUnits`, `unitsByCategory`, `leaderNameMap`, `totalPoints`, `quickAddResults`, portal-derived `loadoutUnit`/`enhancementUnit`/`leaderUnit`/`factionName`), local state, effects, DnD sensors, the remaining handlers, loading/empty renders, the Detachment/Reminders/notes sections, and the JSX composing the five children. Land UnitTable and Portals as separate revertable commits. Do NOT fix IN-011/IN-013/IN-015 (UI-SPEC non-goals).</action>
  <verify>
    <automated>pnpm test -- tests/army-list/ArmyListDetailPage.decomposition.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `ArmyListUnitTable.tsx` (`export function ArmyListUnitTable`, contains `SortableUnitRow`), `ArmyListPortals.tsx` (`export function ArmyListPortals`).
    - `wc -l src/features/army-lists/ArmyListUnitTable.tsx` < 200 and `wc -l src/features/army-lists/ArmyListPortals.tsx` < 200.
    - `wc -l src/features/army-lists/ArmyListDetailPage.tsx` < 250.
    - `grep -c "dispatch" src/features/army-lists/ArmyListPortals.tsx` > 0 (dispatch calls preserved verbatim; state+dispatch passed as props).
    - `grep -c "function SortableUnitRow\|SortableUnitRow" src/features/army-lists/ArmyListDetailPage.tsx` returns 0 (moved out).
    - Task-1 decomposition test still passes; `pnpm build` passes.
    - Full army-list test suite green: `pnpm test -- tests/army-list/`.
  </acceptance_criteria>
  <done>UnitTable and Portals are extracted verbatim, the reducer dispatch contract and never-nested portal rule are preserved, and the orchestrator is under 250 lines with all children under 200 — no behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure mechanical component/hook extraction within an existing local single-user surface. No new input handling, no network, no DB schema or write-path change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-09-01 | Tampering (behavior regression) | ArmyListDetailPage + children | mitigate | Wave-0 render-level integration test (Task 1) is the invariant across all extraction commits; each extraction is a separate revertable commit; existing `tests/army-list/` suite must stay green. |
| T-136-09-02 | Repudiation (export integrity) | useArmyListExport | accept | Export handlers are moved verbatim (no logic change); existing behavior preserved. Low risk — local file/clipboard writes only, no auth or audit surface. |

No high-severity threats introduced — behavior-preserving decomposition only.
</threat_model>

<verification>
- `wc -l src/features/army-lists/ArmyListDetailPage.tsx` < 250; each new child < 200.
- `pnpm test -- tests/army-list/` green (incl. the new decomposition test).
- `pnpm build` green.
- Manual: full army-list detail page render-identical checklist (UI-SPEC §"Render-identical checklist (HON-09)") in EN and FR.
</verification>

<success_criteria>
HON-09 met: `ArmyListDetailPage` is decomposed into the five research-specified focused files, each within the project's file-size conventions (orchestrator < 250, children < 200), with no behavior regression — verified by the behavior-preservation test and the existing army-list suite.
</success_criteria>

<output>
Create `.planning/phases/136-code-honesty-decomposition/136-02-SUMMARY.md` when done.
</output>

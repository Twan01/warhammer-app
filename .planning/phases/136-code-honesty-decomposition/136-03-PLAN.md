---
phase: 136-code-honesty-decomposition
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/049_drop_promoted_to_reminder.sql
  - src-tauri/src/lib.rs
  - src/types/battleLog.ts
autonomous: true
requirements: [HON-11]

must_haves:
  truths:
    - "The promoted_to_reminder column no longer exists on battle_logs after migrations run"
    - "No source file in src/ references promoted_to_reminder"
    - "The migration parity gate (pnpm check:version) passes: disk migration count == lib.rs Migration{} count == data-layer list length, and no CR bytes in any migration"
    - "Migration 027 is unchanged (never edited)"
    - "Context decisions covered — D-10: remove the vestigial column (genuinely dead, zero usage); D-11: new migration 049 DROP COLUMN, never edit 027, remove type field + simplify CreateBattleLogInput Omit; D-12: atomic change keeping the parity gate green (lib.rs registration + LF line endings; db-helpers/check-version auto-derive)"
  artifacts:
    - path: "src-tauri/migrations/049_drop_promoted_to_reminder.sql"
      provides: "DDL drop of the vestigial column (LF line endings)"
      contains: "ALTER TABLE battle_logs DROP COLUMN promoted_to_reminder"
    - path: "src-tauri/src/lib.rs"
      provides: "Migration block version 49 registration"
      contains: "version: 49"
    - path: "src/types/battleLog.ts"
      provides: "BattleLog type without promoted_to_reminder, simplified CreateBattleLogInput Omit"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/migrations/049_drop_promoted_to_reminder.sql"
      via: "include_str! in Migration block 49"
      pattern: "include_str!\\(\"../migrations/049_drop_promoted_to_reminder.sql\"\\)"
---

<objective>
Remove the vestigial `promoted_to_reminder` column from `battle_logs` via a new migration `049`, registered in `lib.rs` and reflected in the `battleLog.ts` types — as a single atomic change that keeps the Theme-A release/parity gate green (HON-11, per D-10/D-11/D-12).

Purpose: The "surface forgotten rules as reminders" feature was never built; the column has zero read/write usage. Removal is the honest choice for the "Bulletproof & Honest" milestone.
Output: A DDL-only LF migration, a 49th lib.rs Migration block, type-file cleanup, and a green parity gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-code-honesty-decomposition/136-CONTEXT.md
@.planning/phases/136-code-honesty-decomposition/136-RESEARCH.md
@.planning/phases/136-code-honesty-decomposition/136-PATTERNS.md

<interfaces>
<!-- Current battleLog.ts state (verified): -->
From src/types/battleLog.ts:
```typescript
// line 34: promoted_to_reminder: number;   <-- DELETE
// line 39: export type CreateBattleLogInput = Omit<BattleLog, "id" | "created_at" | "promoted_to_reminder">;
//          --> becomes: Omit<BattleLog, "id" | "created_at">;
// UpdateBattleLogInput (CreateBattleLogInput & { id: number }) requires no change.
```

<!-- Current parity facts (verified on master): -->
- 48 `.sql` files on disk (001–048). New file makes 49.
- lib.rs has 48 `include_str!("../migrations/...")` Migration blocks; the last is version 48 ("consolidate_factions"). Add a 49th block.
- tests/data-layer/db-helpers.ts auto-derives from disk via readdirSync — NO manual edit.
- scripts/check-version.mjs: leg 2 (disk count == lib.rs count), leg 3 (no CR bytes).

<!-- lib.rs block-48 analog (lines ~289–294): -->
```rust
Migration {
    version: 48,
    description: "consolidate_factions",
    sql: include_str!("../migrations/048_consolidate_factions.sql"),
    kind: MigrationKind::Up,
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 049, register Migration block 49 in lib.rs, and remove the type field — one atomic change</name>
  <files>src-tauri/migrations/049_drop_promoted_to_reminder.sql, src-tauri/src/lib.rs, src/types/battleLog.ts</files>
  <read_first>
    - src-tauri/migrations/047_army_list_unit_wargear.sql (header-comment style analog to match)
    - src-tauri/migrations/027_battle_log_after_action.sql (the column origin — confirm; DO NOT edit)
    - src-tauri/src/lib.rs around the version-48 Migration block (~289–294) (the block-48 analog to copy)
    - src/types/battleLog.ts (line 34 field + line 39 Omit)
    - .planning/phases/136-code-honesty-decomposition/136-PATTERNS.md §"HON-11: 049 migration", §"HON-11: lib.rs", §"HON-11: battleLog.ts" (verbatim content + analogs)
    - .planning/phases/136-code-honesty-decomposition/136-RESEARCH.md §"Pitfall D: lib.rs Migration count drift", §"Pitfall E: CRLF in migration 049"
  </read_first>
  <action>Single atomic change (Pitfall D — file + lib.rs + types together). (1) Using the Write tool ONLY (never a Windows editor — guarantees LF), create `src-tauri/migrations/049_drop_promoted_to_reminder.sql` with a header comment block matching migration 047's style, then the single statement `ALTER TABLE battle_logs DROP COLUMN promoted_to_reminder;`. DDL-only, no BEGIN/COMMIT (the plugin-sql runner wraps each migration in its own transaction). (2) In `src-tauri/src/lib.rs`, copy the version-48 Migration block and add a 49th immediately after it: `version: 49`, `description: "drop_promoted_to_reminder"`, `sql: include_str!("../migrations/049_drop_promoted_to_reminder.sql")`, `kind: MigrationKind::Up`. (3) In `src/types/battleLog.ts`, delete the `promoted_to_reminder: number;` line (~34) and change the `CreateBattleLogInput` Omit (~39) from `Omit<BattleLog, "id" | "created_at" | "promoted_to_reminder">` to `Omit<BattleLog, "id" | "created_at">`. Do NOT edit migration 027. Do NOT manually edit `db-helpers.ts` (auto-derived). Verify LF endings with `git ls-files --eol` after writing.</action>
  <verify>
    <automated>pnpm check:version</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src-tauri/migrations/049_drop_promoted_to_reminder.sql` and `grep -c "ALTER TABLE battle_logs DROP COLUMN promoted_to_reminder" src-tauri/migrations/049_drop_promoted_to_reminder.sql` == 1.
    - `git ls-files --eol src-tauri/migrations/049_drop_promoted_to_reminder.sql` shows `w/lf` (no CRLF).
    - `grep -c "include_str" src-tauri/src/lib.rs` == 49 and `grep -c "version: 49" src-tauri/src/lib.rs` == 1.
    - `grep -rn "promoted_to_reminder" src/` returns 0 results.
    - `git diff --quiet src-tauri/migrations/027_battle_log_after_action.sql` (migration 027 untouched).
    - `pnpm check:version` passes (leg 2 count parity + leg 3 no CR bytes).
    - `pnpm build` passes (battleLog.ts compiles with field removed, no consumer breaks).
  </acceptance_criteria>
  <done>The column-drop migration, lib.rs registration, and type cleanup land as one atomic change; the parity gate is green and migration 027 is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the column is actually dropped by the data-layer migration test</name>
  <files>tests/data-layer/migration-parity.test.ts</files>
  <read_first>
    - tests/data-layer/migration-parity.test.ts (existing parity test — confirm it auto-derives disk migrations and runs them against the in-memory DB)
    - tests/data-layer/db-helpers.ts (readdirSync auto-derivation; HOBBYFORGE_MIGRATIONS, HOBBYFORGE_MIGRATION_COUNT)
    - .planning/phases/136-code-honesty-decomposition/136-RESEARCH.md §"HON-11 Acceptance Tests" (the data-layer column-gone assertion)
  </read_first>
  <action>Run the existing data-layer suite to confirm migration 049 applies cleanly against the in-memory better-sqlite3 DB and the parity test still passes (lib.rs count == disk count == derived list length). If the suite does not already assert the column is gone, add a focused assertion in `tests/data-layer/migration-parity.test.ts` (or the nearest existing data-layer DB test): after running all derived migrations against the in-memory DB, query `PRAGMA table_info(battle_logs)` and assert no column named `promoted_to_reminder` is present. Do not add new test infra — reuse the existing in-memory DB helper from `db-helpers.ts`.</action>
  <verify>
    <automated>pnpm test -- tests/data-layer/</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- tests/data-layer/` passes, including `migration-parity.test.ts`.
    - A data-layer assertion confirms `PRAGMA table_info(battle_logs)` contains no `promoted_to_reminder` column after all migrations run.
    - `pnpm test -- tests/data-layer/migration-parity.test.ts` confirms lib.rs Migration count == disk `.sql` count == derived list length (all 49).
  </acceptance_criteria>
  <done>The data layer proves the column is gone post-migration and the parity invariant holds at 49 migrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration runner → SQLite schema | A DDL `DROP COLUMN` mutates the local single-user `hobbyforge.db` schema at app startup. This is the only state-changing surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-11-01 | Tampering (unintended data loss) | migration 049 DDL | mitigate | The statement targets ONLY `battle_logs.promoted_to_reminder` (a single INTEGER NOT NULL DEFAULT 0 column with no FK, zero reads/writes). No other column or table is touched; data-layer test (Task 2) confirms only this column is removed and the schema otherwise intact. |
| T-136-11-02 | Denial of Service (broken launch) | CRLF checksum drift / migration order | mitigate | Author with LF (Write tool) + `pnpm check:version` leg 3 CR-byte gate; lib.rs/disk count parity (leg 2) prevents the "update breaks launch" drift class; migration 049 sorts after 048 (filename order preserved); migration 027 never edited. |

No high-severity threats introduced beyond the inherent DDL change, which is scoped to a single vestigial column and gated by the parity check and a data-layer verification test.
</threat_model>

<verification>
- `pnpm check:version` green (count parity + LF).
- `pnpm test -- tests/data-layer/` green (migration applies; column gone; parity at 49).
- `grep -rn "promoted_to_reminder" src/` == 0.
- `git diff --quiet src-tauri/migrations/027_battle_log_after_action.sql` (027 untouched).
- `pnpm build` green.
</verification>

<success_criteria>
HON-11 met: the vestigial `promoted_to_reminder` column is removed via a new LF migration 049, registered in lib.rs, with `battleLog.ts` cleaned up, the parity gate green, and the data layer confirming the column no longer exists.
</success_criteria>

<output>
Create `.planning/phases/136-code-honesty-decomposition/136-03-SUMMARY.md` when done.
</output>

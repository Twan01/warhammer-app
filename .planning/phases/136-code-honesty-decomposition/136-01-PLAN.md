---
phase: 136-code-honesty-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/units/WeaponTable.tsx
  - src/features/rules-hub/DatasheetPointsTab.tsx
  - src/features/unit-database/UdbDatasheetSheet.tsx
  - src/features/unit-database/UdbWeaponsTable.tsx
  - tests/units/WeaponTable.test.tsx
autonomous: true
requirements: [HON-08]

must_haves:
  truths:
    - "Exactly one WeaponTable implementation exists in src/ (the canonical units/WeaponTable.tsx)"
    - "UdbWeaponsTable.tsx is deleted and no file imports it"
    - "DatasheetPointsTab and UdbDatasheetSheet render weapon rows identically in EN and FR after re-pointing"
    - "The merged WeaponTable renders the 'Rng' header label, range '\"' suffix for integers, skill '+' guard, non-italic keywords, no header background"
  artifacts:
    - path: "src/features/units/WeaponTable.tsx"
      provides: "Single canonical WeaponTable component"
      contains: "export function WeaponTable"
    - path: "tests/units/WeaponTable.test.tsx"
      provides: "Range/skill format + EN/FR parity regression test"
  key_links:
    - from: "src/features/rules-hub/DatasheetPointsTab.tsx"
      to: "src/features/units/WeaponTable.tsx"
      via: "import { WeaponTable }"
      pattern: "import.*WeaponTable.*from.*units/WeaponTable"
    - from: "src/features/unit-database/UdbDatasheetSheet.tsx"
      to: "src/features/units/WeaponTable.tsx"
      via: "import { WeaponTable }"
      pattern: "import.*WeaponTable.*from.*units/WeaponTable"
---

<objective>
Collapse the three WeaponTable implementations (`units/WeaponTable.tsx` canonical, `unit-database/UdbWeaponsTable.tsx`, and the shadow `function WeaponTable` inside `rules-hub/DatasheetPointsTab.tsx`) into the single canonical component, consumed by every datasheet/weapon surface, rendering identically in EN and FR (HON-08, per D-01/D-02/D-03).

Purpose: Restore architectural honesty — one shared weapon table — and unblock PLAY-01 (Phase 138 comparison view consumes this deduped component).
Output: One canonical `WeaponTable`, two consumers re-pointed, `UdbWeaponsTable.tsx` deleted, a regression test locking the render contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-code-honesty-decomposition/136-CONTEXT.md
@.planning/phases/136-code-honesty-decomposition/136-RESEARCH.md
@.planning/phases/136-code-honesty-decomposition/136-PATTERNS.md
@.planning/phases/136-code-honesty-decomposition/136-UI-SPEC.md

<interfaces>
<!-- The canonical component (already correct) — executor must NOT change its prop shape -->
From src/features/units/WeaponTable.tsx:
```typescript
import type { UdbWeapon } from "@/db/queries/unitDatabase";
interface WeaponTableProps {
  weapons: UdbWeapon[];
  statLabel: "BS" | "WS";
}
export function WeaponTable({ weapons, statLabel }: WeaponTableProps): JSX.Element;
```
The UI-SPEC §"WeaponTable Visual Contract (HON-08)" locks the exact 7-column set, the `"Rng"` header label, the range `"` suffix, the skill `+` guard, the composite row key `${w.unit_id}-${w.weapon_group}-${w.line_order}-${i}`, the non-italic `leading-relaxed` keywords row, and the no-background header. These are the LOCKED baseline — render identically.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the WeaponTable render contract with a regression test, then normalize the canonical component</name>
  <files>src/features/units/WeaponTable.tsx, tests/units/WeaponTable.test.tsx</files>
  <read_first>
    - src/features/units/WeaponTable.tsx (the canonical 43-line component being locked)
    - tests/units/WeaponTable.test.tsx (existing test — extend, do not replace)
    - .planning/phases/136-code-honesty-decomposition/136-UI-SPEC.md §"WeaponTable Visual Contract (HON-08)" (the locked column/format/key/keywords/header rules)
    - .planning/phases/136-code-honesty-decomposition/136-PATTERNS.md §"HON-08: WeaponTable.tsx (enhance)" (verbatim canonical source + the 5 differences to win over UdbWeaponsTable)
  </read_first>
  <behavior>
    - Test: a weapon with `range = "24"` (plain integer string) renders `24"` (with inch suffix) in the Rng column.
    - Test: a weapon with non-numeric `range = "Melee"` renders `Melee` raw; a null range renders `—`.
    - Test: a weapon with `skill = "3"` renders `3+`; a weapon with `skill = "3+"` renders `3+` (no double-suffix); a null skill renders `—`.
    - Test: the header row contains the label `Rng` (NOT `Range`) and renders `statLabel` ("BS" or "WS") in column 4.
    - Test: a weapon with truthy `keywords` renders a keywords row with class containing `leading-relaxed` and NOT `italic`.
    - Test (EN/FR parity): rendering the component with pre-resolved FR weapon name strings produces byte-identical DOM structure to the EN render (the component is locale-agnostic; only the passed strings differ).
  </behavior>
  <action>Extend `tests/units/WeaponTable.test.tsx` with the behavior cases above (range integer suffix, raw non-numeric range, null fallback `—`, skill `+` guard incl. already-suffixed, `Rng` header label, `statLabel` column, non-italic `leading-relaxed` keywords, EN/FR parity). Run the test; if any case fails, normalize `src/features/units/WeaponTable.tsx` to match the UI-SPEC locked contract — keep `"Rng"` (not `"Range"`), the `/^\d+$/.test(w.range)` range guard with `"` suffix, the `w.skill.endsWith("+")` skill guard, the composite row key, the `leading-relaxed` (non-italic) keywords paragraph, and the no-background header. Do NOT change the prop shape and do NOT convert the div-grid to a semantic `<table>` (IN-010 is deferred per D-03). The canonical file should need minimal-to-zero change; the test is the lock.</action>
  <verify>
    <automated>pnpm test -- tests/units/WeaponTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- tests/units/WeaponTable.test.tsx` passes with all behavior cases above green.
    - `grep -n "Rng" src/features/units/WeaponTable.tsx` returns a match; `grep -n "\"Range\"" src/features/units/WeaponTable.tsx` returns no match.
    - `src/features/units/WeaponTable.tsx` contains exactly one `export function WeaponTable` and no semantic `<table>`/`<th` element (div-grid preserved per D-03).
    - `pnpm build` passes.
  </acceptance_criteria>
  <done>WeaponTable render contract is test-locked and the canonical component conforms to the UI-SPEC baseline; div-grid a11y untouched (deferred).</done>
</task>

<task type="auto">
  <name>Task 2: Replace the shadow WeaponTable in DatasheetPointsTab with the canonical import</name>
  <files>src/features/rules-hub/DatasheetPointsTab.tsx</files>
  <read_first>
    - src/features/rules-hub/DatasheetPointsTab.tsx (the local `function WeaponTable` at line ~306–361 + call sites ~190/202)
    - src/features/units/WeaponTable.tsx (the canonical component being imported)
    - .planning/phases/136-code-honesty-decomposition/136-PATTERNS.md §"HON-08 consumer migration: DatasheetPointsTab.tsx" (import-add + delete-local-function instructions)
    - .planning/phases/136-code-honesty-decomposition/136-RESEARCH.md §"Pitfall A: Missing the Third WeaponTable"
  </read_first>
  <action>Delete the local `function WeaponTable` (lines ~306–361) from `DatasheetPointsTab.tsx`. Add `import { WeaponTable } from "@/features/units/WeaponTable";` to the import block. The two existing call sites (lines ~190/202) reference the name `WeaponTable` and continue to resolve to the imported canonical component unchanged. Do NOT touch the three inline `useQuery` calls in this file — those are HON-10 scope (Plan 04) and must stay untouched here. Per UI-SPEC, the canonical no-background header wins over the deleted local function's `bg-muted/50` — this is the one intentional cosmetic normalization. Run `pnpm build` to confirm strict TypeScript surfaces no dangling references.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "function WeaponTable" src/` returns exactly 1 result (`src/features/units/WeaponTable.tsx`).
    - `grep -n "import { WeaponTable }" src/features/rules-hub/DatasheetPointsTab.tsx` matches the canonical `@/features/units/WeaponTable` path.
    - `grep -n "bg-muted/50" src/features/rules-hub/DatasheetPointsTab.tsx` does not match a weapon-table header (the deleted local function's header bg is gone).
    - `grep -cn "useQuery" src/features/rules-hub/DatasheetPointsTab.tsx` is unchanged from before this task (the 3 inline queries remain for Plan 04).
    - `pnpm build` passes.
  </acceptance_criteria>
  <done>The shadow WeaponTable copy is removed; DatasheetPointsTab consumes the canonical component; HON-10 query surface left intact for Plan 04.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point UdbDatasheetSheet to the canonical WeaponTable and delete UdbWeaponsTable.tsx</name>
  <files>src/features/unit-database/UdbDatasheetSheet.tsx, src/features/unit-database/UdbWeaponsTable.tsx</files>
  <read_first>
    - src/features/unit-database/UdbDatasheetSheet.tsx (imports `UdbWeaponsTable`, uses it at lines ~154/164)
    - src/features/unit-database/UdbWeaponsTable.tsx (the duplicate to delete)
    - src/features/units/WeaponTable.tsx (canonical target — identical prop shape `weapons` + `statLabel`)
    - .planning/phases/136-code-honesty-decomposition/136-PATTERNS.md §"HON-08 consumer migration: UdbDatasheetSheet.tsx"
    - .planning/phases/136-code-honesty-decomposition/136-UI-SPEC.md §"Surfaces to Compare" (UdbDatasheetSheet EN+FR checklist)
  </read_first>
  <action>In `UdbDatasheetSheet.tsx`, change the import from `import { UdbWeaponsTable } from "@/features/unit-database/UdbWeaponsTable"` to `import { WeaponTable } from "@/features/units/WeaponTable"`, and replace the two JSX usages (`<UdbWeaponsTable .../>` at ~154/164) with `<WeaponTable .../>`. No prop changes — both take `weapons: UdbWeapon[]` and `statLabel: "BS" | "WS"`. Then delete the file `src/features/unit-database/UdbWeaponsTable.tsx` entirely. Run `pnpm build` — strict TypeScript will flag any remaining import of the deleted file. Manually verify (UI-SPEC checklist) UdbDatasheetSheet renders identical weapon rows in EN and FR: weapon-name column, `Rng` label, range format, non-italic keywords, no header bg.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/features/unit-database/UdbWeaponsTable.tsx` (file deleted).
    - `grep -rn "UdbWeaponsTable" src/` returns 0 results.
    - `grep -n "WeaponTable" src/features/unit-database/UdbDatasheetSheet.tsx` shows the canonical import and 2 usages.
    - `pnpm build` passes.
    - Manual: UdbDatasheetSheet weapon rows render identically in EN and FR (UI-SPEC §"Surfaces to Compare" row 4).
  </acceptance_criteria>
  <done>UdbWeaponsTable is deleted; UdbDatasheetSheet consumes the canonical WeaponTable; no stray references remain; EN/FR parity confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure presentational component dedup; no new input handling, no network, no DB writes. Data arrives pre-resolved (incl. FR via query-layer COALESCE) as props. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-08-01 | Tampering (render drift) | WeaponTable consumers | mitigate | Regression test (Task 1) locks the render contract; `grep` gates assert exactly one implementation and zero stray imports; EN/FR parity asserted in test + manual checklist. |

No high-severity threats introduced — this is a behavior-preserving component dedup. No new attack surface, no input parsing, no persistence change.
</threat_model>

<verification>
- `grep -rn "function WeaponTable" src/` returns exactly 1.
- `grep -rn "UdbWeaponsTable" src/` returns 0.
- `pnpm test -- tests/units/WeaponTable.test.tsx` green.
- `pnpm build` green.
- Manual EN/FR parity on UdbDatasheetSheet and DatasheetPointsTab (UI-SPEC §"Surfaces to Compare").
</verification>

<success_criteria>
HON-08 met: a single shared `WeaponTable` (duplicate `UdbWeaponsTable` and the shadow local function both eliminated) consumed by all datasheet/weapon surfaces, rendering identically in EN and FR, locked by a regression test.
</success_criteria>

<output>
Create `.planning/phases/136-code-honesty-decomposition/136-01-SUMMARY.md` when done.
</output>

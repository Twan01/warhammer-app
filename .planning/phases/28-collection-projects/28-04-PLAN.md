---
phase: 28-collection-projects
plan: 04
type: execute
wave: 3
depends_on:
  - 28-02
  - 28-03
files_modified: []
autonomous: false
requirements:
  - COLL-01
  - COLL-02
  - PROJ-01
  - PROJ-02
  - PROJ-03

must_haves:
  truths:
    - "All 5 requirements verified in the live Tauri app"
    - "No regressions in collection table, gallery, kanban drag, or existing features"
  artifacts: []
  key_links: []
---

<objective>
Manual smoke-test checkpoint verifying all 5 Phase 28 requirements in the live Tauri app. Confirms photo thumbnails, StatusBadge, kanban enrichment, next-action hints, and Log Session shortcut all work end-to-end.

Purpose: Catch visual and interaction issues that unit tests cannot — broken photo rendering, layout shifts, drag interference, sheet overlay behavior.
Output: User confirmation that all requirements are met.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-collection-projects/28-CONTEXT.md
@.planning/phases/28-collection-projects/28-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all Phase 28 requirements in live Tauri app</name>
  <files></files>
  <action>
Launch the app with `pnpm tauri dev` and present the user with the 19-step verification checklist below. Wait for user confirmation.

What was built:
1. Gallery cards with photo thumbnails and faction-colored placeholders (COLL-01)
2. StatusBadge in gallery cards and table rows (COLL-02)
3. Kanban card metadata — last updated, recipe name, photo count (PROJ-01)
4. Next-action hints on kanban cards (PROJ-02)
5. Log Session shortcut button on kanban cards (PROJ-03)

How to verify:

**COLL-01 — Gallery Photo Thumbnails**
1. Navigate to Collection page, switch to Gallery view (grid icon)
2. Find a unit that has journal photos — verify the gallery card shows a square photo thumbnail at the top
3. Find a unit with NO journal photos — verify it shows a colored placeholder square with the unit's first 2 initials centered
4. Verify the placeholder has a colored top border matching the faction color
5. Verify photos use `loading="lazy"` (check Network tab — images load on scroll, not all at once)

**COLL-02 — StatusBadge in Gallery and Table**
6. In Gallery view: verify each card shows a StatusBadge (colored dot + status text) instead of plain text
7. Switch to Table view (list icon) — verify the Status column shows a StatusBadge (colored dot + status text) instead of the old outline badge
8. Click a StatusBadge in the table — verify the status-change popover opens and you can change the status
9. Change a unit's status via the popover — verify the badge updates immediately (optimistic update)

**PROJ-01 — Kanban Card Metadata**
10. Navigate to Painting Projects page
11. Find a kanban card — verify it shows a relative time (e.g. "2d", "1w") below the progress bar
12. If any project unit has a linked recipe — verify the card shows "Recipe: {name}" (may need to link one first via the unit detail sheet)
13. If any project unit has journal photos — verify the card shows a Camera icon + count

**PROJ-02 — Next-Action Hint**
14. Find a kanban card with status other than "Completed" — verify it shows an italicized hint like "→ Apply primer" or "→ Apply shade"
15. If you have a unit with "Completed" status on the board — verify NO hint is shown for that card

**PROJ-03 — Log Session Shortcut**
16. Find the Paintbrush icon button in the top-right of a kanban card (between the unit name and the 3-dot menu)
17. Click the Paintbrush button — verify LogSessionSheet opens with that unit pre-selected in the unit picker
18. Close the sheet — verify you return to the Projects page (no navigation)
19. Try dragging a kanban card — verify drag still works normally (the Paintbrush button does not interfere with drag)

Resume signal: Type "approved" if all 19 steps pass, or describe any issues.
  </action>
  <verify>User confirms "approved" for all 19 steps</verify>
  <done>All 5 Phase 28 requirements verified end-to-end in live Tauri app by user</done>
</task>

</tasks>

<verification>
All 5 Phase 28 requirements verified end-to-end in live app:
- COLL-01: Photo thumbnails + faction placeholder
- COLL-02: StatusBadge in gallery + table
- PROJ-01: Kanban metadata (time, recipe, photos)
- PROJ-02: Next-action hints
- PROJ-03: Log Session shortcut
</verification>

<success_criteria>
User confirms all 19 verification steps pass without issues.
</success_criteria>

<output>
After completion, create `.planning/phases/28-collection-projects/28-04-SUMMARY.md`
</output>

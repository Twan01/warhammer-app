---
phase: 8
slug: army-list-builder
status: draft
shadcn_initialized: true
preset: "new-york / zinc / tailwind-v4 / radix"
created: 2026-05-02
---

# Phase 8 — UI Design Contract

> Visual and interaction contract for Phase 8: Army List Builder.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (new-york style) |
| Preset | zinc base, CSS variables, Tailwind v4, no prefix |
| Component library | radix (via shadcn) |
| Icon library | lucide-react (established in all existing feature files) |
| Font | ui-sans-serif, system-ui, sans-serif (globals.css body declaration) |

Source: `components.json`, `npx shadcn info`, `src/styles/globals.css`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding between badge and text |
| sm | 8px | Compact element spacing, row internal gaps |
| md | 16px | Default element spacing, card internal padding |
| lg | 24px | Section padding, card grid gap |
| xl | 32px | Layout gaps between page sections |
| 2xl | 48px | Major section breaks (empty state vertical padding) |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Page wrapper uses `p-6` (24px) — established pattern from CollectionPage and PaintsPage
- Card grid gap: `gap-6` (24px) — consistent with existing page section gaps
- Summary bar between sheet header and unit table: `py-2 px-4` (8px vertical / 16px horizontal) — compact, always-visible band; `py-2` chosen over `py-4` to preserve a tighter visual band that separates header from scrollable unit table without consuming excess vertical space
- Touch targets for interactive row controls: minimum 32px height, matching existing table row density

Source: established from `CollectionPage.tsx` (`p-6`, `gap-6`), `FactionSummaryCard.tsx` (`px-4 py-4`), `CollectionEmptyState.tsx` (`py-16`, `gap-4`)

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (normal) | 1.5 |
| Label / table cell | 14px (text-sm) | 600 (semibold) for headers; 400 for values | 1.5 |
| Page heading | 20px (text-xl) | 600 (semibold) | 1.2 |
| Card name (list title) | 14px (text-sm) | 600 (semibold) | 1.2 |

Notes:
- Only 2 weights used: 400 (normal) and 600 (semibold) — enforcing the 2-weight rule
- text-xl font-semibold for page heading `<h1>` is established across CollectionPage, PaintsPage
- text-sm / text-base mix: use text-sm throughout table rows and card body; use text-base font-semibold only for empty-state primary heading (matching CollectionEmptyState pattern)
- Muted text color (secondary info): `text-muted-foreground` (dark mode: `hsl(240 5% 64.9%)`)

Source: `CollectionPage.tsx` line 112, `FactionSummaryCard.tsx` lines 46–49, `CollectionEmptyState.tsx` lines 19–24

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `hsl(240 10% 3.9%)` — `--background` | Page background |
| Secondary (30%) | `hsl(240 10% 5.9%)` — `--card` | Army list cards, detail sheet, summary bar background |
| Accent (10%) | `hsl(240 3.7% 15.9%)` — `--muted` / `--accent` | Hover states on interactive cards, selected state highlight, summary bar background band |
| Destructive | `hsl(0 72.2% 50.6%)` — `--destructive` | "Delete List" button, "Delete Anyway" in pre-delete warning dialog |

Accent reserved for:
- Hover background on army list cards (`hover:bg-muted/50` — matching FactionSummaryCard pattern)
- Active/selected card border or highlight (e.g., `ring-1 ring-ring` when selectedListId matches)
- Summary bar background band inside ArmyListDetailSheet (subtle separation from sheet body)
- List type badge background (variant="secondary" — uses `--secondary` token)

Destructive reserved for:
- "Delete List" action button in ArmyListDeleteDialog footer
- "Delete Anyway" button in enhanced UnitDeleteDialog (army list membership warning state)

Source: `src/styles/globals.css` dark mode token block, `FactionSummaryCard.tsx` hover pattern

---

## Component Inventory

All components below are already installed (confirmed via `npx shadcn info`). No new installs required.

| Component | Source | Usage in this phase |
|-----------|--------|---------------------|
| `card.tsx` | shadcn official | ArmyListsPage — one card per list |
| `sheet.tsx` | shadcn official | ArmyListDetailSheet (view/manage units), ArmyListSheet (create/edit form) |
| `dialog.tsx` | shadcn official | ArmyListDeleteDialog; wraps unit-picker Command palette as sibling portal |
| `command.tsx` | shadcn official | Unit picker inside the Dialog wrapper |
| `badge.tsx` | shadcn official | List type tag (Casual/Learning/Narrative/Competitive/Test), painting status per unit row |
| `button.tsx` | shadcn official | "New List" primary CTA, "Add Unit" in detail sheet, all dialog actions |
| `input.tsx` | shadcn official | Per-unit points override inline input in table column |
| `table.tsx` | shadcn official | Unit rows inside ArmyListDetailSheet |
| `separator.tsx` | shadcn official | Visual divider between summary bar and unit table |
| `skeleton.tsx` | shadcn official | Loading state for cards and detail sheet |

---

## Layout Contracts

### ArmyListsPage

- Primary visual anchor: army list card grid — the main content surface; all other elements (page heading, "New List" CTA) are chrome around it
- Page wrapper: `flex flex-col gap-6 p-6` — identical to CollectionPage and PaintsPage
- Page heading row: `flex items-center justify-between` — `<h1 className="text-xl font-semibold">Army Lists</h1>` + "New List" button with `<Plus>` icon (lucide)
- Card grid: `grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3` — responsive, 1→2→3 columns
- Each army list card: `cursor-pointer border hover:bg-muted/50` with `CardHeader` (list name + list type badge) and `CardContent` (faction name, total points / points limit, battle-ready %)
- List type badge: `<Badge variant="secondary">` placed inline with the card title in the header row
- Battle-ready %: rendered as plain text (e.g. "72% battle-ready") — no progress bar at card level (save detail for the sheet)

### ArmyListDetailSheet

- Sheet width: `sm:max-w-[600px]` — wider than default to accommodate the unit table
- Sheet header: list name (SheetTitle) + faction name (SheetDescription)
- Summary bar (always visible, pinned below header, above unit table scroll area): `flex items-center gap-6 px-4 py-2 bg-muted/30 border-b` showing three stats:
  - Total Points: `{total} pts`
  - Painted Points: `{painted} pts`
  - Battle-Ready: `{pct}%`
- Unit table: `<Table>` with columns — Unit Name | Status badge | Points (inline input) | Notes (expand icon) | Remove
- Points column: `<Input type="number" className="w-20 h-7 text-sm" placeholder="—">` saves on blur and Enter; empty value displays inherited `unit.points` as placeholder text
- Per-unit notes: collapsed by default; expand icon uses `<ChevronDown>` (lucide) with `aria-label="Expand notes"` (or `aria-label="Collapse notes"` when open) — reveals a one-line textarea inline below the row; keeps the default table density compact
- Remove button: icon-only `<Button variant="ghost" size="icon">` using `<Trash2>` (lucide) with `aria-label="Remove unit from list"`
- List-level notes: below the unit table, above SheetFooter — `<Textarea>` with a "Save notes" button alongside it; label "List notes"
- SheetFooter: "Edit List" (outline) + "Delete List" (destructive) — always visible at bottom

### ArmyListSheet (create/edit form)

- Standard Sheet form pattern — matches UnitSheet / PaintSheet
- Fields: Name (required), Faction (Select), List Type (Select: Casual / Learning / Narrative / Competitive / Test), Points Limit (number Input, optional), Notes (Textarea, optional)
- SheetFooter: "Cancel" (ghost) + "Save List" (default) / "Update List" (edit mode)

### Unit Picker (Command palette)

- Rendered as `<Dialog>` wrapping `<Command>` — sibling to the Sheet, NOT nested inside it
- Dialog trigger: "Add Unit" button inside ArmyListDetailSheet; sets a `unitPickerOpen` state that triggers the root-level Dialog
- Command search filters units by name; pre-filtered to `faction_id` matching the list's faction
- Each CommandItem shows: unit name + category badge + painting status badge
- Selecting a unit calls `useAddUnitToList`, adds immediately, keeps Dialog open for multi-add
- Close button (X) or Escape closes the Dialog

### Enhanced UnitDeleteDialog (pre-delete warning)

- Normal state (unit in 0 lists): unchanged — existing dialog copy and single-step confirm
- Warning state (unit in ≥1 lists): two-step flow
  - Step 1 — Warning view: heading "This unit is in active army lists", body lists the list names, "Cancel" (outline) + "Delete Anyway" (destructive variant, labeled exactly "Delete Anyway")
  - Step 2 — fires delete on "Delete Anyway" click; DB cascade handles `army_list_units` cleanup

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (page) | "New List" (with Plus icon, button variant="default") |
| Primary CTA (detail sheet — add unit) | "Add Unit" (with Plus icon, button variant="outline" or "secondary") |
| Empty state heading | "Build your first army list" |
| Empty state body | "Create a list to track which units you're taking to the table and see your battle-ready percentage at a glance." |
| Empty state CTA | "New List" (Button, calls same handler as page-level CTA) |
| Error state (page load failure) | "Failed to load army lists. Try refreshing the app." (text-sm text-destructive — matches existing error pattern) |
| Error state (mutation failure) | "Something went wrong. Please try again." (sonner toast.error — matches existing pattern) |
| Destructive confirmation — delete list | Dialog title: "Delete army list?" / Body: `This will permanently delete "{list name}" and remove all units from it.` / Buttons: "Keep List" (outline) + "Delete List" (destructive) |
| Destructive confirmation — delete unit (no lists) | Unchanged: "Delete unit?" / `This will permanently delete "{unit.name}".` / "Keep Unit" + "Delete" |
| Destructive confirmation — delete unit (in lists) | Title: "This unit is in active army lists" / Body: `"{unit.name}" is in {N} army list(s): {list names}. Deleting it will also remove it from those lists.` / "Cancel" + "Delete Anyway" |
| Points override placeholder | `—` (em-dash) when blank; tooltip or placeholder text: "Inherits {unit.points} pts" |
| Summary bar labels | "Total: {N} pts" · "Painted: {N} pts" · "Battle-ready: {N}%" |
| List type badge labels | "Casual" / "Learning" / "Narrative" / "Competitive" / "Test" |
| Success toasts | "Army list created." / "Army list updated." / "Army list deleted." / "Unit added." / "Unit removed." |

---

## Interaction Contracts

| Interaction | Behavior |
|-------------|----------|
| Click army list card | Sets `selectedListId`; opens ArmyListDetailSheet as sibling portal |
| Click "New List" | Opens ArmyListSheet in create mode as sibling portal |
| Click "Edit List" (sheet footer) | Opens ArmyListSheet in edit mode pre-filled |
| Click "Delete List" (sheet footer) | Opens ArmyListDeleteDialog as sibling portal; closes ArmyListDetailSheet on confirm |
| Points override input blur / Enter | Calls `useUpdateArmyListUnit` with `points_override` (null if empty string); no explicit save button for points |
| "Add Unit" button in detail sheet | Sets `unitPickerOpen = true` at page/root level — opens sibling Dialog wrapping Command |
| Select unit in Command palette | Calls `useAddUnitToList` immediately; palette stays open; summary bar recalculates |
| Click remove on unit row | Calls `useRemoveUnitFromList` immediately; no confirmation (reversible by re-adding) |
| Per-unit notes expand icon | Toggles row expansion inline; does NOT open a separate sheet or dialog |
| "Save notes" (list-level) | Calls `useUpdateArmyList` with updated `notes` field; shows toast on success |
| Navigate away from page | Zustand filter state resets (ephemeral — same pattern as CollectionPage) |

---

## States to Implement

| Component | States |
|-----------|--------|
| ArmyListsPage | loading (skeletons), error, empty (no lists), populated (card grid) |
| ArmyListDetailSheet | loading (skeleton rows), loaded (unit table + summary bar), empty units (inline message "No units yet — add some") |
| Unit picker Command | loading (skeleton items), no results ("No units found in this faction"), results list |
| UnitDeleteDialog | normal (simple confirm), warning (army list membership) |
| Points override input | idle (showing effective_points as placeholder), editing, pending (disabled during mutation) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | badge, button, card, command, dialog, drawer, form, input, label, popover, progress, select, separator, sheet, sidebar, skeleton, sonner, table, tabs, tooltip | not required — all components pre-installed |

No third-party registries declared for this phase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

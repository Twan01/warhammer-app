---
phase: 08-army-list-builder
plan: 05
type: execute
wave: 4
depends_on:
  - "08-04"
files_modified: []
autonomous: false
requirements:
  - ARMY-01
  - ARMY-02
  - ARMY-03
  - ARMY-04
  - ARMY-05
  - ARMY-06
  - ARMY-07
must_haves:
  truths:
    - "User runs pnpm tauri dev and reaches the running app window"
    - "User confirms /army-lists route renders from the new sidebar nav entry (ClipboardList icon labeled 'Army Lists')"
    - "User confirms the empty state shows on first visit and the 'New List' CTA opens the create Sheet"
    - "User confirms creating an army list with name + faction + list_type + points_limit + notes saves and the card appears in the grid"
    - "User confirms clicking a card opens the detail Sheet with the pinned summary bar"
    - "User confirms 'Add Unit' opens a sibling Dialog (NOT nested inside the Sheet) with the Command palette pre-filtered by faction"
    - "User confirms the unit picker stays open after each select (multi-add)"
    - "User confirms the inline points override input saves on blur and Enter; clearing it falls back to the unit's base points (effective_points recomputed by SQL)"
    - "User confirms expanding a unit row reveals an inline notes textarea that saves via Save button without collapsing other rows"
    - "User confirms list-level notes save below the unit table"
    - "User confirms the summary bar updates immediately after add/remove/points-edit (cache invalidation working)"
    - "User confirms editing a list (Edit List in footer) reopens the form pre-filled and saves changes"
    - "User confirms deleting a list (Delete List in footer) removes the card and any open detail Sheet"
    - "User confirms attempting to delete a unit on the Collection page that belongs to an army list shows the warning state with list names + 'Delete Anyway' button (ARMY-05)"
    - "User confirms deleting that unit via 'Delete Anyway' removes both the unit and its army_list_units rows (CASCADE)"
  artifacts:
    - path: "human verification only"
      provides: "Manual smoke test of all 7 ARMY-01..07 requirements"
      contains: "n/a"
  key_links:
    - from: "Manual smoke test"
      to: "All 7 ARMY requirements"
      via: "User-driven test of every behavior listed in 08-VALIDATION.md §Manual-Only Verifications + every CONTEXT.md decision"
      pattern: "n/a"
---

<objective>
Manual smoke test of all seven ARMY-01..07 requirements after Plans 08-00 through 08-04 complete. The Tauri IPC layer cannot be mocked in jsdom (per 08-RESEARCH.md and the established codebase pattern), so every save handler, every cache invalidation, the sibling-portal architecture (Pitfall 1), and the cross-component pre-delete warning must be verified by running `pnpm tauri dev` and exercising each behavior in the live application.

Purpose: Closes the validation loop for ARMY-01 (create list with all fields), ARMY-02 (add/remove units via Command picker), ARMY-03 (per-unit points override with SQL COALESCE), ARMY-04 (per-unit + list-level notes), ARMY-05 (UnitDeleteDialog warning state), ARMY-06 (empty state CTA), ARMY-07 (route + sidebar nav). Anything the user finds broken triggers a `/gsd:plan-phase --gaps` re-plan; anything the user approves can move to `/gsd:verify-work` for the formal phase gate.

Output: User approval (or list of issues to fix) — no file changes.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-army-list-builder/08-CONTEXT.md
@.planning/phases/08-army-list-builder/08-RESEARCH.md
@.planning/phases/08-army-list-builder/08-UI-SPEC.md
@.planning/phases/08-army-list-builder/08-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual smoke test of all ARMY-01..07 behaviors</name>
  <what-built>
    Plans 08-00 through 08-04 deliver the full Army List Builder feature:
    - Plan 08-00: getArmyListsByUnitId query + Wave 0 stub tests (2 real + 5 stubs)
    - Plan 08-01: armyListSchema (zod, 5 list_type enum values) + ArmyListSheet (create/edit form) + ArmyListDeleteDialog (simple confirm)
    - Plan 08-02: ArmyListSummaryBar (pinned 3-stat band) + ArmyListUnitRow (inline points + expand notes + remove) + UnitPickerDialog (Dialog wrapping Command)
    - Plan 08-03: ArmyListDetailSheet (composes summary + table + list notes + footer) + ArmyListCard (grid item)
    - Plan 08-04: ArmyListsEmptyState + ArmyListsPage (sibling portal architecture: 4 portals at root) + /army-lists route + sidebar nav + UnitDeleteDialog enhancement (ARMY-05 warning state)

    All automated tests pass (162+ total). Manual verification covers behaviors that require live Tauri IPC.
  </what-built>
  <how-to-verify>
    Run the app:
    ```
    pnpm tauri dev
    ```

    Wait for the desktop window to launch with the HobbyForge sidebar. Confirm the sidebar shows a new "Army Lists" entry with a ClipboardList icon between "Paints" and "Settings" (collapse the sidebar to confirm the icon-only mode also shows the tooltip "Army Lists" on hover).

    **Step 1 — ARMY-07: Route + sidebar nav.**
    1. Click "Army Lists" in the sidebar. Confirm the URL changes to `/army-lists`.
    2. Confirm the page heading is "Army Lists" (text-xl font-semibold) with a "New List" button (Plus icon) on the right.
    3. Refresh the app (Ctrl+R). Confirm the route persists and the page reloads correctly.
    4. With the sidebar collapsed (click ChevronsLeft button at the bottom), hover the ClipboardList icon — confirm the tooltip "Army Lists" appears on the right side.

    **Step 2 — ARMY-06: Empty state.**
    1. On first visit (no lists in DB yet), confirm the page renders the empty state:
       - A Swords icon (centered, 48px, muted)
       - Heading: "Build your first army list"
       - Body: "Create a list to track which units you're taking to the table and see your battle-ready percentage at a glance."
       - A "New List" button (the empty-state CTA — distinct from the page-level CTA in the header)
    2. Click the empty-state "New List" button. Confirm the create Sheet opens.
    3. Close the Sheet (X or Discard changes). Confirm the empty state is still showing.

    **Step 3 — ARMY-01: Create an army list.**
    1. Click "New List" (either the page header or empty-state CTA). The right-side Sheet opens with title "New Army List".
    2. Confirm 5 form fields: Name (required), Faction (Select with "No faction" option + your seeded factions), List Type (Select with 5 options: Casual / Learning / Narrative / Competitive / Test), Points Limit (number input, optional), Notes (textarea, optional).
    3. Try to submit with empty Name — confirm a validation error appears below the Name field ("Name is required").
    4. Fill in: Name = "Test Casual List 1000pt", Faction = (any seeded faction), List Type = "Casual", Points Limit = 1000, Notes = "smoke test list".
    5. Click "Save List". Confirm:
       - Toast appears: "Army list created."
       - Sheet closes.
       - The empty state disappears and a card appears in the grid.
       - Card shows: name "Test Casual List 1000pt", faction name, "Casual" badge, "Total: 0 / 1000 pts", "Battle-ready: 0%".
    6. Create a second list: Name = "No Faction List", Faction = "No faction", List Type = "Test", Points Limit blank, Notes blank.
    7. Confirm the second card shows: name "No Faction List", faction text "No faction", "Test" badge, "Total: 0 / — pts" (em-dash for missing limit), "Battle-ready: 0%".

    **Step 4 — Open detail sheet (sibling portal architecture).**
    1. Click the first card. Confirm the right-side ArmyListDetailSheet opens (width sm:max-w-[600px], wider than UnitDetailSheet).
    2. Confirm sheet header shows the list name (SheetTitle) and a faction badge below it.
    3. Confirm the pinned summary bar shows three stats: "Total: 0 pts", "Painted: 0 pts", "Battle-ready: 0%".
    4. Confirm a "Units" label + "Add Unit" outline button appear above an empty area saying "No units yet — add some."
    5. Confirm a "List notes" label + textarea + "Save notes" button appear below the units area.
    6. Confirm the SheetFooter shows "Delete List" (ghost destructive) on the left and "Edit List" (outline) on the right.

    **Step 5 — ARMY-02: Add units (Command palette + sibling portal verification).**
    1. With the detail sheet open, click "Add Unit". Confirm a Dialog opens IN ADDITION TO the Sheet (BOTH visible — the Dialog appears centered, the Sheet is still on the right). If the Sheet closes when the Dialog opens, the sibling portal pattern is broken (Pitfall 1) — report failure.
    2. Confirm the Dialog title is "Add unit to list" with a description mentioning the faction filter.
    3. Confirm the Command palette has a search input ("Search units...") and a list of units. The list MUST be filtered to units matching the list's faction (ARMY-02). For the second list ("No Faction List"), confirm ALL units appear (Pitfall 3).
    4. Type a few characters in the search — confirm the list narrows.
    5. Click a unit. Confirm:
       - Toast: "Unit added."
       - The Dialog STAYS OPEN (multi-add UX per CONTEXT.md decision).
       - The Sheet's unit table now shows that unit as a row.
       - The summary bar updates: "Total: {unit.points} pts" (or 0 if the unit has no base points).
    6. Click 2 more units. Confirm each adds without closing the Dialog. Confirm 3 rows appear in the table and the summary bar updates after each.
    7. Press Escape (or click the X). Confirm the Dialog closes but the Sheet stays open.

    **Step 6 — ARMY-03: Per-unit points override.**
    1. In the unit table, confirm each row shows 5 cells: Unit Name, Status badge, Points (number Input), Notes (ChevronDown icon button), Remove (Trash2 icon).
    2. The Points cell input shows the unit's base points as a placeholder (or "—" if the unit has no base points). The input value is empty (no override yet).
    3. Click into the Points input on the first row. Type "150". Press Tab (or click outside) — input loses focus, save fires. Confirm:
       - Toast does NOT appear (points saves are silent — only errors toast)
       - Summary bar "Total" updates immediately to reflect the override
    4. On the second row, type "200" then press Enter — input blurs, save fires.
    5. On the first row, clear the Points input (select all + Delete). Click outside. Confirm:
       - Summary bar "Total" updates again — now using the unit's base points (effective_points = COALESCE(NULL, unit.points, 0))
    6. If you have a unit with NULL base points, leaving its override blank should result in 0 pts contribution to the total — confirm this is the case.

    **Step 7 — ARMY-04: Per-unit notes (expandable inline).**
    1. Click the ChevronDown icon on a row. Confirm:
       - The chevron rotates to ChevronUp
       - A new sub-row appears immediately below with a textarea + Save button
       - Other rows remain compact (only this one is expanded)
    2. Type "test note for this unit". Click Save. Confirm:
       - Toast: "Unit notes saved."
       - The sub-row collapses, chevron returns to ChevronDown
    3. Re-expand the same row. Confirm the saved text "test note for this unit" appears in the textarea (persisted).
    4. Clear the textarea (select all + delete). Click Save. Confirm toast + collapse.
    5. Re-expand. Confirm the textarea is empty (cleared notes saved as null/empty).
    6. CRITICAL — verify Pitfall 2: edit the points override on a row whose notes you just saved (or vice versa). Confirm the previously-saved field is NOT lost. (Both saves use full-replacement UPDATE; if either save sends undefined for the un-touched field, the other field clears unexpectedly.)

    **Step 8 — ARMY-04: List-level notes.**
    1. Below the unit table, in the "List notes" textarea, type "this is the list-level note". Click "Save notes". Confirm toast: "Notes saved."
    2. Close the Sheet (click outside the right edge or press Escape). Re-open the same list by clicking its card. Confirm the textarea pre-fills with "this is the list-level note".
    3. Clear the textarea. Click "Save notes". Confirm toast: "Notes saved."
    4. Close + re-open. Confirm the textarea is now empty (Pitfall 5 — empty string overwrote via COALESCE).

    **Step 9 — Remove unit + summary bar refresh.**
    1. Click the Trash2 icon on a unit row. Confirm:
       - Toast: "Unit removed."
       - The row disappears immediately (cache invalidated)
       - The summary bar "Total" decreases by that unit's effective_points
    2. Remove all units. Confirm the table area shows "No units yet — add some." again.

    **Step 10 — Edit list (key prop check).**
    1. Click "Edit List" in the SheetFooter. Confirm the create Sheet re-opens with title "Edit Army List" and all 5 fields pre-filled with the list's current values.
    2. Change the Name to "Edited Test List". Click "Update List" (button copy is different in edit mode per UI-SPEC).
    3. Confirm:
       - Toast: "Army list updated."
       - Sheet closes
       - Card in the grid shows the new name
    4. CRITICAL — verify Pitfall 6 (key prop): Edit list 1, close without saving, then click Edit on list 2. Confirm list 2's data appears (NOT stale list 1 data). If list 1's data shows, the key prop is missing or wrong.

    **Step 11 — Delete list.**
    1. Open a list's detail sheet. Click "Delete List" in the footer. Confirm:
       - The ArmyListDeleteDialog opens (sibling portal — both Sheet AND Dialog visible briefly)
       - Title: "Delete army list?"
       - Body includes the list name in quotes and mentions removing all units
       - Buttons: "Keep List" (outline) + "Delete List" (destructive)
    2. Click "Keep List". Confirm dialog closes, list is NOT deleted, detail sheet still open.
    3. Click "Delete List" footer again. In the dialog, click "Delete List" (destructive). Confirm:
       - Toast: "Army list deleted."
       - Both the dialog AND the detail sheet close
       - The card disappears from the grid

    **Step 12 — ARMY-05: UnitDeleteDialog enhanced warning state.**
    PREP: Create one army list (Step 3) and add at least one unit to it (Step 5). Note the unit's name (e.g. "Intercessors").

    1. Navigate to the Collection page (sidebar > Collection).
    2. Find that unit in the table. Click its Trash2 icon (delete) or use the row's delete action.
    3. Confirm the UnitDeleteDialog opens. Because the unit is in 1+ army lists, the warning state must render:
       - Title: "This unit is in active army lists" (NOT "Delete unit?")
       - Body includes: the unit's name in quotes, the count "is in 1 army list" (or "is in 2 army lists" for plural), and the names of the lists it's in (comma-separated)
       - Buttons: "Keep Unit" (outline) + "Delete Anyway" (destructive — note the EXACT label, not just "Delete")
    4. Click "Keep Unit". Confirm dialog closes, unit still in collection.
    5. Open the same delete dialog again. This time click "Delete Anyway".
    6. Confirm:
       - Toast: "Unit deleted."
       - The unit row disappears from the Collection table
       - Navigate back to /army-lists. Open the list that previously contained the unit. Confirm the unit is GONE from the unit table (CASCADE worked — no orphan).
    7. Find a unit that is NOT in any army list. Click its delete icon. Confirm the OLD simple-confirm dialog renders:
       - Title: "Delete unit?" (NOT the warning title)
       - Body: `This will permanently delete "{unit name}".`
       - Buttons: "Keep Unit" + "Delete" (note: just "Delete", NOT "Delete Anyway")
    8. Click "Keep Unit" — no deletion.

    **Step 13 — Filter reset / state reset on navigation.**
    1. Open a list's detail sheet, expand a unit's notes, type some text (don't save). Navigate to Dashboard.
    2. Navigate back to /army-lists. Confirm the detail sheet is closed (selectedListId reset on unmount).
    3. Re-open the list. Confirm the previously-typed (unsaved) notes are gone and the textarea shows the LAST SAVED value (which should be empty if you didn't save before navigating).

    **Step 14 — Visual / styling spot-check.**
    1. Confirm the card grid is responsive: resize the window to phone-width — cards stack 1-column. Tablet — 2 columns. Desktop — 3 columns (per UI-SPEC `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`).
    2. Hover over a card — confirm the background shifts to muted/50.
    3. Confirm the page wrapper uses p-6 padding, gap-6 between heading row and grid (consistent with CollectionPage and PaintsPage).
    4. Confirm the summary bar inside the detail sheet has a subtle muted background (`bg-muted/30`) and a bottom border (`border-b`).
    5. Confirm the points override input is small (`w-20 h-7 text-sm`) — does NOT take a full table-cell width.
    6. Confirm dark mode is consistent throughout (no flashes of white background).

    If every step passes, type "approved". If any step fails, describe what broke (which step, which sub-step, what you saw, what you expected). The orchestrator will route failures to `/gsd:plan-phase --gaps` for closure.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found per-step</resume-signal>
  <read_first>
    - .planning/phases/08-army-list-builder/08-VALIDATION.md (§Manual-Only Verifications)
    - .planning/phases/08-army-list-builder/08-UI-SPEC.md (§States to Implement, §Copywriting Contract, §Interaction Contracts)
    - .planning/phases/08-army-list-builder/08-CONTEXT.md (decisions — sidebar tooltip, multi-add UX, sibling portals)
  </read_first>
  <action>
    Run `pnpm tauri dev`. Walk through Steps 1–14 above. The agent does not perform the verification — it presents the steps to the user and waits for the resume signal. Capture the user's response verbatim in 08-05-SUMMARY.md.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — no automated verify; awaiting user signal"</automated>
  </verify>
  <acceptance_criteria>
    - User has run `pnpm tauri dev` and reached the running app window
    - User has executed Steps 1 through 14 above in order
    - User has typed "approved" (or provided per-step failure notes that gap-closure planning can act on)
    - If issues are reported, they are scoped per requirement (ARMY-01 through ARMY-07) so a `--gaps` re-plan can target the specific failure
    - The two CRITICAL Pitfall verifications (Pitfall 1 sibling portals in Step 5, Pitfall 2 full-replacement in Step 7, Pitfall 6 key prop in Step 10) explicitly passed or failed
  </acceptance_criteria>
  <done>User approval received OR per-step failure notes captured for gap closure.</done>
</task>

</tasks>

<verification>
- All 14 manual smoke-test steps executed by the user
- User signal received: "approved" OR per-step failure list
- Three Pitfall verifications (sibling portals, full-replacement update, key prop) explicitly tested
</verification>

<success_criteria>
- All 7 ARMY requirements verified end-to-end against the live app
- Phase 8 ready for `/gsd:verify-work` if approved, OR ready for `/gsd:plan-phase --gaps` if any step fails
</success_criteria>

<output>
After approval, create `.planning/phases/08-army-list-builder/08-05-SUMMARY.md` listing:
- Each step (1–14) with PASS or FAIL
- Any per-step notes from the user
- Explicit PASS/FAIL on the three Pitfall verifications
- "Phase 8 ready for /gsd:verify-work" if all steps PASS
- "Phase 8 needs gap closure plan — see notes" if any step FAILs
</output>

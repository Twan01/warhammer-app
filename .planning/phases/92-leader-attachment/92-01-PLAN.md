---
phase: 92-leader-attachment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/groupUnitsWithLeaders.ts
  - src/hooks/useLeaderTargets.ts
  - tests/army-lists/groupUnitsWithLeaders.test.tsx
autonomous: true
requirements:
  - LDR-02
must_haves:
  truths:
    - "groupUnitsWithLeaders reorders a flat unit array so attached leaders appear immediately after their target"
    - "Unattached units maintain their original insertion-order position"
    - "useLeaderTargets fetches synced_leader_targets for a faction with TEXT faction_id conversion"
  artifacts:
    - path: "src/lib/groupUnitsWithLeaders.ts"
      provides: "Pure reorder function for leader-target visual grouping"
      exports: ["groupUnitsWithLeaders", "GroupedUnit"]
    - path: "src/hooks/useLeaderTargets.ts"
      provides: "React Query hook for leader target data"
      exports: ["useLeaderTargets", "LEADER_TARGETS_KEY"]
    - path: "tests/army-lists/groupUnitsWithLeaders.test.tsx"
      provides: "Unit tests for grouping logic edge cases"
  key_links:
    - from: "src/lib/groupUnitsWithLeaders.ts"
      to: "src/types/armyList.ts"
      via: "ArmyListUnitRow type import"
      pattern: "import.*ArmyListUnitRow"
    - from: "src/hooks/useLeaderTargets.ts"
      to: "src/db/queries/bsdataExtended.ts"
      via: "getLeaderTargetsByFaction query"
      pattern: "getLeaderTargetsByFaction"
---

<objective>
Create the groupUnitsWithLeaders pure utility function and useLeaderTargets React Query hook that Plan 02 depends on.

Purpose: Provides the client-side reorder logic (D-06, D-07) and leader target data access needed for visual grouping and eligibility detection in the LeaderAttachmentSheet and ArmyListUnitRow.

Output: Two new source files + comprehensive unit tests for the grouping function.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-leader-attachment/92-CONTEXT.md
@.planning/phases/92-leader-attachment/92-RESEARCH.md
@.planning/phases/92-leader-attachment/92-PATTERNS.md

<interfaces>
<!-- From src/types/armyList.ts -->
export interface ArmyListUnitRow {
  id: number;
  list_id: number;
  unit_id: number | null;
  unit_name: string;
  faction_id: number | null;
  // ...other fields...
  leader_attached_to_id: number | null; // FK to army_list_units.id, ON DELETE SET NULL
  effective_points: number;
}

<!-- From src/db/queries/bsdataExtended.ts -->
export interface SyncedLeaderTargetRow {
  leader_name: string;
  faction_id: string;
  target_name: string;
  synced_at: string;
}
export async function getLeaderTargetsByFaction(factionId: string): Promise<SyncedLeaderTargetRow[]>;

<!-- From src/hooks/useArmyLists.ts -->
export const ARMY_LIST_UNITS_KEY = (listId: number) => ["army-list-units", listId] as const;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: groupUnitsWithLeaders pure function + tests</name>
  <files>src/lib/groupUnitsWithLeaders.ts, tests/army-lists/groupUnitsWithLeaders.test.tsx</files>
  <read_first>
    - src/types/armyList.ts (ArmyListUnitRow interface — need leader_attached_to_id field)
    - src/lib/resolveUnitPoints.ts (analog — pure function file structure pattern: JSDoc header, types section, implementation section)
  </read_first>
  <behavior>
    - Test 1: Returns units unchanged (isIndentedLeader=false for all) when no unit has leader_attached_to_id set
    - Test 2: When unit B has leader_attached_to_id = unit A's id, output is [A (isIndentedLeader=false), B (isIndentedLeader=true), C...] where B is removed from its original position and placed after A
    - Test 3: Multiple leader-target pairs — two separate pairs are both correctly grouped
    - Test 4: Leader attached to target that does not exist in the array — leader is dropped from result (orphan safety)
    - Test 5: Empty array returns empty array
    - Test 6: Ghost units (unit_id=null) with leader_attached_to_id set are grouped correctly
  </behavior>
  <action>
    Create src/lib/groupUnitsWithLeaders.ts exporting GroupedUnit interface ({ unit: ArmyListUnitRow, isIndentedLeader: boolean }) and groupUnitsWithLeaders function. Per D-06 and D-07:

    1. Build a Map&lt;number, ArmyListUnitRow[]&gt; keyed by leader_attached_to_id (target id) -> array of leaders attached to that target
    2. Build a Set&lt;number&gt; of all unit IDs that are attached leaders (have non-null leader_attached_to_id)
    3. Iterate units in original order. For each unit:
       - If it is an attached leader (in the Set), skip it (it will be emitted after its target)
       - Otherwise emit it with isIndentedLeader=false
       - Then emit any leaders from the Map for this unit's id, each with isIndentedLeader=true
    4. Function is pure — returns new array, never mutates input

    Import ArmyListUnitRow from @/types/armyList (use the type-only import). Follow resolveUnitPoints.ts file structure: JSDoc header documenting Phase 92 + D-06/D-07, types section, implementation section.

    Write tests first in tests/army-lists/groupUnitsWithLeaders.test.tsx. Create mock ArmyListUnitRow objects with minimal required fields (id, list_id, unit_id, unit_name, leader_attached_to_id, effective_points — other fields can use defaults). Import from @/lib/groupUnitsWithLeaders.
  </action>
  <verify>
    <automated>pnpm test -- tests/army-lists/groupUnitsWithLeaders.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/groupUnitsWithLeaders.ts exports GroupedUnit interface and groupUnitsWithLeaders function
    - GroupedUnit has exactly two fields: unit (ArmyListUnitRow) and isIndentedLeader (boolean)
    - Function signature: (units: ArmyListUnitRow[]) => GroupedUnit[]
    - All 6 test cases pass
    - Function does not mutate input array (test verifies original array unchanged after call)
    - Orphaned leaders (leader_attached_to_id points to non-existent unit) are excluded from output
  </acceptance_criteria>
  <done>groupUnitsWithLeaders correctly reorders units with leader-target grouping across all edge cases, verified by 6+ passing tests</done>
</task>

<task type="auto">
  <name>Task 2: useLeaderTargets React Query hook</name>
  <files>src/hooks/useLeaderTargets.ts</files>
  <read_first>
    - src/hooks/useArmyLists.ts (lines 1-50 — hook file structure, query key pattern, staleTime convention)
    - src/db/queries/bsdataExtended.ts (lines 167-180 — SyncedLeaderTargetRow type and getLeaderTargetsByFaction signature)
  </read_first>
  <action>
    Create src/hooks/useLeaderTargets.ts exporting LEADER_TARGETS_KEY and useLeaderTargets hook.

    LEADER_TARGETS_KEY: (factionId: string) => ["leader-targets", factionId] as const

    useLeaderTargets(factionId: string | null): accepts nullable faction ID string (already TEXT-converted by caller per Pitfall 1 from RESEARCH.md). Returns useQuery result with:
    - queryKey: factionId ? LEADER_TARGETS_KEY(factionId) : ["leader-targets"]
    - queryFn: () => getLeaderTargetsByFaction(factionId!)
    - enabled: !!factionId
    - staleTime: 5 * 60 * 1000 (5 minutes — project default)

    Import getLeaderTargetsByFaction and SyncedLeaderTargetRow from @/db/queries/bsdataExtended. Use type-only import for the type. Follow the same pattern as other hooks in useArmyLists.ts.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useLeaderTargets.ts exists and exports LEADER_TARGETS_KEY and useLeaderTargets
    - LEADER_TARGETS_KEY returns ["leader-targets", factionId] tuple
    - useLeaderTargets accepts string | null parameter
    - enabled guard prevents query when factionId is null
    - staleTime is 5 * 60 * 1000
    - pnpm build passes with no TypeScript errors
  </acceptance_criteria>
  <done>useLeaderTargets hook provides faction-scoped leader target data with correct query key and null safety</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite -> client | Data from synced_leader_targets is user-imported CSV data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Tampering | groupUnitsWithLeaders input | accept | Pure function operates on React Query cache data already validated by DB layer; personal local app |
| T-92-02 | Denial | useLeaderTargets query | accept | Personal-use scale; enabled guard prevents unnecessary queries |
</threat_model>

<verification>
pnpm test -- tests/army-lists/groupUnitsWithLeaders.test.tsx -x
pnpm build
</verification>

<success_criteria>
- groupUnitsWithLeaders function correctly reorders units for visual grouping with all edge cases tested
- useLeaderTargets hook provides leader target data with proper null safety and TEXT faction_id handling
- All tests pass, TypeScript build clean
</success_criteria>

<output>
Create `.planning/phases/92-leader-attachment/92-01-SUMMARY.md` when done
</output>

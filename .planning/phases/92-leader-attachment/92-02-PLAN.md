---
phase: 92-leader-attachment
plan: 02
type: execute
wave: 2
depends_on:
  - 92-01
files_modified:
  - src/features/army-lists/LeaderAttachmentSheet.tsx
  - src/features/army-lists/ArmyListsPage.tsx
  - src/features/army-lists/ArmyListDetailSheet.tsx
  - src/features/army-lists/ArmyListUnitRow.tsx
  - tests/army-lists/LeaderAttachmentSheet.test.tsx
autonomous: true
requirements:
  - LDR-01
  - LDR-02
must_haves:
  truths:
    - "User can attach a character leader to a valid target unit from the LeaderAttachmentSheet"
    - "User can detach a leader from its target, returning both to independent display"
    - "Only valid pairings from synced_leader_targets are offered as targets"
    - "Target already led by another leader has its Attach button disabled with tooltip"
    - "Attached leader row renders indented below target row with left border accent"
    - "Target row shows 'Leader: [name]' badge when a leader is attached"
    - "Ghost units can participate in leader attachment via ghost_unit_name matching"
  artifacts:
    - path: "src/features/army-lists/LeaderAttachmentSheet.tsx"
      provides: "Sheet for browsing and attaching/detaching leaders"
      exports: ["LeaderAttachmentSheet"]
    - path: "tests/army-lists/LeaderAttachmentSheet.test.tsx"
      provides: "Component tests for leader attachment sheet"
  key_links:
    - from: "src/features/army-lists/LeaderAttachmentSheet.tsx"
      to: "src/hooks/useArmyLists.ts"
      via: "useSetLeaderAttachment / useClearLeaderAttachment mutations"
      pattern: "useSetLeaderAttachment|useClearLeaderAttachment"
    - from: "src/features/army-lists/ArmyListDetailSheet.tsx"
      to: "src/lib/groupUnitsWithLeaders.ts"
      via: "groupUnitsWithLeaders call in useMemo"
      pattern: "groupUnitsWithLeaders"
    - from: "src/features/army-lists/ArmyListsPage.tsx"
      to: "src/features/army-lists/LeaderAttachmentSheet.tsx"
      via: "Sibling portal rendering"
      pattern: "LeaderAttachmentSheet"
    - from: "src/features/army-lists/ArmyListUnitRow.tsx"
      to: "src/hooks/useLeaderTargets.ts"
      via: "Leader eligibility detection (passed as prop from parent)"
      pattern: "leaderTargets"
---

<objective>
Build the LeaderAttachmentSheet component and wire visual grouping into the existing army list UI.

Purpose: Delivers the full leader attachment UX (D-01 through D-11) — users can pair character leaders with valid targets, see the attachment reflected as visual grouping in the unit list, and detach when needed.

Output: New LeaderAttachmentSheet component, modifications to ArmyListsPage/ArmyListDetailSheet/ArmyListUnitRow, and component tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-leader-attachment/92-CONTEXT.md
@.planning/phases/92-leader-attachment/92-RESEARCH.md
@.planning/phases/92-leader-attachment/92-PATTERNS.md
@.planning/phases/92-leader-attachment/92-UI-SPEC.md
@.planning/phases/92-leader-attachment/92-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
export interface GroupedUnit {
  unit: ArmyListUnitRow;
  isIndentedLeader: boolean;
}
export function groupUnitsWithLeaders(units: ArmyListUnitRow[]): GroupedUnit[];

export const LEADER_TARGETS_KEY = (factionId: string) => ["leader-targets", factionId] as const;
export function useLeaderTargets(factionId: string | null): UseQueryResult<SyncedLeaderTargetRow[]>;

<!-- From src/hooks/useArmyLists.ts -->
export interface SetLeaderAttachmentVariables {
  army_list_unit_id: number;
  target_id: number;
  list_id: number;
}
export function useSetLeaderAttachment(): UseMutationResult<void, Error, SetLeaderAttachmentVariables>;

export interface ClearLeaderAttachmentVariables {
  army_list_unit_id: number;
  list_id: number;
}
export function useClearLeaderAttachment(): UseMutationResult<void, Error, ClearLeaderAttachmentVariables>;

<!-- From src/types/armyList.ts -->
export interface ArmyListUnitRow {
  id: number;
  list_id: number;
  unit_id: number | null;
  unit_name: string;
  faction_id: number | null;
  leader_attached_to_id: number | null;
  effective_points: number;
  ghost_unit_name: string | null;
  // ...other fields
}

export interface ArmyList {
  id: number;
  name: string;
  faction_id: number | null;
  detachment_id: string | null;
  detachment_name: string | null;
  points_limit: number | null;
  notes: string | null;
  // ...other fields
}

<!-- From src/db/queries/bsdataExtended.ts -->
export interface SyncedLeaderTargetRow {
  leader_name: string;
  faction_id: string;
  target_name: string;
  synced_at: string;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LeaderAttachmentSheet component + tests</name>
  <files>src/features/army-lists/LeaderAttachmentSheet.tsx, tests/army-lists/LeaderAttachmentSheet.test.tsx</files>
  <read_first>
    - src/features/army-lists/EnhancementPickerSheet.tsx (exact analog — clone structure, adapt for leader targets)
    - src/hooks/useArmyLists.ts (lines 294-335 — useSetLeaderAttachment, useClearLeaderAttachment signatures)
    - src/hooks/useLeaderTargets.ts (Plan 01 output — hook signature)
    - src/db/queries/bsdataExtended.ts (lines 167-180 — SyncedLeaderTargetRow type)
    - tests/army-lists/LoadoutBuilderSheet.test.tsx (test file analog — mock pattern, QueryClient wrapper)
    - .planning/phases/92-leader-attachment/92-UI-SPEC.md (copywriting contract, interaction states)
  </read_first>
  <action>
    Create src/features/army-lists/LeaderAttachmentSheet.tsx cloning the EnhancementPickerSheet pattern.

    Props interface LeaderAttachmentSheetProps: open (boolean), unit (ArmyListUnitRowType | null — the leader unit), list (ArmyList | null), units (ArmyListUnitRowType[] — all units in the list for target filtering), onClose (() => void).

    Implementation per D-01 through D-11:

    1. TEXT faction_id conversion: use the factionIdStr pattern from EnhancementPickerSheet lines 46-50 (unit?.faction_id -> String() -> list?.faction_id fallback).

    2. Fetch leader targets via useLeaderTargets(factionIdStr) hook from src/hooks/useLeaderTargets.ts.

    3. Compute valid targets: filter the passed units array to those whose unit_name (case-insensitive via .toLowerCase()) matches a target_name in the leader targets data for this leader's leader_name. Per D-08 preventive validation — only show valid targets.

    4. Current attachment detection: check if unit.leader_attached_to_id is non-null and find the attached target from the units array.

    5. Sheet layout (per UI-SPEC):
       - Header: Link2 icon + "{unit_name} -- Leader Attachment" title, "Attach this leader to a valid target unit" description
       - Current attachment banner (conditional, per D-03): if leader already attached, show highlighted row (bg-secondary rounded-md border p-3) with current target name + "Detach Leader" Button (variant="destructive", size="sm", h-7 text-xs). Calls useClearLeaderAttachment with { army_list_unit_id: unit.id, list_id: list.id }. Toast on error: "Failed to detach leader. Please try again."
       - Valid targets list: each target is a bordered card (rounded-md border p-3) with target unit_name (text-sm font-medium), Badge variant="secondary" showing effective_points + "pts", and action button on the right.

    6. Per-target action button states (per D-02, D-09):
       - If this target already has a different leader attached (another unit in the list has leader_attached_to_id === target.id AND that unit is not the current leader): disabled Button wrapped in span for Tooltip with "Already led by {existing_leader_name}" (Radix disabled workaround from EnhancementPickerSheet lines 192-209)
       - If this target is the one currently attached to this leader: show "Detach Leader" destructive button
       - Otherwise: "Attach Leader" Button (variant="outline", size="sm", h-7 text-xs). Calls useSetLeaderAttachment with { army_list_unit_id: unit.id, target_id: target.id, list_id: list.id }. Toast on error: "Failed to attach leader. Please try again."

    7. Guard states: no faction guard ("No faction selected for this list."), empty targets state ("No valid targets in this list" heading + "Add one of the following units to attach this leader: {comma-separated target names}" body listing valid target_name values not in the army list).

    8. Sheet does NOT close on attach/detach (per UI-SPEC close behavior).

    9. Ghost unit handling (D-11): matching uses unit_name which is already COALESCE(u.name, alu.ghost_unit_name) from the query layer — no special handling needed beyond case-insensitive comparison.

    Create tests/army-lists/LeaderAttachmentSheet.test.tsx following LoadoutBuilderSheet.test.tsx pattern:
    - Mock useSetLeaderAttachment and useClearLeaderAttachment from @/hooks/useArmyLists
    - Mock useLeaderTargets from @/hooks/useLeaderTargets
    - QueryClient + TooltipProvider wrapper
    - Test: renders sheet title with unit name when open
    - Test: shows valid target units with Attach Leader buttons
    - Test: shows disabled Attach button with tooltip when target already has a leader
    - Test: shows Detach Leader button when leader is already attached
    - Test: calls setLeaderAttachment.mutate on Attach click
    - Test: calls clearLeaderAttachment.mutate on Detach click
    - Test: shows empty state message when no valid targets in list
  </action>
  <verify>
    <automated>pnpm test -- tests/army-lists/LeaderAttachmentSheet.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - LeaderAttachmentSheet.tsx exports LeaderAttachmentSheet component
    - Props interface includes: open, unit, list, units, onClose
    - Sheet title contains unit_name and "Leader Attachment"
    - Valid targets filtered by case-insensitive name match against synced_leader_targets
    - Disabled button wrapped in span for Radix tooltip workaround
    - Tooltip text for disabled target reads "Already led by {name}"
    - Detach button uses variant="destructive"
    - Attach button uses variant="outline"
    - Empty state shows comma-separated valid target names from synced data
    - All 7+ test cases pass
  </acceptance_criteria>
  <done>LeaderAttachmentSheet renders valid targets, handles attach/detach/disabled states, and all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire visual grouping + sibling portal + leader trigger into existing components</name>
  <files>src/features/army-lists/ArmyListsPage.tsx, src/features/army-lists/ArmyListDetailSheet.tsx, src/features/army-lists/ArmyListUnitRow.tsx</files>
  <read_first>
    - src/features/army-lists/ArmyListsPage.tsx (full file — sibling portal state pattern at lines 38-83, portal JSX at lines 128-177)
    - src/features/army-lists/ArmyListDetailSheet.tsx (full file — interface at lines 38-66, unit table rendering at lines 244-270)
    - src/features/army-lists/ArmyListUnitRow.tsx (full file — interface at lines 36-45, enhancement trigger at lines 211-222, keyword check at lines 84-87)
    - src/lib/groupUnitsWithLeaders.ts (Plan 01 output — GroupedUnit interface)
    - src/hooks/useLeaderTargets.ts (Plan 01 output — useLeaderTargets hook)
    - .planning/phases/92-leader-attachment/92-UI-SPEC.md (indent styling, badge placement, trigger states)
    - .planning/phases/92-leader-attachment/92-PATTERNS.md (exact line references for all modification points)
  </read_first>
  <action>
    **ArmyListsPage.tsx modifications (per D-01, sibling portal pattern):**

    1. Add import for LeaderAttachmentSheet from ./LeaderAttachmentSheet
    2. Add state: const [leaderUnitId, setLeaderUnitId] = useState&lt;number | null&gt;(null) — alongside existing loadoutUnitId/enhancementUnitId at line 44-45
    3. Add derived lookup: const leaderUnit = leaderUnitId !== null ? (selectedListUnits ?? []).find((u) => u.id === leaderUnitId) ?? null : null — alongside existing loadoutUnit/enhancementUnit at lines 54-59
    4. Add handlers: openLeaderAttach = (armyListUnitId: number) => setLeaderUnitId(armyListUnitId), closeLeaderAttach = () => setLeaderUnitId(null) — alongside existing openLoadout/closeLoadout at lines 78-81
    5. Add setLeaderUnitId(null) to the closeDetail handler (line 75) alongside existing setLoadoutUnitId(null) and setEnhancementUnitId(null)
    6. Pass onAttachLeader={openLeaderAttach} prop to ArmyListDetailSheet (alongside onEnhanceUnit at line 138)
    7. Add LeaderAttachmentSheet sibling portal JSX after the EnhancementPickerSheet block (after line 171):
       LeaderAttachmentSheet with open={leaderUnitId !== null}, unit={leaderUnit}, list={selectedList}, units={selectedListUnits ?? []}, onClose={closeLeaderAttach}

    **ArmyListDetailSheet.tsx modifications (per D-05, D-06, D-07):**

    1. Add imports: groupUnitsWithLeaders and GroupedUnit from @/lib/groupUnitsWithLeaders, useLeaderTargets from @/hooks/useLeaderTargets, Link2 from lucide-react (for leader badge icon)
    2. Add onAttachLeader: (armyListUnitId: number) => void to the ArmyListDetailSheetProps interface
    3. Add onAttachLeader to the destructured props in the component signature
    4. Compute factionIdStr from list?.faction_id using String() conversion (same Pitfall 1 pattern)
    5. Call useLeaderTargets(factionIdStr) to get leader targets data for the faction
    6. Add useMemo for grouped units: const groupedUnits = useMemo(() => groupUnitsWithLeaders(units ?? []), [units])
    7. Compute a lookup Map for finding leader names attached to each target: iterate units, for each unit with leader_attached_to_id, record the mapping targetId -> leaderUnitName
    8. Replace the direct (units ?? []).map at line 256 with groupedUnits.map(({ unit: alu, isIndentedLeader }) => ...). Pass to ArmyListUnitRow:
       - All existing props unchanged
       - isIndentedLeader={isIndentedLeader}
       - onAttachLeader={() => onAttachLeader(alu.id)}
       - leaderName={leaderNameMap.get(alu.id)} (the name of the leader attached to this target, if any)
       - leaderTargets={leaderTargets ?? []} (the SyncedLeaderTargetRow array from useLeaderTargets)

    **ArmyListUnitRow.tsx modifications (per D-04, D-05, UI-SPEC):**

    1. Add imports: Link2 from lucide-react, type SyncedLeaderTargetRow from @/db/queries/bsdataExtended
    2. Extend ArmyListUnitRowProps interface with:
       - onAttachLeader: () => void
       - isIndentedLeader?: boolean (defaults to false)
       - leaderName?: string (name of leader attached to this unit, if this unit is a target)
       - leaderTargets?: SyncedLeaderTargetRow[] (for eligibility check)
    3. Add these to the destructured props in the component function signature
    4. Leader eligibility check: const isLeader = (leaderTargets ?? []).some(lt => lt.leader_name.toLowerCase() === unit.unit_name.toLowerCase()). Show trigger when isLeader is true (per D-04 — distinct from Character keyword check).
    5. Leader trigger button states (per UI-SPEC):
       - If isLeader AND unit.leader_attached_to_id is null: Button variant="outline" size="sm" className="h-7 text-xs mt-1" with Link2 icon + "Attach Leader" text, onClick={onAttachLeader}
       - If isLeader AND unit.leader_attached_to_id is not null: Button variant="secondary" size="sm" className="h-7 text-xs mt-1" with Link2 icon + "Attached" text, onClick={onAttachLeader} (opens sheet showing current attachment)
       Place this AFTER the showEnhanceTrigger block (after line 222) and before the tactical role Select.
    6. Visual grouping for indented leader rows (per D-05, UI-SPEC):
       - On the TableRow: conditionally add className "border-l-2" and style={{ borderLeftColor: "var(--faction-accent, hsl(var(--muted-foreground)))" }} when isIndentedLeader is true
       - On the first TableCell: add pl-8 (32px indent) when isIndentedLeader is true by conditionally including "pl-8" in className
    7. Leader badge on target rows (per D-05, UI-SPEC):
       - Below the enhancementName badge block, if leaderName is defined, render: Badge variant="outline" className="text-xs mt-0.5" containing Link2 icon (h-3 w-3 mr-1) + "Leader: {leaderName}" text
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - ArmyListsPage.tsx has leaderUnitId state, leaderUnit derived lookup, openLeaderAttach/closeLeaderAttach handlers, and LeaderAttachmentSheet sibling portal
    - closeDetail handler includes setLeaderUnitId(null)
    - ArmyListDetailSheet.tsx imports and calls groupUnitsWithLeaders in a useMemo, maps over groupedUnits instead of raw units
    - ArmyListDetailSheet.tsx passes isIndentedLeader, onAttachLeader, leaderName, and leaderTargets to each ArmyListUnitRow
    - ArmyListUnitRow.tsx shows Link2 "Attach Leader" button for leader-eligible units without attachment
    - ArmyListUnitRow.tsx shows Link2 "Attached" button (variant="secondary") for already-attached leaders
    - ArmyListUnitRow.tsx renders pl-8 + border-l-2 with faction accent color for isIndentedLeader rows
    - ArmyListUnitRow.tsx renders "Leader: {name}" Badge on target rows when leaderName prop is set
    - Leader eligibility uses case-insensitive comparison against leaderTargets prop
    - pnpm build passes with no TypeScript errors
    - pnpm test -- tests/army-lists/ -x passes (existing + new tests)
  </acceptance_criteria>
  <done>Leader attachment is fully wired: trigger buttons on eligible units, visual grouping with indent + border + badge, and LeaderAttachmentSheet accessible from the army list</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synced_leader_targets -> client | BSData-synced leader pairing data used for UI filtering |
| user click -> mutation | Attach/detach actions write to army_list_units.leader_attached_to_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03 | Tampering | LeaderAttachmentSheet attach mutation | accept | Preventive validation ensures only valid targets shown; personal local app with no multi-user access |
| T-92-04 | Information Disclosure | Leader target data display | accept | All data is user's own synced rules data displayed locally |
| T-92-05 | Denial | Multiple leaders on same target | mitigate | Client-side preventive validation: disable Attach button when target.leader_attached_to_id is already set by another leader |
</threat_model>

<verification>
pnpm build
pnpm test -- tests/army-lists/ -x
</verification>

<success_criteria>
- LeaderAttachmentSheet renders correctly with valid targets, disabled states, and attach/detach actions
- Visual grouping shows indented leader rows with left border accent after target rows
- Target rows display "Leader: [name]" badge
- Leader trigger button appears on leader-eligible units with correct state (Attach Leader / Attached)
- All existing army-lists tests continue to pass
- New LeaderAttachmentSheet tests pass
- TypeScript build clean
</success_criteria>

<output>
Create `.planning/phases/92-leader-attachment/92-02-SUMMARY.md` when done
</output>

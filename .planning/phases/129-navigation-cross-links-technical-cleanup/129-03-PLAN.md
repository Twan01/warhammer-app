---
phase: 129-navigation-cross-links-technical-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/army-lists/ArmyListDetailSheet.tsx
  - src/features/army-lists/ArmyListDetailPage.tsx
  - src/features/army-lists/armyListDetailReducer.ts
  - src/features/recipes/RecipeCard.tsx
autonomous: true
requirements: [NAV-07, NAV-08, NAV-09, NAV-11]

must_haves:
  truths:
    - "ArmyListDetailSheet.tsx is deleted (confirmed orphaned dead code)"
    - "RecipeCard is wrapped in React.memo to prevent unnecessary re-renders"
    - "detailPortalReducer is extracted to its own file with no behavior change"
    - "NAV-11 Esc hint is already present in StepFocalView (verification only)"
  artifacts:
    - path: "src/features/army-lists/armyListDetailReducer.ts"
      provides: "Extracted reducer, state type, action types, initial state"
      exports: ["detailPortalReducer", "initialDetailPortalState", "DetailPortalState", "DetailPortalAction"]
    - path: "src/features/recipes/RecipeCard.tsx"
      provides: "Memoized RecipeCard component"
      contains: "memo(function RecipeCard"
  key_links:
    - from: "src/features/army-lists/ArmyListDetailPage.tsx"
      to: "src/features/army-lists/armyListDetailReducer.ts"
      via: "import { detailPortalReducer, initialDetailPortalState }"
      pattern: "armyListDetailReducer"
---

<objective>
Technical cleanup: delete confirmed-orphaned ArmyListDetailSheet (~483 lines), wrap RecipeCard in React.memo, extract detailPortalReducer to its own file, and verify NAV-11 is already done.

Purpose: Per D-09/D-10/D-11/D-13, remove dead code that burdens maintenance, improve render performance for recipe lists, and improve file organization. NAV-11 was found to be already implemented during research.
Output: One file deleted, one new file created (reducer), two files modified.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-navigation-cross-links-technical-cleanup/129-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ArmyListDetailSheet and extract detailPortalReducer</name>
  <files>src/features/army-lists/ArmyListDetailSheet.tsx, src/features/army-lists/ArmyListDetailPage.tsx, src/features/army-lists/armyListDetailReducer.ts</files>
  <read_first>
    - src/features/army-lists/ArmyListDetailSheet.tsx (confirm no live imports — only comment references)
    - src/features/army-lists/ArmyListDetailPage.tsx (reducer definition at lines 82-196, useReducer call, state/action types)
    - src/features/army-lists/armyListsReducer.ts (existing extracted reducer pattern to follow)
  </read_first>
  <action>
Per D-09 — Delete `src/features/army-lists/ArmyListDetailSheet.tsx` entirely. Before deleting, grep for any actual imports (not comments) of `ArmyListDetailSheet` across the entire `src/` directory. The research confirmed only comment references exist in ExportDropdown.tsx, DatasheetBrowserDialog.tsx, ArmyListUnitRow.tsx, LoadoutBuilderSheet.tsx. If any real import is found, keep the file and note the finding.

Per D-11 — Create `src/features/army-lists/armyListDetailReducer.ts`:
- Move the `DetailPortalState` type (or equivalent state type name), `DetailPortalAction` union type, `initialDetailPortalState` constant, and `detailPortalReducer` function from `ArmyListDetailPage.tsx` (lines ~82-196) into the new file
- Export all four: the two types, the initial state, and the reducer function
- Follow the same pattern as `armyListsReducer.ts` for file structure and export style
- In `ArmyListDetailPage.tsx`, replace the inline definitions with imports from `./armyListDetailReducer`
- The `useReducer(detailPortalReducer, initialDetailPortalState)` call stays in ArmyListDetailPage.tsx
- Any types referenced by the reducer (e.g., `ArmyList`) should be imported in the new reducer file from their source
- This is a pure refactor — no behavior changes
  </action>
  <acceptance_criteria>
    - `src/features/army-lists/ArmyListDetailSheet.tsx` does not exist on disk
    - `src/features/army-lists/armyListDetailReducer.ts` exists and exports `detailPortalReducer`, `initialDetailPortalState`, and relevant types
    - `ArmyListDetailPage.tsx` imports from `./armyListDetailReducer` instead of defining inline
    - `ArmyListDetailPage.tsx` still contains the `useReducer(...)` call
    - No grep hits for `import.*ArmyListDetailSheet` in `src/` (comments are OK)
    - `pnpm build` passes with no TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Dead ArmyListDetailSheet deleted; reducer extracted to separate file with unchanged behavior</done>
</task>

<task type="auto">
  <name>Task 2: Wrap RecipeCard in React.memo and verify NAV-11</name>
  <files>src/features/recipes/RecipeCard.tsx</files>
  <read_first>
    - src/features/recipes/RecipeCard.tsx (current export pattern, props type)
    - src/features/painting-projects/KanbanCard.tsx (memo pattern template to follow)
    - src/features/painting-mode/StepFocalView.tsx (line ~181, verify "Esc to exit" text already exists)
  </read_first>
  <action>
Per D-10 — Wrap `RecipeCard` in `React.memo()`:
- Import `memo` from "react"
- Change the export from `export function RecipeCard({...}: RecipeCardProps)` to `export const RecipeCard = memo(function RecipeCard({...}: RecipeCardProps) { ... })`
- Use named function form (not arrow function) to preserve component name in React DevTools
- Follow the exact pattern from `KanbanCard.tsx`
- Do NOT change any internal logic, props, or types

For NAV-11 verification: Read `StepFocalView.tsx` and confirm that the "Esc to exit" text exists in the active step view (not just completion/error). Expected: a `<p>` or `<span>` with `text-xs text-muted-foreground` containing "Esc to exit" around line 181. If confirmed present, NAV-11 requires no code changes. If NOT found, add the hint per D-13 in the bottom-right or footer area with `text-xs text-muted-foreground`.
  </action>
  <acceptance_criteria>
    - RecipeCard.tsx exports via `export const RecipeCard = memo(function RecipeCard(...))`
    - `memo` is imported from "react"
    - RecipeCard props type and internal logic are unchanged
    - StepFocalView.tsx contains "Esc to exit" text visible during active painting (not just completion)
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>RecipeCard is memoized; NAV-11 Esc hint confirmed present in active step view</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure refactoring and dead code removal — no user input handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-04 | Denial of Service | RecipeCard memo | accept | memo prevents excessive re-renders, net positive for performance |
</threat_model>

<verification>
- `pnpm build` passes
- `ArmyListDetailSheet.tsx` no longer exists
- `armyListDetailReducer.ts` exists with exported reducer
- RecipeCard has `memo` wrapper
- `pnpm test` passes (no regressions)
</verification>

<success_criteria>
Dead code deleted, reducer cleanly extracted, RecipeCard memoized, NAV-11 confirmed already done.
</success_criteria>

<output>
Create `.planning/phases/129-navigation-cross-links-technical-cleanup/129-03-SUMMARY.md` when done
</output>

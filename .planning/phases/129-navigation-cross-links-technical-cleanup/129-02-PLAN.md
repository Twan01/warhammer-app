---
phase: 129-navigation-cross-links-technical-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/units/UnitDetailSheet.tsx
  - src/features/battle-log/BattleLogRow.tsx
  - src/app/rules-hub/page.tsx
  - src/app/unit-database/page.tsx
autonomous: true
requirements: [NAV-02, NAV-03, NAV-06]

must_haves:
  truths:
    - "Collection unit detail sheet has a View Datasheet link to Unit Database when unit is linked"
    - "Rules Hub and Unit Database have cross-links to each other in their PageHeader"
    - "Battle Log entries link to the army list used in that battle"
  artifacts:
    - path: "src/features/units/UnitDetailSheet.tsx"
      provides: "View Datasheet ghost button conditional on udb_unit_id"
      contains: "udb_unit_id"
    - path: "src/features/battle-log/BattleLogRow.tsx"
      provides: "Army list name as clickable Link"
      contains: "army-lists/$listId"
  key_links:
    - from: "src/features/units/UnitDetailSheet.tsx"
      to: "/unit-database"
      via: "navigate on button click"
      pattern: "View Datasheet"
    - from: "src/features/battle-log/BattleLogRow.tsx"
      to: "/army-lists/$listId"
      via: "Link component"
      pattern: "army-lists"
---

<objective>
Add cross-page navigation links: UnitDetailSheet -> Unit Database datasheet, Rules Hub <-> Unit Database bidirectional links, and BattleLogRow -> Army List detail.

Purpose: Per D-03/D-04/D-05/D-08, related features should be connected with visible links so users can navigate between logically related pages without going through the sidebar.
Output: Three components updated with cross-links, one page header for each of Rules Hub and Unit Database updated.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-navigation-cross-links-technical-cleanup/129-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add View Datasheet link to UnitDetailSheet and army list link to BattleLogRow</name>
  <files>src/features/units/UnitDetailSheet.tsx, src/features/battle-log/BattleLogRow.tsx</files>
  <read_first>
    - src/features/units/UnitDetailSheet.tsx (SheetHeader area, unit prop type, check for udb_unit_id field)
    - src/features/battle-log/BattleLogRow.tsx (army list name display at line 85-98, log prop type)
    - src/types/unit.ts (confirm udb_unit_id field name and type)
  </read_first>
  <action>
Per D-03 and D-04 — In `UnitDetailSheet.tsx`:
- Add a "View Datasheet" ghost Button in the SheetHeader area, near the unit name
- Condition rendering on `unit.udb_unit_id` being truthy (NOT `canonical_unit_id` — the field is `udb_unit_id: string | null`)
- On click: close the sheet (call `onClose()` or equivalent), then navigate to `/unit-database`
- Use `BookMarked` icon from lucide-react with `className="mr-1 h-3 w-3"`
- Button styling: `variant="ghost" size="sm" className="mt-1 h-auto px-0 text-xs text-muted-foreground hover:text-foreground"`
- Import `useNavigate` from `@tanstack/react-router` and `BookMarked` from `lucide-react`

Per D-08 — In `BattleLogRow.tsx`:
- Find the army list name display (around line 85-98)
- When `armyListName` is truthy AND `log.army_list_id` is not null: wrap the army list name text in a `<Link to="/army-lists/$listId" params={{ listId: String(log.army_list_id) }}>` with `className="hover:underline"` and `onClick={(e) => e.stopPropagation()}` to prevent the Collapsible toggle from firing
- When `armyListName` is falsy (list deleted): keep the existing italic "(Army list deleted)" text with no link
- Import `Link` from `@tanstack/react-router`
  </action>
  <acceptance_criteria>
    - UnitDetailSheet renders "View Datasheet" button only when `unit.udb_unit_id` is truthy
    - UnitDetailSheet does NOT render "View Datasheet" when `udb_unit_id` is null
    - BattleLogRow renders army list name as a Link to `/army-lists/$listId` when armyListName is truthy
    - BattleLogRow still shows "(Army list deleted)" italic text when armyListName is falsy
    - Link in BattleLogRow has `onClick stopPropagation` to prevent row toggle
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>UnitDetailSheet shows conditional "View Datasheet" link; BattleLogRow army list name is a clickable Link</done>
</task>

<task type="auto">
  <name>Task 2: Add bidirectional cross-links between Rules Hub and Unit Database PageHeaders</name>
  <files>src/app/rules-hub/page.tsx, src/app/unit-database/page.tsx</files>
  <read_first>
    - src/app/rules-hub/page.tsx (find PageHeader component and its actions prop)
    - src/app/unit-database/page.tsx (find PageHeader component and its actions prop)
  </read_first>
  <action>
Per D-05 — Add cross-link buttons in PageHeader `actions` prop of each page:

In the Rules Hub page component: add a ghost Button with `asChild` wrapping a `<Link to="/unit-database">` containing `ArrowRight` icon and "Browse Units" text. Use `variant="ghost" size="sm"`. Place it in the PageHeader `actions` area (alongside any existing action buttons).

In the Unit Database page component: add a ghost Button with `asChild` wrapping a `<Link to="/rules-hub">` containing `ArrowRight` icon and "View Rules" text. Same variant/size pattern.

Import `ArrowRight` from `lucide-react`, `Link` from `@tanstack/react-router`, and `Button` from `@/components/ui/button` in each file if not already imported.
  </action>
  <acceptance_criteria>
    - Rules Hub PageHeader contains a "Browse Units" ghost button linking to `/unit-database`
    - Unit Database PageHeader contains a "View Rules" ghost button linking to `/rules-hub`
    - Both buttons use `variant="ghost" size="sm"` with ArrowRight icon
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Rules Hub and Unit Database have bidirectional cross-links in their headers</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | All links are internal static routes with no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-03 | Spoofing | Cross-links | accept | All links are hardcoded internal routes; no user-supplied URLs |
</threat_model>

<verification>
- `pnpm build` passes
- UnitDetailSheet shows "View Datasheet" for linked units, hidden for unlinked
- Rules Hub has "Browse Units" link; Unit Database has "View Rules" link
- Battle Log row army list name is clickable and navigates to army list detail
</verification>

<success_criteria>
Three cross-link patterns are implemented: conditional unit datasheet link, bidirectional Rules/Database links, and battle log to army list links.
</success_criteria>

<output>
Create `.planning/phases/129-navigation-cross-links-technical-cleanup/129-02-SUMMARY.md` when done
</output>

---
phase: 129-navigation-cross-links-technical-cleanup
plan: 04
type: execute
wave: 2
depends_on: [129-01]
files_modified:
  - src/app/router.tsx
  - src/components/common/AppSidebar.tsx
autonomous: true
requirements: [NAV-04, NAV-05, NAV-10]

must_haves:
  truths:
    - "Game Day sidebar item is highlighted when on /game-day/* routes"
    - "Collapsed sidebar shows thin dividers between nav groups"
    - "Sidebar collapse/expand has smooth CSS transition without content snapping"
  artifacts:
    - path: "src/components/common/AppSidebar.tsx"
      provides: "Game Day in PLAY_NAV, collapsed dividers between groups"
      contains: "Game Day"
    - path: "src/app/router.tsx"
      provides: "Game Day index route for sidebar navigation"
      contains: "gameDayIndexRoute"
  key_links:
    - from: "src/components/common/AppSidebar.tsx"
      to: "src/components/common/NavItem.tsx"
      via: "PLAY_NAV Game Day entry with to=/game-day"
      pattern: "game-day"
---

<objective>
Add Game Day to the sidebar Play group with proper active-state highlighting, add collapsed-mode dividers between nav groups, and ensure the sidebar collapse transition is smooth.

Purpose: Per D-06/D-07/D-12, Game Day should be discoverable from the sidebar, collapsed mode should have visual group separation, and the collapse animation should be polished.
Output: Updated sidebar with Game Day entry and collapsed dividers; smooth collapse transition.
</objective>

<execution_context>
@C:/Users/antoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/antoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-navigation-cross-links-technical-cleanup/129-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Game Day to PLAY_NAV with index route, and add collapsed sidebar dividers</name>
  <files>src/app/router.tsx, src/components/common/AppSidebar.tsx</files>
  <read_first>
    - src/app/router.tsx (gameDayRoute at line 181-184, routeTree at line 213-234 — need to add an index route)
    - src/components/common/AppSidebar.tsx (PLAY_NAV array at line 49-54, nav group rendering at lines 152-219)
    - src/components/common/NavItem.tsx (active matching logic — startsWith pattern)
  </read_first>
  <action>
Per D-06 — Game Day sidebar entry:

In `router.tsx`: Add a `gameDayIndexRoute` at path `/game-day` (no `$listId` param) as a child of `layoutRoute`. This route renders a simple component that shows "Select an army list to start Game Day" with a Link to `/army-lists`. Use a minimal inline component or a lazy-loaded page — Claude has discretion. Add `gameDayIndexRoute` to the `layoutRoute.addChildren([...])` array alongside the existing `gameDayRoute`.

In `AppSidebar.tsx`: Add `{ to: "/game-day", label: "Game Day", icon: Sword }` to the `PLAY_NAV` array. Place it after "Battle Log" and before "Rules Hub" (logical grouping: lists -> battles -> game day -> rules). The `Sword` icon is already imported. NavItem's `pathname.startsWith("/game-day")` matching will highlight it for both `/game-day` (index) and `/game-day/123` (detail).

Per D-07 — Collapsed sidebar dividers:

In the `<nav>` section of `AppSidebar.tsx`, between each pair of nav group `<ul>` elements, add a collapsed-only divider: `{collapsed && <div className="my-1 border-b border-border/40" />}`. There are 3 boundaries: Command|Workshop, Workshop|Play, Play|Management. In expanded mode, the `<p>` group labels already serve as separators so the dividers should only render when `collapsed` is true.

Per D-12 — Sidebar collapse transition smoothness:

Verify the `<aside>` already has `transition-[width] duration-200 ease-in-out`. If content (text labels, icons) snaps during collapse, add `overflow-hidden` to the `<aside>` to prevent text wrapping during width transition. Consider adding `transition-opacity duration-150` and `opacity-0` when collapsed to the group label `<p>` elements and the nav item label text for a fade effect. Claude has discretion on the exact CSS approach — the goal is no visual snapping.
  </action>
  <acceptance_criteria>
    - PLAY_NAV array in AppSidebar.tsx contains a Game Day entry with `to: "/game-day"` and `icon: Sword`
    - `router.tsx` has a `/game-day` index route (separate from `/game-day/$listId`) in the routeTree
    - Three collapsed-only dividers exist between nav groups (Command|Workshop, Workshop|Play, Play|Management)
    - Dividers use `border-b border-border/40` and are gated on `collapsed` being true
    - Dividers do NOT render in expanded mode
    - `<aside>` has `overflow-hidden` (or equivalent) to prevent text wrap during collapse transition
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Game Day is in sidebar Play group with active highlighting; collapsed dividers separate groups; collapse animation is smooth</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Sidebar rendering and route additions — no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-05 | Spoofing | gameDayIndexRoute | accept | Static route with no dynamic params; renders informational content only |
</threat_model>

<verification>
- `pnpm build` passes
- Game Day sidebar item highlights when navigating to /game-day/123
- Collapsed sidebar shows thin dividers between groups
- Sidebar collapse/expand animates smoothly without text snap
</verification>

<success_criteria>
Game Day is visible and highlighted in the sidebar; collapsed mode has visual group separators; collapse animation is polished.
</success_criteria>

<output>
Create `.planning/phases/129-navigation-cross-links-technical-cleanup/129-04-SUMMARY.md` when done
</output>
